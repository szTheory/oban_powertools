# Handoff: oban_powertools Phase 1 (Worker Ergonomics & Idempotency)

## Current Status
- **Phase:** 1 (Worker Ergonomics & Idempotency)
- **Status:** In Progress (Implementing Task 3: Idempotency Logic & Enqueue API)
- **Last Action:** Modified `lib/oban_powertools/idempotency.ex` to use `on_conflict` update for reliable receipt retrieval.

## Completed in this session
- [x] Phase 1 Planning: Created `.planning/phases/1-PLAN.md`.
- [x] Task 1: Idempotency Receipts Migration. Updated `lib/mix/tasks/oban_powertools.install.ex`.
- [x] Task 2: Worker Macro & Args Schema. Implemented `lib/oban_powertools/worker.ex` with typed args and synchronous validation.
- [x] Task 3: Initial implementation of `ObanPowertools.Idempotency`.
- [x] Test Setup: Configured `ObanPowertools.TestRepo`, `test_helper.exs`, and migrations for testing with a real Postgres database.

## Remaining Work
- [ ] Task 3: Fix `ObanPowertools.IdempotencyTest` failures.
  - Fix `Protocol.UndefinedError` for `Jason.Encoder` in `ObanPowertools.IdempotencyTest.MockWorker.Args`.
  - Fix `Ecto.StaleEntryError` during duplicate enqueue (idempotency conflict).
  - Fix `assert job.args == %{"id" => 123}` failure (currently returns atom keys `%{id: 123}`).
- [ ] Verify Phase 1: Run all tests and ensure compliance with success criteria.

## Important Decisions
- **Enqueue API:** `MyWorker.enqueue(args)` is the primary entry point, performing synchronous validation and idempotent insertion.
- **Worker Macro:** Generates a nested `Args` module with an Ecto embedded schema for validation.
- **Idempotency Receipts:** Uses a separate table `oban_powertools_idempotency_receipts` with a unique index on `{worker, fingerprint}`.

## Blockers
- None.

## Resume Instructions
1. Run `MIX_ENV=test mix test test/oban_powertools/idempotency_test.exs` to see current failures.
2. Resolve `Jason.Encoder` consolidation issue in tests (Args struct needs to implement the protocol).
3. Fix the `on_conflict` logic in `ObanPowertools.Idempotency.do_enqueue/5` to correctly handle duplicate inserts without stale struct errors.
4. Update `ObanPowertools.IdempotencyTest` to expect atom keys in `job.args` if that's the desired behavior (or ensure JSON serialization in tests matches DB behavior).
