---
milestone: v1.6
audited: 2026-05-29T23:30:00Z
status: gaps_found
scores:
  requirements: 8/13      # 8 satisfied in-repo, +1 partial (REL-03 changelog hygiene), +4 unsatisfied (phases 50/51 unbuilt)
  phases: 3/5             # 47/48/49 built; 50/51 not started
  integration: clean-runtime-with-delivery-blockers   # 47↔48↔49 runtime wiring sound; doctor+limiter absent from published 0.5.0
  flows: 2/3              # doctor + limiter flows work in-repo; published-package E2E (REL-04) not built
gaps:
  requirements:
    - id: "REL-04"
      status: "unsatisfied"
      phase: "Phase 51"
      claimed_by_plans: []
      completed_by_plans: []
      verification_status: "missing"
      evidence: "Phase 51 (Published-Package Verification) not started — no directory. No fresh-from-hex install verification exists. This is the safety net that would have caught the doctor+limiter post-tag drift (see integration I-01)."
    - id: "TEL-01"
      status: "unsatisfied"
      phase: "Phase 50"
      claimed_by_plans: []
      completed_by_plans: []
      verification_status: "missing"
      evidence: "Phase 50 (Telemetry Metrics & SLO Guide) not started. ObanPowertools.Telemetry.metrics/0 not built."
    - id: "TEL-02"
      status: "unsatisfied"
      phase: "Phase 50"
      claimed_by_plans: []
      completed_by_plans: []
      verification_status: "missing"
      evidence: "Phase 50 not started. telemetry_metrics/telemetry_poller optional-dep gating not implemented."
    - id: "TEL-03"
      status: "unsatisfied"
      phase: "Phase 50"
      claimed_by_plans: []
      completed_by_plans: []
      verification_status: "missing"
      evidence: "Phase 50 not started. Parapet/SLO telemetry guide not written."
    - id: "REL-03"
      status: "partial"
      phase: "Phase 47"
      claimed_by_plans: ["47-01-PLAN.md"]
      completed_by_plans: ["47-01-SUMMARY.md"]
      verification_status: "validated"   # 47-VALIDATION.md (nyquist_compliant); no goal-backward 47-VERIFICATION.md
      evidence: "CHANGELOG.md is valid Keep-a-Changelog with a [0.5.0] 2026-05-29 entry and a Path-to-1.0 section — the requirement as written is met. BUT [Unreleased] still holds only the stale comment '<!-- Phases 48-51 accumulate entries here -->'. Phase 48 (doctor) AND Phase 49 (limiter explain/simulate) are built and committed but logged nowhere. The next release-please bump will generate release notes that omit both shipped footguns. Must populate [Unreleased] before the next cut."
  integration:
    - finding: "Published hex.pm 0.5.0 package contains NEITHER the doctor task (Phase 48) NOR the limiter CLI tasks (Phase 49). Independently verified: `git ls-tree -r v0.5.0` shows the only mix task in the tag is `oban_powertools.install.ex`; doctor.ex (first commit 28407a5) and the limiter tasks landed AFTER the v0.5.0 tag. HEAD is 60 commits past the tag."
      severity: "blocker"
      affects: ["OPS-03", "OPS-04", "OPS-05", "OPS-06", "OPS-07", "OPS-08"]
      fix: "Cut a 0.5.1/0.6.0 release (release-please pipeline is in place) that includes the doctor + limiter CLI source so adopters actually receive both shipped footguns. Both work + are verified in-repo, but are unavailable from hex today."
    - finding: "CHANGELOG [Unreleased] is empty except a stale wrong comment ('Phases 48-51 accumulate entries here'). Doctor AND limiter CLI omitted."
      severity: "blocker"
      affects: ["REL-03", "OPS-03", "OPS-06"]
      fix: "Populate [Unreleased] ### Added with doctor + limiter explain/simulate entries; delete the stale comment."
    - finding: "README.md has zero mentions of `mix oban_powertools.doctor`, `limiter.explain`, or `limiter.simulate` — the milestone's two headline footguns are invisible to adopters."
      severity: "blocker"
      affects: ["OPS-03", "OPS-06"]
      fix: "Add a Health Check section (doctor: exit codes, --format json, --strict) and a Limiter CLI section (explain/simulate) to README."
    - finding: "Doctor appears in NO ExDoc guide and NO Operations extras entry. Limiter CLI tasks are not cross-referenced from guides/limits-and-explain.md (which covers only the Elixir Explain/Limits API + glossary)."
      severity: "blocker"
      affects: ["REL-02", "OPS-03", "OPS-08"]
      fix: "Add guides/doctor.md (or a section in production-hardening.md) wired into the Operations extras group; add a 'CLI Tools' section to guides/limits-and-explain.md cross-linking the two limiter tasks."
    - finding: "explain_snapshot/2 (lib/oban_powertools/explain.ex:100-105, pre-existing v1.3 code reused by Phase 49) calls live_blockers_from_snapshot/3 twice — once for blockers:, once for live_now: — a duplicate DB round-trip returning identical data."
      severity: "warning"
      affects: ["OPS-06"]
      fix: "Capture the result once: `live = live_blockers_from_snapshot(repo, snapshot, now); %{blockers: live, live_now: live, ...}`. `now` is already captured once so divergence is near-impossible; the issue is the wasted round-trip + smell."
    - finding: "Doctor migration-drift check depends on Oban.Migrations.Postgres.current_version/0, an `@moduledoc false`/`@doc false` Oban internal with no public API-stability guarantee (checks.ex:161)."
      severity: "warning"
      affects: ["OPS-04"]
      fix: "Prefer the public Oban.Migration.current_version/1 delegation, or pin a comment that this internal is tested against the Oban version range in mix.exs."
    - finding: "Output-channel divergence between Phase 48 and 49: Doctor Formatter uses IO.puts; limiter.explain uses Mix.shell().info for human + IO.puts for JSON; limiter.simulate uses Mix.shell().info throughout. D-01 specified Phase 49 mirror Doctor conventions."
      severity: "info"
      affects: ["OPS-05", "OPS-06", "OPS-07"]
      fix: "Route all task output through Mix.shell().info consistently; have Formatter.print/2 return the string and let the task emit it. (Doctor is still tested via CaptureIO + host-contract subprocess, so this is convention hygiene, not a test-coverage gap.)"
    - finding: "Stale oban_powertools-0.5.0.tar (built 10:03, pre-doctor/pre-limiter) sits in repo root, not gitignored — not the published artifact."
      severity: "warning"
      affects: ["REL-01"]
      fix: "Delete it; add oban_powertools-*.tar to .gitignore. The published tarball is rebuilt fresh by CI at the tag."
  flows:
    - flow: "hex publish (cut 0.5.0 -> tag v0.5.0 -> publish-hex -> live on hex.pm)"
      status: "complete"
      breaks_at: "none — package live on hex.pm 0.5.0; release-please pipeline (config + manifest@0.5.0 + release.yml/publish-hex.yml) in place"
    - flow: "operator runs doctor / limiter explain / limiter simulate (in-repo)"
      status: "complete"
      breaks_at: "none in-repo (both phases verified, CI lanes green); but unavailable from published 0.5.0 (see integration I-01)"
    - flow: "fresh adopter installs 0.5.0 from hex -> first operator session with the footguns (REL-04)"
      status: "not-built"
      breaks_at: "Phase 51 does not exist; and would surface that doctor + limiter are missing from the published package"
nyquist:
  compliant_phases: ["47", "48", "49"]
  partial_phases: []
  missing_phases: []      # 50/51 not built — no phase to validate
  overall: "compliant"    # all three built phases have nyquist_compliant VALIDATION.md
tech_debt:
  - phase: 47-hex-release-foundation
    items:
      - "No 47-VERIFICATION.md — phase has a nyquist-compliant 47-VALIDATION.md (35 green release-contract tests) + external proof (live on hex.pm), but never got a goal-backward verification pass. Process gap, not a deliverable gap."
      - "CHANGELOG [Unreleased] holds a stale, factually-wrong comment ('Phases 48-51 accumulate here') — Phases 48 AND 49 are built and unlogged."
      - "README + ExDoc guides never mention doctor or the limiter CLI (discoverability)."
      - "Stale oban_powertools-0.5.0.tar in repo root; not gitignored."
      - "ROADMAP shows 47-03-PLAN.md as [ ] but 47-03-SUMMARY.md exists and the release-please pipeline shipped — stale checkbox."
  - phase: 48-doctor-health-check-task
    items:
      - "Doctor source landed AFTER the v0.5.0 tag — verified + CI-gated in-repo but NOT shipped in published 0.5.0; needs a follow-up release."
      - "Migration-drift check leans on Oban.Migrations.Postgres.current_version/0, an undocumented Oban internal (fragility on Oban minor bumps)."
      - "oban_db_version queried twice per run (Checks.oban_migration_version + the Mix task re-querying for Formatter) — duplicate round-trip."
      - "Info-level review carryovers (accepted non-blocking): valid_identifier? rejects uppercase prefix (WR-02); states_list compile-time interpolation (WR-04); exit_code_for catch-all clause (WR-03)."
  - phase: 49-limiter-explain-simulate-cli
    items:
      - "Limiter CLI source landed AFTER the v0.5.0 tag — verified + CI-gated in-repo but NOT shipped in published 0.5.0; rides the same follow-up release as doctor."
      - "Glossary parity is enforced at term level (glossary_test.exs + docs_contract markers), not by compile-time single-sourcing — sub-term-level text can drift across the two moduledocs + guide without test failure."
      - "explain_snapshot/2 duplicate round-trip (pre-existing v1.3 explain.ex reused here)."
      - "limiter.simulate resolve_worker/1 skips Code.ensure_loaded? — a typo'd --worker yields a misleading 'no :limits configured' error instead of 'unknown module' (explain.ex does this correctly)."
      - "Deferred cosmetic: explain.ex:325 `b[:retry_at] || b[:retry_at]` self-OR (IN-01)."
---

# Milestone v1.6 — Release & Operability — Audit Report

**Audited:** 2026-05-29 (re-audit; supersedes the 22:00 run, which predated Phase 49)
**Status:** `gaps_found`
**Verdict:** Milestone is **mid-flight, not complete.** Real progress since the prior audit — **Phase 49 (Limiter CLI) is now built and verified 18/18**, lifting satisfied requirements from 5/13 to 8/13. The package is **live on hex.pm at 0.5.0**. But **2 of 5 phases remain unbuilt** (50 Telemetry, 51 Published-Package Verification → 4 unsatisfied requirements), and a release-sequencing drift means **neither shipped footgun (doctor nor limiter CLI) is in the published package.** Do **not** archive.

---

## What Changed Since the Prior Audit (22:00)

| Item | Prior audit | Now |
|------|-------------|-----|
| Phase 49 (Limiter CLI) | "not built" | ✅ **built + verified 18/18** + VALIDATION (nyquist) + SECURITY |
| OPS-06 / OPS-07 / OPS-08 | unsatisfied | ✅ **satisfied** in-repo (verified; CI lanes green) |
| Satisfied requirements | 5/13 | **8/13** (+1 partial) |
| Phases built | 2/5 | **3/5** |
| Published-package drift | doctor only | now **doctor AND limiter CLI** absent from 0.5.0 |

Status stays `gaps_found` — the 4 unbuilt-phase requirements (REL-04, TEL-01/02/03) still trip the FAIL gate.

---

## Scope

| | |
|---|---|
| Milestone | v1.6 Release & Operability (Phases 47-51) |
| Planned phases | 5 (47, 48, 49, 50, 51) |
| Phases built | 3 (47, 48, 49) |
| Phases not started | 2 (50 Telemetry, 51 Published-Package Verification) |
| Requirements | 13 total |

**Definition of done (ROADMAP):** Publish to hex.pm and ship the two named operability footguns (doctor + limiter CLI) plus the telemetry surface, with a fresh-from-hex install verified. Zero new runtime dependencies. *"The release IS the milestone."*

---

## Requirements Coverage (3-Source Cross-Reference)

Sources: **V** = phase VERIFICATION.md · **S** = SUMMARY frontmatter · **R** = REQUIREMENTS.md traceability checkbox
*(SUMMARY `requirements_completed` frontmatter is empty across all phases in this project — VERIFICATION.md is the authoritative source.)*

| REQ | Phase | V | S | R | Final | Note |
|-----|-------|---|---|---|-------|------|
| REL-01 | 47 | n/a (VALIDATION) | — | `[x]` | **satisfied** | Package live on hex.pm 0.5.0; `:files` whitelist tested (35 green) |
| REL-02 | 47 | n/a (VALIDATION) | — | `[x]` | **satisfied** | hexdocs renders; `source_ref v0.5.0` pinned |
| REL-03 | 47 | n/a (VALIDATION) | — | `[x]` | **partial** | CHANGELOG valid + Path-to-1.0, but `[Unreleased]` stale/omits doctor + limiter |
| REL-04 | 51 | missing | — | `[ ]` | **unsatisfied** | Phase 51 not built |
| OPS-03 | 48 | passed | — | `[x]` | **satisfied*** | Verified 13/13 in-repo; *not in published 0.5.0 |
| OPS-04 | 48 | passed | — | `[x]` | **satisfied*** | Verified in-repo; *not in published 0.5.0 |
| OPS-05 | 48 | passed | — | `[x]` | **satisfied*** | Verified in-repo; *not in published 0.5.0 |
| OPS-06 | 49 | passed | — | `[x]` | **satisfied*** | Verified 18/18 in-repo; *not in published 0.5.0 |
| OPS-07 | 49 | passed | — | `[x]` | **satisfied*** | Verified in-repo; *not in published 0.5.0 |
| OPS-08 | 49 | passed | — | `[x]` | **satisfied*** | Glossary term-parity test-locked; *not in published 0.5.0 |
| TEL-01 | 50 | missing | — | `[ ]` | **unsatisfied** | Phase 50 not built |
| TEL-02 | 50 | missing | — | `[ ]` | **unsatisfied** | Phase 50 not built |
| TEL-03 | 50 | missing | — | `[ ]` | **unsatisfied** | Phase 50 not built |

**Tally:** 8 satisfied · 1 partial · 4 unsatisfied → **8/13 fully satisfied**

> **FAIL gate triggered:** 4 unsatisfied requirements force `gaps_found`.
> `*` OPS-03 through OPS-08 are satisfied at the **code + in-repo verification** level (Phase 48 passed 13/13, Phase 49 passed 18/18, both Nyquist-compliant, CI lanes green) but **neither feature is in the published 0.5.0 package** — both landed after the tag. The behavior is real and verified; *delivery* to adopters is pending a follow-up release.

---

## Phase Status

| Phase | Built | Verified | Nyquist | Notes |
|-------|-------|----------|---------|-------|
| 47 Hex Release Foundation | ✅ (3/3 plans) | ⚠ no goal-backward VERIFICATION.md | ✅ compliant | Published live; doc-discoverability gaps; stale ROADMAP checkbox on 47-03 |
| 48 Doctor Health-Check Task | ✅ (2/2 plans) | ✅ passed 13/13 | ✅ compliant | Clean in-repo; **not in published 0.5.0** (landed post-tag) |
| 49 Limiter Explain/Simulate CLI | ✅ (3/3 plans) | ✅ passed 18/18 | ✅ compliant | Clean in-repo; **not in published 0.5.0** (landed post-tag) |
| 50 Telemetry Metrics & SLO Guide | ❌ not started | — | — | No directory |
| 51 Published-Package Verification | ❌ not started | — | — | No directory |

**ROADMAP discrepancy:** ROADMAP shows `47-03-PLAN.md` as `[ ]`, but `47-03-SUMMARY.md` exists and the release-please pipeline shipped — the checkbox is stale.

---

## Cross-Phase Integration

Integration checker scoped to the three built phases (47 ↔ 48 ↔ 49).

**Runtime wiring — sound:**
- ✅ Doctor chain intact: `Doctor.run/2` → `Checks.*` → `Formatter.print/2` → `exit_code_for/1` → `System.halt`.
- ✅ Limiter CLI reuses existing seams: `compute_reservation/4` (pure), `Explain.explain_snapshot/2`, `Explain.explain/3`; no duplicated limiter logic (OPS-06 reuse contract met).
- ✅ Glossary parity test-enforced across both task moduledocs + guide (OPS-08).
- ✅ `{:jason, "~> 1.4"}` is a required (non-optional) dep — `--format json` won't crash adopters.
- ✅ All three Mix tasks boot via `Ecto.Migrator.with_repo/2`, resolve modules via `Module.safe_concat`, return honest integer exit codes; `source_ref: "v0.5.0"` pinned; `igniter` `runtime: false`.

**Blockers (delivery + discoverability of already-working, already-verified features):**
- ⛔ **I-01 / OPS-03…08** — Doctor AND limiter CLI source committed **after** the `v0.5.0` tag → **absent from the published package.** Adopters on `~> 0.5` cannot run either footgun. *(Verified: `git ls-tree v0.5.0` has only `oban_powertools.install.ex` as a mix task.)*
- ⛔ **B-01 / REL-03** — Doctor + limiter absent from CHANGELOG; `[Unreleased]` holds only a stale wrong comment.
- ⛔ **B-02** — Doctor + limiter absent from README.
- ⛔ **B-03 / REL-02** — Doctor absent from all ExDoc guides + Operations extras; limiter CLI not cross-referenced from `limits-and-explain.md`.

**Warnings / info (code quality, in-repo):**
- ⚠ **W-01 / OPS-06** — `explain_snapshot/2` double round-trip (`blockers:`/`live_now:` call the same query twice; pre-existing v1.3 code).
- ⚠ **W-02 / OPS-04** — Doctor leans on the undocumented `Oban.Migrations.Postgres.current_version/0` internal.
- ⚠ **W-03 / OPS-07** — `limiter.simulate` skips `Code.ensure_loaded?` → misleading error on a typo'd `--worker`.
- ⚠ **W-04 / REL-01** — Stale `oban_powertools-0.5.0.tar` in repo root, not gitignored.
- ℹ **I-02 / OPS-05/06/07** — `IO.puts` vs `Mix.shell().info` output-channel divergence between phases 48 and 49 (D-01 convention drift; doctor still tested via CaptureIO + host-contract).

> The four "blockers" are about adopter **delivery + discoverability** of already-working, already-verified features — not in-repo runtime breaks. They resolve together in the next release commit + a doc pass. The warnings are accepted-or-deferred code-quality items.

---

## Nyquist Coverage

| Phase | VALIDATION.md | Compliant | Action |
|-------|---------------|-----------|--------|
| 47 | ✅ exists | true ✓ | none (35 green release-contract tests) |
| 48 | ✅ exists | true ✓ | none |
| 49 | ✅ exists | true ✓ | none |
| 50/51 | n/a (not built) | — | build the phases first |

Overall: **compliant** (all three built phases).

---

## Working Tree

Clean for tracked source. Untracked: `package.json`, `package-lock.json` (npm cruft) and `oban_powertools-0.5.0.tar` (stale build artifact — W-04). All Phase 47/48/49 source is committed — satisfies the v1.6 clean-tree convention.

---

## Bottom Line

The milestone is **60% built by phase (3/5)** and **62% complete by satisfied requirement (8/13)**. The release machinery worked — **0.5.0 is live on hex.pm with rendering hexdocs** (REL-01/02 done) — and both operability footguns are now built and verified in-repo (Phase 48: 13/13, Phase 49: 18/18, both Nyquist-compliant).

Three honest gaps block completion:
1. **Phases 50 (Telemetry) and 51 (Published-Package Verification) are unbuilt** — 4 unsatisfied requirements (TEL-01/02/03, REL-04).
2. **Neither footgun is in the published 0.5.0** — both landed after the tag. They work in-repo; adopters don't have them yet. Needs a follow-up release.
3. **Discoverability** — CHANGELOG `[Unreleased]` is stale, README and ExDoc guides don't mention doctor or the limiter CLI. Phase 51 (the safety net that would have caught #2 and #3) does not exist.

This is a milestone to **keep building**, not to archive.
