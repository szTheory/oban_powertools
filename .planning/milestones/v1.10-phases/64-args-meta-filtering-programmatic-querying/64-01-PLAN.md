---
phase: 64-args-meta-filtering-programmatic-querying
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/oban_powertools/jobs.ex
  - lib/oban_powertools/operator.ex
  - test/oban_powertools/jobs_test.exs
  - test/oban_powertools/operator_test.exs
autonomous: true
requirements:
  - QRY-05
  - API-03
must_haves:
  truths:
    - API consumer can call `Operator.list/2` with args/meta filters and receive matching jobs.
  artifacts:
    - path: lib/oban_powertools/jobs.ex
      provides: JSONB containment query logic for args and meta
    - path: lib/oban_powertools/operator.ex
      provides: Programmatic list API
  key_links:
    - from: lib/oban_powertools/operator.ex
      to: lib/oban_powertools/jobs.ex
      via: list/3
---

<objective>
Update the `Jobs` domain model to support `args` and `meta` JSONB filtering and expose the `Operator.list/2` programmatic API.

Purpose: Allow host applications to securely query jobs by specific JSON subset matches (API-03).
Output: Updated `Jobs` struct, `Jobs.list/3`, `Jobs.count_by_state/2`, and `Operator.list/2` with test coverage.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@lib/oban_powertools/jobs.ex
@lib/oban_powertools/operator.ex
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add args and meta filtering to Jobs domain</name>
  <files>lib/oban_powertools/jobs.ex, test/oban_powertools/jobs_test.exs</files>
  <behavior>
    - Jobs.list/3 filters jobs containing the provided `args` map using Postgres `@>`.
    - Jobs.list/3 filters jobs containing the provided `meta` map using Postgres `@>`.
    - count_by_state/2 properly respects `args` and `meta` filters.
  </behavior>
  <action>
    Update `%ObanPowertools.Jobs{}` struct to add `args: map() | nil` and `meta: map() | nil`.
    Update `list/3` and `count_by_state/2` to pipe through `maybe_filter_args` and `maybe_filter_meta`.
    Implement `maybe_filter_args` using `fragment("? @> ?", j.args, type(^args, :map))`.
    Implement `maybe_filter_meta` using `fragment("? @> ?", j.meta, type(^meta, :map))`.
    Update the module doc to mention that the host application may create `CREATE INDEX CONCURRENTLY oban_jobs_args_gin ON oban_jobs USING gin(args);` (and similarly for meta).
  </action>
  <verify>
    <automated>mix test test/oban_powertools/jobs_test.exs</automated>
  </verify>
  <done>Queries generated include JSONB containment for args and meta when present.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Expose Operator.list programmatic API</name>
  <files>lib/oban_powertools/operator.ex, test/oban_powertools/operator_test.exs</files>
  <behavior>
    - Operator.list(repo, filters, opts \\ []) delegates to Jobs.list/3.
    - Acccepts map or keyword list for `filters`, converting them to `%ObanPowertools.Jobs{}`.
  </behavior>
  <action>
    Add `def list(repo, filters \\ %{}, opts \\ [])` to `Operator`.
    If `filters` is a map or keyword list, cast it to `%ObanPowertools.Jobs{}` and call `Jobs.list(repo, filter, opts)`.
    If `filters` is already a `%Jobs{}` struct, use it directly.
    Add basic integration tests in `operator_test.exs` verifying filtering capabilities.
  </action>
  <verify>
    <automated>mix test test/oban_powertools/operator_test.exs</automated>
  </verify>
  <done>Operator API exists and correctly proxies to the Jobs read model.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Caller→Jobs.list | Caller-provided maps are cast to JSON parameters |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-64-01 | Tampering | Jobs.list query | mitigate | Rely on Ecto's `type(^val, :map)` parameterization to prevent SQL injection in `fragment` macros. |
| T-64-02 | Denial of Service | Jobs.list query | accept | Sequential scanning on JSONB fields without GIN indexes is mitigated by Oban's mandatory `state` leading composite index bounding the scan. Documented need for host GIN index. |
</threat_model>

<verification>
`mix test` passes for the modified files.
</verification>

<success_criteria>
`Operator.list/2` is available and cleanly applies JSONB containment filtering.
</success_criteria>

<output>
Create `.planning/phases/64-args-meta-filtering-programmatic-querying/64-01-SUMMARY.md` when done
</output>
