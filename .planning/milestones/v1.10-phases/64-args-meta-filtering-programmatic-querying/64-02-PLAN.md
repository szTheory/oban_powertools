---
phase: 64-args-meta-filtering-programmatic-querying
plan: 02
type: execute
wave: 2
depends_on: ["64-01"]
files_modified:
  - lib/oban_powertools/web/jobs_live.ex
  - test/oban_powertools/web/jobs_live_test.exs
autonomous: true
requirements:
  - QRY-05
must_haves:
  truths:
    - User can filter the native job list UI by specific arguments and metadata keys/values.
    - UI URL updates to reflect the args/meta filters so they can be bookmarked and shared.
  artifacts:
    - path: lib/oban_powertools/web/jobs_live.ex
      provides: Form handling for JSON filtering with error validation
  key_links:
    - from: lib/oban_powertools/web/jobs_live.ex
      to: URL query parameters
      via: URL serialization
---

<objective>
Enhance the Jobs LiveView to support filtering by `args` and `meta` JSON fragments via native text inputs.

Purpose: Allow operators to precisely pinpoint jobs via partial JSON matches directly from the native interface (QRY-05), and share those queries via URL.
Output: Updated `jobs_live.ex` template and event handlers with validation feedback for invalid JSON.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@lib/oban_powertools/web/jobs_live.ex
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add args and meta state tracking to JobsLive</name>
  <files>lib/oban_powertools/web/jobs_live.ex</files>
  <action>
    In `assign_defaults/1` and `handle_params/3`, track the raw input string and validation error state independently of the `%Jobs{}` filter model.
    Add: `@args_input` (default `""`), `@args_error` (default `nil`), `@meta_input` (default `""`), `@meta_error` (default `nil`).
    When parsing `params` in `handle_params/3`, initialize the inputs with the URL's query strings or `""`.
    Update `filter_from_params/1` to decode the JSON strings using `Jason.decode` for `args` and `meta`, defaulting to `nil` on error or empty string.
  </action>
  <verify>
    <automated>grep -c args_input lib/oban_powertools/web/jobs_live.ex | grep -v '^0$'</automated>
  </verify>
  <done>Assigns correctly manage form lifecycle independent of filter validity.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Implement UI inputs and event handlers</name>
  <files>lib/oban_powertools/web/jobs_live.ex</files>
  <action>
    Update the `phx-change="filter"` handler to parse `args` and `meta` strings from the params using `Jason.decode`.
    If either string fails parsing (and is not empty), set `@args_error` or `@meta_error`, update the input assigns, and DO NOT `push_patch` (which halts filter application until fixed).
    If they parse successfully, clear errors, update the `%Jobs{}` filter, and `push_patch`.
    Update `filter_path/1` to `Jason.encode!` the `args` and `meta` maps back into the URL (if present).
    Update `back_path_from_session/1` to serialize `args` and `meta` as well.
    In the template, add two new `<input type="text">` fields for Args and Meta, binding `value={@args_input}` and `value={@meta_input}`.
    Apply `phx-debounce="blur"` to these two JSON inputs so they evaluate on blur rather than every keystroke, preventing premature validation errors.
    Conditionally add a red border and an error message `<p>` tag below the inputs if `@args_error` or `@meta_error` are present.
  </action>
  <verify>
    <automated>mix test test/oban_powertools/web/jobs_live_test.exs</automated>
  </verify>
  <done>UI correctly filters jobs by valid JSON and halts application on invalid JSON with visual feedback.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Update JobsLive test suite</name>
  <files>test/oban_powertools/web/jobs_live_test.exs</files>
  <behavior>
    - Typing valid JSON in Args/Meta inputs applies the filter and updates the URL.
    - Typing invalid JSON in Args/Meta inputs shows an error and does not update the URL.
  </behavior>
  <action>
    Add integration tests simulating a user typing valid and invalid JSON into the new `args` and `meta` filter fields.
    Assert that the URL is updated with URL-encoded JSON parameters when valid.
    Assert that an error message (e.g. "Invalid JSON") is rendered and the URL is not changed when invalid.
  </action>
  <verify>
    <automated>mix test test/oban_powertools/web/jobs_live_test.exs</automated>
  </verify>
  <done>Test suite covers both success and validation error paths for JSON inputs.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User→Browser | User inputs raw JSON strings into form fields |
| Browser→LiveView | WebSocket submits raw JSON strings |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-64-03 | Tampering | filter param decoding | mitigate | JSON parsing errors are caught cleanly via `Jason.decode`; invalid input never reaches Ecto. |
| T-64-04 | Denial of Service | JSON parsing | accept | Standard VM payload limits protect against extreme JSON size exhaustion at the LiveView input boundary. |
</threat_model>

<verification>
Start host server, navigate to `/ops/jobs/jobs`, type valid and invalid JSON into the new fields, confirm filtering and URL sync logic.
</verification>

<success_criteria>
Operators can seamlessly query exact arguments/metadata structures without breaking form state on typos.
</success_criteria>

<output>
Create `.planning/phases/64-args-meta-filtering-programmatic-querying/64-02-SUMMARY.md` when done
</output>
