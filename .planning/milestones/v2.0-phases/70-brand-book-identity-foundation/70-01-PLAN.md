---
phase: 70-brand-book-identity-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - guides/brand-book.md
  - mix.exs
  - .planning/phases/70-brand-book-identity-foundation/checks.sh
autonomous: true
requirements: [BRAND-01, BRAND-02, BRAND-03, BRAND-04, BRAND-05]
must_haves:
  truths:
    - "guides/brand-book.md exists and contains every decision ID D-01 through D-22"
    - "The brand essence line (D-01) appears verbatim as the brand book header line"
    - "The phrase 'explain, then act' is codified as the design north star"
    - "The canonical confirm template (D-16) appears verbatim: 'Cancel 3 running jobs? They stop now and won't retry. This can't be undone.'"
    - "A BRAND-05 traceability table maps every downstream token category to a named decision ID"
    - "guides/brand-book.md is grouped under a 'Design System' extras group in mix.exs docs config"
  artifacts:
    - path: "guides/brand-book.md"
      provides: "Versioned brand book encoding all 22 locked decisions + traceability table"
      contains: "D-22"
      min_lines: 200
    - path: ".planning/phases/70-brand-book-identity-foundation/checks.sh"
      provides: "Grep content-assertion harness for D-01..D-22 + traceability + canonical confirm template"
      contains: "D-01"
    - path: "mix.exs"
      provides: "Design System extras group including guides/brand-book.md"
      contains: "brand-book.md"
  key_links:
    - from: "mix.exs"
      to: "guides/brand-book.md"
      via: "extras/groups_for_extras list registers the guide for HexDocs"
      pattern: "brand-book\\.md"
  prohibitions:
    - "MUST NOT modify any of the 9 operator LiveView pages: lib/oban_powertools/web/{engine_overview,jobs,batches,workflows,cron,limiters,lifeline,audit,forensics}_live.ex (verified by git diff)"
    - "MUST NOT add any new runtime dependency to mix.exs deps/0"
    - "MUST NOT re-derive, relitigate, or simplify any locked decision — transcribe D-01..D-22 faithfully"
    - "MUST NOT introduce a 'v1/simplified/placeholder' brand book — all 22 decisions are in scope this plan"
---

<objective>
Author the brand book at `guides/brand-book.md` — the single, versioned, in-repo source of truth that faithfully encodes ALL 22 locked decisions (D-01..D-22) from 70-CONTEXT.md, plus a BRAND-05 traceability table mapping every downstream token category back to a named decision ID. Register the guide for HexDocs rendering and create the grep content-assertion harness.

Purpose: Every downstream phase (71 tokens → 84 audit) cites these decision IDs for traceability. The content is ALREADY DECIDED in 70-CONTEXT.md; this plan transcribes and organizes it into a coherent brand book — it does NOT re-derive or relitigate any decision.

Output: `guides/brand-book.md`, the `checks.sh` assertion harness, and the `mix.exs` extras/groups_for_extras registration.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/70-brand-book-identity-foundation/70-CONTEXT.md
@.planning/phases/70-brand-book-identity-foundation/70-RESEARCH.md
@.planning/phases/70-brand-book-identity-foundation/70-VALIDATION.md
@.planning/REQUIREMENTS.md
@mix.exs
</context>

<artifacts_this_phase_produces>
This plan (70-01) produces:
- `guides/brand-book.md` — the versioned brand book (all 22 decisions + traceability table)
- `.planning/phases/70-brand-book-identity-foundation/checks.sh` — grep content-assertion harness (Wave 0 dependency for §1 of 70-VALIDATION.md)
- `mix.exs` "Design System" extras group entry for `guides/brand-book.md`

Plan 70-02 additionally produces: `ObanPowertools.Web.Dev.BrandBookLive` module, the `/ops/jobs/_brand_book` route, the `:dev_routes` config key, the README "Brand Identity" section, and `test/oban_powertools/web/live/brand_book_live_test.exs`.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Create the grep content-assertion harness (Wave 0)</name>
  <read_first>
    - .planning/phases/70-brand-book-identity-foundation/70-VALIDATION.md (§1 Brand book Markdown content checks; §Wave 0 Requirements)
    - .planning/phases/70-brand-book-identity-foundation/70-RESEARCH.md (Validation Architecture §1)
  </read_first>
  <files>.planning/phases/70-brand-book-identity-foundation/checks.sh</files>
  <action>
    Create the content-assertion harness FIRST (Wave 0 — the brand book test scaffold must exist before the content it checks). The script asserts the presence of all required brand-book content and exits non-zero if any item is missing. It must:
    - Loop over decision IDs D-01 through D-22 and assert each appears in guides/brand-book.md.
    - Assert the BRAND-05 traceability table is present (case-insensitive match on the word Traceability).
    - Assert the explain-then-act north star is codified (case-insensitive match on the explain-then-act phrasing).
    - Assert the canonical confirm template (D-16) is present by matching its opening clause about cancelling 3 running jobs.
    - Print a clear FAIL line per missing item and exit 1 if any failed, exit 0 if all present.
    Make the script executable. Match the assertion set in 70-VALIDATION.md §1 exactly so executor verification maps to the validation contract.
    Note: at this point guides/brand-book.md does not yet exist, so a first run is EXPECTED to report failures (RED). Task 2 makes it pass (GREEN). Do not weaken the assertions to make an empty file pass.
  </action>
  <verify>
    <automated>bash .planning/phases/70-brand-book-identity-foundation/checks.sh; test $? -ne 0 && echo "EXPECTED-RED: harness fails before brand book exists"</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/70-brand-book-identity-foundation/checks.sh` exists and is executable.
    - The script contains a loop covering D-01 through D-22 (grep: `grep -q "D-01" checks.sh` and `grep -q "D-22" checks.sh` both succeed).
    - The script greps for the Traceability table, the explain-then-act phrase, and the canonical confirm template.
    - Running the script with no brand-book.md present exits non-zero (RED state confirmed before Task 2).
  </acceptance_criteria>
  <done>checks.sh exists, is executable, encodes the full 70-VALIDATION.md §1 assertion set, and fails cleanly (non-zero) while guides/brand-book.md is absent.</done>
</task>

<task type="auto">
  <name>Task 2: Author guides/brand-book.md encoding all 22 decisions + traceability table</name>
  <read_first>
    - .planning/phases/70-brand-book-identity-foundation/70-CONTEXT.md (the authoritative content — D-01..D-22 verbatim; this is the source to transcribe)
    - .planning/phases/70-brand-book-identity-foundation/70-RESEARCH.md (In-Repo Location & Versioning §version header; Recommended Task Breakdown §Task 3 chapter order)
    - .planning/REQUIREMENTS.md (BRAND-01..05 acceptance language)
    - guides/installation.md (an existing guide — match the in-repo Markdown guide tone/structure/heading style)
  </read_first>
  <files>guides/brand-book.md</files>
  <action>
    Author the versioned brand book that faithfully transcribes ALL 22 locked decisions from 70-CONTEXT.md. This is transcription and organization, NOT re-derivation — every decision is already locked; do not relitigate, simplify, or defer any of them. Cite the decision ID (D-01..D-22) inline in each section so downstream phases and the traceability audit can map shipped values to a brand-book parent.

    Open with the version header block per 70-RESEARCH.md (Version, Status: Locked (2026-06-18), Owner: Phase 70, a Version History table with the v2.0.0-draft initial-authoring row, and the note that phases 71-84 cite D-01..D-22).

    Use this chapter order (per 70-RESEARCH.md §Task 3):
    1. Identity Statement — D-01 essence VERBATIM as the brand book header line; D-02 archetype (Sage + Caregiver; NOT Hero/Magician/Outlaw/Jester); D-03 five traits (Calm, Precise, Trustworthy, Legible, Restrained) with the "if you remember one word: calm" north-star line kept prominently.
    2. What We Are Not — D-04 the three rejected anchors WITH their "why not" rationale (modern dev-tool/Linear-gradient SaaS; terminal/hacker; PostHog playful) so they are not reconsidered.
    3. Codified Brand Principles — D-05 all seven checkable rules, with principles 1-3 explicitly flagged as the high-leverage three; D-06 the single coherence test quoted VERBATIM ("Does this make a stressed operator calmer and more certain about what will happen — or is it decoration, noise, or surprise?").
    4. Color Story — D-07 accent = cool indigo-leaning blue with all three rationales (semantic collision / ownership honesty / convention); D-08 neutral base = cool slate; D-09 semantic status roles (info=blue cyan-shifted distinct from accent, success=green, warning=amber, danger=red highest-chroma reserved); D-10 two-tier token philosophy (Tier 1 primitive = only place raw hex lives; Tier 2 semantic roles; components reference Tier-2 only); D-11 light/dark/high-contrast as three distinct token sets (NOT CSS filters) with the documented pitfalls (dark surface near-black not pure black, elevation steps lighter in dark, desaturate on dark, muted ≥4.5:1 hard floor, amber text must darken, high-contrast ≥7:1 + solid borders + thicker focus); D-12 contrast targets (text ≥7:1, muted ≥4.5:1 floor, non-text ≥3:1, focus ≥2px + ≥3:1) AND colorblind safety (distinct icon silhouettes, luminance separation, success→emerald / danger→true-red). State that exact hex values are deliberately deferred to Phase 71.
    5. Typography & Density — D-13 system fonts only with the explicit Primer-style sans and mono cascades VERBATIM and the "identity lives in color/spacing/hierarchy/mono-as-signal/voice, not a typeface" statement; D-14 mono = signal not default (scoped uses, truncate-the-MIDDLE rule for IDs/hashes, numbers stay in sans with tabular-nums); D-15 type scale & density (rem ~1.2 minor-third ladder, body 14px / dense-row 13px, four weights 400/500/600, 4px spacing base space-1..7, 32-36px rows, density posture "calm but information-dense", long-content mandates: min-width:0 flex rule, visible truncation signal, 320px stacked-card fallback, 200% zoom check). State exact rem steps/spacing values are deferred to Phase 71.
    6. Voice & Microcopy — D-16 register + the canonical confirm template VERBATIM; D-17 five voice traits each with its guardrail; D-18 "explain, then act" as an enforceable copy contract (the four MUST clauses, the friction-scales-with-blast-radius ladder, the dismiss-button-never-mirrors-the-verb rule); D-19 state copy (empty/loading/error-must-say-how-to-recover/permission-denied/stale-vs-unavailable); D-20 honesty/support-truth rule (say what Powertools did, not what host infra will do); D-21 the canonical glossary as a one-term-per-concept table (Cancel ≠ Discard ≠ Delete, Retry, Pause/Resume, Job/Worker, Blocked, Preview, Repair, Reason, Audit log, Limiter; reserve "stuck" and "event log").
    7. Traceability Table — D-22: a Markdown table with columns (Downstream token category | Mandating decision ID(s) | What it constrains), covering color roles, type scale, spacing, radii, elevation, motion, status taxonomy, and voice patterns, each row citing the decision ID(s) above. This satisfies BRAND-05.

    Match the heading style and prose tone of the existing guides (e.g. guides/installation.md). Do NOT modify any of the 9 operator pages. Do NOT add any runtime dependency.
  </action>
  <verify>
    <automated>bash .planning/phases/70-brand-book-identity-foundation/checks.sh</automated>
  </verify>
  <acceptance_criteria>
    - `guides/brand-book.md` exists with ≥200 lines.
    - The checks.sh harness now passes (exit 0): all of D-01..D-22 present, Traceability table present, explain-then-act phrase present, canonical confirm template present.
    - The essence line (D-01) appears verbatim — `grep -q "calm control room for your background jobs" guides/brand-book.md` succeeds.
    - The canonical confirm template appears verbatim — `grep -q "Cancel 3 running jobs? They stop now and won't retry. This can't be undone." guides/brand-book.md` succeeds.
    - The coherence test (D-06) is quoted — `grep -q "calmer and more certain" guides/brand-book.md` succeeds.
    - The mono cascade (D-13) is present — `grep -q "ui-monospace" guides/brand-book.md` succeeds.
    - A traceability table row exists for each category — `grep -qi "motion" guides/brand-book.md` and `grep -qi "spacing" guides/brand-book.md` succeed within the traceability section.
    - `git diff --name-only` shows NONE of the 9 `*_live.ex` operator pages modified.
  </acceptance_criteria>
  <done>guides/brand-book.md faithfully encodes all 22 decisions in the prescribed chapter order with verbatim required strings and a complete BRAND-05 traceability table; checks.sh passes; no operator page changed.</done>
</task>

<task type="auto">
  <name>Task 3: Register brand-book.md for HexDocs under a Design System group</name>
  <read_first>
    - mix.exs (the docs/0 function: extras list and groups_for_extras map)
    - .planning/phases/70-brand-book-identity-foundation/70-RESEARCH.md (Pitfall 3: HexDocs exclusion; §README Linking mix.exs note)
  </read_first>
  <files>mix.exs</files>
  <action>
    In `mix.exs` docs/0, add a "Design System" group to `groups_for_extras` containing `"guides/brand-book.md"`. The existing `extras:` list already globs `Path.wildcard("guides/*.md")`, so brand-book.md is picked up automatically — but per Pitfall 3 it must be assigned to a group so it renders grouped rather than ungrouped. Add the new group (e.g. before or after the existing "Operations" group). Do NOT add any new dependency. Do NOT change `elixirc_paths` (that is Plan 70-02's concern). Run the formatter on mix.exs.
  </action>
  <verify>
    <automated>grep -q 'brand-book.md' mix.exs && grep -q 'Design System' mix.exs && mix compile --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - `mix.exs` contains a "Design System" group in `groups_for_extras` listing `"guides/brand-book.md"`.
    - `grep -q 'Design System' mix.exs` and `grep -q 'guides/brand-book.md' mix.exs` both succeed.
    - `mix compile --warnings-as-errors` succeeds (mix.exs parses cleanly).
    - `mix.exs` deps/0 is unchanged (no new dependency) — diff shows only docs/0 edited.
  </acceptance_criteria>
  <done>brand-book.md is registered under a Design System extras group in mix.exs; mix.exs compiles cleanly with no new dependency.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| committed Markdown → HexDocs / repo readers | Brand book is trusted, version-controlled content authored by the team; no user input crosses here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-70-01 | Tampering | guides/brand-book.md content drift vs locked decisions | mitigate | checks.sh asserts all D-01..D-22 + canonical strings present; CI runs it |
| T-70-02 | Information disclosure | brand book leaking into prod | accept | Markdown guide shipping in the Hex tarball is intended (HexDocs); no secrets present |
| T-70-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan; mix.exs deps/0 unchanged (prohibition enforced by acceptance criteria) |
</threat_model>

<verification>
- `bash .planning/phases/70-brand-book-identity-foundation/checks.sh` exits 0 (all D-01..D-22, traceability, explain-then-act, canonical confirm template present).
- `git diff --name-only` shows none of the 9 operator `*_live.ex` pages modified.
- `mix compile --warnings-as-errors` succeeds.
- `mix.exs` deps/0 unchanged (no new runtime dependency).
</verification>

<success_criteria>
- guides/brand-book.md exists, is versioned, and faithfully encodes all 22 locked decisions plus the BRAND-05 traceability table.
- The content-assertion harness passes.
- brand-book.md is registered for HexDocs under a Design System group.
- Zero changes to the 9 operator pages; zero new runtime dependencies.
</success_criteria>

<output>
Create `.planning/phases/70-brand-book-identity-foundation/70-01-SUMMARY.md` when done.
</output>
