---
phase: 78-component-groups-meta-components
plan: 03
type: execute
wave: 3
depends_on:
  - 78-02
files_modified:
  - lib/oban_powertools/web/components/operator_patterns.ex
  - test/oban_powertools/web/components/operator_patterns_test.exs
  - test/oban_powertools/web/live/operator_patterns_harness_test.exs
  - assets/oban_powertools/tokens.css
  - assets/oban_powertools/theme.js
  - priv/static/oban_powertools/oban_powertools.css
  - priv/static/oban_powertools/oban_powertools.js
  - test/oban_powertools/web/theme_tokens_test.exs
  - test/oban_powertools/web/assets_test.exs
autonomous: true
requirements:
  - GROUP-01
  - GROUP-02
  - COPY-02
  - A11Y-02
must_haves:
  truths:
    - "D-24..D-26: FilterBar is one top-of-results component with closed submit/instant modes; submit is default, drafts do not apply implicitly, and instant mode is reserved for one cheap criterion."
    - "D-25: FilterBar defaults its closed mode API to submit; multi-field drafts may validate on change but only Apply filters mutates results or the canonical URL."
    - "D-27..D-30: parent LiveViews own draft/applied values, validation, canonical URLs, pagination, queries, and history; active removals/Clear use caller-supplied destinations and one exact result status."
    - "D-28: parent-owned query handling preserves AND between filter categories and OR within same-category values while URLs use stable domain values; D-29: applied noun/value filters, exact results, per-filter named removal controls, and Clear filters remain visible outside collapsed fields."
    - "D-31..D-32: narrow Filters (N applied) disclosure uses one field tree, synchronized aria-expanded/controls, fields removed from collapsed tab order, visible applied filters/result summary, and no horizontal page scroll."
    - "D-55..D-56: Apply is the single primary action, targets are comfortable, focus is visible, long/Unicode copy wraps, and behavior remains keyboard-operable across themes/viewports."
    - "Wave 3 runs only string-tagged `phase78_slice: \"filter\"` component and harness contracts; confirmation/detail contracts remain intentionally RED and cannot block this wave."
  artifacts:
    - path: "lib/oban_powertools/web/components/operator_patterns.ex"
      provides: "Stateless submit/instant FilterBar composition"
      exports: ["filter_bar/1"]
    - path: "assets/oban_powertools/theme.js"
      provides: "Scoped idempotent filter disclosure behavior"
      exports: ["setFilterState", "syncFilterDisclosures"]
    - path: "assets/oban_powertools/tokens.css"
      provides: "Token-backed FilterBar layout, applied-filter, disclosure, dirty, and responsive rules"
    - path: "test/oban_powertools/web/live/operator_patterns_harness_test.exs"
      provides: "Connected parent-owned draft/applied, invalid, canonical patch, remove, and clear proof"
  key_links:
    - from: "lib/oban_powertools/web/components/operator_patterns.ex"
      to: "lib/oban_powertools/web/components/forms.ex"
      via: "parent Phoenix.HTML.Form and constrained field slots"
      pattern: "filter_bar"
    - from: "assets/oban_powertools/theme.js"
      to: "lib/oban_powertools/web/components/operator_patterns.ex"
      via: "fixed data-obpt-filter-toggle and controlled-region selectors"
      pattern: "data-obpt-filter"
---

<objective>
Ship the shared FilterBar with deliberate draft/applied semantics, one responsive field tree, and scoped packaged disclosure behavior.

Purpose: give future page migrations a shareable, truthful filter composition without moving query, URL, or validation authority into the component.
Output: FilterBar API/rendering, token CSS, packaged disclosure JS, connected parent harness, and green focused tests.
</objective>

<execution_context>
@/Users/jon/.agents/gsd-core/workflows/execute-plan.md
@/Users/jon/.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/78-component-groups-meta-components/78-CONTEXT.md
@.planning/phases/78-component-groups-meta-components/78-RESEARCH.md
@.planning/phases/78-component-groups-meta-components/78-PATTERNS.md
@.planning/phases/78-component-groups-meta-components/78-UI-SPEC.md
@.planning/phases/78-component-groups-meta-components/78-VALIDATION.md
@.planning/phases/78-component-groups-meta-components/78-02-SUMMARY.md
@.planning/phases/75-form-components/75-UI-SPEC.md
</context>

## Artifacts this phase produces

- New public function: `OperatorPatterns.filter_bar/1`.
- New exact attrs: `id`, `form`, `mode: :submit | :instant` (default `:submit`), `result_summary`, `results_target_id`, normalized `active_filters`, `dirty`, `filters_expanded`, `change_event`, `submit_event`, and `clear_href`; slots `fields` (required) and `advanced_fields`.
- Normalized active-filter fields consumed from Plan 78-02: `id`, `label`, `value`, `remove_href`, and `remove_label`; no status-pill/chip or component-built URL.
- New private component symbols may include `filter_mode/1`, `filter_disclosure_label/1`, and `filter_dom_id/2`; no route/query serializer is created.
- New client selectors/symbols: `[data-obpt-filter-bar]`, `[data-obpt-filter-toggle]`, `[data-obpt-filter-fields]`, `data-obpt-filter-state`, `setFilterState`, and `syncFilterDisclosures`.
- New CSS family: `.obpt-filter-bar` and descendants for fields, actions, dirty copy, summary, applied filters, remove/clear actions, narrow disclosure, focus/high-contrast, and 320px stacking.
- Regenerated packaged CSS/JS and extended component, connected-harness, token, and asset contracts.

<tasks>

<task type="auto">
  <name>Task 78-03-01: Implement the stateless FilterBar contract and responsive CSS</name>
  <files>lib/oban_powertools/web/components/operator_patterns.ex, test/oban_powertools/web/components/operator_patterns_test.exs, assets/oban_powertools/tokens.css, test/oban_powertools/web/theme_tokens_test.exs</files>
  <read_first>
    - @lib/oban_powertools/web/components/operator_patterns.ex
    - @test/oban_powertools/web/components/operator_patterns_test.exs
    - @assets/oban_powertools/tokens.css
    - @test/oban_powertools/web/theme_tokens_test.exs
    - @lib/oban_powertools/web/components/forms.ex
    - @lib/oban_powertools/web/components/primitives.ex
    - @lib/oban_powertools/web/control_plane_presenter.ex
    - @.planning/phases/75-form-components/75-CONTEXT.md
    - @.planning/phases/75-form-components/75-UI-SPEC.md
    - @.planning/phases/78-component-groups-meta-components/78-UI-SPEC.md
  </read_first>
  <action>Add `filter_bar/1` with the exact attrs/slots above and one semantic `&lt;.form for={@form}&gt;` tree. Render a real disclosure button labelled `Filters (N applied)` with `aria-expanded` and `aria-controls`; keep exact result summary, normalized noun/value applied filters, each named remove link/button, and `Clear filters` outside the controlled fields region. In submit mode, render constrained primary/advanced fields, optional validation change event, `Changes not applied.` only when dirty, and a Primitives primary `Apply filters` control after all fields. In instant mode, omit Apply and dirty-not-applied copy; never change mode at a breakpoint. Connect result summary to `results_target_id` as one restrained `role="status"`; do not announce validation keystrokes. Require caller-supplied canonical remove/clear destinations, never render StatusPill as an interactive chip, and never construct routes/query grammar. Add `.obpt-filter-bar` CSS using semantic tokens, one field tree, min-width 0, 44px controls, visible focus/non-color selection, wrapping applied pairs, narrow single-column field/action order, and &lt;=320px no-overflow behavior; fields are collapsed by the controller at narrow widths while applied filters and summary stay visible. Extend and turn green only render/source cases tagged `phase78_slice: "filter"` for submit/instant differences, one form/tree, native names, exact copy, URL ownership, safe attrs, long content, and root/token boundaries; leave later slices unchanged.</action>
  <verify>
    <automated>mix test test/oban_powertools/web/components/operator_patterns_test.exs --only phase78_slice:filter --seed 0</automated>
    <automated>mix test test/oban_powertools/web/theme_tokens_test.exs --seed 0</automated>
    <automated>mix format --check-formatted lib/oban_powertools/web/components/operator_patterns.ex test/oban_powertools/web/components/operator_patterns_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - Submit mode renders one form, one fields tree, dirty copy only when true, and one `Apply filters` control after the fields; instant mode renders none of those apply affordances.
    - Applied filter pairs, exact result summary, per-filter named removal, and `Clear filters` remain visible outside the collapsible region.
    - Component source contains no route/query serialization, Ecto access, status-pill chip, duplicate responsive field tree, class/style escape hatch, or production page import.
    - CSS is `.obpt-root` scoped, token-backed, gives controls at least 44px effective targets, and wraps long content without page horizontal scroll at 320px.
  </acceptance_criteria>
  <done>The filter slice passes with one stateless submit/instant form tree, truthful applied state, caller-owned URLs, and root-scoped 44px no-overflow styling.</done>
</task>

<task type="auto">
  <name>Task 78-03-02: Add scoped idempotent filter disclosure behavior and rebuild assets</name>
  <files>assets/oban_powertools/theme.js, priv/static/oban_powertools/oban_powertools.css, priv/static/oban_powertools/oban_powertools.js, test/oban_powertools/web/assets_test.exs</files>
  <read_first>
    - @assets/oban_powertools/theme.js
    - @assets/oban_powertools/tokens.css
    - @priv/static/oban_powertools/oban_powertools.css
    - @priv/static/oban_powertools/oban_powertools.js
    - @test/oban_powertools/web/assets_test.exs
    - @lib/mix/tasks/oban_powertools.assets.build.ex
    - @.planning/phases/76-navigation-app-shell/76-PATTERNS.md
    - @.planning/phases/78-component-groups-meta-components/78-PATTERNS.md
  </read_first>
  <action>Extend the existing standalone `theme.js` controller with fixed root-scoped filter selectors and idempotent `setFilterState(filterBar, state)` / `syncFilterDisclosures(root)` functions. A delegated click on `[data-obpt-filter-toggle]` toggles only its nearest `.obpt-root` FilterBar; `setFilterState` writes closed `data-obpt-filter-state="open|closed"`, synchronizes the button's `aria-expanded`, and toggles the controlled region's `hidden`/inert tab reachability at the narrow presentation. Re-run synchronization for inserted/patched roots through the existing narrowly filtered observer; do not create a custom `phx-hook`, host LiveSocket hook-map requirement, component localStorage, event payload logging, dynamic evaluation, or query/filter persistence. Extend asset source guards for exact selectors/functions and prohibited host/sensitive behavior, then run the deterministic asset build so packaged CSS/JS derive solely from source and pass repeat-hash equality.</action>
  <verify>
    <automated>mix oban_powertools.assets.build &amp;&amp; mix test test/oban_powertools/web/assets_test.exs test/oban_powertools/web/theme_tokens_test.exs --seed 0</automated>
    <automated>node --check assets/oban_powertools/theme.js &amp;&amp; node --check priv/static/oban_powertools/oban_powertools.js</automated>
  </verify>
  <acceptance_criteria>
    - Source and packaged JS contain the fixed filter selectors and both named idempotent functions; all lookup is constrained to the nearest `.obpt-root`.
    - Toggle state, `aria-expanded`, visibility, and collapsed tab reachability stay synchronized after repeated sync and LiveView DOM patches.
    - No custom host hook registration, localStorage/sessionStorage, console logging, filter values, result data, preview data, or dynamic evaluation is introduced.
    - Source/static CSS and JS are byte-equal after two stable builds.
  </acceptance_criteria>
  <done>Nearest-root filter disclosure synchronization remains idempotent across toggles and patches, and two builds produce byte-equal CSS/JS without sensitive persistence or host hooks.</done>
</task>

<task type="auto">
  <name>Task 78-03-03: Prove parent-owned draft, applied, validation, and navigation truth</name>
  <files>test/oban_powertools/web/live/operator_patterns_harness_test.exs</files>
  <read_first>
    - @test/oban_powertools/web/live/operator_patterns_harness_test.exs
    - @lib/oban_powertools/web/components/operator_patterns.ex
    - @test/oban_powertools/web/live/showcase_live_test.exs
    - @lib/oban_powertools/web/jobs_live.ex
    - @lib/oban_powertools/web/workflows_live.ex
    - @.planning/phases/78-component-groups-meta-components/78-CONTEXT.md
    - @.planning/phases/78-component-groups-meta-components/78-VALIDATION.md
  </read_first>
  <action>Turn only the `phase78_slice: "filter"` portion of the test-only connected harness green without modifying a production page. The harness parent owns a `Phoenix.HTML.Form`, `draft_filters`, `applied_filters`, exact result summary, pagination, and canonical query params. Implement test-only validate/apply/remove/clear events so change retains draft/errors and leaves applied values, results, pagination, and URL unchanged; Apply validates, converts stable domain values to a canonical query, resets pagination to 1, and patches once; removal and clear use canonical caller-owned destinations; an invalid direct query replaces to the canonical safe URL. Pin AND-between-categories/OR-within-category as parent data semantics rather than component code. Assert invalid entered values remain visible, `Changes not applied.` is non-live, and successful apply/remove/clear produces one exact result status tied to the results target. Preserve the production Jobs/Forensics route grammar and queries untouched; do not alter confirmation/detail tags to force a premature full-file green.</action>
  <verify>
    <automated>mix test test/oban_powertools/web/live/operator_patterns_harness_test.exs --only phase78_slice:filter --seed 0</automated>
    <automated>mix format --check-formatted test/oban_powertools/web/live/operator_patterns_harness_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - Connected draft change updates only draft/error state and produces no applied result, pagination, or URL change.
    - Valid Apply resets pagination and emits exactly one canonical patch/status update; invalid Apply retains values/errors and does not patch.
    - Remove and Clear operate through parent-owned canonical destinations, and invalid direct URLs use replacement rather than stacking history.
    - No production LiveView, route grammar, Ecto query, authorization function, mutation engine, or audit schema is modified.
    - The filter-tagged harness is green without skipping, deleting, or requiring still-pending confirmation/detail cases to pass.
  </acceptance_criteria>
  <done>The filter-tagged connected harness proves draft/applied separation, validation, canonical apply/remove/clear navigation, and exact status updates with production pages untouched.</done>
</task>

</tasks>

<threat_model>
## ASVS L1 Threat Model — blocking threshold: high

| Threat ID | Category | Severity | Mitigation and verification |
|-----------|----------|----------|-----------------------------|
| T-78-03-URL | Tampering | high | Parent parses/validates stable domain values and owns canonical patch/replace behavior; component never constructs URLs or queries. |
| T-78-03-AUTH | Elevation of privilege | high | FilterBar renders constrained fields/actions only; authorization and Ecto filtering remain parent/server-owned. |
| T-78-03-XSS | Injection | high | Labels, values, summaries, and errors render escaped; caller attrs/HTML are not projected. |
| T-78-03-LEAK | Information disclosure | high | JS stores/logs no query values or results; normalized active filters expose display values and canonical destinations only. |
| T-78-03-FOCUS | Denial of service / accessibility | high | Real disclosure button synchronizes expanded/controlled/hidden/tab state in one tree and retains applied summary/actions outside collapse. |
| T-78-03-HOST | Tampering | high | Controller is nearest-root scoped and idempotent with no custom hook-map or host/global state dependency. |
</threat_model>

<verification>
- Run component/CSS tests, then asset build/equality, then the connected harness.
- Block on any high-severity URL authority, injection, disclosure, focus/tab-order, or host-boundary failure.
- Confirm the diff contains no production page, auth, query, mutation, route, schema, dependency, or lockfile change.
</verification>

<success_criteria>
- FilterBar ships with closed submit/instant modes, one field tree, truthful draft/applied copy, visible active state, and accessible narrow disclosure.
- Parent/server code remains the only authority for validation, result queries, pagination, URL serialization, and history.
- Scoped CSS/JS and packaged assets are deterministic, root-owned, and host-independent.
- Focused component, connected, token, asset, syntax, and formatting checks pass.
</success_criteria>

<output>
Create `.planning/phases/78-component-groups-meta-components/78-03-SUMMARY.md` when done.
</output>
