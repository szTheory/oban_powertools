---
phase: 78-component-groups-meta-components
plan: 04
type: execute
wave: 4
depends_on:
  - 78-03
files_modified:
  - lib/oban_powertools/web/components/operator_patterns.ex
  - test/oban_powertools/web/components/operator_patterns_test.exs
  - test/oban_powertools/web/live/operator_patterns_harness_test.exs
  - assets/oban_powertools/tokens.css
  - priv/static/oban_powertools/oban_powertools.css
  - test/oban_powertools/web/theme_tokens_test.exs
  - test/oban_powertools/web/assets_test.exs
autonomous: true
requirements:
  - GROUP-01
  - GROUP-02
  - FORM-04
  - COPY-02
  - A11Y-02
must_haves:
  truths:
    - "D-09..D-15: ConfirmActionDialog renders parent-owned preview -> valid confirmation -> submitting -> authoritative result truth, with named object/scope/consequence/reversibility/support boundary, trimmed reason, exact frozen bulk count, and duplicate-submit UI suppression."
    - "D-10: preview names the object, exact/off-page scope and count, consequence, reversibility, and support boundary before action-specific confirmation is enabled; D-11: audited mutations use a trimmed parent-form reason, the Lifeline eight-character minimum where applicable, server revalidation, and do-not-enter-secrets microcopy."
    - "D-12: reversible/irreversible intent controls warning/danger weight and bulk additionally requires the exact frozen count plus off-page membership truth; D-13: the visible dismiss action names the retained safe state and destructive confirmation has no click-away dismissal."
    - "D-14: the stateless contract exposes only closed intent/lifecycle, semantic copy, parent Phoenix.HTML.Form, normalized results, and constrained recovery/audit/support slots—not tokens, action atoms, hashes, raw errors, or storage fields."
    - "D-16..D-19: clean authoritative success leaves one exact parent receipt; partial/failed/skipped/stale outcomes remain open with deterministic recovery, while pending uses only real progress and never fabricates host completion."
    - "D-17: partial, failed, skipped, expired, drifted, and consumed outcomes keep the dialog open, focus a tabindex=-1 result heading, preserve ordered distinct result rows, and show specific recovery plus durable-audit links; D-18: expired/drifted/consumed outcomes require an explicit fresh preview, preserve safe reason text, explain change, and never refresh-and-execute or replay silently."
    - "D-20..D-21: role=dialog focus_wrap starts on static title/consequence, traps Tab, supports dismissible Escape/visible safe action, and restores invoker or required logical fallback."
    - "D-22..D-23: parent/server reauthorizes and revalidates reason/count/membership/freshness/drift/single-use; escaped/redacted presentation never exposes preview tokens, plan hashes, raw errors, secrets, or a bulk executor."
    - "D-53..D-56 and D-61: the explain/action/result/audit nouns remain consistent; copy, focus, 320px reflow, non-color state, stale/replay, duplicate submit, and result order have contract proof."
    - "Wave 4 runs only string-tagged `phase78_slice: \"confirmation\"` component and harness contracts; DetailSurface remains intentionally RED until Plan 78-05."
  artifacts:
    - path: "lib/oban_powertools/web/components/operator_patterns.ex"
      provides: "Stateless authoritative-preview confirmation composition"
      exports: ["confirm_action_dialog/1"]
    - path: "test/oban_powertools/web/live/operator_patterns_harness_test.exs"
      provides: "Server reason/count/authorization/freshness/single-use and exact result/receipt proof"
    - path: "assets/oban_powertools/tokens.css"
      provides: "Token-backed confirmation overlay, form, pending, result, recovery, and responsive styles"
  key_links:
    - from: "lib/oban_powertools/web/components/operator_patterns.ex"
      to: "lib/oban_powertools/web/components/forms.ex"
      via: "parent form reason and confirmation_count fields"
      pattern: "Forms.textarea"
    - from: "test/oban_powertools/web/live/operator_patterns_harness_test.exs"
      to: "lib/oban_powertools/lifeline.ex"
      via: "real preview expiry/drift/consumption execution rejection"
      pattern: "execute_repair"
---

<objective>
Ship ConfirmActionDialog as a stateless, consequence-first composition over parent/server-authoritative preview, reason, count, submission, result, and audit truth.

Purpose: centralize the shared danger pattern without turning rendered preview or disabled controls into authorization or an execution engine.
Output: confirmation component/CSS, connected validation and stale/replay evidence, deterministic packaged CSS, and green focused contracts.
</objective>

<execution_context>
@/Users/jon/.agents/gsd-core/workflows/execute-plan.md
@/Users/jon/.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/78-component-groups-meta-components/78-CONTEXT.md
@.planning/phases/78-component-groups-meta-components/78-RESEARCH.md
@.planning/phases/78-component-groups-meta-components/78-PATTERNS.md
@.planning/phases/78-component-groups-meta-components/78-UI-SPEC.md
@.planning/phases/78-component-groups-meta-components/78-VALIDATION.md
@.planning/phases/78-component-groups-meta-components/78-03-SUMMARY.md
@guides/brand-book.md
</context>

## Artifacts this phase produces

- New public function: `OperatorPatterns.confirm_action_dialog/1`.
- New closed lifecycle: `:preview | :submitting | :partial | :failed | :expired | :drifted | :consumed`; clean success is represented by removal plus one parent-owned receipt.
- New exact attrs: `id`, `intent: :warning | :danger`, lifecycle `state`, `title`, `object_label`, `scope`, `consequence`, `reversibility`, `support_boundary`, parent `form`, optional `bulk_count`/`bulk_scope`, `confirm_label`, `dismiss_label`, `pending_copy`, `logical_fallback_id`, `submit_event`, `dismiss_event`, `dismissible`, optional real `progress`, and normalized `results`; slots `recovery`, `audit`, and `support_details`.
- Fixed form field names: `reason` and optional `confirmation_count`; the parent form/changeset owns values/errors and server revalidation.
- New semantic IDs derived from component id: `#{id}-focus-wrap`, `#{id}-dialog`, `#{id}-title`, `#{id}-result-heading`, and `#{id}-form`.
- New focus/escape attributes: `data-obpt-focus-fallback={logical_fallback_id}`, title `phx-mounted={JS.focus(...)}`, and dismissible-only `phx-window-keydown` with `phx-key="Escape"`; the parent trigger composes `JS.push_focus` before open and close composes the parent event with `JS.pop_focus`.
- New private symbols may include `confirmation_state/1`, `confirmation_intent/1`, `confirmation_fields/1`, `result_rows/1`, `confirmation_dismissible?/1`, `focus_confirmation_title/1`, and `dismiss_confirmation/2`; none accept raw backend structs/errors/tokens.
- New CSS family: `.obpt-confirm-action` with overlay, dialog, consequence, scope, form, actions, busy, result, recovery, 320px, high-contrast, focus-visible, and reduced-motion rules.
- Regenerated packaged CSS and extended component/harness/token/asset tests; packaged JS is unchanged in this plan.

<tasks>

<task type="auto">
  <name>Task 78-04-01: Implement ConfirmActionDialog semantics, focus composition, and token CSS</name>
  <files>lib/oban_powertools/web/components/operator_patterns.ex, test/oban_powertools/web/components/operator_patterns_test.exs, assets/oban_powertools/tokens.css, test/oban_powertools/web/theme_tokens_test.exs</files>
  <read_first>
    - @lib/oban_powertools/web/components/operator_patterns.ex
    - @test/oban_powertools/web/components/operator_patterns_test.exs
    - @assets/oban_powertools/tokens.css
    - @test/oban_powertools/web/theme_tokens_test.exs
    - @lib/oban_powertools/web/components/forms.ex
    - @lib/oban_powertools/web/components/primitives.ex
    - @lib/oban_powertools/web/components/data_display.ex
    - @deps/phoenix_live_view/lib/phoenix_component.ex
    - @deps/phoenix_live_view/lib/phoenix_live_view/js.ex
    - @.planning/phases/78-component-groups-meta-components/78-UI-SPEC.md
  </read_first>
  <action>Add `confirm_action_dialog/1` with the exact lifecycle/attrs/slots/IDs above. Render a `Phoenix.Component.focus_wrap` around `role="dialog"`, `aria-modal="true"`, `aria-labelledby`, and `aria-busy` only while submitting. The title/consequence node is static `tabindex="-1"`, receives `phx-mounted={focus_confirmation_title(@id)}` where that helper returns `JS.focus(to: "##{id}-title")`, and is the initial focus target; never focus the danger action first. Set `data-obpt-focus-fallback` to the required logical fallback id. For dismissible states, attach `phx-window-keydown` with `phx-key="Escape"` to the same `dismiss_confirmation/2` command as the visible safe action; that command pushes the parent dismiss event and calls `JS.pop_focus`, while the caller trigger pushes focus before opening. Omit the Escape close command in accepted non-dismissible state. Preview order is object/title, frozen exact scope including off-page membership, consequence, reversibility, support boundary, required `Forms.textarea` for `@form[:reason]` with label `Reason` and hint `Explain why this action is needed. Do not enter secrets.`, optional required `Forms.input` for `@form[:confirmation_count]` labelled `Type N to confirm`, then safe dismiss and action-specific confirm controls. Use warning for reversible and danger for irreversible intent, reject bare Confirm/generic Cancel/click-away close. Submitting sets busy/disabled/phx-disable-with behavior, names work with pending_copy, renders real processed/total progress only when supplied, and explicitly states when accepted work cannot be canceled. Partial/failed rows remain open, preserve normalized order, render operator-result status plus object/message/recovery/audit, distinguish failed/skipped, and focus `#{id}-result-heading`; expired/drifted/consumed render exact fresh-preview recovery and never silently execute. Clean success is absent from this lifecycle. Add root-scoped token CSS for structured modal layout, calm warning/danger markers, 44px actions, full-width stacked 320px actions with safe action distinct, long-copy wrapping, high-contrast/focus, and reduced-motion-safe feedback. Extend and turn green only render/source tests tagged `phase78_slice: "confirmation"` for every branch, exact copy, required fields, focus/Escape/pop/fallback semantics, no click-away, no fake percentage, no nested/raw dialogs/errors/secrets, and no page/backend imports; preserve the pending detail slice.</action>
  <verify>
    <automated>mix test test/oban_powertools/web/components/operator_patterns_test.exs --only phase78_slice:confirmation --seed 0</automated>
    <automated>mix test test/oban_powertools/web/theme_tokens_test.exs --seed 0</automated>
    <automated>mix format --check-formatted lib/oban_powertools/web/components/operator_patterns.ex test/oban_powertools/web/components/operator_patterns_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - Preview output contains role/dialog/modal/label/focus-wrap semantics, static initial focus, required reason, optional exact-count field, complete consequence/scope/reversibility/support copy, and action-specific controls.
    - Submitting has `aria-busy`, named accepted work, disabled duplicate controls, and either spinner or real progress—not both and never a fabricated percentage.
    - Partial/failed/skipped/stale branches remain open, preserve distinct ordered rows, include recovery/audit seams, and expose a focusable result heading.
    - Source/render tests reject preview tokens, plan hashes, action atoms, raw errors, secrets, generic copy, click-away dismissal, alertdialog, nested dialog, and component-owned server decisions.
    - CSS is root-scoped/token-backed and preserves focus, readable hierarchy, non-color outcome, and 320px no-overflow behavior.
  </acceptance_criteria>
  <done>The confirmation slice passes every preview, submitting, result, stale, focus, Escape, copy, confidentiality, and 320px root-scoped CSS contract.</done>
</task>

<task type="auto">
  <name>Task 78-04-02: Prove server-authoritative reason, count, replay, and result behavior</name>
  <files>test/oban_powertools/web/live/operator_patterns_harness_test.exs</files>
  <read_first>
    - @test/oban_powertools/web/live/operator_patterns_harness_test.exs
    - @lib/oban_powertools/web/components/operator_patterns.ex
    - @lib/oban_powertools/lifeline.ex
    - @lib/oban_powertools/lifeline/repair_preview.ex
    - @test/oban_powertools/lifeline_test.exs
    - @lib/oban_powertools/audit.ex
    - @lib/oban_powertools/web/lifeline_live.ex
    - @.planning/phases/78-component-groups-meta-components/78-CONTEXT.md
    - @.planning/phases/78-component-groups-meta-components/78-VALIDATION.md
  </read_first>
  <action>Turn only the `phase78_slice: "confirmation"` portion of the test-only connected harness green while leaving production pages/Lifeline unchanged. Back the harness reason/count form with a schemaless Ecto changeset or equivalent pure server validation: trim reason, apply the existing eight-character minimum where Lifeline is modeled, compare confirmation_count to the frozen authoritative count, and recheck authorization/scope/freshness on submit rather than trusting form validity or disabled controls. Count submit-side mutation calls and assert two rapid activations yield one accepted transition/receipt. Reuse existing Lifeline fixture/setup patterns to exercise actual expired, drifted, consumed, and single-use `execute_repair/5` rejection; map only finite errors to exact expired/drifted/consumed recovery copy and never expose tokens/hashes/raw errors. Exercise deterministic result rows with success, failed, and skipped outcomes, specific recovery and optional audit destinations; assert partial stays open and focuses the result seam. For clean authoritative success, remove the dialog and emit exactly one existing Flash/Toast receipt that states only what was requested/recorded and closes bulk only when returned count equals frozen count. Test permission denial before preview and execution, reason/count errors, preserved safe reason on fresh-preview recovery, and absence of secret sentinels from complete rendered HTML. Do not weaken or run the pending detail slice as a Wave 4 green requirement.</action>
  <verify>
    <automated>mix test test/oban_powertools/web/live/operator_patterns_harness_test.exs --only phase78_slice:confirmation --seed 0</automated>
    <automated>mix test test/oban_powertools/lifeline_test.exs --seed 0</automated>
    <automated>mix format --check-formatted test/oban_powertools/web/live/operator_patterns_harness_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - Blank/short reason and wrong frozen count produce inline errors, keep preview open, perform zero mutation calls, and make no success claim.
    - Valid authoritative input is revalidated server-side, accepted once under duplicate activation, and produces exactly one truthful receipt.
    - Actual Lifeline expired/drifted/consumed/single-use paths reject execution and require an explicit new preview without replay or raw token/hash/error disclosure.
    - Partial rows remain visible in supplied success/failed/skipped order with specific messages, recovery, and audit destinations; failed and skipped never merge.
    - No implementation change occurs in Lifeline, RepairPreview, authorization, mutation transaction, Audit schema, or a production LiveView.
    - The confirmation-tagged harness is green while the DetailSurface contract remains intact and pending for Plan 78-05.
  </acceptance_criteria>
  <done>The confirmation-tagged harness proves zero calls for invalid input, one authoritative call and receipt for valid duplicate activation, and safe rejection of stale/replayed Lifeline previews.</done>
</task>

<task type="auto">
  <name>Task 78-04-03: Regenerate and prove deterministic confirmation CSS assets</name>
  <files>priv/static/oban_powertools/oban_powertools.css, test/oban_powertools/web/assets_test.exs</files>
  <read_first>
    - @assets/oban_powertools/tokens.css
    - @priv/static/oban_powertools/oban_powertools.css
    - @test/oban_powertools/web/assets_test.exs
    - @lib/mix/tasks/oban_powertools.assets.build.ex
  </read_first>
  <action>Extend asset assertions for representative `.obpt-confirm-action`, result, busy, and reduced-motion selectors. Run the deterministic asset build to regenerate packaged CSS from source, repeat it, and compare hashes. Assert packaged JavaScript remains unchanged because confirmation uses pinned LiveView focus/loading primitives rather than a new custom controller.</action>
  <verify>
    <automated>mix oban_powertools.assets.build &amp;&amp; mix test test/oban_powertools/web/assets_test.exs test/oban_powertools/web/theme_tokens_test.exs --seed 0</automated>
    <automated>git diff --exit-code -- priv/static/oban_powertools/oban_powertools.js</automated>
  </verify>
  <acceptance_criteria>
    - Source and packaged CSS contain the confirmation root/result/busy/reduced-motion selectors and are byte-equal.
    - Two consecutive builds produce identical asset hashes.
    - Packaged JS has no plan-local diff and no generated file was edited independently.
    - No dependency, lockfile, schema, route, production page, auth, query, mutation, or audit storage file changes.
  </acceptance_criteria>
  <done>Two deterministic builds produce byte-equal confirmation CSS, leave packaged JavaScript unchanged, and introduce no protected-boundary changes.</done>
</task>

</tasks>

<threat_model>
## ASVS L1 Threat Model — blocking threshold: high

| Threat ID | Category | Severity | Mitigation and verification |
|-----------|----------|----------|-----------------------------|
| T-78-04-AUTH | Spoofing / elevation | high | Parent/server authorizes before preview and execution and revalidates reason/count/scope; disabled UI is never authority. |
| T-78-04-REPLAY | Tampering | high | Real expiry/drift/consumed/single-use checks reject stale execution and require explicit fresh preview; no silent refresh/replay. |
| T-78-04-DUP | Tampering | high | LiveView loading/disabled controls suppress duplicate activation while server call-count tests prove one accepted transition/receipt. |
| T-78-04-XSS | Injection | high | Complete copy/results render escaped; raw errors/HTML/action atoms are prohibited and hostile values tested. |
| T-78-04-LEAK | Information disclosure | high | Tokens, hashes, reason originals, raw errors, and secrets never enter attrs/DOM/logging; full-HTML sentinels block. |
| T-78-04-TRUTH | Spoofing | high | Copy distinguishes requested/recorded work from host completion and keeps partial/failed/skipped/stale evidence visible. |
| T-78-04-FOCUS | Denial of service / accessibility | high | FocusWrap, static initial focus, Escape/dismiss, result focus, and invoker/fallback restoration are pinned for connected browser proof. |
</threat_model>

<verification>
- Run component/CSS contracts, then server-authority/Lifeline integration tests, then deterministic asset equality.
- Block completion on any high-severity auth, replay, duplicate, injection, leak, false receipt, or focus-semantics failure.
- Confirm the diff excludes every production page, Lifeline/mutation implementation, auth callback, Ecto query/schema, route, dependency, and lockfile.
</verification>

<success_criteria>
- ConfirmActionDialog ships all preview/reason/count/pending/result/recovery semantics with exact copy and accessibility structure.
- Server authority, expiry/drift/single-use, duplicate submission, partial result order, and exact receipt behavior have connected automated proof.
- CSS/package assets are scoped, token-owned, byte-equal, and repeatable.
- FORM-04, COPY-02, GROUP-01/02, and A11Y-02 are satisfied at the reusable group/harness boundary without adding a mutation engine or migrating pages.
</success_criteria>

<output>
Create `.planning/phases/78-component-groups-meta-components/78-04-SUMMARY.md` when done.
</output>
