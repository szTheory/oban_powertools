---
phase: 78-component-groups-meta-components
plan: 02
type: execute
wave: 2
depends_on:
  - 78-01
files_modified:
  - lib/oban_powertools/web/status_taxonomy.ex
  - lib/oban_powertools/web/components/data_display.ex
  - lib/oban_powertools/web/control_plane_presenter.ex
  - lib/oban_powertools/web/components/operator_patterns.ex
  - test/oban_powertools/web/status_taxonomy_test.exs
  - test/oban_powertools/web/components/data_display_test.exs
  - test/oban_powertools/web/operator_pattern_presenter_test.exs
  - test/oban_powertools/web/components/operator_patterns_test.exs
  - assets/oban_powertools/tokens.css
  - priv/static/oban_powertools/oban_powertools.css
  - test/oban_powertools/web/theme_tokens_test.exs
  - test/oban_powertools/web/assets_test.exs
autonomous: true
requirements:
  - GROUP-01
  - GROUP-02
  - COPY-02
  - A11Y-02
must_haves:
  truths:
    - "D-01..D-08: one public stateless OperatorPatterns module consumes closed semantic attrs/slots and finite presentation maps; it never owns authorization, data fetching, routes, mutations, redaction, or audit writes."
    - "D-02: all six APIs remain stateless Phoenix function components with closed attrs/slots while parent LiveViews own lifecycle and server authority; D-03: they compose Primitives, Forms, and DataDisplay without caller class/style/global escape hatches."
    - "D-04: repeated filters, blockers, results, and audit facts use normalized presentation maps plus constrained action/evidence slots rather than backend structs; D-05: every pattern preserves the current-state -> explanation -> impact -> safe-action -> freshness/completeness -> technical-evidence order."
    - "D-06: empty/loading/ready/stale/unavailable/permission/invalid/pending/success/partial/error inputs render explicitly without inferring truth; D-07: ControlPlanePresenter or a tightly scoped pure companion supplies grammatical domain copy rather than component-built sentences."
    - "D-41..D-47: AttentionCard and WhyBlocked remain distinct current-truth patterns, preserve status versus severity and current versus snapshot evidence, render all supplied blockers, and never infer No blockers from incomplete evidence."
    - "D-42: AttentionCard keeps domain status separate from severity and requires title, summary, impact/scope, observed time, and completeness defaulting to unknown; D-43: it remains persistent, contextual, non-dismissible, and non-alert by default with at most one primary next action."
    - "D-44: explanation surfaces use neutral chrome, non-color severity cues, no nested-card/badge alarm noise, and readable 320px action hierarchy; D-45: WhyBlocked renders every supplied blocker with label, summary, clearing condition, source, optional code, and the locked answer-to-evidence order."
    - "D-46: WhyBlocked says current blocker/cause rather than unproven root cause, separates live state from snapshot evidence, and never claims No blockers from stale, partial, unavailable, or denied evidence."
    - "D-48..D-51: AuditEntry is immutable historical evidence with a complete sentence, absolute time/datetime, explicit actor/action/target/reason/source/correlation/outcome facts, and exact missing-field copy."
    - "D-49: AuditEntry renders an article in Timeline/list composition with a complete event heading, explicit outcome, absolute machine-readable time, fact description list, and progressively disclosed safe evidence; D-50: missing reason/outcome stay explicit, system actors remain named, and later outcomes become later entries rather than mutations."
    - "D-52..D-56: explanation, impact, one safe action, freshness/completeness, and technical evidence remain readable at 320px, across four themes, without color-only meaning or secret originals."
    - "D-54: usefulness, progressive disclosure, WCAG 2.2 AA, support truth/recovery, confidentiality, concurrency truth, responsive and bounded rendering, consistent vocabulary, calm hierarchy, reduced motion, long-content resilience, and documented ergonomics are executable merge gates."
    - "Wave 2 makes only the string-tagged `phase78_slice: \"explanation\"` component contract green; filter, confirmation, and detail RED contracts remain intentionally pending until their owning plans."
  artifacts:
    - path: "lib/oban_powertools/web/status_taxonomy.ex"
      provides: "Closed operator-result status specs for success, failed, and skipped"
      exports: ["spec/2", "all_specs/0"]
    - path: "lib/oban_powertools/web/components/data_display.ex"
      provides: "Closed DataDisplay status_pill acceptance of the operator_result domain"
      exports: ["status_pill/1"]
    - path: "lib/oban_powertools/web/control_plane_presenter.ex"
      provides: "Finite normalized filter/result/blocker/audit maps and evidence-completeness truth"
      exports: ["normalize_active_filters/1", "normalize_operator_results/1", "normalize_blockers/1", "normalize_audit_entry/1", "normalize_evidence_completeness/1"]
    - path: "lib/oban_powertools/web/components/operator_patterns.ex"
      provides: "First three non-overlay operator meta-components"
      exports: ["attention_card/1", "why_blocked/1", "audit_entry/1"]
    - path: "assets/oban_powertools/tokens.css"
      provides: "Root-scoped token-backed explanation/audit group styles and responsive states"
    - path: "priv/static/oban_powertools/oban_powertools.css"
      provides: "Generated packaged CSS containing the new source styles"
  key_links:
    - from: "lib/oban_powertools/web/control_plane_presenter.ex"
      to: "lib/oban_powertools/web/components/operator_patterns.ex"
      via: "normalized maps and complete grammatical copy"
      pattern: "normalize_blockers"
    - from: "lib/oban_powertools/web/components/operator_patterns.ex"
      to: "lib/oban_powertools/web/components/data_display.ex"
      via: "status_pill, description_list, and redaction-safe evidence composition"
      pattern: "DataDisplay"
---

<objective>
Build the pure presentation foundation and the three non-overlay current-truth/history groups.

Purpose: establish one finite, escaped, token-owned explain-then-act vocabulary before adding filter, confirmation, or adaptive-detail state machines.
Output: operator-result taxonomy, pure normalizers, AttentionCard, WhyBlocked, AuditEntry, scoped styles, and green focused contracts.
</objective>

<execution_context>
@/Users/jon/.agents/gsd-core/workflows/execute-plan.md
@/Users/jon/.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/78-component-groups-meta-components/78-CONTEXT.md
@.planning/phases/78-component-groups-meta-components/78-RESEARCH.md
@.planning/phases/78-component-groups-meta-components/78-PATTERNS.md
@.planning/phases/78-component-groups-meta-components/78-UI-SPEC.md
@.planning/phases/78-component-groups-meta-components/78-VALIDATION.md
@.planning/phases/78-component-groups-meta-components/78-01-SUMMARY.md
@guides/brand-book.md
</context>

## Artifacts this phase produces

- New production file/module: `lib/oban_powertools/web/components/operator_patterns.ex` / `ObanPowertools.Web.Components.OperatorPatterns`.
- New public component functions in this plan: `attention_card/1`, `why_blocked/1`, and `audit_entry/1`.
- New presenter functions: `normalize_active_filters/1`, `normalize_operator_results/1`, `normalize_blockers/1`, `normalize_audit_entry/1`, and `normalize_evidence_completeness/1`.
- New status taxonomy domain/field: `:operator_result` with exact states `:success`, `:failed`, and `:skipped` and shared status specs.
- Modified DataDisplay closed-domain field: `@status_domains` adds `operator_result`; no other DataDisplay API or presentation behavior changes.
- New component attrs/slots: Attention fields and action slots; WhyBlocked fields including closed evidence state/current freshness, blocker list, action/evidence slots; AuditEntry `id`, normalized `entry`, and evidence slot as listed in Plan 78-01.
- New private finite helpers may be named `presentation_value/2`, `required_presentation_text!/2`, `optional_presentation_text/2`, `normalize_presentation_id!/2`, and `normalize_datetime!/2`; no public generic rendering DSL is created.
- New CSS families: `.obpt-attention-card`, `.obpt-why-blocked`, and `.obpt-audit-entry` with token-backed descendants, state attributes, focus-visible, high-contrast, 320px, and long-content rules.
- Modified/generated evidence files: taxonomy/presenter/component/token/asset tests and generated packaged CSS; no JS or production page file changes.

<tasks>

<task type="auto">
  <name>Task 78-02-01: Implement finite presenter normalization and operator-result taxonomy</name>
  <files>lib/oban_powertools/web/status_taxonomy.ex, lib/oban_powertools/web/components/data_display.ex, lib/oban_powertools/web/control_plane_presenter.ex, test/oban_powertools/web/status_taxonomy_test.exs, test/oban_powertools/web/components/data_display_test.exs, test/oban_powertools/web/operator_pattern_presenter_test.exs</files>
  <read_first>
    - @lib/oban_powertools/web/status_taxonomy.ex
    - @lib/oban_powertools/web/components/data_display.ex
    - @lib/oban_powertools/web/control_plane_presenter.ex
    - @test/oban_powertools/web/status_taxonomy_test.exs
    - @test/oban_powertools/web/components/data_display_test.exs
    - @test/oban_powertools/web/operator_pattern_presenter_test.exs
    - @lib/oban_powertools/audit.ex
    - @lib/oban_powertools/explain.ex
    - @lib/oban_powertools/batches.ex
    - @lib/oban_powertools/lifeline/repair_preview.ex
    - @.planning/phases/78-component-groups-meta-components/78-PATTERNS.md
    - @.planning/phases/78-component-groups-meta-components/78-UI-SPEC.md
  </read_first>
  <action>Add `operator_result` to the existing string-keyed StatusTaxonomy domain registry with exact specs: success -> label `Success`, tone `:success`, icon `:check`, prefix `Operator result`; failed -> `Failed`, `:danger`, `:alert`; skipped -> `Skipped`, `:warning`, `:alert`. Add `operator_result` to DataDisplay's closed `@status_domains` list and add a focused render regression proving `DataDisplay.status_pill/1` delegates those exact specs; change no other DataDisplay contract. Keep compile-time atom records in `all_specs/0` and atom/binary input parity without any string-to-atom conversion. Extend `ControlPlanePresenter` with the five named public normalizers. `normalize_active_filters/1` accepts only id/label/value/remove_href/remove_label; `normalize_operator_results/1` accepts only id/object_label/outcome/message/recovery/audit_href and preserves order; `normalize_blockers/1` accepts only id/evidence_kind (`current | block_start_snapshot`), label, summary, clearing_condition, evidence_source, and technical_code and preserves every supplied blocker; `normalize_audit_entry/1` emits sentence/outcome/actor/action/target/reason/source/correlation/occurred_at/occurred_datetime plus already-redacted optional changes/evidence; `normalize_evidence_completeness/1` closes over complete/partial/unknown/unavailable and defaults missing/unsupported values to unknown. Fetch finite atom/string aliases without dynamic atom creation, trim required grammatical copy, validate stable unique IDs and machine-readable datetime, use exact `No operator reason recorded` and `Outcome not recorded`, and reject raw structs/exceptions/tokens/hashes rather than inspecting them. Extend table-driven tests for exact maps, order, missing/unknown truth, current/snapshot labels, absolute timestamps, hostile strings, and source prohibitions.</action>
  <verify>
    <automated>mix test test/oban_powertools/web/status_taxonomy_test.exs test/oban_powertools/web/components/data_display_test.exs test/oban_powertools/web/operator_pattern_presenter_test.exs --seed 0</automated>
    <automated>mix format --check-formatted lib/oban_powertools/web/status_taxonomy.ex lib/oban_powertools/web/components/data_display.ex lib/oban_powertools/web/control_plane_presenter.ex test/oban_powertools/web/status_taxonomy_test.exs test/oban_powertools/web/components/data_display_test.exs test/oban_powertools/web/operator_pattern_presenter_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `StatusTaxonomy.spec/2` returns the exact three operator-result maps for atom and binary input and `all_specs/0` remains deterministic and unique.
    - `DataDisplay.status_pill/1` accepts literal domain `:operator_result`, renders the shared specs, and no unrelated DataDisplay domain/state behavior changes.
    - The five named presenter functions accept documented atom/string map aliases, preserve stable order, and emit no undocumented input key.
    - Missing completeness normalizes to `:unknown`; missing audit reason/outcome render the two exact required strings.
    - Source contains no `String.to_atom`, new `String.to_existing_atom`, raw exception inspection, token/hash projection, or generic struct-to-map rendering path.
  </acceptance_criteria>
  <done>The three-state operator-result taxonomy and all five finite presenter normalizers pass deterministic, ordered, atom-safe, and redaction-safe tests.</done>
</task>

<task type="auto">
  <name>Task 78-02-02: Implement AttentionCard, WhyBlocked, and AuditEntry composition</name>
  <files>lib/oban_powertools/web/components/operator_patterns.ex, test/oban_powertools/web/components/operator_patterns_test.exs, assets/oban_powertools/tokens.css, test/oban_powertools/web/theme_tokens_test.exs</files>
  <read_first>
    - @lib/oban_powertools/web/components/operator_patterns.ex
    - @test/oban_powertools/web/components/operator_patterns_test.exs
    - @assets/oban_powertools/tokens.css
    - @test/oban_powertools/web/theme_tokens_test.exs
    - @lib/oban_powertools/web/components/primitives.ex
    - @lib/oban_powertools/web/components/data_display.ex
    - @lib/oban_powertools/web/control_plane_presenter.ex
    - @.planning/phases/74-primitives-library/74-UI-SPEC.md
    - @.planning/phases/77-data-display-operator-patterns/77-UI-SPEC.md
    - @.planning/phases/78-component-groups-meta-components/78-CONTEXT.md
    - @.planning/phases/78-component-groups-meta-components/78-UI-SPEC.md
  </read_first>
  <action>Create the new OperatorPatterns module with `use Phoenix.Component`, aliases for Primitives/DataDisplay/ControlPlanePresenter, and exactly the three functions in this task. `attention_card/1` requires id/title/summary/impact/observed_at/observed_datetime/domain/status/severity (`neutral | info | warning | danger`), defaults completeness to unknown and live to off (`off | polite | assertive`), renders neutral persistent card chrome, distinct domain status and visible severity text/icon, one primary action slot, restrained secondary actions, absolute time, and no live role unless explicitly selected. `why_blocked/1` requires id/summary/impact/observed time, closed `evidence_state: :current | :stale | :unavailable | :permission_denied` (default current), completeness, and blockers; defaults title to `Why this workflow is blocked`, renders direct answer then every normalized blocker/clearing condition then impact/action/freshness/evidence, visibly distinguishes `Current state` from `Block-start snapshot`, and renders truthful stale/unknown/partial/unavailable/permission branches without saying `No blockers` unless complete current evidence explicitly proves an empty list. `audit_entry/1` accepts only id plus one normalized entry map and evidence slot, renders an article with complete sentence heading, operator-result status, visible absolute `&lt;time datetime&gt;`, and DataDisplay description facts for actor/action/target/reason/source/correlation/outcome. All text is ordinary escaped HEEx; caller actions/evidence stay constrained slots; no class/style/rest/raw HTML. Add token-backed root-scoped CSS for the exact three families, 44px actions, min-width/overflow wrapping, neutral-dominant severity markers, 320px single-column stacking, high-contrast non-color boundaries, and no nested cards. Extend and turn green only the Wave 0 cases tagged `phase78_slice: "explanation"` for semantics, exact order/copy, default roles, hostile content, sentinel absence, token ownership, root scope, and four-theme selectors; do not weaken, skip, or delete the still-RED filter/confirmation/detail contracts.</action>
  <verify>
    <automated>mix test test/oban_powertools/web/components/operator_patterns_test.exs --only phase78_slice:explanation --seed 0</automated>
    <automated>mix test test/oban_powertools/web/operator_pattern_presenter_test.exs test/oban_powertools/web/theme_tokens_test.exs --seed 0</automated>
    <automated>mix format --check-formatted lib/oban_powertools/web/components/operator_patterns.ex test/oban_powertools/web/components/operator_patterns_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - The three public functions render their documented semantic roots and fixed information order through existing Primitives/DataDisplay calls.
    - Static AttentionCard output has no alert/status role by default; severity has visible text/icon and remains separate from domain status.
    - WhyBlocked preserves all caller order, current/snapshot labels, and honest unknown/unavailable copy; partial or unavailable evidence never says `No blockers`.
    - AuditEntry contains one article, a complete sentence heading, visible absolute time plus valid `datetime`, and exact missing reason/outcome copy.
    - Every group CSS selector is rooted below `.obpt-root`, visual declarations use `var(--obpt-*)`, ordinary content does not horizontally scroll at 320px, and no raw hex/component spacing is added.
    - The `phase78_slice:explanation` component cases are green while filter, confirmation, and detail remain untouched RED contracts for Plans 78-03..78-05.
  </acceptance_criteria>
  <done>AttentionCard, WhyBlocked, and AuditEntry pass the explanation slice with semantic, truthful, escaped, root-scoped, token-backed, and 320px-safe output.</done>
</task>

<task type="auto">
  <name>Task 78-02-03: Regenerate and prove the packaged explanation/audit CSS</name>
  <files>priv/static/oban_powertools/oban_powertools.css, test/oban_powertools/web/assets_test.exs</files>
  <read_first>
    - @assets/oban_powertools/tokens.css
    - @priv/static/oban_powertools/oban_powertools.css
    - @test/oban_powertools/web/assets_test.exs
    - @lib/mix/tasks/oban_powertools.assets.build.ex
    - @.planning/phases/78-component-groups-meta-components/78-VALIDATION.md
  </read_first>
  <action>Extend asset assertions for representative `.obpt-attention-card`, `.obpt-why-blocked`, and `.obpt-audit-entry` selectors and retain the existing source/static equality and repeat-build SHA checks. Run `mix oban_powertools.assets.build` to regenerate the checked-in packaged CSS from `assets/oban_powertools/tokens.css`; do not edit `priv/static` independently. Prove both builds are byte-stable and that packaged JavaScript is unchanged in this plan.</action>
  <verify>
    <automated>mix oban_powertools.assets.build &amp;&amp; mix test test/oban_powertools/web/assets_test.exs test/oban_powertools/web/theme_tokens_test.exs --seed 0</automated>
    <automated>git diff --exit-code -- priv/static/oban_powertools/oban_powertools.js</automated>
  </verify>
  <acceptance_criteria>
    - Source and packaged CSS both contain the three representative root-scoped group selectors and are byte-equal under the build contract.
    - Two consecutive asset builds yield identical CSS and JS hashes.
    - Packaged JS has no diff, and no generated asset was edited independently.
    - No production LiveView, dependency manifest, package lockfile, schema, route, query, mutation engine, or audit storage file changes.
  </acceptance_criteria>
  <done>Two deterministic asset builds produce byte-equal CSS containing all three explanation/audit groups while packaged JavaScript and protected boundaries remain unchanged.</done>
</task>

</tasks>

<threat_model>
## ASVS L1 Threat Model — blocking threshold: high

| Threat ID | Category | Severity | Mitigation and verification |
|-----------|----------|----------|-----------------------------|
| T-78-02-ATOM | Denial of service | high | Finite string-key normalization and compile-time atom records; tests/source guards reject runtime atom creation. |
| T-78-02-XSS | Injection | high | All presentation copy renders through escaped HEEx; raw HTML and arbitrary attrs are absent and hostile fixtures are asserted. |
| T-78-02-LEAK | Information disclosure | high | Presenters accept only documented redacted fields; full-DOM sentinels reject originals in evidence, details, titles, data attrs, and destinations. |
| T-78-02-TRUTH | Spoofing | high | Unknown completeness defaults unknown; current/snapshot and status/severity remain distinct; audit history never becomes current status or causal proof. |
| T-78-02-ACTION | Elevation of privilege | high | Components expose only constrained action slots and permission-disabled reasons; no authorization or mutation is inferred from presentation state. |
| T-78-02-SUPPLY | Tampering | low | No dependency/schema/package change; generated CSS is rebuilt from source and repeat-hash verified. |
</threat_model>

<verification>
- Run taxonomy/presenter tests first, then component/CSS tests, then deterministic asset build/equality checks.
- Inspect the diff for exactly one new production module plus the named presenter/taxonomy/CSS/test changes and generated CSS.
- Block completion on any high-severity secret, injection, dynamic-atom, false-current-truth, or unsafe-action finding.
</verification>

<success_criteria>
- Operator-result taxonomy and all five presenter normalizers are deterministic, finite, atom-safe, redaction-safe, and covered.
- AttentionCard, WhyBlocked, and AuditEntry satisfy the locked truth/copy/accessibility hierarchy at 320px and across themes.
- Source and packaged CSS are scoped, token-owned, byte-equal, and repeat-build stable.
- No page behavior, server authority, schema, route, query, mutation, dependency, or audit storage boundary changes.
</success_criteria>

<output>
Create `.planning/phases/78-component-groups-meta-components/78-02-SUMMARY.md` when done.
</output>
