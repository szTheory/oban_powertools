---
phase: 78-component-groups-meta-components
plan: 07
type: execute
wave: 7
depends_on:
  - 78-06
files_modified:
  - test/browser/specs/operator-patterns.behavior.spec.ts
autonomous: true
requirements:
  - GROUP-01
  - GROUP-02
  - FORM-04
  - COPY-02
  - A11Y-02
must_haves:
  truths:
    - "D-20..D-23: connected confirmation proves static initial focus, containment, Escape/dismiss/restore, fallback, busy duplicate suppression, stale/replay rejection, result-heading focus/order/recovery, and one exact authoritative receipt."
    - "D-24..D-32: connected FilterBar proves disclosure keyboard/ARIA/tab order, draft versus applied URL/result truth, remove/Clear, exact restrained status, and 320px one-tree overflow safety."
    - "D-33..D-40: connected DetailSurface proves modal/inert constrained behavior, wide modeless/no-trap behavior, safe resize switching, Escape/visible close, history/direct-link/fallback, one tree, and no nested dialog."
    - "D-41..D-56: connected explanation/audit proof covers no default alert, non-color severity, every blocker/clearing condition, current versus snapshot, honest unknowns, absolute time/missing fields, long content, and secret absence."
    - "D-61: behavior—not discovery, static markup, axe, or screenshots—proves the live focus, announcement, history, duplicate-submit, and modality contracts on connected LiveView pages."
    - "D-56: representative connected confirmation, filter, detail, explanation, and audit stories pass a mechanically asserted 200% zoom-equivalent reflow gate with usable visible focus, wrapping, one tree, and no ordinary horizontal overflow."
  artifacts:
    - path: "test/browser/specs/operator-patterns.behavior.spec.ts"
      provides: "Focused connected browser proof for all six groups across 320, tablet adaptive transition, and wide layouts"
  key_links:
    - from: "test/browser/specs/operator-patterns.behavior.spec.ts"
      to: "lib/oban_powertools/web/dev/showcase_live.ex"
      via: "connected events, URL history, LiveView patches, and exact status messages"
      pattern: "phx-connected"
    - from: "test/browser/specs/operator-patterns.behavior.spec.ts"
      to: "assets/oban_powertools/theme.js"
      via: "disclosure and native-dialog focus/mode assertions"
      pattern: "matches(':modal')"
---

<objective>
Complete and execute focused connected Playwright behavior proof for the six operator meta-components.

Purpose: verify the focus, modality, history, duplicate-submit, announcement, responsive, and confidentiality behaviors that render tests, axe, and screenshots cannot establish.
Output: one focused behavior spec green on chromium-320, chromium-tablet, and chromium-wide through the canonical Docker-backed showcase server.
</objective>

<execution_context>
@/Users/jon/.agents/gsd-core/workflows/execute-plan.md
@/Users/jon/.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/78-component-groups-meta-components/78-CONTEXT.md
@.planning/phases/78-component-groups-meta-components/78-RESEARCH.md
@.planning/phases/78-component-groups-meta-components/78-PATTERNS.md
@.planning/phases/78-component-groups-meta-components/78-UI-SPEC.md
@.planning/phases/78-component-groups-meta-components/78-VALIDATION.md
@.planning/phases/78-component-groups-meta-components/78-06-SUMMARY.md
@guides/visual-regression-and-a11y.md
</context>

## Artifacts this phase produces

- Completed `test/browser/specs/operator-patterns.behavior.spec.ts`; no new production file or public API is created.
- New private test helpers may be named `groupStory(id)`, `activateGroupStory(id)`, `with200PercentZoom(page)`, `assertVisibleFocus(locator)`, `assertNoHorizontalOverflow(locator)`, `assertOrdinaryContentWraps(locator)`, `tabbableIds(locator)`, and `allDomStrings(locator)`; these remain test-only.
- New test title families: `group confirmation`, `group filter`, `group detail`, `group explanation`, and `group audit`, with representative titles also containing exact text `200% zoom`, enabling focused grep without duplicating the 23-ID catalog.
- The five `200% zoom` cases are explicitly project-gated to `chromium-wide`; their exact grep must report five passes (not skips), while the full three-project run may skip those five only on 320/tablet because those projects already have their own native reflow cases.
- No screenshot, manifest schema, CSS, JS, LiveView, component, dependency, schema, route, or page artifact is modified by this plan.

<tasks>

<task type="auto">
  <name>Task 78-07-01: Prove connected confirmation focus, authority feedback, and recovery</name>
  <files>test/browser/specs/operator-patterns.behavior.spec.ts</files>
  <read_first>
    - @test/browser/specs/operator-patterns.behavior.spec.ts
    - @test/browser/support/manifest.ts
    - @test/browser/support/showcase.ts
    - @test/browser/specs/forms.behavior.spec.ts
    - @test/browser/specs/data-display.behavior.spec.ts
    - @lib/oban_powertools/web/dev/showcase_live.ex
    - @lib/oban_powertools/web/components/operator_patterns.ex
    - @.planning/phases/78-component-groups-meta-components/78-UI-SPEC.md
  </read_first>
  <action>Complete the confirmation cases using generated `groupStories` and shared one-at-a-time activation on a `.phx-connected` page. Assert initial activeElement is the static title/consequence, computed focus is visible, Tab and Shift-Tab wrap inside the dialog, Escape and the visible safe dismiss close dismissible states, focus returns to the invoker, and removing the invoker before close focuses the caller fallback. Submit blank/short reason and wrong count and assert exact inline errors/no accepted result; submit valid reason/count twice rapidly and assert one busy transition, disabled activation, one server result/receipt, and no second receipt. Exercise non-dismissible pending copy and ensure Escape does not claim cancellation. Patch partial results and assert focus moves to result heading, supplied success/failed/skipped order remains distinct, recovery/audit destinations are present, and dialog remains open. Exercise drifted recovery requiring explicit fresh preview with safe reason preservation. Assert the exact success receipt says only requested/recorded facts and never `completed`/`fixed`; scan full text/attributes/details/destinations for the secret/token/hash/raw-error sentinels. Add `group confirmation 200% zoom` on a connected long-copy/count story, project-gated to `chromium-wide` with an assertion that the exact zoom grep cannot silently skip it. Implement `with200PercentZoom(page)` with a Chromium CDP device-metrics override that preserves the wide project's physical pixel budget while halving its CSS viewport and setting device scale factor 2; assert the resulting `innerWidth`, `devicePixelRatio`, and `.phx-connected` preconditions before testing. Mechanically assert the action row becomes separate vertical rows, consequence/scope/labels wrap without clipping, focused dismiss/confirm controls remain fully inside the visual viewport with visible focus, exactly one dialog tree exists, and document/story ordinary scrollWidth stays within clientWidth + 1px.</action>
  <verify>
    <automated>npm run showcase:manifest &amp;&amp; scripts/with-showcase-server.sh scripts/playwright-docker.sh npx playwright test test/browser/specs/operator-patterns.behavior.spec.ts --grep "group confirmation" --project chromium-320 --project chromium-wide</automated>
  </verify>
  <acceptance_criteria>
    - 320 and wide connected cases prove static initial focus, visible focus, Tab wrap, dismissible Escape/visible close, invoker restore, and removed-invoker fallback.
    - Invalid reason/count yields no accepted result; duplicate valid activation yields one busy/result/receipt sequence.
    - Partial result heading receives focus, rows remain success/failed/skipped in deterministic order, and recovery/audit remain visible with dialog open.
    - Non-dismissible copy is explicit, stale preview requires explicit refresh, receipt never overclaims host completion, and no sensitive sentinel exists anywhere in DOM channels.
    - The connected confirmation 200% zoom case proves the zoom precondition, stacked action geometry, wrapped consequence/form copy, usable visible focus, one dialog tree, and no ordinary horizontal overflow.
  </acceptance_criteria>
  <done>Connected confirmation behavior passes at 320 and wide for focus, dismissal/restoration, validation, duplicate suppression, partial/stale recovery, confidentiality, and asserted 200% zoom reflow.</done>
</task>

<task type="auto">
  <name>Task 78-07-02: Prove FilterBar and adaptive DetailSurface live behavior</name>
  <files>test/browser/specs/operator-patterns.behavior.spec.ts</files>
  <read_first>
    - @test/browser/specs/operator-patterns.behavior.spec.ts
    - @test/browser/support/showcase.ts
    - @test/browser/specs/shell.behavior.spec.ts
    - @assets/oban_powertools/theme.js
    - @assets/oban_powertools/tokens.css
    - @lib/oban_powertools/web/dev/showcase_live.ex
    - @lib/oban_powertools/web/components/operator_patterns.ex
    - @.planning/phases/78-component-groups-meta-components/78-CONTEXT.md
  </read_first>
  <action>Add FilterBar cases for Enter/Space disclosure activation, exact `aria-expanded`/`aria-controls`, collapsed descendants absent from tab order, applied filters/result summary visible outside collapse, and &lt;=1px page/story overflow at 320. Change a submit-mode draft and assert results/URL/status unchanged plus exact `Changes not applied.`; Apply once and assert canonical URL/pagination/result/status change; test named remove/Clear and exact one restrained announcement; prove instant mode has no Apply and does not change semantics at a breakpoint. Add DetailSurface cases using native `dialog.matches(':modal')`: constrained adaptive/detail-modal is modal, background cannot receive focus, Tab stays contained, Escape/visible close routes parent state and restores invoker/fallback; wide adaptive is open but not modal, has no aria-modal/inertness/trap, and focus can move back to results. Resize wide -> tablet/320 -> wide and assert close-before-reopen produces the correct mode with one dialog tree and preserves safe focus. Exercise first-open push, selection-switch replace, explicit-close replace with filters, Back close, direct-link fallback, exact loaded status, all content-state/long-content branches, and drawer-to-confirmation transition with at most one dialog. Add connected `group filter 200% zoom` and `group detail 200% zoom` cases through `with200PercentZoom(page)`: require fields/actions/applied pairs and long labels to wrap/stack, filter focus and disclosure tab order to remain usable, adaptive detail to become modal/full available width with one body tree and one ordinary scroll region, close focus to remain visible/in-bounds, and document/story ordinary scrollWidth to stay within clientWidth + 1px. Use bounding boxes, computed styles, focus/DOM APIs, and scroll measurements—not visual assumptions.</action>
  <verify>
    <automated>npm run showcase:manifest &amp;&amp; scripts/with-showcase-server.sh scripts/playwright-docker.sh npx playwright test test/browser/specs/operator-patterns.behavior.spec.ts --grep "group (filter|detail)" --project chromium-320 --project chromium-tablet --project chromium-wide</automated>
  </verify>
  <acceptance_criteria>
    - Filter disclosure keyboard/ARIA/tab order and one-tree 320 overflow checks pass; draft, Apply, remove, Clear, URL, and exact status behavior remain parent-owned and truthful.
    - Constrained detail is native modal/inert/contained and wide detail is modeless/non-inert/no-trap; both close visibly and restore invoker/fallback.
    - Resize changes effective native mode safely with one tree and no stale aria-modal or stacked dialogs.
    - History/direct-link/filter preservation, exact loaded status, explicit content states, long content, and drawer-to-confirmation no-nesting all pass on connected pages.
    - Filter and detail 200% zoom cases prove effective zoom preconditions, stacked/wrapped geometry, keyboard/focus usability, full-width modal detail with one tree, and no ordinary horizontal overflow.
  </acceptance_criteria>
  <done>Connected filter/detail behavior passes across 320, tablet, and wide for disclosure, URL truth, native modality, resize, focus/history, one-tree transitions, and both 200% zoom cases.</done>
</task>

<task type="auto">
  <name>Task 78-07-03: Prove explanation/audit truth, announcements, non-color meaning, and confidentiality</name>
  <files>test/browser/specs/operator-patterns.behavior.spec.ts</files>
  <read_first>
    - @test/browser/specs/operator-patterns.behavior.spec.ts
    - @test/browser/support/showcase.ts
    - @lib/oban_powertools/web/components/operator_patterns.ex
    - @lib/oban_powertools/web/control_plane_presenter.ex
    - @assets/oban_powertools/tokens.css
    - @.planning/phases/78-component-groups-meta-components/78-UI-SPEC.md
    - @.planning/phases/78-component-groups-meta-components/78-VALIDATION.md
  </read_first>
  <action>Add explanation/audit cases that assert static AttentionCard has no alert/status role, while explicit dynamic polite/assertive variants use the requested role without moving focus. Prove status and severity have distinct visible text plus icon/border/programmatic state, not color alone, and permission-disabled actions remain discoverable with reason. Assert WhyBlocked renders every supplied blocker and clearing condition in order, distinguishes Current state from Block-start snapshot, and for unknown/partial/stale/unavailable/permission evidence never emits `No blockers` or `root cause`; small redaction-safe details are keyboard operable and large evidence is absent in favor of a Forensics link. Assert AuditEntry is one article with complete sentence heading, explicit outcome, absolute visible time plus valid datetime, actor/action/target/reason/source/correlation facts, exact missing reason/outcome copy, and no current-status/causality claim. At 320 and wide, verify long Unicode/RTL/IDs wrap, action target/focus is usable, no ordinary horizontal overflow, exact live message strings/cardinality, and the unique original-secret/raw-error/token/hash sentinel is absent from all page text, HTML attributes, titles, hidden/details content, URLs, and copy-related fields. Add connected `group explanation 200% zoom` and `group audit 200% zoom` cases through the asserted zoom helper; mechanically prove prose/IDs/facts/timestamps/action labels wrap rather than clip, one primary/technical-evidence action remains keyboard reachable with visible in-bounds focus, article/card trees are not duplicated, and ordinary document/story surfaces have no horizontal overflow.</action>
  <verify>
    <automated>npm run showcase:manifest &amp;&amp; scripts/with-showcase-server.sh scripts/playwright-docker.sh npx playwright test test/browser/specs/operator-patterns.behavior.spec.ts --grep "group (explanation|audit)" --project chromium-320 --project chromium-wide</automated>
    <automated>npm run showcase:manifest &amp;&amp; scripts/with-showcase-server.sh scripts/playwright-docker.sh npx playwright test test/browser/specs/operator-patterns.behavior.spec.ts --grep "200% zoom" --project chromium-wide</automated>
    <automated>npm run showcase:manifest &amp;&amp; scripts/with-showcase-server.sh scripts/playwright-docker.sh npx playwright test test/browser/specs/operator-patterns.behavior.spec.ts --project chromium-320 --project chromium-tablet --project chromium-wide</automated>
  </verify>
  <acceptance_criteria>
    - Static attention has no live role; explicit live roles have exact message cardinality and do not steal focus.
    - Severity/status/permission state is visible and programmatic without color-only dependence.
    - Every blocker/clearing condition/current-snapshot distinction and honest unknown/unavailable prohibition is proven.
    - Audit semantic time/facts/missing copy are exact and immutable-history copy never becomes current status or causal proof.
    - Full three-project behavior run passes with no secret/raw-error/token/hash sentinel or ordinary page/story horizontal overflow.
    - The exact `200% zoom` grep executes five connected representative stories as five passes on `chromium-wide` (not skipped) and records effective zoom, wrapping/stacking, focus usability, one-tree, and no-overflow assertions.
  </acceptance_criteria>
  <done>The full three-project behavior spec and five-pass wide zoom grep prove explanation/audit truth, exact announcements, non-color meaning, confidentiality, wrapping, focus usability, and no ordinary overflow.</done>
</task>

</tasks>

<threat_model>
## ASVS L1 Threat Model — blocking threshold: high

| Threat ID | Category | Severity | Mitigation and verification |
|-----------|----------|----------|-----------------------------|
| T-78-07-AUTH | Spoofing / elevation | high | Live invalid/valid/stale/permission transitions prove server-owned validation and authority rather than client affordances. |
| T-78-07-REPLAY | Tampering | high | Duplicate activation and stale/fresh-preview behavior are exercised on connected pages with exact call/result/receipt cardinality. |
| T-78-07-LEAK | Information disclosure | high | Browser scans complete DOM channels, details, URLs, and copy fields for original secret/token/hash/raw-error sentinels. |
| T-78-07-FOCUS | Denial of service / accessibility | high | Real keyboard/focus/inertness/resize/restore/no-wide-trap/nested-dialog behavior blocks completion. |
| T-78-07-ZOOM | Denial of service / accessibility | high | Five connected 200% zoom-equivalent cases assert their effective scale/reflow precondition, wrapped/stacked geometry, in-bounds visible focus, one tree, and ordinary overflow absence. |
| T-78-07-TRUTH | Spoofing | high | Exact copy/cardinality checks reject overclaimed receipts, inferred blockers/causality, hidden missing audit fields, and chatty announcements. |
| T-78-07-URL | Tampering | high | Connected history tests distinguish push/replace/Back/direct behavior and filter preservation under parent control. |
</threat_model>

<verification>
- Discover the behavior spec first, then execute confirmation, filter/detail, explanation/audit, the exact five-story `200% zoom` grep, and the full three-project Docker-backed run.
- Block on any high-severity server-authority, replay, leak, focus/modality, copy-truth, or URL-history failure.
- Confirm no screenshot, production code, asset, manifest schema, dependency, schema, route, or page file is changed.
</verification>

<success_criteria>
- All live transitions static tests cannot prove are exercised after `.phx-connected` on their required projects.
- Focus, inertness, Escape, restoration, resize modality, URL history, duplicate submission, status messages, non-color state, long content, representative 200% zoom reflow, and secret absence all pass.
- The full focused behavior spec is green on chromium-320, chromium-tablet, and chromium-wide through the canonical Docker-backed server.
- No implementation or baseline change is hidden in this evidence-only plan.
</success_criteria>

<output>
Create `.planning/phases/78-component-groups-meta-components/78-07-SUMMARY.md` when done.
</output>
