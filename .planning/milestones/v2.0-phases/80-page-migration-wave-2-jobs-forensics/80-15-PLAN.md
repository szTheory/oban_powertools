---
phase: 80-page-migration-wave-2-jobs-forensics
plan: 15
type: execute
wave: 12
depends_on:
  - 80-13
files_modified:
  - lib/oban_powertools/web/jobs_params.ex
  - lib/oban_powertools/jobs/batch_coordinator.ex
  - lib/oban_powertools/web/jobs_live.ex
  - test/oban_powertools/web/jobs_params_test.exs
  - test/oban_powertools/jobs/batch_coordinator_test.exs
  - test/oban_powertools/web/live/jobs_live_test.exs
autonomous: true
gap_closure: true
requirements: []
must_haves:
  truths:
    - "Jobs rejects a page whose 20-row offset exceeds signed 64-bit Postgrex bounds and rejects a job ID above signed 64-bit bounds before either value reaches Repo."
    - "Oversized positive page and job URL values canonicalize to state=available, remove the unsafe value, and show the existing finite invalid-URL notice without crashing the connected LiveView."
    - "Every ready batch target produces exactly one terminal result with its original frozen position, including authorization timeout or outer task exit after later positions finish."
    - "JobsLive validates the exact unique ready-position set before reconciliation and fails closed with interruption/Audit recovery truth instead of raising through Map.fetch!/2."
    - "The real Lifeline coordinator integration test uses an explicit bounded database-appropriate receive timeout and the complete owning suite passes repeatedly."
  artifacts:
    - path: "lib/oban_powertools/web/jobs_params.ex"
      provides: "Distinct signed-64-bit-safe page and job URL parsers"
    - path: "lib/oban_powertools/jobs/batch_coordinator.ex"
      provides: "Target-associated terminal result handling and exact frozen-position reconciliation"
    - path: "lib/oban_powertools/web/jobs_live.ex"
      provides: "Fail-closed exact position-set validation before bulk result join"
    - path: "test/oban_powertools/web/jobs_params_test.exs"
      provides: "Boundary and 50-digit page/job parser regressions"
    - path: "test/oban_powertools/jobs/batch_coordinator_test.exs"
      provides: "Later-finishes-first authorization-timeout regression and stable integration timing"
    - path: "test/oban_powertools/web/live/jobs_live_test.exs"
      provides: "Connected oversized URL and malformed terminal-position recovery regressions"
  key_links:
    - from: "lib/oban_powertools/web/jobs_params.ex"
      to: "lib/oban_powertools/jobs.ex"
      via: "safe page offset and job ID before list/get Repo calls"
      pattern: "parse_page|parse_job_id"
    - from: "lib/oban_powertools/jobs/batch_coordinator.ex"
      to: "preview.ready"
      via: "each async stream terminal tuple remains paired with the originating target"
      pattern: "target.position"
    - from: "lib/oban_powertools/web/jobs_live.ex"
      to: "execution_results"
      via: "exact unique ready-position set validation before map lookup"
      pattern: "execution_positions_valid"
---

<objective>
Close WR-01, WR-02, and V-TEST-01 at the Jobs URL and frozen-result boundaries.

Purpose: prevent oversized URL denial of service, preserve target identity across every batch terminal path, and make the owning integration evidence repeatable.
Output: bounded page/job parsers, exact terminal positions, non-crashing reconciliation, and adversarial connected/coordinator regressions.
</objective>

<execution_context>
@/Users/jon/.agents/gsd-core/workflows/execute-plan.md
@/Users/jon/.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/80-page-migration-wave-2-jobs-forensics/80-CONTEXT.md
@.planning/phases/80-page-migration-wave-2-jobs-forensics/80-RESEARCH.md
@.planning/phases/80-page-migration-wave-2-jobs-forensics/80-PATTERNS.md
@.planning/phases/80-page-migration-wave-2-jobs-forensics/80-UI-SPEC.md
@.planning/phases/80-page-migration-wave-2-jobs-forensics/80-VERIFICATION.md
@.planning/phases/80-page-migration-wave-2-jobs-forensics/80-01-SUMMARY.md
@.planning/phases/80-page-migration-wave-2-jobs-forensics/80-04-SUMMARY.md
</context>

## Must Haves — Gap and Decision Coverage

- **WR-01 / D-25, D-29, D-74, D-80:** URL integers must be safe for the exact Postgrex bindings they drive before canonical applied state reaches `Jobs.list/3` or `Jobs.get/2`.
- **WR-02 / D-38, D-46, D-48, D-50, D-52, D-75:** every terminal result retains frozen identity and recovery never crashes after effects.
- **V-TEST-01:** database-backed supervised integration uses an explicit timeout and proves the complete owning file repeatedly without sleep-based synchronization.
- **FORM-03:** invalid URL state canonicalizes through the existing finite Jobs filter notice; draft behavior and valid URL serialization stay unchanged.

## Artifacts this phase produces

- New module attributes `@max_int64 9_223_372_036_854_775_807` and `@max_page div(@max_int64, @page_size) + 1` in `JobsParams`.
- New private `parse_page/1`, `parse_job_id/1`, and `parse_bounded_positive_integer/3` in `JobsParams`; the existing generic unbounded `parse_positive_integer/2` is removed.
- New private `execution_positions_valid?/2` in `JobsLive`; it compares count and exact unique ready-position sets before any execution-result map lookup.
- New private `reconcile_execution_results/2` in `BatchCoordinator`; it returns exactly one safe terminal result per originating ready target.
- Existing test files gain boundary, 50-digit URL, outer-authorization-timeout, duplicate/missing-position, and explicit receive-timeout assertions.
- No new public API, file, route, URL key, configuration flag, schema, migration, dependency, durable ledger, story, ARIA snapshot, PNG, or visual baseline is created.

<tasks>

<task type="auto" tdd="true">
  <name>Task 80-15-01: Bound Jobs page offsets and job IDs before Repo</name>
  <files>lib/oban_powertools/web/jobs_params.ex, test/oban_powertools/web/jobs_params_test.exs, test/oban_powertools/web/live/jobs_live_test.exs</files>
  <read_first>
    - @lib/oban_powertools/web/jobs_params.ex
    - @lib/oban_powertools/jobs.ex
    - @lib/oban_powertools/web/jobs_live.ex
    - @lib/oban_powertools/web/selectors.ex
    - @test/oban_powertools/web/jobs_params_test.exs
    - @test/oban_powertools/web/live/jobs_live_test.exs
    - @test/oban_powertools/jobs_test.exs
    - @.planning/phases/80-page-migration-wave-2-jobs-forensics/80-VERIFICATION.md
    - @.planning/phases/80-page-migration-wave-2-jobs-forensics/80-CONTEXT.md
  </read_first>
  <action>Write red parser tests for the exact signed-64-bit boundaries and connected LiveView tests for the reproduced 50-digit values. Define `@max_int64` as `9_223_372_036_854_775_807` and `@max_page` as `div(@max_int64, @page_size) + 1`, so the greatest accepted offset `(page - 1) * 20` is encodable by Postgrex. Replace the two calls to unbounded `parse_positive_integer/2` with distinct `parse_page/1` and `parse_job_id/1`, both delegating to `parse_bounded_positive_integer/3`; page accepts only `1..@max_page`, job accepts only `1..@max_int64`, for integer and full-string binary inputs. Values outside those ranges return the existing defaults (`page: 1`, `quick_review_id: nil`), set the existing invalid flags, are absent from canonical params, and emit only `@invalid_url_notice`; do not truncate, clamp, parse prefixes, or add new copy.

In `JobsLive` connected tests, visit each direct URL separately with the exact 50-digit page and job strings from `80-VERIFICATION.md`. Assert the LiveView stays connected, the URL is replaced with canonical `state=available` and contains neither oversized value, the heading `Some filters were not applied` plus existing body renders once, and the unsafe job value never reaches the detail/quick-review Repo lookup. Preserve all valid boundary values, normal page/review history, invalid draft retention, and the eight-key URL allowlist.</action>
  <verify>
    <automated>mix test test/oban_powertools/web/jobs_params_test.exs test/oban_powertools/web/live/jobs_live_test.exs --seed 0</automated>
    <automated>mix test test/oban_powertools/jobs_test.exs test/oban_powertools/web/selectors_test.exs --seed 0</automated>
    <automated>mix format --check-formatted lib/oban_powertools/web/jobs_params.ex test/oban_powertools/web/jobs_params_test.exs test/oban_powertools/web/live/jobs_live_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - Job ID `9_223_372_036_854_775_807` and page `div(9_223_372_036_854_775_807, 20) + 1` parse and round-trip; each boundary plus one is invalid.
    - Both exact 50-digit verification values are removed before Repo binding, produce canonical safe defaults, and emit exactly the existing invalid-URL notice.
    - Direct connected page and job URLs remain connected, replace history safely, expose no unsafe value in the canonical URL, and render no database encoding exception.
    - Negative, zero, malformed, prefix-suffixed, and oversized integer/binary values share the bounded failure path without changing valid URL or draft semantics.
    - No page-size, sorting, cursor, URL-key, route, or query predicate change is introduced.
  </acceptance_criteria>
  <done>Every Jobs page offset and quick-review ID accepted from a URL is encodable by its Postgrex binding before any Repo call.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 80-15-02: Preserve frozen target positions and fail closed during bulk reconciliation</name>
  <files>lib/oban_powertools/jobs/batch_coordinator.ex, lib/oban_powertools/web/jobs_live.ex, test/oban_powertools/jobs/batch_coordinator_test.exs, test/oban_powertools/web/live/jobs_live_test.exs</files>
  <read_first>
    - @lib/oban_powertools/jobs/batch_coordinator.ex
    - @lib/oban_powertools/web/jobs_live.ex
    - @lib/oban_powertools/jobs.ex
    - @lib/oban_powertools/lifeline.ex
    - @lib/oban_powertools/audit.ex
    - @test/oban_powertools/jobs/batch_coordinator_test.exs
    - @test/oban_powertools/web/live/jobs_live_test.exs
    - @test/oban_powertools/lifeline_test.exs
    - @.planning/phases/80-page-migration-wave-2-jobs-forensics/80-VERIFICATION.md
    - @.planning/phases/80-page-migration-wave-2-jobs-forensics/80-CONTEXT.md
  </read_first>
  <action>First add a message-barrier regression with three frozen ready targets at positions 0, 1, and 2. Make authorization for position 1 block past `test_target_timeout_ms`, allow positions 0 and 2 to finish first, and assert exact unique returned positions `[0, 1, 2]`, success for 0 and 2, a finite failed result for 1, correct aggregates, no coordinator/owner crash, and no raw exit or target identity in aggregate messages/telemetry. Do not use `Process.sleep/1`.

Refactor `execute_targets/9` so an outer `async_stream_nolink` terminal tuple is always paired with the originating member of `preview.ready`. Keep maximum concurrency 4 and bounded timeout. Use ordered stream association (or an explicit task-ref-to-target map) rather than `length(results)`; on `{:exit, reason}`, create `safe_execution_result(target.position, :failed, :crashed)` for that exact target. Bound authorization plus Lifeline execution inside the per-target timeout so a stalled authorization returns a finite terminal result. Pass the terminal list through new `reconcile_execution_results/2`, which requires one result for every ready target and returns results in the exact ready/frozen position order with no duplicate or missing position. Never derive position from completion count.

Before `JobsLive.finalize_bulk_execution/2` builds `execution_by_position`, call new `execution_positions_valid?/2` against the ready positions in `preview.results`. Require equal counts, unique integer positions, and exact set equality. On any mismatch, call the existing submitting-state `assign_bulk_failure(socket, :execution_unavailable)` path so the dialog remains failed with the exact interruption/Audit recovery summary and the existing selection remains available for a fresh preview; do not call `Map.fetch!/2`, claim success, or discard unresolved IDs. Add LiveView regressions for duplicate `[0, 2, 2]` and missing `[0, 2]` terminal sets.

Finally change the database-backed test `real Lifeline preview and execution run once per target with independent Audit evidence` so its preview completion `assert_receive` has an explicit `1_000` ms timeout. Retain the existing 5,000 ms bounded completion helper. Prove the entire coordinator suite three consecutive times; do not globally raise ExUnit timeouts or mask actual deadlocks.</action>
  <verify>
    <automated>mix test test/oban_powertools/jobs/batch_coordinator_test.exs test/oban_powertools/web/live/jobs_live_test.exs --seed 0</automated>
    <automated>for attempt in 1 2 3; do mix test test/oban_powertools/jobs/batch_coordinator_test.exs --seed 0 || exit 1; done</automated>
    <automated>mix test test/oban_powertools/auth_test.exs test/oban_powertools/application_test.exs test/oban_powertools/lifeline_test.exs --seed 0</automated>
    <automated>mix format --check-formatted lib/oban_powertools/jobs/batch_coordinator.ex lib/oban_powertools/web/jobs_live.ex test/oban_powertools/jobs/batch_coordinator_test.exs test/oban_powertools/web/live/jobs_live_test.exs</automated>
    <automated>git diff --check -- lib/oban_powertools/jobs/batch_coordinator.ex lib/oban_powertools/web/jobs_live.ex test/oban_powertools/jobs/batch_coordinator_test.exs test/oban_powertools/web/live/jobs_live_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - No production terminal path assigns `length(results)` or completion order as a frozen target position.
    - The stalled-authorization/later-finishes-first regression returns exactly `[0, 1, 2]` once each, with truthful outcomes and aggregates.
    - Normal out-of-order, timeout, crash, drift, owner-death, max-four, message-confidentiality, and stable-order coordinator tests remain green.
    - Duplicate, missing, extra, or non-integer execution positions do not reach `Map.fetch!/2`, do not close the dialog, do not claim success, and render the existing interruption/Audit recovery truth.
    - The real Lifeline preview assertion uses an explicit 1,000 ms timeout, while three consecutive complete coordinator-file runs pass without sleeps, retries inside tests, or global timeout inflation.
    - Every accepted target effect remains independently audited; selection/recovery remains fresh-preview-only after any reconciliation failure.
  </acceptance_criteria>
  <done>Bulk completion preserves exact frozen identity on every terminal path and post-effect reconciliation cannot crash or fabricate recovery truth.</done>
</task>

</tasks>

<threat_model>
## ASVS L1 Assets

- Database availability, canonical Jobs URL truth, frozen batch membership/order, post-effect result integrity, durable per-target Audit evidence, and operator recovery state.

## Trust Boundaries

| Boundary | Untrusted input / protected output |
|---|---|
| Jobs URL -> Postgrex | arbitrary-size positive integers -> signed-64-bit-safe page/job bindings |
| Async task terminal -> coordinator | unordered success/exit/timeout -> exact originating frozen position |
| Coordinator result -> LiveView | possibly malformed terminal set -> exact validated join or failed Audit-based recovery |

## STRIDE Threat Register — blocking threshold: high

| Threat ID | Category | Severity | Disposition | Mitigation and verification |
|---|---|---|---|---|
| T-80-15-WR01 | Denial of service | high | mitigate | Separate bounded parsers reject unsafe page offsets and job IDs before Repo; 50-digit connected regressions remain live and canonical. |
| T-80-15-WR02 | Tampering | high | mitigate | Every terminal tuple remains associated with its originating target and exact unique position sets are validated twice. |
| T-80-15-RECOVERY | Repudiation | high | mitigate | Malformed results fail closed with interruption/Audit truth and retain selection instead of crashing or claiming clean completion. |
| T-80-15-CONF | Information disclosure | high | mitigate | Existing finite result copy and low-cardinality messages/telemetry remain unchanged; raw exits, IDs, tokens, reasons, and errors stay private. |
| T-80-15-REPLAY | Elevation of privilege | high | mitigate | Reconciliation failure cannot replay capabilities; any subsequent action requires fresh authorization and preview. |
| T-80-15-FLAKE | Security misconfiguration | high | mitigate | Explicit local integration timeout plus three complete deterministic suite runs distinguish bounded database latency from deadlock. |
</threat_model>

<verification>
- Run both task gates, three consecutive coordinator suites, and existing auth/application/Lifeline compatibility suites.
- Inspect source for no unbounded integer parser, no completion-count position, no unchecked `Map.fetch!/2`, and no global timeout change.
- Inspect the six-file scope for no ORM schema, migration, dependency, route, fixture, story, screenshot, or generated asset.
</verification>

<success_criteria>
- WR-01 and WR-02 reproduce red and close green through their exact adversarial cases.
- V-TEST-01 is repeatable with a bounded local receive timeout and no timing sleep.
- Existing Jobs URL, bulk authority, audit, confidentiality, and recovery contracts remain compatible.
- No high threat remains unresolved.
</success_criteria>

<output>
Create `.planning/phases/80-page-migration-wave-2-jobs-forensics/80-15-SUMMARY.md` when done.
</output>
