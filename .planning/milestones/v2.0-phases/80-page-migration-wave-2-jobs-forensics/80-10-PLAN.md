---
phase: 80-page-migration-wave-2-jobs-forensics
plan: 10
type: execute
wave: 8
depends_on:
  - 80-08
files_modified:
  - examples/phoenix_host/config/test.exs
  - examples/phoenix_host/lib/phoenix_host_web/router.ex
  - examples/phoenix_host/test/support/phase80_browser_fixtures.ex
  - examples/phoenix_host/test/phase80_browser_fixtures_test.exs
  - scripts/with-showcase-server.sh
  - scripts/playwright-docker.sh
  - test/browser/support/phase80-fixtures.ts
  - test/browser/specs/phase80-fixtures.spec.ts
autonomous: true
requirements:
  - PAGE-02
  - PAGE-09
  - FORM-03
  - PAGE-10
  - A11Y-*
must_haves:
  truths:
    - "The Phase 80 fixture seam is opt-in test-only, secret-protected, POST-only, closed-schema, run/project-isolated, and absent from production/Hex."
    - "Phase 79 fixture routes/specs remain compatible and green while one shared disposable database/build/server carries both fixture generations."
    - "The complete host fixture test file runs real tests in route-on and route-off modes; no tag-selected or zero-test success is accepted."
    - "Host fixtures, secret transport, launcher/shared-port ownership, and fail-closed clients are settled before connected production-route acceptance or package wiring."
  artifacts:
    - path: "examples/phoenix_host/test/support/phase80_browser_fixtures.ex"
      provides: "Test-only deterministic Jobs/Forensics reset, actor, batch barrier, and public evidence seam"
    - path: "test/browser/support/phase80-fixtures.ts"
      provides: "Closed fail-closed Phase 80 fixture client"
  key_links:
    - from: "scripts/with-showcase-server.sh"
      to: "examples/phoenix_host/test/support/phase80_browser_fixtures.ex"
      via: "test-only flag, disposable DB, and ephemeral secret"
      pattern: "PHASE80_BROWSER_FIXTURES|PHASE80_BROWSER_FIXTURE_SECRET"
    - from: "test/browser/support/phase80-fixtures.ts"
      to: "examples/phoenix_host/lib/phoenix_host_web/router.ex"
      via: "closed authenticated POST endpoints"
      pattern: "__phase80_browser_fixtures__"
---

<objective>
Build the isolated authenticated Phase 80 host fixture, launcher/secret transport, and fail-closed browser client contract.

Purpose: stabilize the shared disposable server and fixture bridge before Plan 80-11 consumes it for production-route acceptance.
Output: Phase 80 sibling fixture seam, complete on/off host contract, dual-secret launcher, fail-closed TS client, and connected fixture proof.
</objective>

<execution_context>
@/Users/jon/.agents/gsd-core/workflows/execute-plan.md
@/Users/jon/.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/80-page-migration-wave-2-jobs-forensics/80-CONTEXT.md
@.planning/phases/80-page-migration-wave-2-jobs-forensics/80-RESEARCH.md
@.planning/phases/80-page-migration-wave-2-jobs-forensics/80-PATTERNS.md
@.planning/phases/80-page-migration-wave-2-jobs-forensics/80-UI-SPEC.md
@.planning/phases/80-page-migration-wave-2-jobs-forensics/80-VALIDATION.md
@.planning/phases/80-page-migration-wave-2-jobs-forensics/80-04-SUMMARY.md
@.planning/phases/80-page-migration-wave-2-jobs-forensics/80-07-SUMMARY.md
@.planning/phases/80-page-migration-wave-2-jobs-forensics/80-08-SUMMARY.md
@.planning/phases/79-page-migration-wave-1-overview-cron-limiters-audit/79-10-SUMMARY.md
</context>

## Must Haves — Decision Coverage

- **D-06, D-77:** native production routes remain separate from test-only fixture routes and every fixture request is server authenticated.
- **D-36 through D-52:** deterministic fixtures and barriers cover frozen scope, target classes, supervision, progress, disconnect, and recovery without becoming execution authority.
- **D-58 through D-72:** deterministic data covers all four Forensics families, bounded windows, unavailable equality, legal links, and confidentiality sentinels.
- **D-76, D-78, D-79, D-80, D-86, D-87, D-88:** no dependency/schema/production route surprise, closed public evidence, bounded data, adversarial states, and browser-ready contracts.

## Artifacts this phase produces

- New test-only module/file `examples/phoenix_host/test/support/phase80_browser_fixtures.ex`, containing `PhoenixHostWeb.Phase80BrowserFixtures` and `PhoenixHostWeb.Phase80BrowserFixturesController`.
- New domain functions `reset/1`, `set_actor/2`, `control_batch/3`, and `public_evidence/2`; controller actions `reset/2`, `actor/2`, `batch/2`, and `evidence/2`.
- New opt-in POST scope `/__phase80_browser_fixtures__/{reset,actor,batch,evidence}`, compiled only for `Mix.env() == :test` and `PHASE80_BROWSER_FIXTURES=1`, through the existing API pipeline.
- New ephemeral environment variable `PHASE80_BROWSER_FIXTURE_SECRET`, sent only as header `x-phase80-fixture-secret` and forwarded into Docker by name.
- New TypeScript client `test/browser/support/phase80-fixtures.ts` with exact reset/actor/batch/evidence response types and local missing-secret failure.
- New fixture proof `test/browser/specs/phase80-fixtures.spec.ts`.
- Plan 80-11 owns `test/browser/specs/page-migration-wave-2.spec.ts` and package-script wiring after this launcher/client contract is green.
- The existing Phase 79 fixture routes, secret, helper, specs, response schema, and compatibility aliases remain unchanged.
- No production route, GET/resource/catch-all endpoint, dependency/lockfile, schema/migration, fixture credential value, preview token, plan hash, raw error, or package file is produced.

<tasks>

<task type="auto" tdd="true">
  <name>Task 80-10-01: Add an opt-in isolated Phase 80 host fixture seam</name>
  <files>examples/phoenix_host/config/test.exs, examples/phoenix_host/lib/phoenix_host_web/router.ex, examples/phoenix_host/test/support/phase80_browser_fixtures.ex, examples/phoenix_host/test/phase80_browser_fixtures_test.exs</files>
  <read_first>
    - @examples/phoenix_host/config/test.exs
    - @examples/phoenix_host/lib/phoenix_host_web/router.ex
    - @examples/phoenix_host/test/support/phase80_browser_fixtures.ex (new; confirm absent before creation)
    - @examples/phoenix_host/test/phase80_browser_fixtures_test.exs (new; confirm absent before creation)
    - @examples/phoenix_host/test/support/phase79_browser_fixtures.ex
    - @examples/phoenix_host/test/phase79_browser_fixtures_test.exs
    - @examples/phoenix_host/lib/phoenix_host_web/oban_powertools_auth.ex
    - @examples/phoenix_host/mix.exs
    - @lib/oban_powertools/jobs.ex
    - @lib/oban_powertools/jobs/batch_coordinator.ex
    - @lib/oban_powertools/lifeline.ex
    - @lib/oban_powertools/forensics.ex
    - @lib/oban_powertools/audit.ex
    - @.planning/phases/80-page-migration-wave-2-jobs-forensics/80-UI-SPEC.md
  </read_first>
  <action>Create a sibling Phase 80 fixture domain/controller following the Phase 79 fail-closed pattern. Authenticate `x-phase80-fixture-secret` with constant-time comparison before parsing; missing/wrong secret and disabled route return the same empty 404. Add exactly four POST routes under compile-time test+flag gating. `reset/1` validates closed project/run names, transactionally removes only tagged rows, and seeds fixed deterministic data: seven job states; exact 20-row final-page boundary; counts in the thousands while current reads stay 20; cross-page IDs; under/over 100 limits; eligible/excluded/drifted/expired/consumed/skipped/failed/success targets; sensitive sentinels in args/meta/output/error/reason/runbook; all four forensic types; 55-event workflow/incident windows; missing/unauthorized equality; long Unicode/RTL. `set_actor/2` supports only ops and restricted. `control_batch/3` supports only hold/release/perturb/status for the current run and exposes no execution identity. `public_evidence/2` returns closed counts/states/audit completion only. Use DB/message barriers, not sleep. Structure `phase80_browser_fixtures_test.exs` with a compile-time fixture-enabled branch for the full on contract and a fixture-disabled branch with explicit route-absence/empty-404 tests. Both branches must discover at least one real test when the complete file runs; do not use an optional `:route_off` tag. Test reset idempotency/isolation, all commands, exact schemas, secret denial, route off, Phase 79 coexistence, and Hex exclusion.</action>
  <verify>
    <automated>cd examples/phoenix_host && PHASE79_BROWSER_FIXTURES=1 PHASE80_BROWSER_FIXTURES=1 MIX_TEST_PARTITION=_phase80_fixture_contract mix test test/phase79_browser_fixtures_test.exs test/phase80_browser_fixtures_test.exs --seed 0</automated>
    <automated>cd examples/phoenix_host && PHASE80_BROWSER_FIXTURES=0 MIX_TEST_PARTITION=_phase80_fixture_route_off mix test test/phase80_browser_fixtures_test.exs --seed 0</automated>
    <automated>mix test test/oban_powertools/hex_release_test.exs --seed 0</automated>
  </verify>
  <acceptance_criteria>
    - Both new paths were absent before creation and only four exact POST routes/actions exist under test+flag gating.
    - Consecutive resets are identical per project/run and do not alter another project/run or Phase 79 fixture rows.
    - Public output has only documented IDs/counts/states and rejects unknown/sensitive keys; no secret/token/hash/reason/raw error/metadata is returned or logged.
    - A held batch can be released after observer disconnect and public evidence eventually shows individual audited effects without arbitrary sleep.
    - Missing/wrong secrets and disabled routes are indistinguishable empty 404s; modules/routes remain outside production and Hex.
    - The complete route-off fixture file reports at least one executed test and passes without `--only`, excludes, or zero-selected-test behavior.
  </acceptance_criteria>
  <done>The example host exposes deterministic Jobs/Forensics control only through one closed, secret-protected, opt-in test seam.</done>
</task>

<task type="auto">
  <name>Task 80-10-02: Bridge the Phase 80 secret into the disposable Docker run and add fail-closed clients</name>
  <files>scripts/with-showcase-server.sh, scripts/playwright-docker.sh, test/browser/support/phase80-fixtures.ts, test/browser/specs/phase80-fixtures.spec.ts</files>
  <read_first>
    - @scripts/with-showcase-server.sh
    - @scripts/playwright-docker.sh
    - @test/browser/support/phase80-fixtures.ts (new; confirm absent before creation)
    - @test/browser/specs/phase80-fixtures.spec.ts (new; confirm absent before creation)
    - @test/browser/support/phase79-fixtures.ts
    - @test/browser/specs/phase79-fixtures.spec.ts
    - @examples/phoenix_host/config/test.exs
    - @examples/phoenix_host/lib/phoenix_host_web/router.ex
    - @playwright.config.ts
    - @package.json
    - @.planning/phases/79-page-migration-wave-1-overview-cron-limiters-audit/79-10-SUMMARY.md
  </read_first>
  <action>Before changing any task-owned file, capture the execution-boundary checksum with `shasum -a 256 package-lock.json mix.exs mix.lock priv/static/oban_powertools/oban_powertools.js > /tmp/oban-powertools-80-10-02-protected.before.sha256`; this protects the user's already-dirty `package-lock.json` by comparing with task entry rather than HEAD. Extend the existing disposable test launcher without weakening Phase 79: generate a second cryptographically random nonblank `PHASE80_BROWSER_FIXTURE_SECRET`, start the same validated isolated MIX_ENV=test database/build/server with both fixture flags/secrets, export both names to the child, and preserve exact cleanup. Extend Docker args only when Phase80 secret is nonblank with `-e PHASE80_BROWSER_FIXTURE_SECRET` (name only, no `=value`); never echo/copy/trace values or broaden environment forwarding. Create `phase80-fixtures.ts` that requires the secret at call time before network I/O, sends it only in `x-phase80-fixture-secret`, validates project/run/actor/command allowlists and exact response schemas, and rejects sensitive/unknown fields. Create fixture spec proving reset twice, project isolation, both actors, hold/release/status, evidence polling with finite timeout, missing-secret local failure, wrong-secret 404, and connected page availability. Preserve Phase79 helper/spec behavior and run both in the same disposable server.</action>
  <verify>
    <automated>bash -n scripts/with-showcase-server.sh && bash -n scripts/playwright-docker.sh</automated>
    <automated>npx playwright test --list test/browser/specs/phase79-fixtures.spec.ts test/browser/specs/phase80-fixtures.spec.ts</automated>
    <automated>scripts/with-showcase-server.sh scripts/playwright-docker.sh npx playwright test test/browser/specs/phase79-fixtures.spec.ts test/browser/specs/phase80-fixtures.spec.ts --project=chromium-wide</automated>
    <automated>shasum -a 256 package-lock.json mix.exs mix.lock priv/static/oban_powertools/oban_powertools.js | cmp - /tmp/oban-powertools-80-10-02-protected.before.sha256</automated>
  </verify>
  <acceptance_criteria>
    - Launcher retains one validated disposable database/build and exact cleanup while enabling both fixture versions.
    - Docker forwards each secret by name only; absent Phase80 value remains absent; no secret appears in argv/log/trace/title/attachment/URL/body.
    - TS helper fails locally before a request when the env value is missing and rejects unknown public fields.
    - Docker run proves both Phase79 and Phase80 fixture specs green concurrently without project/run collisions or ambient seed fallback.
    - The protected-file checksum matches the Task 80-10-02 entry boundary exactly, including the user's pre-existing `package-lock.json` state; no dependency/lockfile/root Mix/packaged JS/production route/migration change is introduced.
  </acceptance_criteria>
  <done>The isolated Docker browser can use both fixture generations without secret disclosure, contract drift, or lifecycle regression.</done>
</task>

</tasks>

<threat_model>
## ASVS L1 Assets

- Fixture credentials, isolated test database, deterministic batch barriers, closed public evidence, Phase 79 compatibility, and shared launcher integrity.

## Trust Boundaries

| Boundary | Untrusted input / protected output |
|---|---|
| Browser -> fixture POST routes | project/run/actor/command + secret -> closed test state |
| Fixture process -> disposable DB | deterministic tagged data -> isolated rows/effects |
| Fixture API -> TS client | closed responses -> validated browser control |

## STRIDE Threat Register — blocking threshold: high

| Threat ID | Category | Severity | Disposition | Mitigation and verification |
|---|---|---|---|---|
| T-80-10-AUTH | Elevation of privilege | high | mitigate | Compile-time test gating, per-request constant-time secret, closed actors, and empty 404 denial protect fixture access. |
| T-80-10-DATA | Tampering | high | mitigate | Transactional project/run tagging, exact DB cleanup, closed commands, and isolation tests prevent cross-run/prod mutation. |
| T-80-10-LEAK | Information disclosure | high | mitigate | Name-only secret forwarding, closed responses, and no tracing protect credentials/tokens/payloads. |
| T-80-10-INJECT | Injection | high | mitigate | Closed project/run/actor/command schemas and POST-only exact routes reject arbitrary fixture operations. |
| T-80-10-ZERO | Repudiation | high | mitigate | Complete route-on and route-off files execute real tests; no tag-filtered zero-test success is accepted. |
| T-80-10-DOS | Denial of service | high | mitigate | Disposable DB, bounded polling/barriers, exact cleanup, and no sleeps/unbounded endpoints protect CI/host resources. |
| T-80-10-REGRESS | Repudiation | high | mitigate | Phase 79 and Phase 80 fixture specs run through one validated disposable lifecycle. |
</threat_model>

<verification>
- Run complete host route-on/off/package tests, shell syntax, and both fixture specs through one shared server.
- Inspect outputs/traces/attachments for secret and fixture-sentinel absence.
- Confirm cleanup drops only the validated disposable DB and both fixture routes are absent outside opt-in test runtime.
- Treat zero-test route-off success, any skip/fallback, ambient seed, credential leak, cross-run state, production route/package exposure, or Phase 79 regression as blocking.
</verification>

<success_criteria>
- Phase 80 fixture seam is deterministic, isolated, authenticated, closed, test-only, and compatible with Phase 79.
- Dual-secret browser control uses one validated launcher/shared port and fails closed without leaking protected values.
- Complete on/off fixture files and both fixture generations are independently green.
- No high-severity threat or production/package exposure remains.
</success_criteria>

<output>
Create `.planning/phases/80-page-migration-wave-2-jobs-forensics/80-10-SUMMARY.md` when done.
</output>
