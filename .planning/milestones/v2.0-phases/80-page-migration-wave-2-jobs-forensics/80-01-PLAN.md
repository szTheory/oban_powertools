---
phase: 80-page-migration-wave-2-jobs-forensics
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/oban_powertools/jobs.ex
  - lib/oban_powertools/web/jobs_params.ex
  - lib/oban_powertools/web/selectors.ex
  - test/oban_powertools/jobs_test.exs
  - test/oban_powertools/web/jobs_params_test.exs
  - test/oban_powertools/web/selectors_test.exs
autonomous: true
requirements:
  - PAGE-02
  - FORM-03
  - DATA-*
  - PAGE-10
must_haves:
  truths:
    - "Wave 1 ends green with a complete bounded Jobs query and canonical URL foundation; no assertion targets a later presenter, detail, bulk, or Forensics implementation."
    - "D-23: Jobs ANDs optional queue, worker, all-required tag-array containment, args-map containment, and meta-map containment without accepting an Oban Web qualifier grammar."
    - "D-29: Jobs always orders scheduled_at DESC, id DESC and retains the 20-row offset-page URL contract; this phase adds no sorting, cursor, or pagination-protocol option."
    - "D-10: Canonical Jobs list URLs allow one optional job=<id> quick-review selector, while full-detail return context drops job and every opaque or unknown key."
    - "D-24 and D-25: Invalid JSON draft text and exact field copy remain separate from safe canonical applied URL state; invalid direct URLs are replaced with finite explanations before querying."
    - "Jobs applies one shared optional-filter predicate to bounded rows, exact active counts, one grouped seven-state count query, and stable limit-plus-one ID windows."
    - "Canonical applied URL state is separate from retained invalid draft state, unknown values are rejected before querying, and every emitted URL uses one closed ordered allowlist."
    - "All focused files run as complete suites without speculative tags, skipped future tests, or accepted RED failures."
  artifacts:
    - path: "lib/oban_powertools/jobs.ex"
      provides: "Bounded predicate-consistent Jobs list/count/grouped-count/ID-window APIs"
      exports: ["count/3", "count_by_state/3", "ordered_ids_window/4"]
    - path: "lib/oban_powertools/web/jobs_params.ex"
      provides: "Pure canonical Jobs URL and retained-draft parser"
      exports: ["parse_url/1", "validate_draft/1", "filter_identity/1"]
    - path: "lib/oban_powertools/web/selectors.ex"
      provides: "Closed ordered Jobs list/detail URL encoders"
    - path: "test/oban_powertools/web/jobs_params_test.exs"
      provides: "Executable URL/draft/filter-identity regression contract"
  key_links:
    - from: "lib/oban_powertools/web/jobs_params.ex"
      to: "lib/oban_powertools/jobs.ex"
      via: "validated applied filter structs only"
      pattern: "parse_url|filter_identity"
    - from: "lib/oban_powertools/web/selectors.ex"
      to: "lib/oban_powertools/web/jobs_params.ex"
      via: "one canonical Jobs key order and value grammar"
      pattern: "jobs_path|job_detail_path"
    - from: "test/oban_powertools/jobs_test.exs"
      to: "lib/oban_powertools/jobs.ex"
      via: "query-count, order, bound, and predicate-equivalence assertions"
      pattern: "count_by_state|ordered_ids_window|scheduled_at"
---

<objective>
Build and verify the complete bounded Jobs query and canonical URL foundation as the first green Phase 80 slice.

Purpose: give every later Jobs page, detail, and frozen-batch wave one stable implemented contract without making shared focused test files fail on future work.
Output: production Jobs query, parameter, and selector seams with complete passing focused suites.
</objective>

<execution_context>
@/Users/jon/.agents/gsd-core/workflows/execute-plan.md
@/Users/jon/.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/80-page-migration-wave-2-jobs-forensics/80-CONTEXT.md
@.planning/phases/80-page-migration-wave-2-jobs-forensics/80-RESEARCH.md
@.planning/phases/80-page-migration-wave-2-jobs-forensics/80-PATTERNS.md
@.planning/phases/80-page-migration-wave-2-jobs-forensics/80-UI-SPEC.md
@.planning/phases/80-page-migration-wave-2-jobs-forensics/80-VALIDATION.md
@.planning/phases/79-page-migration-wave-1-overview-cron-limiters-audit/79-02-SUMMARY.md
</context>

## Must Haves — Decision Coverage

- **D-10:** the exact ordered Jobs list allowlist includes optional `job=<id>` for parent-owned quick review; full-detail return encoding removes `job`, `return_to`, and unknown keys.
- **D-23:** the shared optional predicate ANDs queue equality, worker equality, all-required Postgres tag-array containment, args JSON-map containment, and meta JSON-map containment; no qualifier DSL is parsed.
- **D-24:** params/draft contracts preserve the exact tag, JSON help, and invalid-JSON strings required by the Jobs FilterBar.
- **D-25:** invalid draft strings survive with field errors and cannot become applied query values; invalid direct URL values produce safe defaults, `replace?`, and a concise finite notice before a query can run.
- **D-29:** every row and frozen-ID query uses `scheduled_at DESC, id DESC`; the list remains a 20-row offset page with no sorting, cursor, or URL-protocol expansion.
- **D-20, D-21, D-22, D-26, D-27:** required state, submit-mode optional filters, applied/draft separation, canonical URL ordering, and reset identity remain executable in Plan 80-02 on this foundation.
- **D-28 through D-32 and D-35:** one shared predicate, exact active count, one grouped count query, stable 20-row paging order, exact table identity data, and retained host index guidance.
- **D-38 through D-43:** explicit selection identity and stable max/default all-matching ID-window semantics are finite before later batch execution.
- **D-73, D-74, D-75, D-80:** idiomatic Ecto/context boundaries, no new dependency/schema, and bounded work remain locked.

## Artifacts this phase produces

- New module/file `ObanPowertools.Web.JobsParams` at `lib/oban_powertools/web/jobs_params.ex`.
- New public `JobsParams.parse_url/1`, `validate_draft/1`, and `filter_identity/1`.
- New public `Jobs.count/3` and `Jobs.ordered_ids_window/4`; existing `Jobs.count_by_state/2` remains compatible while new `count_by_state/3` performs one grouped query.
- `Selectors.jobs_path/1` becomes a closed ordered encoder; `Selectors.job_detail_path/2` is added while the existing arity remains compatible.
- New complete test file `test/oban_powertools/web/jobs_params_test.exs`.
- No presenter, JobsLive markup/event, full-detail, mutation, Forensics, coordinator, schema, migration, dependency, story, fixture, CSS, or evidence artifact is created in this plan.

<tasks>

<task type="auto" tdd="true">
  <name>Task 80-01-01: Implement bounded predicate-consistent Jobs query APIs</name>
  <files>lib/oban_powertools/jobs.ex, test/oban_powertools/jobs_test.exs</files>
  <read_first>
    - @lib/oban_powertools/jobs.ex
    - @test/oban_powertools/jobs_test.exs
    - @lib/oban_powertools/audit.ex
    - @test/oban_powertools/audit_test.exs
    - @test/support/data_case.ex
    - @.planning/phases/80-page-migration-wave-2-jobs-forensics/80-PATTERNS.md
    - @.planning/phases/80-page-migration-wave-2-jobs-forensics/80-UI-SPEC.md
  </read_first>
  <action>Using a red-green loop contained entirely in this task, refactor `Jobs` around one private non-state predicate pipeline and one optional state predicate. D-23: AND queue equality, worker equality, all-required Postgres array containment for tags, args JSON-map containment, and meta JSON-map containment; accept no qualifier grammar or cross-field OR semantics. Add `count/3` over the exact active predicate. Implement options-capable `count_by_state/3` as one grouped state/count query over non-state filters, merge its result into a literal seven-key zero map, and keep `count_by_state/2` delegating compatibly. D-29: keep `list/3` at page size 20 with offset pagination and make both list and `ordered_ids_window/4` use fixed `scheduled_at DESC, id DESC`; expose no user sort, cursor, or changed URL protocol. Add `ordered_ids_window/4` with a validated positive limit, the same active predicates, and SQL `limit + 1`; return exactly `%{ids: [integer], overflow?: boolean}` with at most `limit` IDs. Keep repo-first APIs, existing host-owned GIN guidance, and no Oban mutation. Add all assertions to the existing complete Jobs suite and finish with the entire file green.</action>
  <verify>
    <automated>mix test test/oban_powertools/jobs_test.exs --seed 0</automated>
    <automated>mix format --check-formatted lib/oban_powertools/jobs.ex test/oban_powertools/jobs_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - The complete Jobs test file passes; no future-tagged, skipped, or intentionally failing test remains.
    - Tests prove identical optional predicates across rows, exact active count, grouped state counts, and bounded ID windows.
    - D-23 table tests prove optional categories combine with AND, every requested tag is required by Postgres array containment, args/meta require JSON map containment, and qualifier-like strings remain literal values rather than a DSL.
    - D-29 tests prove fixed `scheduled_at DESC, id DESC` ties and non-overlapping 20-row offset pages; no sort/cursor argument or URL key is introduced.
    - Grouped counts issue one captured `oban_jobs` count query and emit all seven state keys including zero values.
    - A 21-ID fixture at limit 20 returns 20 stable descending IDs plus `overflow?: true`; it never becomes an executable 21-ID scope.
    - `list_ids/2` remains source-compatible but no new migrated path is directed to use it; no schema, index, dependency, or direct mutation is added.
  </acceptance_criteria>
  <done>The full Jobs context suite is green with stable, exact, bounded query primitives for all later Phase 80 slices.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 80-01-02: Implement canonical Jobs params and allowlisted URLs</name>
  <files>lib/oban_powertools/web/jobs_params.ex, lib/oban_powertools/web/selectors.ex, test/oban_powertools/web/jobs_params_test.exs, test/oban_powertools/web/selectors_test.exs</files>
  <read_first>
    - @lib/oban_powertools/web/jobs_params.ex (new; create this exact path)
    - @lib/oban_powertools/web/selectors.ex
    - @test/oban_powertools/web/jobs_params_test.exs (new; create this exact path)
    - @test/oban_powertools/web/selectors_test.exs
    - @lib/oban_powertools/jobs.ex
    - @lib/oban_powertools/web/jobs_live.ex
    - @lib/oban_powertools/web/audit_live.ex
    - @lib/oban_powertools/web/components/operator_patterns.ex
    - @lib/oban_powertools/web/components/forms.ex
    - @.planning/phases/80-page-migration-wave-2-jobs-forensics/80-CONTEXT.md
    - @.planning/phases/80-page-migration-wave-2-jobs-forensics/80-UI-SPEC.md
  </read_first>
  <action>Using a red-green loop contained entirely in this task, create pure `JobsParams` with a compile-time seven-state list and the exact ordered allowlist `state, queue, worker, tags, args, meta, page, job`. D-10: parse optional `job=<id>` only as a canonical review selector for the Jobs list; return it separately from the `%Jobs{}` query filter so the parent LiveView can own authorization/loading/presentation and the eventual DetailSurface stays stateless. Make `parse_url/1` return a safe `%Jobs{page_size: 20}`, canonical applied params, parsed optional review ID, `replace?`, and finite notices; invalid state/page/JSON/review values must be removed or defaulted before any caller can query. D-24: make `validate_draft/1` preserve original queue/worker/tags/args/meta strings and expose the exact copy `Separate tags with commas. Jobs must contain every listed tag.`, `Enter a JSON object. Filter values are stored in the URL; do not enter secrets.`, and `Enter a valid JSON object.`. D-25: draft errors do not patch, while invalid direct URL state/page/JSON/review values produce safe applied defaults, `replace?: true`, and one concise allowlisted explanation. Args/meta accept only decoded JSON objects and tags retain all-required semantics. Make `filter_identity/1` return deterministic encoded applied state/filter values excluding page/review. Update `Selectors.jobs_path/1` and `job_detail_path/2` to share this closed order; detail return context preserves only state, queue, worker, tags, args, meta, page and drops `job`, `return_to`, and unknown keys while keeping the existing arity compatible. Complete both full focused test files and finish them green.</action>
  <verify>
    <automated>mix test test/oban_powertools/web/jobs_params_test.exs test/oban_powertools/web/selectors_test.exs --seed 0</automated>
    <automated>mix test test/oban_powertools/jobs_test.exs test/oban_powertools/web/jobs_params_test.exs test/oban_powertools/web/selectors_test.exs --seed 0</automated>
    <automated>mix format --check-formatted lib/oban_powertools/web/jobs_params.ex lib/oban_powertools/web/selectors.ex test/oban_powertools/web/jobs_params_test.exs test/oban_powertools/web/selectors_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - Both complete focused test files and the combined Wave 1 command pass without `--only`, skips, or references to later implementation.
    - Valid URLs round-trip byte-stably in the exact allowlist order.
    - D-10 tests prove a valid `job=<id>` round-trips only on the Jobs list, is returned separately from database filters, and is stripped from full-detail return context with `return_to` and unknown keys.
    - D-24 tests assert the tag help, JSON help, and invalid-JSON error strings byte-for-byte.
    - D-25 tests prove invalid draft text remains returned verbatim with its inline field error and no patch intent, while invalid direct state/page/JSON/review inputs return safe canonical params, `replace?: true`, and a finite explanation before any repo call.
    - Invalid draft JSON remains visible with `Enter a valid JSON object.` and causes no applied query/URL mutation; invalid direct JSON safely defaults with the finite replacement notice.
    - JSON scalars/arrays, negative pages, unknown states, malformed review IDs, unknown keys, `return_to`, and delimiter attempts cannot enter canonical query state.
    - Parser and selector functions perform no repo, authorization, clock, mutation, telemetry, or HTML work.
  </acceptance_criteria>
  <done>Wave 1 ends green with one deterministic, allowlisted Jobs query and URL foundation.</done>
</task>

</tasks>

<threat_model>
## ASVS L1 Assets

- Applied filter truth, stable query bounds, job identifier scope, canonical navigation context, and deterministic query regression evidence.

## Trust Boundaries

| Boundary | Untrusted input / protected output |
|---|---|
| Browser URL/form -> JobsParams | malformed state/page/JSON/job/unknown keys -> safe canonical applied state |
| Applied Jobs filters -> database | hostile/high-cardinality filter values -> parameterized bounded Ecto query |
| Database ordering/counts -> later UI/batch | changing rows -> exact count, stable order, explicit overflow |

## STRIDE Threat Register — blocking threshold: high

| Threat ID | Category | Severity | Disposition | Mitigation and verification |
|---|---|---|---|---|
| T-80-01-INJECT | Injection | high | mitigate | JSON-object validation, parameterized Ecto predicates, ordered allowlists, and delimiter tests reject query/URL injection. |
| T-80-01-TAMPER | Tampering | high | mitigate | Canonical applied state is separate from draft input and unknown/invalid values are removed before querying. |
| T-80-01-DOS | Denial of service | high | mitigate | Page size 20, one grouped query, exact count, and limit-plus-one ID reads bound database and downstream work. |
| T-80-01-FALSEGREEN | Repudiation | high | mitigate | Both tasks finish with complete focused suites green; no future assertion, skip, or accepted RED state remains. |
</threat_model>

<verification>
- Run both task commands plus the combined Wave 1 command from `80-VALIDATION.md`.
- Inspect captured query telemetry for a single grouped count and verify all row/count/ID APIs share optional predicates and stable ordering.
- Confirm the diff contains only the six declared production/test paths and no presenter, LiveView composition, Forensics, schema, migration, dependency, asset, fixture, story, or evidence file.
</verification>

<success_criteria>
- The first execution wave is independently green and usable.
- Jobs query, count, grouping, ID-window, params, and selector behavior is exact, bounded, deterministic, and fully implemented.
- No test in a shared focused file depends on implementation scheduled for later waves.
- Every high-severity threat has passing negative or bound evidence.
</success_criteria>

<output>
Create `.planning/phases/80-page-migration-wave-2-jobs-forensics/80-01-SUMMARY.md` when done.
</output>
