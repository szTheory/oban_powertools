---
phase: 80-page-migration-wave-2-jobs-forensics
plan: 02
type: execute
wave: 2
depends_on:
  - 80-01
files_modified:
  - lib/oban_powertools/web/control_plane_presenter.ex
  - lib/oban_powertools/web/jobs_live.ex
  - test/oban_powertools/web/operator_pattern_presenter_test.exs
  - test/oban_powertools/web/live/jobs_live_test.exs
autonomous: true
requirements:
  - PAGE-02
  - FORM-03
  - DATA-*
  - PAGE-10
  - A11Y-*
must_haves:
  truths:
    - "D-21: Jobs uses FilterBar submit mode with Queue, Worker module, and Tags as primary fields and Args contain plus Meta contain as advanced JSON-object fields."
    - "D-22: phx-change validates only retained draft/conditional UI; Apply filters atomically patches canonical applied URL truth and then loads, with dirty draft and applied values visibly distinct."
    - "D-24 and D-25: exact tag/JSON help and invalid-JSON copy render; invalid drafts remain visible inline while invalid direct URLs replace-canonicalize to safe defaults with a concise explanation before querying."
    - "D-26: human noun/value applied-filter chips support individual removal and Clear filters; both preserve the required state while resetting page, review, selection, and pending confirmation."
    - "D-29 and D-31: fixed scheduled_at DESC/id DESC 20-row offset pages render exact state-aware totals/ranges and disable Next from exact count, including a full 20-row final page."
    - "The semantic Jobs table uses the exact eight-column hierarchy, preserves explicit selections across pagination, and never turns a row into a control."
    - "D-10: allowlisted job=<id> quick review is parsed, authorized, loaded, canonicalized, and presented by the parent LiveView; the DetailSurface receives a closed map and remains stateless."
    - "D-11: first review pushes history, switching review replaces, close replaces the same filter/page URL without job, browser Back closes review, and deep-link reload restores it."
    - "This wave ends green with complete presenter and JobsLive files; full-detail and bulk assertions remain in their owning later plans."
  artifacts:
    - path: "lib/oban_powertools/web/control_plane_presenter.ex"
      provides: "Closed Jobs row and quick-review maps"
      exports: ["present_job_row/2", "present_job_quick_review/2"]
    - path: "lib/oban_powertools/web/jobs_live.ex"
      provides: "Jobs scan, filter, selection, quick-review orchestration, and pure index composition"
      exports: ["page_content/1"]
  key_links:
    - from: "lib/oban_powertools/web/jobs_live.ex"
      to: "lib/oban_powertools/web/jobs_params.ex"
      via: "parse and canonicalize before bounded queries"
      pattern: "JobsParams"
    - from: "lib/oban_powertools/web/jobs_live.ex"
      to: "lib/oban_powertools/web/selectors.ex"
      via: "canonical list and quick-review patches"
      pattern: "jobs_path|job_detail_path"
    - from: "lib/oban_powertools/web/jobs_live.ex"
      to: "lib/oban_powertools/web/components/operator_patterns.ex"
      via: "FilterBar and DetailSurface"
      pattern: "filter_bar|detail_surface"
    - from: "lib/oban_powertools/web/jobs_live.ex"
      to: "lib/oban_powertools/web/components/data_display.ex"
      via: "DataTable receives closed row maps"
      pattern: "data_table"
---

<objective>
Migrate the Jobs browse-to-review surface onto the green Phase 80 query/URL foundation, DataTable, and DetailSurface.

Purpose: make large Jobs scans exact, responsive, URL-reloadable, and redaction-safe without pulling full-detail or batch behavior ahead of its owning wave.
Output: complete green presenter and JobsLive browse/quick-review contracts plus public pure index composition.
</objective>

<execution_context>
@/Users/jon/.agents/gsd-core/workflows/execute-plan.md
@/Users/jon/.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/80-page-migration-wave-2-jobs-forensics/80-CONTEXT.md
@.planning/phases/80-page-migration-wave-2-jobs-forensics/80-RESEARCH.md
@.planning/phases/80-page-migration-wave-2-jobs-forensics/80-PATTERNS.md
@.planning/phases/80-page-migration-wave-2-jobs-forensics/80-UI-SPEC.md
@.planning/phases/80-page-migration-wave-2-jobs-forensics/80-VALIDATION.md
@.planning/phases/80-page-migration-wave-2-jobs-forensics/80-01-SUMMARY.md
@.planning/phases/79-page-migration-wave-1-overview-cron-limiters-audit/79-06-SUMMARY.md
@.planning/phases/79-page-migration-wave-1-overview-cron-limiters-audit/79-07-SUMMARY.md
</context>

## Must Haves — Decision Coverage

- **D-10:** quick review is the allowlisted `job=<id>` Jobs-list selector; the parent LiveView parses, authorizes, loads, canonicalizes, and maps it while `detail_surface/1` remains stateless.
- **D-11:** first review uses push, switching uses replace, close replaces to the same applied filter/page URL without `job`, Back closes review, and a deep-link reload restores the authorized review.
- **D-17 through D-19:** separate named controls, adaptive modality/focus, and uniform stale/missing/unauthorized handling.
- **D-21:** submit-mode `filter_bar/1` renders Queue, Worker module, and Tags as primary inputs; `Args contain` and `Meta contain` are advanced JSON-object inputs.
- **D-22:** `phx-change` validates draft/conditional UI without a query or patch; `Apply filters` atomically patches the canonical URL then loads, and dirty draft/applied truth are visibly distinct.
- **D-23:** optional categories are ANDed; every listed tag is required by array containment; args/meta require JSON map containment; no Oban Web qualifier grammar is accepted.
- **D-24:** render the exact tag help, shared JSON help, and `Enter a valid JSON object.` error strings.
- **D-25:** invalid drafts stay visible with inline field errors; invalid direct URLs replace-canonicalize to safe defaults with a concise explanation before any query.
- **D-26:** applied filters are human noun/value chips with individual removal and `Clear filters`; removal/clear preserves state and resets page, review, selection, and pending confirmation.
- **D-29:** retain fixed `scheduled_at DESC, id DESC` and 20-row offset paging; expose no sorting, cursor, or URL-protocol alternative.
- **D-31:** exact state-aware total/range copy drives Next, including the full-size final-page boundary.
- **D-20, D-27, D-28, D-30, D-32, D-33, D-34, D-35:** required state/reset identity, exact Jobs columns/count query, one semantic responsive table, and stable index guidance remain executable.
- **D-68, D-69, D-73, D-74, D-77, D-80:** parent/context/presenter ownership, structural redaction before assigns, server authorization, and finite pure composition.
- **D-81 through D-85:** calm control-room hierarchy, themes, 44px controls, focus truth, and restrained announcements.

## Artifacts this phase produces

- New public presenter functions `present_job_row/2` and `present_job_quick_review/2`.
- New public pure component `JobsLive.page_content/1` covering `page_mode: :index`; Plan 80-03 owns the full-detail branch and its presenter assertions.
- JobsLive event contract: `validate_filters`, `apply_filters`, `remove_filter`, `clear_filters`, `select_state`, `toggle_job`, `toggle_page`, `clear_selection`, `select_review`, and `close_review`.
- No full-detail presenter fields, single-job mutation migration, batch all-matching authority, coordinator, Forensics behavior, schema, migration, dependency, story, fixture, CSS, or evidence is created.

<tasks>

<task type="auto" tdd="true">
  <name>Task 80-02-01: Implement closed Jobs row and quick-review presenters</name>
  <files>lib/oban_powertools/web/control_plane_presenter.ex, test/oban_powertools/web/operator_pattern_presenter_test.exs</files>
  <read_first>
    - @lib/oban_powertools/web/control_plane_presenter.ex
    - @test/oban_powertools/web/operator_pattern_presenter_test.exs
    - @lib/oban_powertools/runtime_config.ex (read the `ObanPowertools.DisplayPolicy` module defined here)
    - @lib/oban_powertools/web/components/data_display.ex
    - @lib/oban_powertools/web/components/operator_patterns.ex
    - @.planning/phases/77-data-display-operator-patterns/77-SECURITY.md
    - @.planning/phases/78-component-groups-meta-components/78-SECURITY.md
    - @.planning/phases/80-page-migration-wave-2-jobs-forensics/80-UI-SPEC.md
  </read_first>
  <action>Using a red-green loop contained in this task, add pure `present_job_row/2` and `present_job_quick_review/2` after DisplayPolicy. Row output contains only the exact eight table/control facts and full accessible identities. Quick review contains only D-12 status/identity/failure-summary facts plus the allowlisted full-detail href; it excludes args, meta, output payload, stacktrace, full attempt history, Audit history, mutation/action fields, tokens, and raw provider values. Table-test nested sentinels under token, secret, password, credential, authorization, cookie, header, URL query, metadata, payload, reason, exception, and stacktrace aliases; require both forbidden-key absence and sentinel absence. Do not add full-detail, batch-result, or Forensics presenter assertions in this task. Run the complete existing presenter test file and leave it green.</action>
  <verify>
    <automated>mix test test/oban_powertools/web/operator_pattern_presenter_test.exs --seed 0</automated>
    <automated>mix format --check-formatted lib/oban_powertools/web/control_plane_presenter.ex test/oban_powertools/web/operator_pattern_presenter_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - The complete presenter test file passes with no tag/skip or assertion for a later full-detail, bulk, or Forensics symbol.
    - Row and quick-review outputs use closed finite keys and grammatical values only.
    - Quick-review output cannot contain args, meta, payload, stacktrace, full history, Audit history, mutation controls, token, or raw source values.
    - No nested sentinel survives projection even when placed in an exception-like, URL, title, list, or metadata-shaped source value.
    - Presentation functions perform no repo, clock, task, authorization, mutation, telemetry, or HTML work.
  </acceptance_criteria>
  <done>The complete presenter suite is green with the exact finite Jobs fields needed by the browse/quick-review slice and no future assertions.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 80-02-02: Compose the bounded Jobs scan and adaptive quick review</name>
  <files>lib/oban_powertools/web/jobs_live.ex, test/oban_powertools/web/live/jobs_live_test.exs</files>
  <read_first>
    - @lib/oban_powertools/web/jobs_live.ex
    - @test/oban_powertools/web/live/jobs_live_test.exs
    - @lib/oban_powertools/web/control_plane_presenter.ex
    - @lib/oban_powertools/web/jobs_params.ex
    - @lib/oban_powertools/jobs.ex
    - @lib/oban_powertools/web/selectors.ex
    - @lib/oban_powertools/web/components/operator_patterns.ex
    - @lib/oban_powertools/web/components/data_display.ex
    - @lib/oban_powertools/web/components/forms.ex
    - @lib/oban_powertools/web/components/primitives.ex
    - @.planning/phases/78-component-groups-meta-components/78-UI-SPEC.md
    - @.planning/phases/79-page-migration-wave-1-overview-cron-limiters-audit/79-06-SUMMARY.md
    - @.planning/phases/80-page-migration-wave-2-jobs-forensics/80-UI-SPEC.md
  </read_first>
  <action>Using a red-green loop contained in this task, rework Jobs index orchestration so `handle_params/3` authorizes, parses, and canonicalizes before querying; it then uses the implemented bounded list/count/grouped-count APIs and assigns only closed rows, pagination, drafts, selection, and review state. D-21: render shipped `filter_bar/1` in `mode: :submit` with Queue, Worker module, and Tags in its primary slot and advanced `Args contain`/`Meta contain` JSON-object fields. D-22: `validate_filters` (`phx-change`) may update only retained draft errors, dirty state, and advanced disclosure—no repo call or navigation; `apply_filters` validates once, atomically patches the canonical applied URL, then lets `handle_params/3` load. D-23: applied queue/worker/tags/args/meta categories remain ANDed, tags require every comma-separated tag, args/meta are JSON-object containment only, and qualifier-looking text stays literal. D-24: render exact help `Separate tags with commas. Jobs must contain every listed tag.`, JSON help `Enter a JSON object. Filter values are stored in the URL; do not enter secrets.`, and inline error `Enter a valid JSON object.`. D-25: keep invalid draft strings visible beside inline errors without a patch/query; replace-canonicalize invalid direct state/page/JSON/job values to safe defaults and render one concise explanation before any load. D-26: present applied queue/worker/tags/args/meta as human noun/value chips with individual `remove_filter` destinations plus `Clear filters`; remove/clear preserves state but resets page to 1, `job`, selected IDs, frozen scope, preview, confirmation, and stale results. State/apply changes use the same reset; ordinary pagination retains explicit selected IDs while clearing review. D-29: query/render only fixed `scheduled_at DESC, id DESC` 20-row offset pages and add no sorting/cursor controls or URL keys. D-31: derive exact `143 retryable jobs`-style state summary and `Showing 21–40 of 143` range from active count; disable Next when `page * 20 >= exact_count`, including a full 20-row last page. Render `page_content/1` through shipped FilterBar, DataTable, Forms, StatusPill, EmptyState, and DetailSurface. Use exact columns Selection, Worker, State, Queue, Scheduled, Attempts, Job ID, Review job; rows are not controls and full worker/ID values remain in the DOM. D-10: parse allowlisted `job=<id>` in the parent, reauthorize and load there, pass only the closed presenter map to stateless DetailSurface, and remove invalid/stale/missing/unauthorized review IDs with uniform `Job unavailable`. D-11: first valid review uses push history; selecting another uses replace; close uses replace to the identical state/filter/page URL without `job`; browser Back from that first entry closes review and direct deep-link reload rehydrates it. Keep existing full-detail and mutation behavior compatible but add no new assertions for those branches until Plan 80-03/04. Finish by running the complete JobsLive file and combined Plan 80-02 command green.</action>
  <verify>
    <automated>mix test test/oban_powertools/web/live/jobs_live_test.exs --seed 0</automated>
    <automated>mix test test/oban_powertools/web/operator_pattern_presenter_test.exs test/oban_powertools/web/live/jobs_live_test.exs --seed 0</automated>
    <automated>mix test test/oban_powertools/web/components/operator_patterns_test.exs test/oban_powertools/web/components/data_display_test.exs --seed 0</automated>
    <automated>mix format --check-formatted lib/oban_powertools/web/jobs_live.ex test/oban_powertools/web/live/jobs_live_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - Complete focused files pass with no speculative tag, skip, or later-plan failure.
    - D-21/D-22 tests prove one submit-mode FilterBar tree has the three primary and two advanced fields, change performs zero repo reads/patches, dirty draft differs visibly from applied chips, and Apply performs one canonical patch followed by bounded loads.
    - D-23/D-24 tests prove AND/all-tag/JSON-object semantics, no qualifier parsing, and byte-exact tag help, JSON help, and invalid-JSON error copy.
    - D-25 tests prove invalid drafts remain visible inline without navigation/query and invalid direct state/page/JSON/job values replace to safe canonical defaults with a concise explanation before any repo call.
    - D-26 tests prove noun/value removal and Clear filters preserve the state selector while resetting page, review, selected IDs, frozen scope, preview, and pending confirmation.
    - D-29/D-31 tests prove fixed descending scheduled-time/ID order, unchanged 20-row offset URLs, exact `143 retryable jobs` and `Showing 21–40 of 143` copy, and no sort/cursor affordance.
    - A 20-row exact final page disables Next from exact total, not row length; empty results remain at page 1 with explicit truth.
    - Explicit IDs persist across pagination and reset on filter/state changes; page checkboxes expose named checked/mixed/unchecked state.
    - D-10 tests prove `job=<id>` is the only review URL key, all parsing/authorization/loading/canonicalization/presentation mapping happens in JobsLive, and DetailSurface performs no stateful work.
    - D-11 tests prove first-review push, switch replace, close replace to the same applied filter/page URL without `job`, Back-to-close behavior, direct deep-link reload, and stale-review replacement.
    - HTML has one H1, one semantic table tree, one review surface maximum, named 44px controls, no ARIA grid, and no page-local table/modal substitute.
  </acceptance_criteria>
  <done>Wave 2 ends green with exact Jobs browse, filtering, pagination, selection, and adaptive quick review on shared components.</done>
</task>

</tasks>

<threat_model>
## ASVS L1 Assets

- Job existence confidentiality, redacted page facts, exact pagination/count truth, explicit selection identity, semantic control names, and deterministic browser-history URLs.

## Trust Boundaries

| Boundary | Untrusted input / protected output |
|---|---|
| Canonical URL/form -> JobsLive | applied/draft/review identity -> authorized bounded query |
| Oban rows -> presenter | raw args/meta/errors/output -> finite safe row/review maps |
| LiveView -> shared components | parent authority/state -> stateless semantic HTML |

## STRIDE Threat Register — blocking threshold: high

| Threat ID | Category | Severity | Disposition | Mitigation and verification |
|---|---|---|---|---|
| T-80-02-IDOR | Elevation of privilege | high | mitigate | Server authorization and identical missing/unauthorized review state prevent job enumeration. |
| T-80-02-LEAK | Information disclosure | high | mitigate | Closed projectors exclude payload/history/action data before assigns and DOM sentinel tests block leakage. |
| T-80-02-DOS | Denial of service | high | mitigate | Implemented 20-row list, exact count, grouped query, and finite presentation maps bound scan/render work. |
| T-80-02-TAMPER | Tampering | high | mitigate | Canonical URL remains applied truth; draft change cannot query or patch and unknown return state is discarded. |
| T-80-02-A11Y | Spoofing | high | mitigate | Explicit named controls, semantic table structure, and programmatic selected/current state prevent visual-only authority. |
| T-80-02-FALSEGREEN | Repudiation | high | mitigate | Entire focused files run green after implementation; later detail/bulk/Forensics assertions stay in owning plans. |
</threat_model>

<verification>
- Run both complete task suites, the combined Plan 80-02 command, and both shared component suites.
- Inspect query telemetry, rendered HTML, and assigns for the exact count/bound and confidentiality assertions.
- Confirm the diff contains only the four declared presenter/JobsLive paths and no detail/batch/Forensics/schema/dependency/story/fixture/asset/evidence work.
</verification>

<success_criteria>
- Jobs filters are submit-mode, canonical, URL-backed, and preserve invalid drafts.
- Jobs rows/counts/state counts and explicit selection are exact, stable, bounded, and semantically rendered.
- DataTable and DetailSurface provide the approved hierarchy, focus, history, and responsive semantics.
- All complete focused tests pass and no high-severity threat remains.
</success_criteria>

<output>
Create `.planning/phases/80-page-migration-wave-2-jobs-forensics/80-02-SUMMARY.md` when done.
</output>
