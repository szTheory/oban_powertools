---
phase: 81-page-migration-wave-3-batches-workflows-lifeline
plan: 07
type: execute
wave: 1
depends_on: []
files_modified:
  - examples/phoenix_host/config/test.exs
  - examples/phoenix_host/lib/phoenix_host_web/router.ex
  - examples/phoenix_host/test/support/phase81_browser_fixtures.ex
  - examples/phoenix_host/test/phase81_browser_fixtures_test.exs
  - scripts/with-showcase-server.sh
  - scripts/playwright-docker.sh
  - test/browser/support/phase81-fixtures.ts
  - test/browser/specs/phase81-fixtures.spec.ts
autonomous: true
requirements:
  - PAGE-03
  - PAGE-04
  - PAGE-07
  - PAGE-10
  - A11Y-01
  - A11Y-02
  - A11Y-04
must_haves:
  truths:
    - "Connected evidence uses isolated real production routes with deterministic authorized and authorization-race states."
    - "The fixture endpoint is test-only, explicit-opt-in, POST-only, credential-protected, closed-schema, and absent from packaged production."
    - "Fixture responses and browser helpers never echo secrets, preview identities, raw snapshots, or domain authority."
  artifacts:
    - path: "examples/phoenix_host/test/support/phase81_browser_fixtures.ex"
      provides: "Isolated deterministic Wave 3 reset/auth/race fixture seam"
    - path: "test/browser/support/phase81-fixtures.ts"
      provides: "Fail-closed typed browser fixture client"
  key_links:
    - from: "test/browser/support/phase81-fixtures.ts"
      to: "examples/phoenix_host/test/support/phase81_browser_fixtures.ex"
      via: "credentialed POST reset/auth/race requests"
      pattern: "PHASE81_BROWSER_FIXTURE_SECRET"
---

<objective>
Create the isolated connected fixture bridge before any production migration.

Purpose: make authorization, URL/history, mutation races, durable evidence, and confidentiality mechanically reproducible without making test authority available in production.
Output: fail-closed host fixture module/route, launch configuration, TypeScript client, and fixture contract tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/81-page-migration-wave-3-batches-workflows-lifeline/81-VALIDATION.md
@.planning/phases/80-page-migration-wave-2-jobs-forensics/80-10-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 81-07-01: Add a test-only closed Wave 3 host fixture seam</name>
  <files>examples/phoenix_host/config/test.exs, examples/phoenix_host/lib/phoenix_host_web/router.ex, examples/phoenix_host/test/support/phase81_browser_fixtures.ex, examples/phoenix_host/test/phase81_browser_fixtures_test.exs</files>
  <read_first>examples/phoenix_host/test/support/phase80_browser_fixtures.ex, examples/phoenix_host/test/phase80_browser_fixtures_test.exs, examples/phoenix_host/config/test.exs, examples/phoenix_host/lib/phoenix_host_web/router.ex</read_first>
  <behavior>
    - "Without test environment, explicit enable flag, and nonblank matching credential, the endpoint is absent or rejects uniformly."
    - "POST reset/auth/race commands accept a closed schema and build deterministic bounded Batches, Workflows, Lifeline, actor, and Audit state."
    - "Responses contain only public fixture handles and never secrets, capabilities, snapshots, or raw evidence."
  </behavior>
  <action>Copy the accepted Phase 80 isolation pattern into a distinct Phase 81 fixture module and route. Compile/route only in Mix.env() == :test, require PHASE81_BROWSER_FIXTURES=1 and a generated PHASE81_BROWSER_FIXTURE_SECRET, accept POST only, validate an exact command/project/actor schema, and create deterministic per-project state. Seed the saturated cases using the exact 81-VALIDATION.md limits plus one: workflows 51, steps 101, workflow results 51, workflow evidence 26, batch members 51, callbacks 26, batch results 51, batch audit 26, incidents 51, executors 26, Lifeline audit 51, and archive rows 26. Provide explicit authorization-revocation/drift/duplicate/disconnect controls needed by the spec. Keep responses public-only.</action>
  <acceptance_criteria>Host tests cover flag off/on, wrong/missing secret, wrong method, unknown fields/commands, deterministic reset, actor setup, race controls, bounded state, and recursive secret/token/snapshot absence.</acceptance_criteria>
  <verify>
    <automated>mix test examples/phoenix_host/test/phase81_browser_fixtures_test.exs --seed 0</automated>
  </verify>
  <done>The fixture bridge is available only under explicit test launch conditions and is closed by default.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 81-07-02: Wire launcher credentials and a fail-closed browser client</name>
  <files>scripts/with-showcase-server.sh, scripts/playwright-docker.sh, test/browser/support/phase81-fixtures.ts, test/browser/specs/phase81-fixtures.spec.ts</files>
  <read_first>scripts/with-showcase-server.sh, scripts/playwright-docker.sh, test/browser/support/phase80-fixtures.ts, test/browser/specs/phase80-fixtures.spec.ts</read_first>
  <behavior>
    - "Host and Docker launchers generate/pass an ephemeral Phase 81 credential without printing it."
    - "The typed client rejects missing credentials, malformed responses, unknown keys, and non-2xx results."
    - "Back-to-back fixture-off/on runs rebuild the correct route/module state."
  </behavior>
  <action>Extend both accepted launcher paths with the Phase 81 opt-in and an ephemeral distinct secret while preserving Phase 79/80 behavior. Add a typed closed-schema reset/auth/race client and fixture contract spec. Ensure logs and thrown errors redact the credential, responses are validated before use, and fixture availability follows the explicit flag across repeated builds. Execute the spec at runtime through both the secret-aware HOST path (`scripts/with-showcase-server.sh npx playwright test ...`) and the authoritative Docker path; list/static parsing alone is insufficient.</action>
  <acceptance_criteria>Fixture Playwright tests prove fail-closed client behavior, exact response validation, deterministic reset, credential confidentiality, and host/Docker parity through successful runtime execution on both launch paths.</acceptance_criteria>
  <verify>
    <automated>npx playwright test test/browser/specs/phase81-fixtures.spec.ts --project=chromium-wide --list &amp;&amp; npm run showcase:manifest &amp;&amp; scripts/with-showcase-server.sh npx playwright test test/browser/specs/phase81-fixtures.spec.ts --project=chromium-wide &amp;&amp; scripts/with-showcase-server.sh scripts/playwright-docker.sh npx playwright test test/browser/specs/phase81-fixtures.spec.ts --project=chromium-wide</automated>
  </verify>
  <done>The connected Wave 3 spec has a deterministic confidential fixture client in both supported launch modes.</done>
</task>

</tasks>

<threat_model>
## ASVS L1 Trust Boundaries

| Boundary | Threat | Severity | Disposition and blocking mitigation |
|---|---|---|---|
| External HTTP → test fixture | Test-only reset/auth authority is exposed in production | critical | Mitigate; compile-time test guard, explicit flag, route toggle, and packaged-absence tests block completion. |
| Request → fixture command | Forged method/schema/credential mutates fixture state | high | Mitigate; POST-only exact-schema constant-time credential checks and negative tests block completion. |
| Launcher/client → logs | Ephemeral credential is printed or echoed | high | Mitigate; channel/log sentinel scans and redacted errors block completion. |
| Fixture generation → database | Oversized seed creates denial of service | high | Mitigate; fixed bounded deterministic records and rejection tests block completion. |
</threat_model>

<verification>
Run host fixture ExUnit, list the browser fixture inventory separately, execute the fixture contract through secret-aware `with-showcase-server.sh` HOST runtime and authoritative Docker runtime, and additionally prove the route is absent when the flag is off.
</verification>

<success_criteria>
The Phase 81 fixture seam is deterministic and usable in tests but unavailable and unobservable outside explicit test launch.
</success_criteria>

## Artifacts this phase produces

- Test-only Phase 81 host fixture module and route.
- Host/Docker launch support with ephemeral credentials.
- Typed fail-closed browser fixture client and isolation tests.

<output>
Create `.planning/phases/81-page-migration-wave-3-batches-workflows-lifeline/81-07-SUMMARY.md` when done.
</output>
