---
phase: 82-cross-cutting-a11y-motion-responsive-hardening-sweep
plan: "05"
type: execute
wave: 3
depends_on:
  - "82-03"
  - "82-04"
files_modified:
  - assets/oban_powertools/tokens.css
  - priv/static/oban_powertools/oban_powertools.css
  - test/oban_powertools/web/theme_tokens_test.exs
  - test/oban_powertools/web/assets_test.exs
autonomous: true
requirements:
  - A11Y-03
  - A11Y-04
  - MOTION-01
  - MOTION-02
  - NAV-02
  - DATA-03
must_haves:
  truths:
    - "Every source and packaged motion declaration is token-owned, purposeful, interruptible, and paired with both reduction paths."
    - "Semantic token pairs meet exact light/dark/high-contrast thresholds, including muted text and focus adjacency."
    - "Focus, target, wrapping, and bounded machine-content repairs remain root-scoped and use one semantic tree."
    - "Source and packaged CSS are byte-identical and repeat-build stable."
    - "The early Node owner scans production HEEx/Elixir, client JavaScript/TypeScript, and source/package CSS; this plan only consumes that policy."
  artifacts:
    - path: assets/oban_powertools/tokens.css
      provides: "Corrected scoped contrast, focus, target, reflow, and motion ownership"
    - path: priv/static/oban_powertools/oban_powertools.css
      provides: "Generated byte-identical packaged CSS"
    - path: test/oban_powertools/web/theme_tokens_test.exs
      provides: "Exact ExUnit token-value, selector-scope, and asset contracts without a source walker"
  key_links:
    - from: assets/oban_powertools/tokens.css
      to: priv/static/oban_powertools/oban_powertools.css
      via: "mix oban_powertools.assets.build"
      pattern: "obpt"
    - from: test/oban_powertools/web/theme_tokens_test.exs
      to: assets/oban_powertools/tokens.css
      via: "parsed exact token and declaration inventory"
      pattern: "reduced-motion"
---

<objective>
Close system-wide token/CSS contrast, focus, target, reflow, and motion defects
at the shared owner seam.

Purpose: Apply D-06, D-07, D-08, D-10, D-11, D-12, D-13, D-19, D-21, and D-22 without page-local
visual systems or package drift.
Output: Corrected root-scoped source CSS, generated packaged CSS, and exact
static/asset contracts.
</objective>

<execution_context>
@/Users/jon/.agents/gsd-core/workflows/execute-plan.md
@/Users/jon/.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/82-cross-cutting-a11y-motion-responsive-hardening-sweep/82-CONTEXT.md
@.planning/phases/82-cross-cutting-a11y-motion-responsive-hardening-sweep/82-RESEARCH.md
@.planning/phases/82-cross-cutting-a11y-motion-responsive-hardening-sweep/82-UI-SPEC.md
@.planning/phases/82-cross-cutting-a11y-motion-responsive-hardening-sweep/82-PATTERNS.md
@.planning/phases/82-cross-cutting-a11y-motion-responsive-hardening-sweep/82-03-SUMMARY.md
@.planning/phases/82-cross-cutting-a11y-motion-responsive-hardening-sweep/82-04-SUMMARY.md
@assets/oban_powertools/tokens.css
@test/oban_powertools/web/theme_tokens_test.exs
@test/oban_powertools/web/assets_test.exs
</context>

<tasks>

The source/package CSS and token tests may contain milestone-closure changes.
Before each task, re-read every listed file and its current diff, preserve all
unrelated hunks, run the task's focused test, and stage only the task-owned
hunks/paths. Package CSS is generated only after preserving the current source
CSS; never replace a dirty source file from an earlier read or use
`git add -A`.

<task type="auto" tdd="true">
  <name>Task 1: Enforce exact contrast, focus, target, and reflow token contracts</name>
  <files>assets/oban_powertools/tokens.css, test/oban_powertools/web/theme_tokens_test.exs</files>
  <behavior>
    - Light/dark text is 4.5:1 normal and 3:1 large; meaningful non-text is 3:1; muted stays 4.5:1.
    - High-contrast body text is 7:1, semantic boundaries are solid, and focus is at least 2px and 3:1 against both adjacent states.
    - Operator comfort controls expose 44px in one axis while normative target geometry remains browser-owned.
    - Ordinary content wraps/reflows; only labelled bounded machine content scrolls; responsive selectors never duplicate trees.
  </behavior>
  <action>
Add exact parsed token-pair tables and scoped source assertions first, then
repair only failures proven by those contracts and Plan 82-04's algorithms.
Use semantic Tier-2 roles; use `border-strong` where a boundary carries
meaning, not for decorative separators. Preserve the UI-SPEC spacing/type
contract and existing information hierarchy. Do not solve overflow by reducing
text, hiding content, or adding mobile/desktop copies.
  </action>
  <verify>
    <automated>mix test test/oban_powertools/web/theme_tokens_test.exs --seed 0</automated>
  </verify>
  <done>Exact token/source tests pass and all changed selectors remain below `.obpt-root` with one responsive tree.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Consume Node motion policy and rebuild byte-stable packaged CSS</name>
  <files>assets/oban_powertools/tokens.css, priv/static/oban_powertools/oban_powertools.css, test/oban_powertools/web/theme_tokens_test.exs, test/oban_powertools/web/assets_test.exs</files>
  <behavior>
    - Every transition/animation/keyframe records token duration/easing, purpose, and both media/root reduction selectors.
    - Raw timing/easing, unknown keyframes, unscoped motion, or reduction that hides content fail.
    - The Plan 82-03 Node validator remains the sole parser for all production roots, declarations, exclusions, and path:line diagnostics.
    - ExUnit remains limited to token values, selector scope, build repeatability, and source/package equality.
    - Asset build produces byte-identical source/package output on consecutive runs.
  </behavior>
  <action>
Run `verify-phase82-quality.mjs` before and after repairs and use its exact
diagnostics as the only source/motion inventory. Do not add any directory walk,
motion parser, exclusion registry, source inventory, or path diagnostic logic
to ExUnit. Keep `theme_tokens_test.exs` and `assets_test.exs` focused on exact
token values, scoped selectors, reduction declarations, build repeatability,
and byte equality. Repair each Node-reported motion declaration with the
shipped duration/easing tokens and paired
`prefers-reduced-motion` plus root `data-obpt-motion` rules. Ensure reduction
does not remove spinners, skeletons, overlays, disclosure, progress, focus, or
announcements. Run the normal asset builder twice; never edit packaged CSS
independently.
  </action>
  <verify>
    <automated>node test/browser/support/verify-phase82-quality.mjs &amp;&amp; mix oban_powertools.assets.build &amp;&amp; cp priv/static/oban_powertools/oban_powertools.css /tmp/obpt-phase82-css &amp;&amp; mix oban_powertools.assets.build &amp;&amp; cmp -s assets/oban_powertools/tokens.css priv/static/oban_powertools/oban_powertools.css &amp;&amp; cmp -s /tmp/obpt-phase82-css priv/static/oban_powertools/oban_powertools.css &amp;&amp; mix test test/oban_powertools/web/theme_tokens_test.exs test/oban_powertools/web/assets_test.exs --seed 0 &amp;&amp; node --test test/browser/support/verify-phase82-quality.test.mjs &amp;&amp; node test/browser/support/verify-phase82-quality.mjs</automated>
  </verify>
  <done>The sole Node inventory is green and both package equality and repeat-build stability pass without an ExUnit source walker.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Reviewed source CSS → shipped package asset | Generated output must not diverge from reviewed semantics. |
| Host page → `.obpt-root` | Repairs must not leak into or depend on host-global styles. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-82-05-ASSET | Tampering | packaged CSS | high | mitigate | Normal builder, byte equality, and repeat-build hash comparison. |
| T-82-05-HOST | Tampering | CSS scope | high | mitigate | Exact root-scope tests reject global selectors/raw visual systems. |
| T-82-05-MOTION | Denial of service | animations/transitions | high | mitigate | Token inventory plus independent reduction paths and no delayed action/focus/content. |
</threat_model>

<verification>
Focused token/assets tests, two builds, byte comparisons, and `git diff --check`
all pass.
</verification>

<success_criteria>
Shared CSS owns all cross-page visual/motion repairs, remains isolated, and
ships reproducibly.
</success_criteria>

<output>
Create `82-05-SUMMARY.md` when complete.
</output>
