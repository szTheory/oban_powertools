---
phase: 82-cross-cutting-a11y-motion-responsive-hardening-sweep
plan: "17"
type: execute
wave: 2
depends_on:
  - "82-02"
files_modified:
  - lib/oban_powertools/web/copy.ex
  - test/oban_powertools/web/copy_contract_test.exs
  - scripts/showcase_manifest.exs
  - test/browser/support/manifest.ts
  - test/browser/support/manifest-smoke.mjs
autonomous: true
requirements:
  - COPY-01
  - COPY-02
  - A11Y-01
  - A11Y-04
must_haves:
  truths:
    - "One finite production Elixir module owns glossary, forbidden phrases, state/recovery requirements, confirmation order, and exact exclusions."
    - "The schema remains 8 and inventory remains exactly 163 targets/99 page stories; copy policy is non-inventory metadata."
    - "TypeScript validates generated policy but does not duplicate its literals or target IDs."
  artifacts:
    - path: lib/oban_powertools/web/copy.ex
      provides: "Deterministic production-owned BRAND-04 policy"
    - path: scripts/showcase_manifest.exs
      provides: "Schema-8 projection of copy policy beside unchanged generated inventory"
    - path: test/browser/support/manifest.ts
      provides: "Typed closed validation of copy policy and existing inventory"
    - path: test/browser/support/manifest-smoke.mjs
      provides: "Independent static schema/policy/inventory validation"
  key_links:
    - from: lib/oban_powertools/web/copy.ex
      to: scripts/showcase_manifest.exs
      via: "Copy.contract/0 serialized once"
      pattern: "Copy.contract"
    - from: scripts/showcase_manifest.exs
      to: test/browser/support/manifest.ts
      via: "copy_contract field"
      pattern: "copy_contract"
---

<objective>
Implement the finite production copy policy and expose it through the existing
schema-8 generated manifest without changing showcase inventory.

Purpose: Satisfy D-01, D-14..D-18, and D-21 with one Elixir owner consumed by
ExUnit and browser scans.
Output: Green copy contracts and exact dual manifest validators.
</objective>

<execution_context>
@/Users/jon/.agents/gsd-core/workflows/execute-plan.md
@/Users/jon/.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/82-cross-cutting-a11y-motion-responsive-hardening-sweep/82-CONTEXT.md
@.planning/phases/82-cross-cutting-a11y-motion-responsive-hardening-sweep/82-RESEARCH.md
@.planning/phases/82-cross-cutting-a11y-motion-responsive-hardening-sweep/82-UI-SPEC.md
@.planning/phases/82-cross-cutting-a11y-motion-responsive-hardening-sweep/82-PATTERNS.md
@.planning/phases/82-cross-cutting-a11y-motion-responsive-hardening-sweep/82-02-SUMMARY.md
@lib/oban_powertools/web/status_taxonomy.ex
@lib/oban_powertools/web/control_plane_presenter.ex
@scripts/showcase_manifest.exs
@test/browser/support/manifest.ts
</context>

<tasks>

The manifest and smoke-validator files may already contain milestone-closure
hunks. Re-read every listed file and its current diff before each task,
preserve unrelated changes, run that task's focused tests, and stage only
task-owned paths/hunks. Commit the Copy owner and schema projection separately;
never rewrite the manifest script from a stale read or use broad staging.

<task type="auto" tdd="true">
  <name>Task 1: Implement the finite Copy contract and source scanner policy</name>
  <files>lib/oban_powertools/web/copy.ex, test/oban_powertools/web/copy_contract_test.exs</files>
  <behavior>
    - Contract data is deterministic, finite, compile-time-owned, and contains no runtime/fixture/provider values.
    - Exact scan roots and exclusions report path, line, phrase, and canonical replacement.
    - Unused/stale exclusions fail and legitimate action-specific terms remain distinguishable.
  </behavior>
  <action>
Create `ObanPowertools.Web.Copy` with closed compile-time maps/lists for the
UI-SPEC taxonomy, forbidden phrases, state/recovery requirements,
confirmation order, safe dismiss rules, host-outcome verbs, scan roots, and
exact exclusions. Return only deterministic policy data. Keep unique page
consequence/support prose out of this module. Make the RED ExUnit suite green
without broad path ignores, dynamic atoms, localization work, or runtime input.
  </action>
  <verify>
    <automated>mix test test/oban_powertools/web/copy_contract_test.exs --seed 0</automated>
  </verify>
  <done>The finite contract and source scan are green with exact diagnostics and no confidential/runtime values.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Project and validate copy policy in schema 8</name>
  <files>scripts/showcase_manifest.exs, test/browser/support/manifest.ts, test/browser/support/manifest-smoke.mjs</files>
  <behavior>
    - copy_contract is validated as exact non-inventory metadata from the Elixir module.
    - Schema version remains 8 and target reconstruction remains 163/99 with existing order.
    - Missing/extra/malformed policy and any duplicated target list fail both validators.
  </behavior>
  <action>
Serialize `Copy.contract/0` beside the current catalog-derived fields. Extend
the typed runtime validator and independent smoke validator with exact nested
field/type/value constraints. Preserve existing target reconstruction and
family order; do not introduce target/story IDs inside copy policy or a
TypeScript phrase registry. Add focused malformed-manifest fixtures through
existing validator self-test patterns.
  </action>
  <verify>
    <automated>npm run showcase:manifest &amp;&amp; node test/browser/support/manifest-smoke.mjs</automated>
  </verify>
  <done>Both validators accept schema 8 with exactly 163 targets and 99 page stories and reject policy/inventory drift.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Production policy → generated JSON | Only finite non-sensitive policy may cross into browser-readable artifacts. |
| Generated JSON → browser loops | Malformed fields or duplicated inventories must fail before tests execute. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-82-17-INVENTORY | Tampering | manifest projection | high | mitigate | Preserve schema-8 reconstruction and exact 163/99 order in two independent validators. |
| T-82-17-POLICY-LEAK | Information disclosure | Copy.contract/0 | high | mitigate | Compile-time finite policy only; prohibit runtime, fixture, reason, token, payload, or exception data. |
| T-82-17-ATOM | Denial of service | Copy parser | high | mitigate | Use fixed atoms/binaries only and never create atoms from manifest/source input. |
</threat_model>

<verification>
Focused ExUnit, manifest generation, independent smoke validation, and
`git diff --check` pass with unchanged inventory counts.
</verification>

<success_criteria>
All copy consumers share one generated policy owner while schema 8 and the
complete showcase graph remain stable.
</success_criteria>

<output>
Create `82-17-SUMMARY.md` when complete.
</output>
