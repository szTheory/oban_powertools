---
phase: 71-token-layer-isolated-theming-engine
plan: 02
type: execute
wave: 1
depends_on:
  - 71-01
files_modified:
  - assets/oban_powertools/tokens.css
  - assets/oban_powertools/theme.js
  - priv/static/oban_powertools/oban_powertools.css
  - priv/static/oban_powertools/oban_powertools.js
  - lib/mix/tasks/oban_powertools.assets.build.ex
autonomous: true
requirements:
  - TOKEN-01
  - TOKEN-02
  - TOKEN-04
  - TOKEN-05
  - MOTION-01
  - A11Y-03
must_haves:
  truths:
    - "Token CSS defines primitive and semantic `--obpt-*` roles under `.obpt-root` only."
    - "Theme/effective-theme selectors remap color semantics without changing layout-affecting tokens or CSS properties."
    - "Theme JS defaults to system, applies only to `.obpt-root`, persists only `oban_powertools:theme`, and honors color/contrast/reduced-motion media preferences."
    - "Compiled CSS/JS output is deterministic and byte-stable."
  artifacts:
    - path: "assets/oban_powertools/tokens.css"
      provides: "Source token layer and proof-seam classes"
    - path: "assets/oban_powertools/theme.js"
      provides: "Source vanilla theme controller"
    - path: "lib/mix/tasks/oban_powertools.assets.build.ex"
      provides: "Deterministic asset build task"
    - path: "priv/static/oban_powertools/oban_powertools.css"
      provides: "Compiled CSS asset"
    - path: "priv/static/oban_powertools/oban_powertools.js"
      provides: "Compiled JS asset"
  key_links:
    - from: "lib/mix/tasks/oban_powertools.assets.build.ex"
      to: "priv/static/oban_powertools/oban_powertools.css"
      via: "deterministic copy from assets/oban_powertools/tokens.css"
      pattern: "oban_powertools\\.css"
    - from: "assets/oban_powertools/theme.js"
      to: ".obpt-root"
      via: "querySelectorAll scoped root application"
      pattern: "obpt-root"
---

<objective>
Implement the source token layer, vanilla theme controller, deterministic asset build task, and compiled static outputs.

Purpose: Later plans can serve and render the assets only after the CSS/JS contract exists and the Wave 0 token/theme checks can turn green.
Output: `assets/oban_powertools/*`, `priv/static/oban_powertools/*`, and `mix oban_powertools.assets.build`.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/71-token-layer-isolated-theming-engine/71-RESEARCH.md
@.planning/phases/71-token-layer-isolated-theming-engine/71-VALIDATION.md
@.planning/phases/70-brand-book-identity-foundation/70-CONTEXT.md
@guides/brand-book.md
@mix.exs
@test/oban_powertools/web/theme_tokens_test.exs
@test/oban_powertools/web/assets_test.exs
</context>

## Artifacts This Phase Produces

See `71-01-PLAN.md` for the complete phase artifact inventory. This plan creates the source CSS/JS assets, compiled CSS/JS assets, and `Mix.Tasks.ObanPowertools.Assets.Build.run/1`.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create scoped two-tier token CSS and proof-seam classes</name>
  <files>assets/oban_powertools/tokens.css</files>
  <read_first>
    - guides/brand-book.md
    - .planning/phases/71-token-layer-isolated-theming-engine/71-RESEARCH.md
    - .planning/phases/71-token-layer-isolated-theming-engine/71-VALIDATION.md
    - test/oban_powertools/web/theme_tokens_test.exs
    - lib/oban_powertools/web/jobs_live.ex
  </read_first>
  <behavior>
    - Test: `theme_tokens_test.exs` finds all required primitive and semantic `--obpt-*` custom properties.
    - Test: contrast helper reports required ratios for text, muted, focus, borders, and status pairs.
    - Test: static selector scan finds token and proof classes scoped to `.obpt-root`.
  </behavior>
  <action>Create `assets/oban_powertools/tokens.css` as the single source CSS for Phase 71. Implement primitive palette tokens per D-07..D-12 with cool slate neutrals, indigo accent, cyan info, emerald success, amber warning, red danger, white, and black. Implement semantic color roles per D-10 for surface, elevated, overlay, border, border-strong, text, muted, accent, info, success, warning, danger, and focus. Implement distinct light, dark, and high-contrast semantic mappings per D-11 and D-12, with `data-obpt-theme`, `data-obpt-effective-theme`, color-scheme, and contrast theme selectors assigning only color/status/focus/semantic color variables. Keep spacing, type scale, line-height, radius, width, height, padding, margin, gap, display, position, transform, and layout dimensions defined outside theme/effective-theme selector overrides so theme changes cannot produce layout shift. Implement exact D-13 font cascades, D-14 mono and `--obpt-numeric-tabular`, D-15 type scale and spacing values, restrained radii/elevation, and MOTION-01 duration/easing tokens. Add proof-seam classes `.obpt-shell`, `.obpt-tab`, `.obpt-tab--active`, `.obpt-badge`, `.obpt-modal-backdrop`, `.obpt-modal`, `.obpt-modal-summary`, `.obpt-form-label`, `.obpt-input`, `.obpt-alert`, `.obpt-alert--danger`, `.obpt-button`, `.obpt-button--neutral`, `.obpt-button--primary`, and `.obpt-button--danger`; those classes must reference semantic tokens, not raw color or spacing values. Keep raw hex values only in primitive palette declarations per D-10.</action>
  <acceptance_criteria>
    - `.obpt-root` is the only root namespace for token declarations and proof-seam class selectors.
    - Light semantic values include white or near-white surfaces, cool slate text, indigo accent, cyan info, emerald success, deep amber warning text, and true-red danger.
    - Dark semantic values use near-black slate surface, lighter dark elevation steps, desaturated accents/statuses, and muted text at least 4.5:1.
    - High-contrast semantic values provide enhanced text contrast and solid borders over tinted fills.
    - Theme and effective-theme selector blocks pass the Wave 0 layout-invariance assertion by remapping only color/status/focus/semantic color variables.
    - Motion tokens include fast/base/slow durations and standard/emphasized easings, with a reduced-motion override that makes non-essential transitions effectively instantaneous.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/oban_powertools/web/theme_tokens_test.exs</automated>
  </verify>
  <done>`assets/oban_powertools/tokens.css` turns the token/category, contrast, motion, and scoped CSS portions of `theme_tokens_test.exs` green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create root-scoped vanilla theme controller</name>
  <files>assets/oban_powertools/theme.js</files>
  <read_first>
    - .planning/phases/71-token-layer-isolated-theming-engine/71-RESEARCH.md
    - .planning/phases/71-token-layer-isolated-theming-engine/71-VALIDATION.md
    - guides/brand-book.md
    - test/oban_powertools/web/theme_tokens_test.exs
  </read_first>
  <behavior>
    - Test: stored invalid theme values fall back to `system`.
    - Test: explicit themes persist only under `oban_powertools:theme`.
    - Test: system theme resolves through color-scheme and contrast media queries and updates `.obpt-root` data attributes.
    - Test: no host root or host theme storage mutation strings are present.
  </behavior>
  <action>Create `assets/oban_powertools/theme.js` as dependency-free browser JavaScript. Expose `window.ObanPowertoolsTheme` with `apply(root, theme)`, `setTheme(theme)`, `storedTheme()`, and `effectiveTheme(theme)`. Allow exactly `system`, `light`, `dark`, and `high-contrast`; invalid values fall back to `system`. Read and write only `localStorage["oban_powertools:theme"]`. Apply `data-obpt-theme`, `data-obpt-effective-theme`, and `data-obpt-motion` only on `.obpt-root` elements. Use `matchMedia` for `prefers-color-scheme: dark`, `prefers-contrast: more`, and `prefers-reduced-motion: reduce`. Register change listeners so roots using `system` update when media preferences change. Add delegated click handling for `[data-obpt-theme-choice]` controls without requiring LiveView hook registration or host bundler imports.</action>
  <acceptance_criteria>
    - A missing or invalid stored value returns `system`.
    - `system` resolves to high-contrast when contrast preference is more, otherwise dark or light from color preference.
    - Explicit `light`, `dark`, and `high-contrast` choices do not get overwritten by media-query changes.
    - The script can run as the first child inside `.obpt-root` and apply attributes before the rest of the LiveView content is parsed.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/oban_powertools/web/theme_tokens_test.exs</automated>
  </verify>
  <done>`assets/oban_powertools/theme.js` satisfies TOKEN-04 and the theme-controller portions of `theme_tokens_test.exs`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add deterministic asset build task and compiled outputs</name>
  <files>lib/mix/tasks/oban_powertools.assets.build.ex, priv/static/oban_powertools/oban_powertools.css, priv/static/oban_powertools/oban_powertools.js</files>
  <read_first>
    - .planning/phases/71-token-layer-isolated-theming-engine/71-RESEARCH.md
    - .planning/phases/71-token-layer-isolated-theming-engine/71-VALIDATION.md
    - assets/oban_powertools/tokens.css
    - assets/oban_powertools/theme.js
    - test/oban_powertools/web/assets_test.exs
  </read_first>
  <behavior>
    - Test: `mix oban_powertools.assets.build` creates both compiled files under `priv/static/oban_powertools`.
    - Test: two consecutive build runs leave identical SHA256 hashes.
    - Test: compiled CSS and JS content match normalized source content without generated timestamps.
  </behavior>
  <action>Create `lib/mix/tasks/oban_powertools.assets.build.ex` defining `Mix.Tasks.ObanPowertools.Assets.Build.run/1`. The task must create `priv/static/oban_powertools`, copy normalized `assets/oban_powertools/tokens.css` to `priv/static/oban_powertools/oban_powertools.css`, copy normalized `assets/oban_powertools/theme.js` to `priv/static/oban_powertools/oban_powertools.js`, preserve LF line endings, ensure one final newline, and avoid timestamps, manifests, random IDs, or dependency-driven minification. Run the task once so the compiled files are committed. Do not add npm, Tailwind, esbuild, or Phoenix digest dependencies for this phase.</action>
  <acceptance_criteria>
    - `mix oban_powertools.assets.build` exits 0 from the repository root.
    - `priv/static/oban_powertools/oban_powertools.css` and `.js` exist after the task runs.
    - Re-running the task twice leaves no git diff in `priv/static/oban_powertools`.
    - Compiled outputs are deterministic copies of the source assets with normalized final newline behavior.
  </acceptance_criteria>
  <verify>
    <automated>mix oban_powertools.assets.build</automated>
    <automated>mix test test/oban_powertools/web/theme_tokens_test.exs test/oban_powertools/web/assets_test.exs</automated>
  </verify>
  <done>The build task and compiled outputs satisfy TOKEN-05 byte-stability checks; asset route checks may remain RED until Plan 71-03 wires the Plug and router.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Browser -> theme controller | Stored theme and media preferences are untrusted browser state. |
| Source assets -> compiled assets | Build task transforms committed source into shipped static files. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-71-05 | Tampering | `theme.js` localStorage handling | mitigate | Whitelist theme values and fall back to `system` for invalid values. |
| T-71-06 | Tampering | `tokens.css` CSS scope | mitigate | Keep token declarations and proof classes under `.obpt-root`; Wave 0 static checks enforce scope. |
| T-71-07 | Denial of Service | asset build | mitigate | Use deterministic copy with no external tools or network-dependent package installs. |
| T-71-SC | Tampering | npm/pip/cargo installs | accept | No package installs are planned; Package Legitimacy Audit says none. |
</threat_model>

<verification>
- `mix test test/oban_powertools/web/theme_tokens_test.exs`
- `mix test test/oban_powertools/web/theme_tokens_test.exs` includes the theme-selector layout-invariance assertions for TOKEN-05.
- `mix oban_powertools.assets.build`
- `mix oban_powertools.assets.build` again, followed by `git diff --exit-code -- priv/static/oban_powertools`
- `mix test test/oban_powertools/web/assets_test.exs` may remain RED only for missing asset Plug/router/package behavior owned by Plan 71-03.
</verification>

<success_criteria>
The source and compiled asset files exist, token/theme static tests pass, and the asset build is byte-stable.
</success_criteria>

<output>
Create `.planning/phases/71-token-layer-isolated-theming-engine/71-02-SUMMARY.md` when done.
</output>
