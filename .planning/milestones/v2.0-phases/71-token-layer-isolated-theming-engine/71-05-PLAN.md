---
phase: 71-token-layer-isolated-theming-engine
plan: 05
type: execute
wave: 4
depends_on:
  - 71-04
files_modified:
  - lib/oban_powertools/web/jobs_live.ex
  - test/oban_powertools/web/live/jobs_live_test.exs
  - examples/phoenix_host/test/phoenix_host_web/oban_powertools_theme_isolation_test.exs
autonomous: true
requirements:
  - TOKEN-01
  - TOKEN-02
  - TOKEN-04
  - TOKEN-05
  - MOTION-01
  - A11Y-03
must_haves:
  truths:
    - "JobsLive state badges, state tabs, and preview modals use token-backed `.obpt-*` classes."
    - "JobsLive behavior for state navigation, preview, execute, bulk preview, and bulk execute remains unchanged."
    - "The example host proves host pages outside `/ops/jobs` are unchanged and Powertools theming remains isolated."
  artifacts:
    - path: "lib/oban_powertools/web/jobs_live.ex"
      provides: "Proof seam migration only: badges, tabs, and single/bulk preview modal classes"
    - path: "test/oban_powertools/web/live/jobs_live_test.exs"
      provides: "Proof seam and behavior regression assertions"
    - path: "examples/phoenix_host/test/phoenix_host_web/oban_powertools_theme_isolation_test.exs"
      provides: "Example host isolation proof"
  key_links:
    - from: "lib/oban_powertools/web/jobs_live.ex"
      to: "assets/oban_powertools/tokens.css"
      via: ".obpt-* classes backed by token CSS"
      pattern: "obpt-"
    - from: "examples/phoenix_host/test/phoenix_host_web/oban_powertools_theme_isolation_test.exs"
      to: "examples/phoenix_host/lib/phoenix_host_web/router.ex"
      via: "host `/` and `/ops/jobs/jobs` assertions"
      pattern: "/ops/jobs/jobs"
---

<objective>
Complete Phase 71 with the narrow JobsLive proof seam and example-host isolation proof.

Purpose: The token/theme layer must be exercised end-to-end without starting the broad migration of all operator pages.
Output: JobsLive badge/tab/modal classes on `.obpt-*` tokens, preserved behavior tests, and passing host-isolation tests.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/71-token-layer-isolated-theming-engine/71-RESEARCH.md
@.planning/phases/71-token-layer-isolated-theming-engine/71-VALIDATION.md
@.planning/phases/70-brand-book-identity-foundation/70-CONTEXT.md
@guides/brand-book.md
@assets/oban_powertools/tokens.css
@assets/oban_powertools/theme.js
@lib/oban_powertools/web/jobs_live.ex
@test/oban_powertools/web/live/jobs_live_test.exs
@examples/phoenix_host/lib/phoenix_host_web/router.ex
@examples/phoenix_host/lib/phoenix_host_web/components/layouts/root.html.heex
@examples/phoenix_host/test/phoenix_host_web/controllers/page_controller_test.exs
@examples/phoenix_host/test/phoenix_host_web/oban_powertools_control_plane_smoke_test.exs
</context>

## Artifacts This Phase Produces

See `71-01-PLAN.md` for the complete phase artifact inventory. This plan finalizes `.obpt-badge`, `data-obpt-tone`, `.obpt-tab`, `.obpt-tab--active`, `.obpt-modal-backdrop`, `.obpt-modal`, `.obpt-modal-summary`, `.obpt-form-label`, `.obpt-input`, `.obpt-alert--danger`, `.obpt-button--neutral`, `.obpt-button--primary`, and `.obpt-button--danger` usage in `JobsLive`.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Migrate state badges and state tabs to token-backed classes</name>
  <files>lib/oban_powertools/web/jobs_live.ex, test/oban_powertools/web/live/jobs_live_test.exs</files>
  <read_first>
    - lib/oban_powertools/web/jobs_live.ex
    - test/oban_powertools/web/live/jobs_live_test.exs
    - assets/oban_powertools/tokens.css
    - guides/brand-book.md
    - .planning/phases/71-token-layer-isolated-theming-engine/71-RESEARCH.md
  </read_first>
  <behavior>
    - Test: state tab buttons render `.obpt-tab` and the active state renders `.obpt-tab--active`.
    - Test: job state badges render `.obpt-badge` and `data-obpt-tone`.
    - Test: current state navigation and row rendering behavior remains unchanged.
  </behavior>
  <action>Update only the state badge and state tab seam in `lib/oban_powertools/web/jobs_live.ex`. Change `state_tab_class/1` so inactive tabs return `obpt-tab` and active tabs return `obpt-tab obpt-tab--active`. Change state badge markup in both detail and list rows so the span class is `obpt-badge` and the tone is carried by `data-obpt-tone={state_badge_tone(job.state)}`. Add private `state_badge_tone/1` mapping: `executing` to `info`, `retryable` to `warning`, `discarded` to `danger`, `completed` to `success`, and every other state to `neutral`. Preserve the rendered state text, `phx-click`, `phx-value-state`, row structure, filter behavior, and existing helper names where tests already reference behavior. Update JobsLive tests only to assert the new token-backed classes and tone attributes; do not weaken existing behavior assertions.</action>
  <acceptance_criteria>
    - `state_tab_class(true)` produces `obpt-tab obpt-tab--active`; `state_tab_class(false)` produces `obpt-tab`.
    - State badges no longer concatenate Tailwind color utility fragments for the proof seam.
    - Each rendered state badge has text plus `data-obpt-tone`, satisfying D-12 color-is-not-the-only-channel.
    - Existing state tab navigation test still patches from available to executing and renders the executing worker.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/oban_powertools/web/live/jobs_live_test.exs</automated>
    <automated>mix test test/oban_powertools/web/theme_tokens_test.exs</automated>
  </verify>
  <done>JobsLive state badges and tabs use `.obpt-*` token-backed classes without behavior regression.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Migrate single and bulk preview modal classes to token-backed classes</name>
  <files>lib/oban_powertools/web/jobs_live.ex, test/oban_powertools/web/live/jobs_live_test.exs</files>
  <read_first>
    - lib/oban_powertools/web/jobs_live.ex
    - test/oban_powertools/web/live/jobs_live_test.exs
    - assets/oban_powertools/tokens.css
    - guides/brand-book.md
    - .planning/phases/71-token-layer-isolated-theming-engine/71-RESEARCH.md
  </read_first>
  <behavior>
    - Test: single-job preview modal renders `.obpt-modal-backdrop`, `.obpt-modal`, `.obpt-modal-summary`, token-backed form/input/button classes, and still executes with a reason.
    - Test: bulk preview modal renders the same modal class family and still executes bulk actions.
    - Test: disabled confirm buttons still depend on `String.trim(@reason) == ""`.
  </behavior>
  <action>Update only the single-job preview modal and bulk preview modal class attributes in `lib/oban_powertools/web/jobs_live.ex`. Replace backdrop, panel, summary, label, input, error, cancel/dismiss button, and confirm button classes with the token-backed class family defined in `assets/oban_powertools/tokens.css`: `.obpt-modal-backdrop`, `.obpt-modal`, `.obpt-modal-summary`, `.obpt-form-label`, `.obpt-input`, `.obpt-alert obpt-alert--danger`, `.obpt-button obpt-button--neutral`, `.obpt-button obpt-button--primary`, and `.obpt-button obpt-button--danger`. Use primary for retry and danger for cancel/discard. Preserve all LiveView events, form names, disabled expressions, copy, preview token flow, Lifeline calls, flash behavior, and bulk selection behavior.</action>
  <acceptance_criteria>
    - Single preview still shows `Retry Job #id`, `Cancel Job #id`, or `Discard Job #id` according to preview action.
    - Single preview execute test still opens preview, accepts reason, submits `execute`, and patches back to the job detail path.
    - Bulk preview still shows `Bulk Retry`, `Bulk Cancel`, or `Bulk Discard` with selected count.
    - Bulk execute test still clears selection and renders no jobs after the existing discard scenario.
    - No other operator LiveView file is modified by this task.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/oban_powertools/web/live/jobs_live_test.exs</automated>
    <automated>git diff --name-only -- 'lib/oban_powertools/web/*_live.ex' | grep -v '^lib/oban_powertools/web/jobs_live.ex$' | grep -q . && exit 1 || exit 0</automated>
  </verify>
  <done>JobsLive single and bulk preview modals use token-backed `.obpt-*` classes and existing behavior remains green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Close example-host isolation, byte-stability, and full phase gates</name>
  <files>examples/phoenix_host/test/phoenix_host_web/oban_powertools_theme_isolation_test.exs, test/oban_powertools/web/live/jobs_live_test.exs</files>
  <read_first>
    - examples/phoenix_host/test/phoenix_host_web/oban_powertools_theme_isolation_test.exs
    - examples/phoenix_host/test/phoenix_host_web/controllers/page_controller_test.exs
    - examples/phoenix_host/test/phoenix_host_web/oban_powertools_control_plane_smoke_test.exs
    - examples/phoenix_host/lib/phoenix_host_web/components/layouts/root.html.heex
    - test/oban_powertools/web/theme_tokens_test.exs
    - test/oban_powertools/web/assets_test.exs
    - test/oban_powertools/web/live/jobs_live_test.exs
  </read_first>
  <behavior>
    - Test: host `/` remains unchanged and contains no Powertools root or asset links.
    - Test: `/ops/jobs/jobs` contains Powertools shell, asset links, and proof-seam classes.
    - Test: byte-stability and forbidden selector/storage checks are green.
  </behavior>
  <action>Run the Wave 0 example-host isolation test and update only assertion details needed to match the final class and route names from Plans 71-02 through 71-05. Keep the test proving both sides of the boundary: host `/` has existing home copy and no Powertools root/assets; Powertools `/ops/jobs/jobs?state=available` has `.obpt-root`, `data-obpt-theme="system"`, md5 CSS/JS asset paths, and the JobsLive proof seam. Run the byte-stability gate by executing `mix oban_powertools.assets.build` twice and confirming `priv/static/oban_powertools` is unchanged. Do not migrate the other operator pages beyond the live-session shell wrapper created in Plan 71-04.</action>
  <acceptance_criteria>
    - Example-host isolation test passes together with the existing host page controller test.
    - Root host markup remains the existing host layout and is not used as the Powertools theme state carrier.
    - `theme_tokens_test.exs` forbidden selector/storage checks pass.
    - `assets_test.exs` byte-stability and md5 route checks pass.
    - `mix test --exclude host_contract` passes.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/oban_powertools/web/theme_tokens_test.exs test/oban_powertools/web/assets_test.exs test/oban_powertools/web/live/jobs_live_test.exs</automated>
    <automated>mix oban_powertools.assets.build && mix oban_powertools.assets.build && git diff --exit-code -- priv/static/oban_powertools</automated>
    <automated>mix test --exclude host_contract</automated>
    <automated>cd examples/phoenix_host && mix test test/phoenix_host_web/oban_powertools_theme_isolation_test.exs test/phoenix_host_web/controllers/page_controller_test.exs test/phoenix_host_web/oban_powertools_control_plane_smoke_test.exs</automated>
  </verify>
  <done>Phase 71 passes token/theme/asset/proof-seam tests, byte-stability checks, full suite gate, and example-host isolation proof.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Operator browser -> JobsLive DOM | Token-backed classes must not change LiveView mutation behavior. |
| Host app -> nested Powertools UI | Host pages and storage remain outside the Powertools theme boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-71-14 | Tampering | `JobsLive` proof seam | mitigate | Limit edits to badge/tab/modal classes and keep existing behavior tests green. |
| T-71-15 | Tampering | example host isolation | mitigate | Test host `/` and nested `/ops/jobs/jobs` separately for leakage in either direction. |
| T-71-16 | Denial of Service | modal class migration | mitigate | Preserve form events, disabled expressions, and Lifeline preview/execute flows exactly. |
| T-71-SC | Tampering | npm/pip/cargo installs | accept | No package installs are planned; Package Legitimacy Audit says none. |
</threat_model>

<verification>
- `mix test test/oban_powertools/web/theme_tokens_test.exs test/oban_powertools/web/assets_test.exs test/oban_powertools/web/live/jobs_live_test.exs`
- `mix oban_powertools.assets.build && mix oban_powertools.assets.build && git diff --exit-code -- priv/static/oban_powertools`
- `mix test --exclude host_contract`
- `cd examples/phoenix_host && mix test test/phoenix_host_web/oban_powertools_theme_isolation_test.exs test/phoenix_host_web/controllers/page_controller_test.exs test/phoenix_host_web/oban_powertools_control_plane_smoke_test.exs`
</verification>

<success_criteria>
The phase goal is achieved when the library-owned token/theme assets are served and rendered under `.obpt-root`, JobsLive proof seam uses token-backed classes, host isolation is proven, and all Phase 71 validation gates are green.
</success_criteria>

<output>
Create `.planning/phases/71-token-layer-isolated-theming-engine/71-05-SUMMARY.md` when done.
</output>
