---
phase: 75-form-components
plan: 04
type: execute
wave: 4
depends_on: [75-03]
files_modified:
  - scripts/showcase_manifest.exs
  - test/browser/support/manifest.ts
  - test/browser/support/manifest-smoke.mjs
  - test/browser/support/showcase.ts
  - test/browser/specs/showcase.structure.spec.ts
  - test/browser/specs/showcase.vrt.spec.ts
  - test/browser/specs/showcase.a11y.spec.ts
autonomous: true
requirements: [FORM-01, FORM-02, COMP-02, COMP-03, COMP-04, A11Y-02]
must_haves:
  truths:
    - "Generated form targets are first-class manifest members and the only browser target source."
    - "Runtime validators reject malformed kinds, selectors, metadata, counts, or ordering."
    - "Generic structure, axe, and VRT loops automatically include every form story."
    - "Decision trace: D-23:"
  artifacts:
    - path: scripts/showcase_manifest.exs
      provides: generated form_stories and unified targets
    - path: test/browser/support/manifest.ts
      provides: ShowcaseFormStory validation and exported formStories
---

<objective>Integrate form stories atomically into the generated showcase manifest and browser target contract.</objective>
<execution_context>@/Users/jon/.codex/gsd-core/workflows/execute-plan.md</execution_context>
<context>
@.planning/phases/75-form-components/75-CONTEXT.md
@.planning/phases/75-form-components/75-PATTERNS.md
@.planning/phases/75-form-components/75-VALIDATION.md
@test/support/form_story_catalog.ex
@scripts/showcase_manifest.exs
@test/browser/support/manifest.ts
@test/browser/support/manifest-smoke.mjs
@test/browser/support/showcase.ts
@test/browser/specs/showcase.structure.spec.ts
@test/browser/specs/showcase.vrt.spec.ts
@test/browser/specs/showcase.a11y.spec.ts
</context>

## Artifacts this phase produces

- The full artifact set listed in 75-01, including all components/helpers, CSS/assets, nine form stories, showcase rendering, generated manifest and validators, behavior spec, baselines, and tests.

## Locked decision coverage

- D-23: Extend the generated manifest with first-class `kind: "form"` targets and require all TypeScript consumers to use generated targets rather than hardcoded form story lists.

<tasks>
<task type="auto">
  <name>Task 1: Generate and strictly validate form manifest targets</name>
  <files>scripts/showcase_manifest.exs, test/browser/support/manifest.ts, test/browser/support/manifest-smoke.mjs</files>
  <read_first>
    - @scripts/showcase_manifest.exs
    - @test/support/form_story_catalog.ex
    - @test/browser/support/manifest.ts
    - @test/browser/support/manifest-smoke.mjs
    - @.planning/phases/75-form-components/75-VALIDATION.md
  </read_first>
  <action>Alias/load `FormStoryCatalog`, serialize its nine entries as `kind: "form"` with normalized component(s), variant, state, story, snapshot, and a11y fields, add a top-level `form_stories`, append them after primitive stories to unified `targets`, and bump schema version if required. Extend TS types/guards with `ShowcaseFormStory`, exact `form-*` slug and derived-target validation, collection/target equality and ordering checks, and exported `formStories`. Extend the smoke validator with equivalent count, kind, selector, snapshot, uniqueness, and target-union checks. Keep form ids generated only from Elixir; do not create a TS form-id array.</action>
  <verify><automated>npm run showcase:manifest && node test/browser/support/manifest-smoke.mjs</automated></verify>
  <acceptance_criteria>
    - Command passes and generated JSON has nine `form_stories` plus nine corresponding unified targets.
    - Validators reject malformed kind/slug/selector/snapshot and duplicated/missing targets.
    - `rg 'form-input-states|form-textarea-select' test/browser --glob '*.ts' --glob '*.mjs'` returns no hardcoded story list entries.
  </acceptance_criteria>
  <done>Elixir catalog metadata is the validated browser source of truth.</done>
</task>

<task type="auto">
  <name>Task 2: Teach showcase structure support about form targets</name>
  <files>test/browser/support/showcase.ts, test/browser/specs/showcase.structure.spec.ts</files>
  <read_first>
    - @test/browser/support/showcase.ts
    - @test/browser/support/manifest.ts
    - @test/browser/specs/showcase.structure.spec.ts
    - @test/browser/specs/showcase.vrt.spec.ts
    - @test/browser/specs/showcase.a11y.spec.ts
    - @lib/oban_powertools/web/dev/showcase_live.ex
  </read_first>
  <action>Extend the existing target-kind branch so `kind === 'form'` resolves and asserts `data-obpt-form-story` plus component/variant/state/a11y metadata. Preserve generic `targetLocator` using each generated target's selector. Update structure checks to verify every unified target exactly once. Confirm VRT and axe specs still iterate `targets` and therefore gain forms without lists or branches; change those specs only if their narrowing currently excludes the new union member. Do not add masks, skip form targets, or alter the theme/viewport matrix.</action>
  <verify>
    <automated>npm run showcase:manifest && scripts/with-showcase-server.sh npm run visual:a11y:host -- test/browser/specs/showcase.structure.spec.ts --project chromium-320</automated>
    <automated>npm run showcase:manifest && scripts/with-showcase-server.sh npm run visual:a11y:host -- test/browser/specs/showcase.a11y.spec.ts --project chromium-320 --grep form</automated>
  </verify>
  <acceptance_criteria>
    - Both commands pass and structure checks count all nine form targets.
    - VRT/axe use unified generated targets, not form-specific lists.
    - Each form axe include points to exactly one story article.
  </acceptance_criteria>
  <done>Forms participate in structure and axe now and the existing full VRT matrix automatically.</done>
</task>
</tasks>

<threat_model standard="OWASP ASVS" level="L1" blocking_threshold="high">
High findings block completion. Boundaries: catalog serialization to JSON; JSON into TypeScript locators and snapshot paths.

| Threat | Risk | Mitigation / proof |
|---|---|---|
| Manifest tampering/path traversal | high | Strict form slug and derived snapshot/selector validators in TS and smoke test. |
| Omitted form targets overstate coverage | high | Collection/union exact equality, ordering, and count checks. |
| Selector injection scans wrong DOM | high | Derived catalog selectors and exactly-one structure assertions. |
| Hardcoded browser list drifts | medium | Generated `targets` only and grep acceptance check. |
| Crafted ARIA metadata masks semantics | medium | Browser scans actual rendered target; component behavior tests remain independent. |
</threat_model>

<verification>
- `npm run showcase:manifest && node test/browser/support/manifest-smoke.mjs`
- `npm run showcase:manifest && scripts/with-showcase-server.sh npm run visual:a11y:host -- test/browser/specs/showcase.structure.spec.ts --project chromium-320`
- `npm run showcase:manifest && scripts/with-showcase-server.sh npm run visual:a11y:host -- test/browser/specs/showcase.a11y.spec.ts --project chromium-320 --grep form`
</verification>
<success_criteria>
- Nine validated form targets join unified structure, VRT, and axe iteration.
- No TypeScript form target list exists.
</success_criteria>
<output>Create `.planning/phases/75-form-components/75-04-SUMMARY.md` when done.</output>
