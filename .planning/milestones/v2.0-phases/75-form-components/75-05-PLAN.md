---
phase: 75-form-components
plan: 05
type: execute
wave: 5
depends_on: [75-04]
files_modified:
  - test/browser/specs/forms.behavior.spec.ts
  - test/browser/__screenshots__/chromium-*/showcase/form-*/*.png
  - .planning/phases/75-form-components/75-VALIDATION.md
autonomous: true
requirements: [FORM-01, FORM-02, COMP-01, COMP-02, COMP-03, COMP-04, A11Y-02]
must_haves:
  truths:
    - "Browser tests prove semantic wiring and native keyboard behavior that axe cannot infer."
    - "Every form target has reviewed baselines across four themes and three viewports with no meaningful masking."
    - "Fast and full validation commands pass and the validation contract records Nyquist completion."
    - "Decision trace: D-25:"
  artifacts:
    - path: test/browser/specs/forms.behavior.spec.ts
      provides: targeted form semantics, keyboard, focus, state, motion, and reflow checks
    - path: test/browser/__screenshots__/chromium-*/showcase/form-*/*.png
      provides: committed form visual regression contracts
    - path: .planning/phases/75-form-components/75-VALIDATION.md
      provides: completed validation sign-off
---

<objective>Close Phase 75 with targeted browser proof, committed form baselines, and green fast/full validation gates.</objective>
<execution_context>@/Users/jon/.codex/gsd-core/workflows/execute-plan.md</execution_context>
<context>
@.planning/phases/75-form-components/75-CONTEXT.md
@.planning/phases/75-form-components/75-VALIDATION.md
@.planning/phases/75-form-components/75-UI-SPEC.md
@guides/visual-regression-and-a11y.md
@test/browser/support/manifest.ts
@test/browser/support/showcase.ts
@test/browser/specs/primitives.behavior.spec.ts
@test/browser/specs/showcase.vrt.spec.ts
@test/browser/specs/showcase.a11y.spec.ts
@lib/oban_powertools/web/components/forms.ex
@assets/oban_powertools/tokens.css
</context>

## Artifacts this phase produces

- The full artifact set listed in 75-01: `Forms` with ten exports and helpers; token/compiled CSS; `FormStoryCatalog` and nine story IDs; showcase and generated manifest/browser integration; component/catalog tests; targeted behavior spec; and all form PNG baselines.

## Locked decision coverage

- D-25: Add targeted browser checks beyond axe for label/description/error wiring, truthful invalid state, native keyboard behavior, disabled/read-only distinction, focus visibility, reduced motion, and 320px overflow.

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Prove form semantics and behavior beyond axe</name>
  <files>test/browser/specs/forms.behavior.spec.ts</files>
  <read_first>
    - @test/browser/specs/primitives.behavior.spec.ts
    - @test/browser/support/manifest.ts
    - @test/browser/support/showcase.ts
    - @lib/oban_powertools/web/dev/showcase_live.ex
    - @lib/oban_powertools/web/components/forms.ex
    - @assets/oban_powertools/tokens.css
    - @.planning/phases/75-form-components/75-VALIDATION.md
  </read_first>
  <action>Create a generated-metadata-backed `forms.behavior.spec.ts` (lookup via exported `formStories`, not local ids). Assert: label click focuses/toggles and final `for` matches control id; fieldset/legend names radio groups; caller description precedes unique generated hint/error ids; `aria-invalid` exists iff visible error exists; named boolean contains one hidden unchecked value while event selection contains none; checkbox and switch toggle with Space; native radio selection moves with arrow keys; disabled cannot focus/change while readonly remains focusable/selectable and retains value; filter controls have no combobox role or popup ARIA; focus outline has visible nonzero style in system/light/dark/high-contrast; pending state remains named and duplicate action prevention belongs to its parent region; reduced-motion collapses form transitions without hiding state; and root/story has no horizontal overflow in chromium-320. Use native control behavior rather than scripting state directly.</action>
  <verify>
    <automated>npm run showcase:manifest && scripts/with-showcase-server.sh npm run visual:a11y:host -- test/browser/specs/forms.behavior.spec.ts --project chromium-320</automated>
    <automated>npm run showcase:manifest && scripts/with-showcase-server.sh npm run visual:a11y:host -- test/browser/specs/forms.behavior.spec.ts --project chromium-wide</automated>
  </verify>
  <acceptance_criteria>
    - Both commands pass and cover every listed behavior.
    - Tests derive targets from generated form metadata and use normal Playwright artifacts on failure.
    - No test claims full manual screen-reader/page traversal or dialog behavior; those remain Phase 82.
  </acceptance_criteria>
  <done>Component-level keyboard, focus, association, state honesty, motion, and reflow are browser-proven.</done>
</task>

<task type="auto">
  <name>Task 2: Generate form baselines and record validation evidence</name>
  <files>test/browser/__screenshots__/chromium-*/showcase/form-*/*.png, .planning/phases/75-form-components/75-VALIDATION.md</files>
  <read_first>
    - @guides/visual-regression-and-a11y.md
    - @package.json
    - @test/browser/specs/showcase.vrt.spec.ts
    - @test/browser/specs/showcase.a11y.spec.ts
    - @test/browser/specs/forms.behavior.spec.ts
    - @.planning/phases/75-form-components/75-VALIDATION.md
  </read_first>
  <action>Use the repository's explicit Docker baseline-update workflow with `--grep form`. Review and keep only the nine story families across chromium-320/tablet/wide and system/light/dark/high-contrast. Never mask labels, focus rings, invalid borders/copy, checked/on signals, disabled/readonly differences, or long content. Fix source/story issues instead. Run the exact VALIDATION fast command and full command. Record the resulting execution evidence and final pass/fail status in `75-VALIDATION.md` while preserving its planning-time task mappings, approved sign-off, Nyquist flags, and deferred manual boundaries.</action>
  <verify>
    <automated>npm run vrt:update -- --grep form</automated>
    <automated>mix test test/oban_powertools/web/components/forms_test.exs test/oban_powertools/form_story_catalog_test.exs</automated>
    <automated>npm run visual:a11y && mix test --exclude host_contract && mix compile --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - All commands pass.
    - Exactly nine form target directories exist per viewport and four theme PNGs exist per target (108 form PNGs total).
    - `git status --short test/browser/.generated playwright-report test-results` shows no report/generated artifacts selected for commit.
    - Validation retains approved/true/true planning sign-off and actual plan/task/wave mappings, and records the executed command evidence and final status.
  </acceptance_criteria>
  <done>Reviewed form baselines and every Phase 75 automated validation gate are green.</done>
</task>
</tasks>

<threat_model standard="OWASP ASVS" level="L1" blocking_threshold="high">
High findings block completion. Boundaries: browser automation to real showcase controls; generated baselines to reviewer trust; automated evidence to accessibility claims.

| Threat | Risk | Mitigation / proof |
|---|---|---|
| Keyboard trap or inaccessible control | high | Native keyboard tests for every choice family and focus visibility in every theme. |
| Hidden checkbox value corrupts selection | high | Browser DOM proof plus ExUnit contract distinguishes modes. |
| Misleading disabled/read-only/pending state | high | Behavior and visual tests assert semantics and perceivable differences. |
| Fake combobox spoofing | medium | Browser proves absence of popup ARIA on filter controls. |
| Baseline tampering/broad masking | high | Explicit Docker workflow, meaningful-pixel prohibition, full gate after update. |
| Overclaimed accessibility | high | Completion is scoped to component automated evidence; Phase 82 manual/page work remains explicit. |
</threat_model>

<verification>
- `mix test test/oban_powertools/web/components/forms_test.exs test/oban_powertools/form_story_catalog_test.exs`
- `npm run visual:a11y:host -- test/browser/specs/forms.behavior.spec.ts`
- `npm run visual:a11y && mix test --exclude host_contract && mix compile --warnings-as-errors`
</verification>
<success_criteria>
- All form behaviors that axe cannot prove have targeted browser evidence.
- All nine targets have 108 reviewed baselines and axe/VRT/structure coverage.
- Validation is Nyquist-compliant and no scope-fenced page, dialog, schema, or combobox work was introduced.
</success_criteria>
<output>Create `.planning/phases/75-form-components/75-05-SUMMARY.md` when done.</output>
