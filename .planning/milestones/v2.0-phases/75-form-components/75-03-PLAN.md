---
phase: 75-form-components
plan: 03
type: execute
wave: 3
depends_on: [75-02]
files_modified:
  - test/support/form_story_catalog.ex
  - test/oban_powertools/form_story_catalog_test.exs
  - lib/oban_powertools/web/dev/showcase_live.ex
  - test/oban_powertools/web/live/showcase_live_test.exs
autonomous: true
requirements: [FORM-01, FORM-02, COMP-01, COMP-02, COMP-03, COMP-04, A11Y-02]
must_haves:
  truths:
    - "A separate deterministic form catalog covers every component and required state without entering production packaging."
    - "The dev showcase renders real to_form-backed controls at stable selectors in its reserved Forms section."
    - "Stories honor the UI copy, hierarchy, scope fences, and hostile-content safety contract."
    - "Decision trace: D-22: D-24:"
  artifacts:
    - path: test/support/form_story_catalog.ex
      provides: ObanPowertools.FormStoryCatalog and nine deterministic stories
    - path: lib/oban_powertools/web/dev/showcase_live.ex
      provides: dev-only Forms section rendering
---

<objective>Add deterministic form stories and render them through the existing dev-only showcase using the real Phase 75 components.</objective>
<execution_context>@/Users/jon/.codex/gsd-core/workflows/execute-plan.md</execution_context>
<context>
@.planning/phases/75-form-components/75-CONTEXT.md
@.planning/phases/75-form-components/75-PATTERNS.md
@.planning/phases/75-form-components/75-VALIDATION.md
@.planning/phases/75-form-components/75-UI-SPEC.md
@test/support/primitive_story_catalog.ex
@test/oban_powertools/primitive_story_catalog_test.exs
@lib/oban_powertools/web/dev/showcase_live.ex
@test/oban_powertools/web/live/showcase_live_test.exs
@lib/oban_powertools/web/components/forms.ex
</context>

## Artifacts this phase produces

- The full artifact set listed in 75-01, including `Forms`, CSS/assets, `FormStoryCatalog` with nine named targets, showcase/manifest/browser support, behavior spec, PNG baselines, and both ExUnit suites.

## Locked decision coverage

- D-22: Create a separate dev/test-only `ObanPowertools.FormStoryCatalog` rather than mixing form stories into domain stress fixtures.
- D-24: Cover valid, invalid, required, optional, disabled, read-only, pending, filter-ready, long-label/value, and 320px wrapping states for the existing four-theme evidence matrix.

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Create and validate the deterministic form story catalog</name>
  <files>test/support/form_story_catalog.ex, test/oban_powertools/form_story_catalog_test.exs</files>
  <read_first>
    - @test/support/primitive_story_catalog.ex
    - @test/oban_powertools/primitive_story_catalog_test.exs
    - @.planning/phases/75-form-components/75-UI-SPEC.md
    - @lib/oban_powertools/web/components/forms.ex
  </read_first>
  <action>Create `ObanPowertools.FormStoryCatalog` with `kind: :form`, exact ids `form-input-states`, `form-textarea-select`, `form-checkbox-modes`, `form-radio-group`, `form-switch-states`, `form-validation-wiring`, `form-disabled-readonly`, `form-filter-ready`, and `form-long-content`; deterministic names/descriptions/components/variant/state/test_targets; and helpers paralleling the primitive catalog. Tests must enforce unique slug ids, derived `obpt-form-story-*` and `showcase/form-*` targets, exact component coverage, and states valid/invalid/required/optional/disabled/readonly/pending/filter/long content. Include named boolean and event selection metadata. Reject combobox popup, FilterBar, destructive dialog, mutation, URL behavior, and page placeholders. Use required operator copy from UI-SPEC.</action>
  <verify><automated>mix test test/oban_powertools/form_story_catalog_test.exs</automated></verify>
  <acceptance_criteria>
    - The command passes and the catalog has exactly nine stable stories with exact target derivation.
    - All ten exports and all required states are represented; no deferred component appears.
    - Catalog lives only under `test/support` and existing package allowlists remain unchanged.
  </acceptance_criteria>
  <done>The form evidence matrix has one deterministic Elixir source of truth.</done>
</task>

<task type="auto">
  <name>Task 2: Render form stories in the dev-only showcase</name>
  <files>lib/oban_powertools/web/dev/showcase_live.ex, test/oban_powertools/web/live/showcase_live_test.exs</files>
  <read_first>
    - @lib/oban_powertools/web/dev/showcase_live.ex
    - @test/oban_powertools/web/live/showcase_live_test.exs
    - @test/support/form_story_catalog.ex
    - @test/support/primitive_story_catalog.ex
    - @lib/oban_powertools/web/components/forms.ex
    - @.planning/phases/75-form-components/75-UI-SPEC.md
  </read_first>
  <action>Load the form catalog with the same compile-time dev/test-only pattern as primitives, assign availability/stories, and replace only the reserved Forms placeholder. Render one stable article per story with id from `test_targets.story`, `data-obpt-form-story`, component/variant/state/a11y metadata, and a `form_story_body/1` dispatch that creates deterministic forms through `to_form/2`. Render realistic label → control → hint → error stacks and matrices for the nine exact stories, including hostile long text as ordinary escaped content. Preserve production exclusion and every scenario/primitive selector. Do not migrate operator pages or add form event/business logic.</action>
  <verify>
    <automated>mix test test/oban_powertools/form_story_catalog_test.exs test/oban_powertools/web/live/showcase_live_test.exs</automated>
    <automated>MIX_ENV=prod mix compile --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - Both commands pass; showcase test finds exactly nine form articles and real controls with stable ids.
    - Production compile has no form catalog production dependency or showcase route regression.
    - Existing scenario and primitive story assertions remain green.
  </acceptance_criteria>
  <done>Real form components render as deterministic dev/test-only stories.</done>
</task>
</tasks>

<threat_model standard="OWASP ASVS" level="L1" blocking_threshold="high">
High findings block completion. Boundaries: test catalog into compile-time showcase and story inputs into component HTML.

| Threat | Risk | Mitigation / proof |
|---|---|---|
| Story input XSS | high | Real components render hostile deterministic text; no raw content path. |
| Production exposure of dev catalog | high | Existing compile_env loader/route pattern and prod compile test. |
| Selector/metadata injection | medium | Catalog tests constrain slug ids and derive selectors/snapshot paths. |
| Story implies unsupported action | medium | Tests exclude mutation, destructive, URL, popup, and page-migration entries. |
| Misleading disabled/pending UI | high | Stories use native state plus explanation; no showcase-owned mutation handler. |
</threat_model>

<verification>
- `mix test test/oban_powertools/form_story_catalog_test.exs test/oban_powertools/web/live/showcase_live_test.exs`
- `MIX_ENV=prod mix compile --warnings-as-errors`
</verification>
<success_criteria>
- Separate form catalog and nine stable rendered stories cover the required state matrix.
- Showcase remains dev/test-only and production pages remain untouched.
</success_criteria>
<output>Create `.planning/phases/75-form-components/75-03-SUMMARY.md` when done.</output>
