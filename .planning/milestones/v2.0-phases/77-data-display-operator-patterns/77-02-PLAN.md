---
phase: 77-data-display-operator-patterns
plan: 02
type: execute
wave: 2
depends_on:
  - 77-01
files_modified:
  - lib/oban_powertools/web/status_taxonomy.ex
  - lib/oban_powertools/web/components/data_display.ex
  - test/oban_powertools/web/status_taxonomy_test.exs
  - test/oban_powertools/web/components/data_display_test.exs
autonomous: true
requirements:
  - DATA-02
  - A11Y-02
must_haves:
  truths:
    - "D-12..D-15 produce one pure domain-aware taxonomy for every approved and source-audited Oban/Powertools state without migrating production pages."
    - "D-04: Component APIs live in ObanPowertools.Web.Components.DataDisplay and reusable presentation mapping stays in the small pure ObanPowertools.Web.StatusTaxonomy support module; neither Primitives nor Forms is bloated."
    - "D-13: Taxonomy tones are canonical: neutral for pending/ordinary/unknown, info for active/running/ready, success for completed/healthy/resolved, warning for retryable/waiting/stale/drifted/ambiguous, and danger for failed/cancelled/discarded/deleted/expired/permission-denied."
    - "D-14: Unknown strings in a known domain humanize deterministically to neutral/dot with that domain's sr_prefix, and exhaustive tests plus all_specs/0 evidence make newly observed states intentional additions."
    - "Atom and binary states are equivalent without dynamic atom creation; known-domain unknown states are neutral/dot/humanized and unknown domains fail explicitly."
    - "DataDisplay.status_pill/1 delegates the exact taxonomy spec to the Phase 74 primitive and remains non-interactive with visible text/icon/sr-prefix channels."
  artifacts:
    - path: "lib/oban_powertools/web/status_taxonomy.ex"
      provides: "Pure closed-domain status registry"
      exports: ["spec/2", "all_specs/0"]
    - path: "lib/oban_powertools/web/components/data_display.ex"
      provides: "Taxonomy-backed StatusPill wrapper and shared module foundation"
      exports: ["status_pill/1"]
  key_links:
    - from: "lib/oban_powertools/web/components/data_display.ex"
      to: "lib/oban_powertools/web/status_taxonomy.ex"
      via: "StatusTaxonomy.spec/2"
      pattern: "StatusTaxonomy.spec"
    - from: "lib/oban_powertools/web/components/data_display.ex"
      to: "lib/oban_powertools/web/components/primitives.ex"
      via: "Primitives.status_pill/1"
      pattern: "Primitives.status_pill"
---

<objective>
Implement the pure unified status taxonomy and its non-interactive DataDisplay wrapper.

Purpose: establish the exhaustive DATA-02 source of truth before tables, stories, and other data displays consume domain status.
Output: atom-safe `StatusTaxonomy` plus `DataDisplay.status_pill/1`, with the Wave 0 taxonomy and wrapper contracts green.
</objective>

<execution_context>
@/Users/jon/.agents/gsd-core/workflows/execute-plan.md
@/Users/jon/.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/77-data-display-operator-patterns/77-CONTEXT.md
@.planning/phases/77-data-display-operator-patterns/77-UI-SPEC.md
@.planning/phases/77-data-display-operator-patterns/77-RESEARCH.md
@.planning/phases/77-data-display-operator-patterns/77-PATTERNS.md
@.planning/phases/77-data-display-operator-patterns/77-01-SUMMARY.md
</context>

## Artifacts this phase produces

- Exact private/support symbols: taxonomy `normalize_state/1`, `humanize/1`, `domain!/1`, `resolve_alias/2`; DataDisplay `visual_safe_rest/2`, `action_attr?/1`, `data_state/1`, `row_dom_id/2`, `aria_sort/3`, `truncate_middle/2`, `preserve_suffix/2`, `clamp_progress/2`, and `render_normalized_display/1` (or private clauses under that exact dispatcher name).
- Created files: `lib/oban_powertools/web/status_taxonomy.ex`, `lib/oban_powertools/web/components/data_display.ex`, `test/oban_powertools/web/status_taxonomy_test.exs`, `test/oban_powertools/web/components/data_display_test.exs`, `test/support/data_display_story_catalog.ex`, `test/oban_powertools/data_display_story_catalog_test.exs`, `test/browser/specs/data-display.behavior.spec.ts`, and `test/browser/support/verify-data-baselines.mjs`; generated files are exactly 120 `test/browser/__screenshots__/chromium-{320,tablet,wide}/showcase/data-*/*.png` baselines.
- Baseline verification CLI: `test/browser/support/verify-data-baselines.mjs` defaults to validating the exact manifest-derived data screenshot set with cardinality 120; its created `--changed-scope` mode rejects tracked, untracked, and renamed screenshot changes outside the expected `data-*` matrix.
- Exact CSS symbols: `.obpt-data-table`, `.obpt-data-state`, `.obpt-description-list`, `.obpt-key-value`, `.obpt-machine-value`, `.obpt-timeline`, `.obpt-progress`, `.obpt-metric-card`, `.obpt-code-block`, `.obpt-args-viewer`, `.obpt-redacted-value`, `.obpt-empty-state`, `.obpt-toast`, and `.obpt-flash-group`, all rooted below `.obpt-root` and copied into the packaged CSS by the deterministic asset build.
- Complete attr/slot manifest: `data_table/1` attrs `id/caption/rows/row_id/state/resource/row_count/pagination_summary/sort_key/sort_direction/sort_event/rest` and slots `toolbar/selection/col(label,sort_key,value_kind)/action/state_detail`; `status_pill/1` attrs `id/domain/state/rest`; `description_list/1` attrs `id/state/resource/rest` and repeated `item(label,value_kind)`; `key_value/1` attrs `id/label/value/value_kind/rest`; `machine_value/1` attrs `id/value/kind/truncate/expand/rest`; `timeline/1` attrs `id/state/resource/rest` and repeated `event(timestamp,title,source,domain,state,detail)`; `progress_bar/1` attrs `id/label/value/max/state/rest`; `metric_card/1` attrs `id/label/value/trend/status/tone/rest` plus `action`; `code_block/1` attrs `id/label/content/language/rest`; `args_viewer/1` attrs `id/label/display/kind`; `redacted_value/1` attrs `id/reason/message`; `empty_state/1` attrs `id/heading/body/rest` plus `action`; `toast/1` attrs `id/tone/urgency/dismiss_event/rest` plus `inner_block`; `flash_group/1` attrs `id/flash/dismiss_event/rest`.
- New production files/modules: `ObanPowertools.Web.StatusTaxonomy` (`spec/2`, `all_specs/0`) and `ObanPowertools.Web.Components.DataDisplay` (`data_table/1`, `status_pill/1`, `description_list/1`, `key_value/1`, `machine_value/1`, `timeline/1`, `progress_bar/1`, `metric_card/1`, `code_block/1`, `args_viewer/1`, `redacted_value/1`, `empty_state/1`, `toast/1`, `flash_group/1`).
- DataTable symbols: attrs `id`, `caption`, `rows`, `row_id`, `state`, `resource`, `row_count`, `pagination_summary`, `sort_key`, `sort_direction`, `sort_event`, `rest`; slots `toolbar`, `selection`, `col`, `action`, `state_detail`; same-DOM mobile labels and explicit state helpers.
- Secondary/redaction symbols: DescriptionList `item` slots; Timeline `event` slots; native progress helper; metric wrapper; machine truncation; CodeBlock label/content; normalized-only ArgsViewer dispatch; closed RedactedValue reasons; EmptyState action; Toast/Flash urgency/dismiss APIs.
- Private/support symbols: closed string-key status tables, domain prefix registry, humanizer, alias resolver, safe rest/action filtering, state copy helpers, progress clamp, and normalized display map/tuple clauses.
- Test files: `test/oban_powertools/web/status_taxonomy_test.exs`, `test/oban_powertools/web/components/data_display_test.exs`, `test/oban_powertools/data_display_story_catalog_test.exs`, and `test/browser/specs/data-display.behavior.spec.ts`.
- Story/catalog symbols: `ObanPowertools.DataDisplayStoryCatalog`, `stories/0`, `story!/1`, `snapshot_name/1`, `a11y_target/1`, `huge_args_fixture/0`, `long_value_fixture/0`, `timeline_fixture/0`, `large_row_total/0`, `large_row_window/0`, and the ten D-23 IDs.
- Integration/browser symbols: ShowcaseLive data catalog attrs/body/sort assigns and `handle_event("sort-data-table", ...)`; CSS bases `.obpt-data-table`, `.obpt-data-state`, `.obpt-description-list`, `.obpt-key-value`, `.obpt-machine-value`, `.obpt-timeline`, `.obpt-progress`, `.obpt-metric-card`, `.obpt-code-block`, `.obpt-args-viewer`, `.obpt-redacted-value`, `.obpt-empty-state`, `.obpt-toast`, `.obpt-flash-group`; manifest `schema_version: 5`, `data_stories`, appended `targets`, `kind: "data"`; TypeScript `ShowcaseDataStory`, `dataStories`, `dataStory(id)`, and the data-kind structure branch; exactly 120 data PNGs and reconciled `77-VALIDATION.md`.

<tasks>

<task type="auto" tdd="true">
  <name>Task 77-02-01: Implement exhaustive atom-safe taxonomy and wrapper</name>
  <files>lib/oban_powertools/web/status_taxonomy.ex, lib/oban_powertools/web/components/data_display.ex, test/oban_powertools/web/status_taxonomy_test.exs, test/oban_powertools/web/components/data_display_test.exs</files>
  <read_first>
    - @lib/oban_powertools/web/status_taxonomy.ex
    - @lib/oban_powertools/web/components/data_display.ex
    - @test/oban_powertools/web/status_taxonomy_test.exs
    - @test/oban_powertools/web/components/data_display_test.exs
    - @lib/oban_powertools/web/control_plane_presenter.ex
    - @lib/oban_powertools/web/components/primitives.ex
    - @lib/oban_powertools/jobs.ex
    - @lib/oban_powertools/batches.ex
    - @lib/oban_powertools/workflow/runtime.ex
    - @lib/oban_powertools/lifeline.ex
    - @lib/oban_powertools/lifeline/repair_preview.ex
    - @lib/oban_powertools/forensics/cron_history.ex
    - @lib/oban_powertools/host_escalation.ex
    - @.planning/phases/77-data-display-operator-patterns/77-UI-SPEC.md
    - @.planning/phases/77-data-display-operator-patterns/77-PATTERNS.md
  </read_first>
  <action>Create `ObanPowertools.Web.StatusTaxonomy` as a pure module with `spec(domain, state)` and deterministic `all_specs/0`. Use private `normalize_state/1`, `domain!/1`, `resolve_alias/2`, and `humanize/1` over a closed atom domain registry and string-keyed state tables; normalize atoms with `Atom.to_string/1` and binaries without either atom-conversion function. Encode exact UI-SPEC labels/tones/icons/sr-prefixes, explicit Lifeline preview aliases, derived lease/output/availability states, and every supplemental state selected in the Wave 0 source audit. Return exactly the four-key primitive map from `spec/2`; return ordered domain/state/spec audit records from `all_specs/0`. Humanize unknown strings only inside known domains and raise `ArgumentError` with the accepted domains for unknown domains. Create the `DataDisplay` module foundation with `use Phoenix.Component`, closed `domain`/state attrs, private `visual_safe_rest/2` plus `action_attr?/1` copied from `Primitives` semantics, and `status_pill/1` that passes `StatusTaxonomy.spec/2` to `Primitives.status_pill/1`; suppress action attrs and semantic/visual overrides. Do not edit page-local helper maps or any production LiveView (D-15). Make the Wave 0 taxonomy and status-wrapper groups green without weakening exact assertions.</action>
  <verify>
    <automated>mix test test/oban_powertools/web/status_taxonomy_test.exs test/oban_powertools/web/components/data_display_test.exs --seed 0</automated>
    <automated>mix format --check-formatted lib/oban_powertools/web/status_taxonomy.ex lib/oban_powertools/web/components/data_display.ex test/oban_powertools/web/status_taxonomy_test.exs test/oban_powertools/web/components/data_display_test.exs</automated>
    <automated>mix compile --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - `StatusTaxonomy.spec/2` returns the exact four-key map for every approved/source-audited state and `all_specs/0` is ordered, unique, and stable across calls.
    - Atom and binary forms of every state return equal results and neither `String.to_atom` nor `String.to_existing_atom` appears in `status_taxonomy.ex`.
    - Unknown states in known domains are humanized neutral/dot values with the domain's exact `sr_prefix`; unknown domains raise and list accepted domains.
    - `DataDisplay.status_pill/1` renders through `Primitives.status_pill/1`, includes visible label/icon/sr-prefix semantics, filters action/visual/role overrides, and has no interactive styling.
    - No file under `lib/oban_powertools/web/*_live.ex` is modified.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## ASVS L1 Threat Model

| ID | Threat | Severity | Mitigation and verification |
|----|--------|----------|-----------------------------|
| T-77-02-LEAK | Status labels or attrs become a channel for raw args/display-policy secrets | high | Wrapper accepts only domain/state plus narrow safe attrs; no raw value/copy/title/detail API; render test rejects hostile secret-bearing attrs. |
| T-77-02-ATTR | Caller rest attrs override pill semantics, styles, roles, or actions | high | Normalize keys and reject `class`, `style`, `role`, inline handlers, and action attrs before delegation; render assertions inspect final HTML. |
| T-77-02-XSS | Hostile unknown state text renders executable markup | high | Humanized label remains plain data rendered by HEEx; hostile-string tests prove escaping and raw HTML APIs are absent. |
| T-77-02-ATOM | Binary states grow VM atoms or unknown domains misrepresent status | high | String-key registry, no atom conversion, explicit domain registry, and unknown-domain `ArgumentError`; source/tests block regressions. |
| T-77-02-DOS | Huge attacker-controlled state strings consume rendering/log resources | medium | Normalize once, avoid regex/backtracking/atom creation, and test a long unknown binary deterministically; no logging side effect in `spec/2`. |
</threat_model>

<verification>
- Run the focused taxonomy/component tests, formatter, and warnings-as-errors compile.
- Review `git diff --name-only` to confirm no page migration or dependency file entered scope.
- Leave `77-VALIDATION.md` execution statuses pending; the final closeout plan records evidence.
</verification>

<success_criteria>
- One pure domain-aware mapping covers every agreed Oban/Powertools state and deterministic unknown behavior.
- The shared StatusPill wrapper is semantic, non-interactive, atom-safe, and uses Phase 74 presentation.
- DATA-02 has unit/render proof without claiming the nine page helpers have migrated.
</success_criteria>

<output>
Create `.planning/phases/77-data-display-operator-patterns/77-02-SUMMARY.md` when done.
</output>
