---
phase: 77-data-display-operator-patterns
plan: 04
type: execute
wave: 4
depends_on:
  - 77-03
files_modified:
  - lib/oban_powertools/web/components/data_display.ex
  - test/oban_powertools/web/components/data_display_test.exs
  - assets/oban_powertools/tokens.css
  - priv/static/oban_powertools/oban_powertools.css
  - test/oban_powertools/web/theme_tokens_test.exs
  - test/oban_powertools/web/assets_test.exs
autonomous: true
requirements:
  - DATA-01
  - DATA-03
  - A11Y-02
must_haves:
  truths:
    - "DescriptionList/KeyValue, Timeline, ProgressBar, MetricCard, machine values, EmptyState, and Toast/Flash use native semantics and closed token-owned APIs."
    - "DescriptionList and Timeline expose explicit loading/empty/error/unavailable/permission-denied states; determinate progress is clamped and unavailable progress never claims a fake percentage."
    - "Long IDs/modules/URLs/stacktraces wrap, middle-truncate, preserve useful trailing text, or offer explicit non-sensitive expansion without title-only disclosure or page overflow."
  artifacts:
    - path: "lib/oban_powertools/web/components/data_display.ex"
      provides: "Secondary data-display and operator-feedback components"
      exports: ["description_list/1", "key_value/1", "machine_value/1", "timeline/1", "progress_bar/1", "metric_card/1", "empty_state/1", "toast/1", "flash_group/1"]
    - path: "assets/oban_powertools/tokens.css"
      provides: "Root-scoped secondary display, state, progress, metric, long-value, and toast styling"
  key_links:
    - from: "lib/oban_powertools/web/components/data_display.ex"
      to: "lib/oban_powertools/web/components/primitives.ex"
      via: "card/surface/stat/button/link/status composition"
      pattern: "Primitives.(card|stat|button|link)"
    - from: "lib/oban_powertools/web/components/data_display.ex"
      to: "lib/oban_powertools/web/status_taxonomy.ex"
      via: "optional timeline status_pill domain/state"
      pattern: "status_pill"
---

<objective>
Complete the lower-risk shared data-display set around the DataTable foundation.

Purpose: provide semantic details, event history, progress/metrics, long-machine-value handling, explicit states, and operator feedback before the confidentiality-sensitive args viewer.
Output: secondary DataDisplay components, root-scoped token CSS/package asset, and green component/static tests.
</objective>

<execution_context>
@/Users/jon/.agents/gsd-core/workflows/execute-plan.md
@/Users/jon/.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/77-data-display-operator-patterns/77-CONTEXT.md
@.planning/phases/77-data-display-operator-patterns/77-UI-SPEC.md
@.planning/phases/77-data-display-operator-patterns/77-RESEARCH.md
@.planning/phases/77-data-display-operator-patterns/77-PATTERNS.md
@.planning/phases/77-data-display-operator-patterns/77-03-SUMMARY.md
@.planning/phases/74-primitives-library/74-01-SUMMARY.md
@.planning/phases/75-form-components/75-CONTEXT.md
</context>

## Artifacts this phase produces

- Exact private/support symbols: taxonomy `normalize_state/1`, `humanize/1`, `domain!/1`, `resolve_alias/2`; DataDisplay `visual_safe_rest/2`, `action_attr?/1`, `data_state/1`, `row_dom_id/2`, `aria_sort/3`, `truncate_middle/2`, `preserve_suffix/2`, `clamp_progress/2`, and `render_normalized_display/1` (or private clauses under that exact dispatcher name).
- Created files: `lib/oban_powertools/web/status_taxonomy.ex`, `lib/oban_powertools/web/components/data_display.ex`, `test/oban_powertools/web/status_taxonomy_test.exs`, `test/oban_powertools/web/components/data_display_test.exs`, `test/support/data_display_story_catalog.ex`, `test/oban_powertools/data_display_story_catalog_test.exs`, `test/browser/specs/data-display.behavior.spec.ts`, and `test/browser/support/verify-data-baselines.mjs`; generated files are exactly 120 `test/browser/__screenshots__/chromium-{320,tablet,wide}/showcase/data-*/*.png` baselines.
- Baseline verification CLI: `test/browser/support/verify-data-baselines.mjs` defaults to validating the exact manifest-derived data screenshot set with cardinality 120; its created `--changed-scope` mode rejects tracked, untracked, and renamed screenshot changes outside the expected `data-*` matrix.
- Exact CSS symbols: `.obpt-data-table`, `.obpt-data-state`, `.obpt-description-list`, `.obpt-key-value`, `.obpt-machine-value`, `.obpt-timeline`, `.obpt-progress`, `.obpt-metric-card`, `.obpt-code-block`, `.obpt-args-viewer`, `.obpt-redacted-value`, `.obpt-empty-state`, `.obpt-toast`, and `.obpt-flash-group`, all rooted below `.obpt-root` and copied into the packaged CSS by the deterministic asset build.
- Complete attr/slot manifest: `data_table/1` attrs `id/caption/rows/row_id/state/resource/row_count/pagination_summary/sort_key/sort_direction/sort_event/rest` and slots `toolbar/selection/col(label,sort_key,value_kind)/action/state_detail`; `status_pill/1` attrs `id/domain/state/rest`; `description_list/1` attrs `id/state/resource/rest` and repeated `item(label,value_kind)`; `key_value/1` attrs `id/label/value/value_kind/rest`; `machine_value/1` attrs `id/value/kind/truncate/expand/rest`; `timeline/1` attrs `id/state/resource/rest` and repeated `event(timestamp,title,source,domain,state,detail)`; `progress_bar/1` attrs `id/label/value/max/state/rest`; `metric_card/1` attrs `id/label/value/trend/status/tone/rest` plus `action`; `code_block/1` attrs `id/label/content/language/rest`; `args_viewer/1` attrs `id/label/display/kind`; `redacted_value/1` attrs `id/reason/message`; `empty_state/1` attrs `id/heading/body/rest` plus `action`; `toast/1` attrs `id/tone/urgency/dismiss_event/rest` plus `inner_block`; `flash_group/1` attrs `id/flash/dismiss_event/rest`.
- Production modules/symbols: `StatusTaxonomy.spec/2`, `all_specs/0`; DataDisplay `data_table/1`, `status_pill/1`, `description_list/1`, `key_value/1`, `machine_value/1`, `timeline/1`, `progress_bar/1`, `metric_card/1`, `code_block/1`, `args_viewer/1`, `redacted_value/1`, `empty_state/1`, `toast/1`, `flash_group/1`.
- DataTable attrs/slots/helpers: id/caption/rows/row_id/state/resource/count/pagination/sort/rest; toolbar/selection/col/action/state_detail; same-DOM mobile labels, state copy, row-id and sort-aria helpers.
- Secondary attrs/slots/helpers: DescriptionList `state`, `resource`, repeated `item(label, value_kind)`; KeyValue label/value; MachineValue value/kind/truncate/expand; Timeline `state` and repeated `event(timestamp,title,source,domain,state,detail)`; ProgressBar label/value/max/state; MetricCard label/value/trend/tone/status; EmptyState heading/body/action; Toast/Flash tone/urgency/dismiss/rest; state renderer, middle truncation, suffix preservation, and progress clamp helpers.
- Redaction APIs: CodeBlock label/content/language; ArgsViewer normalized `display`; RedactedValue closed reason/message with no general rest/raw/slot/copy/title/detail API.
- CSS/package symbols: `.obpt-data-table`, `.obpt-data-state`, `.obpt-description-list`, `.obpt-key-value`, `.obpt-machine-value`, `.obpt-timeline`, `.obpt-progress`, `.obpt-metric-card`, `.obpt-code-block`, `.obpt-args-viewer`, `.obpt-redacted-value`, `.obpt-empty-state`, `.obpt-toast`, `.obpt-flash-group` below `.obpt-root`, plus compiled package CSS.
- Story/test symbols/files: taxonomy/component/catalog tests; `ObanPowertools.DataDisplayStoryCatalog` with `stories/0`, `story!/1`, `snapshot_name/1`, `a11y_target/1`, `huge_args_fixture/0`, `long_value_fixture/0`, `timeline_fixture/0`, `large_row_total/0`, `large_row_window/0`, ten locked IDs; `data-display.behavior.spec.ts`.
- Integration/browser symbols: ShowcaseLive data catalog attrs/body/sort assigns and `handle_event("sort-data-table", ...)`; manifest `schema_version: 5`, `data_stories`, appended `targets`, `kind: "data"`; TypeScript `ShowcaseDataStory`, `dataStories`, `dataStory(id)`, data-kind structure branch; 120 screenshots and validation closeout.

<tasks>

<task type="auto" tdd="true">
  <name>Task 77-04-01: Implement semantic details, timeline, progress, metrics, long values, empty state, and flash</name>
  <files>lib/oban_powertools/web/components/data_display.ex, test/oban_powertools/web/components/data_display_test.exs</files>
  <read_first>
    - @lib/oban_powertools/web/components/data_display.ex
    - @test/oban_powertools/web/components/data_display_test.exs
    - @lib/oban_powertools/web/components/primitives.ex
    - @lib/oban_powertools/web/components/forms.ex
    - @lib/oban_powertools/web/cron_live.ex
    - @lib/oban_powertools/web/batches_live.ex
    - @lib/oban_powertools/web/forensics_live.ex
    - @.planning/phases/77-data-display-operator-patterns/77-UI-SPEC.md
    - @.planning/phases/77-data-display-operator-patterns/77-PATTERNS.md
  </read_first>
  <action>Implement the listed components in `DataDisplay` using `attr/3`, `slot/3`, explicit assigns, closed values, safe rest policies, and private `data_state/1`, `truncate_middle/2`, `preserve_suffix/2`, and `clamp_progress/2`. `description_list/1` renders one `dl` with repeated `dt/dd` pairs and the six explicit data states; `key_value/1` is the documented single-pair convenience wrapper, not a second semantic model. `timeline/1` renders one ordered list with time/title/source, optional taxonomy-backed status, and optional escaped detail slot. `progress_bar/1` uses the native progress element for determinate values, clamps value against a positive max, renders adjacent label/count/percent, and for `:unavailable` renders `Progress unavailable` without `value`/`aria-valuenow`; do not use inline widths. `metric_card/1` composes one primitive surface/stat treatment and is non-interactive unless its caller supplies an explicit Button/Link slot. `machine_value/1` handles IDs/modules/URLs as non-sensitive input only: middle-truncate IDs, preserve module suffixes, use `min-width: 0`, and expose full text through native details/summary when enabled, never title-only. `empty_state/1` always has heading/body and an optional semantic action; do not use it for loading or permission denied. `toast/1`/`flash_group/1` use closed tones and urgency, polite status for info/success, alert only for immediate warning/danger, accessible 44px dismiss control, parent-owned dismiss event, and no focus steal. Reuse `data_state/1` for Table/DescriptionList/Timeline. Make the corresponding Wave 0 groups green. Decision trace: D-03..D-06, D-10..D-15, D-25.</action>
  <verify>
    <automated>mix test test/oban_powertools/web/components/data_display_test.exs --seed 0</automated>
    <automated>mix format --check-formatted lib/oban_powertools/web/components/data_display.ex test/oban_powertools/web/components/data_display_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - DescriptionList uses `dl/dt/dd`; Timeline uses `ol`; determinate progress is native and clamped; unavailable progress has no fake numeric semantics.
    - DescriptionList and Timeline render all explicit state categories with named copy; EmptyState is used only for true empty data.
    - Long-value tests prove useful prefix/suffix or explicit expansion and prohibit `title` as the sole/full-value channel.
    - Toast roles match urgency, dismiss controls are named, and no component gains implicit button/link behavior or focus movement.
    - Hostile text remains escaped and caller attrs cannot override native semantics, visual classes, urgency roles, or actions.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 77-04-02: Style and package the secondary data-display set</name>
  <files>assets/oban_powertools/tokens.css, priv/static/oban_powertools/oban_powertools.css, test/oban_powertools/web/theme_tokens_test.exs, test/oban_powertools/web/assets_test.exs, test/oban_powertools/web/components/data_display_test.exs</files>
  <read_first>
    - @assets/oban_powertools/tokens.css
    - @priv/static/oban_powertools/oban_powertools.css
    - @test/oban_powertools/web/theme_tokens_test.exs
    - @test/oban_powertools/web/assets_test.exs
    - @test/oban_powertools/web/components/data_display_test.exs
    - @lib/mix/tasks/oban_powertools.assets.build.ex
    - @.planning/phases/74-primitives-library/74-02-SUMMARY.md
    - @.planning/phases/77-data-display-operator-patterns/77-UI-SPEC.md
  </read_first>
  <action>Add `.obpt-root` rules for description lists/key values, timeline rail/items, native progress and labels, metric card, machine values/details, explicit data states, empty state, toast stack/tones/dismiss focus, and narrow showcase layouts. Use only semantic tokens and the approved 400/600 type roles; mono only for machine values, tabular sans for metrics/time. Keep ordinary content wrapping/stacking at 24rem and 200% zoom; only future CodeBlock regions may scroll internally. Include focus-visible, token-derived 44px dismiss targets, color-plus-text/icon state channels, and reduced-motion rules that never hide content or animate progress/row/toast layout. Extend token and packaged asset assertions without broadening literal allowlists. Regenerate packaged CSS through the build task and keep JS unchanged.</action>
  <verify>
    <automated>mix oban_powertools.assets.build && mix test test/oban_powertools/web/theme_tokens_test.exs test/oban_powertools/web/assets_test.exs test/oban_powertools/web/components/data_display_test.exs --seed 0</automated>
    <automated>mix oban_powertools.assets.build && git diff --exit-code -- priv/static/oban_powertools/oban_powertools.js</automated>
    <automated>mix compile --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - Every new selector is below `.obpt-root`, uses existing semantic tokens, and preserves the typography/spacing/tone contracts from the UI-SPEC.
    - Description/timeline/metric/empty/toast surfaces wrap or stack without horizontal overflow; no ordinary secondary surface enables internal horizontal scroll.
    - Progress has no inline width rule or animation; focus and dismiss target selectors are present; reduced-motion keeps feedback readable.
    - Compiled CSS is synchronized/byte-stable, packaged JS is unchanged, and focused tests compile and pass.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## ASVS L1 Threat Model

| ID | Threat | Severity | Mitigation and verification |
|----|--------|----------|-----------------------------|
| T-77-04-LEAK | Machine-value expansion/title/copy/detail channels expose raw redacted args | high | `machine_value/1` is explicitly non-sensitive; redacted values are rejected from this API and deferred to RedactedValue; no copy/title API; tests inspect details and attrs. |
| T-77-04-ATTR | Caller attrs override dl/list/progress/live-region semantics or dismiss actions | high | Component-specific safe-rest allowlists reject class/style/role/handlers and suppress actions on non-interactive displays; render tests pass hostile attrs. |
| T-77-04-XSS | Hostile timeline, metric, toast, or long-value content executes markup | high | HEEx escaping only; no raw HTML; HTML-looking/Unicode/RTL fixtures are render-tested. |
| T-77-04-ATOM | Unknown status/tone/state creates atoms or selects unsafe domains | high | Timeline delegates only through closed StatusTaxonomy domains; component states/tones are closed attrs; no string-to-atom conversion. |
| T-77-04-DOS | Huge stacktraces/URLs/timelines or toast floods produce unbounded layout | high | Long values truncate/wrap with opt-in details, timeline rows are caller-windowed, CSS bounds content, and flash grouping renders a finite supplied collection; stress tests cover long inputs. |
</threat_model>

<verification>
- Run focused component/static/asset checks, formatter, and warnings-as-errors compile.
- Confirm no inline style, raw HTML, dynamic atom conversion, host selector, or production page migration enters the diff.
- Preserve Plan 03 table behavior and Phase 74/75 primitive/form tests.
</verification>

<success_criteria>
- Every secondary DATA-01 component ships with semantic markup and token-owned styling.
- Explicit state, long-value, focus, urgency, and progress semantics are independently asserted.
- Package assets are deterministic; no runtime dependency, JavaScript behavior, or page migration is added.
</success_criteria>

<output>
Create `.planning/phases/77-data-display-operator-patterns/77-04-SUMMARY.md` when done.
</output>
