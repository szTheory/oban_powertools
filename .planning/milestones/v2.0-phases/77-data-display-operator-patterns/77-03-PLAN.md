---
phase: 77-data-display-operator-patterns
plan: 03
type: execute
wave: 3
depends_on:
  - 77-02
files_modified:
  - lib/oban_powertools/web/components/data_display.ex
  - test/oban_powertools/web/components/data_display_test.exs
  - assets/oban_powertools/tokens.css
  - priv/static/oban_powertools/oban_powertools.css
  - test/oban_powertools/web/theme_tokens_test.exs
  - test/oban_powertools/web/assets_test.exs
autonomous: true
requirements:
  - DATA-01
  - DATA-03
  - A11Y-02
must_haves:
  truths:
    - "D-07..D-11 render one semantic table DOM that CSS reflows into labelled cards at 320px; no duplicate mobile rows, ids, controls, or hidden values exist."
    - "D-05: Parent LiveViews own fetching, authorization, sort/pagination/selection/URL state, mutations, and audit flows; DataDisplay owns semantic presentation, explicit states, formatting/redaction seams, and accessibility wiring only."
    - "D-08: Every sortable header is a native button inside th[scope=col], only the active column exposes truthful aria-sort, and the configured event/key delegates sort state to the parent."
    - "D-09: The same table DOM reflows at 320px into stacked labelled rows with no page overflow, while each row-selection checkbox remains labelled and has a token-derived effective 44px target."
    - "Sorting is parent-owned: native header buttons emit the configured event/value and only the active th owns truthful aria-sort."
    - "Ready/loading/empty/error/unavailable/permission-denied states are explicit in the data region; long values and ordinary surfaces never cause page-level overflow."
  artifacts:
    - path: "lib/oban_powertools/web/components/data_display.ex"
      provides: "Stateless semantic DataTable API"
      exports: ["data_table/1"]
    - path: "assets/oban_powertools/tokens.css"
      provides: "Root-scoped desktop table and same-DOM 24rem card reflow"
    - path: "priv/static/oban_powertools/oban_powertools.css"
      provides: "Deterministically rebuilt packaged table CSS"
  key_links:
    - from: "lib/oban_powertools/web/components/data_display.ex"
      to: "assets/oban_powertools/tokens.css"
      via: "obpt-data-table classes and data state attrs"
      pattern: "obpt-data-table"
    - from: "lib/oban_powertools/web/components/data_display.ex"
      to: "parent LiveView"
      via: "sort_event and phx-value-sort-key only"
      pattern: "phx-value-sort-key"
---

<objective>
Ship the semantic, parent-controlled, responsive DataTable and its explicit state presentation.

Purpose: solve the roadmap's highest responsive/semantic split risk in isolation.
Output: one-table DOM component, 320px card reflow, token CSS/package asset, and focused component/static tests.
</objective>

<execution_context>
@/Users/jon/.agents/gsd-core/workflows/execute-plan.md
@/Users/jon/.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/77-data-display-operator-patterns/77-CONTEXT.md
@.planning/phases/77-data-display-operator-patterns/77-UI-SPEC.md
@.planning/phases/77-data-display-operator-patterns/77-RESEARCH.md
@.planning/phases/77-data-display-operator-patterns/77-PATTERNS.md
@.planning/phases/77-data-display-operator-patterns/77-02-SUMMARY.md
@.planning/phases/74-primitives-library/74-02-SUMMARY.md
@.planning/phases/75-form-components/75-01-SUMMARY.md
</context>

## Artifacts this phase produces

- Exact private/support symbols: taxonomy `normalize_state/1`, `humanize/1`, `domain!/1`, `resolve_alias/2`; DataDisplay `visual_safe_rest/2`, `action_attr?/1`, `data_state/1`, `row_dom_id/2`, `aria_sort/3`, `truncate_middle/2`, `preserve_suffix/2`, `clamp_progress/2`, and `render_normalized_display/1` (or private clauses under that exact dispatcher name).
- Created files: `lib/oban_powertools/web/status_taxonomy.ex`, `lib/oban_powertools/web/components/data_display.ex`, `test/oban_powertools/web/status_taxonomy_test.exs`, `test/oban_powertools/web/components/data_display_test.exs`, `test/support/data_display_story_catalog.ex`, `test/oban_powertools/data_display_story_catalog_test.exs`, `test/browser/specs/data-display.behavior.spec.ts`, and `test/browser/support/verify-data-baselines.mjs`; generated files are exactly 120 `test/browser/__screenshots__/chromium-{320,tablet,wide}/showcase/data-*/*.png` baselines.
- Baseline verification CLI: `test/browser/support/verify-data-baselines.mjs` defaults to validating the exact manifest-derived data screenshot set with cardinality 120; its created `--changed-scope` mode rejects tracked, untracked, and renamed screenshot changes outside the expected `data-*` matrix.
- Exact CSS symbols: `.obpt-data-table`, `.obpt-data-state`, `.obpt-description-list`, `.obpt-key-value`, `.obpt-machine-value`, `.obpt-timeline`, `.obpt-progress`, `.obpt-metric-card`, `.obpt-code-block`, `.obpt-args-viewer`, `.obpt-redacted-value`, `.obpt-empty-state`, `.obpt-toast`, and `.obpt-flash-group`, all rooted below `.obpt-root` and copied into the packaged CSS by the deterministic asset build.
- Complete attr/slot manifest: `data_table/1` attrs `id/caption/rows/row_id/state/resource/row_count/pagination_summary/sort_key/sort_direction/sort_event/rest` and slots `toolbar/selection/col(label,sort_key,value_kind)/action/state_detail`; `status_pill/1` attrs `id/domain/state/rest`; `description_list/1` attrs `id/state/resource/rest` and repeated `item(label,value_kind)`; `key_value/1` attrs `id/label/value/value_kind/rest`; `machine_value/1` attrs `id/value/kind/truncate/expand/rest`; `timeline/1` attrs `id/state/resource/rest` and repeated `event(timestamp,title,source,domain,state,detail)`; `progress_bar/1` attrs `id/label/value/max/state/rest`; `metric_card/1` attrs `id/label/value/trend/status/tone/rest` plus `action`; `code_block/1` attrs `id/label/content/language/rest`; `args_viewer/1` attrs `id/label/display/kind`; `redacted_value/1` attrs `id/reason/message`; `empty_state/1` attrs `id/heading/body/rest` plus `action`; `toast/1` attrs `id/tone/urgency/dismiss_event/rest` plus `inner_block`; `flash_group/1` attrs `id/flash/dismiss_event/rest`.
- Production modules: `StatusTaxonomy` (`spec/2`, `all_specs/0`) and `DataDisplay` components `data_table/1`, `status_pill/1`, `description_list/1`, `key_value/1`, `machine_value/1`, `timeline/1`, `progress_bar/1`, `metric_card/1`, `code_block/1`, `args_viewer/1`, `redacted_value/1`, `empty_state/1`, `toast/1`, `flash_group/1`.
- DataTable attrs/slots: `id`, `caption`, `rows`, `row_id`, `state`, `resource`, `row_count`, `pagination_summary`, `sort_key`, `sort_direction`, `sort_event`, filtered `rest`; `toolbar`, `selection`, repeated `col(label, sort_key, value_kind)`, `action`, constrained `state_detail`; private state-copy, row-id, sort-aria, and safe-rest helpers.
- Secondary/redaction APIs: DescriptionList items, Timeline events, native progress clamp/unavailable state, metric wrapper, machine-value truncation/expansion, labelled CodeBlock, normalized ArgsViewer tuple/map dispatch, closed RedactedValue reasons, EmptyState, Toast/Flash urgency and dismiss contracts.
- Styling/assets: `.obpt-root`-scoped data table/state/mobile-label/card rules plus later data component families in `tokens.css`; regenerated packaged CSS; theme/assets static assertions.
- Story/test symbols/files: `ObanPowertools.DataDisplayStoryCatalog` with `stories/0`, `story!/1`, `snapshot_name/1`, `a11y_target/1`, `huge_args_fixture/0`, `long_value_fixture/0`, `timeline_fixture/0`, `large_row_total/0`, `large_row_window/0`, ten locked IDs; taxonomy/component/catalog tests; `test/browser/specs/data-display.behavior.spec.ts`.
- Integration/browser symbols: ShowcaseLive data catalog attrs/body/sort assigns and `handle_event("sort-data-table", ...)`; manifest `schema_version: 5`, `data_stories`, appended `targets`, `kind: "data"`; TypeScript `ShowcaseDataStory`, `dataStories`, `dataStory(id)`, and the data-kind structure branch; exactly 120 data PNGs and reconciled `77-VALIDATION.md`.

<tasks>

<task type="auto" tdd="true">
  <name>Task 77-03-01: Implement the one-DOM semantic DataTable</name>
  <files>lib/oban_powertools/web/components/data_display.ex, test/oban_powertools/web/components/data_display_test.exs</files>
  <read_first>
    - @lib/oban_powertools/web/components/data_display.ex
    - @test/oban_powertools/web/components/data_display_test.exs
    - @deps/phoenix/installer/templates/phx_web/components/core_components.ex.eex
    - @lib/oban_powertools/web/components/primitives.ex
    - @lib/oban_powertools/web/components/forms.ex
    - @test/oban_powertools/web/components/forms_test.exs
    - @.planning/phases/77-data-display-operator-patterns/77-UI-SPEC.md
    - @.planning/phases/77-data-display-operator-patterns/77-PATTERNS.md
  </read_first>
  <action>Extend `DataDisplay` with `data_table/1` and private `row_dom_id/2` plus `aria_sort/3`, reusing `visual_safe_rest/2` and `action_attr?/1`. Declare closed `state` values `:ready | :loading | :empty | :error | :unavailable | :permission_denied`, closed sort directions, required id/caption/rows/row_id, row count and pagination summary, named resource copy, parent-owned sort key/direction/event, and the documented slots. Follow Phoenix's generated `rows`/`row_id`/repeated `:col`/`render_slot(col,row)` pattern. Render exactly one `table` with caption, header, body, stable row ids, one cell per slot, and a real visible mobile label inside each `td`; never branch into a second card/list DOM. Put each sortable native button in its `th[scope=col]`, set `aria-sort` only on the matching header, and emit only `phx-click={@sort_event}` plus `phx-value-sort-key`; do not mutate/reorder rows. Render loading with named skeleton/status and `aria-busy`, and render empty/error/unavailable/permission-denied as explicit rows/regions with exact UI-SPEC copy/role urgency (EmptyState composition may be completed in Plan 04, but state semantics/copy must work now). Filter `class`, `style`, `role`, browser handlers, and unsupported action attrs while preserving id and supported aria/data/phx attributes. Make table/state/hostile-content groups green without weakening Wave 0 assertions. Decision trace: D-03..D-11, D-25.</action>
  <verify>
    <automated>mix test test/oban_powertools/web/components/data_display_test.exs --seed 0</automated>
    <automated>mix format --check-formatted lib/oban_powertools/web/components/data_display.ex test/oban_powertools/web/components/data_display_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - Rendered ready state contains exactly one `table`, one row per input row, one selection/action rendering per row, and each `td` contains its original visible column label for CSS reflow.
    - Only the active `th` has `aria-sort`; sortable controls are native buttons and parent event/key attrs render without component-owned sorting state.
    - All six states render explicit named copy in the same data region; loading sets `aria-busy`; immediate error alone uses alert urgency.
    - There is no `role="grid"`, raw HTML API, duplicate responsive tree, inline width/style, or production page import.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 77-03-02: Add token-scoped desktop and 320px same-table reflow</name>
  <files>assets/oban_powertools/tokens.css, priv/static/oban_powertools/oban_powertools.css, test/oban_powertools/web/theme_tokens_test.exs, test/oban_powertools/web/assets_test.exs, test/oban_powertools/web/components/data_display_test.exs</files>
  <read_first>
    - @assets/oban_powertools/tokens.css
    - @priv/static/oban_powertools/oban_powertools.css
    - @test/oban_powertools/web/theme_tokens_test.exs
    - @test/oban_powertools/web/assets_test.exs
    - @test/oban_powertools/web/components/data_display_test.exs
    - @lib/mix/tasks/oban_powertools.assets.build.ex
    - @.planning/phases/74-primitives-library/74-02-PLAN.md
    - @.planning/phases/75-form-components/75-05-SUMMARY.md
    - @.planning/phases/77-data-display-operator-patterns/77-UI-SPEC.md
  </read_first>
  <action>Add `.obpt-root` data-table wrapper, caption/header/cell, toolbar/summary/state, sort/focus, mobile-label, selection/action, and stacked-card rules to `tokens.css`. Use semantic token variables, existing font/weight/space/radius/focus/motion values, calm dense wide rows, `min-width: 0`, and token-derived 44px effective controls. In `@media (max-width: 24rem)`, reflow the existing `thead/tbody/tr/td` presentation into a compact header strip and stacked row cards while revealing each in-cell mobile label; preserve the table DOM and sortable header controls. Ordinary table surfaces must wrap/truncate/stack and must not introduce horizontal scrolling. Add reduced-motion-safe feedback without animating row reorder. Extend token tests with a data class family, selector scoping, token-backed declarations, 24rem rules, focus, and no-literal guards; extend asset tests with representative selectors. Run `mix oban_powertools.assets.build` to regenerate packaged CSS; never hand-edit the compiled file or touch `theme.js`.</action>
  <verify>
    <automated>mix oban_powertools.assets.build && mix test test/oban_powertools/web/theme_tokens_test.exs test/oban_powertools/web/assets_test.exs test/oban_powertools/web/components/data_display_test.exs --seed 0</automated>
    <automated>mix oban_powertools.assets.build && git diff --exit-code -- priv/static/oban_powertools/oban_powertools.js</automated>
    <automated>mix compile --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - Data table selectors are below `.obpt-root`; visual declarations use `var(--obpt-*)` or existing approved calculations and do not broaden literal allowlists.
    - A `max-width: 24rem` rule reflows the same table rows/cells, reveals mobile labels, preserves sort controls, and gives selection controls a token-derived effective 44px target.
    - Ordinary table/state surfaces do not set horizontal overflow; no inline progress/table width or JS hook is introduced.
    - Rebuilding assets is byte-stable, representative packaged selectors exist, and packaged JS is unchanged.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## ASVS L1 Threat Model

| ID | Threat | Severity | Mitigation and verification |
|----|--------|----------|-----------------------------|
| T-77-03-LEAK | Duplicate mobile DOM or attrs expose raw arguments in hidden/copy/title/data/detail channels | high | Exactly one table tree; no duplicate cards; DataTable has no copy/title/detail secret API; full-HTML and source tests block duplicate/hidden channels. |
| T-77-03-ATTR | Rest attrs override table semantics, aria-sort, styles, role, or sort actions | high | Filter semantic/visual/handler overrides; component owns roles and sort attrs; tests pass hostile attrs and inspect final relationships. |
| T-77-03-XSS | Hostile row/caption/label/slot text escapes table cells | high | HEEx/render_slot escaping only, no raw HTML API; HTML-looking fixtures remain text in render tests. |
| T-77-03-ATOM | Sort/state input triggers unsafe atom creation or unknown state behavior | high | Closed compile-time state/direction atoms; sort keys stay opaque binaries passed to parent; no conversion functions; source guard applies. |
| T-77-03-DOS | Literal thousands of rows or pathological content freezes layout/browser | high | Component remains presentation-only; stories must supply bounded windows and truthful totals; rows have stable keys and ordinary content wraps without nested scrolling. |
</threat_model>

<verification>
- Run component, token, asset, formatter, and compile checks after each task.
- Inspect rendered HTML assertion for exactly one semantic table tree and one control per row.
- Confirm `git diff --name-only` contains no `*_live.ex`, dependency, manifest, or browser baseline files.
</verification>

<success_criteria>
- DataTable ships with native semantics, parent-owned sorting, explicit state handling, and one responsive DOM.
- The same table becomes labelled card rows at 320px without page-level overflow.
- Token/package asset contracts and focused tests pass; browser proof remains scheduled for Plan 07.
</success_criteria>

<output>
Create `.planning/phases/77-data-display-operator-patterns/77-03-SUMMARY.md` when done.
</output>
