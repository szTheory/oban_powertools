---
phase: 77-data-display-operator-patterns
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - test/oban_powertools/web/status_taxonomy_test.exs
  - test/oban_powertools/web/components/data_display_test.exs
  - test/oban_powertools/data_display_story_catalog_test.exs
  - test/browser/specs/data-display.behavior.spec.ts
autonomous: true
requirements:
  - DATA-01
  - DATA-02
  - DATA-03
  - DATA-04
  - A11Y-02
must_haves:
  truths:
    - "D-01..D-25 are executable RED contracts before production implementation starts."
    - "D-02: RED contracts derive implementation details from the binding UI-SPEC, prior phase contracts, Phoenix/LiveView norms, and existing code; execution does not reopen resolved product-vision choices."
    - "The contracts require one semantic DataTable DOM, parent-owned sorting, explicit data states, domain-aware atom-safe taxonomy, normalized-only redaction input, and bounded stress rendering."
    - "RED is accepted only when failures identify missing Phase 77 symbols or fixtures, never malformed tests, unavailable pre-existing infrastructure, or unrelated aggregate VRT drift."
  artifacts:
    - path: "test/oban_powertools/web/status_taxonomy_test.exs"
      provides: "Exhaustive domain/state taxonomy and atom-safety contract"
    - path: "test/oban_powertools/web/components/data_display_test.exs"
      provides: "Native semantics, state, safe-attr, hostile-content, long-value, and redaction contract"
    - path: "test/oban_powertools/data_display_story_catalog_test.exs"
      provides: "Ten-story order, deterministic fixture, taxonomy coverage, and bounded-window contract"
    - path: "test/browser/specs/data-display.behavior.spec.ts"
      provides: "Live sorting, responsive, focus, overflow, redaction, toast, progress, and row-window behavior contract"
  key_links:
    - from: "test/oban_powertools/web/components/data_display_test.exs"
      to: "lib/oban_powertools/web/components/data_display.ex"
      via: "Phoenix.LiveViewTest.__render_component__/4"
      pattern: "ObanPowertools.Web.Components.DataDisplay"
    - from: "test/browser/specs/data-display.behavior.spec.ts"
      to: "test/browser/support/manifest.ts"
      via: "generated dataStories lookup"
      pattern: "dataStories"
---

<objective>
Create the Wave 0/RED contract layer for the entire Phase 77 surface without implementing any production component, catalog, manifest, or browser behavior.

Purpose: make the phase's semantic, confidentiality, responsive, bounded-data, and accessibility obligations fail meaningfully before implementation.
Output: four syntactically valid test files whose failures point only to missing Phase 77 artifacts.
</objective>

<execution_context>
@/Users/jon/.agents/gsd-core/workflows/execute-plan.md
@/Users/jon/.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/77-data-display-operator-patterns/77-CONTEXT.md
@.planning/phases/77-data-display-operator-patterns/77-UI-SPEC.md
@.planning/phases/77-data-display-operator-patterns/77-RESEARCH.md
@.planning/phases/77-data-display-operator-patterns/77-PATTERNS.md
@.planning/phases/77-data-display-operator-patterns/77-VALIDATION.md
@.planning/phases/74-primitives-library/74-01-PLAN.md
@.planning/phases/75-form-components/75-01-PLAN.md
</context>

## Artifacts this phase produces

- Exact private/support symbols: taxonomy `normalize_state/1`, `humanize/1`, `domain!/1`, `resolve_alias/2`; DataDisplay `visual_safe_rest/2`, `action_attr?/1`, `data_state/1`, `row_dom_id/2`, `aria_sort/3`, `truncate_middle/2`, `preserve_suffix/2`, `clamp_progress/2`, and `render_normalized_display/1` (or private clauses under that exact dispatcher name).
- Created files: `lib/oban_powertools/web/status_taxonomy.ex`, `lib/oban_powertools/web/components/data_display.ex`, `test/oban_powertools/web/status_taxonomy_test.exs`, `test/oban_powertools/web/components/data_display_test.exs`, `test/support/data_display_story_catalog.ex`, `test/oban_powertools/data_display_story_catalog_test.exs`, `test/browser/specs/data-display.behavior.spec.ts`, and `test/browser/support/verify-data-baselines.mjs`; generated files are exactly 120 `test/browser/__screenshots__/chromium-{320,tablet,wide}/showcase/data-*/*.png` baselines.
- Baseline verification CLI: `test/browser/support/verify-data-baselines.mjs` defaults to validating the exact manifest-derived data screenshot set with cardinality 120; its created `--changed-scope` mode rejects tracked, untracked, and renamed screenshot changes outside the expected `data-*` matrix.
- Exact CSS symbols: `.obpt-data-table`, `.obpt-data-state`, `.obpt-description-list`, `.obpt-key-value`, `.obpt-machine-value`, `.obpt-timeline`, `.obpt-progress`, `.obpt-metric-card`, `.obpt-code-block`, `.obpt-args-viewer`, `.obpt-redacted-value`, `.obpt-empty-state`, `.obpt-toast`, and `.obpt-flash-group`, all rooted below `.obpt-root` and copied into the packaged CSS by the deterministic asset build.
- Complete attr/slot manifest: `data_table/1` attrs `id/caption/rows/row_id/state/resource/row_count/pagination_summary/sort_key/sort_direction/sort_event/rest` and slots `toolbar/selection/col(label,sort_key,value_kind)/action/state_detail`; `status_pill/1` attrs `id/domain/state/rest`; `description_list/1` attrs `id/state/resource/rest` and repeated `item(label,value_kind)`; `key_value/1` attrs `id/label/value/value_kind/rest`; `machine_value/1` attrs `id/value/kind/truncate/expand/rest`; `timeline/1` attrs `id/state/resource/rest` and repeated `event(timestamp,title,source,domain,state,detail)`; `progress_bar/1` attrs `id/label/value/max/state/rest`; `metric_card/1` attrs `id/label/value/trend/status/tone/rest` plus `action`; `code_block/1` attrs `id/label/content/language/rest`; `args_viewer/1` attrs `id/label/display/kind`; `redacted_value/1` attrs `id/reason/message`; `empty_state/1` attrs `id/heading/body/rest` plus `action`; `toast/1` attrs `id/tone/urgency/dismiss_event/rest` plus `inner_block`; `flash_group/1` attrs `id/flash/dismiss_event/rest`.
- New production modules/files: `ObanPowertools.Web.StatusTaxonomy` in `lib/oban_powertools/web/status_taxonomy.ex` with `spec/2` and `all_specs/0`; `ObanPowertools.Web.Components.DataDisplay` in `lib/oban_powertools/web/components/data_display.ex` with `data_table/1`, `status_pill/1`, `description_list/1`, `key_value/1`, `machine_value/1`, `timeline/1`, `progress_bar/1`, `metric_card/1`, `code_block/1`, `args_viewer/1`, `redacted_value/1`, `empty_state/1`, `toast/1`, and `flash_group/1`.
- DataTable API: attrs `id`, `caption`, `rows`, `row_id`, `state`, `resource`, `row_count`, `pagination_summary`, `sort_key`, `sort_direction`, `sort_event`, `rest`; slots `toolbar`, `selection`, repeated `col` with `label`/`sort_key`/`value_kind`, `action`, and constrained `state_detail`.
- Secondary API: repeated DescriptionList `item` slots; Timeline `event` slots; ProgressBar `label`/`value`/`max`/`state`; MetricCard label/value/trend/status attrs; CodeBlock `label`/`content`/`language`; ArgsViewer normalized `display` input; RedactedValue closed `reason`; EmptyState heading/body/action; Toast/Flash closed tone, urgency, dismiss attrs/slots.
- Private/support symbols: string-keyed domain registry and humanizer; safe rest normalization/action filtering; state-copy normalization; clamped progress helper; machine-value truncation helper; normalized display tuple/map dispatch. RedactedValue has no general `rest`, raw-value, slot, tooltip, expansion, or copy API.
- New evidence files/modules: the four tests created by this plan; `ObanPowertools.DataDisplayStoryCatalog` in `test/support/data_display_story_catalog.ex` with `stories/0`, `story!/1`, `snapshot_name/1`, `a11y_target/1`, `huge_args_fixture/0`, `long_value_fixture/0`, `timeline_fixture/0`, `large_row_total/0`, `large_row_window/0`, and ten locked story IDs.
- Modified integration surfaces: exact CSS bases `.obpt-data-table`, `.obpt-data-state`, `.obpt-description-list`, `.obpt-key-value`, `.obpt-machine-value`, `.obpt-timeline`, `.obpt-progress`, `.obpt-metric-card`, `.obpt-code-block`, `.obpt-args-viewer`, `.obpt-redacted-value`, `.obpt-empty-state`, `.obpt-toast`, `.obpt-flash-group` under `.obpt-root`; regenerated packaged CSS; ShowcaseLive `@data_catalog_module`, `@data_catalog_path`, `@data_stories`, `data_story_body/1`, `data_sort_key`, `data_sort_direction`, and `handle_event("sort-data-table", ...)`.
- Manifest/browser symbols: schema field `schema_version: 5`, collection `data_stories`, appended `targets`, target `kind: "data"`, TypeScript `ShowcaseDataStory`, `dataStories`, behavior helper `dataStory(id)`, and the `assertShowcaseStructure` data-kind branch in `test/browser/support/showcase.ts`.
- Browser artifacts: `data-display.behavior.spec.ts`; exactly 120 PNGs below `test/browser/__screenshots__/chromium-{320,tablet,wide}/showcase/data-*/*.png`; reconciled `77-VALIDATION.md` evidence and Nyquist fields.

<tasks>

<task type="auto" tdd="true">
  <name>Task 77-01-01: Pin the exhaustive status taxonomy contract</name>
  <files>test/oban_powertools/web/status_taxonomy_test.exs</files>
  <read_first>
    - @test/oban_powertools/web/status_taxonomy_test.exs
    - @.planning/phases/77-data-display-operator-patterns/77-UI-SPEC.md
    - @.planning/phases/77-data-display-operator-patterns/77-RESEARCH.md
    - @.planning/phases/77-data-display-operator-patterns/77-PATTERNS.md
    - @lib/oban_powertools/web/control_plane_presenter.ex
    - @test/oban_powertools/primitive_story_catalog_test.exs
  </read_first>
  <action>Create an async ExUnit table-driven contract for `StatusTaxonomy.spec/2` and `all_specs/0`. Pin every UI-SPEC domain/value mapping plus the researched supplemental Lifeline health, output availability, cron history, forensics completeness, continuity attempt, and host-follow-up states. Require exact `%{label, tone, icon, sr_prefix}` maps, atom/binary parity, explicit preview aliases, domain-collision cases, neutral/dot humanized unknown-state fallback, unknown-domain rejection, deterministic unique ordered `all_specs/0`, and a source assertion forbidding `String.to_atom/1` and `String.to_existing_atom/1`. Do not add the production module. Decision trace: D-12..D-15.</action>
  <verify>
    <automated>mix test test/oban_powertools/web/status_taxonomy_test.exs --seed 0</automated>
  </verify>
  <acceptance_criteria>
    - The test file compiles and reaches RED assertions identifying the missing `ObanPowertools.Web.StatusTaxonomy` API.
    - Every mandatory UI-SPEC domain/value and each researched supplemental state appears in a table-driven assertion with exact label/tone/icon/sr-prefix expectations.
    - Source assertions reject both atom-conversion calls only in the new taxonomy file and do not scan legacy page modules.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 77-01-02: Pin component semantics, responsive state, and redaction contracts</name>
  <files>test/oban_powertools/web/components/data_display_test.exs</files>
  <read_first>
    - @test/oban_powertools/web/components/data_display_test.exs
    - @test/oban_powertools/web/components/primitives_test.exs
    - @test/oban_powertools/web/components/forms_test.exs
    - @lib/oban_powertools/web/components/primitives.ex
    - @lib/oban_powertools/web/components/forms.ex
    - @lib/oban_powertools/runtime_config.ex
    - @.planning/phases/77-data-display-operator-patterns/77-PATTERNS.md
  </read_first>
  <action>Create render/source tests using the repository's `Phoenix.LiveViewTest.__render_component__/4` and slot-map helpers. Pin every planned public component and exact native relationships: one `table/caption/thead/tbody` tree, `th[scope=col]`, one truthful `aria-sort`, native sort buttons, visible mobile labels within the same cells, stable row ids, state/count summaries, labelled selection/action seams, `dl/dt/dd`, ordered-list timeline, native progress semantics, `pre/code`, polite/assertive toast roles, and dismiss names. Pin ready/loading/empty/error/unavailable/permission-denied copy and `aria-busy`. Assert hostile strings are escaped; `class`, `style`, semantic `role` overrides, inline handlers, unsafe action attrs, raw HTML APIs, ARIA grid roles, inline progress widths, raw hex/px component literals, table JS hooks, and production-page imports are absent. Build a unique-secret matrix for normalized tuples/maps and assert the sentinel is absent from the complete HTML including text, `title`, tooltip, `data-*`, details/expanded markup, and copy-related attributes, while safe siblings remain. Do not implement components. Decision trace: D-03..D-20.</action>
  <verify>
    <automated>mix test test/oban_powertools/web/components/data_display_test.exs --seed 0</automated>
  </verify>
  <acceptance_criteria>
    - The test file compiles and reaches RED failures for missing `DataDisplay` symbols rather than helper syntax or fixture failures.
    - Tests explicitly prove one table DOM and mobile labels in that DOM; no test permits a duplicate mobile card tree.
    - Redaction tests pass normalized display values only and never construct assigns containing raw and redacted values together.
    - Source guards name every forbidden pattern from D-03, D-06, D-09, D-19, and the deep-work constraints.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 77-01-03: Pin data story determinism and bounded stress fixtures</name>
  <files>test/oban_powertools/data_display_story_catalog_test.exs</files>
  <read_first>
    - @test/oban_powertools/data_display_story_catalog_test.exs
    - @test/oban_powertools/shell_story_catalog_test.exs
    - @test/oban_powertools/form_story_catalog_test.exs
    - @test/support/showcase_catalog.ex
    - @.planning/phases/77-data-display-operator-patterns/77-UI-SPEC.md
    - @.planning/phases/77-data-display-operator-patterns/77-PATTERNS.md
  </read_first>
  <action>Create the RED contract for `ObanPowertools.DataDisplayStoryCatalog`. Assert exactly the ten D-23 IDs in locked order, `kind: :data`, deterministic `stories/0`, unique ids, stable `obpt-data-story-*`/snapshot/a11y targets, complete component/state/taxonomy coverage, adversarial long/Unicode/RTL/HTML-looking values, huge nested args with normalized redaction markers, and a secret-sentinel absence check. Require the thousands-row story to report a truthful total of at least 1,000 while rendering a deterministic window of at most 25 rows with pinned first/last keys. Do not create the catalog.</action>
  <verify>
    <automated>mix test test/oban_powertools/data_display_story_catalog_test.exs --seed 0</automated>
  </verify>
  <acceptance_criteria>
    - The test compiles and fails only because `DataDisplayStoryCatalog` is missing.
    - The exact ten story IDs and their order are literal assertions in Elixir; TypeScript is not introduced as a second source of truth.
    - The huge-row assertion rejects literal thousands-of-node fixtures and requires truthful total/window metadata.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 77-01-04: Pin focused live browser behavior</name>
  <files>test/browser/specs/data-display.behavior.spec.ts</files>
  <read_first>
    - @test/browser/specs/data-display.behavior.spec.ts
    - @test/browser/specs/forms.behavior.spec.ts
    - @test/browser/specs/shell.behavior.spec.ts
    - @test/browser/support/manifest.ts
    - @test/browser/support/showcase.ts
    - @.planning/phases/77-data-display-operator-patterns/77-VALIDATION.md
  </read_first>
  <action>Create a syntactically valid Playwright spec that imports generated `dataStories`, resolves named stories through a helper, and defines focused checks for click/Enter/Space sorting and one `aria-sort`; all-theme focus visibility; 320px same-table stacked rows, visible labels, row checkbox names/hit area, and <=1px page/story overflow; long value expansion; focusable bounded CodeBlock scrolling; sentinel absence from text/title/data/details/copy channels; toast roles/focus behavior; determinate clamp/unavailable progress; and truthful large total with bounded stable DOM rows. Use existing `prepareShowcase`, viewport, theme, and computed-focus helpers. Add a module-load guard that makes `--list` fail meaningfully until `dataStories` is exported, following Phase 76's manifest-guard precedent. Do not implement browser support or showcase content.</action>
  <verify>
    <automated>npx playwright test --list test/browser/specs/data-display.behavior.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - TypeScript parsing succeeds and discovery reaches a deliberate RED guard for missing schema-5 `dataStories` support.
    - Test titles separate 320-only and wide behavior and include the `data` kind for focused grep discovery.
    - No complete list of the ten story IDs is duplicated in TypeScript; only behavior-specific named lookups are allowed.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## ASVS L1 Threat Model

| ID | Threat | Severity | Mitigation and verification |
|----|--------|----------|-----------------------------|
| T-77-01-LEAK | Raw argument leakage through rendered text, copy, `title`, tooltip, `data-*`, details, or pretty JSON | high | RED full-HTML sentinel matrix receives normalized display output only and checks every named channel; high severity blocks later GREEN work. |
| T-77-01-ATTR | Unsafe global attr propagation overrides roles/semantics or injects actions/styles | high | RED render/source tests require filtered `class`, `style`, `role`, handlers, and unsafe actions while retaining narrowly supported identity/ARIA/data/phx attrs. |
| T-77-01-XSS | Hostile cell, label, code, status, or policy content executes as markup | high | RED tests require HEEx escaping and forbid raw HTML APIs; use HTML-looking fixtures. |
| T-77-01-ATOM | External state creates atoms or an unknown domain receives a misleading prefix | high | RED taxonomy tests ban atom conversion, require string-key normalization, deterministic known-domain fallback, and unknown-domain rejection. |
| T-77-01-DOS | Huge args or thousands of rows exhaust render/browser resources | high | RED catalog/browser tests cap the rendered row window, require truthful totals, bounded code regions, stable keys, and deterministic payload fixtures. |
</threat_model>

<verification>
- Run each narrow RED command and confirm failures name missing Phase 77 modules/exports only.
- Run `mix format --check-formatted` for the three Elixir test files.
- Treat the existing planning-time task map in `77-VALIDATION.md` as read-only; final execution-evidence reconciliation belongs exclusively to Task `77-07-02`.
</verification>

<success_criteria>
- All four Wave 0 contract files exist and are syntactically valid.
- Every phase requirement and D-01..D-25 has an independently checkable RED assertion or a named later evidence seam.
- The contract forbids duplicate responsive DOM, raw+redacted co-input, dynamic atom creation, unbounded row fixtures, unsafe attrs, and semantic overrides.
- No production implementation or baseline file is created by this plan.
</success_criteria>

<output>
Create `.planning/phases/77-data-display-operator-patterns/77-01-SUMMARY.md` when done.
</output>
