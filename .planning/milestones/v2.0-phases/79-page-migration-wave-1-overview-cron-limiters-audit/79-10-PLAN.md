---
phase: 79-page-migration-wave-1-overview-cron-limiters-audit
plan: 10
type: execute
wave: 4
depends_on:
  - 79-03
  - 79-04
  - 79-05
  - 79-06
  - 79-09
files_modified:
  - examples/phoenix_host/config/test.exs
  - examples/phoenix_host/lib/phoenix_host_web/router.ex
  - examples/phoenix_host/test/support/phase79_browser_fixtures.ex
  - examples/phoenix_host/test/phase79_browser_fixtures_test.exs
  - scripts/with-showcase-server.sh
  - scripts/playwright-docker.sh
  - test/browser/support/phase79-fixtures.ts
  - test/browser/specs/phase79-fixtures.spec.ts
autonomous: true
requirements:
  - PAGE-01
  - PAGE-05
  - PAGE-06
  - PAGE-08
  - PAGE-10
  - A11Y-*
must_haves:
  truths:
    - "Connected Playwright runs use a fresh isolated example-host test database plus an authenticated, idempotent per-test reset seam with deterministic Overview, Cron, Limiter, Audit, authorization, and recovery state."
    - "Fixture modules and routes are compiled and exposed only for MIX_ENV=test with PHASE79_BROWSER_FIXTURES=1, require a per-run secret, return indistinguishable 404 for absent/invalid secrets, and remain outside the library package boundary."
    - "The HTTP seam is exactly PhoenixHostWeb.Phase79BrowserFixturesController with reset/2, actor/2, and recovery/2 actions, reached only through three explicit POST router mappings under /__phase79_browser_fixtures__."
    - "scripts/with-showcase-server.sh creates, migrates, starts, and drops one explicitly validated phoenix_host_test database; it never seeds, connects to, or drops phoenix_host_dev or a broad/unresolved database name."
    - "The narrowly named PHASE79_BROWSER_FIXTURE_SECRET reaches Docker-based browser tests through scripts/playwright-docker.sh by environment name only, is never logged or embedded in Docker argv, and is not forwarded when absent."
    - "The TypeScript fixture helper fails before any request when PHASE79_BROWSER_FIXTURE_SECRET is absent, validates every response, and never accepts ambient seeds or skips a stateful test when reset/auth/recovery setup is unavailable."
    - "Docker-backed fixture proof demonstrates a successful project-keyed reset, explicit actor state, real-preview recovery perturbations, wrong-secret denial, and missing-secret fail-closed behavior."
  artifacts:
    - path: "examples/phoenix_host/test/support/phase79_browser_fixtures.ex"
      provides: "Token-protected test-only reset/auth/recovery domain seam plus concrete Phoenix HTTP controller"
      exports: ["PhoenixHostWeb.Phase79BrowserFixtures.reset/1", "PhoenixHostWeb.Phase79BrowserFixtures.set_actor/2", "PhoenixHostWeb.Phase79BrowserFixtures.perturb_recovery/3", "PhoenixHostWeb.Phase79BrowserFixturesController.reset/2", "PhoenixHostWeb.Phase79BrowserFixturesController.actor/2", "PhoenixHostWeb.Phase79BrowserFixturesController.recovery/2"]
    - path: "examples/phoenix_host/lib/phoenix_host_web/router.ex"
      provides: "Compile-time gated POST mappings for the three fixture controller actions"
      exports: ["POST /__phase79_browser_fixtures__/reset", "POST /__phase79_browser_fixtures__/actor", "POST /__phase79_browser_fixtures__/recovery"]
    - path: "examples/phoenix_host/test/phase79_browser_fixtures_test.exs"
      provides: "Host reset/idempotency/isolation/auth/recovery/route-off proof"
    - path: "scripts/with-showcase-server.sh"
      provides: "Disposable test-database and per-run fixture-secret lifecycle"
    - path: "scripts/playwright-docker.sh"
      provides: "Non-logging name-only PHASE79_BROWSER_FIXTURE_SECRET propagation into Docker"
    - path: "test/browser/support/phase79-fixtures.ts"
      provides: "Fail-closed per-project reset, actor-session, and recovery helpers"
    - path: "test/browser/specs/phase79-fixtures.spec.ts"
      provides: "Docker-connected reset/auth/recovery plus secret propagation/omission evidence"
  key_links:
    - from: "scripts/with-showcase-server.sh"
      to: "scripts/playwright-docker.sh"
      via: "child environment carries PHASE79_BROWSER_FIXTURE_SECRET without printing it"
      pattern: "PHASE79_BROWSER_FIXTURE_SECRET"
    - from: "scripts/playwright-docker.sh"
      to: "test/browser/support/phase79-fixtures.ts"
      via: "conditional Docker -e name-only forwarding"
      pattern: "-e PHASE79_BROWSER_FIXTURE_SECRET"
    - from: "test/browser/support/phase79-fixtures.ts"
      to: "examples/phoenix_host/test/support/phase79_browser_fixtures.ex"
      via: "secret-authenticated reset/actor/recovery requests against an isolated test database"
      pattern: "reset|actor|expired|drifted|consumed|skipped|partial"
    - from: "test/browser/specs/phase79-fixtures.spec.ts"
      to: "test/browser/specs/page-migration-wave-1.spec.ts"
      via: "proven fixture lifecycle consumed by final connected page evidence"
      pattern: "chromium-wide|project.name"
---

<objective>
Add the isolated connected-browser fixture harness, launcher lifecycle, and narrowly scoped Docker secret propagation required by Phase 79's real-page evidence.

Purpose: give connected page tests deterministic database, authorization, and recovery state without ambient seeds, production exposure, secret disclosure, or unsafe database cleanup.
Output: an opt-in example-host fixture seam, disposable test launcher, Docker environment bridge, fail-closed TypeScript helpers, and connected lifecycle proof.
</objective>

<execution_context>
@/Users/jon/.agents/gsd-core/workflows/execute-plan.md
@/Users/jon/.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/79-page-migration-wave-1-overview-cron-limiters-audit/79-CONTEXT.md
@.planning/phases/79-page-migration-wave-1-overview-cron-limiters-audit/79-RESEARCH.md
@.planning/phases/79-page-migration-wave-1-overview-cron-limiters-audit/79-PATTERNS.md
@.planning/phases/79-page-migration-wave-1-overview-cron-limiters-audit/79-UI-SPEC.md
@.planning/phases/79-page-migration-wave-1-overview-cron-limiters-audit/79-03-SUMMARY.md
@.planning/phases/79-page-migration-wave-1-overview-cron-limiters-audit/79-04-SUMMARY.md
@.planning/phases/79-page-migration-wave-1-overview-cron-limiters-audit/79-05-SUMMARY.md
@.planning/phases/79-page-migration-wave-1-overview-cron-limiters-audit/79-06-SUMMARY.md
@.planning/phases/79-page-migration-wave-1-overview-cron-limiters-audit/79-09-SUMMARY.md
</context>

## Artifacts this phase produces

- New test-only module path `examples/phoenix_host/test/support/phase79_browser_fixtures.ex`, containing domain module `PhoenixHostWeb.Phase79BrowserFixtures` with `reset/1`, `set_actor/2`, and `perturb_recovery/3`, plus Phoenix controller `PhoenixHostWeb.Phase79BrowserFixturesController` with exact action entry points `reset/2`, `actor/2`, and `recovery/2`.
- Compile-time gated router scope `/__phase79_browser_fixtures__` with exactly `post "/reset", Phase79BrowserFixturesController, :reset`, `post "/actor", Phase79BrowserFixturesController, :actor`, and `post "/recovery", Phase79BrowserFixturesController, :recovery`, all through the existing JSON `:api` pipeline and present only when `MIX_ENV=test` and `PHASE79_BROWSER_FIXTURES=1`.
- New host test path `examples/phoenix_host/test/phase79_browser_fixtures_test.exs`, proving idempotency, project/run isolation, actor state, recovery scoping, 404 denial, route-off behavior, and public-only response fields.
- New environment variable `PHASE79_BROWSER_FIXTURE_SECRET`, generated per launcher run, consumed only by the opt-in example-host seam and browser helper, forwarded to Docker by name rather than value, and never logged.
- Updated launcher `scripts/with-showcase-server.sh`, using `MIX_ENV=test`, `PHASE79_BROWSER_FIXTURES=1`, a validated unique `MIX_TEST_PARTITION`, a normal test-server pool, exact process cleanup, and exact isolated database drop on success/failure/signal.
- Updated Docker wrapper `scripts/playwright-docker.sh`, conditionally adding only `-e PHASE79_BROWSER_FIXTURE_SECRET` when the host value is nonblank; absence leaves the variable unset in the container and existing non-Phase-79 runs unchanged.
- New TypeScript helper path `test/browser/support/phase79-fixtures.ts`, with reset, actor, and recovery helpers that validate project names, response schemas, and the required secret before requests.
- New Docker-connected proof path `test/browser/specs/phase79-fixtures.spec.ts`, covering reset twice, project identity, actor sessions, every recovery, wrong-secret 404, missing-secret local failure, and connected showcase availability.
- No production route/module, library package file, dependency, lockfile, schema migration, provider adapter, public Cron API, or fixture secret value is produced.

No plain Phase `79-SPEC.md` with `## Edge Coverage` or `## Prohibitions` exists. The fixture boundary implements only the test infrastructure required by `79-UI-SPEC.md`'s connected verification contract.

<tasks>

<task type="auto" tdd="true">
  <name>Task 79-10-01: Add the opt-in example-host reset, actor, and recovery seam</name>
  <files>examples/phoenix_host/config/test.exs, examples/phoenix_host/lib/phoenix_host_web/router.ex, examples/phoenix_host/test/support/phase79_browser_fixtures.ex, examples/phoenix_host/test/phase79_browser_fixtures_test.exs</files>
  <read_first>
    - @examples/phoenix_host/config/test.exs
    - @examples/phoenix_host/lib/phoenix_host_web/router.ex
    - @examples/phoenix_host/test/support/phase79_browser_fixtures.ex (new; confirm absent before creation)
    - @examples/phoenix_host/test/phase79_browser_fixtures_test.exs (new; confirm absent before creation)
    - @examples/phoenix_host/mix.exs
    - @examples/phoenix_host/lib/phoenix_host_web/oban_powertools_auth.ex
    - @examples/phoenix_host/priv/repo/seeds.exs
    - @lib/oban_powertools/cron/entry.ex
    - @lib/oban_powertools/lifeline/repair_preview.ex
    - @lib/oban_powertools/limits/resource.ex
    - @lib/oban_powertools/limits/state.ex
    - @lib/oban_powertools/explain.ex
    - @lib/oban_powertools/audit.ex
    - @mix.exs
    - @.planning/phases/79-page-migration-wave-1-overview-cron-limiters-audit/79-UI-SPEC.md
  </read_first>
  <action>Create domain module `PhoenixHostWeb.Phase79BrowserFixtures` and Phoenix controller `PhoenixHostWeb.Phase79BrowserFixturesController` in `examples/phoenix_host/test/support/phase79_browser_fixtures.ex`. The domain module exports only `reset/1`, `set_actor/2`, and `perturb_recovery/3`; the controller uses `PhoenixHostWeb, :controller` and exposes exactly `reset(conn, params)`, `actor(conn, params)`, and `recovery(conn, params)`. Each controller action must authenticate header `x-phase79-fixture-secret` with constant-time comparison before validating/dispatching to the matching domain function, return only closed JSON on success, and route every absent/invalid-secret or unavailable seam through the same empty 404 response without logging the secret or differentiating endpoint existence. In `PhoenixHostWeb.Router`, add an existing-`:api`-pipeline scope `/__phase79_browser_fixtures__` compiled only when `Mix.env() == :test` and `PHASE79_BROWSER_FIXTURES=1`, with exactly `post "/reset", Phase79BrowserFixturesController, :reset`, `post "/actor", Phase79BrowserFixturesController, :actor`, and `post "/recovery", Phase79BrowserFixturesController, :recovery`; add no catch-all/forward/resource route. Implement one transaction-backed idempotent reset keyed by a validated Playwright project name and run tag that removes only that run's rows and recreates fixed-UTC data: at least two Cron entries for first-open/switch plus active/paused mutation truth; a runnable Limiter and a blocked Limiter with State, block-start Explain snapshot, retained-history facts, and authorized/denied destination states; 45 exact-filter Audit rows for three pages, same-timestamp ties, more than 20 newer unrelated rows, and repair-continuity evidence; active/resolved Overview support state; and clean preview tables. Add token-protected actor-session helpers for `ops` and `read_only`, plus narrow recovery perturbations affecting only the current run's real preview to induce expired, drifted, consumed, skipped, and partial outcomes. Return only closed public fixture IDs/counts/state names—never preview tokens, plan hashes, metadata, secrets, or raw errors. Update `test.exs` so the opt-in browser run enables dev/showcase routes, the endpoint server, and a normal connection pool; ordinary tests retain SQL Sandbox and disabled dev/fixture routes. Test the exact controller actions/router mappings, reset twice, project/run isolation, actor changes, every recovery, cleanup scoping, wrong/missing-secret denial, route-off behavior, and public-only response shape.</action>
  <verify>
    <automated>cd examples/phoenix_host &amp;&amp; PHASE79_BROWSER_FIXTURES=1 MIX_TEST_PARTITION=_phase79_fixture_contract mix test test/phase79_browser_fixtures_test.exs --seed 0</automated>
    <automated>cd examples/phoenix_host &amp;&amp; PHASE79_BROWSER_FIXTURES=0 MIX_TEST_PARTITION=_phase79_fixture_route_off mix test test/phase79_browser_fixtures_test.exs --only route_off --seed 0</automated>
    <automated>mix test test/oban_powertools/hex_release_test.exs --seed 0</automated>
    <automated>rg -n 'def (reset|actor|recovery)\(conn, params\)' examples/phoenix_host/test/support/phase79_browser_fixtures.ex &amp;&amp; rg -n 'post "/(reset|actor|recovery)", Phase79BrowserFixturesController, :(reset|actor|recovery)' examples/phoenix_host/lib/phoenix_host_web/router.ex</automated>
  </verify>
  <acceptance_criteria>
    - Both new fixture paths were confirmed absent before creation and every ordinary existing file in this task was read before modification.
    - Two consecutive resets for each of `chromium-320`, `chromium-tablet`, and `chromium-wide` return the same public identities/counts and leave no duplicate tagged rows.
    - Every reset provides two switchable Cron entries and clean mutation state, runnable/blocked Limiter selection plus current/snapshot/history truth, 45 reachable filtered Audit rows plus newer unrelated rows, Overview repair continuity, and both actor states.
    - A current-run real preview can be perturbed to each locked recovery state without returning/serializing token, hash, metadata, secret, or raw error; another run/project remains untouched.
    - Fixture modules/routes are absent with the flag off, return the same 404 for missing/wrong secrets when on, and remain outside the Hex/runtime package boundary.
    - The only HTTP entry points are `PhoenixHostWeb.Phase79BrowserFixturesController.reset/2`, `actor/2`, and `recovery/2`, mapped one-to-one to the three exact POST paths through the existing API pipeline; no forward, resource, GET, catch-all, or production route is added.
  </acceptance_criteria>
  <done>The example host exposes a deterministic, idempotent, secret-protected fixture seam only in the explicitly opted-in test runtime.</done>
</task>

<task type="auto">
  <name>Task 79-10-02: Isolate the browser database and propagate the fixture secret into Docker</name>
  <files>scripts/with-showcase-server.sh, scripts/playwright-docker.sh</files>
  <read_first>
    - @scripts/with-showcase-server.sh
    - @scripts/playwright-docker.sh
    - @package.json
    - @playwright.config.ts
    - @examples/phoenix_host/config/test.exs
    - @examples/phoenix_host/mix.exs
    - @examples/phoenix_host/priv/repo/seeds.exs
    - @.planning/phases/79-page-migration-wave-1-overview-cron-limiters-audit/79-UI-SPEC.md
  </read_first>
  <action>Update `scripts/with-showcase-server.sh` to derive an explicit `MIX_TEST_PARTITION` in the form `_phase79_{port}_{pid}`, validate it with the closed numeric pattern before any database command, resolve the exact database name as `phoenix_host_test${MIX_TEST_PARTITION}`, create/migrate that database under `MIX_ENV=test`, generate a cryptographically random nonblank `PHASE79_BROWSER_FIXTURE_SECRET`, and start the test endpoint with `PHASE79_BROWSER_FIXTURES=1`, the secret, port, and server flag. Export only `PLAYWRIGHT_TEST_BASE_URL`, `PLAYWRIGHT_DOCKER_BASE_URL`, and `PHASE79_BROWSER_FIXTURE_SECRET` to the child command. Extend the trap to terminate the exact server process and drop only the previously validated exact `phoenix_host_test_phase79_*` database on success, command failure, INT, or TERM; never invoke `priv/repo/seeds.exs`, use `MIX_ENV=dev`, touch `phoenix_host_dev`, accept an empty suffix, or compute a drop target from an unresolved variable/glob. Update `scripts/playwright-docker.sh` after reading its existing forwarded environment list: when and only when `PHASE79_BROWSER_FIXTURE_SECRET` is nonblank, append the Docker pair `-e PHASE79_BROWSER_FIXTURE_SECRET` with no `=value`, so Docker imports the child environment value without placing it in process arguments. Never echo the secret, enable shell tracing, print the Docker args, copy the value to another variable name, or forward unrelated host environment. Preserve all existing CI/HOME/npm cache/base-URL behavior and leave the secret absent inside Docker when the host value is absent.</action>
  <verify>
    <automated>bash -n scripts/with-showcase-server.sh &amp;&amp; bash -n scripts/playwright-docker.sh</automated>
    <automated>rg -n --fixed-strings -- '-e PHASE79_BROWSER_FIXTURE_SECRET' scripts/playwright-docker.sh &amp;&amp; ! rg -n 'PHASE79_BROWSER_FIXTURE_SECRET[}]*=' scripts/playwright-docker.sh</automated>
    <automated>git diff --exit-code -- package.json package-lock.json mix.exs mix.lock priv/static/oban_powertools/oban_powertools.js</automated>
  </verify>
  <acceptance_criteria>
    - Both existing scripts are read before modification; shell syntax passes and existing base-URL/network/cache forwarding remains intact.
    - The launcher validates one `_phase79_{port}_{pid}` suffix before create/migrate/drop, starts MIX_ENV=test with the opt-in fixture flag, and drops only the exact resolved `phoenix_host_test_phase79_*` database on every exit path.
    - The launcher contains no seed command, `MIX_ENV=dev`, `phoenix_host_dev`, broad database glob, unresolved drop target, secret echo, or shell tracing.
    - The Docker wrapper conditionally forwards exactly the name `PHASE79_BROWSER_FIXTURE_SECRET` as `-e PHASE79_BROWSER_FIXTURE_SECRET`, never `-e PHASE79_BROWSER_FIXTURE_SECRET=value`; omission leaves the container environment unset.
    - No dependency, lockfile, root Mix project, packaged JavaScript, production route, or migration changes in this task.
  </acceptance_criteria>
  <done>The connected launcher owns one disposable test database and carries the narrowly named fixture secret into Docker without logging or argument disclosure.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 79-10-03: Prove fail-closed fixture helpers through the Docker runner</name>
  <files>test/browser/support/phase79-fixtures.ts, test/browser/specs/phase79-fixtures.spec.ts</files>
  <read_first>
    - @test/browser/support/phase79-fixtures.ts (new; confirm absent before creation)
    - @test/browser/specs/phase79-fixtures.spec.ts (new; confirm absent before creation)
    - @scripts/with-showcase-server.sh
    - @scripts/playwright-docker.sh
    - @test/browser/specs/page-migration-wave-1.spec.ts
    - @test/browser/support/manifest.ts
    - @test/browser/support/showcase.ts
    - @playwright.config.ts
    - @package.json
    - @.planning/phases/79-page-migration-wave-1-overview-cron-limiters-audit/79-UI-SPEC.md
  </read_first>
  <action>Create `phase79-fixtures.ts` helpers that validate the closed Playwright project names, require nonblank `PHASE79_BROWSER_FIXTURE_SECRET` at call time before issuing any request, send it only in header `x-phase79-fixture-secret`, reset before every stateful test using `testInfo.project.name`, set `ops` or `read_only` through the same BrowserContext, request only closed recovery names, and validate exact public response schemas/counts while rejecting unknown/sensitive fields. Create `phase79-fixtures.spec.ts` that runs through the normal launcher and Docker wrapper: reset the active project twice and compare public identities/counts; prove project isolation; set both actors; open a real Cron preview before each expired/drifted/consumed/skipped/partial perturbation; verify connected showcase availability; and assert absent/wrong secrets fail closed. For the omission case, save the existing environment value, delete `process.env.PHASE79_BROWSER_FIXTURE_SECRET`, assert the helper rejects locally before a request with one stable missing-secret error, then restore it in `finally`; separately send a wrong `x-phase79-fixture-secret` header and require the same 404 as no header. Do not skip or fall back to ambient seeds when any fixture setup fails, and never print the secret in assertion output, traces, request URLs, response bodies, test titles, or attachments.</action>
  <verify>
    <automated>npx playwright test --list test/browser/specs/phase79-fixtures.spec.ts</automated>
    <automated>scripts/with-showcase-server.sh scripts/playwright-docker.sh npx playwright test test/browser/specs/phase79-fixtures.spec.ts --project=chromium-wide</automated>
    <automated>npm run visual:a11y -- test/browser/specs/phase79-fixtures.spec.ts --project=chromium-wide</automated>
  </verify>
  <acceptance_criteria>
    - Both new TypeScript paths were confirmed absent before creation and all launcher/wrapper/support contracts were read first.
    - Docker-based execution successfully resets the `chromium-wide` project twice, validates two Cron entries/two Limiter states/45 filtered Audit rows/Overview continuity, and proves both actor states plus every real-preview recovery.
    - Removing `PHASE79_BROWSER_FIXTURE_SECRET` inside the Docker-run spec causes a deterministic local helper rejection before network I/O; absent and wrong request headers receive indistinguishable 404 responses.
    - The Docker wrapper's successful reset proves the launcher secret reached the container by the narrowly named environment bridge; test output, trace metadata, URLs, and attachments contain no secret value.
    - Missing fixture auth/setup, schema mismatch, cross-project contamination, or cleanup failure is a hard test failure with no skip, seed fallback, or partial success.
  </acceptance_criteria>
  <done>Docker-connected browser tests can reset deterministic Phase 79 state, while secret omission/invalidity remains demonstrably fail-closed.</done>
</task>

</tasks>

<threat_model>
## ASVS L1 Threat Model — blocking threshold: high

| Threat ID | Category | Severity | Mitigation and verification |
|-----------|----------|----------|-----------------------------|
| T-79-10-AUTH | Access control | high | Compile-time test-only routing, explicit actor sessions, per-request secret verification, and indistinguishable 404 denial prevent unauthorized fixture use. |
| T-79-10-INJECT | Injection | high | Project, run, actor, and recovery values use closed allowlists; database operations use existing schemas/queries and returned JSON uses closed scalar fields. |
| T-79-10-LEAK | Data protection | high | Name-only Docker forwarding, no shell tracing/echo, public-only response schemas, and trace/output assertions keep the fixture secret and backend tokens/hashes/metadata private. |
| T-79-10-DATA | Data integrity | high | Transactional run-tagged reset, current-run-only recovery, project isolation, and exact cleanup prevent cross-test or production-data mutation. |
| T-79-10-DB | Security configuration | high | A validated exact test database prefix/suffix is required before create/drop; dev database, empty targets, globs, unresolved names, and seed execution are forbidden. |
| T-79-10-REPLAY | Business logic | high | Recovery perturbations require a real current-run preview and cover expired/drifted/consumed/skipped/partial without exposing or replaying preview identity. |
| T-79-10-SUPPLY | Supply chain | high | Fixture support remains in example-host test paths, routes are opt-in test-only, Hex exclusion is tested, and no dependency/lockfile/package change is allowed. |
</threat_model>

<verification>
- Run host fixture contracts first, then shell syntax/source guards, then the Docker-connected fixture spec through both the direct pinned wrapper and `npm run visual:a11y`.
- Treat missing secret propagation, missing-secret network access, wrong-secret disclosure, database cleanup ambiguity, package exposure, or cross-project state as blocking.
- Confirm the cumulative plan diff contains only the eight listed files and no production library route, dependency, lockfile, migration, provider, public Cron API, packaged JS, or PNG change.
</verification>

<success_criteria>
- The fixture seam is deterministic, idempotent, project/run isolated, authorization/recovery capable, and test-only.
- The launcher creates and drops only one validated exact test database and never touches ambient dev data or seeds.
- PHASE79_BROWSER_FIXTURE_SECRET reaches Docker by name only without logs/argv disclosure, while omission remains unset and fails before requests.
- Docker-based Playwright proves reset, actor, recovery, wrong-secret, and missing-secret behavior with no skip/fallback.
- No high-severity threat or production/package exposure remains.
</success_criteria>
