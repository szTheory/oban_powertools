---
phase: 79-page-migration-wave-1-overview-cron-limiters-audit
plan: 03
type: execute
wave: 3
depends_on:
  - 79-02
files_modified:
  - lib/oban_powertools/web/overview_read_model.ex
  - lib/oban_powertools/web/engine_overview_live.ex
  - test/oban_powertools/web/live/engine_overview_live_test.exs
autonomous: true
requirements:
  - PAGE-01
  - PAGE-10
  - COPY-01
  - A11Y-*
  - MOTION-*
must_haves:
  truths:
    - "Overview is rebuilt inside the existing ThemeShell/AppShell with one H1 and no duplicate shell, page-local component substitute, mutation, polling, feed, chart, or live-region behavior."
    - "The one semantic tree is ordered Current attention (Needs Review, Blocked, Waiting), Bridge-only Follow-up, Runnable, Resolved continuity and never reorders by count, time, severity, viewport, or client state."
    - "All domain counts, projection ranking, selectors, authorization, repository sources, and the three-exemplar bound are unchanged; only presentation order/copy and one bounded Audit support read change."
    - "Bridge count is explicitly representative, Runnable is compact current context, and Resolved continuity is calm historical evidence without a fabricated recent window."
    - "Every zero/nonzero/all-quiet state is truthful about available evidence and offers only an existing legal route."
  artifacts:
    - path: "lib/oban_powertools/web/overview_read_model.ex"
      provides: "Deterministic ordered Overview presentation input with one captured observation time"
    - path: "lib/oban_powertools/web/engine_overview_live.ex"
      provides: "Shared-component stable triage composition"
      exports: ["page_content/1"]
    - path: "test/oban_powertools/web/live/engine_overview_live_test.exs"
      provides: "Connected Overview behavior and confidentiality proof"
  key_links:
    - from: "lib/oban_powertools/web/overview_read_model.ex"
      to: "lib/oban_powertools/forensics/attention_projection.ex"
      via: "existing deterministic ranking and Enum.take(3) projection"
      pattern: "AttentionProjection"
    - from: "lib/oban_powertools/web/engine_overview_live.ex"
      to: "lib/oban_powertools/web/components/operator_patterns.ex"
      via: "truthful nonzero current attention composition"
      pattern: "attention_card"
    - from: "lib/oban_powertools/web/engine_overview_live.ex"
      to: "lib/oban_powertools/web/components/data_display.ex"
      via: "Runnable metric and explicit empty states"
      pattern: "metric_card|empty_state"
---

<objective>
Migrate Overview to a stable triage hierarchy built from the shipped page primitives while preserving every read-model fact, route, and authorization boundary.

Purpose: make the first operator scan calm and predictable: current attention, honest bridge handoff, routine capacity, then retained continuity.
Output: deterministic bounded read-model input, reusable production page composition, and green connected Overview tests.
</objective>

<execution_context>
@/Users/jon/.agents/gsd-core/workflows/execute-plan.md
@/Users/jon/.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/79-page-migration-wave-1-overview-cron-limiters-audit/79-CONTEXT.md
@.planning/phases/79-page-migration-wave-1-overview-cron-limiters-audit/79-RESEARCH.md
@.planning/phases/79-page-migration-wave-1-overview-cron-limiters-audit/79-PATTERNS.md
@.planning/phases/79-page-migration-wave-1-overview-cron-limiters-audit/79-UI-SPEC.md
@.planning/phases/79-page-migration-wave-1-overview-cron-limiters-audit/79-02-SUMMARY.md
@guides/brand-book.md
</context>

## Must Haves — Decision Coverage

- **D-09:** Lock Overview to Needs Review, Blocked, Waiting, Bridge-only Follow-up, Runnable, and Resolved continuity with deterministic three-item bucket bounds.
- **D-10:** Group the first three buckets beneath Current attention while preserving that DOM order through wide and narrow layouts.
- **D-11:** Place the neutral Oban Web-owned bridge handoff after current attention and before routine capacity.
- **D-12:** Label bridge counts as representative/shown rather than comparable global totals, without revealing projection mechanics.
- **D-13:** Render Runnable as compact current context and Resolved continuity last as calm historical evidence without a fabricated recent window.
- **D-14:** Use AttentionCard only when its facts are honest, keep exemplars as bounded direct rows, and use neutral/metric surfaces for bridge, continuity, and capacity.
- **D-15:** Keep compact bucket-specific zero states and truthful all-quiet copy without overstating monitoring completeness.
- **D-16:** Keep H1 Overview, the exact operator-focused introduction, static non-live semantics, existing selectors/auth/read models, and no polling, charts, feed, or configurable cards.

## Artifacts this phase produces

- New public Phoenix function component `ObanPowertools.Web.EngineOverviewLive.page_content/1`, used by both `render/1` and the later deterministic page-story host; it performs no query, authorization, mutation, current-time lookup, or URL parsing.
- New page-root/region IDs: `overview-page`, `overview-current-attention`, `overview-needs-review`, `overview-blocked`, `overview-waiting`, `overview-bridge-follow-up`, `overview-runnable`, and `overview-resolved-continuity`.
- `OverviewReadModel.build/1` gains optional `now:` input; it captures exactly one UTC value when omitted and emits `observed_at`/`observed_datetime` presentation inputs consistently.
- Bucket order becomes exact and explicit: `:needs_review`, `:blocked`, `:waiting`, `:bridge_follow_up`, `:runnable`, `:resolved_continuity`; visible labels remain the exact UI-SPEC strings.
- Bridge presentation gains `sample_count_label` with `{N} representative follow-ups`; resolved label changes from `Resolved Recently` to `Resolved continuity`.
- Existing selector paths, bucket counts, diagnoses, ownership/support fields, and exemplar maps remain; exemplar lists stay at most three.
- No page CSS or packaged asset is added in this plan; Plan 79-07 owns root-scoped composition CSS after all page DOMs stabilize.

<tasks>

<task type="auto" tdd="true">
  <name>Task 79-03-01: Make Overview input deterministic, bounded, and semantically ordered</name>
  <files>lib/oban_powertools/web/overview_read_model.ex, test/oban_powertools/web/live/engine_overview_live_test.exs</files>
  <read_first>
    - @lib/oban_powertools/web/overview_read_model.ex
    - @test/oban_powertools/web/live/engine_overview_live_test.exs
    - @lib/oban_powertools/forensics/attention_projection.ex
    - @lib/oban_powertools/audit.ex
    - @lib/oban_powertools/control_plane.ex
    - @lib/oban_powertools/web/control_plane_presenter.ex
    - @lib/oban_powertools/web/selectors.ex
    - @.planning/phases/79-page-migration-wave-1-overview-cron-limiters-audit/79-CONTEXT.md
    - @.planning/phases/79-page-migration-wave-1-overview-cron-limiters-audit/79-PATTERNS.md
  </read_first>
  <action>Capture `now = Keyword.get(opts, :now, DateTime.utc_now())` once at the beginning of `build/1` and thread it into cooldown/observation formatting so repeated fields cannot drift during one build. Replace the global `Audit.list_all/2` support read with page 1 of `Audit.page/2` already filtered to exact `event_type: "lifeline.repair_executed"`; retain the same latest matching repair evidence, resolved-support destination, and final three-exemplar projection. Add a regression fixture containing one older matching repair event plus more than 20 newer unrelated events and prove the matching event remains reachable and selected for Resolved continuity. Emit six bucket maps in the exact semantic order and add stable atom `id`/`kind` plus observation inputs expected by `present_overview_bucket/1`. Move Bridge before Runnable, rename only the unsupported visible label to `Resolved continuity`, use `Inspect in Oban Web`, and compute bridge wording from the bounded representative rows without claiming a total. Keep repository sources, counts, AttentionProjection ranking, selector destinations, and three-item bound unchanged. Replace label-string grouping in the LiveView tests with semantic id/order assertions.</action>
  <verify>
    <automated>mix test test/oban_powertools/web/live/engine_overview_live_test.exs --only phase79_slice:overview --seed 0</automated>
    <automated>mix format --check-formatted lib/oban_powertools/web/overview_read_model.ex test/oban_powertools/web/live/engine_overview_live_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - A fixed `now:` yields byte-equal presentation maps across repeat builds, and no helper calls `DateTime.utc_now/0` independently for presentation truth.
    - Returned semantic IDs/labels appear in exactly the approved six-item order and never derive from map iteration, count, timestamp, or severity.
    - Every exemplar list is `length &lt;= 3`, existing deterministic ranking tests remain green, and all current selector URLs decode to the same destinations.
    - Overview no longer calls `Audit.list_all/2`; its SQL scope is filtered to `lifeline.repair_executed` before the 20-row bound and then uses the existing final projection.
    - With more than 20 newer unrelated events ahead of an older matching repair event, Resolved continuity still reaches the same latest matching repair exemplar and Audit destination.
    - Bridge wording contains `representative follow-ups` or `shown`; no output contains `Resolved Recently`, `recently`, projection mechanics, or a global-total claim.
  </acceptance_criteria>
  <done>OverviewReadModel emits one deterministic, bounded, exact-order presentation input without changing domain meaning or destinations.</done>
</task>

<task type="auto">
  <name>Task 79-03-02: Compose the stable Overview page from shared primitives and groups</name>
  <files>lib/oban_powertools/web/engine_overview_live.ex, test/oban_powertools/web/live/engine_overview_live_test.exs</files>
  <read_first>
    - @lib/oban_powertools/web/engine_overview_live.ex
    - @test/oban_powertools/web/live/engine_overview_live_test.exs
    - @lib/oban_powertools/web/overview_read_model.ex
    - @lib/oban_powertools/web/control_plane_presenter.ex
    - @lib/oban_powertools/web/components/operator_patterns.ex
    - @lib/oban_powertools/web/components/data_display.ex
    - @lib/oban_powertools/web/components/primitives.ex
    - @lib/oban_powertools/web/theme_shell.ex
    - @.planning/phases/76-navigation-app-shell/76-UI-SPEC.md
    - @.planning/phases/77-data-display-operator-patterns/77-UI-SPEC.md
    - @.planning/phases/78-component-groups-meta-components/78-UI-SPEC.md
    - @.planning/phases/79-page-migration-wave-1-overview-cron-limiters-audit/79-UI-SPEC.md
  </read_first>
  <action>Extract public pure `page_content/1`; make `render/1` delegate to it with assigned normalized buckets. Render one `section#overview-page` under the existing shell, one H1 `Overview`, and exact intro copy. Render `Current attention` followed by direct Needs Review/Blocked/Waiting regions in DOM order. Use `attention_card/1` only for nonzero lanes whose presenter map supplies every required honest fact; otherwise use Primitives Surface/Card plus explicit compact `empty_state/1` copy. Render exemplars as direct separated list rows with explicit existing links, never nested cards. Render Bridge-only Follow-up as a neutral ownership-honest surface with representative count and `Inspect in Oban Web`, Runnable through `metric_card/1` or compact status, and Resolved continuity as the final neutral historical region. Remove raw Tailwind-like chrome, equal six-card grid, broad green fill, generic historical-attention subcards, label-dependent active/resolved helpers, and CTA class helpers. Do not add AppShell, role=alert/status, polling, JS events, mutations, charts, feeds, or duplicate responsive markup.</action>
  <verify>
    <automated>mix test test/oban_powertools/web/live/engine_overview_live_test.exs --seed 0</automated>
    <automated>mix test test/oban_powertools/web/components/operator_patterns_test.exs test/oban_powertools/web/components/data_display_test.exs --seed 0</automated>
    <automated>mix format --check-formatted lib/oban_powertools/web/engine_overview_live.ex test/oban_powertools/web/live/engine_overview_live_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - Rendered HTML has exactly one H1 and the eight documented semantic page/region IDs in approved DOM order.
    - `render/1` and `page_content/1` produce the same page tree for identical normalized assigns; `page_content/1` contains no repository/auth/current-time call.
    - Nonzero current attention uses the shared group only with all required facts; exemplar rows are direct children and no nested attention/card tree exists.
    - Static HTML contains no alert/live region, mutation event, polling/feed/chart copy, raw preview/internal fields, raw Tailwind utility chrome, or second shell.
    - All legacy auth/count/source/selector tests plus new quiet/nonzero/Unicode/order/confidentiality tests pass.
  </acceptance_criteria>
  <done>Overview renders a reusable one-tree stable triage hierarchy through shipped components with all existing behavior and routes preserved.</done>
</task>

</tasks>

<threat_model>
## ASVS L1 Threat Model — blocking threshold: high

| Threat ID | Category | Severity | Mitigation and verification |
|-----------|----------|----------|-----------------------------|
| T-79-03-AUTH | Access control | high | Existing page authorization remains the mount gate; Overview gains no action event or client-side authority. |
| T-79-03-LEAK | Data protection | high | Only normalized bucket/exemplar facts render; internal ranking, raw schemas, metadata, tokens, hashes, and errors are excluded and full-HTML tested. |
| T-79-03-DOS | Availability | high | Audit support retrieval and every exemplar list are bounded; no polling/feed, render-time query, or duplicated responsive tree is allowed. |
| T-79-03-TRUTH | Business logic | high | Representative bridge counts, no-recent-window continuity, and available-evidence empty copy prevent false completeness/current-state claims. |
| T-79-03-XSS | Injection | high | Exemplars render through HEEx/shared components with hostile Unicode/HTML tests and no raw HTML path. |
| T-79-03-A11Y | Accessibility | high | One H1, logical headings, named links, fixed DOM order, no static live noise, one reflowing tree, and later axe/browser gates block. |
</threat_model>

<verification>
- Run the full Overview LiveView suite and shared component regressions.
- Inspect the source for no AppShell instantiation, mutation/polling events, raw utility chrome, duplicated mobile tree, render-time repository call, or `Audit.list_all` support read.
- Confirm only the two production files and one test file changed in this parallel wave.
</verification>

<success_criteria>
- Overview is migrated to shared components with exact stable hierarchy and intro copy.
- Existing counts, sources, rankings, authorization, and destinations remain green.
- Bridge/Runnable/continuity and zero states are truthful and visually distinct by semantics, not alarm color.
- Page composition is reusable by the later story catalog and has one semantic tree.
- No high-severity threat remains open.
</success_criteria>
