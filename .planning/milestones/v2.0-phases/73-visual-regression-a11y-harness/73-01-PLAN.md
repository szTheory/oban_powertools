---
phase: 73-visual-regression-a11y-harness
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - .gitignore
  - scripts/showcase_manifest.exs
  - test/browser/support/manifest.ts
  - test/browser/support/manifest-smoke.mjs
autonomous: true
requirements:
  - VRT-01
  - VRT-03
  - A11Y-01
must_haves:
  truths:
    - "D-03: Root Node tooling is exact-versioned, lockfile-backed, and development/test-only."
    - "D-07: Browser targets are generated from ObanPowertools.ShowcaseCatalog rather than copied into JavaScript."
    - "D-10: Generated manifest data contains no timestamps, absolute paths, random IDs, or host-specific values."
  artifacts:
    - path: "package.json"
      provides: "Root npm scripts and exact Playwright/axe dev dependencies"
      contains: ["showcase:manifest", "visual:a11y", "visual:a11y:docker", "visual:a11y:host", "vrt:update"]
    - path: "scripts/showcase_manifest.exs"
      provides: "JSON manifest derived from ObanPowertools.ShowcaseCatalog"
      contains: ["schema_version", "themes", "viewports", "scenarios"]
    - path: "test/browser/support/manifest.ts"
      provides: "TypeScript manifest loader and schema validator for Playwright specs"
      exports: ["loadManifest", "themes", "viewports", "scenarios"]
    - path: "test/browser/support/manifest-smoke.mjs"
      provides: "Node smoke assertion for generated manifest shape and determinism"
  key_links:
    - from: "scripts/showcase_manifest.exs"
      to: "test/support/showcase_catalog.ex"
      via: "Mix loads ObanPowertools.ShowcaseCatalog and calls scenarios/0, snapshot_name/1, and a11y_target/1"
      pattern: "ObanPowertools\\.ShowcaseCatalog"
    - from: "test/browser/support/manifest.ts"
      to: "test/browser/.generated/showcase-manifest.json"
      via: "Playwright specs load only generated JSON targets"
      pattern: "showcase-manifest\\.json"
  prohibitions:
    - "D-07: Do not duplicate the nine scenario IDs as an authoritative JavaScript list."
    - "D-12: Do not introduce a SaaS visual-regression baseline store."
    - "D-03: Do not add Hex runtime dependencies for the browser harness."
---

<objective>
Create the root Node package foundation and generated showcase target manifest for Phase 73.

Purpose: Close the single-source target gap before browser specs exist, so VRT and axe consume the Phase 72 catalog instead of drifting from it.
Output: Exact npm dev tooling, committed lockfile, generated-manifest script, manifest loader, and manifest smoke check.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/73-visual-regression-a11y-harness/73-CONTEXT.md
@.planning/phases/73-visual-regression-a11y-harness/73-RESEARCH.md
@.planning/phases/73-visual-regression-a11y-harness/73-PATTERNS.md
@.planning/phases/73-visual-regression-a11y-harness/73-VALIDATION.md
@.planning/phases/72-stress-fixtures-showcase-skeleton/72-03-SUMMARY.md
@test/support/showcase_catalog.ex
@test/oban_powertools/showcase_catalog_test.exs
@mix.exs
</context>

## Artifacts This Phase Produces

- npm scripts: `showcase:manifest`, `visual:a11y`, `visual:a11y:host`, `visual:a11y:docker`, `vrt:update`.
- Shell scripts: `scripts/showcase_manifest.exs`, `scripts/playwright-docker.sh`, `scripts/with-showcase-server.sh`.
- Config: `playwright.config.ts` with Chromium projects `chromium-320`, `chromium-tablet`, `chromium-wide`.
- Browser support modules: `test/browser/support/manifest.ts`, `test/browser/support/manifest-smoke.mjs`, `test/browser/support/deterministic.ts`, `test/browser/support/showcase.ts`, `test/browser/support/axe.ts`.
- Browser specs: `test/browser/specs/showcase.structure.spec.ts`, `test/browser/specs/showcase.a11y.spec.ts`, `test/browser/specs/showcase.vrt.spec.ts`.
- Generated and committed outputs: `test/browser/.generated/showcase-manifest.json` during runs, `test/browser/__screenshots__/chromium-*/showcase/*/*.png` with 108 committed PNG baselines.
- CI and docs: `.github/workflows/ci.yml` job `visual_a11y`, `ci-gate` `VISUAL_A11Y` fan-in, `guides/visual-regression-and-a11y.md`, README guide link, docs contract assertions.

## Source Audit

| Source | ID | Feature/Requirement | Plan | Status |
|---|---|---|---|---|
| GOAL | - | Establish VRT plus axe-core gates before page work starts | 73-01, 73-02, 73-03, 73-04, 73-05 | COVERED |
| REQ | VRT-01 | External Node Playwright harness in pinned Docker across themes and viewports | 73-01, 73-02, 73-04, 73-05 | COVERED |
| REQ | VRT-02 | Committed baselines, CI diff failure, explicit reviewed updates | 73-04, 73-05 | COVERED |
| REQ | VRT-03 | Hermetic fixtures, disabled volatility, fixed fonts/media/viewport inputs | 73-01, 73-02, 73-04 | COVERED |
| REQ | A11Y-01 | axe-core WCAG A/AA plus supported WCAG 2.2 AA gate with 0 critical/serious | 73-01, 73-03, 73-05 | COVERED |
| RESEARCH | - | `@playwright/test@1.61.0`, `@axe-core/playwright@4.11.3`, `axe-core@4.11.4`, Docker image `mcr.microsoft.com/playwright:v1.61.0-noble` | 73-01, 73-02 | COVERED |
| RESEARCH | - | Manifest architecture generated from `ObanPowertools.ShowcaseCatalog` | 73-01 | COVERED |
| RESEARCH | - | Artifact upload for Playwright reports, test results, axe JSON, manifest | 73-03, 73-05 | COVERED |
| CONTEXT | D-01..D-04 | `visual_a11y` CI lane, merge blocking, pinned Docker, real example-host showcase route | 73-02, 73-05 | COVERED |
| CONTEXT | D-05..D-08 | Catalog story-only baselines, structure assertions, generated target source, full theme and viewport matrix | 73-01, 73-02, 73-04 | COVERED |
| CONTEXT | D-09..D-12 | Playwright screenshots, deterministic inputs, narrow masks, no SaaS baseline store | 73-02, 73-04 | COVERED |
| CONTEXT | D-13..D-16 | axe same targets/open states, WCAG tags, full output, narrow automated claim | 73-03, 73-05 | COVERED |
| CONTEXT | D-17..D-20 | CI never updates snapshots, failure artifacts, baseline rationale, narrow update command | 73-04, 73-05 | COVERED |

<tasks>

<task type="auto">
  <name>Task 1: Add exact Node tooling and generated-output ignores</name>
  <files>package.json, package-lock.json, .gitignore</files>
  <read_first>
@package.json
@package-lock.json
@.gitignore
@.planning/phases/73-visual-regression-a11y-harness/73-CONTEXT.md
@.planning/phases/73-visual-regression-a11y-harness/73-RESEARCH.md
@.planning/phases/73-visual-regression-a11y-harness/73-PATTERNS.md
  </read_first>
  <action>Replace the current empty root Node metadata with a private `oban_powertools` development tooling package per D-03. Install exact dev dependencies `@playwright/test@1.61.0`, `@axe-core/playwright@4.11.3`, and `axe-core@4.11.4` using npm so `package-lock.json` records the exact tree. Add scripts `showcase:manifest`, `visual:a11y`, `visual:a11y:host`, `visual:a11y:docker`, and `vrt:update`; `vrt:update` must call the pinned Docker path and default to `--update-snapshots=changed` per D-17 and D-20. Add ignored run outputs `/playwright-report/`, `/test-results/`, and `/test/browser/.generated/` to `.gitignore`; leave `test/browser/__screenshots__/` commit-eligible per D-09. Verify package metadata with npm registry output before install if the package-legitimacy seam is available; otherwise preserve the RESEARCH.md manual audit assumptions and exact pins.</action>
  <verify>
    <automated>node -e "const fs=require('fs'); const p=JSON.parse(fs.readFileSync('package.json','utf8')); const deps=p.devDependencies||{}; for (const [name, version] of Object.entries({'@playwright/test':'1.61.0','@axe-core/playwright':'4.11.3','axe-core':'4.11.4'})) { if (deps[name] !== version) throw new Error(name + ' must be pinned to ' + version); } for (const script of ['showcase:manifest','visual:a11y','visual:a11y:host','visual:a11y:docker','vrt:update']) { if (!p.scripts || !p.scripts[script]) throw new Error('missing script ' + script); }"</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` has `"private": true` and exact `devDependencies` values for the three approved npm packages.
    - `package-lock.json` exists and contains lockfile entries for `@playwright/test`, `@axe-core/playwright`, and `axe-core`.
    - `.gitignore` ignores `/playwright-report/`, `/test-results/`, and `/test/browser/.generated/`.
    - `.gitignore` does not ignore `test/browser/__screenshots__/`.
    - No Hex dependency is added in `mix.exs`.
  </acceptance_criteria>
  <done>Root Node tooling is lockfile-backed and scripts exist for the later manifest, Docker, VRT, and axe tasks.</done>
</task>

<task type="auto">
  <name>Task 2: Generate and validate the showcase manifest</name>
  <files>scripts/showcase_manifest.exs, test/browser/support/manifest.ts, test/browser/support/manifest-smoke.mjs</files>
  <read_first>
@scripts/showcase_manifest.exs
@test/browser/support/manifest.ts
@test/browser/support/manifest-smoke.mjs
@test/support/showcase_catalog.ex
@test/oban_powertools/showcase_catalog_test.exs
@.planning/phases/72-stress-fixtures-showcase-skeleton/72-03-SUMMARY.md
@.planning/phases/73-visual-regression-a11y-harness/73-PATTERNS.md
  </read_first>
  <action>Create `scripts/showcase_manifest.exs` so `MIX_ENV=test mix run --no-start scripts/showcase_manifest.exs` emits JSON derived from `ObanPowertools.ShowcaseCatalog.scenarios/0`, `snapshot_name/1`, and `a11y_target/1` per D-07. The JSON must contain `schema_version`, `themes` exactly `system`, `light`, `dark`, `high-contrast`, `viewports` exactly `320`, `tablet`, `wide` with fixed browser dimensions, and `scenarios` containing `id`, `domain`, `persona`, `states`, `story`, `snapshot`, and `a11y`. Keep the output ordered, deterministic, and free of timestamps or absolute paths per D-10. Create `test/browser/support/manifest.ts` with exported `loadManifest`, `themes`, `viewports`, and `scenarios` accessors that fail fast on malformed JSON or unknown theme/viewport/selector values. Create `test/browser/support/manifest-smoke.mjs` to validate the generated JSON without Playwright.</action>
  <verify>
    <automated>npm run showcase:manifest && node test/browser/support/manifest-smoke.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `npm run showcase:manifest` writes `test/browser/.generated/showcase-manifest.json`.
    - The generated manifest contains exactly 9 scenarios, 4 themes, and 3 viewports.
    - Every scenario `snapshot` starts with `showcase/` and every `a11y` selector starts with `[data-obpt-story=`.
    - The smoke script rejects missing required keys and exits 0 for the generated manifest.
    - No scenario ID list is hand-authored as the source of truth in TypeScript.
  </acceptance_criteria>
  <done>Manifest generation and validation are ready for Playwright specs without duplicating catalog authority.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry to repo lockfile | External package metadata enters committed development tooling. |
| Elixir catalog to generated JSON | Trusted test-support catalog is serialized for Node browser specs. |
| Generated JSON to Playwright specs | Browser tests consume manifest data before hitting the local showcase. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-73-01 | Tampering | `scripts/showcase_manifest.exs` and `test/browser/support/manifest.ts` | mitigate | Generate from `ObanPowertools.ShowcaseCatalog`; validate schema, themes, viewports, story selectors, and scenario count before tests run. |
| T-73-02 | Denial of Service | Manifest loader | mitigate | Fail fast on missing or malformed generated JSON with a specific command hint instead of letting Playwright produce cascading browser failures. |
| T-73-SC | Tampering | npm package installs | mitigate | Use exact package versions, commit `package-lock.json`, verify package metadata/no postinstall scripts when the seam is available, and do not add unreviewed packages. |

No high-severity ASVS Level 1 threat remains unmitigated in this plan.
</threat_model>

<verification>
Run `npm run showcase:manifest && node test/browser/support/manifest-smoke.mjs`.
</verification>

<success_criteria>
The root Node toolchain and manifest pipeline exist, are deterministic, and can be consumed by later Playwright specs without hand-copied catalog targets.
</success_criteria>

<output>
Create `.planning/phases/73-visual-regression-a11y-harness/73-01-SUMMARY.md` when done.
</output>
