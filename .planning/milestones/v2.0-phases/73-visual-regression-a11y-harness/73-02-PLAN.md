---
phase: 73-visual-regression-a11y-harness
plan: 02
type: execute
wave: 2
depends_on:
  - 73-01
files_modified:
  - playwright.config.ts
  - scripts/playwright-docker.sh
  - scripts/with-showcase-server.sh
  - test/browser/support/deterministic.ts
  - test/browser/support/showcase.ts
  - test/browser/styles/screenshot.css
  - test/browser/specs/showcase.structure.spec.ts
autonomous: true
requirements:
  - VRT-01
  - VRT-03
must_haves:
  truths:
    - "D-03: Browser execution runs through the official Playwright Docker image `mcr.microsoft.com/playwright:v1.61.0-noble`."
    - "D-04: The harness drives the real `examples/phoenix_host` `/ops/jobs/_showcase` route."
    - "D-06: Browser structure assertions cover `.obpt-root`, theme controls, viewport controls, section anchors, and all catalog story targets."
    - "D-08 D-10: System theme, media preferences, locale, timezone, fonts, and real browser viewports are deterministic."
  artifacts:
    - path: "playwright.config.ts"
      provides: "Deterministic Playwright Test config"
      exports: ["chromium-320", "chromium-tablet", "chromium-wide"]
    - path: "scripts/playwright-docker.sh"
      provides: "Pinned Docker runner for local and CI browser execution"
    - path: "scripts/with-showcase-server.sh"
      provides: "Example-host lifecycle wrapper for Playwright commands"
    - path: "test/browser/support/showcase.ts"
      provides: "Showcase navigation, theme, viewport, and readiness helpers"
      exports: ["prepareShowcase", "storyLocator", "assertShowcaseStructure"]
    - path: "test/browser/specs/showcase.structure.spec.ts"
      provides: "Browser-visible showcase contract smoke test"
  key_links:
    - from: "scripts/with-showcase-server.sh"
      to: "examples/phoenix_host"
      via: "starts the real Phoenix host and waits for `/ops/jobs/_showcase`"
      pattern: "ops/jobs/_showcase"
    - from: "test/browser/support/showcase.ts"
      to: "assets/oban_powertools/theme.js"
      via: "uses `window.ObanPowertoolsTheme.setTheme` when available"
      pattern: "ObanPowertoolsTheme"
    - from: "test/browser/specs/showcase.structure.spec.ts"
      to: "test/browser/.generated/showcase-manifest.json"
      via: "asserts every manifest scenario selector renders"
      pattern: "loadManifest"
  prohibitions:
    - "D-04: Do not use a synthetic static HTML page as the browser test target."
    - "D-10: Do not rely on host OS/browser/font parity for deterministic screenshots."
    - "D-11: Do not hide story content, status tones, focus states, long identifiers, RTL or non-ASCII text, redaction displays, or severity signals."
---

<objective>
Create the deterministic Playwright runtime, Docker runner, example-host wrapper, and browser-level showcase structure smoke.

Purpose: Prove the harness can reach the real Phase 72 showcase through the same host route later VRT and axe checks will scan.
Output: Playwright config, deterministic helpers, pinned Docker script, showcase server script, screenshot CSS, and structure spec.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/73-visual-regression-a11y-harness/73-CONTEXT.md
@.planning/phases/73-visual-regression-a11y-harness/73-RESEARCH.md
@.planning/phases/73-visual-regression-a11y-harness/73-PATTERNS.md
@.planning/phases/73-visual-regression-a11y-harness/73-VALIDATION.md
@.planning/phases/71-token-layer-isolated-theming-engine/71-04-SUMMARY.md
@.planning/phases/72-stress-fixtures-showcase-skeleton/72-04-SUMMARY.md
@lib/oban_powertools/web/theme_shell.ex
@lib/oban_powertools/web/dev/showcase_live.ex
@assets/oban_powertools/theme.js
@examples/phoenix_host/config/dev.exs
@examples/phoenix_host/lib/phoenix_host_web/router.ex
</context>

## Artifacts This Phase Produces

- npm scripts: `showcase:manifest`, `visual:a11y`, `visual:a11y:host`, `visual:a11y:docker`, `vrt:update`.
- Shell scripts: `scripts/showcase_manifest.exs`, `scripts/playwright-docker.sh`, `scripts/with-showcase-server.sh`.
- Config: `playwright.config.ts` with Chromium projects `chromium-320`, `chromium-tablet`, `chromium-wide`.
- Browser support modules: `test/browser/support/manifest.ts`, `test/browser/support/manifest-smoke.mjs`, `test/browser/support/deterministic.ts`, `test/browser/support/showcase.ts`, `test/browser/support/axe.ts`.
- Browser specs: `test/browser/specs/showcase.structure.spec.ts`, `test/browser/specs/showcase.a11y.spec.ts`, `test/browser/specs/showcase.vrt.spec.ts`.
- Generated and committed outputs: `test/browser/.generated/showcase-manifest.json` during runs, `test/browser/__screenshots__/chromium-*/showcase/*/*.png` with 108 committed PNG baselines.
- CI and docs: `.github/workflows/ci.yml` job `visual_a11y`, `ci-gate` `VISUAL_A11Y` fan-in, `guides/visual-regression-and-a11y.md`, README guide link, docs contract assertions.

<tasks>

<task type="auto">
  <name>Task 1: Configure deterministic Playwright projects</name>
  <files>playwright.config.ts, test/browser/support/deterministic.ts, test/browser/styles/screenshot.css</files>
  <read_first>
@playwright.config.ts
@test/browser/support/deterministic.ts
@test/browser/styles/screenshot.css
@package.json
@.planning/phases/73-visual-regression-a11y-harness/73-RESEARCH.md
@.planning/phases/73-visual-regression-a11y-harness/73-UI-SPEC.md
@assets/oban_powertools/tokens.css
  </read_first>
  <action>Create `playwright.config.ts` for `test/browser/specs` with projects `chromium-320`, `chromium-tablet`, and `chromium-wide`; each project must use Chromium, `deviceScaleFactor: 1`, `timezoneId: "UTC"`, `locale: "en-US"`, `colorScheme: "light"`, `reducedMotion: "reduce"`, and browser viewports `320x900`, `768x1000`, and `1440x1000` per D-08 and D-10. Configure output/report paths under `test-results/` and `playwright-report/`, workers `1` in CI, and `snapshotPathTemplate` grouping expected images under `test/browser/__screenshots__/{projectName}/{arg}{ext}` per D-09. Treat `{arg}` as the complete relative snapshot path supplied by the VRT spec; because manifest `scenario.snapshot` starts with `showcase/`, the config must not add a second `showcase/` prefix. Create `test/browser/support/deterministic.ts` with exported theme and viewport constants sourced from the manifest contract. Create scoped `test/browser/styles/screenshot.css` that neutralizes animation/caret volatility without suppressing story text, status colors, focus indicators, long machine values, RTL/non-ASCII content, redaction displays, or severity signals per D-11.</action>
  <verify>
    <automated>node -e "const fs=require('fs'); const c=fs.readFileSync('playwright.config.ts','utf8'); for (const token of ['chromium-320','chromium-tablet','chromium-wide','snapshotPathTemplate','timezoneId','UTC','locale','en-US','reducedMotion','reduce']) { if (!c.includes(token)) throw new Error('missing ' + token); }"</automated>
  </verify>
  <acceptance_criteria>
    - `playwright.config.ts` defines exactly the three Chromium viewport projects named above.
    - `playwright.config.ts` fixes timezone, locale, media preferences, and device scale factor.
    - Snapshot paths are under `test/browser/__screenshots__/` and derive from manifest `scenario.snapshot` plus theme exactly once, so the `showcase/` prefix is never repeated.
    - `test/browser/styles/screenshot.css` contains volatility controls only; it does not hide `.obpt-showcase-story` content or semantic tone attributes.
  </acceptance_criteria>
  <done>Playwright has a deterministic runtime configuration ready for structure, VRT, and axe specs.</done>
</task>

<task type="auto">
  <name>Task 2: Add pinned Docker and showcase server wrappers</name>
  <files>scripts/playwright-docker.sh, scripts/with-showcase-server.sh</files>
  <read_first>
@scripts/playwright-docker.sh
@scripts/with-showcase-server.sh
@examples/phoenix_host/config/dev.exs
@examples/phoenix_host/lib/phoenix_host_web/router.ex
@.planning/phases/73-visual-regression-a11y-harness/73-RESEARCH.md
@.planning/phases/73-visual-regression-a11y-harness/73-PATTERNS.md
  </read_first>
  <action>Create both Bash wrappers with `#!/usr/bin/env bash` and `set -euo pipefail`. `scripts/playwright-docker.sh` must run the caller-provided command inside `mcr.microsoft.com/playwright:v1.61.0-noble` per D-03, mount the repo read-write for reports and baselines, pass through `PLAYWRIGHT_TEST_BASE_URL`, `CI`, and npm environment needed by Playwright, and preserve all args for narrow `--grep` and `--project` runs. `scripts/with-showcase-server.sh` must start the real `examples/phoenix_host` dev server, wait until `/ops/jobs/_showcase` responds, export the base URL for host and Docker networking, run the provided command, and trap exits to stop the server per D-04. Handle Linux CI host networking and macOS Docker Desktop host-name differences without introducing a static page fallback.</action>
  <verify>
    <automated>bash -n scripts/playwright-docker.sh scripts/with-showcase-server.sh && test -x scripts/playwright-docker.sh && test -x scripts/with-showcase-server.sh</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/playwright-docker.sh` contains the exact image tag `mcr.microsoft.com/playwright:v1.61.0-noble`.
    - `scripts/with-showcase-server.sh` waits on `/ops/jobs/_showcase`, not `/` or a synthetic asset.
    - Both scripts preserve arbitrary command arguments after the script name.
    - Both scripts clean up child processes or containers on failure.
  </acceptance_criteria>
  <done>Local and CI browser commands can share the same pinned Docker path and real showcase host lifecycle.</done>
</task>

<task type="auto">
  <name>Task 3: Add showcase browser helpers and structure smoke spec</name>
  <files>test/browser/support/showcase.ts, test/browser/specs/showcase.structure.spec.ts</files>
  <read_first>
@test/browser/support/showcase.ts
@test/browser/specs/showcase.structure.spec.ts
@test/browser/support/manifest.ts
@lib/oban_powertools/web/dev/showcase_live.ex
@lib/oban_powertools/web/theme_shell.ex
@test/oban_powertools/web/live/showcase_live_test.exs
@assets/oban_powertools/theme.js
  </read_first>
  <action>Create `test/browser/support/showcase.ts` with exported helpers `prepareShowcase`, `storyLocator`, and `assertShowcaseStructure`. `prepareShowcase` must navigate to `/ops/jobs/_showcase`, assert one `.obpt-root`, set theme through `window.ObanPowertoolsTheme.setTheme(theme)` when available or the `[data-obpt-theme-choice]` control otherwise, set the matching `[data-obpt-viewport]` control after the real browser viewport is active, wait for `document.fonts.ready`, and assert deterministic `system` effective theme under Playwright media per D-08 and D-10. Create `test/browser/specs/showcase.structure.spec.ts` to assert D-06 browser-visible structure: one `.obpt-root`, md5 CSS and JS asset URLs, all four theme choices, all three viewport choices, all section anchors, and every manifest story selector. This spec must not create screenshot baselines.</action>
  <verify>
    <automated>npm run visual:a11y -- test/browser/specs/showcase.structure.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - Structure spec passes through `npm run visual:a11y`, which uses the Docker and showcase-server wrappers.
    - The spec reads the generated manifest and asserts all 9 story targets render.
    - The spec asserts route assets match `/ops/jobs/_assets/oban_powertools-[a-f0-9]{32}.css` and `.js`.
    - The spec does not call `toHaveScreenshot`.
  </acceptance_criteria>
  <done>The browser harness proves it can drive the real showcase route deterministically before VRT and axe checks are added.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Docker container to local repo | Browser runner executes with mounted working tree for reports and baselines. |
| Playwright browser to example host | Browser automation reaches the local Phoenix host route. |
| Showcase theme API to harness | Harness calls window-scoped theme helpers and stable controls. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-73-03 | Spoofing | `PLAYWRIGHT_TEST_BASE_URL` and server wrapper | mitigate | Wait for the real `/ops/jobs/_showcase` route and assert `.obpt-root` plus md5 assets before any scan/capture. |
| T-73-04 | Information Disclosure | Docker runner and report outputs | mitigate | Mount only the repo, upload only later allowlisted report paths, and run against trusted local routes. |
| T-73-SC | Tampering | npm package installs | mitigate | Continue using the lockfile and exact versions from Plan 73-01; do not add new packages in this plan. |

No high-severity ASVS Level 1 threat remains unmitigated in this plan.
</threat_model>

<verification>
Run `npm run visual:a11y -- test/browser/specs/showcase.structure.spec.ts`.
</verification>

<success_criteria>
The structure smoke passes in the pinned browser path against the real example-host showcase and proves deterministic theme, viewport, and readiness controls.
</success_criteria>

<output>
Create `.planning/phases/73-visual-regression-a11y-harness/73-02-SUMMARY.md` when done.
</output>
