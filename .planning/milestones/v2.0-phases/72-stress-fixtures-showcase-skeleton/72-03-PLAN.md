---
phase: 72-stress-fixtures-showcase-skeleton
plan: 03
type: execute
wave: 2
depends_on:
  - 72-01
files_modified:
  - test/support/showcase_catalog.ex
autonomous: true
requirements:
  - FIX-01
  - FIX-02
  - FIX-03
must_haves:
  truths:
    - "D-01: `ObanPowertools.ShowcaseCatalog` is domain-first across overview, jobs, batches, workflows, cron, limiters, lifeline, audit, and forensics."
    - "D-02 D-03: Scenario IDs and test targets are stable public contracts covering required adversarial states and persona/JTBDs."
    - "D-04 D-05: Screenshot-facing fixtures are plain constant maps/structs in the canonical dev/test-only support file."
  artifacts:
    - path: "test/support/showcase_catalog.ex"
      provides: "Canonical deterministic fixture catalog"
      exports: ["ObanPowertools.ShowcaseCatalog", "domains/0", "required_state_tags/0", "required_personas/0", "scenarios/0", "scenarios_by_domain/0", "scenario!/1", "snapshot_name/1", "a11y_target/1"]
  key_links:
    - from: "test/support/showcase_catalog.ex"
      to: "test/oban_powertools/showcase_catalog_test.exs"
      via: "implements the RED public API contract from Plan 72-01"
      pattern: "def scenarios"
    - from: "test/support/showcase_catalog.ex"
      to: "lib/oban_powertools/web/dev/showcase_live.ex"
      via: "Plan 72-04 loads the catalog without duplicating scenario data"
      pattern: "ShowcaseCatalog"
---

<objective>
Implement the canonical deterministic scenario catalog for Phase 72.

Purpose: Satisfy FIX-01, FIX-02, and FIX-03 by creating the single source of truth for showcase stories, ExUnit assertions, VRT snapshot names, and a11y targets.
Output: `test/support/showcase_catalog.ex` with stable domains, scenarios, metadata, fixtures, and target helpers.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/72-stress-fixtures-showcase-skeleton/72-CONTEXT.md
@.planning/phases/72-stress-fixtures-showcase-skeleton/72-RESEARCH.md
@.planning/phases/72-stress-fixtures-showcase-skeleton/72-VALIDATION.md
@test/oban_powertools/showcase_catalog_test.exs
@test/support/workflow_fixtures.ex
</context>

## Artifacts This Phase Produces

- Modules: `ObanPowertools.ShowcaseCatalog`, `ObanPowertools.Web.Dev.ShowcaseLive`.
- Public catalog functions: `domains/0`, `required_state_tags/0`, `required_personas/0`, `scenarios/0`, `scenarios_by_domain/0`, `scenario!/1`, `snapshot_name/1`, `a11y_target/1`.
- Public route: `/ops/jobs/_showcase`, LiveView action `:index`, mounted inside `:oban_powertools_native`.
- Stable route module: `ObanPowertools.Web.Dev.ShowcaseLive` with `mount/3`, `handle_event/3`, and `render/1`.
- Stable attributes: `data-obpt-showcase`, `data-obpt-section`, `data-obpt-theme-choice`, `data-obpt-viewport`, `data-obpt-story`, `data-obpt-domain`, `data-obpt-persona`, `data-obpt-state`, `data-obpt-fixture-index`.
- Theme choices: `system`, `light`, `dark`, `high-contrast`.
- Viewport choices: `320`, `tablet`, `wide`.
- Catalog domains: `overview`, `jobs`, `batches`, `workflows`, `cron`, `limiters`, `lifeline`, `audit`, `forensics`.
- Persona/JTBD categories: `triage`, `incident_response`, `repair`, `audit_review`.
- Required state tags: `empty`, `one`, `many`, `long_id`, `long_module`, `long_url`, `non_ascii`, `emoji`, `rtl`, `high_count`, `mixed_severity`, `permission_denied`, `stale`, `disconnected`, `boundary_pagination`.
- Initial scenario IDs: `overview-operational-empty`, `jobs-long-identifiers-many`, `batches-high-count-mixed-severity`, `workflows-stale-disconnected-dag`, `cron-permission-denied`, `limiters-boundary-pagination`, `lifeline-repair-preview`, `audit-non-ascii-rtl`, `forensics-long-url-stacktrace`.
- Test modules: `ObanPowertools.ShowcaseCatalogTest`, `ObanPowertools.Web.Dev.ShowcaseLiveTest`, extended `ObanPowertools.Web.RouterTest`, extended `ObanPowertools.HexReleaseTest`, extended `PhoenixHostWeb.ObanPowertoolsThemeIsolationTest`.
- CSS selectors/classes added for showcase shell inspection: `.obpt-showcase`, `.obpt-showcase-controls`, `.obpt-showcase-canvas`, `.obpt-showcase-story`, `.obpt-showcase-section`, `.obpt-showcase-fixture-index`.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create catalog module and public API</name>
  <files>test/support/showcase_catalog.ex</files>
  <read_first>
@test/oban_powertools/showcase_catalog_test.exs
@mix.exs
@.planning/phases/72-stress-fixtures-showcase-skeleton/72-CONTEXT.md
@.planning/phases/72-stress-fixtures-showcase-skeleton/72-RESEARCH.md
  </read_first>
  <behavior>
    - `ObanPowertools.ShowcaseCatalog.domains/0` returns the stable domain list.
    - `required_state_tags/0` and `required_personas/0` return the exact coverage values tested in Plan 72-01.
    - `scenarios/0` returns deterministic data with stable scenario IDs and required metadata.
    - `scenarios_by_domain/0` groups scenarios by domain without changing scenario data.
    - `scenario!/1` returns one scenario by ID and raises `ArgumentError` for unknown IDs.
  </behavior>
  <action>Create `test/support/showcase_catalog.ex` defining `ObanPowertools.ShowcaseCatalog` as the canonical catalog module compiled by the existing test support path. Implement the public API named in the behavior block using module attributes and pure functions. Use atoms or strings consistently with the Plan 72-01 tests. Do not introduce DB setup, application environment reads, random generation, wall-clock values, or dependency calls.</action>
  <verify>
    <automated>mix test test/oban_powertools/showcase_catalog_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `test/support/showcase_catalog.ex` exists and defines only deterministic module attributes/functions.
    - `ObanPowertools.ShowcaseCatalog.scenarios/0` returns equal values across repeated calls in the same VM.
    - Unknown scenario lookup raises a bounded `ArgumentError` naming the missing ID.
  </acceptance_criteria>
  <done>Public catalog API satisfies the Plan 72-01 shape tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Populate domain-first scenarios and targets</name>
  <files>test/support/showcase_catalog.ex</files>
  <read_first>
@test/support/showcase_catalog.ex
@test/oban_powertools/showcase_catalog_test.exs
@guides/brand-book.md
@.planning/REQUIREMENTS.md
  </read_first>
  <behavior>
    - Each required domain has at least one scenario with stable metadata and fixture data.
    - Aggregate scenario states include every required state tag.
    - Aggregate scenario personas/JTBDs cover triage, incident response, repair, and audit review.
    - Each scenario has `test_targets` for a DOM story ID, VRT snapshot name, and a11y selector.
  </behavior>
  <action>Populate the catalog with the nine initial scenario IDs listed in this plan's artifact section. Each scenario must include `id`, `domain`, `name`, `persona`, `jtbd`, `states`, `fixtures`, and `test_targets`; fixture values should include realistic operational examples such as long IDs, long module names, long URLs, non-ASCII/emoji/RTL text, high counts, mixed severities, permission-denied details, stale/disconnected flags, and boundary pagination metadata. Keep visible fixture values constant and escaped by normal rendering later; do not use raw HTML fragments.</action>
  <verify>
    <automated>mix test test/oban_powertools/showcase_catalog_test.exs</automated>
    <automated>! rg 'DateTime\\.utc_now|NaiveDateTime\\.utc_now|System\\.system_time|Ecto\\.UUID\\.generate|Enum\\.random|:rand|Faker' test/support/showcase_catalog.ex</automated>
  </verify>
  <acceptance_criteria>
    - Targeted catalog tests pass.
    - The grep command returns no matches.
    - No fixture in `scenarios/0` requires inserting database rows to be visible in the showcase.
    - Scenario IDs exactly match the public IDs listed in this plan unless the test contract was updated in Plan 72-01 for a concrete coverage reason.
  </acceptance_criteria>
  <done>Catalog content satisfies D-01 through D-05 and FIX-01 through FIX-03.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Fixture catalog -> LiveView render | Constant data will later be rendered into HTML by the showcase. |
| Fixture catalog -> VRT/a11y harness | Scenario IDs become durable external test target names. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-72-08 | Tampering | `test/support/showcase_catalog.ex` | mitigate | Use immutable module attributes and tests for deterministic repeated output. |
| T-72-09 | Information Disclosure | Fixture values | mitigate | Keep fixtures synthetic constants with no host environment reads and no DB records. |
| T-72-10 | Repudiation | Scenario IDs | mitigate | Treat IDs, snapshot names, and a11y selectors as public contracts tested before and after implementation. |
| T-72-11 | Elevation of Privilege | DB helpers | accept | Phase 72 catalog is plain data by default; no DB helper is introduced in this plan. |
| T-72-SC | Tampering | npm/pip/cargo installs | accept | Phase 72 installs no packages; RESEARCH.md Package Legitimacy Audit lists `none`. |
</threat_model>

<verification>
Targeted:
- `mix test test/oban_powertools/showcase_catalog_test.exs`
- `! rg 'DateTime\\.utc_now|NaiveDateTime\\.utc_now|System\\.system_time|Ecto\\.UUID\\.generate|Enum\\.random|:rand|Faker' test/support/showcase_catalog.ex`

Wave-level:
- `mix test test/oban_powertools/showcase_catalog_test.exs test/oban_powertools/hex_release_test.exs`
</verification>

<success_criteria>
The canonical catalog exists at `test/support/showcase_catalog.ex`, passes all catalog contract tests, contains complete deterministic coverage, and remains package-excluded by Plan 72-01's package tests.
</success_criteria>

<output>
Create `.planning/phases/72-stress-fixtures-showcase-skeleton/72-03-SUMMARY.md` when done.
</output>
