# Requirements Archive: Oban Powertools — v1.6 Release & Operability

**Archived:** 2026-05-30
**Milestone Status:** ✅ SHIPPED
**Original Defined:** 2026-05-28
**Core Value:** Ecto-native operational safety with explicit, inspectable behavior for developers and operators, delivered through a native `/ops/jobs` shell with honest host-ownership and support-truth boundaries.

**Milestone goal:** Make Oban Powertools real for adopters — publish it to hex and ship the two named operability footguns — before adding any more capability. The release IS the milestone; the tooling ships adopter trust, not raw capability. Zero new runtime dependencies.

---

## v1.6 Requirements — Final Status

### Release & Packaging

- [x] **REL-01**: Library publishes to hex.pm at `0.5.0` with a strict `:files` whitelist that ships `priv/` migration generators and excludes `.planning/`, tests, and dev cruft.
  - **Outcome:** Satisfied. 0.5.0 live on hex.pm. `:files` whitelist strict. VALIDATION.md: 35 green release-contract tests. No VERIFICATION.md (process gap — external evidence strong).
- [x] **REL-02**: ExDoc API documentation builds with `source_ref` pinned to the release tag and renders correctly on hexdocs (guides included as `extras`).
  - **Outcome:** Satisfied. hexdocs renders. `source_ref: "v#{@version}"` dynamically correct. No VERIFICATION.md (process gap).
- [x] **REL-03**: `CHANGELOG.md` (Keep a Changelog format) documents the `0.5.0` release and the explicit, documented path to `1.0` after real adopter feedback.
  - **Outcome:** Satisfied. CHANGELOG.md with 0.5.0 entry and path-to-1.0 section. `[Unreleased]` section populated with doctor/limiter/telemetry entries for 0.5.1 release-please PR.
- [x] **REL-04**: The getting-started quickstart is verified working from the **published** package — a fresh host installs from hex and reaches a first successful operator session (not just in-repo).
  - **Outcome:** Partially satisfied (static fix in place, live CI gate pending). Phase 51 scaffolded `examples/hex_consumer/` and first-session test proved green via path-dep swap. Phase 52.1 fixed the Igniter committed-modules blocker in `verify-published` CI. Live CI E2E gate is explicitly manual-only — requires a real release cycle. Fix is in place.

### Operability — Doctor

- [x] **OPS-03**: Operator can run `mix oban_powertools.doctor` to check Oban index presence and validity, including `INVALID` indexes left by a failed `CREATE INDEX CONCURRENTLY`, fully read-only over `pg_catalog`.
  - **Outcome:** Satisfied. Phase 48. Verified 13/13 in-repo. CI host-contract lane green. Not in published 0.5.0 (awaiting 0.5.1).
- [x] **OPS-04**: `mix oban_powertools.doctor` detects migration drift and validates configuration — honoring a custom Oban prefix/schema — and flags uniqueness-timeout risk.
  - **Outcome:** Satisfied. Phase 48. Five read-only checks: index validity, missing indexes, Oban migration version drift, Powertools tables, uniqueness-timeout risk.
- [x] **OPS-05**: `mix oban_powertools.doctor` returns honest exit codes (`0` ok / `1` warnings / `2` errors) suitable for CI, with actionable remediation hints in its output.
  - **Outcome:** Satisfied. Phase 48. `exit_code_for/1` with System.halt. Human + JSON (`schema_version: 1`) output. Remediation hints included.

### Operability — Limiter CLI

- [x] **OPS-06**: Operator can run `mix oban_powertools.limiter.explain` to explain a limiter's current blocking state, reusing the existing `Explain` API rather than duplicating limiter logic.
  - **Outcome:** Satisfied. Phase 49. Verified 18/18 in-repo. Not in published 0.5.0 (awaiting 0.5.1).
- [x] **OPS-07**: Operator can run `mix oban_powertools.limiter.simulate` to preview limiter behavior for a given config without mutating any real limiter state.
  - **Outcome:** Satisfied. Phase 49. `compute_reservation/4` pure extraction. Verified 18/18 in-repo.
- [x] **OPS-08**: The limiter CLI ships the rate-limit glossary in its help/documentation output.
  - **Outcome:** Satisfied. Phase 49. `ObanPowertools.Glossary` module; term-parity locked by `docs_contract_test.exs`.

### Telemetry & SLOs

- [x] **TEL-01**: Host can call `ObanPowertools.Telemetry.metrics/0` to obtain `Telemetry.Metrics` definitions over the frozen low-cardinality contract — opt-in and reporter-agnostic.
  - **Outcome:** Satisfied. Phase 50. 17 counter definitions. Verified 9/9 in-repo. Not in published 0.5.0 (awaiting 0.5.1).
- [x] **TEL-02**: `telemetry_metrics` and `telemetry_poller` are optional dependencies gated like the existing `oban_web` integration — no runtime cost or failure when absent.
  - **Outcome:** Satisfied. Phase 50. Optional deps. `Code.ensure_loaded?` guard. Prod-tree compile proof.
- [x] **TEL-03**: A Parapet/SLO telemetry guide documents golden-signals/SLO setup over the metrics surface, with no `oban_met` dependency.
  - **Outcome:** Satisfied. Phase 50. `guides/telemetry-and-slos.md` in Operations group. No `oban_met` framing explicit.

---

## Traceability Table — Final Status

| Requirement | Phase | Status |
|-------------|-------|--------|
| REL-01 | Phase 47 | ✅ Complete (no VERIFICATION.md — process gap) |
| REL-02 | Phase 47 | ✅ Complete (no VERIFICATION.md — process gap) |
| REL-03 | Phase 47 | ✅ Complete (0.5.1 PR pending merge) |
| REL-04 | Phase 51 + 52.1 | ✅ Complete (live CI E2E = next release) |
| OPS-03 | Phase 48 | ✅ Complete |
| OPS-04 | Phase 48 | ✅ Complete |
| OPS-05 | Phase 48 | ✅ Complete |
| OPS-06 | Phase 49 | ✅ Complete |
| OPS-07 | Phase 49 | ✅ Complete |
| OPS-08 | Phase 49 | ✅ Complete |
| TEL-01 | Phase 50 | ✅ Complete |
| TEL-02 | Phase 50 | ✅ Complete |
| TEL-03 | Phase 50 | ✅ Complete |

**Coverage:** 13/13 v1.6 requirements satisfied (REL-04 with one live-CI gate deferred to next release).

---

## Future Requirements (carried to v1.7+)

### Worker Lifecycle & Safety (→ v1.7)

- **WRK-04**: Observe-only worker hooks (`on_start`/`success`/`failure`/`discard`), crash-caught, never changing job outcome.
- **WRK-05**: Soft `deadline:` (pre-run cancel) + `timeout:` pass-through to Oban's own `timeout/1`.
- **WRK-06**: Output recording by generalizing `Workflow.Result` into a shared recordings table.
- **WRK-07**: `redact:` at-persist field drop via the existing `Redactor` seam.

### Batches & Composition (→ v1.8)

- **BAT-01**: Dedicated `batches`/`batch_jobs` tables with `completed` + `exhausted` callbacks via the generalized callback outbox.
- **BAT-02**: Native Batches page with Lifeline-routed bulk-retry of failed members.

### Observability / Live Counts (→ v1.9)

- **QRY-06**: Real-time job/queue counts with `oban_met` as an *optional* read source (never a hard dep, never a metric label).

### Native Job-Surface Polish (opportunistic)

- **QRY-05**: Args/meta filter on the native job surface.
- **QRY-07**: Lifeline→job deep-link.
- **QRY-08**: Cross-page select-all.
- **API-03**: Programmatic job query (`Operator.list/2`).

---

*Requirements archived: 2026-05-30 at v1.6 milestone close*
