---
phase: 49-limiter-explain-simulate-cli
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/oban_powertools/limits.ex
  - lib/oban_powertools/limits/glossary.ex
  - guides/limits-and-explain.md
  - test/oban_powertools/limits_test.exs
  - test/oban_powertools/limits/glossary_test.exs
autonomous: true
requirements: [OPS-07, OPS-08]

must_haves:
  truths:
    - "Limits.compute_reservation/4 returns a verdict tuple with zero side effects (no DB, no telemetry, no history)"
    - "Limits.reserve/3 behavior is provably unchanged after the extraction (existing limits_test.exs passes)"
    - "A single shared glossary string exists, covering all required rate-limit terms"
    - "guides/limits-and-explain.md contains the full glossary text"
  artifacts:
    - path: "lib/oban_powertools/limits.ex"
      provides: "Public pure compute_reservation/4 + refactored attempt_reservation/5"
      contains: "def compute_reservation("
    - path: "lib/oban_powertools/limits/glossary.ex"
      provides: "Single-source glossary string for both Mix tasks and the guide"
      contains: "def text"
    - path: "guides/limits-and-explain.md"
      provides: "Glossary section sourced from the shared string"
      contains: "token_bucket"
  key_links:
    - from: "lib/oban_powertools/limits.ex attempt_reservation/5"
      to: "Limits.compute_reservation/4"
      via: "case on verdict tuple"
      pattern: "compute_reservation\\("
    - from: "guides/limits-and-explain.md"
      to: "lib/oban_powertools/limits/glossary.ex"
      via: "verbatim glossary terms shared via contract test"
      pattern: "bucket_capacity"
---

<objective>
Extract the side-effect-free token-bucket decision logic from `ObanPowertools.Limits.attempt_reservation/5` into a public pure function `compute_reservation/4` (D-06), and establish the single-source rate-limit glossary string (D-08). This is the riskiest task in the phase and the foundation that `simulate` (Plan 49-03) depends on.

Purpose: Satisfy "reuse, don't duplicate" (Success Criterion #4) and "no mutation" (Success Criterion #2) at once via the pure core. Establish the no-drift glossary source (OPS-08) consumed by both new Mix tasks and the existing guide.
Output: Refactored `limits.ex` with `compute_reservation/4`, a new `ObanPowertools.Limits.Glossary` module, an updated guide, and regression-proof tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/49-limiter-explain-simulate-cli/49-CONTEXT.md
@.planning/phases/49-limiter-explain-simulate-cli/49-RESEARCH.md
@.planning/phases/49-limiter-explain-simulate-cli/49-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extract pure compute_reservation/4 from attempt_reservation/5</name>
  <files>lib/oban_powertools/limits.ex, test/oban_powertools/limits_test.exs</files>
  <read_first>
    - lib/oban_powertools/limits.ex (current state — focus on `attempt_reservation/5` lines ~210-247, `normalize_bucket/3` lines ~249-257, `cooldown_active?/2` lines ~259-262, `cooldown_blocker/2`, `limit_blocker/3`, `max_datetime/2`, and `blocked/4` lines ~296-321 to see exactly which side effects must stay out of the pure path)
    - test/oban_powertools/limits_test.exs (the regression suite — every existing test MUST still pass unchanged after the refactor)
  </read_first>
  <behavior>
    - compute_reservation/4 with a fresh empty State (tokens_used: 0) and capacity 5, weight 1 returns {:reserved, 1}
    - Repeated reservations accumulate: from a State with tokens_used: 4, capacity 5, weight 1 returns {:reserved, 5}; from tokens_used: 5 with weight 1 returns {:blocked, "limit_reached", retry_at, %{capacity: 5, used: 5}} where retry_at == bucket_started_at + bucket_span_ms (clamped to >= now)
    - A State whose bucket_started_at + bucket_span_ms is in the past is normalized (tokens_used resets to 0) before the cond, so an "expired" saturated bucket returns {:reserved, weight}
    - A State with cooldown_until in the future returns {:blocked, "cooldown", cooldown_until, %{reason: cooldown_reason}} and takes precedence over capacity check
    - compute_reservation/4 performs NO repo calls, NO Telemetry.execute_limiter_event, NO record_history_fact (pure — assert via telemetry handler that [:oban_powertools, :limiter, :blocked] never fires when calling compute_reservation directly)
  </behavior>
  <action>
    Add a new public function `compute_reservation(%State{} = state, %Resource{} = resource, weight, now)` to `ObanPowertools.Limits` with spec `:: {:reserved, tokens_used_after :: non_neg_integer()} | {:blocked, code :: String.t(), retry_at :: DateTime.t() | nil, details :: map()}`. Move the decision logic currently inside `attempt_reservation/5` into it: first call `normalize_bucket(state, resource.bucket_span_ms, now)`, then a three-clause `cond` returning `{:blocked, "cooldown", normalized.cooldown_until, %{reason: normalized.cooldown_reason}}` when `cooldown_active?(normalized, now)`; `{:blocked, "limit_reached", retry_at, %{capacity: resource.bucket_capacity, used: normalized.tokens_used}}` (retry_at computed as `normalized.bucket_started_at |> DateTime.add(resource.bucket_span_ms, :millisecond) |> max_datetime(now)`) when `normalized.tokens_used + weight > resource.bucket_capacity`; otherwise `{:reserved, normalized.tokens_used + weight}`. Keep `normalize_bucket/3`, `cooldown_active?/2`, and `max_datetime/2` as PRIVATE helpers called by `compute_reservation/4`. Refactor `attempt_reservation/5` to call `compute_reservation/4` and dispatch: run `normalize_bucket(state, resource.bucket_span_ms, now)` ONCE up front and bind the result to `normalized_state` (so there is no redundant double `normalize_bucket` call); on `{:reserved, new_tokens_used}` perform the existing `State.changeset` + `repo.update` write path against `normalized_state`, setting tokens_used: new_tokens_used, bucket_started_at: normalized_state.bucket_started_at, last_reserved_at: now, reservation_snapshot: snapshot; on `{:blocked, code, _retry_at, _details}` rebuild the existing blocker via `cooldown_blocker(resource, normalized_state)` or `limit_blocker(resource, normalized_state, now)` (selected by `code`) using the same `normalized_state` and call `blocked(repo, snapshot, [blocker], now)`. The side effects in `blocked/4` (Telemetry.execute_limiter_event, record_history_fact) and `repo.update` MUST remain only in `attempt_reservation/5`, never in `compute_reservation/4`. Use weight `snapshot.weight` when calling `compute_reservation/4` from `attempt_reservation/5`. Add unit tests to limits_test.exs per the behavior block, including a telemetry-handler assertion that calling `compute_reservation/4` directly fires zero `[:oban_powertools, :limiter, :blocked]` events.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/oban_powertools && mix test test/oban_powertools/limits_test.exs --max-failures 1</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "def compute_reservation(" lib/oban_powertools/limits.ex` matches exactly one public-arity-4 definition
    - `mix test test/oban_powertools/limits_test.exs` passes with zero failures (regression: reserve/3 behavior unchanged)
    - `normalize_bucket` and `cooldown_active?` remain `defp` (private): `grep -c "defp normalize_bucket" lib/oban_powertools/limits.ex` returns 1 and `grep -c "defp cooldown_active?" lib/oban_powertools/limits.ex` returns 1
    - The pure function body contains no `repo.` call, no `Telemetry.`, and no `record_history_fact` — verify by inspecting the `compute_reservation` function span
    - `attempt_reservation/5` calls `normalize_bucket(` exactly once (no redundant double normalization): the refactor binds `normalized_state` once and reuses it for both the changeset write and blocker rebuild
    - A new test attaches a `[:oban_powertools, :limiter, :blocked]` telemetry handler and asserts it never fires across a sequence of `compute_reservation/4` calls that includes a blocked verdict
    - `attempt_reservation/5` calls `compute_reservation(` (grep confirms the dispatch wiring)
  </acceptance_criteria>
  <done>compute_reservation/4 is public and pure; attempt_reservation/5 delegates its decision to it and normalizes the bucket exactly once; all existing limits tests pass unchanged; side-effect-freedom of the pure core is proven by a telemetry-handler test.</done>
</task>

<task type="auto">
  <name>Task 2: Create shared Glossary module and embed it in the guide</name>
  <files>lib/oban_powertools/limits/glossary.ex, guides/limits-and-explain.md, test/oban_powertools/limits/glossary_test.exs</files>
  <read_first>
    - guides/limits-and-explain.md (current content — the glossary section is absent; find the natural insertion point near the limiter declaration / explain sections)
    - .planning/phases/49-limiter-explain-simulate-cli/49-RESEARCH.md (the "Glossary Single Source of Truth (D-08)" section enumerates all required terms and their definitions sourced from live code)
    - lib/oban_powertools/limits/resource.ex (confirm field names bucket_capacity, bucket_span_ms, default_weight, partition_strategy, scope_kind for accurate definitions)
  </read_first>
  <action>
    Create `lib/oban_powertools/limits/glossary.ex` defining `ObanPowertools.Limits.Glossary` with a public `text/0` function (or a `@text` module attribute exposed via `text/0`) returning a single multi-line markdown string — the canonical rate-limit glossary (D-08, single source of truth). The glossary MUST define all required terms with the literal token strings present: `token_bucket`, `bucket_capacity`, `bucket_span_ms`, bare `weight` AND `weight_by`, `partition`, `partition_by`, `scope` (explaining `global` vs `partitioned`), `cooldown`, and the blocker codes `limit_reached` and `cooldown`. Definitions sourced from RESEARCH.md "Glossary Terms Required by D-08" table. The string is the ONE source both Mix task `@moduledoc` sections (Plans 49-02 and 49-03) and the guide consume. Update `guides/limits-and-explain.md` to add a `## Rate-Limit Glossary` section containing the same glossary content verbatim (paste the term/definition list; the guide is a static `.md` so it carries a copy, and the glossary_test enforces no-drift on the term set). Add `test/oban_powertools/limits/glossary_test.exs` asserting `ObanPowertools.Limits.Glossary.text()` is a binary containing every one of the required term strings, and that `guides/limits-and-explain.md` contains each required term string.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/oban_powertools && mix test test/oban_powertools/limits/glossary_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `ObanPowertools.Limits.Glossary.text()` returns a binary (string) containing all of: `token_bucket`, `bucket_capacity`, `bucket_span_ms`, `weight` (bare), `weight_by`, `partition`, `partition_by`, `scope`, `cooldown`, `limit_reached`
    - `grep -c "token_bucket" guides/limits-and-explain.md` returns >= 1 and the guide also contains `bucket_capacity`, `bucket_span_ms`, `weight`, `weight_by`, `partition_by`, `scope`, `cooldown`, `limit_reached`
    - `mix test test/oban_powertools/limits/glossary_test.exs` passes — its assertions cover the full D-08 term set (including bare `weight` and `scope`) in both `Glossary.text()` and the guide file
    - The glossary section in the guide is under a markdown heading (e.g. `## Rate-Limit Glossary`)
  </acceptance_criteria>
  <done>A single Glossary.text/0 source exists with all required terms (including bare `weight` and `scope`); the guide carries the verbatim glossary; the glossary_test locks the full D-08 term set in both surfaces.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| reserve/3 → compute_reservation/4 | Internal refactor; no new untrusted input crosses here |
| simulate (Plan 49-03) → compute_reservation/4 | Pure function must be callable with NO observable side effects |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-49-01 | Tampering | compute_reservation/4 leaking side effects (telemetry/DB/history) into a "pure" path | mitigate | Telemetry-handler test asserts zero `[:oban_powertools, :limiter, :blocked]` events fire from direct `compute_reservation/4` calls; no `repo.`/`Telemetry.`/`record_history_fact` in the function body |
| T-49-02 | Tampering | Refactor silently changing reserve/3 semantics (observable regression) | mitigate | Full existing `limits_test.exs` regression suite must pass unchanged |
| T-49-SC | Tampering | npm/pip/cargo installs | accept | No package-manager installs in this phase (zero-new-runtime-deps, REL-01); RESEARCH Package Legitimacy Audit confirms none |
</threat_model>

<verification>
- `mix test test/oban_powertools/limits_test.exs test/oban_powertools/limits/glossary_test.exs` passes
- `mix compile --warnings-as-errors` clean (post-merge gate per memory: gsd-execute-phase-gotchas)
- `compute_reservation/4` is the single home of the token-bucket decision; no duplicate cond exists elsewhere
</verification>

<success_criteria>
- compute_reservation/4 public, pure, and proven side-effect-free
- reserve/3 regression-clean
- Glossary single-source string established with all required terms, embedded in the guide
- Supports Success Criteria #2 (no mutation) and #4 (reuse, don't duplicate)
</success_criteria>

<output>
Create `.planning/phases/49-limiter-explain-simulate-cli/49-01-SUMMARY.md` when done
</output>
</content>
</invoke>
