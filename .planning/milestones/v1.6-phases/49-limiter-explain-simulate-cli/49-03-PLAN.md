---
phase: 49-limiter-explain-simulate-cli
plan: 03
type: execute
wave: 2
depends_on: ["49-01"]
files_modified:
  - lib/mix/tasks/oban_powertools.limiter.simulate.ex
  - test/mix/tasks/oban_powertools.limiter.simulate_test.exs
  - test/oban_powertools/docs_contract_test.exs
autonomous: true
requirements: [OPS-07, OPS-08]

must_haves:
  truths:
    - "mix oban_powertools.limiter.simulate --worker MOD previews per-request reserved/blocked verdicts for a config without mutating real state"
    - "Simulate runs --count N sequential reservations against a fresh empty bucket via Limits.compute_reservation/4 only"
    - "Simulate never calls do_reserve/reserve/attempt_reservation/blocked and emits zero limiter.blocked telemetry"
    - "Simulate writes zero rows to oban_powertools_limit_states / oban_powertools_limit_resources"
    - "An unknown --worker module or a worker with no :limits is exit 2"
    - "--format json emits a payload containing schema_version:1"
    - "Both new task @moduledoc sections and the guide contain the same glossary terms (no drift)"
  artifacts:
    - path: "lib/mix/tasks/oban_powertools.limiter.simulate.ex"
      provides: "Mix.Tasks.ObanPowertools.Limiter.Simulate CLI entry (OPS-07)"
      contains: "def run("
    - path: "test/mix/tasks/oban_powertools.limiter.simulate_test.exs"
      provides: "Pure-computation + side-effect-freedom + no-DB-writes tests"
      contains: "blocked"
    - path: "test/oban_powertools/docs_contract_test.exs"
      provides: "Glossary single-source-of-truth contract across both tasks + guide"
      contains: "glossary"
  key_links:
    - from: "lib/mix/tasks/oban_powertools.limiter.simulate.ex"
      to: "ObanPowertools.Limits.compute_reservation/4"
      via: "sequential reservation loop over a synthetic State"
      pattern: "Limits\\.compute_reservation"
    - from: "lib/mix/tasks/oban_powertools.limiter.simulate.ex"
      to: "ObanPowertools.Limits.Glossary.text/0"
      via: "@moduledoc glossary reference"
      pattern: "Glossary"
---

<objective>
Ship `mix oban_powertools.limiter.simulate` (OPS-07): preview limiter behavior for a worker's declared config with flag overrides — D-05: `--worker MOD` reads the declared `:limits` (capacity/span/weight), operator may override `--bucket-capacity`/`--bucket-span-ms`/`--weight`/`--count`/`--partition` — by running `--count N` sequential reservations against a fresh empty bucket through the pure `Limits.compute_reservation/4` core, with provable zero side effects (no telemetry, no DB writes). Mirrors the Doctor task's CLI conventions (D-01) and pure-computation exit-code posture (D-02: exit `0` on success, `2` on bad input / unknown worker). Add the glossary single-source-of-truth contract test spanning both task `@moduledoc`s and the guide (OPS-08).

Purpose: Let operators sanity-check the limits they shipped without mutating real state — side-effect-freedom is a hard correctness property (D-06/D-07).
Output: New simulate Mix task, its test file, and glossary contract assertions added to docs_contract_test.exs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/49-limiter-explain-simulate-cli/49-CONTEXT.md
@.planning/phases/49-limiter-explain-simulate-cli/49-RESEARCH.md
@.planning/phases/49-limiter-explain-simulate-cli/49-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the limiter.simulate Mix task</name>
  <files>lib/mix/tasks/oban_powertools.limiter.simulate.ex</files>
  <read_first>
    - lib/mix/tasks/oban_powertools.doctor.ex (boot/flags/format/exit-codes/resolve_repo/resolve_prefix template — copy verbatim)
    - lib/oban_powertools/limits.ex (compute_reservation/4 added in Plan 49-01 — the ONLY limiter function simulate may call; do NOT call reserve/3, do_reserve/3, attempt_reservation/5, upsert_resource/2, get_or_create_state/4, or blocked/4)
    - lib/oban_powertools/worker.ex (limit_snapshot/2 and __powertools_limits__/0 — note Pitfall 4: limit_snapshot(mod, %{}) RAISES ArgumentError for partition_by: {:args, key} with empty args; read @powertools_limits directly instead and apply --partition flag)
    - lib/oban_powertools/limits/resource.ex and state.ex (synthetic struct field names: bucket_capacity, bucket_span_ms, scope_kind, name; State: partition_key, tokens_used, bucket_started_at, cooldown_until, cooldown_reason)
    - lib/oban_powertools/limits/glossary.ex (shared glossary string created in Plan 49-01 — reference its terms in @moduledoc)
  </read_first>
  <action>
    Create `lib/mix/tasks/oban_powertools.limiter.simulate.ex` defining `Mix.Tasks.ObanPowertools.Limiter.Simulate` with `use Mix.Task`, `@shortdoc`, and a `@moduledoc` carrying the glossary terms matching `ObanPowertools.Limits.Glossary.text/0`. Declare `@switches [repo: :string, prefix: :string, oban_name: :string, format: :string, worker: :string, bucket_capacity: :integer, bucket_span_ms: :integer, weight: :integer, count: :integer, partition: :string]`. Implement `run/1` with the identical boot shell: `Mix.Task.run("app.config")` first; `OptionParser.parse(argv, strict: @switches)`; `resolve_repo/1` via `Module.safe_concat`; wrap in `Ecto.Migrator.with_repo(repo_module, fn _repo -> ... end, pool_size: 2)` for CLI-family consistency (the simulation loop itself is pure and touches no DB); `System.halt` only in the outer `case result do` block. Map `--format` via the closed `case`. Resolve the worker via `Module.safe_concat([worker_string])`; if `--worker` missing return exit 2 with "--worker MOD is required". Resolve config by reading `worker_mod.__powertools_limits__()` directly when `function_exported?(worker_mod, :__powertools_limits__, 0)` (AVOID `Worker.limit_snapshot(mod, %{})` to dodge Pitfall 4's ArgumentError for partitioned workers with empty args); if not exported or limits == [] return exit 2 with "worker has no :limits configured — nothing to simulate". Compute effective values applying flag overrides: capacity = `--bucket-capacity` || limits[:bucket_capacity]; span_ms = `--bucket-span-ms` || limits[:bucket_span_ms]; weight = `--weight` || limits[:default_weight] || 1; count = `--count` || 1; partition = `--partition` || "__global__". Compute scope_kind nil-safely: `scope_kind = (limits[:scope] || :global) |> Atom.to_string()` — `:scope` is nil for default-scoped (global) workers, so a bare `Atom.to_string(limits[:scope])` would raise; default the nil to `:global` before converting. Build a SYNTHETIC `%ObanPowertools.Limits.Resource{name: limits[:name], bucket_capacity: capacity, bucket_span_ms: span_ms, scope_kind: scope_kind}` and a fresh `%ObanPowertools.Limits.State{partition_key: partition, tokens_used: 0, bucket_started_at: now, cooldown_until: nil, cooldown_reason: nil}` (now = `DateTime.utc_now()`). Run the sequential loop with `Enum.reduce(1..count, {initial_state, []}, ...)` calling ONLY `ObanPowertools.Limits.compute_reservation(state, resource, weight, now)`; on `{:reserved, new_tokens_used}` thread `%{state | tokens_used: new_tokens_used}` forward and record a `%{request: i, result: :reserved, tokens_used: new_tokens_used}` verdict; on `{:blocked, code, retry_at, details}` keep state unchanged and record `%{request: i, result: :blocked, blocker_code: code, retry_at: retry_at, details: details}`; reverse the accumulated verdicts. Format per D-07: `:human` renders a readable per-request sequence (ANSI via `IO.ANSI.enabled?()`-gated colorize), `:json` emits `Jason.encode!` of a payload with top-level `schema_version: 1` and the verdict list. Return exit 0 on success. Reuse the verbatim `resolve_prefix/1` from doctor.ex. The simulate path MUST NOT reference `reserve`, `do_reserve`, `attempt_reservation`, `upsert_resource`, `get_or_create_state`, or `blocked`.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/oban_powertools && mix compile --warnings-as-errors 2>&1 | tail -5 && grep -c "Limits.compute_reservation" lib/mix/tasks/oban_powertools.limiter.simulate.ex</automated>
  </verify>
  <acceptance_criteria>
    - `mix help oban_powertools.limiter.simulate` succeeds and output contains the glossary term `token_bucket`
    - Source contains `use Mix.Task`, `Ecto.Migrator.with_repo`, `Module.safe_concat`, and `Limits.compute_reservation`
    - Source does NOT (grep -v '^#' filtered) contain any of: `reserve(`, `do_reserve`, `attempt_reservation`, `upsert_resource`, `get_or_create_state`, `.blocked(` — simulate never touches the side-effecting path
    - `grep -c "String.to_atom(" lib/mix/tasks/oban_powertools.limiter.simulate.ex` returns 0
    - Scope is resolved nil-safely: `grep -c "limits\[:scope\] || :global" lib/mix/tasks/oban_powertools.limiter.simulate.ex` returns 1 (a worker whose limits omit an explicit `:scope` key must not raise — defaults to `:global`)
    - Source matches `->\s+System\.halt` (halt outside callback); no `@requirements`; no `Oban.start_link`
    - `@switches` declares worker, bucket_capacity, bucket_span_ms, weight, count, partition, repo, format
    - JSON path emits a payload containing `schema_version` set to 1
  </acceptance_criteria>
  <done>Simulate task compiles warning-free, reads worker config via __powertools_limits__ (avoiding Pitfall 4), resolves scope_kind nil-safely for default-scoped workers, runs --count N reservations against a fresh bucket through compute_reservation/4 only, and never references any side-effecting limiter function.</done>
</task>

<task type="auto">
  <name>Task 2: Test simulate (pure verdicts + side-effect-freedom + no-DB-writes) and glossary docs contract</name>
  <files>test/mix/tasks/oban_powertools.limiter.simulate_test.exs, test/oban_powertools/docs_contract_test.exs</files>
  <read_first>
    - test/mix/tasks/oban_powertools.doctor_test.exs (source-inspection conventions)
    - test/oban_powertools/docs_contract_test.exs (joined_docs/0 pattern, @docs_files list including guides/limits-and-explain.md, one-assert-per-locked-term style)
    - lib/mix/tasks/oban_powertools.limiter.simulate.ex (task from Task 1)
    - lib/oban_powertools/limits.ex (compute_reservation/4 — the function the simulate loop drives; for the verdict test, drive it with a synthetic Resource/State as the task does)
    - .planning/phases/49-limiter-explain-simulate-cli/49-RESEARCH.md ("Side-Effect-Freedom Test" pattern: telemetry handler that flunks on [:oban_powertools, :limiter, :blocked]; DB count-before/after assertion)
  </read_first>
  <action>
    Create `test/mix/tasks/oban_powertools.limiter.simulate_test.exs`. (a) Source-inspection module (`use ExUnit.Case`, `@task_path "lib/mix/tasks/oban_powertools.limiter.simulate.ex"`): assert `run/1` exported; `use Mix.Task` and `refute use Igniter.Mix.Task`; `refute @requirements`; `refute Oban.start_link`; `assert Ecto.Migrator.with_repo`; `refute ~r/String\.to_atom\(/`; `assert Module.safe_concat`; `assert ~r/->\s+System\.halt/`; switches present (worker, bucket_capacity, bucket_span_ms, weight, count, partition); `refute` the source references any side-effecting limiter call (`do_reserve`, `attempt_reservation`, `upsert_resource`, `get_or_create_state`); `@shortdoc` present; `token_bucket` glossary term present. (b) Pure-verdict module (`use ExUnit.Case, async: true`): build a synthetic `%Resource{bucket_capacity: 3, bucket_span_ms: 60_000, ...}` and fresh `%State{tokens_used: 0, ...}`, run the same sequential reduce the task uses with count 4 weight 1, and assert verdicts are reserved on requests 1-3 then blocked "limit_reached" on request 4 with retry_at non-nil. Add a second pure-verdict case for a default-scoped worker config (`:scope` absent → scope_kind resolves to "global") to prove the nil-safe scope handling does not raise and still produces correct verdicts. (c) Side-effect-freedom module (`use ObanPowertools.DataCase, async: false`): attach a `[:oban_powertools, :limiter, :blocked]` telemetry handler that `flunk/1`s if invoked, `on_exit` detach; run a simulate sequence that includes a blocked verdict and assert the handler never fired. Also assert no DB writes: capture `repo().aggregate(ObanPowertools.Limits.State, :count)` and `repo().aggregate(ObanPowertools.Limits.Resource, :count)` before and after running the simulation, asserting equality (proves OPS-07 no-mutation). Name tests mapped to OPS-07. Then update `test/oban_powertools/docs_contract_test.exs`: add the glossary single-source-of-truth assertions per PATTERNS.md — a test asserting `guides/limits-and-explain.md` contains all D-08 term strings, and tests asserting each of `lib/mix/tasks/oban_powertools.limiter.explain.ex` and `lib/mix/tasks/oban_powertools.limiter.simulate.ex` @moduledoc source contains the same D-08 term strings: `token_bucket`, `bucket_capacity`, `bucket_span_ms`, bare `weight`, `weight_by`, `partition`, `partition_by`, `scope`, `cooldown`, `limit_reached`. This locks OPS-08 no-drift across all three surfaces (guide + explain @moduledoc + simulate @moduledoc), including the previously-missing `scope` and bare `weight`.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/oban_powertools && mix test test/mix/tasks/oban_powertools.limiter.simulate_test.exs test/oban_powertools/docs_contract_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `mix test test/mix/tasks/oban_powertools.limiter.simulate_test.exs test/oban_powertools/docs_contract_test.exs` passes with zero failures
    - A pure-verdict test asserts requests 1-3 reserved and request 4 blocked "limit_reached" for capacity 3 / weight 1 / count 4
    - A pure-verdict test covers a default-scoped worker (`:scope` key absent) and asserts no raise + scope_kind == "global"
    - A side-effect-freedom test attaches a `[:oban_powertools, :limiter, :blocked]` handler and asserts it NEVER fires during simulation (handler calls flunk)
    - A no-DB-writes test asserts `State` and `Resource` table counts are unchanged before/after simulation
    - The simulate source-inspection test asserts the source does not reference `do_reserve`, `attempt_reservation`, `upsert_resource`, or `get_or_create_state`
    - docs_contract_test asserts the full D-08 term set — `token_bucket`, `bucket_capacity`, `bucket_span_ms`, bare `weight`, `weight_by`, `partition`, `partition_by`, `scope`, `cooldown`, `limit_reached` — appears in the guide AND in both task source files (no-drift, OPS-08)
  </acceptance_criteria>
  <done>Simulate is proven to produce correct per-request verdicts (including default-scoped workers), emit zero blocked telemetry, and write zero limiter rows; the glossary contract is locked across both tasks and the guide with the full D-08 term set (scope + bare weight included). All tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator CLI → simulate task | Untrusted flag input: `--worker` (module name), `--bucket-capacity`/`--bucket-span-ms`/`--weight`/`--count` (integers), `--partition`, `--format`, `--repo` |
| simulate → Limits.compute_reservation/4 | Pure computation only; MUST NOT cross into the side-effecting reserve path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-49-07 | Tampering | A "simulate" that mutates observable state (telemetry / audit rows / history facts / DB writes) — correctness & safety threat | mitigate | Simulate calls ONLY `compute_reservation/4`; never `reserve`/`do_reserve`/`attempt_reservation`/`blocked`/`upsert_resource`/`get_or_create_state`. Telemetry-handler test asserts zero `[:oban_powertools, :limiter, :blocked]` events; DB count-before/after test asserts zero State/Resource writes |
| T-49-08 | Tampering / DoS | `--worker` module-name flag | mitigate | `Module.safe_concat([string])`; never `String.to_atom` (T-48-05); source test `refute ~r/String\.to_atom\(/` |
| T-49-09 | DoS | Partitioned-worker landmine: `Worker.limit_snapshot(mod, %{})` raises ArgumentError for `partition_by: {:args, key}` with empty args | mitigate | Read `__powertools_limits__/0` directly and apply `--partition`/`"__global__"` rather than calling `limit_snapshot/2` with empty args (Pitfall 4) |
| T-49-10 | Tampering | `--format` flag | mitigate | Closed `case "json" -> :json; _ -> :human`; no atom creation from input |
| T-49-SC | Tampering | npm/pip/cargo installs | accept | No package installs (zero-new-runtime-deps, REL-01); RESEARCH audit confirms none |
</threat_model>

<verification>
- `mix test test/mix/tasks/oban_powertools.limiter.simulate_test.exs test/oban_powertools/docs_contract_test.exs` passes
- `mix compile --warnings-as-errors` clean
- `mix help oban_powertools.limiter.simulate` renders with glossary terms present
- Simulate side-effect-freedom proven by telemetry-handler + DB-count tests (OPS-07 hard property)
- New task file auto-included by `:files` whitelist (REL-01): `lib` glob covers `lib/mix/tasks/` — no mix.exs change required
</verification>

<success_criteria>
- `mix oban_powertools.limiter.simulate` previews limiter behavior for a config without mutating real state (Success Criterion #2)
- Reuses Limits.compute_reservation/4 without duplicating limiter logic (Success Criterion #4)
- Glossary shipped and locked no-drift across explain + simulate @moduledoc and the guide (Success Criterion #3 / OPS-08)
- Untrusted CLI input safely resolved (T-48-05); partitioned-worker landmine avoided
</success_criteria>

<output>
Create `.planning/phases/49-limiter-explain-simulate-cli/49-03-SUMMARY.md` when done
</output>
</content>
</invoke>
