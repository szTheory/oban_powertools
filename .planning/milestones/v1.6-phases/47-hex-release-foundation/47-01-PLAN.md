---
phase: 47-hex-release-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - CHANGELOG.md
  - LICENSE
autonomous: true
requirements: [REL-03]
must_haves:
  truths:
    - "CHANGELOG.md documents the 0.5.0 release in Keep-a-Changelog format"
    - "CHANGELOG.md states the explicit, documented path to 1.0 (per-surface + stability-window gate)"
    - "A LICENSE file with verbatim Apache-2.0 text exists at repo root"
  artifacts:
    - path: "CHANGELOG.md"
      provides: "0.5.0 release notes + Unreleased section + path-to-1.0 gate"
      contains: "## [0.5.0]"
    - path: "LICENSE"
      provides: "Apache-2.0 verbatim license text"
      contains: "Apache License"
  key_links:
    - from: "CHANGELOG.md"
      to: "ExDoc extras (plan 02) and :files whitelist (plan 02)"
      via: "file must exist at root for both to resolve"
      pattern: "CHANGELOG\\.md"
---

<objective>
Create the two greenfield documentation/legal source files that the packaging and docs config in plan 02 depend on: `CHANGELOG.md` (REL-03) and `LICENSE` (D-06/D-07). These have zero dependencies on other phase work and must exist before `mix docs` (extras references CHANGELOG.md) and `mix hex.build` (`:files` references both) can succeed.

Purpose: Establish the human-authored release narrative and the legal artifact that the SPDX `licenses: ["Apache-2.0"]` declaration must match. This is the first public changelog for a library that lived internally at 0.1.0 across v1–v1.5; it must NOT backfill internal milestones (D-13).
Output: `CHANGELOG.md` and `LICENSE` at repo root.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/47-hex-release-foundation/47-CONTEXT.md
@.planning/phases/47-hex-release-foundation/47-RESEARCH.md
@.planning/research/PITFALLS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author CHANGELOG.md (Keep a Changelog) with 0.5.0 entry, Unreleased section, and path-to-1.0 gate</name>
  <files>CHANGELOG.md</files>
  <read_first>
    - .planning/phases/47-hex-release-foundation/47-RESEARCH.md (Pattern 5: CHANGELOG.md structure for first release; "Planning milestones vs Hex releases" prose block)
    - .planning/phases/47-hex-release-foundation/47-CONTEXT.md (D-10, D-11, D-12, D-13 — changelog framing decisions)
    - .planning/research/PITFALLS.md (Pitfall 2: 0.5.0 not 1.0; Pitfall 16: CHANGELOG)
    - .planning/ROADMAP.md (Phase 47 success criterion #4; feature domains to enumerate under Added)
    - lib/oban_powertools/telemetry.ex (the frozen @contract — a named 1.0 surface per D-09; read to describe it accurately, do NOT edit)
  </read_first>
  <action>
    Create CHANGELOG.md at repo root in Keep-a-Changelog 1.1.0 format with the SemVer adherence header (per D-10, Keep-a-Changelog). Structure top-to-bottom:
    (1) `# Changelog` title + the standard "All notable changes..." preamble linking keepachangelog.com/en/1.1.0/ and semver.org.
    (2) A `## Planning milestones vs Hex releases` prose note (per D-11) clarifying that SemVer headings like `[0.5.0]` are published Hex releases, internal v1.x planning milestones in `.planning/` are NOT a second installable version axis, and the library stays 0.x on Hex until a real 1.0.0 after adopter feedback. Use the project's support-truth vocabulary.
    (3) `## [Unreleased]` section (empty, with an HTML comment marker noting Phases 48-51 accumulate entries here, per D-11).
    (4) `## [0.5.0] - 2026-05-29` with a single `### Added` block, feature-grouped by domain via `####` subheadings (per D-10): Workers & Idempotency; Limiters & Explain; Cron; Workflows; Lifeline & Repairs; native `/ops/jobs` shell; Operator API (single + bulk); Telemetry contract; Install & Migrations; optional oban_web bridge. Author honest capability bullets (wording at your discretion per CONTEXT Claude's Discretion) describing what each domain ships.
    (5) A `## Path to 1.0` section (per D-12, satisfying REL-03 in this same file) documenting the hybrid per-surface + stability-window gate (D-08): each named public surface freezes only after it is (i) explicitly enumerated, (ii) exercised by >=1 non-szTheory host, (iii) free of known breaking change, AND (iv) has survived >=2 consecutive 0.x minor releases without a breaking change. Enumerate the four 1.0 surfaces (D-09): installer/migration contract, Operator Elixir API (single + bulk), the frozen Telemetry `@contract`, and the host-ownership boundary. Present as a per-surface checklist.
    Do NOT backfill internal v1.x milestones as prior changelog entries (D-13). The only versioned entry is `[0.5.0]`.
  </action>
  <verify>
    <automated>test -f CHANGELOG.md && grep -q '## \[0.5.0\]' CHANGELOG.md && grep -q '## \[Unreleased\]' CHANGELOG.md && grep -qi 'Path to 1.0' CHANGELOG.md && grep -qi 'keepachangelog' CHANGELOG.md && ! grep -qE '## \[1\.[0-9]+\.[0-9]+\]' CHANGELOG.md && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `CHANGELOG.md` exists at repo root.
    - Contains `## [0.5.0]`, `## [Unreleased]`, and a `## Path to 1.0` (or equivalent) section heading.
    - References Keep a Changelog (`keepachangelog`) and SemVer in the preamble.
    - The path-to-1.0 section enumerates all four surfaces from D-09: installer/migration contract, Operator Elixir API, frozen Telemetry @contract, host-ownership boundary.
    - The `### Added` block has `####` domain subheadings for at least the 10 D-10 domains.
    - Contains NO `## [1.x.y]` SemVer headings and NO backfilled internal-milestone entries (D-13).
    - Contains the "Planning milestones vs Hex releases" prose note (D-11).
  </acceptance_criteria>
  <done>CHANGELOG.md exists and satisfies all acceptance criteria above; REL-03 content (0.5.0 + path to 1.0) is fully present.</done>
</task>

<task type="auto">
  <name>Task 2: Add verbatim Apache-2.0 LICENSE file at repo root</name>
  <files>LICENSE</files>
  <read_first>
    - .planning/phases/47-hex-release-foundation/47-CONTEXT.md (D-06, D-07 — Apache-2.0 choice and verbatim-text requirement)
    - .planning/phases/47-hex-release-foundation/47-PATTERNS.md (No Analog Found table — LICENSE is greenfield, SPDX id "Apache-2.0")
  </read_first>
  <action>
    Create a `LICENSE` file at repo root containing the complete, verbatim Apache License 2.0 text (the canonical text from apache.org/licenses/LICENSE-2.0.txt, including the "Apache License / Version 2.0, January 2004" header, all 9 numbered sections, and the APPENDIX boilerplate). The SPDX identifier this file represents is `Apache-2.0`, which MUST match the `licenses: ["Apache-2.0"]` declaration that plan 02 adds to `package/0` (D-07). In the APPENDIX copyright line, set the copyright holder to the project maintainer (szTheory / Oban Powertools) and year 2026. Do not abbreviate or summarize the license body — hex.pm license detection requires the verbatim text.
  </action>
  <verify>
    <automated>test -f LICENSE && grep -q 'Apache License' LICENSE && grep -q 'Version 2.0, January 2004' LICENSE && grep -q 'http://www.apache.org/licenses/LICENSE-2.0' LICENSE && grep -q 'APPENDIX' LICENSE && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `LICENSE` exists at repo root.
    - Contains the strings `Apache License`, `Version 2.0, January 2004`, the canonical license URL, and `APPENDIX`.
    - Contains all standard numbered sections (1. Definitions through 9. Accepting Warranty) — verbatim, not summarized.
    - Copyright line names the maintainer and year 2026.
  </acceptance_criteria>
  <done>LICENSE file with full verbatim Apache-2.0 text exists at repo root, matching SPDX id `Apache-2.0`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo files → published hex tarball | CHANGELOG.md and LICENSE are whitelisted into the tarball (plan 02 `:files`); their content becomes public artifact. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-47-01 | Information Disclosure | CHANGELOG.md content | mitigate | Hand-author from public roadmap domains only; do NOT paste internal `.planning/` paths, secrets, or host-specific data into changelog bullets (D-13 also forbids backfilling internal milestones). |
| T-47-02 | Repudiation | LICENSE legal terms | accept | Verbatim Apache-2.0 is the canonical text; SPDX id must match plan 02 declaration (low risk, single-vendor project per D-06). |
</threat_model>

<verification>
- `test -f CHANGELOG.md && test -f LICENSE` both succeed.
- CHANGELOG passes the Task 1 automated grep gate.
- LICENSE passes the Task 2 automated grep gate.
- No internal v1.x SemVer headings leaked into CHANGELOG (D-13).
</verification>

<success_criteria>
- REL-03 content delivered: CHANGELOG.md documents 0.5.0 and the explicit path to 1.0 (four surfaces, stability-window gate).
- LICENSE (Apache-2.0 verbatim) present, ready for `:files` whitelist and SPDX match in plan 02.
- Both files exist at root so plan 02's `mix docs` (extras) and `mix hex.build` (`:files`) resolve.
</success_criteria>

<output>
Create `.planning/phases/47-hex-release-foundation/47-01-SUMMARY.md` when done.
</output>
