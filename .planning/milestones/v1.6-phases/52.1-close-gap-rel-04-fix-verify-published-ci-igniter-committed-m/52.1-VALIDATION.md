---
phase: 52.1
slug: close-gap-rel-04-fix-verify-published-ci-igniter-committed-m
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-05-30
---

# Phase 52.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | GitHub Actions CI (integration) + manual shell one-liner |
| **Config file** | `.github/workflows/release.yml` (the `verify-published` job) |
| **Quick run command** | `echo '{:oban_powertools, "~> 0.5"}' \| sed 's\|{:oban_powertools, "~> [0-9][0-9.]*"}\|{:oban_powertools, "== 0.5.0"}\|'` (local sed smoke test) |
| **Full suite command** | `verify-published` CI job triggered on next release run |
| **Estimated runtime** | ~0s (smoke test) / ~8–12 min (full CI job) |

---

## Sampling Rate

- **After every task commit:** Run the sed smoke test (D-04 only — no local test infra for CI YAML changes)
- **After every plan wave:** Inspect the modified release.yml and .gitignore manually (code review)
- **Before `/gsd-verify-work`:** Confirm all 4 decisions are present in the committed diff
- **Max feedback latency:** N/A for CI-only changes; code review is the primary gate

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| 52.1-01-01 | 01 | 1 | D-01 | — | N/A | Code review | `grep -A3 "Delete committed installer-generated files" .github/workflows/release.yml` | ✅ | ⬜ pending |
| 52.1-01-02 | 01 | 1 | D-01a | — | N/A | Code review | `grep -c "NOTE: oban_powertools_auth.ex" examples/hex_consumer/regenerate.sh` | ✅ | ⬜ pending |
| 52.1-01-03 | 01 | 1 | D-02 | — | N/A | Code review | `grep "hex_consumer_local_proof" .gitignore` | ✅ | ⬜ pending |
| 52.1-01-04 | 01 | 1 | D-03 | — | N/A | Code review | `grep -A5 "Compile hex consumer" .github/workflows/release.yml \| grep "MIX_ENV"` | ✅ | ⬜ pending |
| 52.1-01-05 | 01 | 1 | D-04 | — | N/A | Manual smoke + code review | `echo '{:oban_powertools, "~> 0.5"}' \| sed 's\|{:oban_powertools, "~> [0-9][0-9.]*"}\|{:oban_powertools, "== 0.5.0"}\|'` → must output `{:oban_powertools, "== 0.5.0"}` | ✅ | ⬜ pending |
| 52.1-CI | 01 | — | REL-04 | — | N/A | Integration (CI) | `verify-published` job passes on next release run | — | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

Existing infrastructure covers all phase requirements. No new test files or fixtures are needed — all validation is CI-observable on next release run, plus the manual sed smoke test.

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| `verify-published` job passes end-to-end | REL-04 | CI-only; no local equivalent for the full pipeline | Trigger a release run (or push a tag); inspect `verify-published` job log for "6 migrations generated" and passing first-session test |
| sed regex matches current dep string | D-04 | Local smoke test for the regex | `echo '{:oban_powertools, "~> 0.5"}' \| sed 's\|{:oban_powertools, "~> [0-9][0-9.]*"}\|{:oban_powertools, "== 0.5.0"}\|'` → expected: `{:oban_powertools, "== 0.5.0"}` |
| hex_consumer_local_proof absent from git status | D-02 | Local working tree only | `git status` shows no `examples/hex_consumer_local_proof/` after adding gitignore entry |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < N/A (CI-only phase)
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
