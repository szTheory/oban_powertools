---
phase: 52.1-close-gap-rel-04-fix-verify-published-ci-igniter-committed-m
verified: 2026-05-30T18:00:00Z
status: human_needed
score: 6/6 must-haves verified
overrides_applied: 0
human_verification:
  - test: "Trigger a real release and observe the verify-published CI job end-to-end"
    expected: "Igniter generates 6 migration files fresh (no collision), mix ecto.migrate creates tables, seeds.exs succeeds, first-session test passes (REL-04)"
    why_human: "The integration path requires a live Hex publish + CI run; cannot be verified statically or by local grep"
---

# Phase 52.1: Close Gap REL-04 — Fix verify-published CI Verification Report

**Phase Goal:** Close gap REL-04 — make four surgical edits across release.yml, regenerate.sh, and .gitignore so the verify-published CI job completes end-to-end (Igniter generates files fresh, compile uses MIX_ENV: test, sed is version-agnostic, local-proof artifact is ignored).
**Verified:** 2026-05-30T18:00:00Z
**Status:** human_needed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | The verify-published job deletes the two committed installer-generated files before running mix oban_powertools.install, so Igniter regenerates them fresh and emits the 6 migration files | VERIFIED | `Delete committed installer-generated files` step at line 254, after `actions/cache` (line 247) and before `Install Hex + Rebar` (line 259); two `rm -f` lines confirmed at lines 256-257; no `working-directory` key on the step |
| 2 | The Compile hex consumer step runs under MIX_ENV: test, eliminating the env-switch recompile | VERIFIED | `env: MIX_ENV: test` at line 280, placed between `name:` (line 278) and `run:` (line 281); `working-directory: examples/hex_consumer` still present at line 282 |
| 3 | The version-pin sed command matches any ~> X.Y[.Z] dep string, not just the hardcoded ~> 0.5 | VERIFIED | Line 267: `sed -i "s|{:oban_powertools, \"~> [0-9][0-9.]*\"}|..."` — no literal `~> 0.5` found in file; no `''` BSD argument; smoke test ALL PASS for 0.5, 0.6, 1.0, 1.23.4 |
| 4 | regenerate.sh documents that the two committed scaffold files are overwritten by the installer on each fresh install | VERIFIED | Lines 39-45: 7-line `# ` comment block after `replace_once` call (line 37) and before `(` subshell (line 47); names both `oban_powertools_auth.ex` and `oban_powertools_display_policy.ex`; contains "overwritten by"; uses only `# ` prefix (no `###`/`=====` markers) |
| 5 | git status no longer shows examples/hex_consumer_local_proof/ as untracked | VERIFIED | `.gitignore` line 34: `examples/hex_consumer_local_proof/`; positioned after `examples/hex_consumer/deps/` (line 33) and before `# GSD / agent` section (line 36); `git check-ignore` exits 0; directory not tracked in git (0 ls-files results); `git status --short` shows no `??` entry for the path |
| 6 | Local sed smoke test validates the version-agnostic pattern | VERIFIED | All four version strings (`~> 0.5`, `~> 0.6`, `~> 1.0`, `~> 1.23.4`) rewritten correctly; smoke test exits 0 with `ALL PASS` |

**Score:** 6/6 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `.github/workflows/release.yml` | verify-published job with rm-step, MIX_ENV compile, version-agnostic sed; contains "Delete committed installer-generated files" | VERIFIED | All three edits confirmed at lines 254-257 (D-01), 278-282 (D-03), 266-268 (D-04) |
| `examples/hex_consumer/regenerate.sh` | local-dev vs CI-install scaffold comment; contains "overwritten by" | VERIFIED | Comment block at lines 39-45; both file names referenced; "overwritten by" at line 45 |
| `.gitignore` | ignore entry for the local-proof artifact; contains "examples/hex_consumer_local_proof/" | VERIFIED | Line 34 confirmed; `git check-ignore` exits 0 |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| Delete committed installer-generated files step (line 254) | Run installer from published tarball step (line 274) | Step ordering — rm runs after checkout/cache and before installer | WIRED | cache step at line 247, rm step at line 254, Install Hex + Rebar at line 259, Run installer at line 274 — ordering is correct |

### Data-Flow Trace (Level 4)

Not applicable — this phase modifies CI workflow YAML, a shell script, and .gitignore. No dynamic data rendering artifacts.

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| Version-agnostic sed fires for ~> 0.5 | `echo '{:oban_powertools, "~> 0.5"}' \| sed 's\|...\|...\|'` | `{:oban_powertools, "== 0.5.0"}` | PASS |
| Version-agnostic sed fires for ~> 0.6 | same pattern, input ~> 0.6 | `{:oban_powertools, "== 0.5.0"}` | PASS |
| Version-agnostic sed fires for ~> 1.0 | same pattern, input ~> 1.0 | `{:oban_powertools, "== 0.5.0"}` | PASS |
| Version-agnostic sed fires for ~> 1.23.4 | same pattern, input ~> 1.23.4 | `{:oban_powertools, "== 0.5.0"}` | PASS |
| local-proof directory ignored | `git check-ignore examples/hex_consumer_local_proof/` | exit 0 | PASS |

### Probe Execution

No phase-declared probes. No conventional `scripts/*/tests/probe-*.sh` applicable to this CI-config phase. Step 7c: SKIPPED (YAML/config-only phase, no runnable probe scripts).

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|------------|-------------|--------|----------|
| REL-04 | 52.1-01-PLAN.md | Getting-started quickstart verified working from the published package — fresh host installs from hex and reaches a first successful operator session | PARTIALLY SATISFIED (static checks pass; CI end-to-end requires human) | All four surgical edits confirmed in codebase; integration test (live CI run) is the remaining gate |

**REL-04 traceability note:** REQUIREMENTS.md traceability table lists REL-04 as Phase 51 / Complete, but Phase 52.1 was inserted specifically to close a gap in REL-04 (the CI job was failing). The static codebase changes are verified. Full satisfaction of REL-04 requires the verify-published CI job to run and pass on the next release.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| `examples/hex_consumer/regenerate.sh` | 71-74 | TODO markers inside `cat <<EOF` heredoc | Info | These are user-facing instructions printed when the script runs, not source code debt. Introduced in commit `354b839` (Phase 51), not this phase. Not a blocker. |

No `TBD`, `FIXME`, or `XXX` markers found in any file modified by this phase.

### Human Verification Required

#### 1. verify-published CI Job End-to-End Pass

**Test:** Trigger a real release (or manually dispatch `release.yml` with a release tag) and observe the `verify-published` job run to completion.
**Expected:** Igniter encounters no pre-existing scaffold files, generates `oban_powertools_auth.ex`, `oban_powertools_display_policy.ex`, and all 6 migration files fresh from the published tarball. `mix ecto.migrate` creates the required tables. `seeds.exs` succeeds. `mix test test/hex_consumer_web/oban_powertools_first_session_test.exs --trace` passes. REL-04 satisfied.
**Why human:** The integration path requires a live Hex publish + GitHub Actions CI run. No local static check can simulate Igniter's module-collision detection, the six-migration generation path, or the actual database migration/seed sequence.

### Gaps Summary

No gaps. All six observable truths verified in the codebase. The single remaining item is a CI integration gate that requires a real release run — this is explicitly the intended verification model for REL-04, as stated in the PLAN's `<verification>` section.

---

_Verified: 2026-05-30T18:00:00Z_
_Verifier: Claude (gsd-verifier)_
