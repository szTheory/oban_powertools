---
phase: 48-doctor-health-check-task
plan: 02
type: execute
wave: 2
depends_on: ["48-01"]
files_modified:
  - lib/oban_powertools/doctor/formatter.ex
  - lib/mix/tasks/oban_powertools.doctor.ex
  - test/oban_powertools/doctor/formatter_test.exs
  - test/mix/tasks/oban_powertools.doctor_test.exs
autonomous: false
requirements: [OPS-03, OPS-04, OPS-05]
must_haves:
  truths:
    - "Default human output renders a sectioned report with severity labels and remediation hints"
    - "ANSI color auto-degrades off in non-TTY/CI via IO.ANSI.enabled?"
    - "--format json emits valid JSON with a top-level schema_version field"
    - "mix oban_powertools.doctor on a healthy DB exits 0; with an error finding exits 2; warning-only exits 1 (or 2 under --strict)"
    - "Repo resolves from --repo (safe atom) > RuntimeConfig.repo!; no-repo path errors with the config :oban_powertools, repo: contract message"
    - "Prefix resolves from --prefix > host Oban app env (no Oban started) > public"
    - "The task starts only the Ecto repo via Ecto.Migrator.with_repo/2 — Oban is never started"
    - "System.halt/1 is called after with_repo returns, never inside the callback"
  artifacts:
    - path: "lib/oban_powertools/doctor/formatter.ex"
      provides: "Pure human + JSON renderers over [%Finding{}]; print/2"
      contains: "schema_version"
    - path: "lib/mix/tasks/oban_powertools.doctor.ex"
      provides: "Mix.Task entry: OptionParser, repo/prefix resolution, with_repo boot, System.halt"
      contains: "use Mix.Task"
    - path: "test/oban_powertools/doctor/formatter_test.exs"
      provides: "Pure formatter tests (human hints + JSON schema_version)"
    - path: "test/mix/tasks/oban_powertools.doctor_test.exs"
      provides: "CLI flag-parsing + contract tests (no DB)"
  key_links:
    - from: "lib/mix/tasks/oban_powertools.doctor.ex"
      to: "ObanPowertools.Doctor.run/2 + exit_code_for/1"
      via: "inside Ecto.Migrator.with_repo/2 callback"
      pattern: "ObanPowertools\\.Doctor\\.(run|exit_code_for)"
    - from: "lib/mix/tasks/oban_powertools.doctor.ex"
      to: "Ecto.Migrator.with_repo/2"
      via: "repo-only boot, no Oban"
      pattern: "Ecto\\.Migrator\\.with_repo"
    - from: "lib/oban_powertools/doctor/formatter.ex"
      to: "Jason"
      via: "JSON encoding with schema_version"
      pattern: "Jason\\.encode"
---

<objective>
Build the presentation and CLI layer of `mix oban_powertools.doctor`: the pure `ObanPowertools.Doctor.Formatter` (human sectioned report + `--format json` with a `schema_version` stability contract) and the plain `Mix.Tasks.ObanPowertools.Doctor` entry point (OptionParser flags, layered repo/prefix resolution, repo-only `Ecto.Migrator.with_repo/2` boot, and honest `System.halt/1` exit codes).

Purpose: Turns the Plan 01 findings into operator-facing output and a CI-honest CLI. Honors D-01 (dual output + ANSI auto-degrade), D-02/D-03 (Jason, no new dep, top-level `schema_version` contract), D-07/D-08 (layered precedence + actionable no-repo error), and D-09/D-10 (repo-only boot, no Oban start, prefix from flag or app env only). The CLI surfaces OPS-03/OPS-04 findings and delivers OPS-05 (exit codes + remediation hints in output).

Output: `lib/oban_powertools/doctor/formatter.ex`, `lib/mix/tasks/oban_powertools.doctor.ex`, and their tests. Covers OPS-05 (remediation hints, JSON contract, exit codes via CLI) plus the OPS-03/OPS-04 user-facing wiring.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/48-doctor-health-check-task/48-CONTEXT.md
@.planning/phases/48-doctor-health-check-task/48-RESEARCH.md
@.planning/phases/48-doctor-health-check-task/48-PATTERNS.md
@.planning/phases/48-doctor-health-check-task/48-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Doctor.Formatter — human report + JSON schema_version contract (OPS-05)</name>
  <files>lib/oban_powertools/doctor/formatter.ex, test/oban_powertools/doctor/formatter_test.exs</files>
  <read_first>
    - lib/oban_powertools/doctor.ex (Finding struct fields: check, severity, message, remediation; exit_code_for/1)
    - .planning/phases/48-doctor-health-check-task/48-RESEARCH.md (Pattern 3 ANSI colorize; JSON output schema example with schema_version: 1, prefix, oban_version_installed/db, exit_code, findings)
    - .planning/phases/48-doctor-health-check-task/48-PATTERNS.md (formatter module structure; Jason pattern from display_policy.ex line 144; print/format split)
    - .planning/phases/48-doctor-health-check-task/48-CONTEXT.md (D-01 dual output + ANSI degrade, D-03 schema_version stability contract)
    - test/mix/tasks/oban_powertools.install_test.exs (pure ExUnit.Case analog — no DataCase)
  </read_first>
  <behavior>
    - Test: format([], format: :human) renders an all-clear/OK report (no findings)
    - Test: format(findings, format: :human) includes the severity label and the remediation string for each finding
    - Test: format([], format: :json) decodes to a map with "schema_version" == 1
    - Test: format(findings, format: :json) decodes to a map whose "findings" list carries check, severity, message, remediation for each finding
    - Test: JSON top-level includes prefix and exit_code (passed via opts)
  </behavior>
  <action>
    Create lib/oban_powertools/doctor/formatter.ex as a pure module (no Ecto, no DB) aliasing ObanPowertools.Doctor.Finding. Implement format(findings, opts) dispatching on Keyword.get(opts, :format, :human) to private human/2 and json/2. human/2: sectioned text report grouping findings by severity, each line showing a severity label, the message, and the remediation hint; use a private colorize/2 helper gated on IO.ANSI.enabled?() (red for :error, yellow for :warning) that returns plain text when ANSI is disabled (CI/non-TTY/NO_COLOR) per D-01. json/2: Jason.encode!(map) where the map has top-level schema_version: 1 (the D-03 stability contract — document it as CHANGELOG-tracked in the @moduledoc), plus prefix, oban_version_installed, oban_version_db, exit_code (all from opts), and findings as a list of maps with check/severity/message/remediation. Add print(findings, opts) that IO.puts the formatted string. Choose exact human section ordering and JSON field nesting (Claude's Discretion D-01/D-03) — but the schema_version field is mandatory and fixed at 1. Create test/oban_powertools/doctor/formatter_test.exs as pure ExUnit.Case covering the behaviors above, decoding JSON with Jason.decode/1.
  </action>
  <verify>
    <automated>mix test test/oban_powertools/doctor/formatter_test.exs --max-failures 3</automated>
  </verify>
  <acceptance_criteria>
    - Source: lib/oban_powertools/doctor/formatter.ex contains `IO.ANSI.enabled?` and `Jason.encode`
    - Source: formatter.ex is pure — does NOT contain `import Ecto.Query`, `repo.query`, or `Ecto.Adapters.SQL`
    - CLI/Behavior: format([], format: :json) decodes to JSON with top-level `schema_version` == 1
    - Behavior: human format output for a finding contains both its message and its remediation string
    - Behavior: `mix test test/oban_powertools/doctor/formatter_test.exs` exits 0
  </acceptance_criteria>
  <done>Formatter renders human (ANSI-degrading) and JSON (schema_version: 1) output including remediation hints; formatter_test green.</done>
</task>

<task type="auto">
  <name>Task 2: Mix.Tasks.ObanPowertools.Doctor — flags, repo/prefix resolution, with_repo boot, exit codes (OPS-03/04/05)</name>
  <files>lib/mix/tasks/oban_powertools.doctor.ex, test/mix/tasks/oban_powertools.doctor_test.exs</files>
  <read_first>
    - lib/oban_powertools/doctor.ex (run/2 + exit_code_for/1 signatures)
    - lib/oban_powertools/doctor/formatter.ex (print/2 signature, opts it expects)
    - lib/oban_powertools/runtime_config.ex (repo/1, repo!/1, setup_error(:repo) message tone for D-08)
    - lib/mix/tasks/oban_powertools.install.ex (module naming + @shortdoc convention ONLY — it is Igniter-based, NOT a structural template)
    - .planning/phases/48-doctor-health-check-task/48-RESEARCH.md (Pattern 1 plain Mix.Task; Pattern 2 System.halt; Research Q6 OptionParser switches + resolve_prefix; Pitfalls 1/3/4/6)
    - .planning/phases/48-doctor-health-check-task/48-PATTERNS.md (Mix task entry pattern, resolve_repo with Module.safe_concat, resolve_prefix, "halt after with_repo" pitfall)
    - .planning/phases/48-doctor-health-check-task/48-CONTEXT.md (D-07 layered precedence, D-08 no-repo error, D-09/D-10 repo-only boot)
    - test/mix/tasks/oban_powertools.install_test.exs (pure ExUnit.Case test analog for a Mix task)
    - test/test_helper.exs (Ecto.Migrator.with_repo usage in this repo)
  </read_first>
  <action>
    Create lib/mix/tasks/oban_powertools.doctor.ex with `use Mix.Task` (NOT use Igniter.Mix.Task — D-09/PATTERNS note 1), @shortdoc "Diagnose Oban DB/config health (read-only)", and a @moduledoc documenting flags, exit codes 0/1/2, the severity table, the --strict promotion scope, and that --prefix is the reliable production path (auto-detect falls back to "public" with a notice — RESEARCH Pitfall 6). Define @switches [repo: :string, prefix: :string, oban_name: :string, format: :string, strict: :boolean] and @impl run/1: parse with OptionParser.parse(argv, strict: @switches). resolve_repo/1: --repo via Module.safe_concat([repo_string]) guarded with Code.ensure_loaded?/String.to_existing_atom (NEVER String.to_atom on CLI input — Pitfall 4), else ObanPowertools.RuntimeConfig.repo!() ; on no-repo raise/print an actionable error matching the `config :oban_powertools, repo: MyApp.Repo` contract tone (D-08), and exit 2 (cannot-run = error, D-06). resolve_prefix/1: --prefix > Application.get_env(:oban, oban_name_atom) prefix > "public" (D-07/D-10), reading app env WITHOUT starting Oban. Boot via Ecto.Migrator.with_repo(repo_module, fn repo -> findings = ObanPowertools.Doctor.run(repo, prefix: prefix, strict: strict); ObanPowertools.Doctor.Formatter.print(findings, format: ..., prefix: prefix, exit_code: ObanPowertools.Doctor.exit_code_for(findings), oban_version_installed: Oban.Migrations.Postgres.current_version()); ObanPowertools.Doctor.exit_code_for(findings) end, pool_size: 2). Match {:ok, exit_code, _apps} -> System.halt(exit_code) and {:error, reason} -> print cannot-run error + System.halt(2). CRITICAL: call System.halt/1 only AFTER with_repo returns, never inside the callback (Pitfall 3). Do NOT add @requirements. Create test/mix/tasks/oban_powertools.doctor_test.exs as pure ExUnit.Case (no DataCase, no Oban start, no System.halt invocation) asserting: the task is a plain Mix.Task (function_exported? run/1), source declares all five switches, source uses use Mix.Task and not use Igniter.Mix.Task, and unit-test resolve_prefix/resolve_repo behavior if exposed as testable helpers (or assert via source contract). Do not call run/1 in tests (it halts the BEAM).
  </action>
  <verify>
    <automated>mix test test/mix/tasks/oban_powertools.doctor_test.exs --max-failures 3</automated>
  </verify>
  <acceptance_criteria>
    - Source: lib/mix/tasks/oban_powertools.doctor.ex contains `use Mix.Task` and does NOT contain `use Igniter.Mix.Task`
    - Source: contains `Ecto.Migrator.with_repo` and `System.halt`; does NOT contain `@requirements` or `Oban.start_link`
    - Source: contains all five switches `:repo`, `:prefix`, `:oban_name`, `:format`, `:strict`
    - Source: repo resolution uses `Module.safe_concat` / `String.to_existing_atom`; grep shows no `String.to_atom(` on the repo flag
    - Source: `System.halt` does not appear inside the `with_repo` callback (it is in the `case` arms after with_repo returns)
    - Behavior: `mix test test/mix/tasks/oban_powertools.doctor_test.exs` exits 0
  </acceptance_criteria>
  <done>Mix task wires flags → repo/prefix resolution → repo-only with_repo boot → Doctor.run → Formatter.print → System.halt(exit_code) with no Oban start; doctor_test green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Operator smoke-verify doctor against the example host</name>
  <what-built>The full `mix oban_powertools.doctor` CLI: human + JSON output, exit codes 0/1/2, repo-only boot (no Oban started), remediation hints.</what-built>
  <files>n/a (manual operator verification of the built CLI)</files>
  <action>Operator runs the built CLI against the example host and confirms output, exit codes, and read-only boot per the steps below. No code is written in this task — it is a human verification gate after the autonomous CLI work in Tasks 1-2.</action>
  <verify>
    <human-check>Operator confirms healthy-DB exit 0, valid JSON with schema_version, no Oban-start log lines, and a REINDEX/CREATE INDEX remediation hint with exit 2 after dropping an expected index.</human-check>
  </verify>
  <done>Operator types "approved" after confirming all four verification steps pass; or files issues to address.</done>
  <how-to-verify>
    1. From the repo root run: `cd examples/phoenix_host && mix oban_powertools.doctor` (set up the example host DB first if needed via its existing setup task). Expect a sectioned human report and exit 0 on a healthy migrated DB. Confirm NO "Oban started"/queue-processing log lines appear (read-only boot, D-09).
    2. Run `mix oban_powertools.doctor --format json` and pipe to a JSON validator (e.g. `| python3 -m json.tool`); confirm valid JSON with a top-level `schema_version` field. Confirm color codes are absent when piped (ANSI auto-degrade, D-01).
    3. Check the exit code is honest: `mix oban_powertools.doctor; echo "exit=$?"` should print `exit=0` on a healthy DB.
    4. (Optional) Drop an expected index in the example DB (`DROP INDEX CONCURRENTLY IF EXISTS public.oban_jobs_args_index`), rerun, and confirm an error finding with a `REINDEX`/`CREATE INDEX` remediation hint and `exit=2`. Recreate the index afterward.
  </how-to-verify>
  <resume-signal>Type "approved" or describe any incorrect output, missing hints, exit-code mismatch, or any sign Oban was started.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI flags → atoms/SQL | `--repo`, `--oban-name` become atoms/modules; `--prefix` flows to DB (via Plan 01 checks) |
| process → stdout | Findings rendered to human/JSON output; must not leak unexpected data |
| Mix task → runtime | Boot path must start only the repo, never Oban |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-48-05 | Tampering/DoS | `--repo` / `--oban-name` → atom creation | mitigate | Resolve repo via `Module.safe_concat` + `String.to_existing_atom` guarded by `Code.ensure_loaded?`; never `String.to_atom/1` on CLI input (RESEARCH Pitfall 4) |
| T-48-06 | Elevation/Side-effect | repo-only boot must not start Oban | mitigate | `Ecto.Migrator.with_repo/2` boot only; no `@requirements ["app.start"]`, no `Oban.start_link` (D-09; RESEARCH Pitfall 1); human-verify Task 3 confirms no Oban start logs |
| T-48-07 | Repudiation/Integrity | exit code must be honest | mitigate | `System.halt/1` with `exit_code_for/1` value; no-repo/db-unreachable halts with 2, never 0 (D-06); never `raise`/`exit` for exit code (Pattern 2) |
| T-48-08 | Information Disclosure | JSON/human output content | accept | Output contains only schema/index/version metadata and operator-authored remediation strings — no PII, no job args/meta values rendered |
| T-48-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this phase — Jason/ecto_sql/oban/postgrex all already declared (D-02; RESEARCH Package Legitimacy Audit: none) |
</threat_model>

<verification>
- `mix test test/oban_powertools/doctor/formatter_test.exs test/mix/tasks/oban_powertools.doctor_test.exs` is green.
- `grep -nE "use Igniter.Mix.Task|@requirements|Oban\\.start_link|String\\.to_atom\\(" lib/mix/tasks/oban_powertools.doctor.ex` returns nothing.
- `grep -c "Ecto.Migrator.with_repo" lib/mix/tasks/oban_powertools.doctor.ex` is >= 1.
- `grep -c "schema_version" lib/oban_powertools/doctor/formatter.ex` is >= 1.
- Full suite: `mix test` is green.
- Operator smoke-verify (Task 3) approved.
</verification>

<success_criteria>
- `mix oban_powertools.doctor` prints a sectioned human report and exits 0/1/2 honestly; `--format json` emits valid JSON with `schema_version`.
- Repo resolves via --repo (safe) > RuntimeConfig.repo!; no-repo path errors with the config contract tone and exits 2.
- Prefix resolves via --prefix > Oban app env > public, without starting Oban.
- Repo-only `with_repo/2` boot; System.halt after the callback returns; no Oban start path.
- Remediation hints appear in output (OPS-05); index/migration/uniqueness findings surface to the operator (OPS-03/OPS-04).
- Formatter + CLI tests and the full suite green; operator smoke-verify approved.
</success_criteria>

<output>
Create `.planning/phases/48-doctor-health-check-task/48-02-SUMMARY.md` when done.
</output>
