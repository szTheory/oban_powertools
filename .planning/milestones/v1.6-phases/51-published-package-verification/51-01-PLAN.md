---
phase: 51-published-package-verification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - examples/hex_consumer/mix.exs
  - examples/hex_consumer/.formatter.exs
  - examples/hex_consumer/config/config.exs
  - examples/hex_consumer/config/dev.exs
  - examples/hex_consumer/config/test.exs
  - examples/hex_consumer/config/runtime.exs
  - examples/hex_consumer/config/prod.exs
  - examples/hex_consumer/lib/hex_consumer/application.ex
  - examples/hex_consumer/lib/hex_consumer/repo.ex
  - examples/hex_consumer/lib/hex_consumer.ex
  - examples/hex_consumer/lib/hex_consumer_web.ex
  - examples/hex_consumer/lib/hex_consumer_web/endpoint.ex
  - examples/hex_consumer/lib/hex_consumer_web/router.ex
  - examples/hex_consumer/lib/hex_consumer_web/oban_powertools_auth.ex
  - examples/hex_consumer/lib/hex_consumer_web/oban_powertools_display_policy.ex
  - examples/hex_consumer/lib/hex_consumer_web/telemetry.ex
  - examples/hex_consumer/README.md
  - examples/hex_consumer/regenerate.sh
  - .gitignore
autonomous: true
requirements: [REL-04]

must_haves:
  truths:
    - "examples/hex_consumer/ is a compilable mini Phoenix app whose own mix.exs declares {:oban_powertools, \"~> 0.5\"} as a hex dep (per D-02, D-03)"
    - "The consumer app has no oban_web dependency (the native-only path is the verification target, per first-session test refute)"
    - "mix.exs declares elixir: \"~> 1.19\" to match the library minimum"
    - "A regenerate.sh companion exists mirroring examples/phoenix_host/regenerate.sh but inserting the hex dep (per D-03b)"
    - "examples/hex_consumer/mix.lock, deps/, _build/, and priv/repo/migrations/ are gitignored (per D-02b: no committed lock)"
  artifacts:
    - path: "examples/hex_consumer/mix.exs"
      provides: "HexConsumer.MixProject with hex oban_powertools dep, no oban_web, elixir ~> 1.19"
      contains: "{:oban_powertools, \"~> 0.5\"}"
    - path: "examples/hex_consumer/config/test.exs"
      provides: "hex_consumer_test database + Ecto Sandbox pool config"
      contains: "hex_consumer_test"
    - path: "examples/hex_consumer/lib/hex_consumer_web/oban_powertools_auth.ex"
      provides: "HexConsumerWeb.ObanPowertoolsAuth host-owned auth seam with demo_actor/0"
      contains: "def demo_actor"
    - path: "examples/hex_consumer/regenerate.sh"
      provides: "maintainer regeneration script with hex dep insertion + hex.pm-reachability note"
      contains: "hex_consumer"
    - path: ".gitignore"
      provides: "ignore rules for examples/hex_consumer generated/lock artifacts"
      contains: "examples/hex_consumer/mix.lock"
  key_links:
    - from: "examples/hex_consumer/config/config.exs"
      to: "HexConsumerWeb.ObanPowertoolsAuth"
      via: ":oban_powertools auth_module config"
      pattern: "auth_module: HexConsumerWeb.ObanPowertoolsAuth"
    - from: "examples/hex_consumer/mix.exs"
      to: "oban_powertools on hex.pm"
      via: "hex dep declaration"
      pattern: "oban_powertools.*~> 0.5"
---

<objective>
Scaffold the committed `examples/hex_consumer/` mini Phoenix app — a fresh-adopter host whose
own `mix.exs` declares `{:oban_powertools, "~> 0.5"}` as a **true hex dep** (D-02, D-03), distinct
from `examples/phoenix_host` (which uses `path: "../.."`). Every file is a verbatim
namespace-substitution copy of its `examples/phoenix_host/` analog (`PhoenixHost` → `HexConsumer`,
`PhoenixHostWeb` → `HexConsumerWeb`, `:phoenix_host` → `:hex_consumer`, DB-name strings), with the
`mix.exs` dep delta (path → hex, drop `oban_web`), `elixir: "~> 1.19"`, and `.gitignore` entries.

This plan does NOT create the first-session test or seeds (Plan 02) and does NOT touch
`.github/workflows/release.yml` (Plan 03). It produces the app skeleton those plans build on.

Purpose: Stand up the verification target — the published-tarball consumer fixture (REL-04).
Output: A compilable `examples/hex_consumer/` app + `regenerate.sh` + `.gitignore` entries.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/51-published-package-verification/51-CONTEXT.md
@.planning/phases/51-published-package-verification/51-RESEARCH.md
@.planning/phases/51-published-package-verification/51-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold hex_consumer mix.exs, .formatter.exs, README, and .gitignore entries</name>
  <files>examples/hex_consumer/mix.exs, examples/hex_consumer/.formatter.exs, examples/hex_consumer/README.md, .gitignore</files>
  <read_first>
    - examples/phoenix_host/mix.exs (analog being mirrored — copy structure verbatim)
    - examples/phoenix_host/.formatter.exs (analog)
    - examples/phoenix_host/README.md (analog for README shape/tone)
    - .gitignore (the file being edited — note it only covers root-level /_build/, /deps/, and `oban_powertools-*.tar`; subdir paths must be added explicitly)
    - .planning/phases/51-published-package-verification/51-PATTERNS.md §"examples/hex_consumer/mix.exs" (exact line-by-line delta table)
    - .planning/phases/51-published-package-verification/51-RESEARCH.md Focus Q4 (mix.exs deps block, no-committed-lock rationale)
  </read_first>
  <action>
    Create examples/hex_consumer/mix.exs by copying examples/phoenix_host/mix.exs verbatim then
    applying these exact deltas (per D-02, D-03; PATTERNS.md substitution table): module name
    PhoenixHost.MixProject → HexConsumer.MixProject; `app: :phoenix_host` → `app: :hex_consumer`;
    `elixir: "~> 1.15"` → `elixir: "~> 1.19"`; `mod: {PhoenixHost.Application, []}` →
    `mod: {HexConsumer.Application, []}`. In deps/0: change `{:oban_powertools, path: "../.."}` to
    `{:oban_powertools, "~> 0.5"}` (the whole point — hex dep, D-02), and DELETE the
    `{:oban_web, "~> 2.10", optional: true}` line entirely (the first-session test asserts
    `refute html =~ "Oban Web"`). Keep all other deps verbatim including `{:oban, "~> 2.18"}`
    (the installer assumes Oban is a host dep, as phoenix_host declares it). Keep the aliases/0
    block verbatim (only mix-task strings, no module refs). Do NOT commit a mix.lock.

    Create examples/hex_consumer/.formatter.exs as a verbatim copy of examples/phoenix_host/.formatter.exs.

    Create examples/hex_consumer/README.md: a short note explaining this is the hex-dep consumer
    fixture (distinct from phoenix_host's path dep), that it exercises the published tarball + the
    `:files` whitelist, that it REQUIRES hex.pm reachability to resolve `{:oban_powertools, "~> 0.5"}`
    (cannot run offline or against local library changes — by design, per RESEARCH.md Focus Q4), and
    that regenerate.sh is its maintainer companion. Document the intentional no-oban_web difference
    as an explicit (not-a-bug) divergence per D-06.

    Edit .gitignore: append a section adding these four paths (per D-02b, RESEARCH.md Focus Q4) —
    `examples/hex_consumer/mix.lock`, `examples/hex_consumer/priv/repo/migrations/`,
    `examples/hex_consumer/_build/`, `examples/hex_consumer/deps/`. The root .gitignore's `/_build/`
    and `/deps/` are anchored to root only and do NOT cover the subdir, so explicit entries are required.
  </action>
  <verify>
    <automated>test -f examples/hex_consumer/mix.exs &amp;&amp; grep -q '{:oban_powertools, "~> 0.5"}' examples/hex_consumer/mix.exs &amp;&amp; ! grep -q 'oban_web' examples/hex_consumer/mix.exs &amp;&amp; grep -q 'elixir: "~> 1.19"' examples/hex_consumer/mix.exs &amp;&amp; grep -q 'examples/hex_consumer/mix.lock' .gitignore</automated>
  </verify>
  <done>mix.exs declares the hex oban_powertools dep, omits oban_web, pins elixir ~> 1.19; README and .formatter.exs exist; .gitignore ignores hex_consumer mix.lock/migrations/_build/deps.</done>
</task>

<task type="auto">
  <name>Task 2: Scaffold hex_consumer config/, lib/, and host-owned seam modules</name>
  <files>examples/hex_consumer/config/config.exs, examples/hex_consumer/config/dev.exs, examples/hex_consumer/config/test.exs, examples/hex_consumer/config/runtime.exs, examples/hex_consumer/config/prod.exs, examples/hex_consumer/lib/hex_consumer.ex, examples/hex_consumer/lib/hex_consumer/application.ex, examples/hex_consumer/lib/hex_consumer/repo.ex, examples/hex_consumer/lib/hex_consumer_web.ex, examples/hex_consumer/lib/hex_consumer_web/endpoint.ex, examples/hex_consumer/lib/hex_consumer_web/router.ex, examples/hex_consumer/lib/hex_consumer_web/oban_powertools_auth.ex, examples/hex_consumer/lib/hex_consumer_web/oban_powertools_display_policy.ex, examples/hex_consumer/lib/hex_consumer_web/telemetry.ex</files>
  <read_first>
    - examples/phoenix_host/config/config.exs, dev.exs, test.exs, runtime.exs, prod.exs (config analogs)
    - examples/phoenix_host/lib/phoenix_host.ex, lib/phoenix_host/application.ex, lib/phoenix_host/repo.ex (lib analogs)
    - examples/phoenix_host/lib/phoenix_host_web.ex, lib/phoenix_host_web/endpoint.ex, lib/phoenix_host_web/router.ex, lib/phoenix_host_web/telemetry.ex (web analogs)
    - examples/phoenix_host/lib/phoenix_host_web/oban_powertools_auth.ex (host-owned auth seam with demo_actor/0 — copied, NOT regenerated)
    - examples/phoenix_host/lib/phoenix_host_web/oban_powertools_display_policy.ex (host-owned display policy seam)
    - .planning/phases/51-published-package-verification/51-PATTERNS.md §config/* and §"Namespace Substitution Rule" (per-file occurrence tables)
  </read_first>
  <action>
    Copy each examples/phoenix_host/ analog listed in `<files>` to its examples/hex_consumer/
    counterpart, applying the global Namespace Substitution Rule from PATTERNS.md verbatim:
    `PhoenixHost` → `HexConsumer`, `PhoenixHostWeb` → `HexConsumerWeb`, `PhoenixHost.Repo` →
    `HexConsumer.Repo`, `PhoenixHost.PubSub` → `HexConsumer.PubSub`, `:phoenix_host` →
    `:hex_consumer`, and DB-name strings `phoenix_host_dev` → `hex_consumer_dev`,
    `phoenix_host_test` → `hex_consumer_test`. No logic changes in any file.

    In config/config.exs the `:oban_powertools` block (PATTERNS.md L222-226) becomes
    `repo: HexConsumer.Repo, auth_module: HexConsumerWeb.ObanPowertoolsAuth,
    display_policy: HexConsumerWeb.ObanPowertoolsDisplayPolicy`. In config/test.exs the
    `HexConsumer.Repo` database becomes `"hex_consumer_test#{System.get_env("MIX_TEST_PARTITION")}"`
    with `pool: Ecto.Adapters.SQL.Sandbox` preserved verbatim. In config/runtime.exs, per PATTERNS.md
    note, omit the phoenix_host-specific `reverse_proxy_headers`/`websocket_transport_expected` keys
    (lines 26-28 of the analog) — they are not part of the minimal verification surface; rename all
    other blocks.

    Copy oban_powertools_auth.ex (preserving `demo_actor/0`, which seeds.exs and the first-session
    test both call) and oban_powertools_display_policy.ex as host-owned seams with module rename only —
    these are checked-in host code, not installer-generated. After copying, run
    `cd examples/hex_consumer && mix format --check-formatted` against the new files (format if needed).
  </action>
  <verify>
    <automated>test -f examples/hex_consumer/config/test.exs &amp;&amp; grep -q 'hex_consumer_test' examples/hex_consumer/config/test.exs &amp;&amp; grep -q 'auth_module: HexConsumerWeb.ObanPowertoolsAuth' examples/hex_consumer/config/config.exs &amp;&amp; grep -q 'def demo_actor' examples/hex_consumer/lib/hex_consumer_web/oban_powertools_auth.ex &amp;&amp; ! grep -rq 'PhoenixHost' examples/hex_consumer/config examples/hex_consumer/lib</automated>
  </verify>
  <done>All config and lib files exist with HexConsumer/HexConsumerWeb namespaces; no residual `PhoenixHost` token in config/ or lib/; auth seam exposes demo_actor/0; oban_powertools config points at the renamed seams.</done>
</task>

<task type="auto">
  <name>Task 3: Create regenerate.sh maintainer companion with hex dep insertion</name>
  <files>examples/hex_consumer/regenerate.sh</files>
  <read_first>
    - examples/phoenix_host/regenerate.sh (analog — copy structure verbatim: set -euo pipefail, ROOT_DIR derivation, replace_once ruby helper, mix phx.new invocation, closing heredoc)
    - .planning/phases/51-published-package-verification/51-PATTERNS.md §"examples/hex_consumer/regenerate.sh" (exact deltas + hex-dep replace_once call)
    - .planning/phases/51-published-package-verification/51-RESEARCH.md Focus Q4 (regenerate.sh design + internet-access warning)
  </read_first>
  <action>
    Create examples/hex_consumer/regenerate.sh by copying examples/phoenix_host/regenerate.sh
    verbatim, then applying these deltas (per D-03b, PATTERNS.md): set
    `CANONICAL_DIR="${ROOT_DIR}/examples/hex_consumer"` and
    `TARGET_DIR="${ROOT_DIR}/examples/.hex_consumer_regen"`. In the `mix phx.new` invocation change
    `--app phoenix_host` → `--app hex_consumer` and `--module PhoenixHost` → `--module HexConsumer`
    (keep `--database postgres --no-assets --no-dashboard --no-mailer --no-gettext --no-install`).
    Change the `replace_once` dep-insertion call so the inserted block is
    `{:postgrex, ">= 0.0.0"},\n      {:oban, "~> 2.18"},\n      {:oban_powertools, "~> 0.5"},`
    (hex dep, NO oban_web line — unlike the phoenix_host analog which inserts path dep + oban_web).
    Keep the `mix deps.get` + `mix oban_powertools.install` subshell verbatim. Update the closing
    heredoc to add the warning: this script requires hex.pm reachability (hex dep, not path dep) and
    should only be run when oban_powertools is live on hex.pm. Make the script executable
    (`chmod +x examples/hex_consumer/regenerate.sh`). Do NOT execute the script (it needs hex.pm +
    mix phx.new and is a maintainer tool, not part of this plan's verification).
  </action>
  <verify>
    <automated>test -x examples/hex_consumer/regenerate.sh &amp;&amp; bash -n examples/hex_consumer/regenerate.sh &amp;&amp; grep -q 'hex_consumer' examples/hex_consumer/regenerate.sh &amp;&amp; grep -q '{:oban_powertools, "~> 0.5"}' examples/hex_consumer/regenerate.sh &amp;&amp; ! grep -q 'oban_web' examples/hex_consumer/regenerate.sh</automated>
  </verify>
  <done>regenerate.sh is executable, passes `bash -n` syntax check, targets hex_consumer dirs, inserts the hex oban_powertools dep without oban_web, and documents the hex.pm-reachability requirement.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo → committed example fixture | New committed app scaffold; no runtime/network surface, no secrets. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-51-01 | Tampering | `examples/hex_consumer/mix.exs` `:oban_web` omission / dep edits | accept | Intentional native-only target (first-session test asserts `refute html =~ "Oban Web"`); documented in README per D-06. No runtime attack surface added to the library. |
| T-51-02 | Information Disclosure | committed config files (secret_key_base, DB creds) | accept | Values are the same non-production fixture constants already committed in `examples/phoenix_host` (test secret_key_base, `postgres`/`postgres` localhost creds). No real secret introduced. |
| T-51-SC | Tampering | npm/hex/mix installs in this plan | mitigate | This plan installs NO packages — it only writes source files. `mix deps.get` against hex.pm happens in Plan 03's CI job, not here. No package-legitimacy checkpoint required. |
</threat_model>

<verification>
- All files in `<files_modified>` exist with HexConsumer/HexConsumerWeb namespaces.
- `grep -rq 'PhoenixHost' examples/hex_consumer/config examples/hex_consumer/lib` returns nothing.
- mix.exs declares `{:oban_powertools, "~> 0.5"}`, omits `oban_web`, and pins `elixir: "~> 1.19"`.
- `.gitignore` ignores `examples/hex_consumer/mix.lock`, `priv/repo/migrations/`, `_build/`, `deps/`.
- `:files` whitelist in the library's root `mix.exs` is NOT touched by this plan.
</verification>

<success_criteria>
- `examples/hex_consumer/` is a complete, compilable Phoenix app skeleton (config + lib + seams).
- The consumer declares a true hex dep on oban_powertools and no oban_web dep.
- `regenerate.sh` exists, is executable, and inserts the hex dep with a hex.pm-reachability note.
- No committed `mix.lock`; generated/lock artifacts are gitignored.
</success_criteria>

<output>
Create `.planning/phases/51-published-package-verification/51-01-SUMMARY.md` when done.
</output>
