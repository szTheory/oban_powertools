---
phase: 51-published-package-verification
plan: 02
type: execute
wave: 2
depends_on: ["51-01"]
files_modified:
  - examples/hex_consumer/test/test_helper.exs
  - examples/hex_consumer/test/support/conn_case.ex
  - examples/hex_consumer/test/support/data_case.ex
  - examples/hex_consumer/test/hex_consumer_web/oban_powertools_first_session_test.exs
  - examples/hex_consumer/priv/repo/seeds.exs
autonomous: true
requirements: [REL-04]

must_haves:
  truths:
    - "examples/hex_consumer/ has a first-session test that drives /ops/jobs/cron, pauses nightly_sync, and asserts DB state + audit evidence (per D-04, full operator session)"
    - "The nightly_sync Cron.Entry seed matches the field values the test asserts (source: fixture, overlap_policy: queue_one, catch_up_policy: latest)"
    - "The first-session test passes green locally when the consumer resolves oban_powertools (proven via a temporary path-dep swap, then reverted to the committed ~> 0.5 hex dep)"
    - "test/support fixtures (ConnCase, DataCase) use Ecto Sandbox against HexConsumer.Repo"
  artifacts:
    - path: "examples/hex_consumer/test/hex_consumer_web/oban_powertools_first_session_test.exs"
      provides: "REL-04 operator-session proof: cron pause + audit evidence"
      contains: "pause_cron_entry"
    - path: "examples/hex_consumer/priv/repo/seeds.exs"
      provides: "nightly_sync Cron.Entry fixture the test depends on"
      contains: "nightly_sync"
    - path: "examples/hex_consumer/test/support/conn_case.ex"
      provides: "HexConsumerWeb.ConnCase with sandbox + verified_routes"
      contains: "HexConsumerWeb.ConnCase"
    - path: "examples/hex_consumer/test/test_helper.exs"
      provides: "ExUnit start + sandbox manual mode for HexConsumer.Repo"
      contains: "HexConsumer.Repo"
  key_links:
    - from: "examples/hex_consumer/test/hex_consumer_web/oban_powertools_first_session_test.exs"
      to: "examples/hex_consumer/priv/repo/seeds.exs"
      via: "nightly_sync fixture the test asserts is present"
      pattern: "nightly_sync"
    - from: "examples/hex_consumer/test/hex_consumer_web/oban_powertools_first_session_test.exs"
      to: "HexConsumerWeb.ObanPowertoolsAuth.demo_actor/0"
      via: "actor injected into LiveView session"
      pattern: "HexConsumerWeb.ObanPowertoolsAuth.demo_actor"
---

<objective>
Add the full operator-session proof to `examples/hex_consumer/`: the `nightly_sync` seed and the
first-session LiveView test (D-04). The test is a verbatim namespace-substitution copy of
`examples/phoenix_host/test/phoenix_host_web/oban_powertools_first_session_test.exs` — install →
boot → drive `/ops/jobs/cron` → pause the `nightly_sync` cron entry → assert DB state + audit
evidence, all synchronous (no Oban async-pickup flakiness). Prove it green LOCALLY by temporarily
swapping the consumer's dep to `path: "../.."` (so resolution does not require the published version
on hex.pm), then revert to the committed `{:oban_powertools, "~> 0.5"}` hex dep. The CI job that runs
this test against the *published* tarball is Plan 03.

Purpose: Create and locally prove the REL-04 first-session assertion before wiring it into CI.
Output: Seeds + test + support fixtures, proven green via path-dep, committed with the hex dep restored.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/51-published-package-verification/51-CONTEXT.md
@.planning/phases/51-published-package-verification/51-RESEARCH.md
@.planning/phases/51-published-package-verification/51-PATTERNS.md
@.planning/phases/51-published-package-verification/51-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create test infrastructure (test_helper, ConnCase, DataCase) and the nightly_sync seed</name>
  <files>examples/hex_consumer/test/test_helper.exs, examples/hex_consumer/test/support/conn_case.ex, examples/hex_consumer/test/support/data_case.ex, examples/hex_consumer/priv/repo/seeds.exs</files>
  <read_first>
    - examples/phoenix_host/test/test_helper.exs (analog — 2 lines)
    - examples/phoenix_host/test/support/conn_case.ex (analog)
    - examples/phoenix_host/test/support/data_case.ex (analog)
    - examples/phoenix_host/priv/repo/seeds.exs (analog — nightly_sync fixture; all attr values must match exactly)
    - examples/hex_consumer/lib/hex_consumer_web/oban_powertools_auth.ex (the demo_actor/0 seed calls; created in Plan 01)
    - .planning/phases/51-published-package-verification/51-PATTERNS.md §test_helper / §conn_case / §data_case / §seeds (per-file occurrence tables)
  </read_first>
  <action>
    Copy each examples/phoenix_host/ analog to its examples/hex_consumer/ counterpart applying only
    the Namespace Substitution Rule (PhoenixHost → HexConsumer, PhoenixHostWeb → HexConsumerWeb,
    PhoenixHost.Repo → HexConsumer.Repo, PhoenixHost.DataCase → HexConsumer.DataCase), no logic changes:

    test/test_helper.exs → `ExUnit.start()` + `Ecto.Adapters.SQL.Sandbox.mode(HexConsumer.Repo, :manual)`.
    test/support/conn_case.ex → module `HexConsumerWeb.ConnCase`, `@endpoint HexConsumerWeb.Endpoint`,
    `use HexConsumerWeb, :verified_routes`, `import HexConsumerWeb.ConnCase`, and
    `HexConsumer.DataCase.setup_sandbox(tags)`.
    test/support/data_case.ex → module `HexConsumer.DataCase`, `alias HexConsumer.Repo`,
    `Ecto.Adapters.SQL.Sandbox.start_owner!(HexConsumer.Repo, shared: not tags[:async])`.

    priv/repo/seeds.exs → copy the analog verbatim; rename `alias PhoenixHost.Repo` →
    `alias HexConsumer.Repo` and `PhoenixHostWeb.ObanPowertoolsAuth.demo_actor()` →
    `HexConsumerWeb.ObanPowertoolsAuth.demo_actor()`, and the closing IO.puts label
    "Seeded PhoenixHost first-session fixture:" → "Seeded HexConsumer first-session fixture:". CRITICAL
    (per D-04, PATTERNS.md): ALL `nightly_sync_attrs` values stay identical — `name: "nightly_sync"`,
    `source: "fixture"`, `overlap_policy: "queue_one"`, `catch_up_policy: "latest"`, `max_catch_up: 1`,
    `expression: "0 2 * * *"` — because the first-session test asserts these exact field values and
    the "Queue One" / "Latest Only" UI labels derived from them.
  </action>
  <verify>
    <automated>grep -q 'HexConsumer.Repo' examples/hex_consumer/test/test_helper.exs &amp;&amp; grep -q 'HexConsumerWeb.ConnCase' examples/hex_consumer/test/support/conn_case.ex &amp;&amp; grep -q 'HexConsumer.DataCase' examples/hex_consumer/test/support/data_case.ex &amp;&amp; grep -q 'nightly_sync' examples/hex_consumer/priv/repo/seeds.exs &amp;&amp; grep -q 'overlap_policy: "queue_one"' examples/hex_consumer/priv/repo/seeds.exs &amp;&amp; ! grep -rq 'PhoenixHost' examples/hex_consumer/test examples/hex_consumer/priv</automated>
  </verify>
  <done>test_helper, ConnCase, DataCase, and seeds exist with HexConsumer namespaces; nightly_sync fixture preserves the exact attr values the test asserts; no residual PhoenixHost token under test/ or priv/.</done>
</task>

<task type="auto">
  <name>Task 2: Create the first-session test and prove it green locally via a temporary path-dep swap</name>
  <files>examples/hex_consumer/test/hex_consumer_web/oban_powertools_first_session_test.exs</files>
  <read_first>
    - examples/phoenix_host/test/phoenix_host_web/oban_powertools_first_session_test.exs (analog — 91 lines, copy 100% verbatim except 4 namespace substitutions)
    - examples/hex_consumer/mix.exs (the committed hex dep that gets temporarily swapped then restored)
    - examples/hex_consumer/config/test.exs (DB name + sandbox config from Plan 01)
    - .planning/phases/51-published-package-verification/51-PATTERNS.md §oban_powertools_first_session_test.exs (the 4-line substitution summary)
    - .planning/phases/51-published-package-verification/51-RESEARCH.md Focus Q3 (the 11-step assertion flow + D-04 full flow)
  </read_first>
  <action>
    Create examples/hex_consumer/test/hex_consumer_web/oban_powertools_first_session_test.exs by
    copying the analog 100% verbatim and applying ONLY these four substitutions (PATTERNS.md):
    `PhoenixHostWeb.ObanPowertoolsFirstSessionTest` → `HexConsumerWeb.ObanPowertoolsFirstSessionTest`;
    `use PhoenixHostWeb.ConnCase` → `use HexConsumerWeb.ConnCase`;
    `alias PhoenixHost.Repo` → `alias HexConsumer.Repo`;
    `PhoenixHostWeb.ObanPowertoolsAuth` → `HexConsumerWeb.ObanPowertoolsAuth`. Every LiveView selector,
    assertion string (`"nightly_sync"`, `"Queue One"`, `"Latest Only"`, `refute html =~ "Oban Web"`,
    `"cron.paused"`), DB query, and field name stays identical.

    Prove green LOCALLY without depending on the published hex version: temporarily edit
    examples/hex_consumer/mix.exs to swap `{:oban_powertools, "~> 0.5"}` → `{:oban_powertools, path: "../.."}`
    (so it resolves the in-repo library, exactly mirroring how phoenix_host runs). Then run:
    `cd examples/hex_consumer && mix deps.get && MIX_ENV=test mix ecto.create && MIX_ENV=test mix ecto.migrate`
    — note ecto.migrate runs the migrations the installer writes; if the installer has not yet been
    run in this tree, run `MIX_ENV=test mix oban_powertools.install` first (Igniter, non-interactive
    per RESEARCH.md Pitfall D). Then `MIX_ENV=test mix run priv/repo/seeds.exs` and
    `MIX_ENV=test mix test test/hex_consumer_web/oban_powertools_first_session_test.exs --trace`.
    The test MUST pass. After it passes, REVERT mix.exs back to `{:oban_powertools, "~> 0.5"}` (the
    committed state must be the hex dep — the path swap is local proof only, never committed) and run
    `git diff examples/hex_consumer/mix.exs` to confirm no dep-line change remains. The generated
    priv/repo/migrations/ are gitignored (Plan 01), so they will not appear in the commit.
  </action>
  <verify>
    <automated>grep -q 'HexConsumerWeb.ObanPowertoolsFirstSessionTest' examples/hex_consumer/test/hex_consumer_web/oban_powertools_first_session_test.exs &amp;&amp; grep -q 'pause_cron_entry' examples/hex_consumer/test/hex_consumer_web/oban_powertools_first_session_test.exs &amp;&amp; grep -q 'refute html =~ "Oban Web"' examples/hex_consumer/test/hex_consumer_web/oban_powertools_first_session_test.exs &amp;&amp; grep -q '{:oban_powertools, "~> 0.5"}' examples/hex_consumer/mix.exs &amp;&amp; ! grep -q 'path: "../.."' examples/hex_consumer/mix.exs</automated>
  </verify>
  <done>The first-session test exists with the 4 namespace substitutions; it passed locally under a path-dep swap (cron pause + audit assertions green); mix.exs is restored to the committed hex dep with no path remnant.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test fixture → committed repo | New committed test + seed fixture; no runtime/network surface in the committed artifact. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-51-03 | Tampering | local path-dep swap accidentally committed | mitigate | Task 2 explicitly reverts mix.exs to the hex dep and gates on `git diff` showing no dep-line change; the verify step asserts the hex dep is present and `path: "../.."` is absent. |
| T-51-04 | Tampering | generated migrations committed (drift risk) | mitigate | `examples/hex_consumer/priv/repo/migrations/` is gitignored (Plan 01) — migrations are regenerated from the resolved tarball each CI run, never committed (RESEARCH.md A2). |
| T-51-SC | Tampering | hex/mix installs in this plan | accept | Local proof resolves `oban_powertools` via `path:` (in-repo, no new external package) and standard Phoenix/Ecto deps already audited for phoenix_host. No new package legitimacy concern. |
</threat_model>

<verification>
- First-session test passed locally (path-dep proof) exercising the full D-04 flow.
- `nightly_sync` seed values match the test's asserted fields/labels.
- `examples/hex_consumer/mix.exs` committed state declares `{:oban_powertools, "~> 0.5"}`; no `path:` remnant.
- No `examples/hex_consumer/priv/repo/migrations/` files staged for commit (gitignored).
- The library root `mix.exs` `:files` whitelist is NOT touched.
</verification>

<success_criteria>
- The first-session operator proof (cron pause + audit evidence) exists and is locally green.
- Seeds and support fixtures are namespace-correct and sandbox-wired.
- The committed dep remains the hex dep; the path swap was local-only.
</success_criteria>

<output>
Create `.planning/phases/51-published-package-verification/51-02-SUMMARY.md` when done.
</output>
