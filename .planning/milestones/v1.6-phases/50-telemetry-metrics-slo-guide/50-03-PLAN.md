---
phase: 50-telemetry-metrics-slo-guide
plan: 03
type: execute
wave: 1
depends_on: ["50-01"]
files_modified:
  - guides/telemetry-and-slos.md
autonomous: true
requirements: [TEL-03]
must_haves:
  truths:
    - "guides/telemetry-and-slos.md exists and renders under the Operations group in mix docs"
    - "The guide shows hosts how to wire ObanPowertools.Telemetry.metrics() into their own Telemetry supervisor with a generic reporter (ConsoleReporter example), never bundling one"
    - "The guide explicitly distinguishes Oban-core golden signals ([:oban, :job, :stop|:exception]) from Powertools control-plane SLIs"
    - "The guide frames Parapet as one consumer and explicitly states no oban_met dependency is required"
  artifacts:
    - path: "guides/telemetry-and-slos.md"
      provides: "4-part reporter-agnostic telemetry + SLO Operations guide (D-07)"
      contains: "ObanPowertools.Telemetry.metrics()"
      min_lines: 60
  key_links:
    - from: "guides/telemetry-and-slos.md"
      to: "mix.exs groups_for_extras Operations"
      via: "filename registered in Plan 01"
      pattern: "telemetry-and-slos"
    - from: "guide section 1 wiring example"
      to: "ObanPowertools.Telemetry.metrics()"
      via: "host Telemetry supervisor child spec with ConsoleReporter"
      pattern: "ObanPowertools.Telemetry.metrics"
---

<objective>
Write `guides/telemetry-and-slos.md` (TEL-03): a single, reporter-agnostic Operations guide that
documents wiring `ObanPowertools.Telemetry.metrics/0` into a host Telemetry supervisor, draws the
explicit seam between Oban-core golden signals and Powertools control-plane SLIs, and frames Parapet
as one SLO consumer with no `oban_met` dependency. Follows the locked 4-part structure (D-07) and the
Operations-guide tone of the existing donors.

Purpose: SC#3 ("a Parapet/SLO telemetry guide documents golden-signals/SLO setup with no oban_met
dependency"). The group entry was reserved in Plan 01; this plan fills it with content.
Output: new `guides/telemetry-and-slos.md`; `mix docs` builds clean and groups it under Operations.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/50-telemetry-metrics-slo-guide/50-CONTEXT.md
@.planning/phases/50-telemetry-metrics-slo-guide/50-RESEARCH.md
@.planning/phases/50-telemetry-metrics-slo-guide/50-PATTERNS.md
@guides/optional-oban-web-bridge.md
@guides/production-hardening.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the 4-part telemetry-and-slos Operations guide</name>
  <files>guides/telemetry-and-slos.md</files>
  <read_first>
    - guides/optional-oban-web-bridge.md (tone/structure donor — opt-in framing, "what it is / what it is not" sections, host caveats)
    - guides/production-hardening.md (tone donor — imperative, checklist-friendly Operations prose)
    - lib/oban_powertools/telemetry.ex (the frozen `@contract` — source of the exact event names, families, and low-cardinality tag sets the guide documents; e.g. limiter.blocked tags blocker_code/resource/scope, lifeline tags incident_class/outcome, workflow.workflow_terminal tags state/outcome/terminal_cause)
    - .planning/phases/50-telemetry-metrics-slo-guide/50-RESEARCH.md (§"Pattern 4: Supervision Tree Wiring" host example + §"Parapet Integration Framing" — the seam, tenets, and burn-rate framing)
    - .planning/phases/50-telemetry-metrics-slo-guide/50-PATTERNS.md (§"guides/telemetry-and-slos.md — new Operations guide" — required 4-part structure, host wiring sample, "not bundled" framing)
  </read_first>
  <action>
    Create `guides/telemetry-and-slos.md` with a lead paragraph stating the metrics surface is opt-in and
    reporter-agnostic, then the four sections locked in D-07. Section 1 "Wire it up": instruct the host to
    add `{:telemetry_metrics, "~> 1.0"}` and optionally `{:telemetry_poller, "~> 1.0"}` to THEIR deps, then
    mount `ObanPowertools.Telemetry.metrics()` in the host's own `Telemetry` supervisor child spec, using
    `Telemetry.Metrics.ConsoleReporter` as the safe, zero-extra-dependency example reporter (state the host
    swaps it for their production reporter). Use the host-supervisor example from RESEARCH Pattern 4 verbatim
    in style (a `MyApp.Telemetry` Supervisor whose children include `{Telemetry.Metrics.ConsoleReporter, metrics:
    ObanPowertools.Telemetry.metrics()}` and an optional `{:telemetry_poller, ...}`). Section 2 "The four golden
    signals for Oban-backed work": be explicit that latency and throughput come from Oban-core `[:oban, :job, :stop]`
    (`duration`, `queue_time`) and errors from `[:oban, :job, :exception]` — these are Oban-core, NOT Powertools
    metrics; note saturation (live queue depth) is host/Oban-sourced and deferred to v1.9 (QRY-06) per D-05; state
    plainly that `metrics/0` deliberately does NOT re-emit these (D-01). Section 3 "Powertools control-plane SLIs":
    enumerate what `metrics/0` adds that Oban core cannot see — limiter saturation (`oban_powertools.limiter.blocked.count`
    by blocker_code/resource/scope), lifeline repair/incident outcomes (by incident_class/outcome), workflow terminal
    causes (`oban_powertools.workflow.workflow_terminal.count` by outcome/terminal_cause), cron overlap/run-now — using
    only the contract's low-cardinality tags. Section 4 "Feeding Parapet SLOs": explain Powertools telemetry drops cleanly
    into Parapet because the frozen contract already satisfies Parapet's "Telemetry as a Strict Public API" and
    "Cardinality Safety" tenets; give one burn-rate SLO framing example (e.g. repair success rate on
    `oban_powertools.lifeline.repair_executed.count` by `outcome`); frame Parapet as ONE consumer (works equally with
    Prometheus + Grafana without Parapet); include an explicit callout that NO `oban_met` dependency is required, mentioned,
    or needed. Close with a "What this is not" section mirroring the optional-oban-web-bridge donor: metrics/0 returns
    definitions not a reporter or process; Powertools starts no Telemetry supervisor; reporter choice is the host's; tag
    values are strings (e.g. `scope: "partitioned"`). Keep prose tight and imperative; ensure all fenced Elixir code
    samples are syntactically valid so `mix docs` builds clean.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/oban_powertools && test -f guides/telemetry-and-slos.md && grep -q 'ObanPowertools.Telemetry.metrics' guides/telemetry-and-slos.md && grep -qi 'oban_met' guides/telemetry-and-slos.md && mix docs 2>&1 | grep -vq 'error'</automated>
  </verify>
  <acceptance_criteria>
    - CLI: `test -f guides/telemetry-and-slos.md` succeeds.
    - CLI: `mix docs` builds without error and the guide appears under the **Operations** group in the generated sidebar (the group entry was added in Plan 01).
    - Source assertion: the guide contains all four D-07 sections (wire-it-up, four golden signals, Powertools control-plane SLIs, feeding Parapet SLOs).
    - Source assertion: section 1 references `ObanPowertools.Telemetry.metrics()` inside a host Telemetry supervisor child spec and names `Telemetry.Metrics.ConsoleReporter` as the example reporter (no bundled reporter).
    - Source assertion: section 2 explicitly attributes latency/throughput/errors to Oban-core `[:oban, :job, :stop|:exception]` and states Powertools does not re-emit them (D-01); saturation deferred to v1.9 (D-05).
    - Source assertion: the guide contains an explicit "no `oban_met` dependency" callout and frames Parapet as one consumer.
  </acceptance_criteria>
  <done>Guide created with all 4 D-07 sections, reporter-agnostic (ConsoleReporter example only), explicit Oban-core/Powertools seam, no-oban_met callout; mix docs builds clean and groups it under Operations.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| guide examples → host implementation | Wiring guidance must not instruct hosts to expose high-cardinality tags or bundle a reporter Powertools doesn't own |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-50-05 | Information Disclosure | guide tag examples | mitigate | Guide documents only the frozen-contract low-cardinality tags (e.g. blocker_code/resource/scope, incident_class/outcome); it never shows tagging by job_id/args, reinforcing SC-4 at the documentation layer |
| T-50-06 | Tampering / scope-coupling | guide dependency framing | accept | Guide is reporter-agnostic and uses dependency-free `ConsoleReporter` for examples; explicit "no oban_met dependency" and "host owns the reporter" callouts prevent hosts from inferring a hidden coupling. No code threat — documentation only |
</threat_model>

<verification>
- `test -f guides/telemetry-and-slos.md` succeeds.
- `mix docs` exits 0 with no `error` in output; guide grouped under Operations.
- `grep -q 'ObanPowertools.Telemetry.metrics' guides/telemetry-and-slos.md` succeeds.
- `grep -qi 'oban_met' guides/telemetry-and-slos.md` succeeds (the explicit no-dependency callout).
</verification>

<success_criteria>
- SC#3: Parapet/SLO guide documents golden-signals/SLO setup with no oban_met dependency.
- D-06/D-07 honored: single guide, Operations group, 4-part structure.
- Reporter-agnostic: ConsoleReporter example only; no bundled reporter.
</success_criteria>

<output>
Create `.planning/phases/50-telemetry-metrics-slo-guide/50-03-SUMMARY.md` when done.
</output>
