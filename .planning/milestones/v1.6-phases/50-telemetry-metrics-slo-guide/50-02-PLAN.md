---
phase: 50-telemetry-metrics-slo-guide
plan: 02
type: execute
wave: 1
depends_on: ["50-01"]
files_modified:
  - lib/oban_powertools/telemetry.ex
autonomous: true
requirements: [TEL-01, TEL-02]
must_haves:
  truths:
    - "ObanPowertools.Telemetry.metrics/0 returns a non-empty list of Telemetry.Metrics definitions over the 5 frozen families"
    - "Every metric event_name is within a frozen @contract family; no Oban-core [:oban, :job, *] metrics are emitted"
    - "Every metric :tags list is a subset of the contract's per-family (per-suffix for workflow) allowed metadata keys — no job_id/args/reason"
    - "metrics/0 raises a clear actionable error naming :telemetry_metrics when Telemetry.Metrics is not loaded (it does NOT return [])"
    - "The library still compiles in a prod tree without telemetry_metrics present"
  artifacts:
    - path: "lib/oban_powertools/telemetry.ex"
      provides: "metrics/0 function returning Telemetry.Metrics counter definitions for the 5 frozen families, with a Code.ensure_loaded? guard"
      contains: "def metrics"
      min_lines: 90
  key_links:
    - from: "lib/oban_powertools/telemetry.ex metrics/0"
      to: "Telemetry.Metrics.counter/2"
      via: "import Telemetry.Metrics inside the function body (never at module-attribute level)"
      pattern: "counter\\("
    - from: "metrics/0 guard"
      to: "Code.ensure_loaded?(Telemetry.Metrics)"
      via: "unless guard that raises, mirroring application.ex:25"
      pattern: "Code.ensure_loaded\\?\\(Telemetry.Metrics\\)"
    - from: "metric :tags"
      to: "@contract per-family allowed metadata keys"
      via: "tags hand-picked as a subset of contract keys; verified by the tag-containment test"
      pattern: "tags:"
---

<objective>
Implement `ObanPowertools.Telemetry.metrics/0` (TEL-01) inside the existing `telemetry.ex` module:
a pure function returning a list of `Telemetry.Metrics` counter definitions covering exactly the
5 frozen Powertools event families, with tags drawn strictly from the frozen `@contract` (SC-4
cardinality safety). Gate the function on `Code.ensure_loaded?(Telemetry.Metrics)` so that calling
it without the optional dep raises a clear, actionable error rather than returning `[]` (TEL-02 /
D-04). The Wave 0 RED tests turn GREEN here.

Purpose: SC#1 ("metrics/0 returns Telemetry.Metrics definitions over the frozen low-cardinality
contract") and SC#4 ("tags stay within the frozen contract, no job_id/args") are this plan's core
deliverable; SC#2's absence path (raise, not silent failure) is the guard.
Output: new `metrics/0` (+ private builder if used) in `lib/oban_powertools/telemetry.ex`; the two
Wave 0 tests pass; prod-tree compile clean.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/50-telemetry-metrics-slo-guide/50-CONTEXT.md
@.planning/phases/50-telemetry-metrics-slo-guide/50-RESEARCH.md
@.planning/phases/50-telemetry-metrics-slo-guide/50-PATTERNS.md
@.planning/phases/50-telemetry-metrics-slo-guide/50-VALIDATION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement metrics/0 with Code.ensure_loaded? guard over the frozen contract</name>
  <files>lib/oban_powertools/telemetry.ex</files>
  <read_first>
    - lib/oban_powertools/telemetry.ex (the file being modified — `@contract` at lines 32-46 is the single source of truth for families/suffixes/allowed tags; the 5 `execute_*_event/3` emitters at lines 56-94 confirm the live event suffixes)
    - lib/oban_powertools/application.ex (line ~24-29 — the canonical `Code.ensure_loaded?/1` guard shape to mirror; note application.ex skips silently, but metrics/0 must RAISE because the caller explicitly asked for metrics)
    - .planning/phases/50-telemetry-metrics-slo-guide/50-PATTERNS.md (§"lib/oban_powertools/telemetry.ex — add metrics/0" — the complete metrics/0 list to copy, the compile-time constraint, and the sum/2 omission rationale)
    - .planning/phases/50-telemetry-metrics-slo-guide/50-RESEARCH.md (§"Concrete Metric List" + §"Common Pitfalls" — per-family tag selection and the archived_count/pruned_count metadata-not-measurement trap)
    - test/oban_powertools/telemetry_test.exs (the RED tests from Plan 01 — `metrics/0 returns a non-empty list...` and `metrics/0 tags stay within frozen contract` — these define the GREEN target)
  </read_first>
  <behavior>
    - metrics/0 returns a list of Telemetry.Metrics.Counter structs (RED test 1 → GREEN: non-empty list, every element struct in [Counter, Sum]).
    - Every metric's :tags is a subset of @contract's allowed keys for that family+suffix (RED test 2 → GREEN: tag-containment).
    - Calling metrics/0 when Telemetry.Metrics is unloaded raises a RuntimeError whose message names `:telemetry_metrics` and tells the host to add it (not `[]`).
    - Each metric event_name is `[:oban_powertools, family, suffix]` — never `[:oban, :job, *]` (D-01).
  </behavior>
  <action>
    Add a public `metrics/0` function to `ObanPowertools.Telemetry`. As the first line of the body, guard
    with `unless Code.ensure_loaded?(Telemetry.Metrics)` and `raise` a multi-line message instructing the
    host to add `{:telemetry_metrics, "~> 1.0"}` to deps and run `mix deps.get` — do NOT return `[]` (D-04 /
    TEL-02). CRITICAL compile-time constraint (Pitfall 3): keep ALL `Telemetry.Metrics.*` references inside
    the function body — use `import Telemetry.Metrics, only: [counter: 2]` inside the function (or fully
    qualify `Telemetry.Metrics.counter/2`); NEVER reference `Telemetry.Metrics` at module-attribute level or
    the prod tree (where the dep is absent) fails to compile. Return one `counter/2` definition per documented
    live event suffix across the 5 families, naming them `"oban_powertools.<family>.<suffix>.count"` so the
    string auto-parses to `event_name: [:oban_powertools, family, suffix]`, `measurement: :count`. Use this
    exact per-family tag selection (each a subset of `@contract`, per D-02 — omit `:action` where it merely
    mirrors the event-name suffix per Pitfall 4):
    operator_action.previewed → `[:action, :source]`; operator_action.complete → `[:action, :source]`;
    limiter.blocked → `[:blocker_code, :resource, :scope]`; limiter.released → `[:resource, :scope]`;
    limiter.cooled_down → `[:resource, :scope]`; cron.paused/resumed/run_now → `[:source, :overlap_policy]`;
    cron.slot_claimed → `[:source, :overlap_policy, :catch_up_policy]`; workflow.step_completed →
    `[:outcome, :terminal_cause, :semantics_version]`; workflow.step_unblocked → `[:scope, :state, :semantics_version]`;
    workflow.cascade_cancelled → `[:scope, :outcome, :terminal_cause, :semantics_version]`; workflow.workflow_terminal →
    `[:state, :outcome, :terminal_cause, :semantics_version]`; lifeline.heartbeat_refresh → `[]`;
    lifeline.incident_projection → `[]`; lifeline.repair_previewed → `[:action, :incident_class, :target_type]`;
    lifeline.repair_executed → `[:action, :incident_class, :target_type]`; lifeline.archive_prune_completed → `[:outcome]`.
    Per D-01 emit ONLY these `[:oban_powertools, ...]` metrics — never `[:oban, :job, *]` Oban-core metrics.
    Per Claude's Discretion (D-02) and RESEARCH Open Question 1, ship `counter/2`-only for v1.0 and OMIT the
    `sum/2` archived_count/pruned_count quantity metrics: those values live in the metadata map (not the
    measurements map), so `:archived_count`/`:pruned_count` must NOT appear as `:tags` (they are variable counts
    — cardinality risk) and a `sum/2` over them would require a `:measurement` function; defer that to keep v1.0
    clean. Give each counter a one-line `:description`. Add a `@doc` describing the opt-in surface and the
    raise-when-absent behavior.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/oban_powertools && mix test test/oban_powertools/telemetry_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - Test command: `mix test test/oban_powertools/telemetry_test.exs` reports `0 failures` (the two Wave 0 tests now GREEN).
    - Source assertion: `lib/oban_powertools/telemetry.ex` defines `def metrics` whose body opens with an `unless Code.ensure_loaded?(Telemetry.Metrics)` guard that `raise`s a message containing the string `:telemetry_metrics`.
    - Source assertion: no `Telemetry.Metrics` reference exists at module-attribute level — all are inside the `metrics/0` body (grep confirms `import Telemetry.Metrics` / `counter(` only inside the function).
    - Behavior assertion: returned list contains exactly one counter per documented suffix across the 5 families; no metric `event_name` begins `[:oban, :job, ...]` (D-01); no metric `:tags` contains `:job_id`, `:args`, `:reason`, `:archived_count`, or `:pruned_count` (SC-4).
  </acceptance_criteria>
  <done>metrics/0 implemented; both Wave 0 tests green; guard raises actionable error when dep absent; tags strictly within @contract; counter-only (no sum) for v1.0.</done>
</task>

<task type="auto">
  <name>Task 2: Prove prod-tree compile with the optional dep absent</name>
  <files>lib/oban_powertools/telemetry.ex</files>
  <read_first>
    - lib/oban_powertools/telemetry.ex (the metrics/0 from Task 1 — confirm zero module-level Telemetry.Metrics references that would break compilation when the dep is excluded)
    - mix.exs (confirm `telemetry_metrics` is `only: [:test, :dev]` so it is NOT present in `:prod`, which is exactly the condition this task exercises)
    - .planning/phases/50-telemetry-metrics-slo-guide/50-RESEARCH.md (§"Common Pitfalls" Pitfall 3 — the compile-time failure mode this task guards against)
  </read_first>
  <action>
    Compile the library in the prod environment, where `telemetry_metrics` is excluded by its
    `only: [:test, :dev]` restriction, to prove TEL-02's "no failure when absent" property: the module
    must compile cleanly even though `Telemetry.Metrics` is not loadable. Run `MIX_ENV=prod mix compile`.
    If it fails with `function Telemetry.Metrics.counter/2 is undefined` or a CompileError referencing
    `Telemetry.Metrics`, a module-level reference leaked out of the `metrics/0` body — move it back inside
    the function (Task 1 / Pitfall 3) and recompile. This task is verification-only; it adds no new code
    unless a leak is found.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/oban_powertools && MIX_ENV=prod mix compile --force 2>&1 | grep -vq 'Telemetry.Metrics'</automated>
  </verify>
  <acceptance_criteria>
    - CLI: `MIX_ENV=prod mix compile --force` exits 0 with no `Telemetry.Metrics`-related CompileError.
    - Behavior assertion: the prod build contains no compiled reference that requires `Telemetry.Metrics` at load time (the guard defers the requirement to call-time).
    - Regression: `mix test test/oban_powertools/telemetry_test.exs` still reports `0 failures` after the force-compile.
  </acceptance_criteria>
  <done>MIX_ENV=prod mix compile succeeds with telemetry_metrics absent, proving the optional-dep gate holds.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| telemetry event metadata → metric :tags | The only place a high-cardinality or sensitive metadata key could leak into a published metric series |
| host caller → metrics/0 | Calling without the optional dep must fail honestly, not silently degrade |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-50-02 | Information Disclosure | metrics/0 `:tags` lists | mitigate | Tags hand-picked as a strict subset of the frozen `@contract`; the `"metrics/0 tags stay within frozen contract"` test asserts every tag ∈ contract per family+suffix — `job_id`/`args`/`reason` are absent from `@contract` so they cannot appear (SC-4) |
| T-50-03 | Denial of Service (cardinality blow-up) | metrics/0 `:tags` lists | mitigate | `:archived_count`/`:pruned_count` (variable integer counts) are explicitly excluded from `:tags`; only low-cardinality string/version keys are tagged; tag-containment test enforces |
| T-50-04 | Repudiation / honest-failure | metrics/0 absent-dep path | mitigate | `Code.ensure_loaded?` guard raises an actionable error naming `:telemetry_metrics` instead of returning `[]` (D-04) — no silent misconfiguration |
| T-50-SC | Tampering | telemetry_metrics install | accept | Inherited from Plan 01; beam-telemetry org, Approved in RESEARCH Package Legitimacy Audit; no checkpoint needed |
</threat_model>

<verification>
- `mix test test/oban_powertools/telemetry_test.exs` → `0 failures` (structural + tag-containment GREEN).
- `MIX_ENV=prod mix compile --force` → exits 0, no `Telemetry.Metrics` CompileError.
- `grep -c 'def metrics' lib/oban_powertools/telemetry.ex` ≥ 1.
- Manual/spot: no metric `event_name` starts `[:oban, :job, ...]`; no `:tags` entry is `:job_id`/`:args`/`:archived_count`/`:pruned_count`.
</verification>

<success_criteria>
- SC#1: `metrics/0` returns Telemetry.Metrics definitions over the frozen contract.
- SC#4: every tag is within the frozen low-cardinality contract (no job_id/args).
- TEL-02: absent-dep call raises actionable error; prod tree compiles without the dep.
- D-01 honored: Powertools-contract metrics only, no Oban-core job metrics.
</success_criteria>

<output>
Create `.planning/phases/50-telemetry-metrics-slo-guide/50-02-SUMMARY.md` when done.
</output>
