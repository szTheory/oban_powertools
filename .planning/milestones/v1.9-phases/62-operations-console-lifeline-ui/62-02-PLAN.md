---
phase: 62-operations-console-lifeline-ui
plan: 02
type: execute
wave: 1
depends_on: [62-01]
files_modified:
  - lib/oban_powertools/web/router.ex
  - lib/oban_powertools/web/selectors.ex
  - lib/oban_powertools/web/live_auth.ex
autonomous: true
requirements: [BUI-01, BUI-03, BUI-04]
user_setup: []

must_haves:
  truths:
    - "Operator can reach `/ops/jobs/batches` and `/ops/jobs/batches/:id` inside the existing host-owned `/ops/jobs` shell."
    - "Batch index and detail links are generated through canonical selector helpers rather than inline path strings."
    - "Batch pages remain inspectable for authorized viewers while mutation controls have explicit read-only permission copy."
  artifacts:
    - path: "lib/oban_powertools/web/router.ex"
      provides: "Native batch index/detail LiveView routes"
      contains: "live(\"/batches\""
    - path: "lib/oban_powertools/web/selectors.ex"
      provides: "Canonical batch path helpers"
      exports: ["batches_path/1", "batch_detail_path/1"]
    - path: "lib/oban_powertools/web/live_auth.ex"
      provides: "Batch page/action permission and read-only copy"
      contains: "view_batches"
  key_links:
    - from: "lib/oban_powertools/web/router.ex"
      to: "ObanPowertools.Web.BatchesLive"
      via: "Phoenix live routes"
      pattern: "live\\(\"/batches"
    - from: "lib/oban_powertools/web/selectors.ex"
      to: "/ops/jobs/batches"
      via: "@canonical_paths and helper functions"
      pattern: "batches_path|batch_detail_path"
    - from: "lib/oban_powertools/web/live_auth.ex"
      to: "ObanPowertools.Web.BatchesLive"
      via: "permission messages and page_read_only_banner"
      pattern: "view_batches|retry_batch_jobs|retry_callback"
---

<objective>
Add native route, selector, and authorization copy contracts for the Phase 62 batch surface.

Purpose: Establish the shared shell and permission vocabulary that later read-model and LiveView work will consume.
Output: Router entries, selector helpers, and LiveAuth copy for `/ops/jobs/batches` and `/ops/jobs/batches/:id`.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/62-operations-console-lifeline-ui/62-CONTEXT.md
@.planning/phases/62-operations-console-lifeline-ui/62-UI-SPEC.md
@.planning/phases/62-operations-console-lifeline-ui/62-PATTERNS.md
@.planning/phases/62-operations-console-lifeline-ui/62-01-SUMMARY.md
@lib/oban_powertools/web/router.ex
@lib/oban_powertools/web/selectors.ex
@lib/oban_powertools/web/live_auth.ex
@test/oban_powertools/web/router_test.exs
@test/oban_powertools/web/selectors_test.exs
@test/support/test_auth.ex

<interfaces>
Existing route macro:
- `ObanPowertools.Web.Router.oban_powertools_routes(path)` imports `Phoenix.LiveView.Router` and mounts native pages inside `live_session :oban_powertools_native`.

Existing selector helpers:
- `Selectors.encode(destination, params)` uses `@canonical_paths`, drops `nil` and empty string values, and preserves keyword-list order.
- `Selectors.jobs_path(params \\ [])` and `Selectors.job_detail_path(id)` are the closest analogs.

Existing auth helpers:
- `LiveAuth.authorize_page(socket, action, resource)`
- `LiveAuth.authorize_action(socket, action, resource, opts \\ [])`
- `LiveAuth.authorized?(actor, action, resource)`
- `LiveAuth.page_read_only_banner(surface)`
- `ObanPowertools.TestAuth` authorizes when `action in actor.permissions`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add canonical batch routes and selectors</name>
  <read_first>
    .planning/phases/62-operations-console-lifeline-ui/62-CONTEXT.md
    .planning/phases/62-operations-console-lifeline-ui/62-UI-SPEC.md
    .planning/phases/62-operations-console-lifeline-ui/62-PATTERNS.md
    lib/oban_powertools/web/router.ex
    lib/oban_powertools/web/selectors.ex
    test/oban_powertools/web/router_test.exs
    test/oban_powertools/web/selectors_test.exs
  </read_first>
  <files>lib/oban_powertools/web/router.ex, lib/oban_powertools/web/selectors.ex</files>
  <action>Per D-01 and D-02, add `live("/batches", ObanPowertools.Web.BatchesLive, :index)` and `live("/batches/:id", ObanPowertools.Web.BatchesLive, :show)` inside the existing `:oban_powertools_native` live session before the optional Oban Web bridge. Extend `@canonical_paths` with `batches: "/ops/jobs/batches"`, add `batches_path(params \\ [])`, and add `batch_detail_path(id)`. Keep query encoding delegated to `encode/2`; do not concatenate query strings outside `Selectors.encode/2`.</action>
  <acceptance_criteria>
    - Source assertion: `lib/oban_powertools/web/router.ex` contains both `live("/batches", ObanPowertools.Web.BatchesLive, :index)` and `live("/batches/:id", ObanPowertools.Web.BatchesLive, :show)`.
    - Source assertion: `lib/oban_powertools/web/selectors.ex` contains `batches: "/ops/jobs/batches"`, `def batches_path(params \\ [])`, and `def batch_detail_path(id)`.
    - Behavior assertion: `Selectors.batches_path([{"status", "callback_failed"}, {"query", ""}])` returns `/ops/jobs/batches?status=callback_failed`.
    - Behavior assertion: `Selectors.batch_detail_path("batch-1")` returns `/ops/jobs/batches/batch-1`.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/oban_powertools/web/router_test.exs test/oban_powertools/web/selectors_test.exs</automated>
  </verify>
  <done>Batch index/detail routes and selector helpers exist and pass the Wave 0 router/selector tests.</done>
</task>

<task type="auto">
  <name>Task 2: Add batch permission and read-only copy</name>
  <read_first>
    .planning/phases/62-operations-console-lifeline-ui/62-CONTEXT.md
    .planning/phases/62-operations-console-lifeline-ui/62-UI-SPEC.md
    .planning/phases/62-operations-console-lifeline-ui/62-PATTERNS.md
    lib/oban_powertools/web/live_auth.ex
    test/support/test_auth.ex
    test/oban_powertools/web/live/batches_live_test.exs
  </read_first>
  <files>lib/oban_powertools/web/live_auth.ex</files>
  <action>Per D-17 and D-18, extend `@permission_messages` with exact atoms `:view_batches`, `:view_batch_detail`, `:retry_batch_jobs`, and `:retry_callback`. Extend `@page_read_only_banners` with exact surfaces `:batches` and `:batch_detail`. Use the UI-SPEC read-only posture: evidence stays visible, mutation controls stay disabled until broader permission is granted. Keep `:preview_repair` and `:execute_repair` unchanged because Lifeline still owns preview/execute authorization.</action>
  <acceptance_criteria>
    - Source assertion: `lib/oban_powertools/web/live_auth.ex` contains permission messages for `view_batches`, `view_batch_detail`, `retry_batch_jobs`, and `retry_callback`.
    - Source assertion: `lib/oban_powertools/web/live_auth.ex` contains page read-only banners for `batches` and `batch_detail`.
    - Source assertion: new copy includes `Permission: read-only` and `Powertools-native` to match the existing LiveAuth style.
    - Behavior assertion: `LiveAuth.page_read_only_banner(:batches)` returns a string instead of raising.
  </acceptance_criteria>
  <verify>
    <automated>rg -n "view_batches|view_batch_detail|retry_batch_jobs|retry_callback|batches:|batch_detail:" lib/oban_powertools/web/live_auth.ex</automated>
  </verify>
  <done>Batch page/detail permission atoms and read-only copy are available for the LiveView implementation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host router to Powertools native routes | Host owns `/ops/jobs`; Powertools owns only mounted native LiveViews. |
| browser URL to selector helpers | Query params are untrusted and must be encoded through canonical selectors. |
| actor session to page/action authorization | Actor permissions determine view and mutation posture. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-62-01 | Tampering | batch route/query selectors | mitigate | Use `Selectors.encode/2` for batch paths and route tests for canonical `/ops/jobs/batches` paths. |
| T-62-03 | Elevation of Privilege | batch mutation controls | mitigate | Add explicit `:retry_batch_jobs` and `:retry_callback` read-only copy; later LiveView controls must use these atoms plus Lifeline gates. |
| T-62-06 | Spoofing | page authorization | mitigate | Batch pages must use `LiveAuth.authorize_page/3` with `:view_batches` or `:view_batch_detail` in Plan 62-05. |
| T-62-SC | Tampering | package installs | accept | No package-manager install tasks exist. |
</threat_model>

<verification>
Run route and selector focused tests. `batches_live_test.exs` can remain RED until Plan 62-05, but LiveAuth copy assertions in that test should no longer fail because of missing `page_read_only_banner/1` keys.
</verification>

<success_criteria>
- `/ops/jobs/batches` and `/ops/jobs/batches/:id` are registered native routes.
- Batch links are available through `Selectors.batches_path/1` and `Selectors.batch_detail_path/1`.
- Batch view and mutation permission copy exists in `LiveAuth`.
</success_criteria>

<output>
Create `.planning/phases/62-operations-console-lifeline-ui/62-02-SUMMARY.md` when done.
</output>
