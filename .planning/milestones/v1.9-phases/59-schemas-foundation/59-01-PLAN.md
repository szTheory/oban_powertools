---
phase: 59-schemas-foundation
plan: 01
type: execute
wave: 1
depends_on: []
requirements: ["BAT-01"]
files_modified:
  - lib/oban_powertools/batch.ex
  - lib/oban_powertools/batch_job.ex
  - lib/oban_powertools/callback.ex
  - lib/oban_powertools/workflow/callback_outbox.ex
  - lib/oban_powertools/workflow/workflow.ex
  - lib/oban_powertools/workflow/runtime.ex
  - lib/oban_powertools/explain.ex
  - test/oban_powertools/workflow_callbacks_test.exs
autonomous: true
requirements:
  - BAT-01
must_haves:
  truths:
    - "Batches and BatchJobs schemas exist"
    - "Callback outbox schema is generalized"
  artifacts:
    - path: "lib/oban_powertools/batch.ex"
      provides: "Batch schema"
    - path: "lib/oban_powertools/batch_job.ex"
      provides: "BatchJob schema"
    - path: "lib/oban_powertools/callback.ex"
      provides: "Generalized callback schema"
  key_links:
    - from: "lib/oban_powertools/workflow/workflow.ex"
      to: "lib/oban_powertools/callback.ex"
      via: "has_many relationship"
      pattern: "has_many.*:callback_outbox.*ObanPowertools.Callback"
---

<objective>
Establish the core Ecto schemas for dedicated batch tracking and generalize the existing workflow callback outbox into a unified callback outbox.

Purpose: Provides durable, Ecto-native data models to enable exactly-once progress tracking and scalable callback dispatching, without overloading Oban's metadata.
Output: Batch, BatchJob, and Callback Ecto schemas, alongside refactored workflow dependencies pointing to the new Callback module.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/59-schemas-foundation/59-CONTEXT.md
@.planning/phases/59-schemas-foundation/59-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Batches Core Schemas</name>
  <files>
    lib/oban_powertools/batch.ex
    lib/oban_powertools/batch_job.ex
  </files>
  <read_first>
    lib/oban_powertools/workflow/workflow.ex
    lib/oban_powertools/workflow/step.ex
  </read_first>
  <action>
    Create `ObanPowertools.Batch` schema in `lib/oban_powertools/batch.ex` following the styling and module patterns from `ObanPowertools.Workflow`. Define the table as `oban_powertools_batches` with an autogenerated `:binary_id` primary key. Include fields: `status` (string, default "executing"), `total_count`, `success_count`, `discard_count`, `cancelled_count`, and `snooze_count` (all integers, default 0). Include Ecto timestamps. Add a `changeset/2` function handling standard casting and validations.
    
    Create `ObanPowertools.BatchJob` schema in `lib/oban_powertools/batch_job.ex` following the styling and module patterns from `ObanPowertools.Workflow.Step`. Define the table as `oban_powertools_batch_jobs` with an autogenerated `:binary_id` primary key. Include fields: `batch_id` (belongs_to `ObanPowertools.Batch`, type `:binary_id`) and `job_id` (integer) and `state` (string, default "available"). Include Ecto timestamps (updated_at: true). Add a `changeset/2` function with `unique_constraint([:batch_id, :job_id])`.
  </action>
  <acceptance_criteria>
    - Source `lib/oban_powertools/batch.ex` contains `defmodule ObanPowertools.Batch` and `schema "oban_powertools_batches"`
    - Source `lib/oban_powertools/batch.ex` explicitly lists `success_count` and other explicit counter fields
    - Source `lib/oban_powertools/batch_job.ex` contains `defmodule ObanPowertools.BatchJob` and `schema "oban_powertools_batch_jobs"`
  </acceptance_criteria>
  <verify>
    <automated>mix compile</automated>
  </verify>
  <done>Batch schemas created and project compiles.</done>
</task>

<task type="auto">
  <name>Task 2: Generalize Callback Outbox Schema</name>
  <files>
    lib/oban_powertools/callback.ex
    lib/oban_powertools/workflow/callback_outbox.ex
  </files>
  <read_first>
    lib/oban_powertools/workflow/callback_outbox.ex
  </read_first>
  <action>
    Create `lib/oban_powertools/callback.ex` with module name `ObanPowertools.Callback`, copying the original content of `ObanPowertools.Workflow.CallbackOutbox`.
    Change the schema definition to use the table name `oban_powertools_callbacks`.
    Add a new `belongs_to` for `:batch` pointing to `ObanPowertools.Batch` with type `:binary_id`.
    In the `changeset/2` function, add `:batch_id` to the `cast` fields. Remove `:workflow_id` from the `validate_required` list since it is now nullable (a callback might belong to a batch instead). Ensure `validate_inclusion` for `:event` is either removed entirely, or extended to include `"batch.completed"` and `"batch.exhausted"`.
    Finally, delete the original `lib/oban_powertools/workflow/callback_outbox.ex` file.
  </action>
  <acceptance_criteria>
    - Source `lib/oban_powertools/callback.ex` contains `defmodule ObanPowertools.Callback` and `schema "oban_powertools_callbacks"`
    - Source `lib/oban_powertools/callback.ex` contains `belongs_to(:batch, ...)`
    - File `lib/oban_powertools/workflow/callback_outbox.ex` does not exist
  </acceptance_criteria>
  <verify>
    <automated>test ! -f lib/oban_powertools/workflow/callback_outbox.ex && mix compile</automated>
  </verify>
  <done>Callback outbox schema is generalized and old file removed.</done>
</task>

<task type="auto">
  <name>Task 3: Refactor Workflow Dependents</name>
  <files>
    lib/oban_powertools/workflow/workflow.ex
    lib/oban_powertools/workflow/runtime.ex
    lib/oban_powertools/explain.ex
    test/oban_powertools/workflow_callbacks_test.exs
  </files>
  <read_first>
    lib/oban_powertools/workflow/workflow.ex
    lib/oban_powertools/workflow/runtime.ex
    lib/oban_powertools/explain.ex
    test/oban_powertools/workflow_callbacks_test.exs
  </read_first>
  <action>
    In `lib/oban_powertools/workflow/workflow.ex`, update the `has_many` association for `:callback_outbox` to reference `ObanPowertools.Callback` instead of `ObanPowertools.Workflow.CallbackOutbox`.
    In `lib/oban_powertools/workflow/runtime.ex` and `lib/oban_powertools/explain.ex`, change aliases from `ObanPowertools.Workflow.CallbackOutbox` to `ObanPowertools.Callback` and update all module/struct references (e.g., `%CallbackOutbox{}` to `%Callback{}`).
    In `test/oban_powertools/workflow_callbacks_test.exs`, change the alias and all module references to use `ObanPowertools.Callback` instead of `CallbackOutbox`.
  </action>
  <acceptance_criteria>
    - Source `lib/oban_powertools/workflow/workflow.ex` contains `ObanPowertools.Callback`
    - Source `test/oban_powertools/workflow_callbacks_test.exs` contains `alias ObanPowertools.Callback`
    - CLI output `grep CallbackOutbox lib/oban_powertools/workflow/workflow.ex | wc -l` exits with 0
  </acceptance_criteria>
  <verify>
    <automated>mix compile</automated>
  </verify>
  <done>All references to the old CallbackOutbox module have been safely updated.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DB Interface | Internal services creating jobs and updating explicit counters. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-59-01 | Denial of Service | Batch Counters | accept | Atomic increment/decrement via `Repo.update_all(inc: [...])` will mitigate lock starvation, implemented in execution phase. Schema supports explicit counters to avoid large JOIN operations. |
</threat_model>

<verification>
mix compile successfully builds the app without the old module.
</verification>

<success_criteria>
Batches and Callbacks schemas are defined according to CONTEXT constraints and existing code seamlessly integrates the new module alias.
</success_criteria>

<output>
Create `.planning/phases/59-schemas-foundation/59-01-SUMMARY.md` when done
</output>