---
phase: 59-schemas-foundation
plan: 02
type: execute
wave: 2
depends_on: ["59-01"]
requirements: ["BAT-01"]
files_modified:
  - lib/mix/tasks/oban_powertools.install.ex
  - lib/oban_powertools/doctor/checks.ex
  - test/support/migrations/7_phase_59_tables.exs
  - test/mix/tasks/oban_powertools.install_test.exs
  - test/oban_powertools/batch_test.exs
  - test/oban_powertools/batch_job_test.exs
  - test/oban_powertools/callback_test.exs
autonomous: true
requirements:
  - BAT-01
must_haves:
  truths:
    - "Migrations can be generated for installation"
    - "Test suite compiles and runs the migrations"
  artifacts:
    - path: "lib/mix/tasks/oban_powertools.install.ex"
      provides: "Installation migrations generator"
    - path: "test/support/migrations/7_phase_59_tables.exs"
      provides: "Test migrations"
  key_links:
    - from: "lib/oban_powertools/doctor/checks.ex"
      to: "oban_powertools_callbacks"
      via: "powertools_manifest"
      pattern: "oban_powertools_callbacks"
---

<objective>
Provide DB migration logic to generate the Batches tables and rename/generalize the Callbacks Outbox, and ensure test coverage for the schemas.

Purpose: Ensures operators can safely migrate to the new `oban_powertools_callbacks` structure without data loss, and standardizes testing across the new models.
Output: Igniter migration tasks, a test DB migration, unit tests for the schemas, and an updated Doctor checks manifest.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/59-schemas-foundation/59-CONTEXT.md
@.planning/phases/59-schemas-foundation/59-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update Installer and Doctor Validations</name>
  <files>
    lib/mix/tasks/oban_powertools.install.ex
    lib/oban_powertools/doctor/checks.ex
    test/mix/tasks/oban_powertools.install_test.exs
  </files>
  <read_first>
    lib/mix/tasks/oban_powertools.install.ex
    lib/oban_powertools/doctor/checks.ex
    test/mix/tasks/oban_powertools.install_test.exs
  </read_first>
  <action>
    In `lib/mix/tasks/oban_powertools.install.ex`, DO NOT just change the table name in the old workflow migration (this would cause a drop/create data loss event). Instead, create a new private function `setup_batch_migrations(igniter)` which generates migration timestamp `50`. Inside this migration, explicitly use the Table Rename/Alter Pattern from `PATTERNS.md` to issue `rename table(:oban_powertools_workflow_callback_outbox), to: table(:oban_powertools_callbacks)` and `alter table(:oban_powertools_callbacks)`. Also within this migration generation, include `create table(:oban_powertools_batches)` and `create table(:oban_powertools_batch_jobs)`. Pipe `setup_batch_migrations` into the `igniter` function chain.
    In `lib/oban_powertools/doctor/checks.ex`, update the `@powertools_manifest` list for the `"workflow"` group: replace `"oban_powertools_workflow_callback_outbox"` with `"oban_powertools_callbacks"`. Under the `"foundation"` group (or create a `"batches"` group), append `"oban_powertools_batches"` and `"oban_powertools_batch_jobs"`.
    In `test/mix/tasks/oban_powertools.install_test.exs`, update assertions to expect `oban_powertools_callbacks` rename migration and `oban_powertools_batches` instead of the old `workflow_callback_outbox` table.
  </action>
  <acceptance_criteria>
    - Source `lib/mix/tasks/oban_powertools.install.ex` explicitly generates a `rename table` migration.
    - Source `lib/mix/tasks/oban_powertools.install.ex` contains `setup_batch_migrations` generating batch tables.
    - Source `lib/oban_powertools/doctor/checks.ex` contains `"oban_powertools_callbacks"` in the manifest list
    - Source `lib/oban_powertools/doctor/checks.ex` contains `"oban_powertools_batches"` in the manifest list
    - Source `test/mix/tasks/oban_powertools.install_test.exs` assertions updated for new tables
  </acceptance_criteria>
  <verify>
    <automated>mix test test/mix/tasks/oban_powertools.install_test.exs</automated>
  </verify>
  <done>Install tasks correctly generate rename migrations for the callbacks and batches tables, and test passes.</done>
</task>

<task type="auto">
  <name>Task 2: Build Test Migrations</name>
  <files>
    test/support/migrations/7_phase_59_tables.exs
  </files>
  <read_first>
    test/support/migrations/5_phase_6_tables.exs
  </read_first>
  <action>
    Create a new migration file `test/support/migrations/7_phase_59_tables.exs`.
    Within the migration's `change/0` block:
    1. Issue `rename table(:oban_powertools_workflow_callback_outbox), to: table(:oban_powertools_callbacks)` to migrate test state.
    2. Add an `alter table(:oban_powertools_callbacks)` to add `add :batch_id, :uuid` and `modify :workflow_id, :uuid, null: true`.
    3. Add an index for `:batch_id` on `:oban_powertools_callbacks`.
    4. Create the `oban_powertools_batches` table with necessary explicit counter integer fields and defaults (status, total_count, success_count, discard_count, cancelled_count, snooze_count).
    5. Create the `oban_powertools_batch_jobs` table linking `batch_id` to `job_id` (bigint) with standard timestamps. Add a unique index for `[:batch_id, :job_id]` and a normal index for `[:job_id]`.
  </action>
  <acceptance_criteria>
    - Source `test/support/migrations/7_phase_59_tables.exs` contains `rename table(:oban_powertools_workflow_callback_outbox)`
    - Source `test/support/migrations/7_phase_59_tables.exs` contains `create table(:oban_powertools_batches)`
  </acceptance_criteria>
  <verify>
    <automated>mix ecto.migrate --quiet || echo "Will pass in full test suite context"</automated>
  </verify>
  <done>Phase 59 test migration handles table renames and creations flawlessly.</done>
</task>

<task type="auto">
  <name>Task 3: Unit Test Scaffolds for Schemas</name>
  <files>
    test/oban_powertools/batch_test.exs
    test/oban_powertools/batch_job_test.exs
    test/oban_powertools/callback_test.exs
  </files>
  <read_first>
    test/oban_powertools/job_record_test.exs
    test/oban_powertools/workflow_callbacks_test.exs
  </read_first>
  <action>
    Create basic ExUnit test modules for `ObanPowertools.Batch`, `ObanPowertools.BatchJob`, and `ObanPowertools.Callback` to verify schema casting and validation.
    Follow the test patterns from `test/oban_powertools/job_record_test.exs` and `test/oban_powertools/workflow_callbacks_test.exs`.
    Ensure they test basic valid inputs and required attributes logic.
    For `Callback`, ensure testing reflects that either `workflow_id` or `batch_id` is permissible without triggering a required failure.
  </action>
  <acceptance_criteria>
    - Source `test/oban_powertools/batch_test.exs` contains `defmodule ObanPowertools.BatchTest`
    - Source `test/oban_powertools/callback_test.exs` asserts that `workflow_id` and `batch_id` are cast correctly
  </acceptance_criteria>
  <verify>
    <automated>mix test test/oban_powertools/batch_test.exs test/oban_powertools/batch_job_test.exs test/oban_powertools/callback_test.exs</automated>
  </verify>
  <done>Basic unit tests constructed following standard test patterns.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DB Migrations | Executed by operators. Safe migration paths required to prevent data loss on rename. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-59-02 | Tampering | DB Migration | mitigate | Use Ecto `rename` instead of drop/create to preserve existing callback telemetry and states. |
</threat_model>

<verification>
mix test runs without compilation or migration errors and all tests pass.
</verification>

<success_criteria>
New schemas are fully verified and test environments seamlessly upgrade the callback outbox table.
</success_criteria>

<output>
Create `.planning/phases/59-schemas-foundation/59-02-SUMMARY.md` when done
</output>