---
phase: "60"
plan: "03"
type: execute
wave: 3
depends_on: ["60-02"]
files_modified:
  - lib/oban_powertools/worker/hooks.ex
  - lib/oban_powertools/batch/tracker.ex
autonomous: true
requirements:
  - BAT-03
  - BAT-04
must_haves:
  truths:
    - "Operator sees accurate real-time batch progress as jobs succeed or fail"
  artifacts:
    - path: "lib/oban_powertools/worker/hooks.ex"
      provides: "Batch progress tracking hook integration"
  key_links:
    - from: "lib/oban_powertools/worker/hooks.ex"
      to: "lib/oban_powertools/batch/tracker.ex"
      via: "Tracker.record_progress/3"
      pattern: "record_progress"
---

<objective>
Wire the Tracker into Worker Hooks and handle callback exhaustion.
Purpose: Automatically record exactly-once batch progress when Oban Jobs succeed or discard, and transition batch state when callbacks exhaust retries.
Output: Hooks integrated with Tracker.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/60-execution-engine-tracker-hooks/60-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire Tracker into Worker Hooks and handle callback exhaustion</name>
  <files>lib/oban_powertools/worker/hooks.ex, lib/oban_powertools/batch/tracker.ex</files>
  <read_first>
    - lib/oban_powertools/worker/hooks.ex
    - lib/oban_powertools/batch/tracker.ex
  </read_first>
  <action>
    Reference analog `lib/oban_powertools/worker/hooks.ex` from PATTERNS.md for hook modification patterns.
    In `ObanPowertools.Worker.Hooks.after_result/3` and `after_exception/5`, inject calls to `ObanPowertools.Batch.Tracker.record_progress(ObanPowertools.RuntimeConfig.repo(), job, state)` BEFORE `safe_invoke` is called.
    - For `:ok` and `{:ok, value}` outcomes: `record_progress(..., job, :success)`.
    - For `:discard` and `{:discard, reason}` outcomes: `record_progress(..., job, :discard)`.
    - For `{:error, reason}` inside `after_result` AND inside `after_exception`, only if `terminal_attempt?(job)` is true: `record_progress(..., job, :discard)`.
    Leave `:cancel` and `:snooze` unaffected.

    Additionally, per D-06, update the hook logic to detect when a callback job exhausts its retries (is discarded). Check if the `job` is a callback job (e.g., via `job.worker` or `job.meta`). If it is a callback job that is discarded, call a new function `ObanPowertools.Batch.Tracker.record_callback_exhaustion(repo, job)` to transition the associated Batch's status to `"callback_failed"`. Add this `record_callback_exhaustion/2` function to `lib/oban_powertools/batch/tracker.ex`.
  </action>
  <verify>
    <automated>mix test</automated>
  </verify>
  <done>
    - `grep` confirms `ObanPowertools.Batch.Tracker.record_progress` is called appropriately in `hooks.ex`.
    - `hooks.ex` checks for callback job exhaustion and updates Batch state to `callback_failed`.
    - `mix test` passes without compilation errors.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Worker Context | Internal state tracking execution flow |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-60-04   | Information Disclosure | Hooks  | accept | Internal integration mapping job results to batch states. No trust boundary crossed. |
</threat_model>

<verification>
Run `mix test` to verify no compilation errors.
</verification>

<success_criteria>
The Worker Hooks reliably call `Tracker.record_progress/3` across all terminal completion states (success and discard), and callback exhaustions appropriately update the Batch state to `callback_failed`.
</success_criteria>

<output>
Create `.planning/phases/60-execution-engine-tracker-hooks/60-03-SUMMARY.md` when done
</output>