---
phase: 46-operator-elixir-api
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/oban_powertools/lifeline.ex
  - test/oban_powertools/lifeline_test.exs
  - lib/oban_powertools/operator.ex
  - test/oban_powertools/operator_test.exs
autonomous: true
requirements: [API-01, API-02]
must_haves:
  truths:
    - Lifeline repair telemetry events accept and merge caller-provided metadata
    - Operator API can invoke single-job retry, cancel, and discard
    - Operator API automatically injects source="api" telemetry metadata
  artifacts:
    - path: lib/oban_powertools/operator.ex
      provides: Single-job API surface
    - path: lib/oban_powertools/lifeline.ex
      provides: Telemetry plumbing
  key_links:
    - from: Operator
      to: Lifeline
      via: do_repair delegating to preview_repair and execute_repair
---

<objective>
Update Lifeline to support telemetry metadata threading and create the single-job operations for the `Operator` API.

Purpose: Provide the foundational programmatic surface for host apps to mutate jobs while preserving full auditability.
Output: Updated `Lifeline.ex`, new `Operator.ex` with single-job actions, and corresponding tests.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/46-operator-elixir-api/46-CONTEXT.md
@.planning/phases/46-operator-elixir-api/46-RESEARCH.md
@.planning/phases/46-operator-elixir-api/46-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Thread telemetry metadata through Lifeline</name>
  <files>lib/oban_powertools/lifeline.ex, test/oban_powertools/lifeline_test.exs</files>
  <behavior>
    - `preview_repair` accepts `:telemetry_metadata` in `opts` and merges it into the `:repair_previewed` telemetry event metadata.
    - `execute_repair` passes `opts` down to `apply_repair`.
    - `apply_repair` merges `opts[:telemetry_metadata]` into the `:repair_executed` telemetry event metadata.
  </behavior>
  <action>
    Modify `preview_repair/4` to read `opts[:telemetry_metadata]` (defaulting to `%{}`) and `Map.merge` it with the event metadata before calling `Telemetry.execute_lifeline_event(:repair_previewed, ...)`. 
    Update `execute_repair/5` to pass `opts` down to `apply_repair/6` (change its arity from 5 to 6).
    In `apply_repair/6`, read `opts[:telemetry_metadata]` and merge it into the `:repair_executed` telemetry event metadata.
    Update `lifeline_test.exs` to verify telemetry metadata injection for both preview and execute events.
  </action>
  <verify>
    <automated>mix test test/oban_powertools/lifeline_test.exs</automated>
  </verify>
  <done>Telemetry events contain injected metadata from options.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement single-job Operator API</name>
  <files>lib/oban_powertools/operator.ex, test/oban_powertools/operator_test.exs</files>
  <behavior>
    - `Operator.retry_job/5` successfully runs Lifeline preview and execute, emitting telemetry with `source: "api"`.
    - `Operator.cancel_job/5` works properly with actor attribution.
    - `Operator.discard_job/5` works properly with actor attribution.
    - Invalid operations return the underlying Lifeline errors.
  </behavior>
  <action>
    Create `ObanPowertools.Operator`. Add public functions `retry_job/5`, `cancel_job/5`, and `discard_job/5`. All take `(repo, actor, job_id, reason, opts \\ [])`.
    Implement a private `do_repair(repo, actor, action, job_id, reason, opts)` that:
    1. Injects `telemetry_metadata: %{source: "api"}` into `opts`.
    2. Calls `Lifeline.preview_repair/4` with `target_type: "job"` and `target_id: job_id` passing the modified `opts`.
    3. On `{:ok, preview}`, calls `Lifeline.execute_repair/5` with `preview.preview_token` passing the modified `opts`.
    4. Returns the result from `execute_repair` or `preview_repair`.
    Add tests in `operator_test.exs` simulating actual jobs and asserting successful repair and telemetry events. (Reference `JobsTest` for job insertion pattern per 46-PATTERNS.md).
  </action>
  <verify>
    <automated>mix test test/oban_powertools/operator_test.exs</automated>
  </verify>
  <done>Single job mutations work programmatically and include telemetry metadata.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Caller→Operator | Host app code calling the programmatic API |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-46-01 | Spoofing | Operator | mitigate | All API actions require an explicit `actor` parameter passed directly to `Lifeline` authorization |
| T-46-02 | Tampering | Operator | mitigate | Mutations only executed through secure `Lifeline.execute_repair` preview-token flow |
</threat_model>

<verification>
mix test test/oban_powertools/operator_test.exs
</verification>

<success_criteria>
`ObanPowertools.Operator` exists with single-job functions that thread telemetry correctly.
</success_criteria>

<output>
After completion, create `.planning/phases/46-operator-elixir-api/46-01-SUMMARY.md`
</output>
