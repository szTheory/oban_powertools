---
phase: 45-bulk-operations
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified: ["lib/oban_powertools/web/jobs_live.ex", "test/oban_powertools/web/live/jobs_live_test.exs"]
autonomous: true
requirements: ["QRY-04"]

must_haves:
  truths:
    - "Selection state is tracked via MapSet in LiveView assigns"
    - "Selection is cleared when filtering changes, but preserved on pagination"
  artifacts:
    - path: "lib/oban_powertools/web/jobs_live.ex"
      provides: "selected_jobs MapSet and event handlers"
    - path: "test/oban_powertools/web/live/jobs_live_test.exs"
      provides: "selection state tests"
---

<objective>
Implement the checkbox selection UI and state management in `JobsLive` to allow operators to select multiple jobs for bulk operations.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/45-bulk-operations/45-UI-SPEC.md
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Job Selection State and UI</name>
  <files>lib/oban_powertools/web/jobs_live.ex, test/oban_powertools/web/live/jobs_live_test.exs</files>
  <behavior>
    - Add `@selected_jobs` assign (initialized to `MapSet.new()`) to `assign_defaults/1`.
    - Render a checkbox column in the job list table.
    - Implement `phx-click="toggle_job"` with `phx-value-id` to add/remove job IDs from `@selected_jobs`.
    - Implement `phx-click="toggle_all"` to add all currently visible job IDs to `@selected_jobs`, or clear if all are currently selected.
    - Clear `@selected_jobs` when `handle_event("filter", ...)` or `handle_event("select_state", ...)` are called.
  </behavior>
  <action>
    Update `jobs_live.ex` and matching LiveView tests.
  </action>
  <verify>
    <automated>mix test test/oban_powertools/web/live/jobs_live_test.exs</automated>
  </verify>
  <done>Checkboxes render and update the selection MapSet correctly.</done>
</task>
</tasks>
