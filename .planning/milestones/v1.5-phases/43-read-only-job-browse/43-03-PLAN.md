---
phase: 43-read-only-job-browse
plan: 03
type: execute
wave: 3
depends_on:
  - 43-01
  - 43-02
files_modified:
  - lib/oban_powertools/web/jobs_live.ex
  - test/oban_powertools/web/live/jobs_live_test.exs
autonomous: true
requirements:
  - QRY-02
tags:
  - phoenix-liveview
  - display-policy
  - oban
  - redaction

must_haves:
  truths:
    - "Operator can click any job row on the list page and land on /ops/jobs/jobs/:id rendering the full detail view"
    - "Detail view displays identity (worker, queue, state, id, attempt), all non-nil timing fields, args panel with DisplayPolicy redaction, meta panel with DisplayPolicy redaction, errors panel, and attempt history panel — per D-14"
    - "DisplayPolicy returning nil renders args/meta as raw pretty-printed JSON; String renders the string verbatim in a <pre>; Map renders the host-returned map as pretty-printed JSON; any exception renders [redacted] muted text"
    - "Back link on the detail page navigates to /ops/jobs/jobs preserving the last-known filter state (state param at minimum)"
    - "Unauthorized actors (no :view_job_detail permission) are redirected to / by LiveAuth.authorize_page/3"
    - "Job-not-found case renders a friendly 'Job not found' message rather than crashing"
  artifacts:
    - path: "lib/oban_powertools/web/jobs_live.ex"
      provides: ":show action — handle_params loads job via Jobs.get/2, calls DisplayPolicy.render_job_field/3 for args and meta, renders detail panels; private helpers for args/meta/errors/attempts/timing rendering"
      contains: "def handle_params(%{\"id\" => id}"
    - path: "test/oban_powertools/web/live/jobs_live_test.exs"
      provides: "Detail-page integration tests — render, redaction (nil/String/Map/raise paths), not-found, unauthorized"
      contains: "Detail page"
  key_links:
    - from: "lib/oban_powertools/web/jobs_live.ex (:show action)"
      to: "ObanPowertools.Jobs.get/2"
      via: "handle_params/3 detail loader"
      pattern: "Jobs\\.get\\("
    - from: "lib/oban_powertools/web/jobs_live.ex (:show action)"
      to: "ObanPowertools.DisplayPolicy.render_job_field/3"
      via: ":job_args and :job_meta calls"
      pattern: "DisplayPolicy\\.render_job_field\\(:job_(args|meta)"
    - from: "lib/oban_powertools/web/jobs_live.ex (:show render)"
      to: "Selectors.jobs_path/1"
      via: "back-link href"
      pattern: "Selectors\\.jobs_path"
---

<objective>
Replace the Wave 2 `:show` stub with the real job detail view, completing QRY-02. Wave 3 wires `Jobs.get/2` (Wave 1) and `DisplayPolicy.render_job_field/3` (Wave 1) into a detail render that exposes all of D-14's fields — identity card, args panel (DisplayPolicy-redacted), meta panel (DisplayPolicy-redacted), errors panel, attempt history panel, timing fields — and adds back-link filter preservation.

Purpose: Phase 43 success criterion 3 from ROADMAP — "Operator can click any job row and see its full detail — args, meta, errors, attempt history, and timing — with `DisplayPolicy` redaction applied to args and meta." Wave 1 produced the data + policy contracts; Wave 2 shipped the list page; this wave closes the read-only browse loop by attaching the detail render to the same module via the `:show` action.

Output: Two modified files (jobs_live.ex render + handle_params; jobs_live_test.exs extended with detail tests). The Wave 2 :show stub is gone. All 4 DisplayPolicy return-arms are covered by tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/43-read-only-job-browse/43-CONTEXT.md
@.planning/phases/43-read-only-job-browse/43-RESEARCH.md
@.planning/phases/43-read-only-job-browse/43-PATTERNS.md
@.planning/phases/43-read-only-job-browse/43-UI-SPEC.md

# Implementation surfaces this plan touches or mirrors
@lib/oban_powertools/web/jobs_live.ex
@lib/oban_powertools/jobs.ex
@lib/oban_powertools/runtime_config.ex
@lib/oban_powertools/web/lifeline_live.ex
@test/oban_powertools/web/live/jobs_live_test.exs

# Prior wave summaries
@.planning/phases/43-read-only-job-browse/43-01-SUMMARY.md
@.planning/phases/43-read-only-job-browse/43-02-SUMMARY.md

<interfaces>
<!-- Contracts produced by prior waves, consumed here -->

From lib/oban_powertools/jobs.ex (43-01):
- `Jobs.get(repo, job_id) :: %Oban.Job{} | nil` — used to load detail data.

From lib/oban_powertools/runtime_config.ex (43-01):
- `DisplayPolicy.render_job_field(kind, value, context) :: {:raw_json, json_string} | {:string, text} | {:fallback, "[redacted]"}` — call once for `:job_args`, once for `:job_meta`. The kind atom is part of the host policy contract; the policy module sees these atoms in its `display/3` callback.

From lib/oban_powertools/web/selectors.ex (43-01):
- `Selectors.jobs_path(params)` — used to build back-link URL preserving filter state.

From lib/oban_powertools/web/jobs_live.ex (43-02):
- `mount/3` branch-on-action for `:show` already calls `LiveAuth.authorize_page(socket, :view_job_detail, %{type: :job, id: params["id"]})` and `DisplayPolicy.assert_configured!()` — Wave 3 does NOT modify `mount/3`.
- `handle_params/3` for `%{"id" => id}` is the stub Wave 2 left behind; this wave replaces it.
- `defp repo/0`, `defp timestamp_copy/1`, `defp state_badge_class/1`, `defp short_worker_name/1` — already present from Wave 2; reuse them.

UI-SPEC Component Inventory (Job Detail Page section) — exact Tailwind classes and copy strings are normative:
- Page header `h1.text-2xl.font-semibold` "Job #<%= @job.id %>" + back link styled `text-indigo-700 underline`
- Read-only banner same as list page, sourced from `LiveAuth.page_read_only_banner(:job_detail)`
- Identity card: `div.rounded-lg.border.bg-white.p-4`
- Args panel: `div.rounded-lg.border.bg-white.p-4` with `h2.text-base.font-semibold` reading `Args`
- Meta panel: same structure, heading `Meta`
- Errors panel: same structure, heading `Errors`; empty body reads `No errors recorded for this job.`
- Attempt history panel: same structure, heading `Attempt History`; empty body reads `No attempt history available.`
- JSON-display `<pre>` element: `pre.text-sm.bg-slate-50.p-3.rounded.overflow-x-auto`
- Fallback display: `span.text-zinc-500` with `[redacted]` text
- Layout: outer `div.space-y-6.p-6`; on `xl` viewport args/meta lay out side-by-side via `xl:grid-cols-2 gap-6`

UI-SPEC Copywriting Contract — verbatim strings:
- Job detail title: `Job #<id>` (use `Job ##{@job.id}` interpolation)
- Back link: `Back to Jobs`
- Args section heading: `Args`
- Meta section heading: `Meta`
- Errors section heading: `Errors`
- Attempt history heading: `Attempt History`
- Errors empty: `No errors recorded for this job.`
- Attempt history empty: `No attempt history available.`
- Job-not-found copy: `Job not found. It may have been pruned or the ID is invalid. Return to the job list.`

UI-SPEC Interaction Contract — DisplayPolicy redaction:
- `{:raw_json, json}` → render inside `pre.text-sm.bg-slate-50.p-3.rounded.overflow-x-auto`
- `{:string, text}` → render inside the same `<pre>` block (string treated as host-formatted text)
- `{:fallback, msg}` → render as `<span class="text-zinc-500"><%= msg %></span>` (muted text, never crashes)

Oban.Job error record shape (per RESEARCH.md Code Examples):
- `job.errors :: [%{"at" => iso8601_string, "attempt" => integer, "error" => string_with_backtrace}]`
- Errors list may be empty (`[]`) — render the empty body in that case.

Oban.Job timing fields used:
- `inserted_at, scheduled_at` — always present.
- `attempted_at` — present if a worker has started the job at least once; otherwise `nil`.
- `completed_at, cancelled_at, discarded_at` — present only for the corresponding terminal state.
- Per D-14, render only non-nil timing fields.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Replace the Wave 2 :show stub — implement handle_params + render for the detail view with DisplayPolicy redaction</name>
  <files>lib/oban_powertools/web/jobs_live.ex</files>
  <read_first>
    - lib/oban_powertools/web/jobs_live.ex (the entire file — Wave 2 left a stub at the `handle_params(%{"id" => id}, ...)` clause and a placeholder render branch under `:show`)
    - lib/oban_powertools/web/lifeline_live.ex (for the error-record rendering analog around its error display block — match field labels and `<pre>` structure)
    - lib/oban_powertools/web/workflows_live.ex (for the `<.link navigate=...>` back-link pattern)
    - lib/oban_powertools/runtime_config.ex (`render_job_field/3` return tuple shape — the LiveView matches on the first element)
    - .planning/phases/43-read-only-job-browse/43-CONTEXT.md (D-08, D-14, D-15, D-16, D-18)
    - .planning/phases/43-read-only-job-browse/43-UI-SPEC.md (Job Detail Page Component Inventory, Copywriting Contract for detail-page strings, Interaction Contract DisplayPolicy Redaction)
    - .planning/phases/43-read-only-job-browse/43-PATTERNS.md (Pattern Assignments — render_job_field tagged-tuple rendering)
  </read_first>
  <action>
Modify `lib/oban_powertools/web/jobs_live.ex` in place. Three changes:

**Change 1: Replace the Wave 2 `handle_params` `:show` clause.** The current stub is:

`def handle_params(%{"id" => id}, _uri, socket), do: {:noreply, assign(socket, :job, nil) |> assign(:job_id, id)}` (or equivalent stub from Wave 2).

Replace it with a real `load_job_detail/2` call:

`def handle_params(%{"id" => id}, _uri, socket), do: {:noreply, load_job_detail(socket, id)}`

Add a new private function `defp load_job_detail(socket, job_id)` that:

- Calls `Jobs.get(repo(), job_id)`.
- If the return is `nil`, assigns `:job` to `nil`, `:job_not_found?` to `true`, leaves `:args_display` and `:meta_display` unset (or sets them to `nil`), and returns the socket unchanged otherwise. The render must check `:job_not_found?` and emit the not-found copy.
- If the return is an `%Oban.Job{} = job`, computes `args_display = DisplayPolicy.render_job_field(:job_args, job.args, %{job: job})` and `meta_display = DisplayPolicy.render_job_field(:job_meta, job.meta, %{job: job})`. The context map carries `%{job: job}` so the host policy can inspect job context if needed.
- Assigns `:job`, `:args_display`, `:meta_display`, `:job_not_found?` (false), and `:back_path` (computed via `back_path_from_session/1` — see helper below).

Add a new private helper `defp back_path_from_session(socket)` that returns `Selectors.jobs_path([{"state", socket.assigns.filter && socket.assigns.filter.state}, {"queue", socket.assigns.filter && socket.assigns.filter.queue}, {"worker", socket.assigns.filter && socket.assigns.filter.worker}, {"tags", socket.assigns.filter && socket.assigns.filter.tags && Enum.join(socket.assigns.filter.tags, ",")}])`. If the user landed directly on the detail URL (no prior list view in this LiveView session), `socket.assigns.filter` may be nil — in that case fall back to `Selectors.jobs_path([])` which returns `/ops/jobs/jobs`. This is acceptable per RESEARCH.md Open Question 2 — best-effort back-link, not URL-param-persisted.

**Change 2: Replace the Wave 2 `:show` render branch.** The Wave 2 stub render block (the placeholder `div.text-sm.text-zinc-600` reading "Job detail loading — Wave 3 owns this view.") must be removed. Replace with the real detail render.

The `:show` render branch must emit:

1. Outer wrapper `div.space-y-6.p-6`.
2. Branch on `@job_not_found?`:
   - If true: render `div.rounded-lg.border.bg-white.p-6` containing `h1.text-2xl.font-semibold` reading `Job not found` and `p.mt-2.text-sm.text-zinc-600` reading the UI-SPEC copy `Job not found. It may have been pruned or the ID is invalid. Return to the job list.` plus a `<.link navigate={Selectors.jobs_path([])}>` back link with text `Back to Jobs`.
3. If `@job_not_found?` is false (the normal case):
   - Header: `<h1 class="text-2xl font-semibold">Job #<%= @job.id %></h1>` + back link `<.link navigate={@back_path} class="text-indigo-700 underline">Back to Jobs</.link>`.
   - Read-only banner: `<p :if={@read_only?} class="rounded-lg border border-amber-200 bg-amber-50 px-4 py-3 text-sm text-amber-800"><%= LiveAuth.page_read_only_banner(:job_detail) %></p>`.
   - Identity card: `div.rounded-lg.border.bg-white.p-4` containing definition-list-style rows for worker (short module name via `short_worker_name(@job.worker)`), queue, state badge (using `state_badge_class(@job.state)`), job id, and attempt (`<%= @job.attempt %> / <%= @job.max_attempts %>`). Render non-nil timing fields below the identity row: `inserted_at`, `scheduled_at`, `attempted_at`, `completed_at`, `cancelled_at`, `discarded_at` — each formatted with `timestamp_copy/1`, each rendered conditionally with `<dt :if={value} ...>` / `<dd :if={value} ...>` so nil fields are hidden per D-14.
   - Args panel and Meta panel side-by-side on `xl` viewports (`div.grid.gap-6.xl:grid-cols-2`). Each panel uses the helper `render_display_field/2` (described below) and the UI-SPEC copy `Args`/`Meta` headings.
   - Errors panel: `div.rounded-lg.border.bg-white.p-4` with `h2.text-base.font-semibold` reading `Errors`. Body branches: if `@job.errors == []`, render `<p class="text-sm text-zinc-600">No errors recorded for this job.</p>`. Otherwise iterate via `:for={err <- @job.errors}` rendering each error as `div.rounded.border.bg-slate-50.p-3.text-sm.space-y-1` containing the attempt number (`<span class="font-semibold">Attempt <%= err["attempt"] %></span>`), the `at` timestamp (formatted; use `err["at"]` passed through `timestamp_copy/1`), and the error text wrapped in `<pre class="text-sm whitespace-pre-wrap">`. Match Lifeline's existing error display field selection per Claude's Discretion item in CONTEXT.md.
   - Attempt history panel: `div.rounded-lg.border.bg-white.p-4` with `h2.text-base.font-semibold` reading `Attempt History`. Render a small table or list of attempt entries; each entry surfaces the attempt number (`1..job.attempt`) and any matching error record's timestamp. For Phase 43, deriving attempts from `job.errors` plus the current `job.attempt` is acceptable — there is no separate attempt-history table. If `job.attempt == 0` and `job.errors == []`, render `<p class="text-sm text-zinc-600">No attempt history available.</p>`.

**Change 3: Add a `render_display_field/2` private helper** that consumes the tagged tuple from `DisplayPolicy.render_job_field/3` and emits the corresponding HEEX. Implement as a function returning a HEEX fragment via `Phoenix.Component.assigns_to_attributes/2` or — more directly — as a small render helper using `Phoenix.LiveView.HTMLEngine`. Easier alternative: emit the three branches inline in the detail render using a pattern match expression:

- For `{:raw_json, json}`: render `<pre class="text-sm bg-slate-50 p-3 rounded overflow-x-auto"><%= json %></pre>`
- For `{:string, text}`: render `<pre class="text-sm bg-slate-50 p-3 rounded overflow-x-auto"><%= text %></pre>`
- For `{:fallback, msg}`: render `<span class="text-zinc-500"><%= msg %></span>`

Either approach is acceptable as long as a code reviewer can verify all three branches are reachable. Prefer a single `defp render_display_field(assigns, display_tuple)` Phoenix function-component that uses `assigns_to_attributes` if the inline branching gets verbose; otherwise inline `cond do match?({:raw_json, _}, x) -> ... ; match?({:string, _}, x) -> ... ; true -> ... end` inside the template via `case` blocks.

Anti-patterns to avoid:

- DO NOT add any mutation controls (retry/cancel/discard buttons) — Phase 43 is read-only per D-15.
- DO NOT call `Jason.encode!/2` directly in the LiveView — the `render_job_field/3` helper already returns pre-encoded JSON in the `{:raw_json, _}` tuple.
- DO NOT call `Oban.Job` runtime functions — read-only context.
- DO NOT short-circuit DisplayPolicy by reading `job.args` raw — every render goes through `render_job_field/3`.
- DO NOT crash on `nil` `job.errors` (defensive — Oban may emit `[]` but never nil; still guard the `:for=` against nil with `@job.errors || []`).
- DO NOT delete the existing list-page render code added in Wave 2 — this task only modifies the `:show` branch and adds the helpers; the `:index` branch must remain untouched.
- DO NOT delete `socket.assigns.filter` plumbing for `:show` — the back link relies on it. If `filter` is nil in `:show` mount (which it will be on a direct URL hit), the `back_path_from_session/1` helper falls back to the bare path.
  </action>
  <verify>
    <automated>mix compile --warnings-as-errors 2>&amp;1 | grep -E "warning|error" | grep -v "Compiling" || echo "clean"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'load_job_detail' lib/oban_powertools/web/jobs_live.ex` returns at least `2` (def + call site).
    - `grep -c 'Jobs.get(' lib/oban_powertools/web/jobs_live.ex` returns at least `1`.
    - `grep -c 'DisplayPolicy.render_job_field(:job_args' lib/oban_powertools/web/jobs_live.ex` returns `1`.
    - `grep -c 'DisplayPolicy.render_job_field(:job_meta' lib/oban_powertools/web/jobs_live.ex` returns `1`.
    - `grep -c '@job_not_found?\|job_not_found?:' lib/oban_powertools/web/jobs_live.ex` returns at least `2` (assignment + template check).
    - `grep -c 'page_read_only_banner(:job_detail)' lib/oban_powertools/web/jobs_live.ex` returns `1`.
    - `grep -c '@back_path\|:back_path' lib/oban_powertools/web/jobs_live.ex` returns at least `2`.
    - `grep -c 'Wave 3 owns this view' lib/oban_powertools/web/jobs_live.ex` returns `0` (Wave 2 stub gone).
    - `grep -c 'Job not found' lib/oban_powertools/web/jobs_live.ex` returns at least `1` (UI-SPEC not-found copy).
    - `grep -c 'Back to Jobs' lib/oban_powertools/web/jobs_live.ex` returns at least `1` (back link text).
    - `grep -cE 'Args|Meta|Errors|Attempt History' lib/oban_powertools/web/jobs_live.ex` returns at least `4` (one per panel heading).
    - `grep -c 'No errors recorded for this job' lib/oban_powertools/web/jobs_live.ex` returns `1`.
    - `grep -c 'No attempt history available' lib/oban_powertools/web/jobs_live.ex` returns `1`.
    - `grep -c ':raw_json' lib/oban_powertools/web/jobs_live.ex` returns at least `1` (display tuple pattern).
    - `grep -c ':fallback' lib/oban_powertools/web/jobs_live.ex` returns at least `1`.
    - `grep -c 'xl:grid-cols-2' lib/oban_powertools/web/jobs_live.ex` returns `1` (UI-SPEC §Job Detail Page layout).
    - `grep -c 'text-indigo-700 underline' lib/oban_powertools/web/jobs_live.ex` returns at least `1` (back-link styling).
    - `grep -c 'font-medium' lib/oban_powertools/web/jobs_live.ex` returns `0` (UI-SPEC 2-weight constraint).
    - `mix compile --warnings-as-errors` exits 0.
  </acceptance_criteria>
  <done>The `:show` action loads a job via `Jobs.get/2`, redacts args and meta via `DisplayPolicy.render_job_field/3`, and renders the full UI-SPEC detail layout including identity card, args/meta panels, errors panel, attempt history panel, and back link. The Wave 2 stub render is removed. The not-found branch handles a missing job id without crashing. `mix compile --warnings-as-errors` exits 0.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend jobs_live_test.exs with detail-page integration tests covering DisplayPolicy redaction arms, not-found, and unauthorized</name>
  <files>test/oban_powertools/web/live/jobs_live_test.exs</files>
  <read_first>
    - test/oban_powertools/web/live/jobs_live_test.exs (existing Wave 2 test file — extend, do not replace; the inline `ObanPowertools.Web.JobsLiveTestDisplayPolicy` module must be expanded to cover the redaction arms)
    - lib/oban_powertools/web/jobs_live.ex (the detail view, post-Task-1)
    - test/oban_powertools/web/live/workflows_live_test.exs (analog for `Application.put_env(:oban_powertools, :display_policy, ...)` redirection between tests with different policies)
    - .planning/phases/43-read-only-job-browse/43-RESEARCH.md (Validation Architecture → Phase Requirements → Test Map; QRY-02 rows)
    - .planning/phases/43-read-only-job-browse/43-UI-SPEC.md (Copywriting Contract — detail-page strings)
    - .planning/phases/43-read-only-job-browse/43-CONTEXT.md (D-16 return contract — informs the test policy modules)
  </read_first>
  <action>
Extend `test/oban_powertools/web/live/jobs_live_test.exs`. Two structural additions:

**Addition 1: Add three new test-display-policy modules at the top of the file** (each in its own `defmodule`):

- `ObanPowertools.Web.JobsLiveDetailNilPolicy` — `display(:job_args, _, _), do: nil`, `display(:job_meta, _, _), do: nil`, catch-all `display(_, _, _), do: nil`. Exercises the `{:raw_json, _}` arm via the nil → JSON path.
- `ObanPowertools.Web.JobsLiveDetailStringPolicy` — `display(:job_args, _, _), do: "args text from policy"`, `display(:job_meta, _, _), do: "meta text from policy"`, catch-all `display(_, _, _), do: nil`. Exercises the `{:string, _}` arm.
- `ObanPowertools.Web.JobsLiveDetailMapPolicy` — `display(:job_args, args, _), do: Map.put(args, "secret", "REDACTED")` (or similar host-redaction shape), `display(:job_meta, meta, _), do: %{"trace_id" => "abc-redacted"}`, catch-all `display(_, _, _), do: nil`. Exercises the `{:raw_json, _}` arm via the Map → JSON path.
- `ObanPowertools.Web.JobsLiveDetailRaisingPolicy` — `display(:job_args, _, _), do: raise("intentional test failure")`, `display(:job_meta, _, _), do: "meta ok"`, catch-all `display(_, _, _), do: nil`. Exercises the `{:fallback, "[redacted]"}` rescue arm.

These modules must compile but never execute during Wave 2 tests — each new detail test uses `Application.put_env/3` to swap the policy inside the test body, with restoration handled by either a local `setup` or an inline `on_exit`.

**Addition 2: Add a `describe "Detail page" do ... end` block** containing at minimum these tests:

1. **"redirects unauthorized viewers from detail page"** — actor with no permissions. `live(conn, "/ops/jobs/jobs/1")` returns `{:error, {:redirect, %{to: "/"}}}`.

2. **"renders job detail with identity, timing, and panel headings"** — actor with `[:view_job_detail]`. Insert one job. `live(conn, "/ops/jobs/jobs/#{job.id}")`. Assert HTML contains `"Job ##{job.id}"`, `"Back to Jobs"`, `"Args"`, `"Meta"`, `"Errors"`, `"Attempt History"`. Assert the worker short name (last `.`-segment) is rendered. Assert the `inserted_at` is rendered via `timestamp_copy/1` (just assert presence of `"ago"` or `"UTC"` substring from the formatter).

3. **"renders args as raw pretty JSON when policy returns nil"** — actor with `[:view_job_detail]`. Insert one job with `args: %{"id" => 42, "action" => "ingest"}`. Swap `Application.put_env(:oban_powertools, :display_policy, ObanPowertools.Web.JobsLiveDetailNilPolicy)`; on_exit restore. `live(conn, "/ops/jobs/jobs/#{job.id}")`. Assert HTML contains the JSON-formatted `"\"id\""` and `"\"action\""` keys (or equivalent substrings proving pretty JSON output). Assert HTML contains a `<pre` element wrapping the JSON.

4. **"renders args as host string when policy returns a String"** — swap to `JobsLiveDetailStringPolicy`. Insert a job. Assert HTML contains `"args text from policy"` and `"meta text from policy"`.

5. **"renders args/meta as redacted JSON when policy returns a Map"** — swap to `JobsLiveDetailMapPolicy`. Insert a job with `args: %{"id" => 7, "secret" => "TOP"}`. Assert HTML contains `"REDACTED"` (the map-arm output) and contains `"trace_id"` (from the meta path).

6. **"renders [redacted] fallback when policy raises"** — swap to `JobsLiveDetailRaisingPolicy`. Insert a job. Assert HTML contains `"[redacted]"` for the args panel (and the meta panel still renders the string `"meta ok"` since only args raises). The page must NOT crash.

7. **"renders 'Job not found' message for unknown id"** — actor with `[:view_job_detail]`. `live(conn, "/ops/jobs/jobs/999999999")`. Assert HTML contains the UI-SPEC not-found copy `"Job not found. It may have been pruned or the ID is invalid."` and a back link with `"Back to Jobs"` text.

8. **"renders errors panel with attempt records when errors exist"** — actor with `[:view_job_detail]`. Insert a job; update via changeset to set `errors: [%{"at" => "2026-05-27T10:00:00Z", "attempt" => 1, "error" => "some failure\nbacktrace line"}]` and `state: "retryable"`. Render the detail page. Assert HTML contains `"Attempt 1"` and `"some failure"`.

9. **"renders 'No errors recorded' empty state"** — actor with `[:view_job_detail]`. Insert a job with `errors: []` (the default for a brand-new job). Assert HTML contains `"No errors recorded for this job."`.

10. **"detail page read-only banner appears for actor without retry permission"** — actor with `[:view_job_detail]` only. Render the detail page. Assert HTML contains `"Job detail stays visible"`.

11. **"detail page back link points to /ops/jobs/jobs"** — actor with `[:view_job_detail]`. Render the detail page. Assert the rendered HTML contains the substring `"/ops/jobs/jobs"` inside a back-link element (anchor or `.link navigate`). The exact href shape may include or omit query params depending on whether `socket.assigns.filter` is populated on a direct URL hit — assert the bare path is present.

All tests must run alongside the existing Wave 2 tests without policy-state leakage. Use a per-test `Application.put_env` inside the test body wrapped in `on_exit(fn -> Application.put_env(:oban_powertools, :display_policy, original) end)` to restore between tests, OR re-use the `setup` block by extending it to capture-and-restore the policy that each test installs. The simpler pattern is per-test on_exit hooks; the existing setup remains the safety net.

Do NOT remove or weaken any Wave 2 test in this file — extend only.
  </action>
  <verify>
    <automated>mix test test/oban_powertools/web/live/jobs_live_test.exs --max-failures 1</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'JobsLiveDetailNilPolicy' test/oban_powertools/web/live/jobs_live_test.exs` returns at least `2` (defmodule + put_env reference).
    - `grep -c 'JobsLiveDetailStringPolicy' test/oban_powertools/web/live/jobs_live_test.exs` returns at least `2`.
    - `grep -c 'JobsLiveDetailMapPolicy' test/oban_powertools/web/live/jobs_live_test.exs` returns at least `2`.
    - `grep -c 'JobsLiveDetailRaisingPolicy' test/oban_powertools/web/live/jobs_live_test.exs` returns at least `2`.
    - `grep -c 'describe "Detail page"' test/oban_powertools/web/live/jobs_live_test.exs` returns `1`.
    - `grep -cE '^[[:space:]]*test "' test/oban_powertools/web/live/jobs_live_test.exs` returns at least `21` (10+ Wave 2 list tests + 11 detail tests above).
    - `grep -c 'Job ##' test/oban_powertools/web/live/jobs_live_test.exs` returns at least `1` (detail header assertion).
    - `grep -c 'Back to Jobs' test/oban_powertools/web/live/jobs_live_test.exs` returns at least `1`.
    - `grep -c 'Job not found. It may have been pruned' test/oban_powertools/web/live/jobs_live_test.exs` returns at least `1`.
    - `grep -c '\[redacted\]' test/oban_powertools/web/live/jobs_live_test.exs` returns at least `1`.
    - `grep -c 'Job detail stays visible' test/oban_powertools/web/live/jobs_live_test.exs` returns at least `1`.
    - `grep -c 'Attempt 1' test/oban_powertools/web/live/jobs_live_test.exs` returns at least `1`.
    - `grep -c 'No errors recorded for this job' test/oban_powertools/web/live/jobs_live_test.exs` returns at least `1`.
    - `grep -c ':view_job_detail' test/oban_powertools/web/live/jobs_live_test.exs` returns at least `8` (most detail tests carry it).
    - `mix test test/oban_powertools/web/live/jobs_live_test.exs` exits 0 (all 21+ tests pass).
  </acceptance_criteria>
  <done>All 21+ integration tests pass. The detail tests cover all 4 DisplayPolicy return arms (nil → raw JSON, String → string, Map → redacted JSON, raise → fallback) plus unauthorized redirect, not-found copy, errors panel rendering, empty errors copy, read-only banner gate, and back link presence — collectively proving QRY-02 acceptance criteria end-to-end.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → LiveView mount | Untrusted `params["id"]` reaches `:show` mount; bound only as a query param to `Jobs.get/2`. |
| Oban.Job.args / Oban.Job.meta → host display_policy | Untrusted job payload data crosses the redaction seam; the host policy is also untrusted code from this module's perspective. |
| host display_policy return → render | Tagged-tuple return is the only sanctioned path to the rendered output. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-43-03-01 | Information Disclosure | sensitive `args`/`meta` exposed to unauthorized actors | mitigate | `:show` mount calls `LiveAuth.authorize_page(socket, :view_job_detail, %{type: :job, id: id})` (Wave 2). Unauthorized actors are redirected before any data load; Detail Test 1 asserts the redirect. |
| T-43-03-02 | Information Disclosure | host policy returns junk and raw `args`/`meta` is exposed | mitigate | `render_job_field/3`'s `rescue → {:fallback, "[redacted]"}` path (Wave 1) catches any exception OR invalid return shape. Detail Test 6 asserts a raising policy produces the `[redacted]` fallback rather than crashing or leaking raw args. |
| T-43-03-03 | Information Disclosure | bypassed redaction via direct `<%= @job.args %>` interpolation in the template | mitigate | Template renders `args_display` and `meta_display` (the tagged tuple) — NEVER `@job.args` or `@job.meta` directly. `grep` gate `'render_job_field(:job_args'` returns 1 confirms the seam is the sole render path. |
| T-43-03-04 | Tampering | path-traversal via id parameter | mitigate | `params["id"]` is bound through `Jobs.get(repo, id)` which uses `repo.get(Oban.Job, id)` — Ecto coerces the id to the schema's integer type and parameterizes the query; non-integer ids fail with a typed cast error before hitting the DB. |
| T-43-03-05 | Denial of Service | unbounded errors-list render on a job with thousands of error records | accept | Oban.Job errors-list growth is bounded by `max_attempts` (typically <30). No iteration cap needed at the LiveView render layer. |
| T-43-03-06 | Repudiation | none | accept | Phase 43 is read-only; no mutation occurs in this plan. |
| T-43-03-07 | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan. |
</threat_model>

<verification>
- `mix compile --warnings-as-errors` exits 0.
- `mix test test/oban_powertools/web/live/jobs_live_test.exs` exits 0 (21+ tests).
- `mix test test/oban_powertools/jobs_test.exs` exits 0 (regression check on Wave 1 unit tests).
- `mix test` exits 0 (full suite — no regressions in `workflows_live_test.exs`, `lifeline_live_test.exs`, `selectors_test.exs`, etc.).
- D-08, D-14, D-15, D-16, D-18 are observable in source via the grep gates above.
- All four `DisplayPolicy.render_job_field/3` return arms have at least one passing test.
</verification>

<success_criteria>
1. `JobsLive.handle_params/3` `:show` clause replaced — calls `Jobs.get/2` and `DisplayPolicy.render_job_field/3` per D-08 and D-16.
2. Detail render includes identity card, args panel, meta panel, errors panel, attempt history panel, and timing fields per D-14.
3. Args and meta panels render the three DisplayPolicy arms (`{:raw_json, _}`, `{:string, _}`, `{:fallback, _}`) correctly — covered by Detail Tests 3-6.
4. Read-only banner sources from `LiveAuth.page_read_only_banner(:job_detail)` per D-19.
5. Job-not-found case renders the UI-SPEC copy without crashing.
6. Back link navigates to `/ops/jobs/jobs` preserving filter state when available.
7. `font-semibold` typography contract maintained — `grep -c 'font-medium' lib/oban_powertools/web/jobs_live.ex` returns `0`.
8. All 21+ LiveView integration tests pass.
9. `mix compile --warnings-as-errors` exits 0; `mix test` exits 0.
10. Phase 43 ROADMAP success criteria 1-5 all satisfied across this plan + Wave 1 + Wave 2 evidence.
</success_criteria>

<output>
Create `.planning/phases/43-read-only-job-browse/43-03-SUMMARY.md` when done. Include:
- File-by-file diff summary (jobs_live.ex render delta, jobs_live_test.exs test additions)
- LiveView test counts (passed/skipped/excluded; should be 21+)
- Confirmation that all 4 DisplayPolicy return arms are exercised by passing tests
- Confirmation that D-08, D-14, D-15, D-16, D-18 are implemented
- Phase 43 ROADMAP success criteria checkbox status (1-5)
- Note: Phase 43 is now ready for `/gsd:verify-work 43`
</output>
