---
phase: 40-phase-34-manual-acceptance-closure
plan: 02
type: execute
wave: 1
depends_on:
  - 40-01
files_modified:
  - .github/workflows/host-contract-proof.yml
  - test/oban_powertools/docs_contract_test.exs
  - .planning/phases/39-ci-continuity-proof-lane-closure/39-PROOF-MANIFEST.json
  - .planning/phases/40-phase-34-manual-acceptance-closure/40-02-SUMMARY.md
autonomous: true
requirements:
  - OPS-03
  - RNB-01
  - RNB-02
requirements_addressed:
  - OPS-03
  - RNB-01
  - RNB-02
security:
  asvs: L1
  block_on_high: true
must_haves:
  truths:
    - "Phase 40 CI shift-left extends existing continuity lanes (`continuity-ver04-c3` and `continuity-ver04-c4`) only; no new CI jobs are introduced."
    - "C3 covers the visual-hierarchy proxy by including `engine_overview_live_test.exs`; C4 covers the copy-contract proxy by including `runbook_copy_contract_test.exs`."
    - "Proof packet publishes an explicit `phase40-gate-report.json` so downstream audits can confirm Phase 40 coverage without scraping logs."
  artifacts:
    - path: ".github/workflows/host-contract-proof.yml"
      provides: "Expanded C3/C4 claim command coverage and proof packet publication"
    - path: "test/oban_powertools/docs_contract_test.exs"
      provides: "Drift protection for continuity command fragments and Phase 40 gate markers"
    - path: ".planning/phases/39-ci-continuity-proof-lane-closure/39-PROOF-MANIFEST.json"
      provides: "C3/C4 artifact_refs reference the new phase40-gate-report.json"
---

<objective>
Wire the Plan 40-01 proxy tests into the existing C3/C4 continuity lanes so a regression that drops the Phase 40 automated gate breaks merge-blocking CI. Add a small `phase40-gate-report.json` aggregate artifact to the existing `continuity-proof-status` job; do not add new top-level jobs.

Purpose: shift Phase 40 acceptance left without growing CI topology.
Output: surgical edits to `host-contract-proof.yml`, an artifact reference in `39-PROOF-MANIFEST.json`, and command-fragment locks in `docs_contract_test.exs`.
</objective>

<context>
@.github/workflows/host-contract-proof.yml
@test/oban_powertools/docs_contract_test.exs
@.planning/phases/39-ci-continuity-proof-lane-closure/39-PROOF-MANIFEST.json
@test/oban_powertools/web/live/engine_overview_live_test.exs
@test/oban_powertools/web/live/forensics_live_test.exs
@test/oban_powertools/web/live/lifeline_live_test.exs
@test/oban_powertools/web/live/workflows_live_test.exs
</context>

<exact_additions>

## continuity-ver04-c3 command (replace existing command string)

```
mix test test/oban_powertools/lifeline_test.exs test/oban_powertools/web/live/lifeline_live_test.exs test/oban_powertools/web/live/forensics_live_test.exs test/oban_powertools/web/live/engine_overview_live_test.exs --seed 0
```

Rationale: adds the engine overview live test so the visual-hierarchy proxy (added in Plan 40-01) runs in the same merge-blocking lane that already proves lifeline/forensics continuity.

## continuity-ver04-c4 command (replace existing command string)

```
mix test test/oban_powertools/forensics_test.exs test/oban_powertools/lifeline_test.exs test/oban_powertools/web/live/audit_live_test.exs test/oban_powertools/web/live/workflows_live_test.exs test/oban_powertools/web/live/runbook_copy_contract_test.exs --seed 0 && mix test test/oban_powertools/docs_contract_test.exs --seed 0
```

Rationale: adds the workflows live test and the new `runbook_copy_contract_test.exs` so the copy-contract proxy runs alongside the existing docs-contract gate.

## Proof packet additions (same aggregate job, no new topology)

In `continuity-proof-status`, generate and upload:
- `tmp/ver04/phase40-gate-report.json`

Report fields (written with the same `cat <<EOF` pattern already used in the job):
```
{
  "coverage_scope": "phase40",
  "checked_claims": ["VER04-C3", "VER04-C4"],
  "required_markers": ["ownership-triad", "runbook-ordering", "overview-continuity", "copy-contract", "visual-hierarchy"],
  "status": "pass"
}
```

`status` is computed from `needs.continuity-ver04-c3.result` and `needs.continuity-ver04-c4.result` (both must be `success`; otherwise `fail`).

Upload it via an existing `actions/upload-artifact@v4` step in the same job with `if-no-files-found: error` and a clearly named artifact (e.g. `phase40-gate-report`).

## 39-PROOF-MANIFEST.json updates

For both the `VER04-C3` and `VER04-C4` claim entries, add `"phase40_gate_report": "tmp/ver04/phase40-gate-report.json"` to `artifact_refs`. Do not change `claim_id`, `requirement_id`, `workflow_job`, or `status_source`. Update `command` strings to match the exact strings above.

## docs_contract_test.exs marker locks

Extend the existing workflow-locks test (or add a new sibling test) so future workflow edits cannot silently drop Phase 40 coverage:
- `assert source =~ "engine_overview_live_test.exs"`
- `assert source =~ "workflows_live_test.exs"`
- `assert source =~ "runbook_copy_contract_test.exs"`
- `assert source =~ "phase40-gate-report.json"`
</exact_additions>

<tasks>
<task type="auto">
  <name>Task 1: Extend C3/C4 deterministic claim commands without adding CI jobs</name>
  <files>.github/workflows/host-contract-proof.yml</files>
  <action>Replace only the `CLAIM_COMMAND` strings inside `continuity-ver04-c3` and `continuity-ver04-c4` with the exact strings in `<exact_additions>`. Do not introduce new job names or change artifact upload structure for these two jobs.</action>
  <acceptance_criteria>
    - `continuity-ver04-c3` includes `engine_overview_live_test.exs`.
    - `continuity-ver04-c4` includes `workflows_live_test.exs` and `runbook_copy_contract_test.exs`.
    - no new `continuity-ver04-*` job names are introduced.
    - `--seed 0` suffix preserved on both claim commands.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Add phase40-gate-report.json to continuity-proof-status and update manifest</name>
  <files>.github/workflows/host-contract-proof.yml, .planning/phases/39-ci-continuity-proof-lane-closure/39-PROOF-MANIFEST.json</files>
  <action>In `continuity-proof-status`, write `tmp/ver04/phase40-gate-report.json` (status computed from C3/C4 job results) and upload it via `actions/upload-artifact@v4` with `if-no-files-found: error`. In `39-PROOF-MANIFEST.json`, add `phase40_gate_report` to C3 and C4 `artifact_refs` and update their `command` fields to match the new claim commands.</action>
  <acceptance_criteria>
    - aggregate lane writes `phase40-gate-report.json` and uploads it with `if-no-files-found: error`.
    - manifest C3/C4 sections reference the new artifact path.
    - manifest C3/C4 `command` strings exactly match the new workflow `CLAIM_COMMAND` strings.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Lock command fragments in docs-contract tests</name>
  <files>test/oban_powertools/docs_contract_test.exs</files>
  <action>Add marker assertions ensuring future workflow edits cannot silently drop Phase 40 coverage (engine overview, workflows live, runbook copy contract, phase40-gate-report.json). Keep the test marker-based (no YAML parser dependency).</action>
  <acceptance_criteria>
    - docs-contract tests assert presence of `engine_overview_live_test.exs`, `workflows_live_test.exs`, `runbook_copy_contract_test.exs`, and `phase40-gate-report.json` in the workflow file.
    - test remains marker-based.
  </acceptance_criteria>
</task>
</tasks>

<verification>
- `rg -n "continuity-ver04-c3|engine_overview_live_test\\.exs|--seed 0" .github/workflows/host-contract-proof.yml`
- `rg -n "continuity-ver04-c4|workflows_live_test\\.exs|runbook_copy_contract_test\\.exs|docs_contract_test\\.exs" .github/workflows/host-contract-proof.yml`
- `rg -n "phase40-gate-report\\.json" .github/workflows/host-contract-proof.yml .planning/phases/39-ci-continuity-proof-lane-closure/39-PROOF-MANIFEST.json`
- `mix test test/oban_powertools/docs_contract_test.exs --seed 0`
</verification>

<success_criteria>
- Deterministic gate checks for Phase 40 prerequisites run in existing C3/C4 lanes.
- No CI topology growth beyond lane command expansion and packet enrichment.
- Workflow drift that drops these checks fails docs-contract tests pre-merge.
- A Phase 40 gate report artifact is published for downstream audits.
</success_criteria>

<output>
After completion, create `.planning/phases/40-phase-34-manual-acceptance-closure/40-02-SUMMARY.md` recording the exact CLAIM_COMMAND strings, the manifest diff, the new docs-contract assertions, and how `phase40-gate-report.json` is computed.
</output>
