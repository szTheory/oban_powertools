---
phase: 70-brand-book-identity-foundation
plan: 02
type: execute
wave: 2
depends_on: ["70-01"]
files_modified:
  - mix.exs
  - config/dev.exs
  - config/test.exs
  - lib/oban_powertools/web/dev/brand_book_live.ex
  - lib/oban_powertools/web/router.ex
  - test/oban_powertools/web/live/brand_book_live_test.exs
  - README.md
autonomous: true
requirements: [DOC-03, BRAND-01]
must_haves:
  truths:
    - "Visiting /ops/jobs/_brand_book in dev renders the brand book HTML including D-01 and 'explain, then act'"
    - "ObanPowertools.Web.Dev.BrandBookLive does NOT compile/exist under MIX_ENV=prod"
    - "config :oban_powertools, dev_routes: true is set in config/dev.exs and config/test.exs"
    - "The README links the brand book via both the static guides/brand-book.md path and the dev route"
    - "The brand book Markdown is rendered with zero new runtime dependencies (compile-time EarmarkParser AST)"
  artifacts:
    - path: "lib/oban_powertools/web/dev/brand_book_live.ex"
      provides: "Dev-only LiveView rendering guides/brand-book.md at compile time"
      contains: "BrandBookLive"
    - path: "test/oban_powertools/web/live/brand_book_live_test.exs"
      provides: "LiveView render test gated by dev_routes compile_env"
      contains: "_brand_book"
    - path: "config/dev.exs"
      provides: "dev_routes flag enabling the brand book route in dev"
      contains: "dev_routes"
  key_links:
    - from: "lib/oban_powertools/web/router.ex"
      to: "lib/oban_powertools/web/dev/brand_book_live.ex"
      via: "live/3 route inside oban_powertools_routes macro, gated by compile_env(:dev_routes)"
      pattern: "_brand_book"
    - from: "lib/oban_powertools/web/dev/brand_book_live.ex"
      to: "guides/brand-book.md"
      via: "@external_resource + compile-time EarmarkParser.as_ast/2 of the brand book file"
      pattern: "brand-book\\.md"
    - from: "README.md"
      to: "guides/brand-book.md"
      via: "Brand Identity section static link + dev route link (DOC-03)"
      pattern: "brand-book"
  prohibitions:
    - "MUST NOT modify any of the 9 operator LiveView pages: lib/oban_powertools/web/{engine_overview,jobs,batches,workflows,cron,limiters,lifeline,audit,forensics}_live.ex (verified by git diff)"
    - "MUST NOT add any new runtime dependency to mix.exs deps/0 (earmark_parser is already a transitive dev dep; use it at compile time only)"
    - "MUST NOT set dev_routes: true in config/prod.exs (it must default to false in prod)"
    - "MUST NOT use a bare Mix.env() check inside the router macro quote block — use Application.compile_env"
    - "MUST NOT require auth/LiveAuth state beyond the existing dev mount — the route is dev-only and read-only"
---

<objective>
Build the delivery plumbing that renders the brand book at a dev-ONLY route and links it from the README (DOC-03 initial). Add `ObanPowertools.Web.Dev.BrandBookLive` (compile-time Markdown render, zero new runtime deps), mount `/ops/jobs/_brand_book` behind a `compile_env(:dev_routes)` guard inside the existing router macro, set the `dev_routes` flag in dev/test config, add the render test + prod-exclusion check, and add the README "Brand Identity" section.

Purpose: BRAND-01 requires the brand book be deliverable as Markdown PLUS a dev-rendered route; DOC-03 (initial) requires the README link. The route must be absent in prod builds.

Output: the BrandBookLive module, the route, the config flags, the render test, and the README section.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/70-brand-book-identity-foundation/70-RESEARCH.md
@.planning/phases/70-brand-book-identity-foundation/70-VALIDATION.md
@lib/oban_powertools/web/router.ex
@test/support/test_router.ex
@test/support/live_case.ex
@mix.exs
@config/dev.exs
</context>

<artifacts_this_phase_produces>
This plan (70-02) produces:
- `ObanPowertools.Web.Dev.BrandBookLive` — dev-only LiveView at `lib/oban_powertools/web/dev/brand_book_live.ex`
- The `/ops/jobs/_brand_book` route (added to `oban_powertools_routes/1` macro, gated by `compile_env(:dev_routes)`)
- The `:oban_powertools, :dev_routes` config key (config/dev.exs, config/test.exs)
- The README "Brand Identity" section (DOC-03 initial)
- `test/oban_powertools/web/live/brand_book_live_test.exs` — render test

Depends on Plan 70-01 having produced `guides/brand-book.md` (the LiveView reads it at compile time).
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Set dev_routes config flag (dev + test)</name>
  <read_first>
    - config/dev.exs (currently just `import Config`)
    - config/test.exs
    - config/prod.exs (confirm it must NOT set the flag)
    - .planning/phases/70-brand-book-identity-foundation/70-RESEARCH.md (§Web Layer & Route Mounting — the compile_env flag; Pitfall 1 and Pitfall 4)
  </read_first>
  <files>config/dev.exs, config/test.exs</files>
  <action>
    Add `config :oban_powertools, dev_routes: true` to config/dev.exs and config/test.exs so the router macro compiles the route and the LiveView module is defined in dev and test envs. Do NOT touch config/prod.exs — the macro fallback is `Mix.env() == :dev`, so prod defaults to false and the route stays absent. The flag must be evaluated via Application.compile_env in the router and module guard (Task 2/3), per Pitfall 1.
  </action>
  <verify>
    <automated>grep -q 'dev_routes: true' config/dev.exs && grep -q 'dev_routes: true' config/test.exs && ! grep -q 'dev_routes' config/prod.exs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'dev_routes: true' config/dev.exs` succeeds.
    - `grep -q 'dev_routes: true' config/test.exs` succeeds.
    - `grep -q 'dev_routes' config/prod.exs` FAILS (flag absent in prod — maps to 70-VALIDATION.md §4).
  </acceptance_criteria>
  <done>dev_routes is true in dev and test config and absent from prod config.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create BrandBookLive (compile-time Markdown render, zero new runtime deps) and render test</name>
  <read_first>
    - lib/oban_powertools/web/router.ex (existing LiveView mount idiom, Code.ensure_loaded? guard, LiveAuth on_mount)
    - test/support/live_case.ex (LiveView test harness)
    - test/support/test_router.ex (test router mounts oban_powertools_routes under /ops/jobs)
    - .planning/phases/70-brand-book-identity-foundation/70-RESEARCH.md (§Markdown Rendering Approach — EarmarkParser.as_ast/2 + ast_to_html; §Dev-Only Gating — module-level compile_env guard; Pitfall 2, Pitfall 5, Pitfall 6)
    - .planning/phases/70-brand-book-identity-foundation/70-VALIDATION.md (§3 render test, §4 prod-exclusion)
  </read_first>
  <files>lib/oban_powertools/web/dev/brand_book_live.ex, test/oban_powertools/web/live/brand_book_live_test.exs</files>
  <behavior>
    - Test: visiting /ops/jobs/_brand_book in the test env returns {:ok, _live, html} and the html contains "Brand Book", "D-01", and the explain-then-act phrase.
    - Test is wrapped in the same `Application.compile_env(:oban_powertools, :dev_routes, Mix.env() == :dev)` guard so it only compiles where the module exists.
    - Behavior: under MIX_ENV=prod, the module is not defined (Code.ensure_loaded? returns false).
  </behavior>
  <action>
    Write the render test FIRST (RED) at test/oban_powertools/web/live/brand_book_live_test.exs using the existing LiveView test harness (test/support/live_case.ex) and the test router. Gate the whole test module/block with `Application.compile_env(:oban_powertools, :dev_routes, Mix.env() == :dev)` so it only compiles when the route exists. Assert `live(conn, "/ops/jobs/_brand_book")` returns html matching "Brand Book", "D-01", and the explain-then-act phrase.

    Then implement lib/oban_powertools/web/dev/brand_book_live.ex (GREEN):
    - Wrap the entire `defmodule ObanPowertools.Web.Dev.BrandBookLive do ... end` in a module-level `if Application.compile_env(:oban_powertools, :dev_routes, Mix.env() == :dev) do ... end` guard so the module is NOT defined under prod (per 70-RESEARCH.md §Dev-Only Gating).
    - `use Phoenix.LiveView`.
    - Resolve the brand book path with `Path.join(__DIR__, "../../../../guides/brand-book.md")` per Pitfall 5 (compute the correct number of `..` segments from the module's source dir to the repo root `guides/`), set `@external_resource` to it so the LiveView recompiles when the brand book changes.
    - Render the Markdown to HTML at COMPILE TIME into a module attribute via `EarmarkParser.as_ast/2` (already a transitive dev dep — do NOT add a new dep; do NOT call EarmarkParser.as_html! which does not exist, per Pitfall 2), then convert the AST to an HTML string with a small private `ast_to_html/1` helper (or delegate to ExDoc.DocAST.to_html/1). The content is trusted committed Markdown; render with Phoenix.HTML.raw/1 and add a module comment that raw/1 is intentional for trusted version-controlled content, not user input (Pitfall 6).
    - `mount/3` requires no auth state beyond the existing dev mount and performs no mutations; `render/1` outputs the precomputed HTML inside a container div.
    - Verify the path resolves at compile time by running `mix compile --warnings-as-errors` in dev.
    Do NOT modify any of the 9 operator pages.
  </action>
  <verify>
    <automated>mix test test/oban_powertools/web/live/brand_book_live_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `lib/oban_powertools/web/dev/brand_book_live.ex` defines `ObanPowertools.Web.Dev.BrandBookLive` wrapped in a module-level `Application.compile_env(:oban_powertools, :dev_routes, ...)` guard.
    - The module uses `EarmarkParser.as_ast/2` (grep: `grep -q "EarmarkParser.as_ast" lib/oban_powertools/web/dev/brand_book_live.ex`) and does NOT call `EarmarkParser.as_html` (grep for `as_html!` returns nothing).
    - `mix.exs` deps/0 is unchanged — no new dependency added (zero new runtime deps prohibition).
    - The render test exists and is guarded by `compile_env(:oban_powertools, :dev_routes, ...)`.
    - `mix test test/oban_powertools/web/live/brand_book_live_test.exs` passes (route renders with "Brand Book", "D-01", explain-then-act — maps to 70-VALIDATION.md §3). NOTE: this passes only after Task 3 mounts the route; run the test after Task 3 if RED here is solely a missing-route error.
    - `git diff --name-only` shows none of the 9 operator `*_live.ex` pages modified.
  </acceptance_criteria>
  <done>BrandBookLive renders the brand book HTML from a compile-time EarmarkParser AST, is gated to dev/test by compile_env, adds zero new deps, and the guarded render test exists.</done>
</task>

<task type="auto">
  <name>Task 3: Mount the dev-only route in the router macro and verify prod exclusion</name>
  <read_first>
    - lib/oban_powertools/web/router.ex (the oban_powertools_routes/1 macro and the :oban_powertools_native live_session block)
    - .planning/phases/70-brand-book-identity-foundation/70-RESEARCH.md (§Adding the brand-book route — compile_env guard inside the live_session; Pitfall 1; §Tarball check / §4 prod-exclusion)
    - .planning/phases/70-brand-book-identity-foundation/70-VALIDATION.md (§3 render, §4 route absent in prod)
  </read_first>
  <files>lib/oban_powertools/web/router.ex</files>
  <action>
    Inside the existing `:oban_powertools_native` `live_session` block in `oban_powertools_routes/1`, add a route gated by `Application.compile_env(:oban_powertools, :dev_routes, Mix.env() == :dev)`:
      `live("/_brand_book", ObanPowertools.Web.Dev.BrandBookLive, :index)`
    Use Application.compile_env, NOT a bare Mix.env() inside the quote block (Pitfall 1 — Mix.env() in the macro evaluates the library's env at expansion time, not the host's). The route lands at /ops/jobs/_brand_book within the host-owned scope; the leading underscore matches the dev-only convention (same as the upcoming /_showcase). Do NOT modify the 12 existing native routes or the bridge routes. Do NOT touch the 9 operator pages.

    Then verify prod exclusion: compiling with MIX_ENV=prod must leave the module and route absent (70-VALIDATION.md §4).
  </action>
  <verify>
    <automated>grep -q '_brand_book' lib/oban_powertools/web/router.ex && grep -q 'compile_env' lib/oban_powertools/web/router.ex && mix test test/oban_powertools/web/live/brand_book_live_test.exs && MIX_ENV=prod mix compile --force && MIX_ENV=prod mix run --no-start -e 'if Code.ensure_loaded?(ObanPowertools.Web.Dev.BrandBookLive), do: (IO.puts("FAIL: BrandBookLive compiled in prod"); System.halt(1)), else: IO.puts("OK: absent in prod")'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q '_brand_book' lib/oban_powertools/web/router.ex` succeeds and the route is inside the `:oban_powertools_native` live_session block.
    - The route is gated by `Application.compile_env(:oban_powertools, :dev_routes, ...)`; `grep -q 'compile_env' lib/oban_powertools/web/router.ex` succeeds and no bare `Mix.env()` gates the route inside the quote.
    - `mix test test/oban_powertools/web/live/brand_book_live_test.exs` passes (route renders — 70-VALIDATION.md §3).
    - `MIX_ENV=prod mix compile --force` then `Code.ensure_loaded?(ObanPowertools.Web.Dev.BrandBookLive)` returns false (70-VALIDATION.md §4 — route absent in prod).
    - `git diff --name-only` shows none of the 9 operator `*_live.ex` pages modified, and the 12 existing native routes are unchanged.
  </acceptance_criteria>
  <done>The /ops/jobs/_brand_book route is mounted behind compile_env(:dev_routes), the render test passes, and the module/route are provably absent under MIX_ENV=prod.</done>
</task>

<task type="auto">
  <name>Task 4: Add README Brand Identity section (DOC-03 initial)</name>
  <read_first>
    - README.md (existing section structure — "60-Second Install" at line 17, "Guides" at line 108)
    - .planning/phases/70-brand-book-identity-foundation/70-RESEARCH.md (§README Linking — the Brand Identity section content)
    - .planning/phases/70-brand-book-identity-foundation/70-VALIDATION.md (§6 README links)
  </read_first>
  <files>README.md</files>
  <action>
    Add a "Brand Identity" section to README.md (e.g. near the Guides section) per 70-RESEARCH.md §README Linking. It must contain BOTH:
    - a static-Markdown link to `guides/brand-book.md` (works on GitHub and renders on HexDocs),
    - a link to the dev route `http://localhost:4000/ops/jobs/_brand_book`, annotated as dev-only / not present in production builds.
    Keep it to a short paragraph plus the two bullet links. Do NOT modify the 9 operator pages.
  </action>
  <verify>
    <automated>grep -q 'guides/brand-book.md' README.md && grep -q '_brand_book' README.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'guides/brand-book.md' README.md` succeeds (static Markdown link — 70-VALIDATION.md §6).
    - `grep -q '_brand_book' README.md` succeeds (dev route link — 70-VALIDATION.md §6).
    - The dev route link is annotated as dev-only / absent in production.
    - `git diff --name-only` shows none of the 9 operator `*_live.ex` pages modified.
  </acceptance_criteria>
  <done>README has a Brand Identity section linking both the static guide and the dev-only route, satisfying DOC-03 (initial).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host prod build → library route surface | The dev-only route must never compile into a host's production app |
| committed Markdown → rendered HTML | Brand book is trusted committed content; raw/1 render is safe (no user input) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-70-03 | Elevation of privilege | dev route leaking into prod | mitigate | Module + route gated by Application.compile_env(:dev_routes, Mix.env()==:dev); prod-exclusion compile check asserts Code.ensure_loaded? is false |
| T-70-04 | Tampering | XSS via Phoenix.HTML.raw/1 | accept | Content is trusted, version-controlled Markdown with no user-input path (Pitfall 6); documented in module |
| T-70-SC | Tampering | npm/pip/cargo installs | accept | No package installs; mix.exs deps/0 unchanged — earmark_parser is reused as an existing transitive dev dep (enforced by acceptance criteria) |
</threat_model>

<verification>
- `mix test test/oban_powertools/web/live/brand_book_live_test.exs` passes (route renders the brand book).
- `MIX_ENV=prod mix compile --force` then `Code.ensure_loaded?(ObanPowertools.Web.Dev.BrandBookLive)` returns false.
- `grep` confirms README links both the static guide and the dev route.
- `config/prod.exs` does not set dev_routes.
- `git diff --name-only` shows none of the 9 operator `*_live.ex` pages modified; `mix.exs` deps/0 unchanged.
</verification>

<success_criteria>
- The brand book renders at the dev-only /ops/jobs/_brand_book route with zero new runtime dependencies.
- The route and module are provably absent under MIX_ENV=prod.
- The README links the brand book (static guide + dev route), satisfying DOC-03 (initial).
- Zero changes to the 9 operator pages.
</success_criteria>

<output>
Create `.planning/phases/70-brand-book-identity-foundation/70-02-SUMMARY.md` when done.
</output>
