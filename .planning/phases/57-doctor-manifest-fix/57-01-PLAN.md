---
phase: 57-doctor-manifest-fix
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/oban_powertools/doctor/checks.ex
  - test/oban_powertools/doctor/checks_test.exs
autonomous: true
requirements: [INT-01]

must_haves:
  truths:
    - "mix oban_powertools.doctor on a DB missing oban_powertools_job_records emits an error finding that names the table and its output-recording group"
    - "mix oban_powertools.doctor on a fully-migrated DB returns no error for oban_powertools_job_records (happy path, no regression)"
    - "The Doctor test suite references 'all 5 groups present' (not '4 groups')"
  artifacts:
    - path: "lib/oban_powertools/doctor/checks.ex"
      provides: "@powertools_manifest with output-recording group"
      contains: '"output-recording" => ['
    - path: "test/oban_powertools/doctor/checks_test.exs"
      provides: "Updated happy-path test description"
      contains: "all 5 groups present"
  key_links:
    - from: "lib/oban_powertools/doctor/checks.ex @powertools_manifest"
      to: "powertools_tables/1 (line 244)"
      via: "Enum.flat_map over manifest groups"
      pattern: "Enum\\.flat_map\\(@powertools_manifest"
    - from: '@powertools_manifest "output-recording" group'
      to: "oban_powertools_job_records table"
      via: "manifest table-name string matches ObanPowertools.JobRecord schema"
      pattern: "oban_powertools_job_records"
---

<objective>
Add `oban_powertools_job_records` to `@powertools_manifest` in `lib/oban_powertools/doctor/checks.ex` so the Doctor health check detects when the Phase 55 output-recording table is absent, and update the Doctor test description to reflect the new group count.

This closes the silent gap from Phase 55, which shipped the `oban_powertools_job_records` table and `ObanPowertools.JobRecord` schema without adding the table to the Doctor manifest — meaning an operator running `mix oban_powertools.doctor` on an under-migrated DB got a clean bill of health even though output-recording was non-functional.

Purpose: Doctor warns operators about a missing output-recording table instead of silently passing.
Output: Two edited files — one manifest map entry added, one test description string corrected.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/57-doctor-manifest-fix/57-CONTEXT.md
@.planning/phases/57-doctor-manifest-fix/57-RESEARCH.md
@.planning/phases/57-doctor-manifest-fix/57-PATTERNS.md

<interfaces>
<!-- Key facts the executor needs. Extracted from direct source reads. No codebase exploration required. -->

`@powertools_manifest` (lib/oban_powertools/doctor/checks.ex:26-59) is a compile-time
Elixir map. Each key is a kebab-case group string; each value is a list of snake_case
PostgreSQL table-name strings. The last existing entry is "heartbeat-lifeline" (lines 52-58),
and the map closes with `}` on line 59. The "heartbeat-lifeline" entry currently has NO
trailing comma after its closing `]`.

`powertools_tables/1` (lib/oban_powertools/doctor/checks.ex:244) iterates the manifest
dynamically:
  all_tables = Enum.flat_map(@powertools_manifest, fn {_group, tables} -> tables end)
It queries information_schema.tables WHERE table_name = ANY($1) and emits one error
Finding per group that has missing tables. No change to this function is needed — a manifest
data addition is sufficient (D-05).

Canonical table name is `oban_powertools_job_records` (plural), confirmed by
ObanPowertools.JobRecord schema declaration `schema "oban_powertools_job_records"`
in lib/oban_powertools/job_record.ex:21.

Current test (test/oban_powertools/doctor/checks_test.exs:103-107):
  describe "powertools_tables/1" do
    test "returns [] on the migrated test DB (all 4 groups present)" do
      result = Checks.powertools_tables(TestRepo)
      assert result == []
    end
The string "all 4 groups present" appears only on line 104 (confirmed by research —
no other occurrences of "4 groups" in the file).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add output-recording group to manifest and update test description</name>
  <files>lib/oban_powertools/doctor/checks.ex, test/oban_powertools/doctor/checks_test.exs</files>
  <read_first>
    - lib/oban_powertools/doctor/checks.ex (see current @powertools_manifest at lines 26-59 and powertools_tables/1 at line 244 — confirm the "heartbeat-lifeline" entry is the last map entry and that line 59 is the closing `}`)
    - test/oban_powertools/doctor/checks_test.exs (see the "powertools_tables/1" describe block at lines 103-115 — confirm line 104 is the happy-path test with "all 4 groups present")
    - lib/oban_powertools/job_record.ex (confirm `schema "oban_powertools_job_records"` so the table-name string is exact)
  </read_first>
  <action>
    Two co-dependent edits in two existing files.

    Edit 1 — lib/oban_powertools/doctor/checks.ex (per D-01, D-02):
    Add a trailing comma after the closing `]` of the "heartbeat-lifeline" entry (currently line 58, no comma today), then append a new last group entry inside the `@powertools_manifest` map, after "heartbeat-lifeline" and before the map's closing `}` on line 59. The new entry has key `"output-recording"` (kebab-case, matching the `record_output:` worker option and the ObanPowertools.JobRecord naming convention) and a single-element list value containing exactly the string `"oban_powertools_job_records"` (plural — must match job_record.ex:21 verbatim). Do NOT modify `powertools_tables/1` or any SQL — the existing Enum.flat_map iteration handles the new group automatically (per D-05). Do NOT change the Doctor CLI output format or JSON schema (per D-06).

    Edit 2 — test/oban_powertools/doctor/checks_test.exs (per D-03):
    On line 104, change the test description string from `"returns [] on the migrated test DB (all 4 groups present)"` to `"returns [] on the migrated test DB (all 5 groups present)"`. Change only the count word "4" to "5"; leave the assertion body (`assert result == []`) and all other lines untouched. No new test cases (per D-04) — the existing happy-path test becomes the INT-01 validator once the table (already present in oban_powertools_test) is covered by the manifest.

    Pitfall guard: after Edit 1, `mix compile` must succeed — a missing comma after the "heartbeat-lifeline" entry produces a SyntaxError.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/oban_powertools && mix compile --warnings-as-errors 2>&1 | tail -5 && grep -q '"output-recording" =>' lib/oban_powertools/doctor/checks.ex && grep -q '"oban_powertools_job_records"' lib/oban_powertools/doctor/checks.ex && grep -q "all 5 groups present" test/oban_powertools/doctor/checks_test.exs && ! grep -q "all 4 groups present" test/oban_powertools/doctor/checks_test.exs && mix test test/oban_powertools/doctor/checks_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - Source: `lib/oban_powertools/doctor/checks.ex` contains the line `"output-recording" => [` inside the `@powertools_manifest` map
    - Source: `lib/oban_powertools/doctor/checks.ex` contains `"oban_powertools_job_records"` (plural) within the output-recording group
    - Source: `powertools_tables/1` is unchanged — `grep -c "def powertools_tables" lib/oban_powertools/doctor/checks.ex` returns 1 and the function body still uses `Enum.flat_map(@powertools_manifest`
    - Source: `test/oban_powertools/doctor/checks_test.exs` contains `all 5 groups present` and does NOT contain `all 4 groups present`
    - Behavior: `mix compile --warnings-as-errors` exits 0 (no SyntaxError from a missing comma)
    - Behavior: `mix test test/oban_powertools/doctor/checks_test.exs` exits 0 (happy-path test still returns `[]` on the fully-migrated test DB — no regression)
  </acceptance_criteria>
  <done>
    The `@powertools_manifest` map has a fifth group `"output-recording" => ["oban_powertools_job_records"]`; the manifest compiles; the Doctor test description reads "all 5 groups present"; and `mix test test/oban_powertools/doctor/checks_test.exs` passes with the manifest now covering 5 groups.
  </done>
</task>

</tasks>

<verification>
- `mix compile --warnings-as-errors` succeeds (no manifest syntax error)
- `mix test test/oban_powertools/doctor/checks_test.exs` passes (happy path: 5 groups present on migrated DB returns `[]`)
- `mix test` full suite green before phase gate (no cross-file regression from the manifest change)
- Manual confirmation of error path (covered by existing framework, per D-04): on a DB missing `oban_powertools_job_records`, `powertools_tables/1` now emits an error Finding naming the table under the output-recording group, because the manifest iteration includes the new entry
</verification>

<success_criteria>
1. Running `mix oban_powertools.doctor` on a DB missing `oban_powertools_job_records` produces an error finding that names the table and its output-recording group (manifest entry drives `powertools_tables/1`)
2. Running `mix oban_powertools.doctor` on a fully-migrated DB returns no error for `oban_powertools_job_records` — `mix test test/oban_powertools/doctor/checks_test.exs` happy-path test passes (no regression)
3. The Doctor test suite references "all 5 groups present" (not "4 groups")
</success_criteria>

<output>
Create `.planning/phases/57-doctor-manifest-fix/57-01-SUMMARY.md` when done
</output>
