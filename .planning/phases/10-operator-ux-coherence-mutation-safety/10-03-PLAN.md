---
phase: 10
plan: 03
type: execute
wave: 3
depends_on: ["10-01", "10-02"]
files_modified: ["lib/oban_powertools/web/oban_web_bridge.ex", "lib/oban_powertools/web/router.ex", "README.md", "test/oban_powertools/web/router_test.exs"]
autonomous: true
requirements: ["HST-02"]
must_haves:
  truths:
    - "The optional `/ops/jobs/oban` bridge remains a read-only inspection surface in Phase 10."
    - "Bridge coherence comes from shared auth/display seams and plain support-truth, not full mutation parity."
    - "Operators are told to use native Powertools pages for audited mutations whenever they cross into generic Oban Web inspection."
  artifacts:
    - path: "lib/oban_powertools/web/oban_web_bridge.ex"
      provides: "Read-only bridge adapter over documented hooks"
      contains: ":read_only"
    - path: "lib/oban_powertools/web/router.ex"
      provides: "Nested route boundary for the optional bridge"
      contains: "oban_dashboard"
    - path: "README.md"
      provides: "Public support-truth for native mutation ownership vs bridge inspection"
      contains: "/ops/jobs/oban"
  key_links:
    - from: "native Powertools pages"
      to: "optional Oban Web bridge"
      via: "shared auth/display policy plus explicit read-only support-truth"
      pattern: "native pages own audited mutations; bridge owns bounded inspection"
    - from: "router contract"
      to: "docs and tests"
      via: "same nested read-only bridge guarantees"
      pattern: "route shape == adapter shape == README language"
---

<objective>
Finish Phase 10 by making the optional bridge explicitly read-only, policy-consistent, and support-truthful wherever the repo documents or proves its operator-facing contract.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/10-operator-ux-coherence-mutation-safety/10-CONTEXT.md
@.planning/phases/10-operator-ux-coherence-mutation-safety/10-RESEARCH.md
@.planning/phases/10-operator-ux-coherence-mutation-safety/10-PATTERNS.md
@README.md
@lib/oban_powertools/web/router.ex
@lib/oban_powertools/web/oban_web_bridge.ex
@test/oban_powertools/web/router_test.exs
</context>

<tasks>

<task type="execute" tdd="true">
  <name>Task 1: Lock the bridge to explicit read-only and support-truth semantics</name>
  <files>lib/oban_powertools/web/oban_web_bridge.ex, lib/oban_powertools/web/router.ex, test/oban_powertools/web/router_test.exs</files>
  <read_first>
    - lib/oban_powertools/web/oban_web_bridge.ex
    - lib/oban_powertools/web/router.ex
    - test/oban_powertools/web/router_test.exs
    - .planning/phases/9-policy-boundaries-optional-bridge-contracts/9-CONTEXT.md
    - .planning/phases/10-operator-ux-coherence-mutation-safety/10-CONTEXT.md
  </read_first>
  <action>
    Preserve the existing nested `/ops/jobs/oban` mount and the documented-hook bridge adapter, but make the Phase 10 operator-facing contract explicit in code and tests: the bridge is read-only, it reuses the same Phase 9 auth/display seams as native pages, and it does not own audited mutations.
    Keep `resolve_access/1` returning `:read_only` for authorized bridge viewers. If any code path can imply mutation capability, remove that implication and replace it with explicit read-only semantics.
    Strengthen `router_test.exs` so it asserts the bridge remains nested under `/ops/jobs/oban`, uses the Powertools bridge adapter, keeps the LiveAuth mount, and remains the optional inspection surface rather than a native mutation equivalent.
  </action>
  <acceptance_criteria>
    - `lib/oban_powertools/web/oban_web_bridge.ex` contains `:read_only` as the authorized bridge access posture
    - `test/oban_powertools/web/router_test.exs` asserts the bridge adapter and shared auth hook contract
    - `test/oban_powertools/web/router_test.exs` still proves `/oban` does not resolve at the router root
    - `mix test test/oban_powertools/web/router_test.exs` exits 0
  </acceptance_criteria>
  <verify>
    <automated>mix test test/oban_powertools/web/router_test.exs</automated>
  </verify>
  <done>The optional bridge stays technically and semantically read-only under the existing nested route contract.</done>
</task>

<task type="execute">
  <name>Task 2: Publish the Phase 10 bridge support-truth in README and execution proof</name>
  <files>README.md</files>
  <read_first>
    - README.md
    - lib/oban_powertools/web/oban_web_bridge.ex
    - test/oban_powertools/web/router_test.exs
    - .planning/phases/10-operator-ux-coherence-mutation-safety/10-RESEARCH.md
  </read_first>
  <action>
    Update README so the bridge contract is explicit in Phase 10 terms: `/ops/jobs/oban` is optional, nested, and read-only; it reuses the host-owned `auth_module` and `display_policy`; native Powertools pages own audited mutations and richer preview/reason UX.
    Keep the language support-truthful. Do not promise full UX parity, bridge-side writes, nav injection, or generic plugin behavior.
    Add the exact bridge-language strings needed for grep-verifiable proof, including `read-only`, `native Powertools pages`, `audited mutations`, `auth_module`, and `display_policy`.
  </action>
  <acceptance_criteria>
    - `README.md` contains `/ops/jobs/oban`
    - `README.md` contains `read-only`
    - `README.md` contains `native Powertools pages`
    - `README.md` contains `audited mutations`
    - `README.md` contains both `auth_module` and `display_policy`
  </acceptance_criteria>
  <verify>
    <automated>rg -n "/ops/jobs/oban|read-only|native Powertools pages|audited mutations|auth_module|display_policy" README.md && mix test test/oban_powertools/web/router_test.exs</automated>
  </verify>
  <done>The public bridge docs match the actual route and adapter contract and reinforce native mutation ownership.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Native mutation shell -> optional bridge | Operators must not mistake the bridge for an audited mutation surface. |
| Shared auth/display seams -> upstream Oban Web UI | The bridge should inherit policy consistency without implying native surface parity. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-06 | Elevation of Privilege | bridge access posture | mitigate | Keep the bridge adapter read-only and verify it in route-level tests. |
| T-10-07 | Tampering / support-truth confusion | bridge docs and operator messaging | mitigate | Make README and route proof state plainly that native Powertools pages own audited mutations while the bridge stays a bounded inspection surface. |
</threat_model>

<verification>
mix test test/oban_powertools/web/router_test.exs
</verification>

<success_criteria>
Hosts and operators get one explicit truth about `/ops/jobs/oban`: it is an optional, nested, read-only inspection surface that shares Powertools policy seams but not native mutation authority.
</success_criteria>

<output>
After completion, create `.planning/phases/10-operator-ux-coherence-mutation-safety/10-03-SUMMARY.md`
</output>
