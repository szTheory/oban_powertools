---
phase: 16-semantics-contract-cause-vocabulary-compatibility-baseline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/oban_powertools/workflow/workflow.ex
  - lib/oban_powertools/workflow/step.ex
  - lib/oban_powertools/workflow/runtime.ex
  - lib/oban_powertools/workflow.ex
  - lib/oban_powertools/lifeline.ex
  - lib/oban_powertools/web/workflows_live.ex
  - .planning/REQUIREMENTS.md
  - .planning/PROJECT.md
autonomous: true
requirements:
  - WFS-01
  - WFS-03
must_haves:
  truths:
    - "Workflow and step lifecycle semantics are defined explicitly in one durable vocabulary instead of being inferred from mixed runtime behavior."
    - "Terminal causes and transition meanings are explainable from persisted fields and blocker evidence."
    - "Pre-v1.2 in-flight workflows have a documented compatibility policy instead of silently inheriting v1.2 meaning."
  artifacts:
    - path: "lib/oban_powertools/workflow/workflow.ex"
      provides: "Durable workflow-level semantics versioning and lifecycle fields"
    - path: "lib/oban_powertools/workflow/step.ex"
      provides: "Durable step-level terminal cause and blocker/wait fields"
    - path: "lib/oban_powertools/workflow/runtime.ex"
      provides: "One repo-local lifecycle/terminal-cause interpretation contract"
    - path: ".planning/phases/16-semantics-contract-cause-vocabulary-compatibility-baseline/16-CONTEXT.md"
      provides: "Locked lifecycle, compatibility, and truth-source decisions for the phase"
  key_links:
    - from: "lib/oban_powertools/workflow/runtime.ex"
      to: "lib/oban_powertools/workflow/workflow.ex"
      via: "semantics version and terminal-cause interpretation"
      pattern: "semantics_version|terminal_cause"
    - from: "lib/oban_powertools/workflow/runtime.ex"
      to: "lib/oban_powertools/workflow/step.ex"
      via: "step lifecycle, blocker, and wait-state semantics"
      pattern: "awaiting_signal|cancel_requested_at|last_transition_at"
---

<objective>
Freeze the Phase 16 lifecycle contract: define the durable workflow and step state vocabulary, formalize terminal-cause meanings, and publish the pre-v1.2 compatibility posture.

Purpose: establish one semantics source of truth before later callback, recovery, and signal work depends on it.
Output: a repo-local lifecycle contract reflected in runtime code, persisted fields, and planning/support-truth artifacts.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/16-semantics-contract-cause-vocabulary-compatibility-baseline/16-CONTEXT.md
@lib/oban_powertools/workflow.ex
@lib/oban_powertools/workflow/workflow.ex
@lib/oban_powertools/workflow/step.ex
@lib/oban_powertools/workflow/runtime.ex
@lib/oban_powertools/lifeline.ex
@lib/oban_powertools/web/workflows_live.ex
</context>

<tasks>

<task type="auto">
  <name>Task 1: Freeze the lifecycle and terminal-cause vocabulary in one repo-local contract</name>
  <files>lib/oban_powertools/workflow/workflow.ex, lib/oban_powertools/workflow/step.ex, lib/oban_powertools/workflow/runtime.ex</files>
  <read_first>.planning/phases/16-semantics-contract-cause-vocabulary-compatibility-baseline/16-CONTEXT.md, lib/oban_powertools/workflow/workflow.ex, lib/oban_powertools/workflow/step.ex, lib/oban_powertools/workflow/runtime.ex</read_first>
  <action>Audit the current in-flight v1.2 workflow fields and runtime helpers, then collapse them into one explicit lifecycle vocabulary for workflow state, step state, and terminal causes. Remove ambiguous or duplicate meanings where one persisted field can represent the truth. Make the runtime interpretation helpers the single place where terminal cause and request-vs-outcome meanings are derived, and ensure the persisted workflow and step fields are sufficient for diagnosis without relying on transient coordinator state.</action>
  <acceptance_criteria>
    - Runtime code exposes one clear workflow/step lifecycle vocabulary with explicit terminal-cause meanings.
    - Workflow and step persisted fields are sufficient to explain cancel-requested, waiting, expired, cancelled, failed, and completed-after-cancel states.
    - No lifecycle meaning depends on PubSub or coordinator timing for correctness.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/oban_powertools/workflow_runtime_test.exs test/oban_powertools/workflow_coordinator_test.exs test/oban_powertools/web/live/workflows_live_test.exs</automated>
  </verify>
  <done>The repo has one explicit lifecycle/terminal-cause contract instead of mixed inferred semantics.</done>
</task>

<task type="auto">
  <name>Task 2: Define the pre-v1.2 compatibility posture and legal interpretation boundaries</name>
  <files>lib/oban_powertools/workflow/runtime.ex, .planning/REQUIREMENTS.md, .planning/PROJECT.md</files>
  <read_first>.planning/phases/16-semantics-contract-cause-vocabulary-compatibility-baseline/16-CONTEXT.md, .planning/REQUIREMENTS.md, .planning/PROJECT.md, lib/oban_powertools/workflow/runtime.ex</read_first>
  <action>Document and encode how workflows created before semantics version 2 are treated during reconciliation and inspection. The policy must be additive and explicit: historical rows may follow a compatibility path, but the repo must not silently reinterpret them as if they had been created under the new contract. Reflect that policy in the planning artifacts so later phases can depend on it without reopening the question.</action>
  <acceptance_criteria>
    - The compatibility path for pre-v1.2 in-flight workflows is stated explicitly in planning docs or runtime-adjacent comments.
    - New semantics version 2 rows remain the default for new workflow creation.
    - Historical rows are not silently reclassified under the v1.2 contract.
  </acceptance_criteria>
  <verify>
    <automated>rg -n "semantics version|compatibility|pre-v1.2|historical rows|terminal cause" .planning/PROJECT.md .planning/REQUIREMENTS.md lib/oban_powertools/workflow/runtime.ex lib/oban_powertools/workflow/workflow.ex</automated>
  </verify>
  <done>The semantics contract includes an explicit compatibility posture instead of hidden reinterpretation.</done>
</task>

<task type="auto">
  <name>Task 3: Align diagnosis-facing surfaces to the frozen Phase 16 vocabulary</name>
  <files>lib/oban_powertools/lifeline.ex, lib/oban_powertools/web/workflows_live.ex, lib/oban_powertools/workflow/runtime.ex</files>
  <read_first>lib/oban_powertools/lifeline.ex, lib/oban_powertools/web/workflows_live.ex, lib/oban_powertools/workflow/runtime.ex, .planning/phases/16-semantics-contract-cause-vocabulary-compatibility-baseline/16-CONTEXT.md</read_first>
  <action>Make sure the native workflow screen and Lifeline-facing workflow diagnosis helpers consume the same frozen lifecycle and terminal-cause vocabulary from the runtime layer. The goal here is not broad UX expansion; it is to prevent Phase 16 from locking a contract in code while the diagnosis surfaces keep using older or ad hoc wording.</action>
  <acceptance_criteria>
    - Workflow UI and Lifeline workflow diagnosis share the same vocabulary source.
    - Diagnosis-facing wording does not rely on obsolete or duplicate lifecycle meanings.
    - The phase closes with one supportable vocabulary for later recovery and signal work.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/oban_powertools/lifeline_test.exs test/oban_powertools/web/live/workflows_live_test.exs</automated>
  </verify>
  <done>The frozen lifecycle contract is already reflected in the native diagnosis surfaces that operators use.</done>
</task>

</tasks>

<verification>
- `mix compile`
- `mix test test/oban_powertools/workflow_runtime_test.exs test/oban_powertools/workflow_coordinator_test.exs test/oban_powertools/web/live/workflows_live_test.exs`
- `mix test test/oban_powertools/lifeline_test.exs`
- `rg -n "semantics version|compatibility|pre-v1.2|historical rows|terminal cause" .planning/PROJECT.md .planning/REQUIREMENTS.md lib/oban_powertools/workflow/runtime.ex lib/oban_powertools/workflow/workflow.ex`
</verification>

<success_criteria>
- Phase 16 has one explicit lifecycle and terminal-cause contract.
- Pre-v1.2 compatibility posture is explicit and additive.
- Diagnosis surfaces already consume the same lifecycle vocabulary the runtime uses.
</success_criteria>

<output>
After completion, create `.planning/phases/16-semantics-contract-cause-vocabulary-compatibility-baseline/16-01-SUMMARY.md`
</output>
