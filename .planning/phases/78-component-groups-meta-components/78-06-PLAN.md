---
phase: 78-component-groups-meta-components
plan: 06
type: execute
wave: 6
depends_on:
  - 78-05
files_modified:
  - test/support/operator_pattern_story_catalog.ex
  - test/oban_powertools/operator_pattern_story_catalog_test.exs
  - lib/oban_powertools/web/dev/showcase_live.ex
  - test/oban_powertools/web/live/showcase_live_test.exs
  - test/oban_powertools/web/live/operator_patterns_harness_test.exs
  - test/oban_powertools/hex_release_test.exs
  - scripts/showcase_manifest.exs
  - test/browser/support/manifest.ts
  - test/browser/support/manifest-smoke.mjs
  - test/browser/support/showcase.ts
  - test/browser/specs/showcase.structure.spec.ts
  - test/browser/specs/showcase.a11y.spec.ts
  - test/browser/specs/showcase.vrt.spec.ts
autonomous: true
requirements:
  - GROUP-01
  - GROUP-02
  - FORM-04
  - COPY-02
  - A11Y-02
must_haves:
  truths:
    - "D-57: a dedicated dev/test-only OperatorPatternStoryCatalog is optional/fail-closed at the package boundary, manifest schema bumps exactly once from 5 to 6, and group targets append after data targets."
    - "D-58..D-60: exactly 23 stable story IDs render in binding order with deterministic normalized presentation fixtures and one-at-a-time activation for the six confirmation plus four detail overlay stories."
    - "D-59: filter/detail catalog coverage uses the exact stable IDs group-filter-submit, group-filter-instant, group-filter-active-clear, group-filter-unapplied-invalid, group-detail-inline, group-detail-modal, group-detail-loading-unavailable, and group-detail-long-content."
    - "D-61: ShowcaseLive is the connected parent for reason/count/stale/duplicate/partial, draft/applied/history, detail URL/focus, current/snapshot/audit, and nested-dialog contracts; production components remain stateless."
    - "The generated manifest has exactly 64 targets (9 scenario + 7 primitive + 9 form + 6 shell + 10 data + 23 group), `kind: group`, and closed `activation: none | overlay` validated independently in Elixir, TypeScript, and Node."
    - "Generic structure, axe, and VRT consumers activate only the current overlay target on a fresh connected page; initial showcase mount never stacks dialogs or makes unrelated stories inert."
    - "Schema-6 generic structure behavior is executed against the connected Docker-backed showcase after activation support lands; Playwright discovery alone is not accepted as DOM/metadata/activation proof."
  artifacts:
    - path: "test/support/operator_pattern_story_catalog.ex"
      provides: "Exact deterministic normalized 23-story group catalog"
      exports: ["stories/0", "story!/1"]
    - path: "lib/oban_powertools/web/dev/showcase_live.ex"
      provides: "Fail-closed group rendering and connected parent-owned interaction states"
    - path: "scripts/showcase_manifest.exs"
      provides: "Schema-6 group_stories collection, activation serialization, and append order"
    - path: "test/browser/support/manifest.ts"
      provides: "Strict ShowcaseGroupStory type and groupStories export"
      exports: ["ShowcaseGroupStory", "groupStories"]
    - path: "test/browser/support/showcase.ts"
      provides: "One-at-a-time group overlay activation shared by structure, axe, and VRT"
      exports: ["activateTarget"]
  key_links:
    - from: "test/support/operator_pattern_story_catalog.ex"
      to: "lib/oban_powertools/web/dev/showcase_live.ex"
      via: "optional list-validated support-module loader"
      pattern: "group_catalog"
    - from: "scripts/showcase_manifest.exs"
      to: "test/browser/support/manifest.ts"
      via: "schema_version 6 group_stories and activation metadata"
      pattern: "group_stories"
    - from: "test/browser/support/showcase.ts"
      to: "lib/oban_powertools/web/dev/showcase_live.ex"
      via: "activate-group-story event on a connected fresh page"
      pattern: "activate-group-story"
---

<objective>
Integrate the complete operator-pattern catalog with the connected showcase, fail-closed package boundary, schema-6 manifest, and generic browser target pipeline.

Purpose: create one deterministic source of story IDs/data and one safe activation path that supports behavior, axe, and VRT without stacked overlays or production leakage.
Output: 23-story catalog, rendered/harnessed showcase groups, schema-6/64-target manifest, strict browser support, package fallbacks, and green discovery/structure gates.
</objective>

<execution_context>
@/Users/jon/.agents/gsd-core/workflows/execute-plan.md
@/Users/jon/.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/78-component-groups-meta-components/78-CONTEXT.md
@.planning/phases/78-component-groups-meta-components/78-RESEARCH.md
@.planning/phases/78-component-groups-meta-components/78-PATTERNS.md
@.planning/phases/78-component-groups-meta-components/78-UI-SPEC.md
@.planning/phases/78-component-groups-meta-components/78-VALIDATION.md
@.planning/phases/78-component-groups-meta-components/78-05-SUMMARY.md
@.planning/phases/77-data-display-operator-patterns/77-09-SUMMARY.md
</context>

## Artifacts this phase produces

- New support file/module: `test/support/operator_pattern_story_catalog.ex` / `ObanPowertools.OperatorPatternStoryCatalog` with `stories/0`, `story!/1`, 23 exact IDs, normalized fixtures, and stable test targets.
- New story fields: `id`, `kind: :group`, `name`, `description`, `components`, `variant`, `state`, `fixtures`, `activation: :none | :overlay`, and `test_targets`; overlay is assigned to exactly the six `group-confirm-*` and four `group-detail-*` stories.
- New ShowcaseLive fields/symbols: `@group_catalog_module`, `@group_catalog_path`, `group_catalog_available?`, `group_stories`, `active_group_overlay`, `group_confirmation_state`, `group_filter_state`, `group_detail_state`, `load_group_catalog/0`, `group_story_body/1`, and finite handlers `activate-group-story`, `validate-group-confirmation`, `submit-group-confirmation`, `fresh-group-preview`, `validate-group-filters`, `apply-group-filters`, `remove-group-filter`, `clear-group-filters`, `open-group-detail`, `select-group-detail`, and `close-group-detail`.
- New manifest fields/types: `schema_version: 6`, `group_stories`, target `kind: "group"`, target `activation: "none" | "overlay"`, TypeScript `ShowcaseGroupStory`, and exported `groupStories`; target count 64 with groups appended last.
- New browser helper: `activateTarget(page, target)` or equivalent exact exported symbol, used by `assertShowcaseStructure`, axe, and VRT before locating/scanning/snapshotting a target.
- Modified generic specs only for generated activation support; no hardcoded 23-ID duplicate in TypeScript and no baseline files in this plan.
- New/modified tests prove exact catalog order/fixtures, optional absent/non-list fallback, Hex exclusion, connected story events, schema/order/count/activation, one open overlay, and browser discovery.

<tasks>

<task type="auto">
  <name>Task 78-06-01: Implement the exact deterministic OperatorPatternStoryCatalog</name>
  <files>test/support/operator_pattern_story_catalog.ex, test/oban_powertools/operator_pattern_story_catalog_test.exs</files>
  <read_first>
    - @test/support/operator_pattern_story_catalog.ex
    - @test/oban_powertools/operator_pattern_story_catalog_test.exs
    - @test/support/data_display_story_catalog.ex
    - @test/oban_powertools/data_display_story_catalog_test.exs
    - @test/support/showcase_catalog.ex
    - @.planning/phases/78-component-groups-meta-components/78-CONTEXT.md
    - @.planning/phases/78-component-groups-meta-components/78-UI-SPEC.md
  </read_first>
  <action>Create `ObanPowertools.OperatorPatternStoryCatalog` following the deterministic support-only registry analog. Define exactly the 23 D-58..D-60 IDs in the binding order and stable `test_targets` values: story `obpt-group-story-#{id}`, snapshot `showcase/#{id}`, a11y `[data-obpt-group-story="#{id}"]`, kind `:group`. Set `activation: :overlay` on exactly IDs 1-6 (all confirmation) and 11-14 (all detail), and `:none` on the other 13. Fixtures contain only normalized presentation maps and complete grammatical copy: frozen counts, action-specific labels, reason/count form values/errors, ordered success/failed/skipped results, filter draft/applied/invalid states, explicit detail states, status/severity/completeness matrices, current-versus-snapshot blockers, absolute audit timestamps/missing fields, already-redacted changes, stable long IDs, Unicode/RTL/HTML-looking strings, and one unique original-secret sentinel used only as an absence assertion—not as rendered fixture data. Use constants only: no Faker/random time/database/backend structs/exceptions/tokens/hashes/raw errors. Implement deterministic `stories/0` and `story!/1`; extend tests for exact equality/order/uniqueness, closed fields/activation, target shapes, full component/state coverage, normalized-only fixtures, and secret absence.</action>
  <verify>
    <automated>mix test test/oban_powertools/operator_pattern_story_catalog_test.exs --seed 0</automated>
    <automated>mix format --check-formatted test/support/operator_pattern_story_catalog.ex test/oban_powertools/operator_pattern_story_catalog_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `stories/0` returns exactly 23 deterministic unique stories in the approved order and `story!/1` resolves each id.
    - Exactly ten stories are overlay-activated: six confirmation plus four detail; thirteen are none.
    - Every target uses the exact group story/snapshot/a11y convention, and no TypeScript source owns a duplicate full ID list.
    - Fixtures are normalized, deterministic, absolute-time, long/Unicode capable, and contain no raw secret, struct, exception, preview token, hash, random source, or database operation.
  </acceptance_criteria>
  <done>The catalog returns exactly 23 ordered unique normalized stories with ten overlay activations, stable targets, deterministic fixtures, and no secret or backend values.</done>
</task>

<task type="auto">
  <name>Task 78-06-02: Render groups through a connected fail-closed ShowcaseLive parent</name>
  <files>lib/oban_powertools/web/dev/showcase_live.ex, test/oban_powertools/web/live/showcase_live_test.exs, test/oban_powertools/web/live/operator_patterns_harness_test.exs, test/oban_powertools/hex_release_test.exs</files>
  <read_first>
    - @lib/oban_powertools/web/dev/showcase_live.ex
    - @test/oban_powertools/web/live/showcase_live_test.exs
    - @test/oban_powertools/web/live/operator_patterns_harness_test.exs
    - @test/oban_powertools/hex_release_test.exs
    - @test/support/operator_pattern_story_catalog.ex
    - @test/support/data_display_story_catalog.ex
    - @lib/oban_powertools/web/components/operator_patterns.ex
    - @.planning/phases/77-data-display-operator-patterns/77-PATTERNS.md
    - @.planning/phases/78-component-groups-meta-components/78-CONTEXT.md
  </read_first>
  <action>Add the optional group catalog module/path and a fail-closed loader that accepts only an exported list; absent, unloadable, or non-list catalogs yield `available?: false`, empty stories, and the existing Operator Groups placeholder. On valid load, render one article per catalog story with `id`, `data-obpt-group-story`, metadata, a story stage, and `group_story_body/1` clauses composing the six production components. Initial mount sets no active overlay. `activate-group-story` validates the requested id against the catalog, closes any prior overlay, and opens only that story; no fixture may cause all overlays to open. Keep connected parent assigns/events finite and deterministic for confirmation validation/submission/fresh preview/results/receipt, filter draft/apply/remove/clear and canonical params, detail open/select/close/direct state, and explanation/audit stories. Reuse the server-truth helpers proven in the test harness; do not create mutation/auth/query/route logic in components. Ensure drawer-to-confirmation closes/leaves detail first. Extend LiveView tests for exact 23 article metadata, one active overlay, all event transitions, URL/history assertions, exact status/receipt copy, no nested dialog, and sentinel absence. Extend fresh child-VM/package tests so absent and malformed catalogs safely render the placeholder and the support file is absent from Hex contents; keep production dev-route compile gating unchanged.</action>
  <verify>
    <automated>mix test test/oban_powertools/operator_pattern_story_catalog_test.exs test/oban_powertools/web/live/operator_patterns_harness_test.exs test/oban_powertools/web/live/showcase_live_test.exs test/oban_powertools/hex_release_test.exs --seed 0</automated>
    <automated>mix format --check-formatted test/support/operator_pattern_story_catalog.ex lib/oban_powertools/web/dev/showcase_live.ex test/oban_powertools/web/live/showcase_live_test.exs test/oban_powertools/web/live/operator_patterns_harness_test.exs test/oban_powertools/hex_release_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - Valid catalog renders exactly 23 stable group articles; initial mount has zero active overlay and activation leaves exactly one requested overlay open.
    - Connected events cover reason/count/stale/duplicate/result, draft/applied/filter URL, detail/history/direct-link, and explain/audit truth without moving ownership into components.
    - Absent or non-list catalogs render the existing placeholder in fresh isolation and do not crash/enumerate malformed values.
    - `test/support/operator_pattern_story_catalog.ex` is absent from Hex package contents and no prod route/module exposure broadens.
    - Complete rendered HTML contains no original secret/token/hash/raw error and never nests confirmation under detail.
  </acceptance_criteria>
  <done>Connected ShowcaseLive renders exactly 23 safe group articles, activates at most one overlay, exercises all parent-owned states, and fails closed for absent or malformed package-excluded catalogs.</done>
</task>

<task type="auto">
  <name>Task 78-06-03: Bump once to schema 6 and validate the 64-target group manifest</name>
  <files>scripts/showcase_manifest.exs, test/browser/support/manifest.ts, test/browser/support/manifest-smoke.mjs</files>
  <read_first>
    - @scripts/showcase_manifest.exs
    - @test/browser/support/manifest.ts
    - @test/browser/support/manifest-smoke.mjs
    - @test/support/operator_pattern_story_catalog.ex
    - @test/support/data_display_story_catalog.ex
    - @test/browser/.generated/showcase-manifest.json
    - @.planning/phases/78-component-groups-meta-components/78-PATTERNS.md
  </read_first>
  <action>Load the new catalog in `showcase_manifest.exs`, serialize group metadata/targets including closed string activation, append group targets after data targets, emit `group_stories`, and change schema_version exactly once from 5 to 6. In `manifest.ts`, add `ShowcaseGroupStory` with kind group and activation none/overlay; include it in the union, strict collection validation, expected append order, exact collection count 23, total count 64, returned manifest, and exported `groupStories`. In the independent CommonJS/Node smoke implementation, pin schema 6, collection count/order, prefix/selector/snapshot shape, activation values, group append position, and total 64 without importing the TypeScript validator. Keep story IDs Elixir-owned and reject missing/extra/duplicate/reordered targets and unknown activation. Regenerate the ignored manifest through `npm run showcase:manifest`; do not edit it manually or change package scripts/dependencies.</action>
  <verify>
    <automated>npm run showcase:manifest &amp;&amp; node test/browser/support/manifest-smoke.mjs</automated>
    <automated>npx playwright test --list test/browser/specs/showcase.structure.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - Generated manifest reports schema 6, 23 group stories, and exactly 64 targets with group targets last.
    - Every group target has kind group and activation none/overlay matching the Elixir catalog; missing/extra/reordered data fails both TS and Node validators.
    - `groupStories` is exported and `ShowcaseTarget` includes `ShowcaseGroupStory`; no complete 23-ID list is copied into TypeScript/Node.
    - No dependency, package script, lockfile, route, schema, or production page change occurs.
  </acceptance_criteria>
  <done>Independent Elixir, TypeScript, and Node validation accepts schema 6 with 23 group stories and exactly 64 ordered targets, while rejecting activation, count, and ordering drift.</done>
</task>

<task type="auto">
  <name>Task 78-06-04: Share one-at-a-time activation across structure, axe, and VRT discovery</name>
  <files>test/browser/support/showcase.ts, test/browser/specs/showcase.structure.spec.ts, test/browser/specs/showcase.a11y.spec.ts, test/browser/specs/showcase.vrt.spec.ts</files>
  <read_first>
    - @test/browser/support/showcase.ts
    - @test/browser/specs/showcase.structure.spec.ts
    - @test/browser/specs/showcase.a11y.spec.ts
    - @test/browser/specs/showcase.vrt.spec.ts
    - @test/browser/specs/operator-patterns.behavior.spec.ts
    - @test/browser/support/manifest.ts
    - @lib/oban_powertools/web/dev/showcase_live.ex
    - @.planning/phases/78-component-groups-meta-components/78-VALIDATION.md
  </read_first>
  <action>Add exported `activateTarget(page, target)` (or keep that exact public name if the helper signature includes options) to browser support. After `prepareShowcase` confirms `.phx-connected`, activation none returns the normal target; overlay dispatches the deterministic ShowcaseLive activation for that one id, waits for its story stage/dialog, and asserts exactly one active group overlay and at most one modal. Make `assertShowcaseStructure`, generic axe, and generic VRT call this helper before visibility/scan/screenshot so all use identical fresh-page preparation. Extend structure checks for group metadata, unique IDs, correct activation state, one responsive tree, and overlay count. Do not hardcode group IDs or special-case screenshot names in the generic specs. Run manifest smoke and Playwright list discovery for structure, axe, VRT, and the RED behavior spec, then execute `showcase.structure.spec.ts` against the connected Docker-backed showcase so schema-6 DOM metadata and activation assertions—not merely parsing—must pass; behavior may now discover but is completed in Plan 78-07.</action>
  <verify>
    <automated>npm run showcase:manifest &amp;&amp; node test/browser/support/manifest-smoke.mjs &amp;&amp; npx playwright test --list test/browser/specs/showcase.structure.spec.ts test/browser/specs/showcase.a11y.spec.ts test/browser/specs/showcase.vrt.spec.ts test/browser/specs/operator-patterns.behavior.spec.ts</automated>
    <automated>npm run showcase:manifest &amp;&amp; scripts/with-showcase-server.sh scripts/playwright-docker.sh npx playwright test test/browser/specs/showcase.structure.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - All four specs discover schema-6 group targets and no module-load RED guard remains.
    - Shared activation waits for connected LiveView, opens only the current overlay story, and asserts one overlay/at-most-one modal.
    - Structure, axe, and VRT have no duplicated activation algorithm or hardcoded 23-ID list.
    - Initial page mount leaves all overlays closed, so unrelated stories remain visible/non-inert before target activation.
    - The connected Docker-backed structure run passes its schema-6 group metadata, unique-ID, one-tree, activation-state, and overlay-cardinality assertions.
  </acceptance_criteria>
  <done>Structure, axe, VRT, and behavior discovery share one connected one-at-a-time activation path, and the Docker structure run passes all schema-6 group DOM and overlay assertions.</done>
</task>

</tasks>

<threat_model>
## ASVS L1 Threat Model — blocking threshold: high

| Threat ID | Category | Severity | Mitigation and verification |
|-----------|----------|----------|-----------------------------|
| T-78-06-FIXTURE | Information disclosure | high | Catalog is normalized-only, deterministic, secret-sentinel checked, and excluded from Hex; no backend structs/tokens/hashes/raw errors. |
| T-78-06-PACKAGE | Tampering / disclosure | high | Optional loader validates list shape and fails closed in fresh child VMs; Hex tests prove support source absent. |
| T-78-06-ACTIVATE | Denial of service / accessibility | high | Initial overlays closed; validated one-id activation plus fresh pages prevents stacked top-layer dialogs/inert unrelated stories. |
| T-78-06-MANIFEST | Tampering | high | Elixir owns IDs/order; independent TS/Node validators enforce schema, activation, append order, duplicates, and exact counts. |
| T-78-06-XSS | Injection | high | Showcase renders catalog copy through escaped components; full-DOM sentinel and hostile fixtures cover attrs/details/destinations. |
| T-78-06-AUTH | Elevation of privilege | high | Showcase parent only models deterministic test interactions; production components and pages gain no authorization/mutation capability. |
| T-78-06-SUPPLY | Tampering | high | No dependency/lock/package-script change; catalog remains test support and generated manifest is derived, ignored evidence. |
</threat_model>

<verification>
- Run catalog tests, connected showcase/harness/package tests, schema generation/smoke/TS validation, all browser discovery, and the connected Docker-backed structure spec.
- Block on any high-severity fixture leak, package fallback, stacked overlay, manifest drift, injection, or authority-boundary failure.
- Confirm no screenshot is created/updated and no dependency, lockfile, schema, route, production page, query, auth, mutation, or audit storage file changes.
</verification>

<success_criteria>
- The exact 23-story normalized catalog is deterministic, test-only, fail-closed, and package-excluded.
- ShowcaseLive renders every group and owns all connected demonstration state while opening only one requested overlay.
- Schema 6, groupStories, activation, append order, and 64-target equality pass independent validation.
- Generic structure, axe, VRT, and behavior specs discover group targets through one shared activation path.
</success_criteria>

<output>
Create `.planning/phases/78-component-groups-meta-components/78-06-SUMMARY.md` when done.
</output>
