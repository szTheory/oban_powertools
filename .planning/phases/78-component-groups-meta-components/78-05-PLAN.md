---
phase: 78-component-groups-meta-components
plan: 05
type: execute
wave: 5
depends_on:
  - 78-04
files_modified:
  - lib/oban_powertools/web/components/operator_patterns.ex
  - test/oban_powertools/web/components/operator_patterns_test.exs
  - test/oban_powertools/web/live/operator_patterns_harness_test.exs
  - assets/oban_powertools/tokens.css
  - assets/oban_powertools/theme.js
  - priv/static/oban_powertools/oban_powertools.css
  - priv/static/oban_powertools/oban_powertools.js
  - test/oban_powertools/web/theme_tokens_test.exs
  - test/oban_powertools/web/assets_test.exs
autonomous: true
requirements:
  - GROUP-01
  - GROUP-02
  - COPY-02
  - A11Y-02
must_haves:
  truths:
    - "D-33..D-35: DetailSurface is one native dialog content tree with closed adaptive/inline/drawer variants; adaptive uses modeless show() wide and modal showModal() constrained through scoped idempotent packaged JS."
    - "D-34: adaptive mode uses a content-driven ~64rem breakpoint for a nonmodal inline-end panel wide, a modal full-height drawer below it, and full-screen 320px reflow, while complex mutation-heavy content routes to Open full details."
    - "D-36..D-37: modal mode supplies inertness, focus entry/containment, Escape/visible close, and invoker/fallback restore; wide inline mode has no aria-modal/trap/inertness, and selection is text/icon/programmatic rather than color-only."
    - "D-38: parent LiveViews own selection, fetching, authorization, redaction, route grammar, and history; first open pushes, switch/close replace, Back closes naturally, and direct links use fallback focus."
    - "D-39..D-40: one narrow body scroll region, reduced-motion-safe feedback, explicit loading/empty/unavailable/permission/error/ready inputs, no lazy fetch, no sensitive duplicate tree, and no nested confirmation/dialog."
    - "D-55..D-56 and D-61: resize modality, focus/history/direct-link fallback, 320px full-screen reflow, reduced motion, long content, secrets, and no wide trap have executable source/harness/browser seams."
    - "Plan 78-05 turns the final string-tagged `phase78_slice: \"detail\"` contract green and then runs the unfiltered component/presenter/harness files as the first required complete six-component state-machine green gate."
  artifacts:
    - path: "lib/oban_powertools/web/components/operator_patterns.ex"
      provides: "Single-tree native adaptive detail surface"
      exports: ["detail_surface/1"]
    - path: "assets/oban_powertools/theme.js"
      provides: "Scoped show/showModal resize/patch/open/close/focus synchronization"
      exports: ["effectiveDetailMode", "syncDetailSurface", "syncDetailSurfaces"]
    - path: "assets/oban_powertools/tokens.css"
      provides: "Inline/drawer/full-screen detail geometry, one-scroll, backdrop, focus, and reduced-motion rules"
    - path: "test/oban_powertools/web/live/operator_patterns_harness_test.exs"
      provides: "Parent selection/history/transition contract without page migration"
  key_links:
    - from: "lib/oban_powertools/web/components/operator_patterns.ex"
      to: "assets/oban_powertools/theme.js"
      via: "fixed data-obpt-detail-surface attributes and native dialog open state"
      pattern: "data-obpt-detail-surface"
    - from: "assets/oban_powertools/theme.js"
      to: "Phoenix.LiveView.JS.ignore_attributes"
      via: "native open state survives patches while parent owns requested open state"
      pattern: "ignore_attributes"
---

<objective>
Ship DetailSurface as one adaptive native-dialog tree that is modeless inline on wide layouts and modal drawer/full-screen on constrained layouts.

Purpose: centralize accessible master/detail behavior without duplicate sensitive DOM, nested modals, page-owned client hooks, or component-owned URLs/data fetching.
Output: DetailSurface API/rendering, scoped native-dialog controller, responsive/reduced-motion CSS, parent harness, deterministic assets, and green focused contracts.
</objective>

<execution_context>
@/Users/jon/.agents/gsd-core/workflows/execute-plan.md
@/Users/jon/.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/78-component-groups-meta-components/78-CONTEXT.md
@.planning/phases/78-component-groups-meta-components/78-RESEARCH.md
@.planning/phases/78-component-groups-meta-components/78-PATTERNS.md
@.planning/phases/78-component-groups-meta-components/78-UI-SPEC.md
@.planning/phases/78-component-groups-meta-components/78-VALIDATION.md
@.planning/phases/78-component-groups-meta-components/78-04-SUMMARY.md
@.planning/phases/76-navigation-app-shell/76-PATTERNS.md
</context>

## Artifacts this phase produces

- New public function: `OperatorPatterns.detail_surface/1`.
- New closed variants/states: `variant: :adaptive | :inline | :drawer` (default adaptive) and `state: :loading | :empty | :ready | :unavailable | :permission_denied | :error`.
- New exact attrs: `id`, `title`, resource-specific `close_label`, `open`, `variant`, `state`, `resource`, `logical_fallback_id`, `close_event`, optional `loaded_announcement`, and optional `full_details_href`; slots `body` (required), `actions`, and `evidence`.
- New semantic IDs: `#{id}-title`, `#{id}-body`, and `#{id}-status`; one `<dialog id={id}>` tree only.
- New data selectors/fields: `data-obpt-detail-surface`, `data-obpt-detail-variant`, `data-obpt-detail-requested`, `data-obpt-detail-mode`, `data-obpt-detail-close`, and `data-obpt-detail-fallback`.
- New JS symbols: a `DETAIL_WIDE_QUERY` equivalent for `(min-width: 64rem)`, `effectiveDetailMode`, `syncDetailSurface`, `syncDetailSurfaces`, shared `restoreOwnedFocus`, close-before-reopen mode switching, invoker tracking, and safe fallback restoration for detail and confirmation owners carrying `data-obpt-focus-fallback`.
- New CSS family: `.obpt-detail-surface` with inline-end, modal drawer, 320px full-screen, one body-scroll, backdrop, long-content, focus-visible, high-contrast, and both reduced-motion paths.
- Regenerated packaged CSS/JS and extended component/harness/token/asset contracts; no host hook map or production route/page is created.

<tasks>

<task type="auto">
  <name>Task 78-05-01: Implement the one-tree native DetailSurface and responsive CSS</name>
  <files>lib/oban_powertools/web/components/operator_patterns.ex, test/oban_powertools/web/components/operator_patterns_test.exs, assets/oban_powertools/tokens.css, test/oban_powertools/web/theme_tokens_test.exs</files>
  <read_first>
    - @lib/oban_powertools/web/components/operator_patterns.ex
    - @test/oban_powertools/web/components/operator_patterns_test.exs
    - @assets/oban_powertools/tokens.css
    - @test/oban_powertools/web/theme_tokens_test.exs
    - @lib/oban_powertools/web/components/primitives.ex
    - @lib/oban_powertools/web/components/data_display.ex
    - @deps/phoenix_live_view/lib/phoenix_live_view/js.ex
    - @lib/oban_powertools/web/workflows_live.ex
    - @.planning/phases/78-component-groups-meta-components/78-UI-SPEC.md
  </read_first>
  <action>Add `detail_surface/1` with the exact attrs/slots/IDs/states above. Render exactly one native `&lt;dialog&gt;` using fixed data attributes and `phx-mounted={JS.ignore_attributes("open")}` where supported so client-owned native open state survives LiveView patches. The visible title labels the dialog, the Primitives close control uses the resource-specific close_label, the body appears once, optional actions/evidence/full-details follow, and a restrained status region may announce loaded_announcement once. Content state branches use existing DataDisplay state/evidence components for loading/empty/unavailable/permission/error and never fetch or inspect backend data. Do not wrap with focus_wrap, do not hardcode aria-modal (the controller sets it only for effective drawer mode), and do not render a second mobile/desktop tree or any component-owned nested dialog; document that callers must leave/close modal detail before invoking confirmation, then prove that approved transition in the connected harness. Add `.obpt-detail-surface` CSS: around 64rem modeless inline-end panel with no backdrop/nested scroll; below it full-height end drawer with one body overflow region; at 320px full-screen and 16px body padding; min-width 0/overflow-wrap for ordinary content; only bounded machine evidence may horizontally scroll; 44px close/action targets; token-backed focus/high-contrast/non-color state; transform/opacity feedback using motion tokens and instant media/root reduced-motion branches. Extend and turn green the `phase78_slice: "detail"` component/source/CSS tests for one dialog/body, explicit states, labels, no component-owned nested/duplicate tree, no lazy fetch/secrets/classes/styles, exact selectors, breakpoint, one-scroll, and reduced motion.</action>
  <verify>
    <automated>mix test test/oban_powertools/web/components/operator_patterns_test.exs --only phase78_slice:detail --seed 0</automated>
    <automated>mix test test/oban_powertools/web/components/operator_patterns_test.exs test/oban_powertools/web/operator_pattern_presenter_test.exs test/oban_powertools/web/theme_tokens_test.exs --seed 0</automated>
    <automated>mix format --check-formatted lib/oban_powertools/web/components/operator_patterns.ex test/oban_powertools/web/components/operator_patterns_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - Every variant/state renders exactly one labelled native dialog and one body; no focus_wrap, hardcoded blanket aria-modal, duplicate responsive tree, nested dialog, or lazy-fetch code exists.
    - Close control has the supplied resource-specific accessible name, and optional loaded status/full-details/action/evidence seams remain constrained and escaped.
    - CSS has a 64rem-around inline branch, constrained drawer branch, 320px full-screen branch, exactly one ordinary body scroll region, and no page horizontal overflow.
    - Both reduced-motion paths collapse feedback without hiding content/backdrop/focus, and status/selection never relies on color alone.
  </acceptance_criteria>
  <done>The detail slice passes with exactly one labelled native dialog/body across every variant/state and responsive, one-scroll, reduced-motion, focus, and no-overflow CSS branch.</done>
</task>

<task type="auto">
  <name>Task 78-05-02: Implement scoped native-dialog mode, patch, close, and focus synchronization</name>
  <files>assets/oban_powertools/theme.js, priv/static/oban_powertools/oban_powertools.css, priv/static/oban_powertools/oban_powertools.js, test/oban_powertools/web/assets_test.exs</files>
  <read_first>
    - @assets/oban_powertools/theme.js
    - @assets/oban_powertools/tokens.css
    - @priv/static/oban_powertools/oban_powertools.css
    - @priv/static/oban_powertools/oban_powertools.js
    - @test/oban_powertools/web/assets_test.exs
    - @lib/mix/tasks/oban_powertools.assets.build.ex
    - @deps/phoenix_live_view/lib/phoenix_live_view/js.ex
    - @.planning/phases/78-component-groups-meta-components/78-PATTERNS.md
  </read_first>
  <action>Extend the standalone nearest-root-scoped controller with fixed detail and `[data-obpt-focus-fallback]` selectors, `(min-width: 64rem)` media matching, and idempotent `effectiveDetailMode(surface)`, `syncDetailSurface(surface)`, `syncDetailSurfaces(root)`, and shared `restoreOwnedFocus(owner)` behavior. Before a LiveView patch inserts a confirmation/detail owner, a delegated trigger with matching `aria-controls` records the current invoker against that controlled id in root-scoped ephemeral memory; once the owner appears, transfer it to a WeakMap entry for that owner. On owner close/removal, focus the connected invoker or the explicit same-root fallback and discard the entry; retain no reason/resource/filter/token/result payload. Requested closed detail state closes an open dialog and calls that restoration helper. Requested open calls `show()` for inline/effective wide adaptive and `showModal()` for drawer/effective constrained adaptive; set/remove `aria-modal` and `data-obpt-detail-mode` accordingly. When resize changes mode, capture a safely focusable descendant, call `close()` before reopening with the other native API, then restore internal focus when safe; wide inline must allow focus outside. Escape/native cancel and `[data-obpt-detail-close]` must prevent DOM-only close divergence and dispatch the existing parent close event/click so URL/open assigns agree. Re-sync inserted/patched owners through a narrowly filtered observer and media-query listener. Never use a custom `phx-hook`, global query outside nearest root, dialog polyfill/dependency, local/session storage, console logging, dynamic evaluation, or sensitive IDs/filter/reason/token/result persistence. Extend asset assertions for selectors/functions/prohibitions and run repeatable asset build/equality.</action>
  <verify>
    <automated>node --check assets/oban_powertools/theme.js &amp;&amp; mix oban_powertools.assets.build &amp;&amp; mix test test/oban_powertools/web/assets_test.exs test/oban_powertools/web/theme_tokens_test.exs --seed 0</automated>
    <automated>node --check priv/static/oban_powertools/oban_powertools.js</automated>
  </verify>
  <acceptance_criteria>
    - Source/packaged JS has the three named functions, fixed selectors, close-before-reopen behavior, mode-specific `show`/`showModal`, and exact aria-modal synchronization.
    - Repeated sync, LiveView patches, and resize do not stack calls or create a second tree; inline mode never traps or inerts background content.
    - Escape/visible close routes through parent close ownership, and restoration prefers a connected invoker then same-root logical fallback.
    - No host hook map, storage/logging, dynamic evaluation, dependency, sensitive state, or unscoped document behavior is added; source/static assets are byte-equal and repeat-build stable.
  </acceptance_criteria>
  <done>Scoped detail synchronization idempotently switches show/showModal modes, routes close and focus restoration correctly, and yields byte-equal repeat-built assets without host or sensitive state.</done>
</task>

<task type="auto">
  <name>Task 78-05-03: Pin parent-owned selection, history, and drawer-to-confirmation transitions</name>
  <files>test/oban_powertools/web/live/operator_patterns_harness_test.exs</files>
  <read_first>
    - @test/oban_powertools/web/live/operator_patterns_harness_test.exs
    - @lib/oban_powertools/web/components/operator_patterns.ex
    - @lib/oban_powertools/web/workflows_live.ex
    - @lib/oban_powertools/web/jobs_live.ex
    - @test/oban_powertools/web/live/showcase_live_test.exs
    - @.planning/phases/78-component-groups-meta-components/78-CONTEXT.md
    - @.planning/phases/78-component-groups-meta-components/78-VALIDATION.md
  </read_first>
  <action>Turn the `phase78_slice: "detail"` portion of the test-only connected harness green. The parent owns selected id, explicit content state, authorization/redaction, canonical filter-preserving list/detail URLs, and open/close commands. Assert first open pushes a meaningful detail query, switching while open replaces it, explicit close replaces with preserved filters, Back after the initial push closes naturally, and direct/reloaded detail URLs load with the page/results fallback because no invoker exists. Assert each trigger uses matching `aria-expanded`/`aria-controls` and selected text/icon/programmatic state. Exercise loading, empty, unavailable, permission-denied, error, ready, long-content, and loaded-announcement transitions. Model a confirmation launch from constrained detail by first closing/leaving the drawer or navigating to full details; assert rendered HTML never contains a confirmation dialog nested inside the detail dialog. After the detail slice passes, run the complete unfiltered harness and require all confirmation/filter/detail plus cross-slice no-nesting/history cases to be green. Keep route grammar, data fetching, auth, redaction, and mutations test-parent-owned and do not edit production pages.</action>
  <verify>
    <automated>mix test test/oban_powertools/web/live/operator_patterns_harness_test.exs --only phase78_slice:detail --seed 0</automated>
    <automated>mix test test/oban_powertools/web/live/operator_patterns_harness_test.exs --seed 0</automated>
    <automated>mix format --check-formatted test/oban_powertools/web/live/operator_patterns_harness_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - Connected assertions distinguish push first-open, replace switch/close, natural Back close, direct-link load, and filter-preserving URL state.
    - Trigger expanded/controls/selected semantics stay synchronized with parent selection and are non-color.
    - All six content states and long/redacted content are explicit caller inputs; no component fetch or duplicate sensitive tree exists.
    - Drawer-to-confirmation transition leaves at most one dialog and no confirmation descendant under detail; production pages/routes/auth/queries/mutations remain unchanged.
    - The complete unfiltered component/presenter/harness gate is green for the first time only after all six components and all three connected state-machine slices exist.
  </acceptance_criteria>
  <done>The connected detail harness proves push/replace/Back/direct-link semantics, all explicit states, synchronized triggers, and a one-dialog transition before the complete six-component gate turns green.</done>
</task>

</tasks>

<threat_model>
## ASVS L1 Threat Model — blocking threshold: high

| Threat ID | Category | Severity | Mitigation and verification |
|-----------|----------|----------|-----------------------------|
| T-78-05-AUTH | Elevation of privilege | high | Parent owns selection, fetch, authorization, redaction, routes, and actions; component/client controller never loads or authorizes data. |
| T-78-05-LEAK | Information disclosure | high | One DOM tree prevents hidden duplicates; normalized body/evidence and full-DOM sentinels exclude secret originals and sensitive attrs/storage/logs. |
| T-78-05-XSS | Injection | high | Title/state/body copy is escaped and slots remain constrained; no raw HTML/eval/unsafe attributes. |
| T-78-05-FOCUS | Denial of service / accessibility | high | Native modal containment only constrained, wide no-trap, close-before-reopen resize, Escape/close parent sync, and invoker/fallback restore are mandatory. |
| T-78-05-STATE | Tampering | high | Parent requested state and URL remain authoritative; native cancel cannot leave DOM closed while assigns reopen it. |
| T-78-05-HOST | Tampering | high | Fixed nearest-root selectors, no host hook map/polyfill/dependency/global storage, and idempotent patch synchronization preserve host isolation. |
| T-78-05-NEST | Denial of service | high | Component/source/harness/browser contracts prohibit nested dialogs and require leaving drawer before confirmation. |
</threat_model>

<verification>
- Run component/CSS tests, JS syntax and asset-equality checks, then connected parent/history transitions.
- Block on any high-severity authorization, secret duplication, injection, focus/inertness, state divergence, host boundary, or nested-dialog issue.
- Confirm no production LiveView, route grammar, auth callback, Ecto query/schema, mutation engine, dependency, or lockfile changes.
</verification>

<success_criteria>
- DetailSurface ships one native tree with truthful explicit states and correct inline/drawer/full-screen geometry.
- Scoped packaged JS safely synchronizes open, modality, resize, patches, Escape/close, and focus without host integration work.
- Parent history/selection/direct-link/nested-dialog boundaries have connected test proof.
- Assets are deterministic and every focused component/harness/token/asset/syntax check passes.
</success_criteria>

<output>
Create `.planning/phases/78-component-groups-meta-components/78-05-SUMMARY.md` when done.
</output>
