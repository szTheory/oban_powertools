---
phase: 78-component-groups-meta-components
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/78-component-groups-meta-components/78-START-SHA
  - test/oban_powertools/web/components/operator_patterns_test.exs
  - test/oban_powertools/web/operator_pattern_presenter_test.exs
  - test/oban_powertools/web/live/operator_patterns_harness_test.exs
  - test/oban_powertools/operator_pattern_story_catalog_test.exs
  - test/browser/specs/operator-patterns.behavior.spec.ts
  - test/browser/support/verify-group-baselines.mjs
autonomous: true
requirements:
  - GROUP-01
  - GROUP-02
  - FORM-04
  - COPY-02
  - A11Y-02
must_haves:
  truths:
    - "D-01..D-61 are represented by syntactically valid RED contracts before any Phase 78 production component, presenter, catalog, manifest, or client behavior is implemented."
    - "GROUP-01 and GROUP-02 are pinned to exactly six stateless public Phoenix function components in ObanPowertools.Web.Components.OperatorPatterns, with parent LiveViews retaining authorization, server truth, mutation, redaction, URL, and audit ownership."
    - "FORM-04 and COPY-02 are pinned to action-specific object, scope, consequence, reversibility, support-boundary, reason, exact bulk-count, recovery, and receipt copy; generic confirmation/error claims are rejected."
    - "A11Y-02 is pinned to connected focus trap/restore, Escape, modal inertness, wide no-trap behavior, disclosure keyboard behavior, exact status messages, and one responsive DOM tree."
    - "Wave 0 component and harness contracts are tagged by explanation, filter, confirmation, and detail slice; Plans 78-02..78-04 run only the slice they implement, and the unfiltered six-component/complete-harness gate is required to turn green only after Plan 78-05."
    - "Before Phase 78 execution edits begin, the current commit is captured once in 78-START-SHA and retained across atomic task commits for the cumulative protected-boundary audit in Plan 78-08."
    - "RED is accepted only when failures identify missing Phase 78 modules, exports, fixtures, schema-6 fields, or selectors; malformed tests and unrelated pre-existing visual residuals are not valid RED evidence."
  artifacts:
    - path: ".planning/phases/78-component-groups-meta-components/78-START-SHA"
      provides: "Immutable pre-execution Phase 78 base commit for cumulative scope verification"
    - path: "test/oban_powertools/web/components/operator_patterns_test.exs"
      provides: "Render, composition, copy-order, safe-attribute, escaping, one-tree, and semantic contracts for all six groups"
    - path: "test/oban_powertools/web/operator_pattern_presenter_test.exs"
      provides: "Finite-map normalization and truthful unknown/missing evidence contracts"
    - path: "test/oban_powertools/web/live/operator_patterns_harness_test.exs"
      provides: "Connected parent/server authority, confirmation, filter, detail, and history contracts"
    - path: "test/oban_powertools/operator_pattern_story_catalog_test.exs"
      provides: "Exact 23-story order, deterministic normalized fixtures, activation, and secret-absence contracts"
    - path: "test/browser/specs/operator-patterns.behavior.spec.ts"
      provides: "Connected keyboard, focus, modality, history, status-message, responsive, and confidentiality contracts"
    - path: "test/browser/support/verify-group-baselines.mjs"
      provides: "Independent exact-276 group baseline and changed-scope CLI contract"
  key_links:
    - from: "test/oban_powertools/web/components/operator_patterns_test.exs"
      to: "lib/oban_powertools/web/components/operator_patterns.ex"
      via: "Phoenix.LiveViewTest.__render_component__/4 for six named functions"
      pattern: "ObanPowertools.Web.Components.OperatorPatterns"
    - from: "test/oban_powertools/web/live/operator_patterns_harness_test.exs"
      to: "lib/oban_powertools/lifeline.ex"
      via: "server-side reason, freshness, drift, and single-use execution proof"
      pattern: "execute_repair"
    - from: "test/browser/specs/operator-patterns.behavior.spec.ts"
      to: "test/browser/support/manifest.ts"
      via: "generated schema-6 groupStories export and one-at-a-time activation"
      pattern: "groupStories"
---

<objective>
Create the Wave 0 RED contract layer for the complete Phase 78 surface without implementing production components, presenters, catalogs, manifest support, packaged behavior, or baselines.

Purpose: force the phase's server-truth, copy, confidentiality, responsive, focus, modality, and deterministic-evidence obligations to fail at their intended seams before implementation begins.
Output: one durable Phase 78 base-SHA marker plus six syntactically valid test/CLI files whose failures point only to missing Phase 78 artifacts.
</objective>

<execution_context>
@/Users/jon/.agents/gsd-core/workflows/execute-plan.md
@/Users/jon/.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/78-component-groups-meta-components/78-CONTEXT.md
@.planning/phases/78-component-groups-meta-components/78-RESEARCH.md
@.planning/phases/78-component-groups-meta-components/78-PATTERNS.md
@.planning/phases/78-component-groups-meta-components/78-UI-SPEC.md
@.planning/phases/78-component-groups-meta-components/78-VALIDATION.md
</context>

## Artifacts this phase produces

- Public production module/file: `ObanPowertools.Web.Components.OperatorPatterns` in `lib/oban_powertools/web/components/operator_patterns.ex`, exporting exactly `confirm_action_dialog/1`, `filter_bar/1`, `detail_surface/1`, `attention_card/1`, `audit_entry/1`, and `why_blocked/1`.
- Presenter symbols in `ControlPlanePresenter`: `normalize_active_filters/1`, `normalize_operator_results/1`, `normalize_blockers/1`, `normalize_audit_entry/1`, and `normalize_evidence_completeness/1`; finite atom/string aliases only, no runtime atom creation.
- Status taxonomy field/domain: closed `:operator_result` domain with `:success`, `:failed`, and `:skipped` states for result rows.
- Confirmation contract fields: `id`, `intent`, `state`, `title`, `object_label`, `scope`, `consequence`, `reversibility`, `support_boundary`, `form`, `bulk_count`, `bulk_scope`, `confirm_label`, `dismiss_label`, `pending_copy`, `logical_fallback_id`, `submit_event`, `dismiss_event`, `dismissible`, `progress`, and `results`; slots `recovery`, `audit`, and `support_details`.
- Filter contract fields: `id`, `form`, `mode`, `result_summary`, `results_target_id`, `active_filters`, `dirty`, `filters_expanded`, `change_event`, `submit_event`, and `clear_href`; slots `fields` and `advanced_fields`.
- Detail contract fields: `id`, `title`, `close_label`, `open`, `variant`, `state`, `resource`, `logical_fallback_id`, `close_event`, `loaded_announcement`, and `full_details_href`; slots `body`, `actions`, and `evidence`.
- Attention contract fields: `id`, `title`, `summary`, `impact`, `observed_at`, `observed_datetime`, `domain`, `status`, `severity`, `completeness`, and `live`; slots `primary_action` and `secondary_actions`.
- Why-blocked contract fields: `id`, `title`, `summary`, `impact`, `observed_at`, `observed_datetime`, `evidence_state: :current | :stale | :unavailable | :permission_denied`, `completeness`, and normalized `blockers`; slots `next_action` and `evidence`.
- Audit contract fields: `id` and normalized `entry`; slot `evidence`.
- CSS symbols under `.obpt-root`: `.obpt-confirm-action`, `.obpt-filter-bar`, `.obpt-detail-surface`, `.obpt-attention-card`, `.obpt-why-blocked`, `.obpt-audit-entry`, plus their `__*` descendants, state attributes, `64rem` adaptive branch, 320px reflow, focus-visible, high-contrast, and reduced-motion branches.
- Client symbols in `theme.js`: fixed group selectors plus `setFilterState`, `syncFilterDisclosures`, `effectiveDetailMode`, `syncDetailSurface`, `syncDetailSurfaces`, and focus restoration helpers; no custom host hook map.
- Story/support module/file: `ObanPowertools.OperatorPatternStoryCatalog` in `test/support/operator_pattern_story_catalog.ex` with `stories/0`, `story!/1`, exact 23 IDs, normalized fixtures, and `activation: :none | :overlay`.
- Showcase/manifest fields: `@group_catalog_module`, `@group_catalog_path`, `group_stories`, `active_group_overlay`, `group_stories` JSON collection, `kind: "group"`, `activation: "none" | "overlay"`, `schema_version: 6`, `ShowcaseGroupStory`, and exported `groupStories`; 64 total targets.
- Browser symbols/files: `activateTarget/2` group activation support, `operator-patterns.behavior.spec.ts`, `verify-group-baselines.mjs` with `--changed-scope`, and exactly 276 `test/browser/__screenshots__/chromium-{320,tablet,wide}/showcase/group-*/*.png` files.
- Execution-boundary artifact: `.planning/phases/78-component-groups-meta-components/78-START-SHA`, containing exactly the commit at Phase 78 execution start and never refreshed by later waves.
- Validation artifact: reconciled `.planning/phases/78-component-groups-meta-components/78-VALIDATION.md` with task IDs, connected behavior, axe, compare-only VRT, exact-baseline, asset-equality, and package-fallback evidence.

## Source Coverage Audit

| Source | Executable coverage |
|--------|---------------------|
| ROADMAP Phase 78 | Six named groups, explain-then-act hierarchy, reason/consequence/scope, focus/Escape/restore, stories, VRT, and axe are split across Plans 78-02..78-08. |
| REQUIREMENTS | GROUP-01/02, FORM-04, COPY-02, and A11Y-02 are all assigned in frontmatter and receive RED, implementation, connected, and visual evidence. |
| CONTEXT | D-01..08 -> 78-02; D-09..23 -> 78-04; D-24..32 -> 78-03; D-33..40 -> 78-05; D-41..56 -> 78-02/07; D-57..61 -> 78-06..08. |
| RESEARCH | Recommended eight-plan sequence, parent/server truth, standalone packaged controller, exact 276 baselines, and no page migration/dependency change are adopted verbatim as execution boundaries. |
| UI-SPEC | Exact component list, four-theme/three-viewport matrix, 64rem adaptive behavior, 320px one-tree reflow, representative connected 200% zoom reflow, copy prohibitions, 44px targets, tokens, and motion/accessibility gates are planned. |
| VALIDATION | All six Wave 0 files, focused ExUnit, schema-6 smoke, connected browser, group axe, Docker VRT, exact cardinality, assets, and package fallback map to tasks. |
| PATTERNS | `Primitives`/`Forms`/`DataDisplay`, `ControlPlanePresenter`, `StatusTaxonomy`, scoped CSS/JS, fail-closed catalogs, manifest append order, and verifier analogs are named in task read sets. |

No plain Phase `*-SPEC.md` with `## Edge Coverage` or `## Prohibitions` exists; `78-UI-SPEC.md` is handled as the binding UI contract.

<tasks>

<task type="auto" tdd="true">
  <name>Task 78-01-01: Pin the six public component and finite presenter contracts</name>
  <files>.planning/phases/78-component-groups-meta-components/78-START-SHA, test/oban_powertools/web/components/operator_patterns_test.exs, test/oban_powertools/web/operator_pattern_presenter_test.exs</files>
  <read_first>
    - @test/oban_powertools/web/components/operator_patterns_test.exs
    - @test/oban_powertools/web/operator_pattern_presenter_test.exs
    - @lib/oban_powertools/web/components/primitives.ex
    - @lib/oban_powertools/web/components/forms.ex
    - @lib/oban_powertools/web/components/data_display.ex
    - @lib/oban_powertools/web/control_plane_presenter.ex
    - @lib/oban_powertools/web/status_taxonomy.ex
    - @test/oban_powertools/web/components/data_display_test.exs
    - @.planning/phases/78-component-groups-meta-components/78-UI-SPEC.md
    - @.planning/phases/78-component-groups-meta-components/78-PATTERNS.md
  </read_first>
  <action>Before editing any Phase 78 contract or implementation file, assert the protected production/dependency worktree is clean and run `test -s .planning/phases/78-component-groups-meta-components/78-START-SHA || git rev-parse HEAD &gt; .planning/phases/78-component-groups-meta-components/78-START-SHA` to capture one newline-terminated full commit id; never overwrite an existing valid marker on resume. Create async ExUnit RED tests using `Phoenix.LiveViewTest.__render_component__/4` and the existing slot-map helper pattern. Split component cases into independently runnable `phase78_slice: "explanation" | "filter" | "confirmation" | "detail"` describe/test tags (string values intentionally match Mix's CLI tag parser); keep only genuinely phase-wide export/source assertions unfiltered. Assert the exact six exports; closed attrs/states; Primitives/Forms/DataDisplay composition; fixed information order; one responsive tree; required reason and optional exact-count fields; role/dialog/time/article/disclosure semantics; explicit loading/empty/unavailable/permission/invalid/pending/partial/error branches; and exact missing evidence copy. Add source guards rejecting caller `class`/`style`/raw HTML/arbitrary globals, production-page imports, backend struct pattern matching, `String.to_atom`, raw `inspect(error)`, nested dialogs, duplicate mobile trees, generic `Are you sure?`/bare `Confirm`/ambiguous `Cancel`/`Something went wrong`/`N/A`/`fixed` copy, and secret-bearing `data-*`/title/hidden content. In presenter tests, pin finite atom/string aliases, stable ordering and unique IDs for filters/results/blockers, default unknown completeness, current-versus-snapshot labels, absolute audit time plus machine datetime, exact missing reason/outcome copy, hostile Unicode/HTML escaping at render time, and rejection of unsupported structs/raw errors/tokens/hashes. The unfiltered command below is the phase-wide RED gate: record its missing-slice failures now, but do not require it to become green until Plan 78-05. Do not create production modules.</action>
  <verify>
    <automated>mix test test/oban_powertools/web/components/operator_patterns_test.exs test/oban_powertools/web/operator_pattern_presenter_test.exs --seed 0</automated>
    <automated>mix format --check-formatted test/oban_powertools/web/components/operator_patterns_test.exs test/oban_powertools/web/operator_pattern_presenter_test.exs</automated>
    <automated>PHASE78_BASE=$(tr -d '[:space:]' &lt; .planning/phases/78-component-groups-meta-components/78-START-SHA) &amp;&amp; test "$(wc -l &lt; .planning/phases/78-component-groups-meta-components/78-START-SHA | tr -d ' ')" -eq 1 &amp;&amp; git cat-file -e "${PHASE78_BASE}^{commit}"</automated>
  </verify>
  <acceptance_criteria>
    - Both files compile and reach RED failures that name missing `OperatorPatterns` or presenter symbols, not syntax/helper failures.
    - Literal assertions cover all six public function names and every closed lifecycle/content-state enum from the UI-SPEC.
    - Full-HTML sentinel checks cover text, attributes, titles, details, destinations, and hidden markup; hostile text is expected to be escaped rather than dropped.
    - Presenter tests prove no runtime atom creation and no inference of success, completeness, current truth, causality, or audit durability.
    - Component tests expose the four exact `phase78_slice` tag values, and the initial unfiltered RED output is recorded as a complete contract gate rather than reused as an incremental-wave green gate.
    - `78-START-SHA` contains one resolvable commit id captured before Phase 78 execution changes and is not regenerated by later tasks.
  </acceptance_criteria>
  <done>All six component contracts, five presenter seams, four slice tags, and the immutable resolvable start SHA compile and reach RED only at missing Phase 78 symbols.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 78-01-02: Pin connected parent and server-truth state machines</name>
  <files>test/oban_powertools/web/live/operator_patterns_harness_test.exs</files>
  <read_first>
    - @test/oban_powertools/web/live/operator_patterns_harness_test.exs
    - @test/oban_powertools/web/live/showcase_live_test.exs
    - @lib/oban_powertools/web/dev/showcase_live.ex
    - @lib/oban_powertools/lifeline.ex
    - @lib/oban_powertools/lifeline/repair_preview.ex
    - @test/oban_powertools/lifeline_test.exs
    - @lib/oban_powertools/web/workflows_live.ex
    - @.planning/phases/78-component-groups-meta-components/78-CONTEXT.md
    - @.planning/phases/78-component-groups-meta-components/78-VALIDATION.md
  </read_first>
  <action>Create a connected LiveView RED contract with a test-only `OperatorPatternsHarnessLive` parent. Organize the harness cases under the same independently runnable string-valued `phase78_slice: "confirmation" | "filter" | "detail"` tags so each implementation wave can prove only its owned state machine; keep cross-slice/no-nested-dialog assertions phase-wide for the post-78-05 unfiltered gate. Require blank/short reason and wrong frozen count to keep preview open with inline errors; valid input to be revalidated server-side and accepted once; duplicate submission to produce one mutation receipt; real Lifeline expired/drifted/consumed execution attempts to reject and require a fresh preview; partial rows to preserve caller order and distinct success/failed/skipped recovery. Require filter draft edits not to change applied result/URL state, Apply to reset pagination and patch a canonical query, invalid drafts to remain visible, and invalid direct URLs to canonicalize with replacement. Require first detail open to push, selection switch and explicit close to replace, Back to close after first open, direct URLs to use fallback focus, and drawer-to-confirmation transition to leave at most one dialog. The unfiltered command below records the complete harness RED contract and is not an incremental green requirement before Plan 78-05. Do not change production LiveViews, Lifeline, auth, queries, mutations, routes, or audit schemas.</action>
  <verify>
    <automated>mix test test/oban_powertools/web/live/operator_patterns_harness_test.exs --seed 0</automated>
    <automated>mix format --check-formatted test/oban_powertools/web/live/operator_patterns_harness_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - The test module compiles and RED failures identify missing group components/harness transitions only.
    - Server-side assertions distinguish HTML-disabled duplicate suppression from authorization, reason, count, scope, freshness, drift, and single-use checks.
    - URL assertions distinguish push-on-first-open from replace-on-switch/close and preserve filter parameters.
    - No production page module, Lifeline implementation, Ecto query, route macro, authorization callback, or audit schema is edited.
    - Confirmation, filter, and detail cases are independently selectable with the exact `phase78_slice` values used by Plans 78-03..78-05.
  </acceptance_criteria>
  <done>The connected harness compiles and independently exercises confirmation, filter, and detail server-truth transitions without modifying production authority boundaries.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 78-01-03: Pin the exact operator-pattern story catalog</name>
  <files>test/oban_powertools/operator_pattern_story_catalog_test.exs</files>
  <read_first>
    - @test/oban_powertools/operator_pattern_story_catalog_test.exs
    - @test/oban_powertools/data_display_story_catalog_test.exs
    - @test/support/data_display_story_catalog.ex
    - @test/support/showcase_catalog.ex
    - @.planning/phases/78-component-groups-meta-components/78-CONTEXT.md
    - @.planning/phases/78-component-groups-meta-components/78-UI-SPEC.md
  </read_first>
  <action>Create the RED contract for `ObanPowertools.OperatorPatternStoryCatalog`. Assert the exact 23 D-58..D-60 IDs in literal order; `kind: :group`; stable `obpt-group-story-*`, `showcase/group-*`, and `[data-obpt-group-story="..."]` targets; closed `activation: :none | :overlay`; deterministic normalized presentation fixtures; absolute timestamps; long IDs; Unicode/RTL/HTML-looking content; all explicit unavailable/permission/stale/unknown/partial states; and unique-secret sentinel absence across inspected fixture metadata. Require overlay metadata only for confirmation/modal-open stories and require initial catalog data to open no overlay globally. Do not create the catalog.</action>
  <verify>
    <automated>mix test test/oban_powertools/operator_pattern_story_catalog_test.exs --seed 0</automated>
    <automated>mix format --check-formatted test/oban_powertools/operator_pattern_story_catalog_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - The test compiles and fails only because `OperatorPatternStoryCatalog` is missing.
    - Exactly 23 literal IDs appear in the binding order; TypeScript does not become a second story-ID source.
    - Every fixture is deterministic normalized presentation data with no backend structs, random time, database access, Faker, raw secrets, preview tokens, plan hashes, or raw errors.
    - Activation assertions prevent multiple top-layer dialogs from being open on initial showcase mount.
  </acceptance_criteria>
  <done>The catalog contract compiles, pins exactly 23 ordered deterministic stories and safe activation metadata, and fails only because the catalog is absent.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 78-01-04: Pin connected browser behavior and exact baseline scope</name>
  <files>test/browser/specs/operator-patterns.behavior.spec.ts, test/browser/support/verify-group-baselines.mjs</files>
  <read_first>
    - @test/browser/specs/operator-patterns.behavior.spec.ts
    - @test/browser/support/verify-group-baselines.mjs
    - @test/browser/specs/data-display.behavior.spec.ts
    - @test/browser/specs/forms.behavior.spec.ts
    - @test/browser/specs/shell.behavior.spec.ts
    - @test/browser/support/manifest.ts
    - @test/browser/support/showcase.ts
    - @test/browser/support/verify-data-baselines.mjs
    - @playwright.config.ts
    - @.planning/phases/78-component-groups-meta-components/78-VALIDATION.md
  </read_first>
  <action>Create a syntactically valid Playwright RED spec that imports generated `groupStories`, has a module-load guard until schema 6 exports them, and uses behavior-specific lookups rather than duplicating all 23 IDs. Define connected cases for confirmation initial focus, Tab/Shift-Tab wrap, Escape/visible dismiss, invoker/fallback restore, busy/duplicate suppression, partial result focus/order, exact one receipt; FilterBar disclosure/draft/apply/remove/clear/status/URL behavior; DetailSurface modal/inertness below the breakpoint, wide no-trap, resize switching, close/history/fallback; and explanation/audit non-color status, unknown truth, absolute time, and secret absence. Also define representative `200% zoom` cases for confirmation, filter, detail, explanation, and audit using a Chromium test helper that halves the CSS viewport and sets/asserts device scale 2 for equivalent browser-zoom reflow, then mechanically asserts stacked actions/fields, wrapped long content, usable visible focus, one responsive tree, and no ordinary page/story horizontal overflow. Create a RED `verify-group-baselines.mjs` by following the independent data verifier: require schema 6, derive 23 group snapshots x four themes x three projects, assert exact cardinality 276, reject missing/extra files, and implement `--changed-scope` to reject tracked/untracked/renamed screenshot changes outside `group-*`. Do not add manifest support or PNGs.</action>
  <verify>
    <automated>npx playwright test --list test/browser/specs/operator-patterns.behavior.spec.ts</automated>
    <automated>node --check test/browser/support/verify-group-baselines.mjs</automated>
  </verify>
  <acceptance_criteria>
    - Playwright parsing succeeds and discovery reaches a deliberate RED guard for missing schema-6 `groupStories`.
    - Test titles include `group` and separate 320, tablet-adaptive, and wide behavior for focused grep/project execution.
    - Test titles include `200% zoom` for one connected confirmation, filter, detail, explanation, and audit story, and the helper asserts its effective half-width/device-scale precondition before reflow assertions.
    - The verifier parses but fails against the missing schema-6 collection/baseline set rather than using hardcoded filesystem-only assumptions.
    - Exact expected baseline count is computed as `23 * 4 * 3 = 276`, and changed scope handles porcelain renames as well as tracked/untracked paths.
  </acceptance_criteria>
  <done>Playwright discovery reaches the deliberate schema-6 RED guard, and the syntax-valid verifier derives exactly 276 baselines with tracked, untracked, and renamed scope checks.</done>
</task>

</tasks>

<threat_model>
## ASVS L1 Threat Model — blocking threshold: high

| Threat ID | Category | Severity | Mitigation and verification |
|-----------|----------|----------|-----------------------------|
| T-78-ATTR | Tampering / elevation | high | RED render/source contracts reject caller visual/semantic/action escape hatches and arbitrary map keys reaching DOM attributes. |
| T-78-XSS | Injection | high | Hostile HTML/Unicode is escaped through HEEx; raw HTML and raw error inspection are forbidden. |
| T-78-LEAK | Information disclosure | high | Full-DOM and fixture sentinel checks cover text, attributes, hidden disclosures, destinations, copy channels, tokens, hashes, and reasons. |
| T-78-AUTH | Spoofing / tampering | high | Connected RED contracts require server reauthorization and reason/count/scope/freshness/single-use validation; client state is never authority. |
| T-78-REPLAY | Tampering | high | Real Lifeline expired, drifted, and consumed previews must reject and require explicit refresh; duplicate UI suppression is not accepted as replay protection. |
| T-78-FOCUS | Denial of service / accessibility | high | Browser RED cases require modal containment, Escape, invoker/fallback restore, wide no-trap behavior, and no stacked/nested dialogs. |
| T-78-HOST | Supply/host boundary | high | Contracts reject custom host hook registration, unscoped selectors, component storage/logging, and production catalog/package leakage. |
</threat_model>

<verification>
- Run every narrow Wave 0 command and record meaningful RED only when the failure is a missing Phase 78 symbol/export/catalog/schema/selectors/baselines.
- Treat the unfiltered component and harness commands as complete phase RED evidence; Plans 78-02..78-04 use `--only phase78_slice:<slice>`, and Plan 78-05 is the first wave required to make the unfiltered files green.
- Run formatting or syntax checks so RED cannot be attributed to malformed Elixir, TypeScript, or JavaScript.
- Confirm the diff contains only the six planned contract files and no production implementation, generated asset, manifest JSON, screenshot, dependency, schema, or production page file.
</verification>

<success_criteria>
- All six Wave 0 contract files exist and are syntactically valid.
- A durable pre-execution SHA exists for cumulative protected-boundary proof, and all incremental implementation waves have executable slice-filtered verification.
- Every Phase 78 requirement and D-01..D-61 has an independently checkable RED assertion or an explicitly named later evidence seam.
- The contract blocks unsafe attrs, secret/raw-error leakage, client-authority drift, stale/replay execution, duplicate responsive trees, nested/stacked dialogs, generic copy, and baseline-scope contamination.
- No production implementation or VRT baseline is created by this plan.
</success_criteria>

<output>
Create `.planning/phases/78-component-groups-meta-components/78-01-SUMMARY.md` when done.
</output>
