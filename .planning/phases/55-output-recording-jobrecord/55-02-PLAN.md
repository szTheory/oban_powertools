---
phase: 55
plan: 02
type: execute
wave: 2
depends_on: ["55-01"]
files_modified:
  - lib/oban_powertools/worker.ex
  - test/oban_powertools/worker_test.exs
autonomous: true
requirements:
  - REC-01
  - REC-05
must_haves:
  truths:
    - "D-06: record_output: true is the only opt-in switch; workers without it never persist successful return values."
    - "D-07: only {:ok, payload} is recorded; plain :ok remains success-without-output and creates no JobRecord row."
    - "D-08: Phase 55 does not add {:ok, payload, record_opts} as a public worker return shape."
    - "D-09: Phase 55 does not add a public manual record_output/2 API."
    - "D-10: Worker validates record_output, output_limit, and output_retention at compile time and strips them before use Oban.Worker."
    - "D-11: Recording runs after process/1 returns {:ok, payload} and before Hooks.after_result/3 routes on_success."
    - "D-12: Recording failures, conflicts, encoding failures, and size rejections warn but never alter the result Oban sees."
    - "D-13: Error, raise/throw/exit, discard, cancel, snooze, and unknown successful shapes are not recorded."
    - "D-14: Recorder integration remains internally structured around normalize, settings, best-effort insert, and original-result return."
    - "D-37: Worker tests prove opt-in {:ok, payload} recording before on_success and no rows for :ok or non-success outcomes."
  artifacts:
    - path: "lib/oban_powertools/worker.ex"
      provides: "Option validation and wrapper integration"
  key_links:
    - from: "lib/oban_powertools/worker.ex"
      to: "lib/oban_powertools/job_record.ex"
      via: "JobRecord.record/5 call inside perform wrapper"
      pattern: "JobRecord.record("
---

<objective>
Inject output recording into the `ObanPowertools.Worker` lifecycle wrapper. Expose the options at compile-time and run the recording *after* the job process returns `{:ok, payload}` but *before* the `on_success` hook fires.

Purpose: Guarantee that opting into `record_output` captures successful payloads automatically and makes them available to consumers downstream in hooks.
Output: Modified worker macro, generated `__powertools_perform__`, and matching tests.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/55-output-recording-jobrecord/55-CONTEXT.md
@.planning/phases/55-output-recording-jobrecord/55-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add recording parameters to worker macro</name>
  <files>lib/oban_powertools/worker.ex, test/oban_powertools/worker_test.exs</files>
  <read_first>
    - lib/oban_powertools/worker.ex
    - test/oban_powertools/worker_test.exs
  </read_first>
  <action>
    Reference `lib/oban_powertools/worker.ex` as an analog for stripping compile-time options out of `oban_opts` (e.g., `opts |> Keyword.delete(:args) |> Keyword.delete(:limits)`).

    TDD Behavior to implement:
    - Test 1: Workers compiled with `record_output: true` do not pass this key down to `use Oban.Worker`.
    - Test 2: `__powertools_output_recording__/0` generated function exposes the configured `record_output`, `output_limit`, and `output_retention`.
    - Test 3: `record_output: false` is the default. `output_limit` defaults to `65_536`. `output_retention` defaults to `:standard`.

    Modify `lib/oban_powertools/worker.ex` `__using__/1` macro. Extract `record_output`, `output_limit`, and `output_retention` from `opts`. Validate them (e.g. `output_retention` must be in `[:standard, :extended, :ephemeral]`). Remove them from `oban_opts`.
    Inject a `def __powertools_output_recording__()` to expose a map of these settings onto the module, so they can be read efficiently at runtime.
  </action>
  <verify>
    <automated>mix test test/oban_powertools/worker_test.exs</automated>
  </verify>
  <done>
    - `lib/oban_powertools/worker.ex` contains `Keyword.delete(:record_output)`
    - `test/oban_powertools/worker_test.exs` has a test asserting `__powertools_output_recording__` behaves correctly.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Inject recording execution into `__powertools_perform__`</name>
  <files>lib/oban_powertools/worker.ex, test/oban_powertools/worker_test.exs</files>
  <read_first>
    - lib/oban_powertools/worker.ex
  </read_first>
  <action>
    Reference `lib/oban_powertools/worker.ex` for how hooks are executed in `__powertools_perform__` and ensure `JobRecord.record/5` happens before routing to hooks.

    TDD Behavior to implement:
    - Test 1: An `{:ok, payload}` return from `process/1` with `record_output: true` calls `JobRecord.record/5` before routing to hooks.
    - Test 2: An `:ok` return does not call `JobRecord.record/5`.
    - Test 3: The repo is dynamically fetched via `Application.fetch_env!(:oban_powertools, :repo)`.

    Inside `__powertools_perform__` in `lib/oban_powertools/worker.ex`, right after `result = process(job)`:
    Check if `result` matches `{:ok, payload}`. If so, fetch the settings map via `__MODULE__.__powertools_output_recording__()`.
    If `settings.record_output` is true, call `ObanPowertools.JobRecord.record(Application.fetch_env!(:oban_powertools, :repo), __MODULE__, job, payload, settings)`.
    Then allow the normal `Hooks.after_result` to fire. The original `result` MUST be returned to Oban intact regardless of whether recording happened or failed.
  </action>
  <verify>
    <automated>mix test test/oban_powertools/worker_test.exs</automated>
  </verify>
  <done>
    - `lib/oban_powertools/worker.ex` calls `ObanPowertools.JobRecord.record` inside `__powertools_perform__`.
    - `test/oban_powertools/worker_test.exs` exits 0, confirming successful payloads are recorded when `record_output: true` is configured.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Worker -> Database | Data flows automatically from `process/1` to `JobRecord`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-55-03 | Tampering | Wrapper injection | mitigate | Ensure recording failure (network, format) doesn't bubble up. The wrapper ignores return from `JobRecord.record/5`. |
</threat_model>

<verification>
- `mix test test/oban_powertools/worker_test.exs` runs without issue.
</verification>

<success_criteria>
- Worker macro cleanly accepts and strips the new options.
- Wrapping correctly intercepts `{:ok, payload}`, pushes to the JobRecord service without blocking execution, and hands off to hooks.
</success_criteria>

<output>
Create `.planning/phases/55-output-recording-jobrecord/55-02-SUMMARY.md` when done
</output>
