---
phase: 55
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/mix/tasks/oban_powertools.install.ex
  - test/mix/tasks/oban_powertools.install_test.exs
  - test/support/migrations/6_phase_55_tables.exs
  - lib/oban_powertools/job_record.ex
  - test/oban_powertools/job_record_test.exs
autonomous: true
requirements:
  - REC-02
  - REC-03
must_haves:
  truths:
    - "D-01: ObanPowertools.JobRecord is a dedicated schema/table independent of Workflow.Result."
    - "D-02: oban_powertools_job_records uses oban_job_id as a soft reference and does not add a hard FK to oban_jobs."
    - "D-03: oban_powertools_job_records includes oban_job_id, worker, attempt, status, payload, payload_bytes, retention, redacted, summary, recorded_at, expires_at, and insert timestamp fields."
    - "D-04: JobRecord enforces one row per oban_job_id/attempt and preserves successful job results on conflicts."
    - "D-05: Migration indexes support oban_job_id lookup plus worker/status/expires_at pruning and filtering."
    - "D-15: JobRecord.payload stores normalized JSONB-compatible data, not opaque Elixir terms."
    - "D-16: Payload normalization recursively stringifies map keys, recurses lists/maps, and preserves JSON scalars."
    - "D-17: payload_bytes is measured from compact Jason-encoded normalized payload bytes."
    - "D-18: output_limit defaults to 65_536 bytes and accepts positive integer byte caps."
    - "D-19: non-encodable or oversized payloads are rejected with warnings; no truncation or opaque fallback rows."
    - "D-20: fetch_result/1 returns the stored JSON-compatible payload shape."
    - "D-22: output_retention accepts only standard, extended, or ephemeral and persists those policy names as strings."
    - "D-23: retention TTLs are fixed at 6 hours ephemeral, 7 days standard, and 30 days extended."
    - "D-24: recorded_at and expires_at are UTC microsecond timestamps computed at record time, with expires_at non-null."
    - "D-38: JobRecord tests cover lookup, normalization, byte counts, rejection paths, uniqueness, retention, and expiry."
    - "D-41: Installer and test migrations define oban_powertools_job_records consistently with existing generated migration style."
  artifacts:
    - path: "test/support/migrations/6_phase_55_tables.exs"
      provides: "Test database table for job records"
    - path: "lib/oban_powertools/job_record.ex"
      provides: "Schema and Ecto integration"
  key_links:
    - from: "lib/oban_powertools/job_record.ex"
      to: "oban_powertools_job_records"
      via: "Ecto.Schema"
      pattern: "schema \"oban_powertools_job_records\""
---

<objective>
Build the `ObanPowertools.JobRecord` Ecto schema, related migrations, and internal insert/lookup functions. This lays the durable storage foundation for worker output recording, strictly independent of workflow results and decoupled from `oban_jobs`.

Purpose: Enable recording of `{:ok, payload}` without risking Oban failure on insert, adhering to size limits, and normalizing data to JSON-safe structures.
Output: Schema module, Ecto migrations, and tests for CRUD/limits.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/55-output-recording-jobrecord/55-CONTEXT.md
@.planning/phases/55-output-recording-jobrecord/55-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Generate `oban_powertools_job_records` migrations</name>
  <files>lib/mix/tasks/oban_powertools.install.ex, test/mix/tasks/oban_powertools.install_test.exs, test/support/migrations/6_phase_55_tables.exs</files>
  <read_first>
    - lib/mix/tasks/oban_powertools.install.ex
    - test/support/migrations/5_phase_6_tables.exs
  </read_first>
  <action>
    Reference `lib/oban_powertools/workflow/result.ex` as an analog for schema definition (e.g., `@primary_key {:id, :binary_id, autogenerate: true}` and `schema "oban_powertools_workflow_results" do`).

    TDD Behavior to implement:
    - Test 1: `mix oban_powertools.install` (or internal logic) generates the new table string correctly.
    - Test 2: Test migration runs and creates the table successfully in the local test DB.

    Add the `oban_powertools_job_records` table definition inside `lib/mix/tasks/oban_powertools.install.ex` (inside the `Igniter.Libs.Ecto.gen_migration` body). It must include:
    `id` (uuid, primary key), `oban_job_id` (integer), `worker` (string), `attempt` (integer, default 1), `status` (string, default "ok"), `payload` (map, default %{}), `payload_bytes` (integer, default 0), `retention` (string, default "standard"), `redacted` (boolean, default false), `summary` (string), `recorded_at` (utc_datetime_usec), `expires_at` (utc_datetime_usec), and `timestamps(updated_at: false)`.
    Also add `create unique_index(:oban_powertools_job_records, [:oban_job_id, :attempt])` (this composite index satisfies the `oban_job_id` lookup requirement from D-05) and indexes for `worker`, `status`, and `expires_at`.
    Create a new test migration `test/support/migrations/6_phase_55_tables.exs` with the exact same table definition so local tests can use it.
  </action>
  <verify>
    <automated>mix test test/mix/tasks/oban_powertools.install_test.exs && mix ecto.migrate -r ObanPowertools.TestRepo</automated>
  </verify>
  <done>
    - `lib/mix/tasks/oban_powertools.install.ex` contains `create table(:oban_powertools_job_records, primary_key: false) do`
    - `test/support/migrations/6_phase_55_tables.exs` exists and implements `up/0` with the table creation.
    - `mix ecto.migrate -r ObanPowertools.TestRepo` runs without errors.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build `ObanPowertools.JobRecord` schema and APIs</name>
  <files>lib/oban_powertools/job_record.ex, test/oban_powertools/job_record_test.exs</files>
  <read_first>
    - lib/oban_powertools/workflow/result.ex
  </read_first>
  <action>
    Reference `lib/oban_powertools/workflow/result.ex` as an analog for schema definition and CRUD handling.

    TDD Behavior to implement:
    - Test 1: `record/5` successfully normalizes and inserts JSON-compatible payloads.
    - Test 2: `record/5` rejects payloads over the provided limit (e.g. 64 KiB) by returning the original result and logging a warning.
    - Test 3: `record/5` computes `expires_at` correctly based on retention ("standard" -> 7 days, "extended" -> 30 days, "ephemeral" -> 6 hours).
    - Test 4: `fetch_result/1` retrieves the most recently recorded row for a given `oban_job_id`.

    Create `ObanPowertools.JobRecord` schema. Add a changeset that enforces required fields and `unique_constraint([:oban_job_id, :attempt])`.
    Implement `record(repo, worker_name, %Oban.Job{} = job, payload, opts)` where `opts` contains limit and retention policy. 
    It MUST:
    1. Normalize payload recursively (stringify map keys, retain scalars). 
    2. Encode to JSON with `Jason.encode!/1` to compute `payload_bytes`. If JSON encoding fails or size exceeds limit, log a warning and return `:ok` (do not fail the worker).
    3. Determine TTL based on retention string.
    4. Attempt `repo.insert(changeset)`. On `unique_constraint` error or DB error, log warning and return `:ok`. Do NOT raise.
    Implement `fetch_result(repo, oban_job_id_or_struct)` to get the latest record (order by `recorded_at DESC, id DESC`).
  </action>
  <verify>
    <automated>mix test test/oban_powertools/job_record_test.exs</automated>
  </verify>
  <done>
    - `lib/oban_powertools/job_record.ex` contains `def record(` and `def fetch_result(`
    - `test/oban_powertools/job_record_test.exs` exits 0.
    - `record/5` does not raise when `Jason.encode!` fails, instead logging and returning safely.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Worker -> Database | Job output payloads represent untrusted data from execution crossing into durable host storage. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-55-01 | Denial of Service | JobRecord payload storage | mitigate | `record/5` computes Jason byte size and drops payload if it exceeds `output_limit` to prevent DB/TOAST bloat. |
| T-55-02 | Denial of Service | JSON decoding to atoms | mitigate | Payload keys are recursively stringified before `Jason.encode!` to prevent runtime atom exhaustion upon later decoding. |
</threat_model>

<verification>
- Install tasks compile cleanly.
- `mix test test/oban_powertools/job_record_test.exs` passes with all tests green.
</verification>

<success_criteria>
- Test database has the `oban_powertools_job_records` table.
- Schema is properly formed.
- `JobRecord.record/5` is completely safe (never raises, respects limits, handles unique conflicts).
- `JobRecord.fetch_result/1` provides correct lookup.
</success_criteria>

<output>
Create `.planning/phases/55-output-recording-jobrecord/55-01-SUMMARY.md` when done
</output>
