---
phase: 55
plan: 04
type: execute
wave: 2
depends_on: ["55-01"]
files_modified:
  - lib/oban_powertools/lifeline.ex
  - test/oban_powertools/lifeline_test.exs
  - guides/workers-and-idempotency.md
  - guides/lifeline-and-repairs.md
autonomous: true
requirements:
  - REC-05
must_haves:
  truths:
    - "Lifeline archive_prune cycle deletes expired JobRecords"
    - "Deleted JobRecord count is added to ArchiveRun.pruned_count"
    - "Documentation clearly states JobRecords are operational data, not business history"
  artifacts:
    - path: "lib/oban_powertools/lifeline.ex"
      provides: "Pruning logic inside existing transaction"
  key_links:
    - from: "lib/oban_powertools/lifeline.ex"
      to: "oban_powertools_job_records"
      via: "delete_all query"
      pattern: "from(record in ObanPowertools.JobRecord"
---

<objective>
Tie the `JobRecord` lifecycle to the global `Lifeline` pruning cycle and document the support-truth boundaries. JobRecords exist to help operators understand recent work, not to serve as an immutable data warehouse.

Purpose: Prevent the `oban_powertools_job_records` table from bloating over time by honoring the `output_retention` schedule.
Output: Modified `Lifeline.run_archive_prune/3` and updated MD guides.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/55-output-recording-jobrecord/55-CONTEXT.md
@.planning/phases/55-output-recording-jobrecord/55-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Integrate `JobRecord` into `Lifeline.run_archive_prune/3`</name>
  <files>lib/oban_powertools/lifeline.ex, test/oban_powertools/lifeline_test.exs</files>
  <read_first>
    - lib/oban_powertools/lifeline.ex
    - test/oban_powertools/lifeline_test.exs
  </read_first>
  <behavior>
    - Test 1: `run_archive_prune/3` issues a `delete_all` query against `ObanPowertools.JobRecord` where `expires_at <= now`.
    - Test 2: The count of deleted records is added to the total `pruned_count` in the `ArchiveRun` ledger.
    - Test 3: The `:archive_prune_completed` telemetry event includes the pruned JobRecords in its total.
  </behavior>
  <action>
    Inside `lib/oban_powertools/lifeline.ex`'s `run_archive_prune/3` transaction block:
    Perform a `repo.delete_all` for `JobRecord` where `expires_at <= ^now`, limiting by the existing `batch_size` and ordering by `[asc: record.expires_at, asc: record.id]`.
    Ensure the returned `{pruned_job_records, _}` count is added to the sum assigned to `ArchiveRun`'s `pruned_count` and passed out of the transaction to the `:archive_prune_completed` telemetry emit.
  </action>
  <acceptance_criteria>
    - `lib/oban_powertools/lifeline.ex` contains `from(record in ObanPowertools.JobRecord` inside `run_archive_prune/3`.
    - `test/oban_powertools/lifeline_test.exs` exits 0 with new coverage showing expired records are deleted.
  </acceptance_criteria>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Document output recording support-truth</name>
  <files>guides/workers-and-idempotency.md, guides/lifeline-and-repairs.md</files>
  <read_first>
    - guides/workers-and-idempotency.md
    - guides/lifeline-and-repairs.md
  </read_first>
  <behavior>
    - Guides are updated to accurately document how to enable recording and what retention implies.
  </behavior>
  <action>
    Update `guides/workers-and-idempotency.md` to introduce `record_output: true`, `output_limit` (64 KiB default), and `output_retention` (`:standard`, `:extended`, `:ephemeral`). Explain that recording only captures `{:ok, payload}`.
    Update `guides/lifeline-and-repairs.md` to note that JobRecords are pruned by the Lifeline archive process alongside previews and heartbeats, explicitly stating they are operational context, not a substitute for durable domain data.
  </action>
  <acceptance_criteria>
    - Both markdown files contain instructions and explicit "operational context" boundary warnings.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Internal / Cron | Safe internal pruning mechanism prevents DoS via unbounded disk growth. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-55-05 | Denial of Service | DB Pruning | mitigate | The pruning is bounded by `batch_size` and uses an indexed `expires_at` field to ensure table scans are avoided. |
</threat_model>

<verification>
- `mix test test/oban_powertools/lifeline_test.exs` passes.
- Documentation renders correctly with new sections.
</verification>

<success_criteria>
- Unused/expired JobRecords are safely swept away without hanging the database.
- Adopters understand the limits and guarantees of the feature.
</success_criteria>

<output>
Create `.planning/phases/55-output-recording-jobrecord/55-04-SUMMARY.md` when done
</output>