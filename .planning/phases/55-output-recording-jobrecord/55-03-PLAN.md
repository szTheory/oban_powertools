---
phase: 55
plan: 03
type: execute
wave: 2
depends_on: ["55-01"]
files_modified:
  - lib/oban_powertools/runtime_config.ex
  - lib/oban_powertools/web/jobs_live.ex
  - test/oban_powertools/web/live/jobs_live_test.exs
autonomous: true
requirements:
  - REC-04
must_haves:
  truths:
    - "D-29: DisplayPolicy adds a dedicated :job_recorded kind and does not reuse :workflow_result for job output."
    - "D-30: Job recorded output may reuse workflow-result display shape internally while keeping the public kind and context separate."
    - "D-31: JobsLive renders a Recorded Output card after Args/Meta and before Errors."
    - "D-32: Available output displays availability, summary, status, attempt, payload bytes, recorded timestamp, retention, expiry, and policy-rendered payload."
    - "D-33: Missing output renders the neutral copy 'No recorded output found for this job.' without claiming recording was disabled."
    - "D-34: Oversized or non-encodable rejected payloads do not get metadata-only rows; warnings and docs are the source of truth."
    - "D-35: :job_recorded policy nil/string/map/invalid/raising returns are normalized or safely fall back without crashing the page."
    - "D-36: Redaction-related display copy treats any redacted flag as stored metadata, not proof that args were redacted in Phase 55."
    - "D-40: Job detail tests cover available output, missing output, policy variants/fallback, and retention metadata."
  artifacts:
    - path: "lib/oban_powertools/web/jobs_live.ex"
      provides: "Job detail view with Recorded Output rendering"
  key_links:
    - from: "lib/oban_powertools/web/jobs_live.ex"
      to: "lib/oban_powertools/job_record.ex"
      via: "JobRecord.fetch_result/1 in load_job_detail"
      pattern: "JobRecord.fetch_result"
---

<objective>
Expose recorded JobRecord output in the native `/ops/jobs` view. Operators should clearly see successful payloads, payload size, retention rules, and timestamps, respecting the established DisplayPolicy.

Purpose: Provide immediate operational visibility into job results without querying the database manually.
Output: Updated `JobsLive` component and `DisplayPolicy` extension.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/55-output-recording-jobrecord/55-CONTEXT.md
@.planning/phases/55-output-recording-jobrecord/55-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add `:job_recorded` support to DisplayPolicy</name>
  <files>lib/oban_powertools/runtime_config.ex</files>
  <read_first>
    - lib/oban_powertools/runtime_config.ex
  </read_first>
  <action>
    Reference `lib/oban_powertools/runtime_config.ex` as an analog for formatting DisplayPolicy fields (e.g., `render_job_field/3` returning `{:raw_json, Jason.encode!(...)}` and the `rescue _ -> {:fallback, "[redacted]"}` fallback pattern).

    TDD Behavior to implement:
    - Test 1: `DisplayPolicy.render_job_field(:job_recorded, record_input, context)` normalizes to a default map.
    - Test 2: If the host app's display policy crashes, it rescues and returns a safe fallback message.

    Add a new `def job_recorded(record_input, context)` to `DisplayPolicy` behavior. Provide a default implementation returning `nil` (meaning pass-through).
    In `render_job_field/3` of `RuntimeConfig`, add a branch for `:job_recorded`. 
    Ensure the result of `apply_policy(:job_recorded, value, context)` is normalized:
    - `nil` returns default payload mapping
    - a `String.t()` returns the text with default metadata
    - `%{} = map` merges known keys
    - Rescues any errors, yielding `%{available?: true, payload: "Recorded output hidden by display policy fallback."}`.
  </action>
  <verify>
    <automated>mix test test/oban_powertools_test.exs</automated>
  </verify>
  <done>
    - `lib/oban_powertools/runtime_config.ex` supports `:job_recorded` kind in `render_job_field/3`.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Inject "Recorded Output" card in `JobsLive`</name>
  <files>lib/oban_powertools/web/jobs_live.ex, test/oban_powertools/web/live/jobs_live_test.exs</files>
  <read_first>
    - lib/oban_powertools/web/jobs_live.ex
    - test/oban_powertools/web/live/jobs_live_test.exs
  </read_first>
  <action>
    Reference `lib/oban_powertools/web/jobs_live.ex` as an analog for how job details are fetched (e.g., inside `load_job_detail/2`) and how properties are rendered in HEEx templates.

    TDD Behavior to implement:
    - Test 1: When output is present, `JobsLive` displays the JSON payload.
    - Test 2: When missing, `JobsLive` renders "No recorded output found for this job."

    In `lib/oban_powertools/web/jobs_live.ex` inside `load_job_detail/2`:
    Fetch the record via `ObanPowertools.JobRecord.fetch_result(repo(), job.id)`. 
    If `{:ok, record}`, pass `record` to `DisplayPolicy.render_job_field(:job_recorded, record, %{surface: :jobs, field: :recorded, job: job})`. Assign it to socket.
    If `{:error, :not_found}`, assign a `nil` or empty indicator to socket.
    Update the `JobsLive` HEEx template. Between the Meta and Error panels, render a card titled "Recorded Output". It should explicitly display all required D-32 fields: result availability, summary, status, attempt, payload bytes, recorded timestamp, retention policy, expiry timestamp, and the policy-rendered payload. If no record, show the neutral empty state message.
  </action>
  <verify>
    <automated>mix test test/oban_powertools/web/live/jobs_live_test.exs</automated>
  </verify>
  <done>
    - `lib/oban_powertools/web/jobs_live.ex` calls `JobRecord.fetch_result` inside `load_job_detail/2`.
    - `test/oban_powertools/web/live/jobs_live_test.exs` contains assertions for the Recorded Output card text.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DB -> UI | Payload rendering via DisplayPolicy crosses into LiveView display. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-55-04 | Information Disclosure | DisplayPolicy Fallback | mitigate | Safe fallback ensures crashing host policies don't spew raw backend state to the operator screen. |
</threat_model>

<verification>
- `mix test test/oban_powertools/web/live/jobs_live_test.exs` proves the UI loads without crashing for both recorded and non-recorded jobs.
</verification>

<success_criteria>
- Operator `/ops/jobs` detail page securely renders `JobRecord` data, properly insulated by DisplayPolicy.
</success_criteria>

<output>
Create `.planning/phases/55-output-recording-jobrecord/55-03-SUMMARY.md` when done
</output>
