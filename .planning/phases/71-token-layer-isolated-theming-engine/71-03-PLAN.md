---
phase: 71-token-layer-isolated-theming-engine
plan: 03
type: execute
wave: 2
depends_on:
  - 71-02
files_modified:
  - lib/oban_powertools/web/assets.ex
  - lib/oban_powertools/web/router.ex
  - mix.exs
autonomous: true
requirements:
  - TOKEN-03
  - TOKEN-05
must_haves:
  truths:
    - "Compiled CSS and JS are served by Powertools routes under `/ops/jobs`, not by the host asset pipeline."
    - "Asset URLs include the current content md5 and return immutable cache headers."
    - "The Hex package file list includes `priv` so compiled assets ship."
  artifacts:
    - path: "lib/oban_powertools/web/assets.ex"
      provides: "MD5 hash helper and Plug asset serving"
      exports: ["init/1", "call/2", "current_hash/1", "path/1"]
    - path: "lib/oban_powertools/web/router.ex"
      provides: "GET routes for compiled assets under the host `/ops/jobs` scope"
    - path: "mix.exs"
      provides: "Package files list including `priv`"
  key_links:
    - from: "lib/oban_powertools/web/router.ex"
      to: "lib/oban_powertools/web/assets.ex"
      via: "GET /_assets/oban_powertools-:hash.css and .js Plug routes"
      pattern: "_assets/oban_powertools"
    - from: "lib/oban_powertools/web/assets.ex"
      to: "priv/static/oban_powertools"
      via: "@external_resource and File.read!"
      pattern: "priv/static/oban_powertools"
---

<objective>
Add the library-owned asset serving boundary for the compiled token and theme assets.

Purpose: Hosts opt in through the existing Powertools router macro without Tailwind, endpoint, or Plug.Static changes.
Output: `ObanPowertools.Web.Assets`, scoped md5 asset routes, and package inclusion for `priv`.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/71-token-layer-isolated-theming-engine/71-RESEARCH.md
@.planning/phases/71-token-layer-isolated-theming-engine/71-VALIDATION.md
@mix.exs
@lib/oban_powertools/web/router.ex
@priv/static/oban_powertools/oban_powertools.css
@priv/static/oban_powertools/oban_powertools.js
@test/oban_powertools/web/assets_test.exs
@examples/phoenix_host/lib/phoenix_host_web/router.ex
</context>

## Artifacts This Phase Produces

See `71-01-PLAN.md` for the complete phase artifact inventory. This plan creates `ObanPowertools.Web.Assets`, `ObanPowertools.Web.Assets.current_hash/1`, `ObanPowertools.Web.Assets.path/1`, and the `/ops/jobs/_assets/oban_powertools-<md5>.css|js` routes.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create ObanPowertools.Web.Assets md5 Plug</name>
  <files>lib/oban_powertools/web/assets.ex</files>
  <read_first>
    - .planning/phases/71-token-layer-isolated-theming-engine/71-RESEARCH.md
    - .planning/phases/71-token-layer-isolated-theming-engine/71-VALIDATION.md
    - priv/static/oban_powertools/oban_powertools.css
    - priv/static/oban_powertools/oban_powertools.js
    - test/oban_powertools/web/assets_test.exs
  </read_first>
  <behavior>
    - Test: `current_hash(:css)` and `current_hash(:js)` return 32 lowercase hex md5 strings derived from compiled file contents.
    - Test: `path(:css)` and `path(:js)` return route paths under `/ops/jobs/_assets`.
    - Test: valid route responses have correct content type, immutable cache header, and body bytes.
    - Test: mismatched hashes return 404.
  </behavior>
  <action>Create `lib/oban_powertools/web/assets.ex` modeled on the Oban Web asset Plug pattern, but scoped to Powertools. Read `priv/static/oban_powertools/oban_powertools.css` and `.js` at compile time with `@external_resource`. Implement `init/1`, `call/2`, `current_hash/1`, and `path/1`. `call/2` must serve only `:css` and `:js`, compare `conn.path_params["hash"]` to `current_hash(asset)`, return 404 for mismatch or unknown asset, return content types `text/css; charset=utf-8` and `text/javascript; charset=utf-8`, set `cache-control` to `public, max-age=31536000, immutable`, mark CSRF skip private for the static response, send the body, and halt. Use md5 only as a cache-busting route fingerprint; do not describe it as a security integrity check.</action>
  <acceptance_criteria>
    - `ObanPowertools.Web.Assets.current_hash(:css)` equals the md5 of the compiled CSS bytes.
    - `ObanPowertools.Web.Assets.current_hash(:js)` equals the md5 of the compiled JS bytes.
    - `ObanPowertools.Web.Assets.path(:css)` returns `/ops/jobs/_assets/oban_powertools-<hash>.css`.
    - `ObanPowertools.Web.Assets.path(:js)` returns `/ops/jobs/_assets/oban_powertools-<hash>.js`.
    - Unknown assets and mismatched hashes produce 404 and halt.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/oban_powertools/web/assets_test.exs</automated>
  </verify>
  <done>`ObanPowertools.Web.Assets` satisfies TOKEN-03 asset helper and Plug behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Route assets inside the Powertools scope and ship priv in package files</name>
  <files>lib/oban_powertools/web/router.ex, mix.exs</files>
  <read_first>
    - lib/oban_powertools/web/router.ex
    - mix.exs
    - test/oban_powertools/web/assets_test.exs
    - examples/phoenix_host/lib/phoenix_host_web/router.ex
    - .planning/phases/71-token-layer-isolated-theming-engine/71-RESEARCH.md
  </read_first>
  <behavior>
    - Test: GET `/ops/jobs/_assets/oban_powertools-<hash>.css` reaches `ObanPowertools.Web.Assets`.
    - Test: GET `/ops/jobs/_assets/oban_powertools-<hash>.js` reaches `ObanPowertools.Web.Assets`.
    - Test: package files include `priv`.
  </behavior>
  <action>Update `lib/oban_powertools/web/router.ex` inside `oban_powertools_routes/1` so the quoted router imports `Phoenix.Router.get/3` and mounts two asset routes before the native `live_session`: `/_assets/oban_powertools-:hash.css` to `ObanPowertools.Web.Assets` with init arg `:css`, and `/_assets/oban_powertools-:hash.js` with init arg `:js`. These routes must be emitted by the existing macro inside the host-owned `/ops/jobs` scope and must not require host endpoint or Tailwind changes. Update `mix.exs` package `files:` to include `priv` while preserving the existing guide/docs entries and dependency list.</action>
  <acceptance_criteria>
    - The asset routes are available under `/ops/jobs/_assets/...` in `ObanPowertools.TestRouter`.
    - The route declarations do not move or rename existing native routes or the `/ops/jobs/oban` bridge.
    - `mix.exs` package files include `priv` exactly once.
    - No host example config file is modified for asset serving.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/oban_powertools/web/assets_test.exs</automated>
    <automated>mix compile --warnings-as-errors</automated>
  </verify>
  <done>TOKEN-03 is satisfied by library-owned asset routes and Hex package inclusion.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Browser -> asset route | Public HTTP request asks for static CSS/JS by hash. |
| Package tarball -> adopter app | Published package must include compiled static assets. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-71-08 | Spoofing | md5 route path | mitigate | Compare requested hash to current content hash and return 404 on mismatch. |
| T-71-09 | Information Disclosure | asset serving | accept | CSS/JS are public static assets with no secrets. |
| T-71-10 | Tampering | package file list | mitigate | Test `mix.exs` package metadata includes `priv` so compiled assets ship. |
| T-71-SC | Tampering | npm/pip/cargo installs | accept | No package installs are planned; Package Legitimacy Audit says none. |
</threat_model>

<verification>
- `mix test test/oban_powertools/web/assets_test.exs`
- `mix compile --warnings-as-errors`
- `mix hex.build --unpack` if available in the local toolchain, then inspect the unpacked package includes `priv/static/oban_powertools/oban_powertools.css` and `.js`
</verification>

<success_criteria>
Compiled CSS/JS assets are served under md5 immutable Powertools routes and are included in the package file list without host asset pipeline changes.
</success_criteria>

<output>
Create `.planning/phases/71-token-layer-isolated-theming-engine/71-03-SUMMARY.md` when done.
</output>
