---
phase: 27-control-plane-vocabulary-status-taxonomy-ownership-contract
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/oban_powertools/control_plane.ex
  - test/oban_powertools/control_plane_test.exs
  - lib/oban_powertools/audit.ex
  - lib/mix/tasks/oban_powertools.install.ex
  - test/support/migrations/0_create_tables.exs
  - test/oban_powertools/cron_test.exs
  - test/oban_powertools/lifeline_test.exs
autonomous: true
requirements:
  - CTL-01
  - CTL-03
must_haves:
  truths:
    - "One shared control-plane contract defines the approved operator statuses, ownership labels, and venue semantics before any page flow rewrite begins."
    - "Durable audit truth distinguishes operator command vocabulary from canonical event vocabulary and structured resource identity."
    - "The bridge remains explicit and bounded; contract work does not widen scope into a native generic job or queue UI."
  artifacts:
    - path: "lib/oban_powertools/control_plane.ex"
      provides: "Pure machine-facing taxonomy for operator_status, ownership, venue, and surface-specific mapping helpers"
    - path: "lib/oban_powertools/audit.ex"
      provides: "Additive audit event contract with event_type and structured resource identity helpers"
    - path: "test/oban_powertools/control_plane_test.exs"
      provides: "Pure contract proof for the shared status/ownership taxonomy"
  key_links:
    - from: "lib/oban_powertools/control_plane.ex"
      to: ".planning/phases/27-control-plane-vocabulary-status-taxonomy-ownership-contract/27-CONTEXT.md"
      via: "The module must encode exactly `needs_review`, `blocked`, `waiting`, `runnable`, `resolved`, and `bridge_only`, plus explicit native/bridge ownership meanings"
      pattern: "needs_review|blocked|waiting|runnable|resolved|bridge_only|Powertools-native|Oban Web bridge"
    - from: "lib/oban_powertools/audit.ex"
      to: ".planning/phases/27-control-plane-vocabulary-status-taxonomy-ownership-contract/27-CONTEXT.md"
      via: "Audit storage must separate command_key, event_type, and structured resource identity while preserving compatibility"
      pattern: "command_key|event_type|resource_type|resource_id|actor_id"
---

<objective>
Freeze the shared machine-facing control-plane contract and the additive audit event contract that later page rewires will consume.

Purpose: establish one durable vocabulary for status, ownership, venue, and event identity before Phase 27 touches page rendering or docs.
Output: a pure control-plane module, additive audit schema/reader changes, and unit/integration proof for the new contract.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/27-control-plane-vocabulary-status-taxonomy-ownership-contract/27-CONTEXT.md
@.planning/phases/27-control-plane-vocabulary-status-taxonomy-ownership-contract/27-RESEARCH.md
@.planning/phases/27-control-plane-vocabulary-status-taxonomy-ownership-contract/27-PATTERNS.md
@.planning/phases/27-control-plane-vocabulary-status-taxonomy-ownership-contract/27-VALIDATION.md
@lib/oban_powertools/explain.ex
@lib/oban_powertools/audit.ex
@lib/mix/tasks/oban_powertools.install.ex
@test/support/migrations/0_create_tables.exs
@test/oban_powertools/explain_test.exs
@test/oban_powertools/cron_test.exs
@test/oban_powertools/lifeline_test.exs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the pure shared control-plane contract module and prove the canonical taxonomy</name>
  <files>lib/oban_powertools/control_plane.ex, test/oban_powertools/control_plane_test.exs</files>
  <read_first>lib/oban_powertools/explain.ex, lib/oban_powertools/web/limiters_live.ex, lib/oban_powertools/web/workflows_live.ex, lib/oban_powertools/web/lifeline_live.ex, .planning/phases/27-control-plane-vocabulary-status-taxonomy-ownership-contract/27-CONTEXT.md, .planning/phases/27-control-plane-vocabulary-status-taxonomy-ownership-contract/27-RESEARCH.md</read_first>
  <action>Create `lib/oban_powertools/control_plane.ex` as the pure machine-facing Phase 27 contract. Define the exact operator statuses `:needs_review`, `:blocked`, `:waiting`, `:runnable`, `:resolved`, and `:bridge_only`; the exact ownership labels `:powertools_native`, `:oban_web_bridge`, and `:host_owned`; and venue-aware helpers that preserve raw state beneath the shared layer. Implement concrete mapping helpers for at least these cases: limiter cooldown -> `:waiting`, limiter saturation -> `:blocked`, healthy limiter -> `:runnable`; paused cron entry -> shared waiting/diagnosis semantics instead of a raw primary label; workflow diagnosis/rejection stories -> shared operator status plus diagnosis; active Lifeline incidents -> `:needs_review`, resolved incidents -> `:resolved`; bridge handoffs and generic job inspection -> `:bridge_only`. Expose functions such as `statuses/0`, `ownership_badge/1`, `venue_label/1`, `limiter_status/1`, `cron_status/1`, `workflow_status/1`, `lifeline_status/1`, and `audit_status/1` with stable return values. Add `test/oban_powertools/control_plane_test.exs` to assert the exact status list, the ownership labels, and the approved mappings above. Do not put HEEx-specific rendering or CSS classes in this module.</action>
  <acceptance_criteria>
    - `lib/oban_powertools/control_plane.ex` contains the exact status atoms `:needs_review`, `:blocked`, `:waiting`, `:runnable`, `:resolved`, and `:bridge_only`.
    - The module exposes concrete mapping helpers for limiter, cron, workflow, Lifeline, and audit/bridge contexts.
    - The module contains explicit ownership labels for Powertools-native and Oban Web bridge semantics.
    - `mix test test/oban_powertools/control_plane_test.exs` exits 0.
    - The module does not contain HEEx markup or Tailwind class strings.
  </acceptance_criteria>
  <verify>
    <automated>bash -lc 'rg -n ":needs_review|:blocked|:waiting|:runnable|:resolved|:bridge_only|powertools_native|oban_web_bridge" lib/oban_powertools/control_plane.ex && ! rg -n "~H|text-|rounded|bg-" lib/oban_powertools/control_plane.ex && mix test test/oban_powertools/control_plane_test.exs'</automated>
  </verify>
  <done>The repo has one pure status and ownership contract that future phases can depend on without reopening vocabulary choices.</done>
</task>

<task type="auto">
  <name>Task 2: Normalize the durable audit contract additively toward event_type and structured resource identity</name>
  <files>lib/oban_powertools/audit.ex, lib/mix/tasks/oban_powertools.install.ex, test/support/migrations/0_create_tables.exs, test/oban_powertools/cron_test.exs, test/oban_powertools/lifeline_test.exs</files>
  <read_first>lib/oban_powertools/audit.ex, lib/mix/tasks/oban_powertools.install.ex, test/support/migrations/0_create_tables.exs, lib/oban_powertools/web/audit_live.ex, .planning/phases/27-control-plane-vocabulary-status-taxonomy-ownership-contract/27-CONTEXT.md, .planning/phases/27-control-plane-vocabulary-status-taxonomy-ownership-contract/27-RESEARCH.md</read_first>
  <action>Update the audit contract additively instead of destructively. In `lib/oban_powertools/audit.ex`, keep compatibility for current callers but add explicit durable fields/helpers for `command_key`, `event_type`, `resource_type`, and `resource_id`, plus helper functions that return typed resource/principal metadata without relying only on the legacy overloaded `resource` string. Update the installer migration source in `lib/mix/tasks/oban_powertools.install.ex` and the test bootstrap migration in `test/support/migrations/0_create_tables.exs` so the audit table includes the new columns and indexes for `event_type` and structured resource identity. Keep existing audit use cases in cron and Lifeline working by updating their tests to assert the new durable fields alongside compatibility for the existing event rows. Do not rename the bridge into a native mutation surface, and do not remove existing `action`/`resource` compatibility in this phase.</action>
  <acceptance_criteria>
    - `lib/oban_powertools/audit.ex` contains the exact identifiers `command_key`, `event_type`, `resource_type`, and `resource_id`.
    - `lib/mix/tasks/oban_powertools.install.ex` and `test/support/migrations/0_create_tables.exs` both include the new audit columns.
    - The installer migration source includes an index for `event_type` and structured resource identity.
    - `mix test test/oban_powertools/cron_test.exs test/oban_powertools/lifeline_test.exs` exits 0.
    - Existing tests still prove durable native mutation auditing instead of falling back to presenter-only strings.
  </acceptance_criteria>
  <verify>
    <automated>bash -lc 'rg -n "command_key|event_type|resource_type|resource_id" lib/oban_powertools/audit.ex lib/mix/tasks/oban_powertools.install.ex test/support/migrations/0_create_tables.exs && mix test test/oban_powertools/cron_test.exs test/oban_powertools/lifeline_test.exs'</automated>
  </verify>
  <done>The audit layer separates canonical event truth from operator copy and gives later UI work structured data to render coherently.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| raw engine/domain state -> shared operator layer | The shared layer must simplify vocabulary without destroying access to underlying truth. |
| operator command copy -> durable audit event truth | UI vocabulary may evolve, but durable event semantics must remain stable and queryable. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-01 | T | shared status taxonomy | mitigate | Freeze the exact approved status atoms in one pure module and prove them with unit tests. |
| T-27-02 | I | ownership/venue semantics | mitigate | Encode Powertools-native versus Oban Web bridge labels centrally and keep bridge-only distinct from failure status. |
| T-27-03 | T | audit contract evolution | mitigate | Add `event_type`, `command_key`, and structured resource identity additively, not by overwriting current fields. |
| T-27-04 | R | historical audit compatibility | mitigate | Preserve current `action` and `resource` compatibility while tests assert the new durable fields. |
</threat_model>

<verification>
- `bash -lc 'rg -n ":needs_review|:blocked|:waiting|:runnable|:resolved|:bridge_only|powertools_native|oban_web_bridge" lib/oban_powertools/control_plane.ex && ! rg -n "~H|text-|rounded|bg-" lib/oban_powertools/control_plane.ex && mix test test/oban_powertools/control_plane_test.exs'`
- `bash -lc 'rg -n "command_key|event_type|resource_type|resource_id" lib/oban_powertools/audit.ex lib/mix/tasks/oban_powertools.install.ex test/support/migrations/0_create_tables.exs && mix test test/oban_powertools/cron_test.exs test/oban_powertools/lifeline_test.exs'`
</verification>

<success_criteria>
- One pure contract module owns the approved status, ownership, and venue vocabulary.
- Audit storage exposes additive structured identity and durable event semantics.
- Phase 28 can rely on the new contract without re-deciding vocabulary.
</success_criteria>

<output>
After completion, create `.planning/phases/27-control-plane-vocabulary-status-taxonomy-ownership-contract/27-01-SUMMARY.md`
</output>

