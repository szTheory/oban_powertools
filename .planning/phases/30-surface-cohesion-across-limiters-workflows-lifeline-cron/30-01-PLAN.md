---
phase: 30-surface-cohesion-across-limiters-workflows-lifeline-cron
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/oban_powertools/web/control_plane_presenter.ex
  - lib/oban_powertools/web/limiters_live.ex
  - test/oban_powertools/web/live/limiters_live_test.exs
  - test/oban_powertools/web/live/control_plane_copy_coherence_test.exs
autonomous: true
requirements:
  - OVR-03
  - ACT-02
must_haves:
  truths:
    - "Limiters stay a Powertools-native diagnosis surface, not a hidden native mutation venue."
    - "The selected limiter detail opens in the shared order `status badge -> diagnosis sentence -> next action -> venue -> evidence`."
    - "The canonical limiter continuity contract remains `resource=` only; rendered diagnosis, preview, and refusal prose stay off the URL."
  artifacts:
    - path: "lib/oban_powertools/web/control_plane_presenter.ex"
      provides: "Shared opening-story helpers for diagnosis, next action, venue, and evidence framing"
    - path: "lib/oban_powertools/web/limiters_live.ex"
      provides: "Limiter table/detail surface rewritten around review-first diagnosis and venue-honest bridge follow-up"
    - path: "test/oban_powertools/web/live/limiters_live_test.exs"
      provides: "Limiter-specific proof for diagnosis-first ordering, review CTA wording, and remount-safe `resource=` continuity"
    - path: "test/oban_powertools/web/live/control_plane_copy_coherence_test.exs"
      provides: "Cross-surface copy-order proof that includes the limiter opening story"
  key_links:
    - from: "lib/oban_powertools/web/limiters_live.ex"
      to: ".planning/phases/30-surface-cohesion-across-limiters-workflows-lifeline-cron/30-CONTEXT.md"
      via: "D-12 through D-18 freeze limiters as diagnosis-only and require review/open wording instead of action-shaped copy"
      pattern: "resource=|Powertools-native|Oban Web bridge|Inspection only|Live Now|Snapshot at Block Start"
    - from: "lib/oban_powertools/web/control_plane_presenter.ex"
      to: ".planning/phases/30-surface-cohesion-across-limiters-workflows-lifeline-cron/30-UI-SPEC.md"
      via: "The opening-story block must be status badge first, then one diagnosis sentence, next action, venue, and evidence"
      pattern: "status_label|ownership_posture|venue_label|audit_follow_up_path"
---

<objective>
Reframe limiters around shared "needs review / blocked / runnable" operator language and explicit handoffs.

Purpose: make the limiter page feel like part of the same control plane as overview, workflow, Lifeline, and cron without widening it into a native action venue.
Output: one shared opening-story seam for limiter diagnosis plus a limiter UI/test pass that proves review-first wording and remount-safe continuity.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/30-surface-cohesion-across-limiters-workflows-lifeline-cron/30-CONTEXT.md
@.planning/phases/30-surface-cohesion-across-limiters-workflows-lifeline-cron/30-RESEARCH.md
@.planning/phases/30-surface-cohesion-across-limiters-workflows-lifeline-cron/30-VALIDATION.md
@.planning/phases/30-surface-cohesion-across-limiters-workflows-lifeline-cron/30-UI-SPEC.md
@lib/oban_powertools/web/control_plane_presenter.ex
@lib/oban_powertools/web/limiters_live.ex
@test/oban_powertools/web/live/limiters_live_test.exs
@test/oban_powertools/web/live/control_plane_copy_coherence_test.exs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract the shared limiter opening-story contract in the presenter seam</name>
  <files>lib/oban_powertools/web/control_plane_presenter.ex, lib/oban_powertools/web/limiters_live.ex, test/oban_powertools/web/live/control_plane_copy_coherence_test.exs</files>
  <read_first>.planning/phases/30-surface-cohesion-across-limiters-workflows-lifeline-cron/30-CONTEXT.md, .planning/phases/30-surface-cohesion-across-limiters-workflows-lifeline-cron/30-RESEARCH.md, lib/oban_powertools/web/control_plane_presenter.ex, lib/oban_powertools/web/limiters_live.ex</read_first>
  <action>Extend `ControlPlanePresenter` with a shared opening-story helper for limiter detail that derives the first diagnosis sentence, the legal next step, the venue label, and the evidence posture from durable limiter state instead of page-local prose. Keep `ControlPlane.limiter_status/1` as the machine-facing status source, but stop treating `Live Now` and `Snapshot at Block Start` as the opening story. The helper must preserve explicit bridge honesty when the next useful drilldown is generic job inspection and must keep the durable continuity selector limited to `resource=`.</action>
  <acceptance_criteria>
    - Limiter detail copy is generated from a shared presenter seam rather than hand-authored in HEEx.
    - The opening-story contract yields one diagnosis-first stack that still preserves `Live Now` and `Snapshot at Block Start` as evidence.
    - Bridge follow-up language stays explicit about `Inspection only` and does not imply audited native mutation.
    - `mix test test/oban_powertools/web/live/control_plane_copy_coherence_test.exs --seed 0` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>bash -lc 'mix test test/oban_powertools/web/live/control_plane_copy_coherence_test.exs --seed 0'</automated>
    <automated>bash -lc 'rg -n "resource=|Inspection only|Powertools-native|Live Now|Snapshot at Block Start" lib/oban_powertools/web/control_plane_presenter.ex lib/oban_powertools/web/limiters_live.ex'</automated>
  </verify>
  <done>The shared presenter owns the limiter opening story before the page wording and CTA labels are changed.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite the limiter page around review-first diagnosis and continuity-safe detail proof</name>
  <files>lib/oban_powertools/web/limiters_live.ex, test/oban_powertools/web/live/limiters_live_test.exs, test/oban_powertools/web/live/control_plane_copy_coherence_test.exs</files>
  <read_first>.planning/phases/30-surface-cohesion-across-limiters-workflows-lifeline-cron/30-UI-SPEC.md, lib/oban_powertools/web/limiters_live.ex, test/oban_powertools/web/live/limiters_live_test.exs</read_first>
  <action>Rework the limiter table and selected-detail pane so the list CTA reads as review/open diagnosis instead of `Action`, and the detail pane renders the shared order `status badge -> diagnosis sentence -> next action -> venue -> evidence`. Keep the page read-only, keep job drilldown as an explicit Oban Web bridge handoff, and preserve `resource=` remount continuity without serializing preview, reason, or diagnosis text into params. Update limiter and coherence tests to assert the new CTA wording, the diagnosis-first selected pane, bridge honesty, and remount-safe detail restoration.</action>
  <acceptance_criteria>
    - No limiter table copy implies native execution, preview, or audit-backed mutation.
    - The first full sentence on selected limiter detail answers what is happening and why before evidence blocks appear.
    - Tests cover remount with `resource=` and confirm that rendered story text is not stored in the URL.
    - `mix test test/oban_powertools/web/live/limiters_live_test.exs test/oban_powertools/web/live/control_plane_copy_coherence_test.exs --seed 0` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>bash -lc 'mix test test/oban_powertools/web/live/limiters_live_test.exs test/oban_powertools/web/live/control_plane_copy_coherence_test.exs --seed 0'</automated>
    <automated>bash -lc '! rg -n "preview_token=.*\\?|reason=.*\\?|diagnosis=.*\\?" lib/oban_powertools/web/limiters_live.ex test/oban_powertools/web/live/limiters_live_test.exs'</automated>
  </verify>
  <done>The limiter surface now matches the control-plane mental model without changing its read-only scope or continuity contract.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| limiter durable state -> operator-facing diagnosis copy | Shared opening-story wording must simplify limiter facts without inventing executable mutation posture. |
| native limiter detail -> bridge inspection handoff | Review links may hand off identity and venue, but they must not imply Powertools-native parity with Oban Web inspection. |
| URL selector -> rendered story | `resource=` may restore the selected slice, but diagnosis, preview, and refusal prose must always be recomputed from durable truth. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-01 | T / I | limiter URL continuity | mitigate | Preserve `resource=` as the only durable selector and recompute all diagnosis/venue text in `handle_params/3`. |
| T-30-02 | S / I | limiter CTA and venue wording | mitigate | Rename action-shaped table language to review/open diagnosis and keep explicit `Inspection only` bridge copy. |
| T-30-03 | T | limiter opening-story copy | mitigate | Centralize diagnosis/next-action/venue wording in `ControlPlanePresenter` and prove order in LiveView/coherence tests. |
</threat_model>

<verification>
- `bash -lc 'mix test test/oban_powertools/web/live/control_plane_copy_coherence_test.exs --seed 0'`
- `bash -lc 'mix test test/oban_powertools/web/live/limiters_live_test.exs test/oban_powertools/web/live/control_plane_copy_coherence_test.exs --seed 0'`
- `bash -lc '! rg -n "preview_token=.*\\?|reason=.*\\?|diagnosis=.*\\?" lib/oban_powertools/web/limiters_live.ex test/oban_powertools/web/live/limiters_live_test.exs'`
</verification>

<success_criteria>
- Limiters read like one native diagnosis surface inside the shared control plane rather than a special-case action page.
- The opening-story contract is presenter-owned and testable.
- Remount-safe continuity remains URL-backed and durable-truth-driven.
</success_criteria>

<output>
After completion, create `.planning/phases/30-surface-cohesion-across-limiters-workflows-lifeline-cron/30-01-SUMMARY.md`
</output>
