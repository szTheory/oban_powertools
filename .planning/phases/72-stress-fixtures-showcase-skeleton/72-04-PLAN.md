---
phase: 72-stress-fixtures-showcase-skeleton
plan: 04
type: execute
wave: 3
depends_on:
  - 72-02
  - 72-03
files_modified:
  - lib/oban_powertools/web/dev/showcase_live.ex
  - lib/oban_powertools/web/router.ex
  - assets/oban_powertools/tokens.css
  - priv/static/oban_powertools/oban_powertools.css
autonomous: true
requirements:
  - FIX-02
  - "SHOW-01 (skeleton)"
  - SHOW-02
  - SHOW-03
must_haves:
  truths:
    - "D-05 D-07: Showcase renders catalog-backed story cells inside the existing native ThemeShell without duplicating fixture data."
    - "D-06 D-14: `/ops/jobs/_showcase` is available in dev/test and route-info absent in prod."
    - "D-08 D-09 D-10: The route exposes full future skeleton sections and open-state naming conventions while staying operator-quality and token-driven."
    - "D-11 D-12 D-13: Theme and viewport controls expose stable state and never depend on host layout, host CSS, or host-owned theme state."
    - "D-14: Example-host dev compile/route proof works from the existing router macro with no host router/layout changes; package proof keeps runtime assets and excludes fixtures."
  artifacts:
    - path: "lib/oban_powertools/web/dev/showcase_live.ex"
      provides: "Dev-only showcase LiveView shell"
      exports: ["ObanPowertools.Web.Dev.ShowcaseLive", "mount/3", "handle_event/3", "render/1"]
    - path: "lib/oban_powertools/web/router.ex"
      provides: "Guarded `/ops/jobs/_showcase` route inside `:oban_powertools_native`"
      exports: ["oban_powertools_routes/1"]
    - path: "assets/oban_powertools/tokens.css"
      provides: "Token-backed showcase shell classes"
      contains: ".obpt-showcase"
    - path: "priv/static/oban_powertools/oban_powertools.css"
      provides: "Compiled token/showcase CSS asset"
  key_links:
    - from: "lib/oban_powertools/web/dev/showcase_live.ex"
      to: "test/support/showcase_catalog.ex"
      via: "optional runtime catalog module/file lookup with no fixture duplication"
      pattern: "ShowcaseCatalog"
    - from: "lib/oban_powertools/web/router.ex"
      to: "lib/oban_powertools/web/theme_shell.ex"
      via: "`:oban_powertools_native` live_session layout"
      pattern: "ThemeShell"
    - from: "lib/oban_powertools/web/dev/showcase_live.ex"
      to: "assets/oban_powertools/theme.js"
      via: "`data-obpt-theme-choice` delegated click contract"
      pattern: "data-obpt-theme-choice"
    - from: "assets/oban_powertools/tokens.css"
      to: "priv/static/oban_powertools/oban_powertools.css"
      via: "`mix oban_powertools.assets.build` deterministic source copy"
      pattern: "obpt-showcase"
---

<objective>
Implement the dev-only `/ops/jobs/_showcase` shell and turn the Phase 72 route/control/example/package contracts green.

Purpose: Provide the deterministic, ThemeShell-hosted surface that Phase 73 VRT/a11y and later component/page phases will target.
Output: `ObanPowertools.Web.Dev.ShowcaseLive`, a guarded router entry, token-backed showcase CSS, compiled CSS asset update, and final proof commands.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/72-stress-fixtures-showcase-skeleton/72-CONTEXT.md
@.planning/phases/72-stress-fixtures-showcase-skeleton/72-RESEARCH.md
@.planning/phases/72-stress-fixtures-showcase-skeleton/72-VALIDATION.md
@.planning/phases/71-token-layer-isolated-theming-engine/71-02-SUMMARY.md
@.planning/phases/71-token-layer-isolated-theming-engine/71-03-SUMMARY.md
@.planning/phases/71-token-layer-isolated-theming-engine/71-04-SUMMARY.md
@.planning/phases/71-token-layer-isolated-theming-engine/71-05-SUMMARY.md
@guides/brand-book.md
@lib/oban_powertools/web/router.ex
@lib/oban_powertools/web/theme_shell.ex
@lib/oban_powertools/web/assets.ex
@lib/oban_powertools/web/dev/brand_book_live.ex
@assets/oban_powertools/theme.js
@assets/oban_powertools/tokens.css
@test/support/showcase_catalog.ex
@test/oban_powertools/web/live/showcase_live_test.exs
@test/oban_powertools/web/router_test.exs
@test/oban_powertools/hex_release_test.exs
@examples/phoenix_host/lib/phoenix_host_web/router.ex
@examples/phoenix_host/test/phoenix_host_web/oban_powertools_theme_isolation_test.exs
</context>

## Artifacts This Phase Produces

- Modules: `ObanPowertools.ShowcaseCatalog`, `ObanPowertools.Web.Dev.ShowcaseLive`.
- Public catalog functions: `domains/0`, `required_state_tags/0`, `required_personas/0`, `scenarios/0`, `scenarios_by_domain/0`, `scenario!/1`, `snapshot_name/1`, `a11y_target/1`.
- Public route: `/ops/jobs/_showcase`, LiveView action `:index`, mounted inside `:oban_powertools_native`.
- Stable route module: `ObanPowertools.Web.Dev.ShowcaseLive` with `mount/3`, `handle_event/3`, and `render/1`.
- Stable attributes: `data-obpt-showcase`, `data-obpt-section`, `data-obpt-theme-choice`, `data-obpt-viewport`, `data-obpt-story`, `data-obpt-domain`, `data-obpt-persona`, `data-obpt-state`, `data-obpt-fixture-index`.
- Theme choices: `system`, `light`, `dark`, `high-contrast`.
- Viewport choices: `320`, `tablet`, `wide`.
- Catalog domains: `overview`, `jobs`, `batches`, `workflows`, `cron`, `limiters`, `lifeline`, `audit`, `forensics`.
- Persona/JTBD categories: `triage`, `incident_response`, `repair`, `audit_review`.
- Required state tags: `empty`, `one`, `many`, `long_id`, `long_module`, `long_url`, `non_ascii`, `emoji`, `rtl`, `high_count`, `mixed_severity`, `permission_denied`, `stale`, `disconnected`, `boundary_pagination`.
- Initial scenario IDs: `overview-operational-empty`, `jobs-long-identifiers-many`, `batches-high-count-mixed-severity`, `workflows-stale-disconnected-dag`, `cron-permission-denied`, `limiters-boundary-pagination`, `lifeline-repair-preview`, `audit-non-ascii-rtl`, `forensics-long-url-stacktrace`.
- Test modules: `ObanPowertools.ShowcaseCatalogTest`, `ObanPowertools.Web.Dev.ShowcaseLiveTest`, extended `ObanPowertools.Web.RouterTest`, extended `ObanPowertools.HexReleaseTest`, extended `PhoenixHostWeb.ObanPowertoolsThemeIsolationTest`.
- CSS selectors/classes added for showcase shell inspection: `.obpt-showcase`, `.obpt-showcase-controls`, `.obpt-showcase-canvas`, `.obpt-showcase-story`, `.obpt-showcase-section`, `.obpt-showcase-fixture-index`.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement dependency-safe ShowcaseLive skeleton</name>
  <files>lib/oban_powertools/web/dev/showcase_live.ex</files>
  <read_first>
@lib/oban_powertools/web/dev/brand_book_live.ex
@lib/oban_powertools/web/theme_shell.ex
@test/support/showcase_catalog.ex
@test/oban_powertools/web/live/showcase_live_test.exs
@.planning/phases/72-stress-fixtures-showcase-skeleton/72-RESEARCH.md
  </read_first>
  <behavior>
    - `mount/3` assigns `:page_title`, available viewport choices, selected viewport default `wide`, theme choices, showcase section list, and catalog scenarios.
    - `handle_event/3` accepts only viewport values `320`, `tablet`, and `wide`; invalid values keep the previous selected viewport.
    - `render/1` emits stable section anchors, theme controls, viewport controls, catalog-backed story cells, and fixture index rows using HEEx escaping.
    - The LiveView contains no direct compile-time dependency on `test/support/showcase_catalog.ex`; it loads `ObanPowertools.ShowcaseCatalog` when available and can require the canonical support file by path when running from the repo/path dependency.
  </behavior>
  <action>Create `ObanPowertools.Web.Dev.ShowcaseLive` as a read-only LiveView in the dev namespace. Keep it dependency-safe for path dependency compilation: the module must not require test/support at compile time and must not depend on ExDoc/EarmarkParser. Load catalog data from `ObanPowertools.ShowcaseCatalog` if loaded, otherwise require the canonical `test/support/showcase_catalog.ex` file when it exists, then call the same catalog API; if neither is available, render a bounded empty catalog state with the same stable section/control anchors. This closes the research-identified `_brand_book` route/module mismatch without duplicating fixture data. Render only tokens/theming controls, fixture index/examples, and stable empty states for future sections; do not implement primitive/form/data/group/page story libraries or Playwright/axe harnesses.</action>
  <verify>
    <automated>mix test test/oban_powertools/web/live/showcase_live_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - The LiveView render contains `data-obpt-showcase`, all required `data-obpt-section` values, and catalog story cells derived from `ObanPowertools.ShowcaseCatalog.scenarios/0`.
    - Theme controls are plain controls with `data-obpt-theme-choice` values `system`, `light`, `dark`, and `high-contrast`.
    - Viewport controls expose stable selected state through `data-obpt-viewport` and do not require browser globals or host JavaScript beyond Phase 71 theme JS.
    - Fixture strings are rendered via HEEx interpolation; no raw fixture HTML is used.
  </acceptance_criteria>
  <done>ShowcaseLive passes its render/control/story selector tests and does not duplicate catalog data.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Mount guarded route inside native Powertools session</name>
  <files>lib/oban_powertools/web/router.ex</files>
  <read_first>
@lib/oban_powertools/web/router.ex
@lib/oban_powertools/web/theme_shell.ex
@test/oban_powertools/web/router_test.exs
@.planning/phases/72-stress-fixtures-showcase-skeleton/72-CONTEXT.md
@.planning/phases/72-stress-fixtures-showcase-skeleton/72-RESEARCH.md
  </read_first>
  <behavior>
    - `/ops/jobs/_showcase` mounts as `live("/_showcase", ObanPowertools.Web.Dev.ShowcaseLive, :index)` inside the existing `:oban_powertools_native` live session.
    - The route is gated by `Application.compile_env(:oban_powertools, :dev_routes, Mix.env() == :dev)` consistently with the dev-route pattern.
    - The route receives `ThemeShell`, md5 CSS/JS asset tags, `.obpt-root`, and `LiveAuth` through the existing live session.
  </behavior>
  <action>Add the `_showcase` route next to `_brand_book` inside the native live session and under the existing dev-route guard. Preserve all existing native routes, bridge routes, asset route, session metadata, and `ThemeShell` layout wiring. Do not edit host routers, host layouts, or host Tailwind configuration.</action>
  <verify>
    <automated>mix test test/oban_powertools/web/router_test.exs test/oban_powertools/web/live/showcase_live_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `Phoenix.Router.route_info(TestRouter, "GET", "/ops/jobs/_showcase", "localhost")` returns `ObanPowertools.Web.Dev.ShowcaseLive` action `:index` in test.
    - `live(conn, "/ops/jobs/_showcase")` renders through exactly one `.obpt-root`.
    - Existing `/ops/jobs/_brand_book`, native page, asset, and optional bridge route tests remain green.
  </acceptance_criteria>
  <done>Showcase route is wired inside `:oban_powertools_native` and inherits the Phase 71 shell.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add token-backed showcase CSS and run final gates</name>
  <files>assets/oban_powertools/tokens.css, priv/static/oban_powertools/oban_powertools.css</files>
  <read_first>
@assets/oban_powertools/tokens.css
@priv/static/oban_powertools/oban_powertools.css
@lib/mix/tasks/oban_powertools.assets.build.ex
@test/oban_powertools/web/theme_tokens_test.exs
@test/oban_powertools/web/assets_test.exs
@test/oban_powertools/hex_release_test.exs
@examples/phoenix_host/test/phoenix_host_web/oban_powertools_theme_isolation_test.exs
  </read_first>
  <behavior>
    - Source CSS defines `.obpt-showcase`, `.obpt-showcase-controls`, `.obpt-showcase-canvas`, `.obpt-showcase-story`, `.obpt-showcase-section`, and `.obpt-showcase-fixture-index` under `.obpt-root`.
    - Showcase CSS uses existing `--obpt-*` tokens for color, spacing, type, border, radius, focus, and motion values.
    - `mix oban_powertools.assets.build` updates the compiled CSS asset deterministically.
    - Final verification proves targeted tests, full suite, prod route absence, Hex package proof, and example-host dev route/module proof.
  </behavior>
  <action>Add only the showcase shell styles needed for controls, section layout, fixture index, story cells, and viewport canvas states. Keep selectors scoped under `.obpt-root` and use the existing token variables. Set canvas states for `320`, `tablet`, and `wide` with stable widths while preserving responsive containment. Run the existing deterministic asset build task after source CSS changes so `priv/static/oban_powertools/oban_powertools.css` matches the source. Do not add JavaScript files or npm dependencies.</action>
  <verify>
    <automated>mix oban_powertools.assets.build</automated>
    <automated>mix test test/oban_powertools/showcase_catalog_test.exs test/oban_powertools/web/live/showcase_live_test.exs test/oban_powertools/web/router_test.exs test/oban_powertools/web/theme_tokens_test.exs test/oban_powertools/web/assets_test.exs</automated>
    <automated>OBAN_POWERTOOLS_SKIP_DB_BOOT=1 mix test test/oban_powertools/hex_release_test.exs</automated>
    <automated>MIX_ENV=prod mix compile --warnings-as-errors</automated>
    <automated>cd examples/phoenix_host && MIX_ENV=dev mix compile --warnings-as-errors</automated>
    <automated>cd examples/phoenix_host && MIX_ENV=dev mix run --no-start -e 'IO.inspect({Code.ensure_loaded?(ObanPowertools.Web.Dev.ShowcaseLive), Phoenix.Router.route_info(PhoenixHostWeb.Router, "GET", "/ops/jobs/_showcase", "localhost")})'</automated>
    <automated>cd examples/phoenix_host && MIX_ENV=prod mix run --no-start -e 'IO.inspect(Phoenix.Router.route_info(PhoenixHostWeb.Router, "GET", "/ops/jobs/_showcase", "localhost"))'</automated>
    <automated>rm -rf /tmp/obpt_phase72_pkg && OBAN_POWERTOOLS_SKIP_DB_BOOT=1 mix hex.build --unpack -o /tmp/obpt_phase72_pkg && test -n "$(find /tmp/obpt_phase72_pkg -path '*/priv/static/oban_powertools/oban_powertools.css' -print -quit)" && test -n "$(find /tmp/obpt_phase72_pkg -path '*/priv/static/oban_powertools/oban_powertools.js' -print -quit)" && test ! -e /tmp/obpt_phase72_pkg/test/support/showcase_catalog.ex && ! find /tmp/obpt_phase72_pkg -type f | rg '(^|/)showcase_catalog\.ex$|^\.planning/|^test/'</automated>
    <automated>mix test --exclude host_contract</automated>
  </verify>
  <acceptance_criteria>
    - Source and compiled CSS both contain `.obpt-showcase` selectors after the build.
    - Targeted tests for catalog, showcase LiveView, router, theme tokens, assets, and Hex package pass.
    - `MIX_ENV=prod` route-info proof for `/ops/jobs/_showcase` prints `:error`.
    - Example-host dev proof prints `true` for `Code.ensure_loaded?(ObanPowertools.Web.Dev.ShowcaseLive)` and a LiveView route-info map for `/ops/jobs/_showcase`.
    - Hex unpack proof finds `priv/static/oban_powertools` CSS/JS and does not find the catalog, `test`, or `.planning`.
    - `mix test --exclude host_contract` passes before the plan is summarized.
  </acceptance_criteria>
  <done>Showcase shell is implemented, assets rebuilt, and all final Phase 72 proof commands pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| LiveView render -> browser DOM | Showcase controls and fixture strings become HTML. |
| Powertools router macro -> host app | Library routes are mounted inside the host-owned `/ops/jobs` scope. |
| Dev/test route -> production build | `_showcase` must be route-info absent in production. |
| Test support -> Hex package | Catalog data must remain out of adopter packages while runtime assets remain included. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-72-12 | Information Disclosure | `/ops/jobs/_showcase` production route | mitigate | Keep route under compile-env guard and prove prod route-info returns `:error`. |
| T-72-13 | Tampering | Theme controls | mitigate | Use Phase 71 delegated `data-obpt-theme-choice` values and no host root layout/config changes. |
| T-72-14 | Tampering | Viewport controls | mitigate | Allowlist `320`, `tablet`, `wide` in `handle_event/3` and render selected state through `data-obpt-viewport`. |
| T-72-15 | Tampering / XSS | Fixture rendering | mitigate | Render all fixture values via HEEx interpolation and avoid raw fixture HTML. |
| T-72-16 | Information Disclosure | Hex package | mitigate | Hex unpack proof requires runtime assets and rejects catalog/test/planning files. |
| T-72-17 | Denial of Service | Missing catalog in path/package dev contexts | mitigate | Optional catalog lookup renders bounded empty catalog state rather than crashing when the canonical file is unavailable. |
| T-72-SC | Tampering | npm/pip/cargo installs | accept | Phase 72 installs no packages; RESEARCH.md Package Legitimacy Audit lists `none`. |
</threat_model>

<verification>
Targeted:
- `mix test test/oban_powertools/showcase_catalog_test.exs test/oban_powertools/web/live/showcase_live_test.exs test/oban_powertools/web/router_test.exs`
- `mix test test/oban_powertools/web/theme_tokens_test.exs test/oban_powertools/web/assets_test.exs`
- `OBAN_POWERTOOLS_SKIP_DB_BOOT=1 mix test test/oban_powertools/hex_release_test.exs`

Final phase gates:
- `MIX_ENV=prod mix compile --warnings-as-errors`
- `cd examples/phoenix_host && MIX_ENV=dev mix compile --warnings-as-errors`
- `cd examples/phoenix_host && MIX_ENV=dev mix run --no-start -e 'IO.inspect({Code.ensure_loaded?(ObanPowertools.Web.Dev.ShowcaseLive), Phoenix.Router.route_info(PhoenixHostWeb.Router, "GET", "/ops/jobs/_showcase", "localhost")})'`
- `cd examples/phoenix_host && MIX_ENV=prod mix run --no-start -e 'IO.inspect(Phoenix.Router.route_info(PhoenixHostWeb.Router, "GET", "/ops/jobs/_showcase", "localhost"))'`
- `rm -rf /tmp/obpt_phase72_pkg && OBAN_POWERTOOLS_SKIP_DB_BOOT=1 mix hex.build --unpack -o /tmp/obpt_phase72_pkg && test -n "$(find /tmp/obpt_phase72_pkg -path '*/priv/static/oban_powertools/oban_powertools.css' -print -quit)" && test -n "$(find /tmp/obpt_phase72_pkg -path '*/priv/static/oban_powertools/oban_powertools.js' -print -quit)" && test ! -e /tmp/obpt_phase72_pkg/test/support/showcase_catalog.ex && ! find /tmp/obpt_phase72_pkg -type f | rg '(^|/)showcase_catalog\.ex$|^\.planning/|^test/'`
- `cd examples/phoenix_host && mix test test/phoenix_host_web/oban_powertools_theme_isolation_test.exs test/phoenix_host_web/controllers/page_controller_test.exs`
- `mix test --exclude host_contract`
</verification>

<success_criteria>
The showcase route renders from `/ops/jobs/_showcase` in dev/test through the existing ThemeShell, exposes stable controls/selectors/story cells, proves prod route absence and package exclusion, runs from the example host without host router/layout changes, and leaves Playwright, axe CI, full story libraries, and page migration to their later phases.
</success_criteria>

<output>
Create `.planning/phases/72-stress-fixtures-showcase-skeleton/72-04-SUMMARY.md` when done.
</output>
