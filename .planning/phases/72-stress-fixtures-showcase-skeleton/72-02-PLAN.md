---
phase: 72-stress-fixtures-showcase-skeleton
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - test/oban_powertools/web/live/showcase_live_test.exs
  - test/oban_powertools/web/router_test.exs
  - examples/phoenix_host/test/phoenix_host_web/oban_powertools_theme_isolation_test.exs
autonomous: true
requirements:
  - "SHOW-01 (skeleton)"
  - SHOW-02
  - SHOW-03
must_haves:
  truths:
    - "D-06 D-07: `/ops/jobs/_showcase` renders only through the native Powertools LiveView session and ThemeShell in dev/test."
    - "D-08 D-09 D-10: The shell exposes stable future sections and open-state naming without building later-phase component stories."
    - "D-11 D-12 D-13 D-14: Theme, viewport, story selectors, example-host, and prod-route absence are explicit contracts."
  artifacts:
    - path: "test/oban_powertools/web/live/showcase_live_test.exs"
      provides: "RED LiveView render/control/story selector contract"
      exports: ["ObanPowertools.Web.Dev.ShowcaseLiveTest"]
    - path: "test/oban_powertools/web/router_test.exs"
      provides: "Route-info contract for `/ops/jobs/_showcase` in dev/test"
      exports: ["ObanPowertools.Web.RouterTest"]
    - path: "examples/phoenix_host/test/phoenix_host_web/oban_powertools_theme_isolation_test.exs"
      provides: "Example host root isolation assertions and dev proof target context"
      exports: ["PhoenixHostWeb.ObanPowertoolsThemeIsolationTest"]
  key_links:
    - from: "test/oban_powertools/web/live/showcase_live_test.exs"
      to: "lib/oban_powertools/web/dev/showcase_live.ex"
      via: "live(conn, \"/ops/jobs/_showcase\")"
      pattern: "data-obpt-showcase"
    - from: "test/oban_powertools/web/router_test.exs"
      to: "lib/oban_powertools/web/router.ex"
      via: "Phoenix.Router.route_info/4"
      pattern: "/ops/jobs/_showcase"
    - from: "examples/phoenix_host/test/phoenix_host_web/oban_powertools_theme_isolation_test.exs"
      to: "examples/phoenix_host/lib/phoenix_host_web/router.ex"
      via: "existing oban_powertools_routes(\"/oban\") mount; no host router/layout edits"
      pattern: "/ops/jobs"
---

<objective>
Create the validation-first RED contracts for the dev-only showcase route, controls, stable selectors, and example-host proof.

Purpose: Lock SHOW-01, SHOW-02, and SHOW-03 before implementation, including the research-identified route/module availability risk in the example host.
Output: Failing LiveView, router, and example-host isolation contracts that Plan 72-04 must turn green.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/72-stress-fixtures-showcase-skeleton/72-CONTEXT.md
@.planning/phases/72-stress-fixtures-showcase-skeleton/72-RESEARCH.md
@.planning/phases/72-stress-fixtures-showcase-skeleton/72-VALIDATION.md
@.planning/phases/71-token-layer-isolated-theming-engine/71-04-SUMMARY.md
@.planning/phases/71-token-layer-isolated-theming-engine/71-05-SUMMARY.md
@lib/oban_powertools/web/router.ex
@lib/oban_powertools/web/theme_shell.ex
@lib/oban_powertools/web/assets.ex
@assets/oban_powertools/theme.js
@test/oban_powertools/web/router_test.exs
@test/oban_powertools/web/live/brand_book_live_test.exs
@examples/phoenix_host/lib/phoenix_host_web/router.ex
@examples/phoenix_host/test/phoenix_host_web/oban_powertools_theme_isolation_test.exs
</context>

## Artifacts This Phase Produces

- Modules: `ObanPowertools.ShowcaseCatalog`, `ObanPowertools.Web.Dev.ShowcaseLive`.
- Public catalog functions: `domains/0`, `required_state_tags/0`, `required_personas/0`, `scenarios/0`, `scenarios_by_domain/0`, `scenario!/1`, `snapshot_name/1`, `a11y_target/1`.
- Public route: `/ops/jobs/_showcase`, LiveView action `:index`, mounted inside `:oban_powertools_native`.
- Stable route module: `ObanPowertools.Web.Dev.ShowcaseLive` with `mount/3`, `handle_event/3`, and `render/1`.
- Stable attributes: `data-obpt-showcase`, `data-obpt-section`, `data-obpt-theme-choice`, `data-obpt-viewport`, `data-obpt-story`, `data-obpt-domain`, `data-obpt-persona`, `data-obpt-state`, `data-obpt-fixture-index`.
- Theme choices: `system`, `light`, `dark`, `high-contrast`.
- Viewport choices: `320`, `tablet`, `wide`.
- Catalog domains: `overview`, `jobs`, `batches`, `workflows`, `cron`, `limiters`, `lifeline`, `audit`, `forensics`.
- Persona/JTBD categories: `triage`, `incident_response`, `repair`, `audit_review`.
- Required state tags: `empty`, `one`, `many`, `long_id`, `long_module`, `long_url`, `non_ascii`, `emoji`, `rtl`, `high_count`, `mixed_severity`, `permission_denied`, `stale`, `disconnected`, `boundary_pagination`.
- Initial scenario IDs: `overview-operational-empty`, `jobs-long-identifiers-many`, `batches-high-count-mixed-severity`, `workflows-stale-disconnected-dag`, `cron-permission-denied`, `limiters-boundary-pagination`, `lifeline-repair-preview`, `audit-non-ascii-rtl`, `forensics-long-url-stacktrace`.
- Test modules: `ObanPowertools.ShowcaseCatalogTest`, `ObanPowertools.Web.Dev.ShowcaseLiveTest`, extended `ObanPowertools.Web.RouterTest`, extended `ObanPowertools.HexReleaseTest`, extended `PhoenixHostWeb.ObanPowertoolsThemeIsolationTest`.
- CSS selectors/classes added for showcase shell inspection: `.obpt-showcase`, `.obpt-showcase-controls`, `.obpt-showcase-canvas`, `.obpt-showcase-story`, `.obpt-showcase-section`, `.obpt-showcase-fixture-index`.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add RED showcase LiveView render and selector tests</name>
  <files>test/oban_powertools/web/live/showcase_live_test.exs</files>
  <read_first>
@test/oban_powertools/web/live/brand_book_live_test.exs
@test/oban_powertools/web/assets_test.exs
@test/oban_powertools/web/theme_tokens_test.exs
@lib/oban_powertools/web/theme_shell.ex
@assets/oban_powertools/theme.js
@.planning/phases/72-stress-fixtures-showcase-skeleton/72-CONTEXT.md
  </read_first>
  <behavior>
    - Test `live(conn, "/ops/jobs/_showcase")` renders exactly one `.obpt-root`, md5 CSS/JS asset paths, and `data-obpt-showcase`.
    - Test theme controls expose `data-obpt-theme-choice` values `system`, `light`, `dark`, and `high-contrast`.
    - Test viewport controls expose stable `data-obpt-viewport` values `320`, `tablet`, and `wide`.
    - Test story cells expose `data-obpt-story`, `data-obpt-domain`, `data-obpt-persona`, and `data-obpt-state`.
    - Test stable section anchors exist for tokens, primitives, forms, data display, operator groups, pages, and stress fixtures without requiring later-phase story content.
  </behavior>
  <action>Create `ObanPowertools.Web.Dev.ShowcaseLiveTest` guarded consistently with dev/test route availability. Assert the exact theme choices, viewport values, section IDs, and story attributes named in D-02, D-08, D-11, and D-12. Use route/render assertions rather than display-copy selectors. Do not add Playwright, axe, primitive stories, page migration checks, or host-layout assertions here.</action>
  <verify>
    <automated>mix test test/oban_powertools/web/live/showcase_live_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `test/oban_powertools/web/live/showcase_live_test.exs` exists and defines `ObanPowertools.Web.Dev.ShowcaseLiveTest`.
    - The targeted command is RED only because `/ops/jobs/_showcase` or `ObanPowertools.Web.Dev.ShowcaseLive` is not implemented.
    - Assertions use stable `data-obpt-*` attributes and route paths, not visible copy as selectors.
  </acceptance_criteria>
  <done>Showcase render/control tests establish the SHOW-01 skeleton and SHOW-02 control contract before implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend router tests for showcase route-info contract</name>
  <files>test/oban_powertools/web/router_test.exs</files>
  <read_first>
@test/oban_powertools/web/router_test.exs
@test/support/test_router.ex
@lib/oban_powertools/web/router.ex
@.planning/phases/72-stress-fixtures-showcase-skeleton/72-VALIDATION.md
  </read_first>
  <behavior>
    - Test `Phoenix.Router.route_info(TestRouter, "GET", "/ops/jobs/_showcase", "localhost")` returns a LiveView route for `ObanPowertools.Web.Dev.ShowcaseLive` action `:index` when dev routes are enabled in test.
    - Keep existing native route and optional bridge assertions unchanged.
  </behavior>
  <action>Add a `_showcase` route-info assertion next to the existing native route assertions in `ObanPowertools.Web.RouterTest`. The expected route must be `"/ops/jobs/_showcase"` and the expected LiveView tuple must reference `ObanPowertools.Web.Dev.ShowcaseLive` with action `:index`. Do not change `test/support/test_router.ex` unless the existing router macro test setup cannot mount the new route under current test config.</action>
  <verify>
    <automated>mix test test/oban_powertools/web/router_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - Router tests name `/ops/jobs/_showcase` exactly.
    - Existing route assertions for native pages, bridge placement, and bridge auth contract remain present.
    - The targeted command is RED only for missing `_showcase` route/module implementation.
  </acceptance_criteria>
  <done>Router contract covers the dev/test side of SHOW-01 before route implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Extend example-host isolation expectations without host router edits</name>
  <files>examples/phoenix_host/test/phoenix_host_web/oban_powertools_theme_isolation_test.exs</files>
  <read_first>
@examples/phoenix_host/test/phoenix_host_web/oban_powertools_theme_isolation_test.exs
@examples/phoenix_host/lib/phoenix_host_web/router.ex
@.planning/phases/72-stress-fixtures-showcase-skeleton/72-RESEARCH.md
@.planning/phases/72-stress-fixtures-showcase-skeleton/72-VALIDATION.md
  </read_first>
  <behavior>
    - Preserve the host `/` clean-page assertion: no Powertools root, asset path, or theme-storage strings on host pages.
    - Keep the example host using its existing `ObanPowertools.Web.Router.oban_powertools_routes("/oban")` mount; do not edit router or layouts.
    - Prepare the test module for Phase 72 proof by asserting host root remains clean after `_showcase` exists, while the dev-mode route/module proof is performed by the verification command in Plan 72-04.
  </behavior>
  <action>Extend only `PhoenixHostWeb.ObanPowertoolsThemeIsolationTest` with host-root non-leakage assertions relevant to the showcase route and keep the existing jobs-page proof intact. Do not add or require example-host test config changes for `dev_routes`; the research-identified `_brand_book` mismatch is closed by Plan 72-04 through a dev-mode compile/route command and dependency-safe `ShowcaseLive` implementation.</action>
  <verify>
    <automated>cd examples/phoenix_host && mix test test/phoenix_host_web/oban_powertools_theme_isolation_test.exs test/phoenix_host_web/controllers/page_controller_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `examples/phoenix_host/lib/phoenix_host_web/router.ex` and host root layouts are unchanged.
    - Existing example-host tests still prove `/` is free of Powertools theme shell and `/ops/jobs/jobs` owns the scoped shell.
    - The plan records that the `_showcase` route/module proof must be a `MIX_ENV=dev` compile/route command because example-host test env does not enable dev routes.
  </acceptance_criteria>
  <done>Example-host contract remains no-host-change and is ready for Plan 72-04 dev-mode route proof.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Browser -> showcase LiveView | Route renders fixture strings and controls into HTML. |
| Host app -> Powertools router macro | Example host imports the library route tree under a host-owned scope. |
| Dev/test route -> production route table | `_showcase` must not exist when compiled for production. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-72-04 | Information Disclosure | `/ops/jobs/_showcase` route | mitigate | Route-info tests and final prod command prove the route exists only under dev/test gating and prints `:error` in prod. |
| T-72-05 | Tampering | Theme controls | mitigate | Tests assert controls use `data-obpt-theme-choice` values consumed by Phase 71 JS and never require host root layout or host theme state. |
| T-72-06 | Repudiation | Story selectors | mitigate | Tests assert `data-obpt-story/domain/persona/state` so VRT/a11y targets do not depend on display copy. |
| T-72-07 | Tampering | Example host router/layout | mitigate | Example-host plan tasks explicitly forbid router/layout changes and verify existing host root isolation remains green. |
| T-72-SC | Tampering | npm/pip/cargo installs | accept | Phase 72 installs no packages; RESEARCH.md Package Legitimacy Audit lists `none`. |
</threat_model>

<verification>
Targeted:
- `mix test test/oban_powertools/web/live/showcase_live_test.exs`
- `mix test test/oban_powertools/web/router_test.exs`
- `cd examples/phoenix_host && mix test test/phoenix_host_web/oban_powertools_theme_isolation_test.exs test/phoenix_host_web/controllers/page_controller_test.exs`

Phase-level commands this plan contributes to:
- `cd examples/phoenix_host && MIX_ENV=dev mix compile --warnings-as-errors`
- `cd examples/phoenix_host && MIX_ENV=dev mix run --no-start -e 'IO.inspect({Code.ensure_loaded?(ObanPowertools.Web.Dev.ShowcaseLive), Phoenix.Router.route_info(PhoenixHostWeb.Router, "GET", "/ops/jobs/_showcase", "localhost")})'`
- `cd examples/phoenix_host && MIX_ENV=prod mix run --no-start -e 'IO.inspect(Phoenix.Router.route_info(PhoenixHostWeb.Router, "GET", "/ops/jobs/_showcase", "localhost"))'`
</verification>

<success_criteria>
The RED showcase route/control/example contracts exist, are stable-selector based, and fail only for missing Phase 72 implementation work.
</success_criteria>

<output>
Create `.planning/phases/72-stress-fixtures-showcase-skeleton/72-02-SUMMARY.md` when done.
</output>
