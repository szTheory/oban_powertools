---
phase: 72-stress-fixtures-showcase-skeleton
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - test/oban_powertools/showcase_catalog_test.exs
  - test/oban_powertools/hex_release_test.exs
autonomous: true
requirements:
  - FIX-01
  - FIX-02
  - FIX-03
  - SHOW-03
must_haves:
  truths:
    - "D-01 D-02 D-03 D-04 D-05: Catalog contract proves a domain-first, deterministic, constant-only canonical fixture source at test/support/showcase_catalog.ex."
    - "D-03 D-14: Catalog tests cover normal, adversarial, and persona/JTBD scenarios while package tests prove fixture artifacts stay out of Hex."
  artifacts:
    - path: "test/oban_powertools/showcase_catalog_test.exs"
      provides: "RED contract for ObanPowertools.ShowcaseCatalog public fixture API"
      exports: ["ObanPowertools.ShowcaseCatalogTest"]
    - path: "test/oban_powertools/hex_release_test.exs"
      provides: "Package inclusion/exclusion assertions for Phase 71 assets and Phase 72 fixture catalog"
      exports: ["ObanPowertools.HexReleaseTest"]
  key_links:
    - from: "test/oban_powertools/showcase_catalog_test.exs"
      to: "test/support/showcase_catalog.ex"
      via: "tests call ObanPowertools.ShowcaseCatalog.scenarios/0 and related contract functions"
      pattern: "ObanPowertools\\.ShowcaseCatalog"
    - from: "test/oban_powertools/hex_release_test.exs"
      to: "mix.exs"
      via: "package :files contract keeps priv/static assets and excludes test/.planning"
      pattern: "Mix\\.Project\\.config\\(\\)\\[:package\\]"
---

<objective>
Create the validation-first RED contracts for the Phase 72 fixture catalog and package boundary.

Purpose: Lock FIX-01, FIX-02, FIX-03, and the package side of SHOW-03 before implementation so deterministic fixtures and Hex exclusion are not left to interpretation.
Output: Failing catalog contract tests and package assertions that the implementation plans must turn green.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/72-stress-fixtures-showcase-skeleton/72-CONTEXT.md
@.planning/phases/72-stress-fixtures-showcase-skeleton/72-RESEARCH.md
@.planning/phases/72-stress-fixtures-showcase-skeleton/72-VALIDATION.md
@.planning/phases/71-token-layer-isolated-theming-engine/71-03-SUMMARY.md
@test/oban_powertools/hex_release_test.exs
@mix.exs
</context>

## Artifacts This Phase Produces

- Modules: `ObanPowertools.ShowcaseCatalog`, `ObanPowertools.Web.Dev.ShowcaseLive`.
- Public catalog functions: `domains/0`, `required_state_tags/0`, `required_personas/0`, `scenarios/0`, `scenarios_by_domain/0`, `scenario!/1`, `snapshot_name/1`, `a11y_target/1`.
- Public route: `/ops/jobs/_showcase`, LiveView action `:index`, mounted inside `:oban_powertools_native`.
- Stable route module: `ObanPowertools.Web.Dev.ShowcaseLive` with `mount/3`, `handle_event/3`, and `render/1`.
- Stable attributes: `data-obpt-showcase`, `data-obpt-section`, `data-obpt-theme-choice`, `data-obpt-viewport`, `data-obpt-story`, `data-obpt-domain`, `data-obpt-persona`, `data-obpt-state`, `data-obpt-fixture-index`.
- Theme choices: `system`, `light`, `dark`, `high-contrast`.
- Viewport choices: `320`, `tablet`, `wide`.
- Catalog domains: `overview`, `jobs`, `batches`, `workflows`, `cron`, `limiters`, `lifeline`, `audit`, `forensics`.
- Persona/JTBD categories: `triage`, `incident_response`, `repair`, `audit_review`.
- Required state tags: `empty`, `one`, `many`, `long_id`, `long_module`, `long_url`, `non_ascii`, `emoji`, `rtl`, `high_count`, `mixed_severity`, `permission_denied`, `stale`, `disconnected`, `boundary_pagination`.
- Initial scenario IDs: `overview-operational-empty`, `jobs-long-identifiers-many`, `batches-high-count-mixed-severity`, `workflows-stale-disconnected-dag`, `cron-permission-denied`, `limiters-boundary-pagination`, `lifeline-repair-preview`, `audit-non-ascii-rtl`, `forensics-long-url-stacktrace`.
- Test modules: `ObanPowertools.ShowcaseCatalogTest`, `ObanPowertools.Web.Dev.ShowcaseLiveTest`, extended `ObanPowertools.Web.RouterTest`, extended `ObanPowertools.HexReleaseTest`, extended `PhoenixHostWeb.ObanPowertoolsThemeIsolationTest`.
- CSS selectors/classes added for showcase shell inspection: `.obpt-showcase`, `.obpt-showcase-controls`, `.obpt-showcase-canvas`, `.obpt-showcase-story`, `.obpt-showcase-section`, `.obpt-showcase-fixture-index`.

## Source Audit

| Source | ID | Feature/Requirement | Plan | Status |
|---|---|---|---|---|
| GOAL | - | Deterministic fixtures plus dev-only showcase shell for later audit, VRT, and a11y scans | 72-01, 72-02, 72-03, 72-04 | COVERED |
| REQ | FIX-01 | Deterministic named-scenario catalog with normal/adversarial coverage | 72-01, 72-03 | COVERED |
| REQ | FIX-02 | Plain constant fixtures shared by showcase, ExUnit, VRT names, and a11y targets | 72-01, 72-03, 72-04 | COVERED |
| REQ | FIX-03 | Persona/JTBD scenarios and Hex tarball exclusion | 72-01, 72-03, 72-04 | COVERED |
| REQ | SHOW-01 (skeleton) | Dev-only `/ops/jobs/_showcase` skeleton route absent in prod | 72-02, 72-04 | COVERED |
| REQ | SHOW-02 | Theme and viewport switchers with stable targets | 72-02, 72-04 | COVERED |
| REQ | SHOW-03 | Example-host independence and prod/package non-leakage | 72-01, 72-02, 72-04 | COVERED |
| RESEARCH | - | Avoid `_brand_book` route/module mismatch in example host | 72-02, 72-04 | COVERED |
| RESEARCH | - | No new package installs; use ExUnit/Phoenix.LiveViewTest/package proof | 72-01, 72-02, 72-03, 72-04 | COVERED |
| CONTEXT | D-01..D-05 | Domain-first canonical catalog, stable selectors, deterministic constants, Hex exclusion | 72-01, 72-03, 72-04 | COVERED |
| CONTEXT | D-06..D-10 | Dev-only route, native session/ThemeShell, full future skeleton, open-state naming, operator-quality surface | 72-02, 72-04 | COVERED |
| CONTEXT | D-11..D-14 | Scoped theme controls, viewport state, deterministic showcase, production/package proof | 72-01, 72-02, 72-04 | COVERED |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add RED catalog contract tests</name>
  <files>test/oban_powertools/showcase_catalog_test.exs</files>
  <read_first>
@.planning/phases/72-stress-fixtures-showcase-skeleton/72-CONTEXT.md
@.planning/phases/72-stress-fixtures-showcase-skeleton/72-VALIDATION.md
@.planning/REQUIREMENTS.md
@test/support/workflow_fixtures.ex
  </read_first>
  <behavior>
    - Test `ObanPowertools.ShowcaseCatalog.domains/0` returns exactly the nine required domain atoms or strings in stable order.
    - Test `scenarios/0` returns deterministic results on repeated calls, unique slug IDs, and maps/structs containing `:id`, `:domain`, `:name`, `:persona`, `:jtbd`, `:states`, `:fixtures`, and `:test_targets`.
    - Test aggregate states include every required FIX-01 tag and aggregate personas/JTBDs include triage, incident response, repair, and audit review per D-03.
    - Test each scenario exposes stable `snapshot_name/1` and `a11y_target/1` values derived from its ID rather than display copy per D-02.
  </behavior>
  <action>Create `ObanPowertools.ShowcaseCatalogTest` with assertions for the public catalog API that Plan 72-03 will implement. The tests must cite D-01 through D-05 in assertion messages or describe blocks, assert all required domains and state/persona coverage by concrete values, and assert selector/snapshot/a11y target stability from scenario IDs. Keep tests independent of the database and do not weaken assertions to tolerate missing domains or missing state tags.</action>
  <verify>
    <automated>mix test test/oban_powertools/showcase_catalog_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `test/oban_powertools/showcase_catalog_test.exs` exists and defines `ObanPowertools.ShowcaseCatalogTest`.
    - Running the targeted test command is RED only because `ObanPowertools.ShowcaseCatalog` or its required functions are not implemented yet.
    - Source assertions name all domains, all required state tags, all four persona/JTBD categories, and the initial scenario ID contract.
  </acceptance_criteria>
  <done>Catalog tests fail for missing implementation, not syntax, DB setup, or unrelated application boot failures.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend package boundary tests for fixture exclusion</name>
  <files>test/oban_powertools/hex_release_test.exs</files>
  <read_first>
@test/oban_powertools/hex_release_test.exs
@mix.exs
@.planning/phases/71-token-layer-isolated-theming-engine/71-03-SUMMARY.md
@.planning/phases/72-stress-fixtures-showcase-skeleton/72-CONTEXT.md
  </read_first>
  <behavior>
    - Test package configuration still includes `priv` so Phase 71 runtime assets ship.
    - Test package configuration continues to exclude `test` and `.planning`.
    - Test Phase 72 catalog source path `test/support/showcase_catalog.ex` exists for local dev/test use and is not included in package files; the path is named in the failure message.
  </behavior>
  <action>Extend the existing packaging describe block in `ObanPowertools.HexReleaseTest` with Phase 72 assertions that the canonical catalog source exists locally for dev/test and remains excluded from package file declarations, while preserving the existing Phase 71 `priv/static/oban_powertools` asset requirements. Keep the package proof focused on package contents; do not change `mix.exs` in this plan.</action>
  <verify>
    <automated>OBAN_POWERTOOLS_SKIP_DB_BOOT=1 mix test test/oban_powertools/hex_release_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - Existing package tests still require `priv/static/oban_powertools/oban_powertools.css` and `priv/static/oban_powertools/oban_powertools.js`.
    - New package assertions reject `test`, `.planning`, and the canonical fixture catalog path from Hex package configuration.
    - The command is RED before Plan 72-03 because the canonical catalog source does not exist yet, or because an existing package invariant is already broken.
  </acceptance_criteria>
  <done>Package boundary tests cover D-14 and FIX-03 before implementation begins.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Catalog test contract -> fixture module | Tests define public fixture IDs and metadata before the module exists. |
| Package config -> Hex tarball | Internal test/dev artifacts must not cross into adopter packages. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-72-01 | Tampering | `ObanPowertools.ShowcaseCatalog` contract | mitigate | Assert slug IDs, complete domain coverage, required state tags, and deterministic repeated output before implementation. |
| T-72-02 | Repudiation | VRT/a11y target names | mitigate | Assert snapshot and a11y target values derive from stable scenario IDs, not display text. |
| T-72-03 | Information Disclosure | Hex package contents | mitigate | Keep `priv/static/oban_powertools` assets required while asserting `test`, `.planning`, and the fixture catalog path are excluded. |
| T-72-SC | Tampering | npm/pip/cargo installs | accept | Phase 72 installs no packages; RESEARCH.md Package Legitimacy Audit lists `none`. |
</threat_model>

<verification>
Targeted:
- `mix test test/oban_powertools/showcase_catalog_test.exs`
- `OBAN_POWERTOOLS_SKIP_DB_BOOT=1 mix test test/oban_powertools/hex_release_test.exs`

Phase-level commands this plan contributes to:
- `rm -rf /tmp/obpt_phase72_pkg`
- `OBAN_POWERTOOLS_SKIP_DB_BOOT=1 mix hex.build --unpack -o /tmp/obpt_phase72_pkg`
- `test -n "$(find /tmp/obpt_phase72_pkg -path '*/priv/static/oban_powertools/oban_powertools.css' -print -quit)"`
- `test -n "$(find /tmp/obpt_phase72_pkg -path '*/priv/static/oban_powertools/oban_powertools.js' -print -quit)"`
- `test ! -e /tmp/obpt_phase72_pkg/test/support/showcase_catalog.ex`
- `! find /tmp/obpt_phase72_pkg -type f | rg '(^|/)showcase_catalog\.ex$|^\.planning/|^test/'`
</verification>

<success_criteria>
The RED catalog and package contracts exist, are specific to FIX-01 through FIX-03 and SHOW-03, and fail only for missing Phase 72 implementation work.
</success_criteria>

<output>
Create `.planning/phases/72-stress-fixtures-showcase-skeleton/72-01-SUMMARY.md` when done.
</output>
