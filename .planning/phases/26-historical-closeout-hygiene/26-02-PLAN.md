---
phase: 26-historical-closeout-hygiene
plan: 02
type: execute
wave: 2
depends_on:
  - 26-01
files_modified:
  - /Users/jon/.codex/get-shit-done/bin/lib/audit.cjs
  - /Users/jon/.codex/get-shit-done/bin/lib/uat.cjs
autonomous: true
requirements: []
must_haves:
  truths:
    - "Artifact normalization remains the primary Phase 26 fix, but the archival audit layer also tolerates legacy fully-closed aliases narrowly enough that stale closed files do not look open forever."
    - "Compatibility remains explicit and read-only: no open UAT state is silently treated as closed."
    - "The repo-local open-artifact audit returns cleanly once the historical closeout artifact is normalized."
  artifacts:
    - path: "/Users/jon/.codex/get-shit-done/bin/lib/audit.cjs"
      provides: "Narrow legacy-closed UAT handling for milestone-close artifact scans"
    - path: "/Users/jon/.codex/get-shit-done/bin/lib/uat.cjs"
      provides: "Narrow completion-marker compatibility for already-complete UAT sessions"
  key_links:
    - from: "/Users/jon/.codex/get-shit-done/bin/lib/audit.cjs"
      to: ".planning/phases/12-fresh-host-install-path-example-fixture-repair/12-UAT.md"
      via: "Legacy `passed` may be recognized only when no pending, skipped, or blocked scenarios remain"
      pattern: "passed|open_scenario_count|complete"
    - from: "/Users/jon/.codex/get-shit-done/bin/lib/uat.cjs"
      to: ".planning/phases/12-fresh-host-install-path-example-fixture-repair/12-UAT.md"
      via: "Legacy `[completed]` completion marker may be accepted only for already-complete sessions"
      pattern: "testing complete|completed"
---

<objective>
Add narrow archival-tooling hardening so legacy fully-closed UAT artifacts do not keep tripping milestone-close gates after the repo-owned artifact has been normalized.

Purpose: keep compatibility explicit and minimal without replacing the source-fix from Plan 01.
Output: `audit.cjs` and, if still needed, `uat.cjs` patched for legacy-closed-only compatibility and validated against the repo-local audit gate.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/26-historical-closeout-hygiene/26-CONTEXT.md
@.planning/phases/26-historical-closeout-hygiene/26-RESEARCH.md
@.planning/phases/26-historical-closeout-hygiene/26-PATTERNS.md
@.planning/phases/26-historical-closeout-hygiene/26-VALIDATION.md
@.planning/phases/26-historical-closeout-hygiene/26-01-PLAN.md
@.planning/phases/12-fresh-host-install-path-example-fixture-repair/12-UAT.md
@/Users/jon/.codex/get-shit-done/bin/lib/audit.cjs
@/Users/jon/.codex/get-shit-done/bin/lib/uat.cjs
@/Users/jon/.codex/get-shit-done/workflows/complete-milestone.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Harden `audit.cjs` so it treats only legacy fully-closed UAT aliases as closed</name>
  <files>/Users/jon/.codex/get-shit-done/bin/lib/audit.cjs</files>
  <read_first>/Users/jon/.codex/get-shit-done/bin/lib/audit.cjs, .planning/phases/12-fresh-host-install-path-example-fixture-repair/12-UAT.md, .planning/phases/26-historical-closeout-hygiene/26-CONTEXT.md, .planning/phases/26-historical-closeout-hygiene/26-RESEARCH.md</read_first>
  <action>Patch `scanUatGaps()` in `audit.cjs` so the open-artifact audit still treats `status: complete` as canonical closed state, but also accepts the legacy alias `status: passed` only when the file has zero pending scenarios and no skipped or blocked result tokens. Compute those counts from the UAT body before deciding whether the file is open. Keep `testing`, `partial`, `diagnosed`, `unknown`, skipped, blocked, or pending paths open exactly as before. Add a small local variable or comment that makes the legacy-closed rule explicit instead of magical. Do not broaden compatibility beyond the `passed` alias.</action>
  <acceptance_criteria>
    - `audit.cjs` contains explicit logic for legacy `passed` handling.
    - The logic still keys canonical closure on `complete`.
    - The logic requires zero pending scenarios and rejects skipped or blocked scenarios before treating `passed` as closed.
    - `node "$HOME/.codex/get-shit-done/bin/gsd-tools.cjs" audit-open --json` returns `"uat_gaps": 0` and `"has_open_items": false` in this repo state.
  </acceptance_criteria>
  <verify>
    <automated>bash -lc 'rg -n "passed|open_scenario_count|skipped|blocked|complete" /Users/jon/.codex/get-shit-done/bin/lib/audit.cjs && node "$HOME/.codex/get-shit-done/bin/gsd-tools.cjs" audit-open --json | rg -n "\"uat_gaps\": 0|\"has_open_items\": false"'</automated>
  </verify>
  <done>The archival audit is narrowly hardened for legacy closed aliases without weakening open-state detection.</done>
</task>

<task type="auto">
  <name>Task 2: If parser compatibility is still needed, accept a legacy completion marker only for already-complete UAT sessions</name>
  <files>/Users/jon/.codex/get-shit-done/bin/lib/uat.cjs</files>
  <read_first>/Users/jon/.codex/get-shit-done/bin/lib/uat.cjs, .planning/phases/12-fresh-host-install-path-example-fixture-repair/12-UAT.md, .planning/phases/26-historical-closeout-hygiene/26-CONTEXT.md, .planning/phases/26-historical-closeout-hygiene/26-RESEARCH.md</read_first>
  <action>Inspect `parseCurrentTest()` and only add compatibility if it is still needed after Plan 01 normalization. If needed, expand the completion-marker check so `[completed]` is treated the same as `[testing complete]` for already-complete sessions. Keep pending-session parsing strict and unchanged. If Plan 01 normalization makes this unnecessary, make no functional change and leave `uat.cjs` untouched rather than inventing extra compatibility. In either case, document the decision in the plan summary.</action>
  <acceptance_criteria>
    - If `uat.cjs` changes, it contains explicit completion-marker compatibility for legacy `[completed]`.
    - Pending-session parsing is not broadened.
    - If `uat.cjs` does not need to change, the execution summary explicitly says artifact normalization made parser hardening unnecessary.
  </acceptance_criteria>
  <verify>
    <automated>rg -n "testing complete|completed|Current Test" /Users/jon/.codex/get-shit-done/bin/lib/uat.cjs</automated>
  </verify>
  <done>Any parser hardening stays optional, explicit, and strictly limited to already-complete sessions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo-local normalized artifact -> shared archival tooling | Tooling must remain a secondary guardrail, not the only fix. |
| legacy closed alias -> true open states | Compatibility must recognize only clearly closed legacy cases and keep every ambiguous state open. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-03 | T | audit/parser compatibility | mitigate | Limit compatibility to the explicit `passed` alias and completion marker only; keep pending/skipped/blocked paths strict. |
</threat_model>

<verification>
- `bash -lc 'rg -n "passed|open_scenario_count|skipped|blocked|complete" /Users/jon/.codex/get-shit-done/bin/lib/audit.cjs && node "$HOME/.codex/get-shit-done/bin/gsd-tools.cjs" audit-open --json | rg -n "\"uat_gaps\": 0|\"has_open_items\": false"'`
- `rg -n "testing complete|completed|Current Test" /Users/jon/.codex/get-shit-done/bin/lib/uat.cjs`
</verification>

<success_criteria>
- The repo-local audit gate clears after the Phase 12 artifact is normalized.
- Any compatibility added is explicit, narrow, and legacy-closed-only.
- No open UAT state gains a silent path past milestone-close auditing.
</success_criteria>

<output>
After completion, create `.planning/phases/26-historical-closeout-hygiene/26-02-SUMMARY.md`
</output>
