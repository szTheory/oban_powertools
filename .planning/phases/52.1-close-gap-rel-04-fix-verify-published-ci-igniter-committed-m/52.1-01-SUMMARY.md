---
plan: 52.1-01
phase: 52.1
status: complete
completed: 2026-05-30
commits:
  - ac60dd7
  - 0a1f648
key-files:
  created: []
  modified:
    - .github/workflows/release.yml
    - examples/hex_consumer/regenerate.sh
    - .gitignore
deviations: []
---

## Summary

Closed gap REL-04 with four surgical edits across three files so the `verify-published` CI job can complete end-to-end.

## What Was Built

**D-01 — rm step in verify-published job**
Inserted `Delete committed installer-generated files` step immediately after `actions/cache` and before `Install Hex + Rebar` in the `verify-published` job. The step runs two `rm -f` commands (repo-root-relative paths, no `working-directory`) to delete the committed scaffold files before the installer runs. This allows Igniter to regenerate them fresh and produce the 6 required migration files.

**D-03 — MIX_ENV: test on Compile step**
Added `env: MIX_ENV: test` block to the `Compile hex consumer` step, between `name:` and `run:`. Eliminates the recompile triggered when later steps switch environments.

**D-04 — Version-agnostic sed pattern**
Updated the `sed -i` version-pin regex from the hardcoded `~> 0.5` to the POSIX character class `~> [0-9][0-9.]*`, so it matches any `~> X.Y[.Z]` dep string regardless of future version bumps. No `''` argument added — CI runs ubuntu-latest (GNU sed).

**D-01a — regenerate.sh scaffold comment**
Added a 7-line `# ` comment block after the `replace_once` call and before the `( cd "${TARGET_DIR}" )` subshell, documenting that `oban_powertools_auth.ex` and `oban_powertools_display_policy.ex` are committed as local dev scaffolding and will be overwritten by `mix oban_powertools.install` on a fresh install (which is expected and correct).

**D-02 — .gitignore for local-proof artifact**
Added `examples/hex_consumer_local_proof/` to `.gitignore` after the existing `examples/hex_consumer/deps/` entry. The directory itself was neither committed nor deleted.

## Validation

- All Task 1 grep assertions pass (rm step present, MIX_ENV on Compile, no hardcoded `~> 0.5`, version-agnostic pattern present).
- All Task 2 assertions pass (gitignore entry present, `git check-ignore` exits 0, comment contains both file names and "overwritten by").
- Task 3 sed smoke test: ALL PASS across `0.5`, `0.6`, `1.0`, and `1.23.4` — proving assumption A1 (GNU/BSD BRE handles `[0-9][0-9.]*` correctly for this substitution).

## Self-Check: PASSED
