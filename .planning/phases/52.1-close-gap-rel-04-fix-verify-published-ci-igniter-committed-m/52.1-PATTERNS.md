# Phase 52.1: Close gap: REL-04 — fix verify-published CI - Pattern Map

**Mapped:** 2026-05-30
**Files analyzed:** 3
**Analogs found:** 3 / 3

## File Classification

| New/Modified File | Role | Data Flow | Closest Analog | Match Quality |
|-------------------|------|-----------|----------------|---------------|
| `.github/workflows/release.yml` | config (CI workflow) | event-driven | itself (verify-published job, lines 237–293) | exact — editing existing steps in the same job |
| `examples/hex_consumer/regenerate.sh` | utility (shell script) | batch | itself (lines 1–72) | exact — adding a comment block to an existing script |
| `.gitignore` | config | — | itself (lines 29–33) | exact — adding one line to an established comment section |

## Pattern Assignments

### `.github/workflows/release.yml` (config, CI workflow)

**Analog:** itself — `verify-published` job, `.github/workflows/release.yml` lines 237–293

**Existing step structure (lines 237–293) — the full job being modified:**
```yaml
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6
        with:
          ref: ${{ needs.release-please.outputs.tag_name }}

      - uses: erlef/setup-beam@fc68ffb90438ef2936bbb3251622353b3dcb2f93 # v1.24.0
        with:
          elixir-version: "1.19.5"
          otp-version: "27.3"

      - uses: actions/cache@27d5ce7f107fe9357f9df03efb73ab90386fccae # v5
        with:
          path: |
            examples/hex_consumer/deps
            examples/hex_consumer/_build
          key: ${{ runner.os }}-hex-consumer-${{ needs.release-please.outputs.version }}

      - name: Install Hex + Rebar
        run: |
          mix local.hex --force
          mix local.rebar --force

      - name: Pin consumer to exact published version
        run: |
          VERSION="${{ needs.release-please.outputs.version }}"
          sed -i "s|{:oban_powertools, \"~> 0.5\"}|{:oban_powertools, \"== ${VERSION}\"}|" \
            examples/hex_consumer/mix.exs

      - name: Fetch hex consumer deps (from hex.pm)
        run: mix deps.get
        working-directory: examples/hex_consumer

      - name: Run installer from published tarball
        run: mix oban_powertools.install
        working-directory: examples/hex_consumer

      - name: Compile hex consumer
        run: mix compile --warnings-as-errors
        working-directory: examples/hex_consumer

      - name: Create and migrate database
        env:
          MIX_ENV: test
        run: mix ecto.create && mix ecto.migrate
        working-directory: examples/hex_consumer

      - name: Seed nightly_sync cron entry
        env:
          MIX_ENV: test
        run: mix run priv/repo/seeds.exs
        working-directory: examples/hex_consumer

      - name: Run first-session proof (REL-04)
        env:
          MIX_ENV: test
        run: mix test test/hex_consumer_web/oban_powertools_first_session_test.exs --trace
        working-directory: examples/hex_consumer
```

**D-01: New step to insert** — place immediately after the `actions/cache` step (line 247–252) and before the "Install Hex + Rebar" step (line 254). No `working-directory` — paths are repo-root-relative:
```yaml
      - name: Delete committed installer-generated files
        run: |
          rm -f examples/hex_consumer/lib/hex_consumer_web/oban_powertools_auth.ex
          rm -f examples/hex_consumer/lib/hex_consumer_web/oban_powertools_display_policy.ex
```

**D-03: env block pattern** — copied from the "Create and migrate database" step (lines 278–280), which is the established pattern for MIX_ENV in this job. Apply to the "Compile hex consumer" step (lines 273–275):
```yaml
      - name: Compile hex consumer
        env:
          MIX_ENV: test
        run: mix compile --warnings-as-errors
        working-directory: examples/hex_consumer
```

**D-04: sed pattern replacement** — current line 262 (inside "Pin consumer to exact published version"):
```yaml
          # Current (hardcoded version literal — fragile):
          sed -i "s|{:oban_powertools, \"~> 0.5\"}|{:oban_powertools, \"== ${VERSION}\"}|" \
            examples/hex_consumer/mix.exs

          # Fixed (version-agnostic character class):
          sed -i "s|{:oban_powertools, \"~> [0-9][0-9.]*\"}|{:oban_powertools, \"== ${VERSION}\"}|" \
            examples/hex_consumer/mix.exs
```
Only the `sed -i` line changes; the `VERSION=` assignment and the file path argument are unchanged.

---

### `examples/hex_consumer/regenerate.sh` (utility, batch)

**Analog:** itself — `examples/hex_consumer/regenerate.sh` lines 1–72

**Existing comment style** (line 33) — single-line `#` comment before a logical block:
```bash
# Insert hex dep: {:oban_powertools, "~> 0.5"} (native-only target, no bridge dep)
replace_once \
  ...
```

**D-01a: Comment block to insert** — insert after line 37 (after the closing argument of the `replace_once` call) and before line 39 (the `(` that opens the subshell cd block). Use the same `# ` prefix style as all other comments in the file:
```bash
# NOTE: oban_powertools_auth.ex and oban_powertools_display_policy.ex are committed
# in examples/hex_consumer/lib/hex_consumer_web/ as local dev scaffolding so that
# `mix test` and `mix compile` work without running the installer. The CI
# verify-published job deletes these files before running `mix oban_powertools.install`
# so that Igniter generates them fresh from the published tarball (which also triggers
# the 6 required migration files). If you regenerate the scaffold, Igniter will
# overwrite these files — that is expected and correct.
```

---

### `.gitignore` (config)

**Analog:** itself — `.gitignore` lines 29–33

**Existing examples/ section pattern** (lines 29–33) — comment header then bare `examples/` paths, one per line:
```
# examples/hex_consumer generated and lock artifacts (not committed — hex dep, no lock).
examples/hex_consumer/mix.lock
examples/hex_consumer/priv/repo/migrations/
examples/hex_consumer/_build/
examples/hex_consumer/deps/
```

**D-02: New entry to add** — append on line 34, immediately after `examples/hex_consumer/deps/` and before the blank line that opens the "GSD / agent run scratch artifacts" section. No comment needed — fits the existing semantic group:
```
examples/hex_consumer_local_proof/
```

---

## Shared Patterns

### Named step style (CI)
**Source:** `.github/workflows/release.yml` lines 254–293
**Apply to:** the new rm step (D-01)
Every step in the `verify-published` job uses `name:` for readability. All multi-line `run:` blocks use a leading `|` scalar. The new step must follow the same indentation (6-space for `- name:`, 8-space for `run:` and its body).

### env block placement (CI)
**Source:** `.github/workflows/release.yml` lines 278–280, 284–286, 290–292
**Apply to:** the "Compile hex consumer" step (D-03)
When a step needs an env var, the `env:` key sits between `name:` and `run:`, before `working-directory:`. That ordering is consistent across all three MIX_ENV steps in this job.

### Shell comment style (bash)
**Source:** `examples/hex_consumer/regenerate.sh` line 33
**Apply to:** the new comment block in regenerate.sh (D-01a)
All comments use `# ` (hash-space) prefix. No block markers (no `###`, no `=====`). Multi-line comments are consecutive `# ` lines.

---

## No Analog Found

None — all three files exist in the codebase and serve as their own analogs.

---

## Metadata

**Analog search scope:** `.github/workflows/`, `examples/hex_consumer/`, repo root (`.gitignore`)
**Files scanned:** 3
**Pattern extraction date:** 2026-05-30
