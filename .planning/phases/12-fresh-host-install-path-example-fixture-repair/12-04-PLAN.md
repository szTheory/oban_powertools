---
phase: 12
plan: 04
type: execute
wave: 3
depends_on: ["12-01", "12-02", "12-03"]
files_modified: ["README.md", "guides/installation.md", "guides/first-operator-session.md", "guides/example-app-walkthrough.md", "guides/support-truth-and-ownership-boundaries.md", "test/oban_powertools/docs_contract_test.exs", ".github/workflows/host-contract-proof.yml"]
autonomous: true
requirements: ["PKG-01", "DOC-01"]
must_haves:
  truths:
    - "Public docs describe the repaired day-0 path exactly as the code now proves it, including compile, migrate/reset, and boot before the first native operator action."
    - "The first successful operator session is defined as one real native audited mutation, not compile/reset/seed alone."
    - "Docs drift and proof-lane drift are both caught automatically in CI."
  artifacts:
    - path: "README.md"
      provides: "Concise public install contract that matches the repaired paved road"
      contains: "mix oban_powertools.install"
    - path: "guides/first-operator-session.md"
      provides: "Canonical first-session definition and proof steps"
      contains: "pause_cron_entry"
    - path: "test/oban_powertools/docs_contract_test.exs"
      provides: "Docs contract test for repaired install and first-session markers"
      contains: "nightly_sync"
    - path: ".github/workflows/host-contract-proof.yml"
      provides: "CI lanes aligned to the repaired docs and proof stack"
      contains: "fresh-host"
  key_links:
    - from: "README.md"
      to: "guides/installation.md"
      via: "same paved-road install sequence"
      pattern: "mix oban_powertools.install"
    - from: "guides/first-operator-session.md"
      to: "test/oban_powertools/example_host_contract_test.exs"
      via: "same canonical native mutation flow"
      pattern: "pause_cron_entry|nightly_sync|ops-demo"
    - from: "guides/installation.md"
      to: "test/oban_powertools/fresh_host_contract_test.exs"
      via: "same fresh-host compile/migrate/boot proof lane"
      pattern: "mix compile|mix ecto.migrate|mix phx.server"
    - from: "test/oban_powertools/docs_contract_test.exs"
      to: ".github/workflows/host-contract-proof.yml"
      via: "docs drift enforced in CI"
      pattern: "docs-contract"
---

<objective>
Realign the public docs, docs contract tests, and CI workflow so every Phase 12 support claim matches the repaired installer, fixture, and first-session proof exactly.

Purpose: Finish the phase by making `PKG-01` and `DOC-01` self-enforcing at the public-documentation layer instead of relying on stale prose.
Output: Updated README and guides, strengthened docs contract tests, and a host-contract workflow that runs the repaired fresh-host and first-session proof lanes.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/milestones/v1.1-MILESTONE-AUDIT.md
@.planning/phases/12-fresh-host-install-path-example-fixture-repair/12-CONTEXT.md
@.planning/phases/12-fresh-host-install-path-example-fixture-repair/12-RESEARCH.md
@README.md
@guides/installation.md
@guides/first-operator-session.md
@guides/example-app-walkthrough.md
@guides/support-truth-and-ownership-boundaries.md
@examples/phoenix_host/README.md
@test/oban_powertools/docs_contract_test.exs
@test/oban_powertools/fresh_host_contract_test.exs
@test/oban_powertools/example_host_contract_test.exs
@.github/workflows/host-contract-proof.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite the public paved-road docs around the repaired install and first-session contract</name>
  <files>README.md, guides/installation.md, guides/first-operator-session.md, guides/example-app-walkthrough.md, guides/support-truth-and-ownership-boundaries.md</files>
  <read_first>
    - README.md
    - guides/installation.md
    - guides/first-operator-session.md
    - guides/example-app-walkthrough.md
    - guides/support-truth-and-ownership-boundaries.md
    - examples/phoenix_host/README.md
    - test/oban_powertools/example_host_contract_test.exs
    - .planning/phases/12-fresh-host-install-path-example-fixture-repair/12-CONTEXT.md
  </read_first>
  <acceptance_criteria>
    - `README.md` documents `mix phx.new` then `mix oban_powertools.install`
    - `README.md` and `guides/installation.md` mention both host-owned starter seams `ObanPowertoolsAuth` and `ObanPowertoolsDisplayPolicy`
    - `README.md` or `guides/installation.md` explicitly describe compile, migrate or reset, and one boot check before the first operator session
    - `guides/first-operator-session.md` defines success as native action `pause_cron_entry` on `nightly_sync` by `ops-demo`
    - `guides/example-app-walkthrough.md` points to the curated fixture provenance rather than claiming a fully generated demo app
    - `guides/support-truth-and-ownership-boundaries.md` preserves the read-only `/ops/jobs/oban` bridge posture
  </acceptance_criteria>
  <action>
    Rewrite the top-level README and guides so they describe the repaired paved road in one exact sequence: `mix phx.new`, add `oban_powertools`, run `mix oban_powertools.install`, fill in the thin host-owned starter seams `ObanPowertoolsAuth` and `ObanPowertoolsDisplayPolicy`, run migrations and seeds, then complete the canonical first session by pausing cron entry `nightly_sync` as operator `ops-demo`.
    Make the day-0 threshold explicit per D-03 and D-14: the install path is not complete until the generated host compiles, completes the required migrate or reset path, and passes one bounded boot check before the native first-session mutation. Keep those steps aligned to the fresh-host execution lane introduced in Plan 01.
    Make the first-session definition concrete per D-14 and D-15. `guides/first-operator-session.md` must stop treating compile/reset/seed as success and instead name the native audited mutation plus durable audit evidence as the proof threshold. `guides/example-app-walkthrough.md` must match the curated-fixture provenance from Plan 02, and `guides/support-truth-and-ownership-boundaries.md` must restate that `/ops/jobs/oban` remains a bounded read-only inspection surface.
    Keep the docs least-surprise and narrow. Do not widen support claims, add new bridge promises, or imply that the starter seam modules are production-ready policy implementations.
  </action>
  <verify>
    <automated>rg -n "mix phx.new|mix oban_powertools.install|mix compile|mix ecto.migrate|mix ecto.reset|mix phx.server|ObanPowertoolsAuth|ObanPowertoolsDisplayPolicy|ops-demo|nightly_sync|pause_cron_entry|read-only" README.md guides/installation.md guides/first-operator-session.md guides/example-app-walkthrough.md guides/support-truth-and-ownership-boundaries.md</automated>
  </verify>
  <done>The public docs all describe the same repaired install and first-session contract, with explicit support-truth and no overclaiming.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Tighten docs contract enforcement and CI lanes around the repaired proof stack</name>
  <files>test/oban_powertools/docs_contract_test.exs, .github/workflows/host-contract-proof.yml</files>
  <read_first>
    - test/oban_powertools/docs_contract_test.exs
    - .github/workflows/host-contract-proof.yml
    - README.md
    - guides/installation.md
    - guides/first-operator-session.md
    - test/oban_powertools/fresh_host_contract_test.exs
    - test/oban_powertools/example_host_contract_test.exs
    - .planning/phases/12-fresh-host-install-path-example-fixture-repair/12-VALIDATION.md
  </read_first>
  <acceptance_criteria>
    - `test/oban_powertools/docs_contract_test.exs` asserts the repaired install markers `mix oban_powertools.install`, `mix compile`, `mix ecto.migrate` or reset, `mix phx.server` or equivalent boot wording, `ObanPowertoolsDisplayPolicy`, `ops-demo`, `nightly_sync`, and `read-only`
    - `.github/workflows/host-contract-proof.yml` contains lane names `structural`, `fresh-host`, `docs-contract`, `native-only`, and `bridge-enabled`
    - the workflow runs both `test/oban_powertools/fresh_host_contract_test.exs` and `test/oban_powertools/example_host_contract_test.exs`
    - `mix test test/oban_powertools/docs_contract_test.exs` exits 0
  </acceptance_criteria>
  <action>
    Extend `test/oban_powertools/docs_contract_test.exs` so the docs contract matches the repaired public truth exactly: fresh-host install must mention `mix oban_powertools.install`, the generated starter seams `ObanPowertoolsAuth` and `ObanPowertoolsDisplayPolicy`, the compile or migrate or boot threshold from D-03, the canonical first-session values `ops-demo` and `nightly_sync`, and the read-only bridge boundary.
    Update `.github/workflows/host-contract-proof.yml` so the workflow lanes point at the repaired proof stack from Plans 01 through 03. Preserve the existing `structural`, `docs-contract`, `native-only`, and `bridge-enabled` jobs, and add a distinct `fresh-host` job for the new execution lane instead of folding it into the structural installer test. The workflow must run `test/oban_powertools/fresh_host_contract_test.exs` for the fresh-host contract and keep `test/oban_powertools/example_host_contract_test.exs` as the DOC-01 first-session authority.
    Keep the workflow and docs test narrowly bound to the supported contract. Do not add browser-E2E lanes or unsupported optional-dependency claims in this phase.
  </action>
  <verify>
    <automated>mix test test/oban_powertools/docs_contract_test.exs && rg -n "structural|fresh-host|docs-contract|native-only|bridge-enabled|fresh_host_contract_test\\.exs|example_host_contract_test\\.exs" .github/workflows/host-contract-proof.yml && rg -n "mix oban_powertools.install|mix compile|mix ecto.migrate|mix ecto.reset|mix phx.server|ObanPowertoolsDisplayPolicy|ops-demo|nightly_sync|read-only" test/oban_powertools/docs_contract_test.exs</automated>
  </verify>
  <done>Docs drift and proof-lane drift are both enforced automatically, and CI now runs the repaired contract the public docs actually describe.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Public docs -> adopter expectations | README and guides define the support contract and can mislead adopters if they outrun the code. |
| CI workflow -> public proof promise | Workflow lane names can keep sounding trustworthy while no longer proving the documented path. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-10 | Repudiation | public install and first-session docs | mitigate | Assert concrete repaired markers in docs contract tests, including the starter seams, first-session values, and read-only bridge language. |
| T-12-11 | Tampering | host-contract CI workflow | mitigate | Keep explicit lane names but point them at the repaired proof files so stale workflow commands cannot silently pass. |
| T-12-12 | Information Disclosure | support-truth wording | accept | Repeating the read-only bridge boundary and starter-seam TODO posture reveals intended ownership, not sensitive data, and reduces adopter confusion. |
</threat_model>

<verification>
mix test test/oban_powertools/docs_contract_test.exs test/oban_powertools/fresh_host_contract_test.exs test/oban_powertools/example_host_contract_test.exs
rg -n "structural|fresh-host|docs-contract|native-only|bridge-enabled|mix oban_powertools.install|mix compile|mix ecto.migrate|mix ecto.reset|mix phx.server|ops-demo|nightly_sync" .github/workflows/host-contract-proof.yml README.md guides/installation.md guides/first-operator-session.md guides/example-app-walkthrough.md guides/support-truth-and-ownership-boundaries.md
</verification>

<success_criteria>
The public docs, docs contract tests, and CI workflow all describe and enforce the same repaired day-0 host contract and canonical first-session proof.
</success_criteria>

<output>
After completion, create `.planning/phases/12-fresh-host-install-path-example-fixture-repair/12-04-SUMMARY.md`
</output>
