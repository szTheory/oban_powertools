---
phase: 12
plan: 03
type: execute
wave: 2
depends_on: ["12-01", "12-02"]
files_modified: ["examples/phoenix_host/test/phoenix_host_web/oban_powertools_first_session_test.exs", "test/support/example_host_contract.ex", "test/oban_powertools/example_host_contract_test.exs"]
autonomous: true
requirements: ["DOC-01"]
must_haves:
  truths:
    - "The repaired fixture can complete one real native operator flow after reset and seed."
    - "That flow writes durable audit evidence tied to the seeded operator principal."
    - "The root proof harness can rerun the first-session lane deterministically in CI."
  artifacts:
    - path: "examples/phoenix_host/test/phoenix_host_web/oban_powertools_first_session_test.exs"
      provides: "Fixture-local Phoenix/LiveView proof of the native operator flow"
      contains: "pause_cron_entry"
    - path: "test/support/example_host_contract.ex"
      provides: "Reusable shell helper for fixture proof commands"
      contains: "examples/phoenix_host"
    - path: "test/oban_powertools/example_host_contract_test.exs"
      provides: "Root contract test that runs the first-session proof lane"
      contains: "first_session"
  key_links:
    - from: "examples/phoenix_host/test/phoenix_host_web/oban_powertools_first_session_test.exs"
      to: "examples/phoenix_host/priv/repo/seeds.exs"
      via: "seeded actor and cron resource"
      pattern: "ops-demo|nightly_sync"
    - from: "test/oban_powertools/example_host_contract_test.exs"
      to: "test/support/example_host_contract.ex"
      via: "fixture shell proof"
      pattern: "first_session"
---

<objective>
Add the single honest first-session proof lane for Phase 12: a real native operator action in the canonical fixture that leaves durable audit evidence.

Purpose: Satisfy D-13 through D-16 and close the broken `DOC-01` claim with deterministic proof instead of structural green checks.
Output: A fixture-local Phoenix integration test for one native audited mutation plus a root harness that executes the lane in CI-friendly form.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/12-fresh-host-install-path-example-fixture-repair/12-CONTEXT.md
@.planning/phases/12-fresh-host-install-path-example-fixture-repair/12-RESEARCH.md
@.planning/phases/12-fresh-host-install-path-example-fixture-repair/12-PATTERNS.md
@examples/phoenix_host/priv/repo/seeds.exs
@examples/phoenix_host/lib/phoenix_host_web/oban_powertools_auth.ex
@test/support/example_host_contract.ex
@test/oban_powertools/example_host_contract_test.exs
@test/oban_powertools/web/live/cron_live_test.exs
@lib/oban_powertools/audit.ex
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the fixture-local native first-session integration proof</name>
  <files>examples/phoenix_host/test/phoenix_host_web/oban_powertools_first_session_test.exs</files>
  <read_first>
    - examples/phoenix_host/priv/repo/seeds.exs
    - examples/phoenix_host/lib/phoenix_host_web/oban_powertools_auth.ex
    - test/oban_powertools/web/live/cron_live_test.exs
    - lib/oban_powertools/audit.ex
    - .planning/phases/10-operator-ux-coherence-mutation-safety/10-CONTEXT.md
    - .planning/phases/12-fresh-host-install-path-example-fixture-repair/12-CONTEXT.md
  </read_first>
  <acceptance_criteria>
    - `examples/phoenix_host/test/phoenix_host_web/oban_powertools_first_session_test.exs` drives a native Powertools page, not a bridge page
    - the test uses actor `ops-demo`
    - the test targets cron resource `nightly_sync`
    - the test executes the audited action `pause_cron_entry`
    - the test asserts a durable audit record for that action and actor after execution
  </acceptance_criteria>
  <action>
    Create `examples/phoenix_host/test/phoenix_host_web/oban_powertools_first_session_test.exs` as the canonical first-session proof per D-14 through D-16. Use idiomatic Phoenix and LiveView test helpers inside the fixture app, not browser E2E, and keep the proof on a native Powertools page.
    Make the concrete flow explicit and stable: authenticate as seeded actor `ops-demo`, navigate to the native cron operator surface, preview and execute `pause_cron_entry` against seeded resource `nightly_sync`, then assert durable audit evidence exists for that action using the current audit schema and principal fields.
    Keep the scope narrow per D-17. This file proves one representative audited mutation only; it must not branch into multi-page parity or bridge mutation coverage.
  </action>
  <verify>
    <automated>cd examples/phoenix_host && MIX_ENV=test mix test test/phoenix_host_web/oban_powertools_first_session_test.exs</automated>
  </verify>
  <done>The fixture itself proves one real native operator action from page interaction through durable audit evidence.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire the root host-contract harness to run the first-session lane deterministically</name>
  <files>test/support/example_host_contract.ex, test/oban_powertools/example_host_contract_test.exs</files>
  <read_first>
    - test/support/example_host_contract.ex
    - test/oban_powertools/example_host_contract_test.exs
    - examples/phoenix_host/test/phoenix_host_web/oban_powertools_first_session_test.exs
    - .github/workflows/host-contract-proof.yml
    - .planning/phases/12-fresh-host-install-path-example-fixture-repair/12-VALIDATION.md
  </read_first>
  <acceptance_criteria>
    - `test/support/example_host_contract.ex` exposes a helper for the first-session lane that shells into `examples/phoenix_host`
    - `test/oban_powertools/example_host_contract_test.exs` contains a dedicated first-session proof case
    - the root proof references `ops-demo`, `nightly_sync`, and `pause_cron_entry`
    - `mix test test/oban_powertools/example_host_contract_test.exs` exits 0
  </acceptance_criteria>
  <action>
    Extend `test/support/example_host_contract.ex` and `test/oban_powertools/example_host_contract_test.exs` so the root proof stack runs the fixture-local first-session lane after reset and seeds. Reuse the existing fixture shelling pattern, but add a dedicated helper and test name for `first_session` rather than folding it into compile-only smoke checks.
    Keep the root harness deterministic and CI-friendly: it should prepare the fixture, run the single fixture test file, and fail if the native audited mutation or audit evidence disappears. Preserve any existing native-only or bridge-enabled compile lanes that still matter, but treat this new first-session lane as the DOC-01 authority.
    Do not create a second contract vocabulary. The root proof must use the same concrete values as the fixture test: actor `ops-demo`, resource `nightly_sync`, and action `pause_cron_entry`.
  </action>
  <verify>
    <automated>mix test test/oban_powertools/example_host_contract_test.exs && rg -n "first_session|ops-demo|nightly_sync|pause_cron_entry" test/support/example_host_contract.ex test/oban_powertools/example_host_contract_test.exs</automated>
  </verify>
  <done>The root host-contract proof harness can rerun the canonical first-session mutation lane deterministically and fail on any loss of durable audit behavior.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Fixture operator session -> durable audit storage | The proof must cross the real mutation boundary and verify persisted audit evidence, not only UI copy. |
| Root proof harness -> fixture app | The shell helper must execute the fixture test deterministically without masking failures. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-07 | Repudiation | first-session audit proof | mitigate | Assert a durable audit row for concrete actor `ops-demo`, action `pause_cron_entry`, and resource `nightly_sync` after the native mutation executes. |
| T-12-08 | Elevation of Privilege | fixture native operator flow | mitigate | Drive the proof through the real native auth seam and preview/execute flow instead of bypassing authorization with direct service calls. |
| T-12-09 | Tampering | root proof helper | mitigate | Reuse the fixture shell helper pattern and require the root test to fail on any non-zero fixture command or missing audit evidence. |
</threat_model>

<verification>
cd examples/phoenix_host && MIX_ENV=test mix test test/phoenix_host_web/oban_powertools_first_session_test.exs
mix test test/oban_powertools/example_host_contract_test.exs
</verification>

<success_criteria>
Phase 12 has one deterministic, CI-friendly first-session proof that completes a real native operator mutation and verifies durable audit evidence end to end.
</success_criteria>

<output>
After completion, create `.planning/phases/12-fresh-host-install-path-example-fixture-repair/12-03-SUMMARY.md`
</output>
