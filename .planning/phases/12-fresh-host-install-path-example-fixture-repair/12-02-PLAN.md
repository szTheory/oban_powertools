---
phase: 12
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: ["examples/phoenix_host/priv/repo/migrations/20260522000000_install_oban.exs", "examples/phoenix_host/priv/repo/migrations", "examples/phoenix_host/priv/repo/seeds.exs", "examples/phoenix_host/README.md", "examples/phoenix_host/regenerate.sh"]
autonomous: true
requirements: ["PKG-01", "DOC-01"]
must_haves:
  truths:
    - "The canonical example fixture can reset into a database that includes Powertools tables, not only base Oban tables."
    - "The fixture seeds exactly the operator-visible state needed for one honest first-session proof."
    - "Maintainers can tell which parts of the fixture came from `mix phx.new`, which came from `mix oban_powertools.install`, and which remain manual host-owned follow-up."
  artifacts:
    - path: "examples/phoenix_host/priv/repo/migrations"
      provides: "Canonical fixture migration set including Powertools tables"
      contains: "oban_powertools_"
    - path: "examples/phoenix_host/priv/repo/seeds.exs"
      provides: "Deterministic first-session seed data"
      contains: "ops-demo"
    - path: "examples/phoenix_host/regenerate.sh"
      provides: "Repeatable provenance path from `mix phx.new` through installer and manual follow-up"
      contains: "mix oban_powertools.install"
    - path: "examples/phoenix_host/README.md"
      provides: "Honest curated-fixture support truth"
      contains: "manual follow-up"
  key_links:
    - from: "examples/phoenix_host/priv/repo/migrations"
      to: "examples/phoenix_host/priv/repo/seeds.exs"
      via: "fixture reset before seed data"
      pattern: "oban_powertools_"
    - from: "examples/phoenix_host/regenerate.sh"
      to: "examples/phoenix_host/README.md"
      via: "documented provenance story"
      pattern: "mix phx.new|mix oban_powertools.install|manual"
---

<objective>
Repair the canonical fixture so it becomes a thin, migration-complete, provenance-honest contract host instead of a curated demo that overclaims how it was produced.

Purpose: Close the reopened fixture half of `PKG-01` and set up the deterministic seeded state required for `DOC-01` proof.
Output: A checked-in `examples/phoenix_host` with Powertools migrations, narrow first-session seed data, and README/regeneration artifacts that distinguish generated output from manual host-owned follow-up.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/milestones/v1.1-MILESTONE-AUDIT.md
@.planning/phases/12-fresh-host-install-path-example-fixture-repair/12-CONTEXT.md
@.planning/phases/12-fresh-host-install-path-example-fixture-repair/12-RESEARCH.md
@.planning/phases/12-fresh-host-install-path-example-fixture-repair/12-PATTERNS.md
@examples/phoenix_host/README.md
@examples/phoenix_host/regenerate.sh
@examples/phoenix_host/priv/repo/migrations/20260522000000_install_oban.exs
@examples/phoenix_host/priv/repo/seeds.exs
@test/support/example_host_contract.ex
</context>

<tasks>

<task type="auto">
  <name>Task 1: Check in the full Powertools migration set and the narrow seeded first-session state</name>
  <files>examples/phoenix_host/priv/repo/migrations/20260522000000_install_oban.exs, examples/phoenix_host/priv/repo/migrations, examples/phoenix_host/priv/repo/seeds.exs</files>
  <read_first>
    - examples/phoenix_host/priv/repo/migrations/20260522000000_install_oban.exs
    - examples/phoenix_host/priv/repo/seeds.exs
    - lib/mix/tasks/oban_powertools.install.ex
    - test/support/example_host_contract.ex
    - .planning/phases/11-docs-example-app-compatibility-contract-proof/11-02-PLAN.md
    - .planning/phases/12-fresh-host-install-path-example-fixture-repair/12-RESEARCH.md
  </read_first>
  <acceptance_criteria>
    - `examples/phoenix_host/priv/repo/migrations/` contains the Powertools table definitions needed for native operator flows, including `oban_powertools_audit_events` and `oban_powertools_cron_entries`
    - `cd examples/phoenix_host && MIX_ENV=test mix ecto.reset` succeeds
    - `examples/phoenix_host/priv/repo/seeds.exs` defines a stable operator actor `ops-demo`
    - `examples/phoenix_host/priv/repo/seeds.exs` defines one canonical cron resource named `nightly_sync`
    - the seed set stays narrow and does not add showcase-only records
  </acceptance_criteria>
  <action>
    Repair the checked-in fixture database baseline per D-08 through D-10 and D-14: preserve the real base Oban migration, then add the Powertools migration set or faithful generated equivalent so `MIX_ENV=test mix ecto.reset` creates the tables the public operator contract actually needs.
    In `examples/phoenix_host/priv/repo/seeds.exs`, seed concrete first-session values for the later proof lane: one operator actor `ops-demo` and one native mutation target `nightly_sync`. Seed only the data required to view `/ops/jobs`, preview the native cron action, execute it, and inspect durable audit evidence. Do not add showcase or marketing data per D-12.
    Keep the fixture migration and seed story diffable against installer output rather than clever runtime generation, so maintainers can review provenance honestly per D-10 and D-11.
  </action>
  <verify>
    <automated>cd examples/phoenix_host && MIX_ENV=test mix ecto.reset && MIX_ENV=test mix run priv/repo/seeds.exs && rg -n "oban_powertools_audit_events|oban_powertools_cron_entries|ops-demo|nightly_sync" priv/repo/migrations priv/repo/seeds.exs</automated>
  </verify>
  <done>The fixture can reset into a Powertools-complete schema and seeds the exact operator-visible state needed for one deterministic first-session proof.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite the fixture README and regeneration script around honest curated-fixture provenance</name>
  <files>examples/phoenix_host/README.md, examples/phoenix_host/regenerate.sh</files>
  <read_first>
    - examples/phoenix_host/README.md
    - examples/phoenix_host/regenerate.sh
    - README.md
    - guides/example-app-walkthrough.md
    - .planning/phases/11-docs-example-app-compatibility-contract-proof/11-CONTEXT.md
    - .planning/phases/12-fresh-host-install-path-example-fixture-repair/12-CONTEXT.md
  </read_first>
  <acceptance_criteria>
    - `examples/phoenix_host/README.md` distinguishes `mix phx.new`, `mix oban_powertools.install`, and manual host-owned follow-up
    - `examples/phoenix_host/README.md` describes the fixture as curated and canonical, not as a polished demo
    - `examples/phoenix_host/regenerate.sh` contains `mix phx.new`
    - `examples/phoenix_host/regenerate.sh` contains `mix oban_powertools.install`
    - `examples/phoenix_host/regenerate.sh` leaves explicit markers for manual auth/display-policy follow-up
  </acceptance_criteria>
  <action>
    Rewrite `examples/phoenix_host/README.md` and `examples/phoenix_host/regenerate.sh` to satisfy D-08 through D-12 exactly: the fixture is a thin curated contract host, not a showcase app, and its provenance must name the three buckets plainly: `mix phx.new`, `mix oban_powertools.install`, and irreducible manual host-owned follow-up.
    Make `regenerate.sh` deterministic and diff-friendly. It must recreate the host from a fresh Phoenix app, apply the library dependency and installer, preserve the checked-in Powertools migrations, and leave obvious TODO markers where maintainers reapply thin auth/display-policy or seed details that remain deliberately host-owned.
    Do not overclaim future generator provenance. The README should say the checked-in fixture is support-truth aligned today, while stricter end-to-end generation remains a later tightening step per D-11.
  </action>
  <verify>
    <automated>rg -n "mix phx.new|mix oban_powertools.install|manual|host-owned|curated|canonical|showcase" examples/phoenix_host/README.md examples/phoenix_host/regenerate.sh</automated>
  </verify>
  <done>The fixture README and regeneration script tell one honest provenance story that matches the checked-in host and does not blur the host-vs-library boundary.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Checked-in fixture -> public support claims | The example host is public evidence and can overstate supported behavior if its migrations or prose drift. |
| Fixture seed data -> operator proof lane | Seeded records can accidentally imply broad demo coverage instead of one narrow contract flow. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-04 | Tampering | fixture migration baseline | mitigate | Check in the Powertools migration set and require `MIX_ENV=test mix ecto.reset` plus grep assertions for concrete Powertools tables. |
| T-12-05 | Repudiation | fixture provenance story | mitigate | Make README and `regenerate.sh` explicitly separate generated steps from manual host-owned follow-up so maintainers can explain how the host was built. |
| T-12-06 | Information Disclosure | seeded example data | accept | Seed only non-sensitive demo operator values `ops-demo` and `nightly_sync`; no production-like secrets or user data belong in the fixture. |
</threat_model>

<verification>
cd examples/phoenix_host && MIX_ENV=test mix ecto.reset && MIX_ENV=test mix run priv/repo/seeds.exs
rg -n "mix phx.new|mix oban_powertools.install|manual|ops-demo|nightly_sync|oban_powertools_" examples/phoenix_host/README.md examples/phoenix_host/regenerate.sh examples/phoenix_host/priv/repo/seeds.exs examples/phoenix_host/priv/repo/migrations
</verification>

<success_criteria>
The canonical fixture is migration-complete, seeds only the real first-session path, and states its provenance with no inflated “fully generated” claim.
</success_criteria>

<output>
After completion, create `.planning/phases/12-fresh-host-install-path-example-fixture-repair/12-02-SUMMARY.md`
</output>
