---
phase: 80-page-migration-wave-2-jobs-forensics
plan: 16
type: execute
wave: 13
depends_on:
  - 80-14
  - 80-15
files_modified:
  - .planning/phases/80-page-migration-wave-2-jobs-forensics/80-VALIDATION.md
  - .planning/phases/80-page-migration-wave-2-jobs-forensics/80-VERIFICATION.md
  - .planning/REQUIREMENTS.md
autonomous: true
gap_closure: true
requirements:
  - PAGE-02
  - PAGE-09
  - FORM-03
  - DATA-*
  - PAGE-10
  - A11Y-*
must_haves:
  truths:
    - "The only incomplete Phase 80 plan is this corrected Plan 80-16; normal execute-phase 80 --gaps-only selects it once and can finish with one real 80-16 Summary."
    - "The full-suite gate calibrates current residuals from their exact owning contract files, then requires byte-identical module, complete test title, repository-relative source path, and positive source-line tuples from the full suite and its failed-test rerun."
    - "All three attribution files must exist, every row must satisfy the exact four-field schema and allowed module/path binding, every file must agree with its command exit status and printed failure total, and zero failures require three existing zero-byte TSV files."
    - "CR-01, WR-01, WR-02, and V-TEST-01 stay closed under the focused five-file suite and three consecutive coordinator reruns."
    - "Every artifact, VoiceOver-discovery, accessibility, connected-page, and compare-only visual gate skipped after the prior comparator failure reruns fresh without baseline updates or fabricated transcripts."
    - "Any failed blocking gate preserves the ROADMAP and REQUIREMENTS snapshots, leaves 80-16-SUMMARY.md absent, and cannot trigger terminal bookkeeping; only passed verification can enter Summary-first closeout."
  artifacts:
    - path: ".planning/phases/80-page-migration-wave-2-jobs-forensics/80-VALIDATION.md"
      provides: "Append-only corrected Plan 80-16 evidence, including calibrated/full/rerun identity tuples and every rerun gate"
    - path: ".planning/phases/80-page-migration-wave-2-jobs-forensics/80-VERIFICATION.md"
      provides: "Fresh passed-or-gaps-found report with exact attribution provenance and requirement status"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Passed-branch-only PAGE-02 and PAGE-09 traceability normalization before Summary-first tracking"
    - path: ".planning/phases/80-page-migration-wave-2-jobs-forensics/80-16-SUMMARY.md"
      provides: "Executor-created completion record, created only after both tasks pass"
  key_links:
    - from: ".planning/phases/80-page-migration-wave-2-jobs-forensics/80-VERIFICATION.md"
      to: "test/oban_powertools/docs_contract_test.exs"
      via: "fresh isolated residual calibration identity"
      pattern: "DocsContractTest"
    - from: ".planning/phases/80-page-migration-wave-2-jobs-forensics/80-VERIFICATION.md"
      to: "test/oban_powertools/example_host_contract_test.exs"
      via: "fresh isolated residual calibration identity"
      pattern: "ExampleHostContractTest"
    - from: ".planning/phases/80-page-migration-wave-2-jobs-forensics/80-VERIFICATION.md"
      to: "test/oban_powertools/fresh_host_contract_test.exs"
      via: "fresh isolated residual calibration identity"
      pattern: "FreshHostContractTest"
    - from: ".planning/phases/80-page-migration-wave-2-jobs-forensics/80-VERIFICATION.md"
      to: ".planning/phases/80-page-migration-wave-2-jobs-forensics/80-14-SUMMARY.md"
      via: "CR-01 retained focused evidence"
      pattern: "CR-01"
    - from: ".planning/phases/80-page-migration-wave-2-jobs-forensics/80-VERIFICATION.md"
      to: ".planning/phases/80-page-migration-wave-2-jobs-forensics/80-15-SUMMARY.md"
      via: "WR-01, WR-02, and V-TEST-01 retained focused evidence"
      pattern: "WR-01|WR-02|V-TEST-01"
---

<objective>
Supersede the failed stale-title comparator in the existing incomplete Plan 80-16 with fresh, independently reproduced residual attribution, then rerun the complete fail-closed Phase 80 closeout.

Purpose: give standard gap-only execution one GSD-recognized path that distinguishes current out-of-scope contract debt from a Phase 80 regression using exact executable provenance.
Output: exact calibrated/full/rerun attribution evidence, fresh focused and page-quality evidence, a truthful verification report, and ledgers prepared for normal Summary-first completion.
</objective>

<execution_context>
@/Users/jon/.agents/gsd-core/workflows/execute-plan.md
@/Users/jon/.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/80-page-migration-wave-2-jobs-forensics/80-CONTEXT.md
@.planning/phases/80-page-migration-wave-2-jobs-forensics/80-RESEARCH.md
@.planning/phases/80-page-migration-wave-2-jobs-forensics/80-PATTERNS.md
@.planning/phases/80-page-migration-wave-2-jobs-forensics/80-UI-SPEC.md
@.planning/phases/80-page-migration-wave-2-jobs-forensics/80-VALIDATION.md
@.planning/phases/80-page-migration-wave-2-jobs-forensics/80-VERIFICATION.md
@.planning/phases/80-page-migration-wave-2-jobs-forensics/80-14-SUMMARY.md
@.planning/phases/80-page-migration-wave-2-jobs-forensics/80-15-SUMMARY.md
@package.json
@playwright.config.ts
@voiceover.config.ts
</context>

## Must Haves — Gap, Requirement, and Bookkeeping Coverage

- **PAGE-02 / FORM-03:** oversized Jobs URL inputs, canonical recovery, and every frozen batch terminal position remain green.
- **PAGE-09 / PAGE-10:** workflow-step evidence remains relationally authorized inside the shipped shared production composition.
- **DATA-\*:** bounded reads, structural redaction, closed presenters, stable identities, and explicit result truth remain intact.
- **A11Y-\*:** exact manifest, ARIA, axe, connected interaction, compare-only VRT, and seven-case VoiceOver discovery rerun fresh; unsupported real transcripts remain explicit and are never invented.
- **Attribution:** the isolated calibration, full suite, and `mix test --failed` rerun each produce an existing schema-valid TSV bound to that command's status and printed failure total, and the three TSVs compare byte-for-byte.
- **Bookkeeping:** any blocking failure leaves the ledger snapshots unchanged and no Summary; successful completion remains Summary-first and STATE remains owned by registered execute-phase/execute-plan handlers.

## Artifacts this phase produces

- Existing `80-VALIDATION.md` gains a dated corrected Plan 80-16 addendum with exact isolated-calibration, full-suite, failed-rerun, focused, repeated, compile, artifact, VoiceOver-discovery, and page-quality commands and observed results.
- Existing `80-VERIFICATION.md` is rewritten from current evidence. It records `status: passed` only if every blocking gate passes; otherwise it remains `gaps_found` with the exact failed gate and finite identity-set difference.
- Existing `.planning/REQUIREMENTS.md` receives only the passed-branch PAGE-02/PAGE-09 traceability-row split needed by exact post-Summary requirement tracking.
- The executor creates `80-16-SUMMARY.md` only after both tasks pass; registered execute-plan handlers then own STATE, ROADMAP, metrics, decisions, and requirement completion.
- Temporary logs, statuses, failure totals, hashes, extractor, and sorted identity TSV files exist only under `.planning/tmp/80-16-ledger-snapshot/` during execution and are removed after their finite evidence is transcribed.
- No production symbol, public API, route, schema, migration, dependency, fixture, manifest entry, ARIA YAML, PNG baseline, CSS, JavaScript artifact, CI file, example-host file, or prior Plan/Summary is created or modified.

<tasks>

<task type="auto">
  <name>Task 80-16-01: Calibrate exact residual identities and rerun every blocked closeout gate</name>
  <files>.planning/phases/80-page-migration-wave-2-jobs-forensics/80-VALIDATION.md, .planning/phases/80-page-migration-wave-2-jobs-forensics/80-VERIFICATION.md</files>
  <read_first>
    - @.planning/phases/80-page-migration-wave-2-jobs-forensics/80-VALIDATION.md
    - @.planning/phases/80-page-migration-wave-2-jobs-forensics/80-VERIFICATION.md
    - @.planning/phases/80-page-migration-wave-2-jobs-forensics/80-14-PLAN.md
    - @.planning/phases/80-page-migration-wave-2-jobs-forensics/80-14-SUMMARY.md
    - @.planning/phases/80-page-migration-wave-2-jobs-forensics/80-15-PLAN.md
    - @.planning/phases/80-page-migration-wave-2-jobs-forensics/80-15-SUMMARY.md
    - @test/oban_powertools/docs_contract_test.exs
    - @test/oban_powertools/example_host_contract_test.exs
    - @test/oban_powertools/fresh_host_contract_test.exs
    - @test/support/example_host_contract.ex
    - @test/support/fresh_host_contract.ex
    - @test/oban_powertools/forensics_test.exs
    - @test/oban_powertools/web/live/forensics_live_test.exs
    - @test/oban_powertools/web/jobs_params_test.exs
    - @test/oban_powertools/jobs/batch_coordinator_test.exs
    - @test/oban_powertools/web/live/jobs_live_test.exs
    - @package.json
    - @test/browser/support/verify-page-baselines.mjs
    - @test/browser/support/verify-page-aria-snapshots.mjs
    - @test/browser/voiceover/page.voiceover.spec.ts
  </read_first>
  <action>Before any gate or report edit, require `.planning/tmp/80-16-ledger-snapshot` not to exist; create it; copy `.planning/ROADMAP.md` and `.planning/REQUIREMENTS.md`; and write `git hash-object` output for `test/oban_powertools/docs_contract_test.exs`, `test/oban_powertools/example_host_contract_test.exs`, `test/oban_powertools/fresh_host_contract_test.exs`, `test/support/example_host_contract.ex`, and `test/support/fresh_host_contract.ex` to `residual-owner.hashes`. Do not edit these files or any unrelated CI/example-host/installer/host-contract failure.

Run the focused five-file Jobs/Forensics command below and record its exact count. Run the coordinator file three additional consecutive times with `--seed 0`; any failure reopens V-TEST-01. Run `mix format --check-formatted` and `mix compile --warnings-as-errors --force`.

Inside the snapshot directory create a temporary `extract-exunit.awk` and use that same extractor for all three logs. Its executable contract is exact: recognize only headings matching `^  [0-9]+\\) test .+ \\([^()]+\\)$`; obtain the module from the final parenthesized heading suffix and the complete title from everything after `test ` through the text immediately before that suffix; search only until the next outer heading for exactly one location matching `^[[:space:]]*test/[^[:space:]:]+_test\\.exs:[1-9][0-9]*[[:space:]]*$`; fail on a heading with zero or multiple such locations; emit `module<TAB>complete title<TAB>repository-relative path<TAB>positive source line`; and fail on embedded tabs, empty fields, duplicate tuples, or any module/path pair other than `ObanPowertools.DocsContractTest` with `test/oban_powertools/docs_contract_test.exs`, `ObanPowertools.ExampleHostContractTest` with `test/oban_powertools/example_host_contract_test.exs`, or `ObanPowertools.FreshHostContractTest` with `test/oban_powertools/fresh_host_contract_test.exs`. For each log, run the extractor into a dedicated unsorted temporary TSV and check the extractor exit status before invoking `LC_ALL=C sort` to create the requested identity file; never pipe the extractor directly into `sort`. Nested host output is ineligible because it does not satisfy both the exact outer heading and following repository test-location rules.

Execute the following authoritative three-run protocol without changing the five hashed owner/support files:

1. Run exactly `mix test test/oban_powertools/docs_contract_test.exs test/oban_powertools/example_host_contract_test.exs test/oban_powertools/fresh_host_contract_test.exs --seed 0`, capturing combined output in `calibration.log`, numeric status in `calibration.exit`, the single printed ExUnit failure total in `calibration.failures`, and extracted identities in `calibration.identities.tsv`.
2. Run exactly `mix test --seed 0`, capturing the corresponding four `full.*` files.
3. Immediately run exactly `mix test --failed --seed 0`, capturing the corresponding four `failed-rerun.*` files.

For each run, require the log, exit, failures, and identities files all exist. Re-parse the corresponding log for exactly one terminal summary matching `^[0-9]+ tests?, [0-9]+ failures?(, [0-9]+ excluded)?$`; write the exact matched line and its derived failure count to temporary evidence files, and fail on zero or multiple matches. Require the derived failure count to equal the stored `.failures` value and the TSV line count. Require exit 0 when the derived total is zero and exit 2 when it is positive. Validate every TSV independently as exactly four tab-separated nonempty fields, an allowed module/path pair, and a positive decimal source line before comparison. Then require the three TSV files byte-identical. The zero-failure branch is valid only when all three TSV paths exist and each is a zero-byte regular file; missing files never count as empty. Preserve all log, summary, count, status, unsorted, and sorted evidence until these checks and the final comparison finish. Recompute and compare the five source hashes after the protocol. A current FreshHost tuple may replace the historical first-session tuple only when that exact four-field tuple appeared independently in calibration and reproduced in both later runs.

After attribution passes, rerun every gate skipped by the prior fail-closed attempt: `npm run showcase:manifest`; both exact artifact validators; Git-tracked counts of exactly 147 ARIA YAML and 588 page PNG files; exact VoiceOver `--list` discovery of seven cases with one Jobs and one Forensics required story; and `npm run verify:pages` compare-only. Never run a snapshot-update command. Do not run Guidepup host setup, fabricate a transcript, or substitute axe/ARIA for the documented real VoiceOver transcript gap.

Append current commands, counts, exact identity tuples, command statuses and printed totals, source/log hashes, CR-01/WR-01/WR-02/V-TEST-01 status, requirement coverage, ASVS dispositions, artifact cardinalities, and supported-environment status to `80-VALIDATION.md`. Rewrite `80-VERIFICATION.md` from current evidence. `status: passed` requires every focused, repeat, format, forced-compile, three-run attribution, manifest, validator, inventory, VoiceOver-discovery, and compare-only page gate to pass.

If any gate fails, write `status: gaps_found` and the exact finite difference/blocker; prove ROADMAP and REQUIREMENTS byte-identical to their snapshots; require `80-16-SUMMARY.md` absent; and require STATE free of `Phase complete — ready for verification`, a `| Phase 80 P16 |` metric row, or `Completed 80-16-PLAN.md`. Handler-owned `status: executing`, Phase 80 `EXECUTING`, Plan 16 of 16, and `Ready to execute` are allowed. Remove only the exact snapshot directory, then fail this task. Do not enter Task 80-16-02, choose a skip escape hatch, mark a requirement complete, or invoke `state.advance-plan`, `roadmap.update-plan-progress`, or `requirements.mark-complete`.</action>
  <verify>
    <automated>mix test test/oban_powertools/forensics_test.exs test/oban_powertools/web/live/forensics_live_test.exs test/oban_powertools/web/jobs_params_test.exs test/oban_powertools/jobs/batch_coordinator_test.exs test/oban_powertools/web/live/jobs_live_test.exs --seed 0</automated>
    <automated>for attempt in 1 2 3; do mix test test/oban_powertools/jobs/batch_coordinator_test.exs --seed 0 || exit 1; done</automated>
    <automated>mix format --check-formatted &amp;&amp; mix compile --warnings-as-errors --force</automated>
    <automated>snapshot=.planning/tmp/80-16-ledger-snapshot; test -f "$snapshot/extract-exunit.awk" || exit 1; for run in calibration full failed-rerun; do test -f "$snapshot/$run.log" &amp;&amp; test -f "$snapshot/$run.exit" &amp;&amp; test -f "$snapshot/$run.failures" &amp;&amp; test -f "$snapshot/$run.identities.tsv" || exit 1; unsorted="$snapshot/$run.reextracted.unsorted.tsv"; reextracted="$snapshot/$run.reextracted.tsv"; summary="$snapshot/$run.reextracted.summary"; printed_failures="$snapshot/$run.reextracted.failures"; LC_ALL=C awk -f "$snapshot/extract-exunit.awk" "$snapshot/$run.log" &gt; "$unsorted" || exit 1; LC_ALL=C sort "$unsorted" &gt; "$reextracted" || exit 1; cmp -s "$reextracted" "$snapshot/$run.identities.tsv" || exit 1; LC_ALL=C sed -nE '/^[0-9]+ tests?, [0-9]+ failures?(, [0-9]+ excluded)?$/p' "$snapshot/$run.log" &gt; "$summary" || exit 1; test "$(wc -l &lt; "$summary" | tr -d ' ')" = 1 || exit 1; LC_ALL=C sed -nE 's/^[0-9]+ tests?, ([0-9]+) failures?(, [0-9]+ excluded)?$/\1/p' "$summary" &gt; "$printed_failures" || exit 1; test "$(wc -l &lt; "$printed_failures" | tr -d ' ')" = 1 &amp;&amp; rg -q '^[0-9]+$' "$printed_failures" || exit 1; test "$(wc -l &lt; "$snapshot/$run.exit" | tr -d ' ')" = 1 &amp;&amp; rg -q '^[0-9]+$' "$snapshot/$run.exit" || exit 1; test "$(wc -l &lt; "$snapshot/$run.failures" | tr -d ' ')" = 1 &amp;&amp; rg -q '^[0-9]+$' "$snapshot/$run.failures" || exit 1; cmp -s "$printed_failures" "$snapshot/$run.failures" || exit 1; awk -F '\t' 'NF != 4 || $1 == "" || $2 == "" || $3 == "" || $4 !~ /^[1-9][0-9]*$/ {exit 1} !($1 == "ObanPowertools.DocsContractTest" &amp;&amp; $3 == "test/oban_powertools/docs_contract_test.exs") &amp;&amp; !($1 == "ObanPowertools.ExampleHostContractTest" &amp;&amp; $3 == "test/oban_powertools/example_host_contract_test.exs") &amp;&amp; !($1 == "ObanPowertools.FreshHostContractTest" &amp;&amp; $3 == "test/oban_powertools/fresh_host_contract_test.exs") {exit 1}' "$snapshot/$run.identities.tsv" || exit 1; test "$(wc -l &lt; "$snapshot/$run.identities.tsv" | tr -d ' ')" = "$(LC_ALL=C sort -u "$snapshot/$run.identities.tsv" | wc -l | tr -d ' ')" || exit 1; test "$(wc -l &lt; "$snapshot/$run.identities.tsv" | tr -d ' ')" = "$(cat "$printed_failures")" || exit 1; if test "$(cat "$printed_failures")" -eq 0; then test "$(cat "$snapshot/$run.exit")" -eq 0 || exit 1; else test "$(cat "$snapshot/$run.exit")" -eq 2 || exit 1; fi; done; cmp -s "$snapshot/calibration.identities.tsv" "$snapshot/full.identities.tsv" &amp;&amp; cmp -s "$snapshot/full.identities.tsv" "$snapshot/failed-rerun.identities.tsv" &amp;&amp; { test -s "$snapshot/calibration.identities.tsv" || { test -f "$snapshot/calibration.identities.tsv" &amp;&amp; test ! -s "$snapshot/calibration.identities.tsv" &amp;&amp; test -f "$snapshot/full.identities.tsv" &amp;&amp; test ! -s "$snapshot/full.identities.tsv" &amp;&amp; test -f "$snapshot/failed-rerun.identities.tsv" &amp;&amp; test ! -s "$snapshot/failed-rerun.identities.tsv"; }; }</automated>
    <automated>git hash-object test/oban_powertools/docs_contract_test.exs test/oban_powertools/example_host_contract_test.exs test/oban_powertools/fresh_host_contract_test.exs test/support/example_host_contract.ex test/support/fresh_host_contract.ex | cmp -s - .planning/tmp/80-16-ledger-snapshot/residual-owner.hashes</automated>
    <automated>npm run showcase:manifest &amp;&amp; node test/browser/support/verify-page-baselines.mjs &amp;&amp; node test/browser/support/verify-page-aria-snapshots.mjs</automated>
    <automated>test "$(git ls-files 'test/browser/__aria_snapshots__/**/*.yml' | wc -l | tr -d ' ')" = 147 &amp;&amp; test "$(git ls-files 'test/browser/__screenshots__/**/showcase/page-*/*.png' | wc -l | tr -d ' ')" = 588</automated>
    <automated>voiceover_list="$(mktemp -t obpt-phase80-voiceover-list.XXXXXX)" &amp;&amp; npx playwright test --config=voiceover.config.ts --list | tee "$voiceover_list" &amp;&amp; test "$(rg -c '^  \[voiceover-webkit\] › page\.voiceover\.spec\.ts:[0-9]+:[0-9]+ › ' "$voiceover_list")" -eq 7 &amp;&amp; test "$(rg -c '› page-jobs-full-detail ' "$voiceover_list")" -eq 1 &amp;&amp; test "$(rg -c '› page-forensics-incident-partial-remediation ' "$voiceover_list")" -eq 1 &amp;&amp; rg -q '^Total: 7 tests in 1 file$' "$voiceover_list"</automated>
    <automated>npm run verify:pages</automated>
    <automated>verification_status="$(sed -nE 's/^status:[[:space:]]*([^[:space:]]+).*/\1/p' .planning/phases/80-page-migration-wave-2-jobs-forensics/80-VERIFICATION.md | head -1)"; if test "$verification_status" = passed; then true; elif test "$verification_status" = gaps_found; then cmp -s .planning/tmp/80-16-ledger-snapshot/ROADMAP.md .planning/ROADMAP.md &amp;&amp; cmp -s .planning/tmp/80-16-ledger-snapshot/REQUIREMENTS.md .planning/REQUIREMENTS.md &amp;&amp; test ! -e .planning/phases/80-page-migration-wave-2-jobs-forensics/80-16-SUMMARY.md &amp;&amp; ! rg -q 'Phase complete — ready for verification|^\| Phase 80 P16 \||Completed 80-16-PLAN\.md' .planning/STATE.md &amp;&amp; rm -rf -- .planning/tmp/80-16-ledger-snapshot &amp;&amp; false; else false; fi</automated>
  </verify>
  <acceptance_criteria>
    - The focused suite and all three additional coordinator runs pass with exact recorded counts; CR-01, WR-01, WR-02, and V-TEST-01 remain closed.
    - Calibration, full-suite, and failed-rerun TSVs all exist; every row has exactly module, complete title, allowed repository path, and positive source line; each command's status and printed failure total match its TSV; and the three TSVs are byte-equal.
    - The zero-failure case passes only with three existing zero-byte identity TSVs, three printed zero-failure totals, and three zero command statuses; a missing or malformed file is always blocking.
    - The five residual-owner/support source hashes are identical before and after attribution; no unrelated CI, host, installer, example-host, or contract failure is edited to manufacture a pass.
    - FreshHost versus first-session drift is accepted only from the calibration set observed immediately before the full suite; no stale alternative title list or count-only fallback remains.
    - Manifest remains schema 8 with 49 page stories and 113 targets; validators and Git-tracked checks report exactly 147 ARIA and 588 PNG artifacts.
    - Exact seven-case VoiceOver discovery and `npm run verify:pages` pass fresh; real unsupported transcripts remain open and no baseline update runs.
    - A failed gate leaves ROADMAP/REQUIREMENTS byte-identical to their snapshots, no Summary, no requirement completion, and no terminal Plan 80-16 tracking.
  </acceptance_criteria>
  <done>Current residuals are independently calibrated and exactly reproduced, and every previously skipped Phase 80 closeout gate has fresh evidence.</done>
</task>

<task type="auto">
  <name>Task 80-16-02: Prepare passed evidence for registered Summary-first completion tracking</name>
  <files>.planning/REQUIREMENTS.md, .planning/phases/80-page-migration-wave-2-jobs-forensics/80-VALIDATION.md, .planning/phases/80-page-migration-wave-2-jobs-forensics/80-VERIFICATION.md</files>
  <read_first>
    - @.planning/phases/80-page-migration-wave-2-jobs-forensics/80-VERIFICATION.md
    - @.planning/phases/80-page-migration-wave-2-jobs-forensics/80-VALIDATION.md
    - @.planning/ROADMAP.md
    - @.planning/REQUIREMENTS.md
    - @.planning/STATE.md
    - @.planning/phases/80-page-migration-wave-2-jobs-forensics/80-14-SUMMARY.md
    - @.planning/phases/80-page-migration-wave-2-jobs-forensics/80-15-SUMMARY.md
  </read_first>
  <action>Before mutation, mechanically require `80-VERIFICATION.md` frontmatter `status: passed`; explicit closed CR-01/WR-01/WR-02/V-TEST-01 rows; exact calibrated/full/failed-rerun attribution equality; and passing artifact/page gates. Otherwise fail without touching a ledger.

On passed status only, replace the exact grouped traceability row `| PAGE-02, PAGE-09 | Phase 80 | Pending |` with separate exact rows `| PAGE-02 | Phase 80 | Pending |` and `| PAGE-09 | Phase 80 | Pending |`, in that order. Leave PAGE-02, PAGE-09, and FORM-03 definitions unchecked; leave DATA-01..04, PAGE-10, and A11Y-01..04 at their existing ledger truth. Reconcile VALIDATION wording so Plans 80-01 through 80-16 cover the 15 executed plans, the historically failed Plan 80-16 comparator attempt, and the corrected Plan 80-16 closure tasks without implying the failed attempt completed.

Do not edit `.planning/STATE.md`. Assert its handler-owned pre-Summary position remains `status: executing`, Phase 80 `EXECUTING`, Plan 16 of 16, and `Ready to execute`, preserving unrelated debt and the VoiceOver supported-environment note. Do not invoke any `state.*` handler inside the task: `state.begin-phase` already owns this pre-Summary position, and normal execute-plan Summary-first handlers own later advancement.

Do not mark Phase 80, Plan 80-16, ROADMAP, or a requirement complete inside this task, and do not create `80-16-SUMMARY.md`. Remove only `.planning/tmp/80-16-ledger-snapshot` after passed-branch assertions succeed. The executor must create `80-16-SUMMARY.md` next. Only after that file exists may registered execute-plan operations run: `state.advance-plan`, the normal metrics/session/decision handlers, `roadmap.update-plan-progress`, and `requirements.mark-complete PAGE-02 PAGE-09 FORM-03`. This is the sole terminal completion path.</action>
  <verify>
    <automated>test "$(sed -nE 's/^status:[[:space:]]*([^[:space:]]+).*/\1/p' .planning/phases/80-page-migration-wave-2-jobs-forensics/80-VERIFICATION.md | head -1)" = passed &amp;&amp; for gap in CR-01 WR-01 WR-02 V-TEST-01; do rg -q "$gap.*(closed|pass|PASS|satisfied|SATISFIED)" .planning/phases/80-page-migration-wave-2-jobs-forensics/80-VERIFICATION.md || exit 1; done</automated>
    <automated>node -e 'const fs=require("fs"); const road=fs.readFileSync(".planning/ROADMAP.md","utf8"); const phase=road.split("### Phase 80:")[1].split("### Phase 81:")[0]; if(!/^- \[ \] \*\*Phase 80:/m.test(road)||/^\*\*Plans\*\*: 16\/16 plans complete$/m.test(phase)||/^- \[x\] 80-16-PLAN\.md$/m.test(phase)) process.exit(1); const req=fs.readFileSync(".planning/REQUIREMENTS.md","utf8"); for(const id of ["PAGE-02","PAGE-09","FORM-03"]) if(!new RegExp("^- \\[ \\] \\*\\*"+id+"\\*\\*:","m").test(req)) process.exit(1); if(/^\| PAGE-02, PAGE-09 \|/m.test(req)||!/^\| PAGE-02 \| Phase 80 \| Pending \|$/m.test(req)||!/^\| PAGE-09 \| Phase 80 \| Pending \|$/m.test(req)||!/^\| FORM-03 \| Phase 80 \| Pending \|$/m.test(req)) process.exit(1); for(const id of ["DATA-01","DATA-02","DATA-03","DATA-04","PAGE-10","A11Y-01","A11Y-02","A11Y-03","A11Y-04"]) if(!new RegExp("^- \\[x\\] \\*\\*"+id+"\\*\\*:","m").test(req)) process.exit(1); const state=fs.readFileSync(".planning/STATE.md","utf8"); if(!/^status: executing$/m.test(state)||!/^Phase: 80 .*— EXECUTING$/m.test(state)||!/^Plan: 16 of 16$/m.test(state)||!/^Status: Ready to execute$/m.test(state)||!/VoiceOver/.test(state)) process.exit(1); if(fs.existsSync(".planning/phases/80-page-migration-wave-2-jobs-forensics/80-16-SUMMARY.md")) process.exit(1);'</automated>
    <automated>rg -q 'Plans 80-01 through 80-16' .planning/phases/80-page-migration-wave-2-jobs-forensics/80-VALIDATION.md &amp;&amp; rg -q 'calibrat' .planning/phases/80-page-migration-wave-2-jobs-forensics/80-VALIDATION.md &amp;&amp; rg -q 'supported-environment' .planning/phases/80-page-migration-wave-2-jobs-forensics/80-VERIFICATION.md</automated>
    <automated>git diff --check -- .planning/REQUIREMENTS.md .planning/phases/80-page-migration-wave-2-jobs-forensics/80-VALIDATION.md .planning/phases/80-page-migration-wave-2-jobs-forensics/80-VERIFICATION.md</automated>
  </verify>
  <acceptance_criteria>
    - Only a passed fresh reverification can enter this task.
    - PAGE-02, PAGE-09, and FORM-03 remain Pending before Summary creation, with PAGE-02/PAGE-09 represented by separate exact traceability rows.
    - DATA-01..04, PAGE-10, and A11Y-01..04 remain represented at their existing completed ledger truth while their fresh Phase 80 gates are recorded in VALIDATION/VERIFICATION.
    - STATE is not directly edited and remains at the handler-owned pre-Summary Plan 16 executing position; no terminal phase status, metric, decision, roadmap checkmark, or requirement completion is written early.
    - All 15 executed Plans/Summaries remain unchanged and `80-16-SUMMARY.md` is absent before the executor's Summary-first handoff.
    - No production, schema, migration, dependency, route, fixture, manifest, baseline, generated asset, CI, example-host, or prior plan/summary file is modified.
  </acceptance_criteria>
  <done>Passed evidence and exact Pending requirement rows are ready for the executor to create the Plan 80-16 Summary before registered terminal bookkeeping.</done>
</task>

</tasks>

<threat_model>
## ASVS L1 Assets

- Verification provenance, current residual identity, requirement truth, roadmap/state integrity, accessibility evidence, and immutable artifact inventory.

## Trust Boundaries

| Boundary | Untrusted input / protected output |
|---|---|
| Isolated residual lane -> full suite | environment/order-dependent failures -> exact independently observed identities |
| ExUnit text -> attribution tuples | nested logs and volatile output -> outer module/title/source/line identity only |
| Dirty working tree -> Phase 80 closure | unrelated contract debt -> no false Phase 80 regression or manufactured green |
| Verification -> project ledgers | partial evidence -> no completion before a real Summary |
| Unsupported macOS setup -> accessibility record | unavailable VoiceOver startup -> explicit open transcripts without synthetic evidence |

## STRIDE Threat Register — blocking threshold: high

| Threat ID | Category | Severity | Disposition | Mitigation and verification |
|---|---|---|---|---|
| T-80-16-INVENTORY | Tampering | high | mitigate | One corrected incomplete 80-16 plan and no 80-17 plan give normal gaps-only execution one recognized completion path. |
| T-80-16-STALE | Repudiation | high | mitigate | Fresh calibration replaces the stale fixed title list and is bound to exact owner/support file hashes. |
| T-80-16-COUNT | Spoofing | high | mitigate | Existing schema-valid TSVs require byte-equal module/title/path/line tuples and bind to exit status plus printed failure total; counts alone are insufficient. |
| T-80-16-MISSING | Spoofing | high | mitigate | Every attribution path must be a regular file; zero failures require three existing zero-byte TSVs, never absent files. |
| T-80-16-NESTED | Spoofing | high | mitigate | One concrete extractor accepts only exact outer headings paired with one allowed repository test location and rejects malformed rows or module/path mismatches. |
| T-80-16-SCOPE | Tampering | high | mitigate | Owner hashes and dirty-file audit prohibit edits to CI, example-host, installer, host-contract, product, schema, dependency, or baseline files. |
| T-80-16-BOOKKEEP | Tampering | high | mitigate | Ledger snapshots and absent Summary block failed completion; STATE remains handler-owned and passed closeout leaves completion to registered Summary-first tracking. |
| T-80-16-A11Y | Repudiation | high | mitigate | Exact seven-case discovery plus fresh axe/ARIA/page gates; real unsupported transcripts stay open and are not replaced. |
| T-80-16-VRT | Tampering | high | mitigate | Exact 147/588 tracked inventories and compare-only aggregate prohibit snapshot laundering. |
| T-80-16-DISCLOSE | Information disclosure | high | mitigate | Reports retain finite identities and hashes only, excluding raw logs, host paths, tokens, payloads, credentials, and secrets. |

All high-severity threats are blocking: any unmitigated high threat requires `status: gaps_found`, unchanged ledger snapshots, no Summary, and no terminal tracking.
</threat_model>

<verification>
- Require this file to be the sole incomplete Phase 80 plan and require no `80-17-PLAN.md`.
- Reproduce current residual identities independently with one exact outer-ExUnit extractor; validate every TSV and command status/printed total before byte comparison.
- Rerun the focused five-file lane, three coordinator repetitions, formatting, forced compilation, schema-8 manifest, both exact validators, 147/588 inventories, exact VoiceOver discovery, and the complete compare-only page aggregate.
- Audit that the 15 executed plans/summaries and shipped code remain unchanged and no schema-like file matching repository Ecto migration patterns is added or modified.
- On failure, preserve ROADMAP/REQUIREMENTS snapshots and prohibit Summary/terminal tracking; on pass, normalize only exact Pending traceability and assert handler-owned pre-Summary STATE.
</verification>

<success_criteria>
- Standard `/gsd:execute-phase 80 --gaps-only` selects one corrected Plan 80-16 and no superseded incomplete plan remains.
- FreshHost versus first-session drift is resolved by current exact reproduction, not by weakening to counts, malformed identity files, missing-file-as-empty behavior, or a stale title allowlist.
- All four actionable gaps remain closed and every gate skipped after the prior comparator failure passes fresh.
- PAGE-02, PAGE-09, FORM-03, DATA-*, PAGE-10, and A11Y-* have explicit current evidence and truthful ledger representation.
- No unrelated failure is edited away, no baseline is updated, no transcript is fabricated, no STATE edit bypasses registered handlers, and no schema push occurs.
- Only the executor-created `80-16-SUMMARY.md` may unlock normal terminal bookkeeping.
</success_criteria>

<output>
Create `.planning/phases/80-page-migration-wave-2-jobs-forensics/80-16-SUMMARY.md` only after both tasks pass. A `gaps_found` Task 80-16-01 must stop without this file and without requirement, roadmap, or terminal STATE completion.
</output>
