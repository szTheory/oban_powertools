---
phase: 80-page-migration-wave-2-jobs-forensics
plan: 14
type: execute
wave: 12
depends_on:
  - 80-13
files_modified:
  - lib/oban_powertools/forensics.ex
  - test/oban_powertools/forensics_test.exs
  - test/oban_powertools/web/live/forensics_live_test.exs
autonomous: true
gap_closure: true
requirements: []
must_haves:
  truths:
    - "A workflow-step Forensics scope becomes authoritative only when one Step row matches the supplied resource_id, workflow_id, and step name together."
    - "A workflow-step ID from workflow B cannot be combined with workflow A authorization or diagnosis, cannot issue a foreign Audit read, and returns the same Evidence unavailable presentation as any other unavailable valid scope."
    - "Audit identity for a workflow-step window is derived from the validated Step record rather than reused from raw URL selectors."
    - "Existing root-workflow and valid workflow-step evidence remain bounded, newest-first, redaction-safe, and behaviorally unchanged."
  artifacts:
    - path: "lib/oban_powertools/forensics.ex"
      provides: "Relationally validated workflow-step evidence dispatch before Audit access"
    - path: "test/oban_powertools/forensics_test.exs"
      provides: "Two-workflow context regression proving foreign step evidence is unavailable and unread"
    - path: "test/oban_powertools/web/live/forensics_live_test.exs"
      provides: "Uniform connected Evidence unavailable regression for the forged cross-workflow selector"
  key_links:
    - from: "lib/oban_powertools/forensics.ex"
      to: "ObanPowertools.Workflow.Step"
      via: "one Ecto predicate over id, workflow_id, and step_name"
      pattern: "step.id.*step.workflow_id.*step.step_name"
    - from: "lib/oban_powertools/forensics.ex"
      to: "ObanPowertools.Audit.forensic_window/2"
      via: "a Scope rebuilt from the validated Step identity"
      pattern: "Audit.forensic_window"
    - from: "test/oban_powertools/forensics_test.exs"
      to: "lib/oban_powertools/forensics.ex"
      via: "workflow A authorization context plus workflow B step UUID adversarial fixture"
      pattern: "workflow_step"
---

<objective>
Close CR-01 by making workflow-step Forensics evidence relationally authoritative before any Audit lookup.

Purpose: prevent an actor authorized for workflow A from combining workflow A's diagnosis with workflow B's step chronology.
Output: exact three-column step resolution, validated Audit identity, and context plus connected regressions for uniform non-disclosing failure.
</objective>

<execution_context>
@/Users/jon/.agents/gsd-core/workflows/execute-plan.md
@/Users/jon/.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/80-page-migration-wave-2-jobs-forensics/80-CONTEXT.md
@.planning/phases/80-page-migration-wave-2-jobs-forensics/80-RESEARCH.md
@.planning/phases/80-page-migration-wave-2-jobs-forensics/80-PATTERNS.md
@.planning/phases/80-page-migration-wave-2-jobs-forensics/80-UI-SPEC.md
@.planning/phases/80-page-migration-wave-2-jobs-forensics/80-VERIFICATION.md
@.planning/phases/80-page-migration-wave-2-jobs-forensics/80-05-SUMMARY.md
@.planning/phases/80-page-migration-wave-2-jobs-forensics/80-06-SUMMARY.md
@.planning/phases/80-page-migration-wave-2-jobs-forensics/80-07-SUMMARY.md
</context>

## Must Haves — Gap and Decision Coverage

- **CR-01 / D-61, D-67, D-68, D-77, D-88:** syntactic scope acceptance is not authority; a workflow-step selector must resolve one row under all three supplied identities before evidence access.
- **PAGE-09:** Forensics may render only evidence belonging to the authorized, selected workflow scope.
- **PAGE-10:** the correction stays in the existing `Forensics.bundle/2` → closed presenter → `ForensicsLive.page_content/1` composition path.

## Artifacts this phase produces

- New private `resolve_workflow_step/3` in `lib/oban_powertools/forensics.ex`; it performs the exact `Step.id == resource_id AND Step.workflow_id == workflow_id AND Step.step_name == step` lookup and returns no raw selector as authority.
- New private `validated_workflow_step_scope/1` in `lib/oban_powertools/forensics.ex`; it rebuilds the workflow-step `Scope` from the resolved Step row for `Audit.forensic_window/2`.
- New adversarial test fixtures/assertions in the two existing Forensics test files; no new public symbol, file, route, field, flag, schema, migration, dependency, presenter shape, story, ARIA snapshot, or PNG is created.

<tasks>

<task type="auto" tdd="true">
  <name>Task 80-14-01: Relationally validate workflow-step evidence before Audit access</name>
  <files>lib/oban_powertools/forensics.ex, test/oban_powertools/forensics_test.exs, test/oban_powertools/web/live/forensics_live_test.exs</files>
  <read_first>
    - @lib/oban_powertools/forensics.ex
    - @lib/oban_powertools/forensics/scope.ex
    - @lib/oban_powertools/audit.ex
    - @lib/oban_powertools/workflow/step.ex
    - @lib/oban_powertools/workflow/workflow.ex
    - @lib/oban_powertools/web/forensics_live.ex
    - @test/oban_powertools/forensics_test.exs
    - @test/oban_powertools/web/live/forensics_live_test.exs
    - @test/oban_powertools/audit_test.exs
    - @.planning/phases/80-page-migration-wave-2-jobs-forensics/80-VERIFICATION.md
    - @.planning/phases/80-page-migration-wave-2-jobs-forensics/80-CONTEXT.md
  </read_first>
  <action>Write the adversarial regressions first. Insert workflow A and workflow B, give A and B distinct Step rows and distinct Audit events, then submit a syntactically valid `%Scope{kind: :workflow, resource_type: "workflow_step"}` containing A's workflow ID and step name but B's step UUID. Assert `Forensics.bundle/2` returns exactly `{:unavailable, "Evidence unavailable"}`; capture Audit-repository queries and assert no `oban_powertools_audit_events` query occurs for the mismatch; assert B's action, actor, status, timestamp, ID, and sentinel note are absent. Add a LiveView regression using the same selector and actor capability shape, and compare its unavailable heading/body/destination set byte-for-byte with an ordinary unavailable valid workflow-step scope.

Then change only the workflow branch in `Forensics.workflow_bundle/2`. For a `resource_type: "workflow_step"` scope, call new private `resolve_workflow_step/3` with the supplied `resource_id`, `workflow_id`, and `step`; its single Step query must contain all three equality predicates. If it returns nil, return `{:unavailable, "Evidence unavailable"}` before `Audit.forensic_window/2`, presenter work, destination derivation, or any fallback to another step. If it returns a Step, use that exact row as `selected_step` and pass `validated_workflow_step_scope/1`—rebuilt with `resource_type: "workflow_step"`, `resource_id: selected_step.id`, `workflow_id: selected_step.workflow_id`, and `step: selected_step.step_name`—to `Audit.forensic_window/2`. Preserve the current root-workflow behavior for scopes without a workflow-step resource pair. Do not change `Scope.parse/1` into a repository boundary, loosen the six-key grammar, add a query to rendering, or expose whether a relationship, workflow, step, or authorization check failed.</action>
  <verify>
    <automated>mix test test/oban_powertools/forensics_test.exs test/oban_powertools/web/live/forensics_live_test.exs --seed 0</automated>
    <automated>mix test test/oban_powertools/audit_test.exs test/oban_powertools/forensics/evidence_bundle_test.exs --seed 0</automated>
    <automated>mix format --check-formatted lib/oban_powertools/forensics.ex test/oban_powertools/forensics_test.exs test/oban_powertools/web/live/forensics_live_test.exs</automated>
    <automated>git diff --check -- lib/oban_powertools/forensics.ex test/oban_powertools/forensics_test.exs test/oban_powertools/web/live/forensics_live_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - Source contains one workflow-step lookup whose Ecto predicate simultaneously constrains `step.id`, `step.workflow_id`, and `step.step_name`.
    - A valid same-workflow triple returns the selected Step and only that step's bounded Audit chronology; existing root-workflow scopes retain their current behavior.
    - The workflow-A/workflow-B mismatch returns exactly `{:unavailable, "Evidence unavailable"}` before any Audit query and exposes none of workflow B's event fields.
    - Connected missing and cross-workflow mismatch output have identical `Evidence unavailable` heading, body, and authorized-destination set.
    - The Audit scope passed after successful resolution is rebuilt from the Step row; no raw foreign `resource_id` survives as query authority.
    - All four focused files pass with no skipped test, raw selector fallback, hidden precedence, new route, or unbounded read.
  </acceptance_criteria>
  <done>Workflow-step Forensics evidence is relationally bound to the authorized workflow and cannot disclose or mislabel another workflow's Audit chronology.</done>
</task>

</tasks>

<threat_model>
## ASVS L1 Assets

- Workflow and step existence, Audit action/actor/status/timing confidentiality, authorization scope, diagnosis integrity, and uniform unavailable output.

## Trust Boundaries

| Boundary | Untrusted input / protected output |
|---|---|
| Six URL selectors -> Forensics context | attacker-composed workflow/step/resource tuple -> one relationally validated scope or uniform unavailable |
| Validated Step -> Audit | database-owned identity -> bounded chronology for that exact step |
| Forensics result -> LiveView | missing/unauthorized/mismatched causes -> byte-equivalent non-enumerating presentation |

## STRIDE Threat Register — blocking threshold: high

| Threat ID | Category | Severity | Disposition | Mitigation and verification |
|---|---|---|---|---|
| T-80-14-CR01 | Elevation of privilege | critical | mitigate | A three-predicate Step query binds resource, workflow, and name before Audit; a two-workflow regression proves the prior authorization bypass is closed. |
| T-80-14-CONF | Information disclosure | critical | mitigate | Mismatches return before Audit and tests prohibit foreign action, actor, status, time, ID, and notes in context or DOM output. |
| T-80-14-SPOOF | Spoofing | high | mitigate | Audit identity is rebuilt from the database Step record, preventing a false workflow-A diagnosis over workflow-B chronology. |
| T-80-14-ENUM | Information disclosure | high | mitigate | Missing, unauthorized, and relationally mismatched scopes share exact unavailable copy and destination absence. |
| T-80-14-BOUND | Denial of service | high | mitigate | Exact Step lookup plus existing 50-row Audit window remain database-bounded; no list-all or render-time query is added. |
</threat_model>

<verification>
- Run both Forensics suites plus Audit and EvidenceBundle compatibility suites.
- Inspect query capture to prove the adversarial mismatch performs the exact Step resolution and zero Audit-event reads.
- Inspect the scoped diff for only the declared three files and confirm no ORM schema, migration, dependency, route, story, baseline, or generated asset is present.
</verification>

<success_criteria>
- CR-01 is reproduced red and closed green by the same two-workflow fixture.
- Valid workflow-step evidence is derived from one relationally validated Step; invalid relationships fail before Audit.
- Context and connected presentation remain uniform, bounded, redaction-safe, and compatible.
- No high or critical threat remains unresolved.
</success_criteria>

<output>
Create `.planning/phases/80-page-migration-wave-2-jobs-forensics/80-14-SUMMARY.md` when done.
</output>
