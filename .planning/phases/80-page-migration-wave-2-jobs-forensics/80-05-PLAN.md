---
phase: 80-page-migration-wave-2-jobs-forensics
plan: 05
type: execute
wave: 4
depends_on:
  - 80-01
  - 80-03
files_modified:
  - lib/oban_powertools/audit.ex
  - lib/oban_powertools/forensics/scope.ex
  - lib/oban_powertools/web/selectors.ex
  - test/oban_powertools/audit_test.exs
  - test/oban_powertools/forensics_test.exs
  - test/oban_powertools/web/selectors_test.exs
autonomous: true
requirements:
  - PAGE-09
  - DATA-*
  - PAGE-10
  - A11Y-*
must_haves:
  truths:
    - "Forensics accepts exactly one typed Workflow, Lifeline incident, Cron entry, or Limiter scope family and rejects mixed/orphan/unknown scopes before any evidence read."
    - "Workflow and incident Audit evidence is filtered and bounded in Ecto with stable newest-first ordering; no list_all plus in-memory filter remains."
    - "Workflow windows report exact shown/total/has-more truth; incident windows report bounded shown/has-more truth without claiming an exact total for the unindexed JSONB predicate."
    - "This foundation wave is independently green and creates no evidence presenter, LiveView composition, schema, migration, index, dependency, or new source."
  artifacts:
    - path: "lib/oban_powertools/forensics/scope.ex"
      provides: "Typed six-key Forensics URL grammar"
      exports: ["parse/1", "canonical_params/1"]
    - path: "lib/oban_powertools/audit.ex"
      provides: "Stable bounded forensic Audit window"
      exports: ["forensic_window/2"]
  key_links:
    - from: "lib/oban_powertools/forensics/scope.ex"
      to: "lib/oban_powertools/forensics.ex"
      via: "only {:ok, %Scope{}} may reach a source query"
      pattern: "Scope"
---

<objective>
Replace hidden Forensics selector precedence and unbounded Audit access with a strict typed scope grammar and stable bounded query foundation.

Purpose: make invalid/mixed scopes fail before reads and give the next plan a measured database-owned window contract.
Output: strict Scope parser, canonical six-key links, bounded workflow/incident Audit windows, and query-plan evidence.
</objective>

<execution_context>
@/Users/jon/.agents/gsd-core/workflows/execute-plan.md
@/Users/jon/.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/80-page-migration-wave-2-jobs-forensics/80-CONTEXT.md
@.planning/phases/80-page-migration-wave-2-jobs-forensics/80-RESEARCH.md
@.planning/phases/80-page-migration-wave-2-jobs-forensics/80-PATTERNS.md
@.planning/phases/80-page-migration-wave-2-jobs-forensics/80-UI-SPEC.md
@.planning/phases/80-page-migration-wave-2-jobs-forensics/80-VALIDATION.md
@.planning/phases/80-page-migration-wave-2-jobs-forensics/80-01-SUMMARY.md
@.planning/phases/80-page-migration-wave-2-jobs-forensics/80-03-SUMMARY.md
@.planning/phases/79-page-migration-wave-1-overview-cron-limiters-audit/79-06-SUMMARY.md
</context>

## Must Haves — Decision Coverage

- **D-05, D-53, D-58, D-59, D-60, D-61, D-62:** read-only full-page boundary, four human types, six keys, submit semantics, mixed-scope rejection, and no search/guessing.
- **D-63, D-65, D-66, D-67:** stable newest-first bounded evidence, honest exact-versus-has-more coverage, no seventh key, and scoped Ecto.
- **D-68, D-72:** context/query/selector ownership and supported canonical destinations are established without page composition.
- **D-73, D-74, D-76, D-77, D-80:** idiomatic context boundaries, no schema/dependency/router surprise, fail-before-read behavior, and bounded source input.

## Artifacts this phase produces

- New module/file `ObanPowertools.Forensics.Scope` at `lib/oban_powertools/forensics/scope.ex`.
- New struct fields `kind, resource_type, resource_id, workflow_id, step, incident_fingerprint, view`.
- New public functions `Scope.parse/1` and `Scope.canonical_params/1`; parse returns only `{:empty, [], notice}`, `{:ok, %Scope{}, params}`, or `{:invalid, [], notice}`.
- Accepted resource types are exactly `workflow`, `workflow_step`, `job` only as consistent context inside workflow/incident families, plus standalone `cron_entry` and `limiter`; incident `view` is only `active|resolved`.
- New public `Audit.forensic_window/2`, fixed at 50 events. Relational scope output fields: `events, shown_count, total_count, has_more?`; incident output uses `total_count: nil` and a 51-row has-more probe.
- `Selectors.forensic_path/1` becomes a closed ordered encoder for exactly `resource_type, resource_id, workflow_id, step, incident_fingerprint, view`.
- New internal constant `@forensic_event_limit 50` and the existing Cron/Limiter `@history_limit 8` remain authoritative; note truncation is owned by Plan 80-06.
- No evidence assembly/presenter, migration/index, seventh key, new source, search, causal inference, persistent investigation, dependency, route shape, or mutation is produced.

<tasks>

<task type="auto" tdd="true">
  <name>Task 80-05-01: Implement the typed scope grammar and canonical Forensics URLs</name>
  <files>lib/oban_powertools/forensics/scope.ex, lib/oban_powertools/web/selectors.ex, test/oban_powertools/forensics_test.exs, test/oban_powertools/web/selectors_test.exs</files>
  <read_first>
    - @lib/oban_powertools/forensics/scope.ex (new; confirm absent before creation)
    - @lib/oban_powertools/web/selectors.ex
    - @test/oban_powertools/forensics_test.exs
    - @test/oban_powertools/web/selectors_test.exs
    - @lib/oban_powertools/forensics.ex
    - @lib/oban_powertools/web/forensics_live.ex
    - @lib/oban_powertools/workflow.ex
    - @lib/oban_powertools/lifeline.ex
    - @lib/oban_powertools/forensics/cron_history.ex
    - @lib/oban_powertools/forensics/limiter_history.ex
    - @.planning/phases/80-page-migration-wave-2-jobs-forensics/80-PATTERNS.md
    - @.planning/phases/80-page-migration-wave-2-jobs-forensics/80-UI-SPEC.md
  </read_first>
  <action>Create Scope as a pure sum-type parser over exactly the six keys. Empty values produce the real empty chooser. Workflow requires `workflow_id`, allows optional step and a consistent workflow/workflow_step resource pair. Incident requires fingerprint, allows `view` only active/resolved and a complete consistent job/workflow/workflow_step context pair. Cron and Limiter require their exact resource type plus nonblank resource ID and no workflow/incident/view/step keys. Reject orphan step/view, incomplete pair, unknown resource type, arbitrary standalone job pair, cross-family key, or seventh key with the exact finite `Choose one evidence type` notice and empty canonical params. Canonical params preserve key order and delimiter-safe encoding. Update `Selectors.forensic_path/1` to drop unknown/empty keys, preserve the six-key order, and remain compatible with valid existing callers.</action>
  <verify>
    <automated>mix test test/oban_powertools/forensics_test.exs test/oban_powertools/web/selectors_test.exs --seed 0</automated>
    <automated>mix format --check-formatted lib/oban_powertools/forensics/scope.ex lib/oban_powertools/web/selectors.ex test/oban_powertools/forensics_test.exs test/oban_powertools/web/selectors_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - Table tests cover empty, four valid families, active/resolved incident views, workflow step, and consistent contextual pairs.
    - Every mixed/orphan/incomplete/unknown/arbitrary-job/seventh-key case returns invalid before any repo callback is invoked.
    - Valid canonical params round-trip in exact six-key order with Unicode and delimiter-heavy IDs.
    - Invalid output contains no original conflicting selector values in a destination URL.
    - Scope/Selectors perform no repository, authorization, clock, rendering, inference, or mutation work.
  </acceptance_criteria>
  <done>Forensics has one closed four-family grammar and one exact six-key URL encoder with no hidden precedence.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 80-05-02: Add stable bounded Audit windows and measure incident predicates</name>
  <files>lib/oban_powertools/audit.ex, test/oban_powertools/audit_test.exs</files>
  <read_first>
    - @lib/oban_powertools/audit.ex
    - @test/oban_powertools/audit_test.exs
    - @lib/oban_powertools/forensics/scope.ex
    - @lib/oban_powertools/forensics.ex
    - @test/support/data_case.ex
    - @examples/phoenix_host/priv/repo/migrations/20260522000001_oban_powertools_audit_events.exs
    - @lib/mix/tasks/oban_powertools.install.ex
    - @.planning/phases/80-page-migration-wave-2-jobs-forensics/80-RESEARCH.md
    - @.planning/phases/80-page-migration-wave-2-jobs-forensics/80-VALIDATION.md
  </read_first>
  <action>Add `Audit.forensic_window/2` with explicit repo option and closed typed Scope input. Workflow/workflow-step windows use authoritative resource_type/resource_id predicates, exact count, and SQL limit 50. Incident windows use the authoritative `metadata->>'incident_fingerprint'` predicate, `inserted_at DESC, id DESC`, SQL limit 51, return at most 50 and `has_more?`, and deliberately return `total_count: nil` because the predicate lacks a host-owned index. Event type restrictions, when supplied by source assembly, are finite allowlisted values and remain in SQL. Add telemetry/query capture proving no `list_all/1` path, stable ties, and bounded rows. Generate the incident SQL through the adapter and run local `EXPLAIN (ANALYZE, BUFFERS)` only against deterministic test data when supported; record the plan in test output/summary without asserting production latency or adding an index/migration.</action>
  <verify>
    <automated>mix test test/oban_powertools/audit_test.exs --seed 0</automated>
    <automated>mix format --check-formatted lib/oban_powertools/audit.ex test/oban_powertools/audit_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - 55 matching workflow rows return 50, exact total 55, and has_more true; 55 incident rows return 50, total nil, and has_more true.
    - Both windows order inserted_at DESC/id DESC and never load an unrelated scope.
    - Query capture proves finite SQL limit and no call to `Audit.list_all/1` or post-load Enum.filter.
    - The incident EXPLAIN evidence explicitly notes the unindexed JSONB predicate and local-cardinality limitation.
    - No migration/index/schema/dependency is created and existing Audit.page/fetch/list compatibility tests remain green.
  </acceptance_criteria>
  <done>Forensic Audit evidence is stable and database-bounded, with honest exact-versus-has-more coverage and explicit index limitations.</done>
</task>

</tasks>

<threat_model>
## ASVS L1 Assets

- Scope identity, resource existence, bounded Audit access, evidence provenance/completeness truth, sensitive audit/runbook data, and authorized native destinations.

## Trust Boundaries

| Boundary | Untrusted input / protected output |
|---|---|
| Six URL keys -> Scope | mixed/orphan/forged selectors -> one typed family or empty |
| Scope -> Ecto/source contexts | authorized identity -> bounded stable evidence |

## STRIDE Threat Register — blocking threshold: high

| Threat ID | Category | Severity | Disposition | Mitigation and verification |
|---|---|---|---|---|
| T-80-05-CONFUSION | Tampering | high | mitigate | Sum-type parser rejects mixed precedence/orphans before reads; exact canonical empty replacement is tested. |
| T-80-05-IDOR | Elevation of privilege | high | mitigate | Scoped predicates and uniform unavailable result prevent cross-scope enumeration; page reauthorization remains required. |
| T-80-05-DOS | Denial of service | high | mitigate | SQL limits 50/51, stable indexes where available, existing history limit eight, and no list_all/Enum.filter bound reads/DOM. |
| T-80-05-TRUTH | Repudiation | high | mitigate | Exact workflow totals and incident has-more-only metadata prevent completeness overclaims at the query boundary. |
| T-80-05-PERF | Security misconfiguration | high | mitigate | Incident EXPLAIN records unindexed host-sensitive predicate; no hidden performance promise or surprise migration. |
</threat_model>

<verification>
- Run the Plan 80-05 validation command and inspect query capture/EXPLAIN evidence.
- Prove every invalid scope reaches zero source-query callbacks and every valid scope stays within its source.
- Confirm no evidence presenter, migration/index/dependency/route/new source/search/seventh key/mutation is added.
</verification>

<success_criteria>
- Scope grammar is strict, canonical, and precedence-free.
- Workflow/incident Audit evidence is Ecto-scoped, stable, bounded, and coverage-aware.
- The next plan can assemble four source families only from a typed Scope and bounded Audit API.
- All focused scope/query/selector tests pass; local query limitations are documented; no high threat remains.
</success_criteria>

<output>
Create `.planning/phases/80-page-migration-wave-2-jobs-forensics/80-05-SUMMARY.md` when done.
</output>
