---
phase: 80-page-migration-wave-2-jobs-forensics
plan: 17
type: execute
wave: 14
depends_on:
  - 80-15
  - 80-16
files_modified:
  - lib/oban_powertools/web/jobs_params.ex
  - lib/oban_powertools/web/jobs_live.ex
  - test/oban_powertools/web/jobs_params_test.exs
  - test/oban_powertools/web/live/jobs_live_test.exs
autonomous: true
gap_closure: true
requirements:
  - PAGE-02
must_haves:
  truths:
    - "Every canonical full Jobs detail path ID is parsed through the same signed-int64 boundary as the Jobs quick-review ID before the value can enter authorization or Repo access."
    - "The maximum signed-int64 detail ID 9_223_372_036_854_775_807 remains an accepted deep link, while 9_223_372_036_854_775_808 and larger values render the existing uniform Job unavailable state."
    - "Mount authorization receives exactly %{type: :job, id: canonical_decimal_string} for valid IDs and exactly %{type: :job, id: nil} for malformed or overflow IDs; it never receives an integer or raw path value."
    - "For an otherwise authorized detail viewer, oversized and malformed full-detail paths stay connected, perform zero oban_jobs queries, expose no database encoding failure, and render the same unavailable body as a missing canonical job."
    - "Existing valid and allowlisted-return-context detail behavior remains unchanged, and a viewer denied mount-time :view_job_detail authorization still redirects to /."
  artifacts:
    - path: "lib/oban_powertools/web/jobs_params.ex"
      provides: "One reusable signed-int64-safe canonical Jobs ID parser for quick-review and full-detail paths"
    - path: "lib/oban_powertools/web/jobs_live.ex"
      provides: "Canonical decimal-string valid resources, one nil parse-failure sentinel, and bounded repository loading with the existing unavailable presentation"
    - path: "test/oban_powertools/web/jobs_params_test.exs"
      provides: "Direct canonical Jobs ID parser boundary regressions"
    - path: "test/oban_powertools/web/live/jobs_live_test.exs"
      provides: "Connected maximum/overflow detail-path, authorization-input, uniform-unavailable, and zero-query regressions"
  key_links:
    - from: "lib/oban_powertools/web/jobs_live.ex"
      to: "lib/oban_powertools/web/jobs_params.ex"
      via: "mount-time authorization converts parser success to a canonical decimal-string job resource or failure to the nil sentinel, and detail loading consumes only parser success"
      pattern: "JobsParams\\.(parse|normalize)_job_id"
    - from: "lib/oban_powertools/web/jobs_live.ex"
      to: "lib/oban_powertools/jobs.ex"
      via: "only a successfully normalized signed-int64 ID may reach Jobs.get/2"
      pattern: "Jobs\\.get\\(repo\\(\\), normalized_id\\)"
    - from: "test/oban_powertools/web/live/jobs_live_test.exs"
      to: "Ecto repository query telemetry"
      via: "capture_job_queries/1 proves overflow is rejected before an oban_jobs query"
      pattern: "capture_job_queries"
---

<objective>
Close WR-01 at the canonical full Jobs detail deep-link boundary.

Purpose: prevent positive integers above PostgreSQL/Postgrex signed `bigint` range from entering host authorization or `Repo.get/2`, while preserving the existing non-enumerating detail unavailable state and every valid deep link.
Output: one shared canonical Jobs ID parser used by quick review and full detail, plus connected boundary and zero-query regressions.
</objective>

<execution_context>
@/Users/jon/.agents/gsd-core/workflows/execute-plan.md
@/Users/jon/.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/80-page-migration-wave-2-jobs-forensics/80-CONTEXT.md
@.planning/phases/80-page-migration-wave-2-jobs-forensics/80-RESEARCH.md
@.planning/phases/80-page-migration-wave-2-jobs-forensics/80-PATTERNS.md
@.planning/phases/80-page-migration-wave-2-jobs-forensics/80-UI-SPEC.md
@.planning/phases/80-page-migration-wave-2-jobs-forensics/80-15-SUMMARY.md
@.planning/phases/80-page-migration-wave-2-jobs-forensics/80-16-SUMMARY.md
@.planning/phases/80-page-migration-wave-2-jobs-forensics/80-VERIFICATION.md
</context>

## Must Haves — WR-01 Coverage

- **WR-01 / PAGE-02 / D-09, D-25, D-77:** canonical full-detail deep links accept only positive signed-int64 IDs before authorization and database work; for an otherwise authorized detail viewer, overflow and malformed paths share the existing missing-job unavailable result rather than crashing, redirecting, or disclosing existence detail. **D-19 remains scoped to quick review and is not extended to mount-time full-detail denial.**
- **D-14:** the seven allowlisted Jobs list return parameters and deterministic Back to Jobs link remain unchanged.
- **D-29:** the fixed list pagination and query protocol remain unchanged; this plan changes only canonical Jobs resource-ID normalization.

## Artifacts this phase produces

- One reusable production parser in `ObanPowertools.Web.JobsParams` for positive Jobs IDs in `1..9_223_372_036_854_775_807`; the existing quick-review parse path and the full-detail path both consume it.
- `JobsLive` no longer owns a separate unbounded `normalize_detail_job_id/1`; mount-time resource authorization receives exactly `%{type: :job, id: Integer.to_string(normalized_id)}` on parser success or the single finite non-resource sentinel `%{type: :job, id: nil}` on parser failure, and `Jobs.get/2` receives only a successful normalized integer.
- Existing Jobs parameter and connected LiveView test files gain maximum, maximum-plus-one, very-large, authorization-input, byte-equivalent unavailable-state, and query-telemetry assertions.
- No new route, URL key, page state, operator copy, mutation, public domain capability, schema, migration, dependency, configuration, fixture, browser story, ARIA snapshot, PNG, or visual baseline is created.

<tasks>

<task type="auto" tdd="true">
  <name>Task 80-17-01: Reuse the signed-int64 Jobs ID parser for canonical full detail</name>
  <files>lib/oban_powertools/web/jobs_params.ex, lib/oban_powertools/web/jobs_live.ex, test/oban_powertools/web/jobs_params_test.exs, test/oban_powertools/web/live/jobs_live_test.exs</files>
  <read_first>
    - @lib/oban_powertools/web/jobs_params.ex
    - @lib/oban_powertools/web/jobs_live.ex
    - @lib/oban_powertools/web/live_auth.ex
    - @lib/oban_powertools/auth.ex
    - @lib/oban_powertools/jobs.ex
    - @test/oban_powertools/web/jobs_params_test.exs
    - @test/oban_powertools/web/live/jobs_live_test.exs
    - @test/support/test_auth.ex
    - @.planning/phases/80-page-migration-wave-2-jobs-forensics/80-VERIFICATION.md
    - @.planning/phases/80-page-migration-wave-2-jobs-forensics/80-15-PLAN.md
    - @.planning/phases/80-page-migration-wave-2-jobs-forensics/80-15-SUMMARY.md
  </read_first>
  <action>Start with red unit and connected regressions. In `JobsParams`, promote the already-established `@max_int64` job-ID normalization into one reusable, documented parser whose success shape is unambiguous (`{:ok, id}` for a positive integer or full-string decimal binary at or below `9_223_372_036_854_775_807`, otherwise `:error`). Keep `parse_url/1` behavior unchanged by adapting its optional `job` handling around that parser: absent `job` remains valid and produces `quick_review_id: nil`, while zero, negative, malformed, prefix/suffix, maximum-plus-one, and arbitrarily large values retain the existing invalid-URL canonicalization and notice.

In `JobsLive`, remove the separate unbounded `normalize_detail_job_id/1` implementation. Parse the `:show` path ID through the shared `JobsParams` parser before constructing the resource passed to `LiveAuth.authorize_page/3`; a raw malformed or oversized path string must never reach the host authorization callback. Fix the authorization resource contract exactly: parser success produces `%{type: :job, id: Integer.to_string(normalized_id)}` (therefore the maximum is `%{type: :job, id: "9223372036854775807"}`), while every parser failure produces the one bounded, non-resource-bearing sentinel `%{type: :job, id: nil}`. Do not use an integer ID, the raw path, an overflow string, or any other sentinel. The host policy remains authoritative: a viewer denied `:view_job_detail` at mount still redirects to `/` through the existing `authorize_page/3` behavior. For an otherwise authorized detail viewer, the host policy must treat the nil sentinel as the page-level detail permission check and return `:ok`, allowing `handle_params/3` to render the existing unavailable state; document this integration expectation through the recording test policy rather than bypassing authorization. Reuse the same parser in `load_job_detail/3`; on success, perform the existing resource-level `LiveAuth.authorized?/3` check with `%{type: :job, id: Integer.to_string(normalized_id)}`, then keep `Jobs.get(repo(), normalized_id)` strictly after successful normalization and resource authorization. Parser failure stops before that second resource check and before Repo access. Do not clamp, truncate, partially parse, rescue a Postgrex error, add a preliminary existence query, or change unavailable copy.

In `jobs_params_test.exs`, assert the shared parser directly at `9_223_372_036_854_775_807`, at maximum plus one, and for integer/binary malformed classes while retaining existing `parse_url/1` quick-review coverage. In `jobs_live_test.exs`, temporarily configure a test-only recording auth module (restoring the prior `:auth_module` on exit) whose `authorize/3` records every call, follows the existing actor permission list, and explicitly returns `:ok` for `:view_job_detail` with `%{type: :job, id: nil}` only when that actor otherwise has `:view_job_detail`; actors lacking the permission still receive `{:error, :unauthorized}` and redirect at mount. Prove that the maximum path records `%{type: :job, id: "9223372036854775807"}`—never the integer—and issues exactly one bounded `oban_jobs` lookup before rendering unavailable when absent. Prove that maximum-plus-one, a 50-digit ID, and a malformed ID each record exactly the nil-sentinel mount resource—never their raw values—issue zero `oban_jobs` queries, stay connected for the permitted viewer, and render the exact same normalized `#job-unavailable` fragment and body text as a missing canonical detail target under that same permitted policy. Keep the existing no-permission detail test as a separate mount-denial assertion requiring `{:error, {:redirect, %{to: "/"}}}`; do not compare that redirect with unavailable HTML. Assert no oversized value, Postgrex/Ecto exception, invalid-filter copy, or policy/existence distinction appears in the connected unavailable HTML. Preserve valid detail, mount-time unauthorized redirect, return-context allowlist, quick-review, and action tests.</action>
  <acceptance_criteria>
    - The shared canonical parser accepts integer and binary `9_223_372_036_854_775_807` and rejects `9_223_372_036_854_775_808`, larger integers/binaries, zero, negatives, whitespace, partial decimals, and non-decimals.
    - `JobsParams.parse_url/1` retains its current absent/valid/invalid quick-review semantics and canonical URL notice behavior.
    - Full-detail mount authorization never receives an integer or raw unparsed path ID: every valid ID uses `%{type: :job, id: canonical_decimal_string}`, including `%{type: :job, id: "9223372036854775807"}` at the maximum, and every malformed/overflow ID uses only `%{type: :job, id: nil}`.
    - The recording auth policy permits the nil sentinel only for an actor who otherwise has `:view_job_detail`, records both the maximum-string and nil-sentinel resources, and proves that an actor without `:view_job_detail` still follows the existing mount-time redirect to `/`.
    - A missing maximum-ID detail path performs exactly one `oban_jobs` query and renders Job unavailable; maximum-plus-one and the 50-digit overflow path perform zero `oban_jobs` queries and render the same unavailable fragment/body.
    - Overflow does not crash or disconnect LiveView, does not expose database/auth failures, and does not add invalid-filter copy to the detail surface.
    - Existing valid detail loading, mount-time unauthorized-viewer redirect, deterministic Back to Jobs allowlist, quick-review missing/unauthorized indistinguishability, action authorization, and all Jobs parameter tests remain green.
    - No schema-like file, migration, dependency, route, URL protocol, browser artifact, or prior Plan/Summary file is added or modified.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/oban_powertools/web/jobs_params_test.exs test/oban_powertools/web/live/jobs_live_test.exs --seed 0</automated>
    <automated>mix test test/oban_powertools/jobs_test.exs test/oban_powertools/auth_test.exs test/oban_powertools/web/router_test.exs --seed 0</automated>
    <automated>mix format --check-formatted lib/oban_powertools/web/jobs_params.ex lib/oban_powertools/web/jobs_live.ex test/oban_powertools/web/jobs_params_test.exs test/oban_powertools/web/live/jobs_live_test.exs</automated>
    <automated>git diff --check -- lib/oban_powertools/web/jobs_params.ex lib/oban_powertools/web/jobs_live.ex test/oban_powertools/web/jobs_params_test.exs test/oban_powertools/web/live/jobs_live_test.exs</automated>
    <automated>git diff --name-only --diff-filter=ACMR HEAD -- priv test/support/migrations | test "$(wc -l | tr -d ' ')" = 0</automated>
  </verify>
  <done>Canonical full-detail Jobs IDs are signed-int64 bounded before authorization and repository access, with exact boundary and zero-query overflow evidence.</done>
</task>

</tasks>

<threat_model>
## ASVS L1 Assets

- Jobs detail availability, host authorization integrity, PostgreSQL/Postgrex query safety, non-enumerating unavailable presentation, canonical deep-link behavior, and operator-visible service continuity.

## Trust Boundaries

| Boundary | Untrusted input / protected output |
|---|---|
| Browser path -> LiveView mount | arbitrary decimal/malformed `:id` -> canonical decimal-string signed-int64 identity or the nil sentinel |
| LiveView -> host authorization | attacker-controlled path string -> exactly `%{type: :job, id: canonical_decimal_string}` or `%{type: :job, id: nil}` |
| LiveView -> Jobs/Repo | parsed resource ID -> Postgrex-encodable `bigint` parameter only |
| Repository/auth outcome -> HTML | invalid or missing detail for an authorized viewer -> one non-enumerating unavailable state; mount denial -> existing redirect |

## STRIDE Threat Register — blocking threshold: high

| Threat ID | Category | Severity | Disposition | Mitigation and verification |
|---|---|---|---|---|
| T-80-17-DOS | Denial of service | high | mitigate | Reject the raw value above signed-int64 maximum before authorization, substitute only the nil page-permission sentinel, and never call `Jobs.get/2`; connected overflow regression requires zero `oban_jobs` queries and no crash. |
| T-80-17-AUTH | Tampering | high | mitigate | Mount passes only `%{type: :job, id: canonical_decimal_string}` or `%{type: :job, id: nil}`; detail reauthorization passes only the canonical decimal string. Recording-auth evidence asserts the maximum string and nil sentinel exactly, permits nil only for an otherwise authorized detail viewer, and separately preserves the no-permission mount redirect. |
| T-80-17-ENUM | Information disclosure | high | mitigate | For the same otherwise authorized viewer, overflow, malformed, and missing paths retain the same finite unavailable presentation without existence or database detail; mount-time authorization denial remains an intentionally distinct redirect. |
| T-80-17-DRIFT | Repudiation | medium | mitigate | Exact maximum and maximum-plus-one tests bind the implementation to PostgreSQL signed `bigint` range rather than an undocumented arbitrary cap. |
| T-80-17-SCOPE | Tampering | medium | mitigate | Focused file list and schema/migration guard prohibit route, protocol, persistence, or browser-evidence expansion. |

All high-severity threats are blocking. Any integer/raw oversized ID observed by authorization, any parse-failure resource other than `%{type: :job, id: nil}`, any overflow `oban_jobs` query, any crash/disconnect for an otherwise authorized viewer, any distinguishable malformed/overflow/missing unavailable output for that viewer, or any regression of the mount-denial redirect fails the plan.
</threat_model>

<verification>
- Run the focused parser and connected Jobs suites from a clean test database sandbox with deterministic query telemetry.
- Require exact parser behavior at signed-int64 maximum and maximum plus one for both integer and full-string binary inputs.
- Require the missing maximum detail to perform one encodable lookup, while both maximum-plus-one and a 50-digit path perform zero `oban_jobs` queries.
- With the recording policy and the same otherwise authorized viewer, compare normalized unavailable fragments/body text across oversized, malformed, and missing cases without accepting redirect, exception, invalid-filter, or policy-specific output. Separately assert that a viewer denied `:view_job_detail` at mount still redirects to `/`; do not include that redirect in the unavailable-body comparison.
- Run adjacent Jobs context, Auth, and router suites to catch boundary or route regressions.
- Verify formatting, whitespace, declared file scope, and absence of schema/migration changes.
</verification>

<success_criteria>
- WR-01 is closed exactly: all canonical full Jobs detail IDs are positive signed-int64 values before host resource authorization or Repo binding.
- For an otherwise authorized detail viewer, maximum accepted and overflow deep links remain connected and produce the existing safe detail behavior with exact expected query counts; mount-time denial continues to redirect.
- Quick review and full detail use one canonical Jobs ID parser rather than divergent numeric rules.
- PAGE-02 deep-link preservation is restored without changes to filters, bulk behavior, actions, routes, return context, schemas, dependencies, or visual artifacts.
</success_criteria>

<output>
Create `.planning/phases/80-page-migration-wave-2-jobs-forensics/80-17-SUMMARY.md` after the task and all verification gates pass. Then rerun Phase 80 verification so WR-01 and PAGE-02 are reconciled from actual behavior.
</output>
