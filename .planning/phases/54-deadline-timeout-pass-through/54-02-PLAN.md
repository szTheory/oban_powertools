---
phase: 54-deadline-timeout-pass-through
plan: "02"
type: execute
wave: 2
depends_on:
  - "54-01"
files_modified:
  - lib/oban_powertools/idempotency.ex
  - test/oban_powertools/idempotency_test.exs
autonomous: true
requirements:
  - SAFE-02
must_haves:
  truths:
    - "A worker declaring deadline: :timer.hours(24) stores top-level meta[\"__deadline_at__\"] as an ISO8601 UTC timestamp during enqueue."
    - "Caller meta is preserved while Powertools reserved key meta[\"__deadline_at__\"] wins over host-supplied spoofed values."
    - "Deadline metadata is added after fingerprint generation and does not perturb duplicate detection."
    - "Deadline metadata coexists with existing Powertools limiter/idempotency meta."
  artifacts:
    - path: lib/oban_powertools/idempotency.ex
      provides: "enqueue-time deadline meta merge in ObanPowertools.Idempotency.transaction/3"
      contains: "build_meta"
    - path: test/oban_powertools/idempotency_test.exs
      provides: "SAFE-02 integration coverage for deadline meta, meta precedence, and duplicate detection"
      contains: "__deadline_at__"
  key_links:
    - from: lib/oban_powertools/idempotency.ex
      to: lib/oban_powertools/worker/deadlines.ex
      via: "ObanPowertools.Worker.Deadlines.build_meta(deadline_ms, now)"
      pattern: "Deadlines\\.build_meta"
    - from: lib/oban_powertools/idempotency.ex
      to: lib/oban_powertools/worker.ex
      via: "worker_mod.__powertools_deadline_ms__/0"
      pattern: "__powertools_deadline_ms__"
---

<objective>
Add enqueue-time deadline metadata without changing idempotency fingerprints.

Purpose: SAFE-02 requires jobs enqueued through Powertools to carry a concrete wall-clock expiry in Oban meta while preserving existing duplicate suppression and limiter metadata.
Output: Idempotency merge changes and integration tests proving ISO8601 meta, reserved-key precedence, and duplicate behavior.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/54-deadline-timeout-pass-through/54-CONTEXT.md
@.planning/phases/54-deadline-timeout-pass-through/54-RESEARCH.md
@.planning/phases/54-deadline-timeout-pass-through/54-VALIDATION.md
@.planning/phases/54-deadline-timeout-pass-through/54-PATTERNS.md
@.planning/phases/54-deadline-timeout-pass-through/54-01-SUMMARY.md
@lib/oban_powertools/idempotency.ex
@lib/oban_powertools/worker.ex
@lib/oban_powertools/worker/deadlines.ex
@test/oban_powertools/idempotency_test.exs

<interfaces>
Created by Plan 54-01:
- `ObanPowertools.Worker.Deadlines.meta_key/0` returns `"__deadline_at__"`.
- `ObanPowertools.Worker.Deadlines.build_meta(nil, now)` returns `%{}`.
- `ObanPowertools.Worker.Deadlines.build_meta(duration_ms, now)` returns top-level deadline meta using `DateTime.add(now, duration_ms, :millisecond)` and `DateTime.to_iso8601/1`.
- Worker modules using `ObanPowertools.Worker` expose `__powertools_deadline_ms__/0`.

Existing in lib/oban_powertools/idempotency.ex:
- `transaction(worker_mod, args, opts \\ [])`
- `generate_fingerprint(worker_mod, args)` hashes only worker identity and validated args.
- `merge_limits_meta(opts, worker_mod, args, fingerprint)` currently deep-merges caller meta with limiter/idempotency meta.
</interfaces>
</context>

<tasks>

<task id="54-02-T1" type="auto" tdd="true">
  <name>Task 1: Add RED idempotency tests for deadline meta insertion</name>
  <files>test/oban_powertools/idempotency_test.exs</files>
  <read_first>
    @test/oban_powertools/idempotency_test.exs
    @lib/oban_powertools/idempotency.ex
    @lib/oban_powertools/worker.ex
    @lib/oban_powertools/worker/deadlines.ex
    @.planning/phases/54-deadline-timeout-pass-through/54-CONTEXT.md
    @.planning/phases/54-deadline-timeout-pass-through/54-VALIDATION.md
    @.planning/phases/54-deadline-timeout-pass-through/54-PATTERNS.md
    @.planning/phases/54-deadline-timeout-pass-through/54-01-SUMMARY.md
  </read_first>
  <behavior>
    - SAFE-02/D-06: Enqueue through a deadline worker writes `meta["__deadline_at__"]` as an ISO8601 timestamp.
    - D-08: Host-supplied `meta["__deadline_at__"]` is overwritten by the Powertools-computed value while unrelated caller meta remains.
    - D-14/D-16: Duplicate enqueue attempts with different `now:` values return the existing job conflict and do not create a fresh job because the deadline timestamp is not part of the fingerprint.
    - D-25: Deadline metadata coexists with existing `"oban_powertools"` limiter/idempotency meta.
  </behavior>
  <action>Add `DeadlineWorker` using `use ObanPowertools.Worker, args: [id: :integer], deadline: :timer.hours(24)` and `LimitedDeadlineWorker` using the same args plus `limits: [name: "deadline-worker", scope: :partitioned, partition_by: {:args, :id}, bucket_capacity: 10, bucket_span_ms: 60_000]`. Add tests that call `DeadlineWorker.enqueue(%{id: 100}, now: ~U[2026-06-12 12:00:00Z])` and assert `job.meta["__deadline_at__"] == "2026-06-13T12:00:00Z"`. Add a caller-meta test with `meta: %{"source" => "host", "__deadline_at__" => "1999-01-01T00:00:00Z"}` and assert `"source"` remains `"host"` while `"__deadline_at__"` is the computed 2026 value. Add a duplicate test that enqueues the same args twice with `now: ~U[2026-06-12 12:00:00Z]` and `now: ~U[2026-06-13 12:00:00Z]`, then asserts the second result is `{:conflict, job}` with the same id. Add a limited-worker test that asserts both `job.meta["__deadline_at__"]` and `job.meta["oban_powertools"]["idempotency_fingerprint"]` are present. These tests implement D-06, D-08, D-14, D-15, D-16, and D-25.</action>
  <verify>
    <automated>mix test test/oban_powertools/idempotency_test.exs --trace</automated>
  </verify>
  <acceptance_criteria>
    - `test/oban_powertools/idempotency_test.exs` contains `defmodule DeadlineWorker`.
    - `test/oban_powertools/idempotency_test.exs` contains `defmodule LimitedDeadlineWorker`.
    - `test/oban_powertools/idempotency_test.exs` contains `2026-06-13T12:00:00Z`.
    - `test/oban_powertools/idempotency_test.exs` contains `"__deadline_at__" => "1999-01-01T00:00:00Z"`.
    - `test/oban_powertools/idempotency_test.exs` contains `assert {:conflict,`.
    - Before Task 2, the automated command fails because enqueue-time deadline meta is absent.
  </acceptance_criteria>
  <done>RED idempotency tests prove the exact SAFE-02 behavior and fail before production changes.</done>
</task>

<task id="54-02-T2" type="auto" tdd="true">
  <name>Task 2: Merge deadline metadata after fingerprint generation</name>
  <files>lib/oban_powertools/idempotency.ex, test/oban_powertools/idempotency_test.exs</files>
  <read_first>
    @lib/oban_powertools/idempotency.ex
    @lib/oban_powertools/worker.ex
    @lib/oban_powertools/worker/deadlines.ex
    @test/oban_powertools/idempotency_test.exs
    @.planning/phases/54-deadline-timeout-pass-through/54-CONTEXT.md
    @.planning/phases/54-deadline-timeout-pass-through/54-RESEARCH.md
    @.planning/phases/54-deadline-timeout-pass-through/54-PATTERNS.md
    @.planning/phases/54-deadline-timeout-pass-through/54-01-SUMMARY.md
  </read_first>
  <behavior>
    - SAFE-02: `worker_mod.new/2` receives opts whose `:meta` contains `__deadline_at__` for workers with a configured deadline.
    - D-14: `generate_fingerprint/2` remains based only on worker identity and validated args.
    - D-08: `deep_merge(caller_meta, powertools_meta)` gives Powertools reserved keys precedence.
  </behavior>
  <action>In `lib/oban_powertools/idempotency.ex`, keep `fingerprint = generate_fingerprint(worker_mod, casted_args)` exactly before `do_enqueue/5`. Replace the call to `merge_limits_meta(opts, worker_mod, args, fingerprint)` with a helper named `merge_powertools_meta(opts, worker_mod, args, fingerprint)`. In that helper, read `meta = Keyword.get(opts, :meta, %{})`, keep the existing limiter/idempotency meta under `"oban_powertools"`, read `now = Keyword.get(opts, :now, DateTime.utc_now())`, read `deadline_ms` only when `function_exported?(worker_mod, :__powertools_deadline_ms__, 0)` is true, and build deadline meta with `ObanPowertools.Worker.Deadlines.build_meta(deadline_ms, now)`. Merge `limits_meta` and `deadline_meta` into `powertools_meta`, then use `Keyword.put(opts, :meta, deep_merge(meta, powertools_meta))`. Do not put deadline data in args and do not pass a `:deadline` option to `worker_mod.new/2`. This implements D-06, D-08, D-09, D-14, D-15, and D-16.</action>
  <verify>
    <automated>mix test test/oban_powertools/idempotency_test.exs --trace</automated>
  </verify>
  <acceptance_criteria>
    - `lib/oban_powertools/idempotency.ex` contains `merge_powertools_meta`.
    - `lib/oban_powertools/idempotency.ex` contains `ObanPowertools.Worker.Deadlines.build_meta`.
    - `lib/oban_powertools/idempotency.ex` contains `function_exported?(worker_mod, :__powertools_deadline_ms__, 0)`.
    - `lib/oban_powertools/idempotency.ex` still contains `fingerprint = generate_fingerprint(worker_mod, casted_args)` before `do_enqueue`.
    - `lib/oban_powertools/idempotency.ex` does not add `"__deadline_at__"` to the fingerprint payload.
    - `mix test test/oban_powertools/idempotency_test.exs --trace` exits 0.
  </acceptance_criteria>
  <done>SAFE-02 is implemented: deadline workers enqueue jobs with computed top-level deadline meta while existing duplicate and limiter behavior remain stable.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Caller enqueue opts -> Idempotency transaction | Host-provided `opts[:meta]` crosses into persisted Oban job metadata. |
| Worker compile-time config -> enqueue runtime | The generated `__powertools_deadline_ms__/0` value is consumed during job insertion. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-54-01 | Tampering | worker option validation | transfer | Plan 54-01 validates timeout/deadline compile-time options and strips Powertools-only keys before Oban receives opts. |
| T-54-02 | Tampering | `Idempotency.merge_powertools_meta/5` | mitigate | Merge caller meta first and Powertools meta second so host-supplied `__deadline_at__` cannot spoof the worker's declared deadline. |
| T-54-03 | Denial of Service | deadline metadata parser | transfer | Plan 54-01 handles malformed deadline meta in the perform wrapper. |
| T-54-04 | Tampering | Doctor SQL prefix | transfer | Plan 54-03 owns prefix validation for Doctor deadline diagnostics. |
| T-54-05 | Repudiation | Doctor `--strict` behavior | transfer | Plans 54-03 and 54-04 keep expired deadline findings as warnings. |
| T-54-SC | Tampering | package installs | accept | No package installation is part of this plan; RESEARCH.md says no new packages. |
</threat_model>

<verification>
Run the bounded idempotency suite:
`mix test test/oban_powertools/idempotency_test.exs --trace`

Then run the phase sampling command:
`mix test test/oban_powertools/worker_test.exs test/oban_powertools/idempotency_test.exs test/oban_powertools/doctor/checks_test.exs --trace`
</verification>

<success_criteria>
SAFE-02 is complete when deadline workers enqueue a top-level ISO8601 `__deadline_at__`, caller meta is preserved with reserved-key precedence, limiter/idempotency meta remains present, and duplicate detection ignores changing deadline timestamps.
</success_criteria>

<output>
Create `.planning/phases/54-deadline-timeout-pass-through/54-02-SUMMARY.md` when done.
</output>
