---
phase: 54-deadline-timeout-pass-through
plan: "04"
type: execute
wave: 1
depends_on: []
files_modified:
  - guides/workers-and-idempotency.md
  - lib/mix/tasks/oban_powertools.doctor.ex
  - test/oban_powertools/docs_contract_test.exs
  - test/mix/tasks/oban_powertools.doctor_test.exs
autonomous: true
requirements:
  - SAFE-01
  - SAFE-02
  - SAFE-03
  - SAFE-04
must_haves:
  truths:
    - "Worker docs explain that timeout: is an Oban per-attempt kill timeout in milliseconds."
    - "Worker docs explain that deadline: is a Powertools soft pre-run wall-clock expiry and does not interrupt already-running work."
    - "Worker docs state timeout kills may bypass Powertools worker hooks and deadline expiry does not dispatch lifecycle hooks."
    - "Doctor CLI docs list expired deadline jobs as warning findings under both default and --strict modes."
    - "Docs-contract tests lock the support-truth strings so future edits cannot overclaim hard interruption or telemetry."
  artifacts:
    - path: guides/workers-and-idempotency.md
      provides: "Builder-facing timeout/deadline support truth"
      contains: "deadline:"
    - path: lib/mix/tasks/oban_powertools.doctor.ex
      provides: "Doctor CLI severity table and strict-mode support truth"
      contains: "Expired deadline jobs"
    - path: test/oban_powertools/docs_contract_test.exs
      provides: "Docs contract assertions for worker safety support truth"
      contains: "soft pre-run"
    - path: test/mix/tasks/oban_powertools.doctor_test.exs
      provides: "CLI source contract assertions for strict scope"
      contains: "expired_deadline_jobs"
  key_links:
    - from: guides/workers-and-idempotency.md
      to: lib/oban_powertools/worker.ex
      via: "documented timeout:/deadline: worker opts"
      pattern: "timeout:"
    - from: lib/mix/tasks/oban_powertools.doctor.ex
      to: lib/oban_powertools/doctor/checks.ex
      via: "severity table mentions expired_deadline_jobs warning"
      pattern: "expired_deadline"
---

<objective>
Document timeout, deadline, and Doctor strict-mode support truth.

Purpose: Phase 54 adds public worker options and an operator diagnostic, so docs must prevent hard-deadline, hook, telemetry, and strict-mode overclaims.
Output: Worker guide and Doctor CLI docs with source-contract tests.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/54-deadline-timeout-pass-through/54-CONTEXT.md
@.planning/phases/54-deadline-timeout-pass-through/54-RESEARCH.md
@.planning/phases/54-deadline-timeout-pass-through/54-VALIDATION.md
@.planning/phases/54-deadline-timeout-pass-through/54-PATTERNS.md
@.planning/phases/53-worker-lifecycle-hooks/53-02-SUMMARY.md
@guides/workers-and-idempotency.md
@lib/mix/tasks/oban_powertools.doctor.ex
@test/oban_powertools/docs_contract_test.exs
@test/mix/tasks/oban_powertools.doctor_test.exs

<interfaces>
Existing docs-contract surfaces:
- `test/oban_powertools/docs_contract_test.exs` reads `guides/workers-and-idempotency.md` through `@worker_guide_path`.
- `test/mix/tasks/oban_powertools.doctor_test.exs` reads `lib/mix/tasks/oban_powertools.doctor.ex` as source.
- `lib/mix/tasks/oban_powertools.doctor.ex` currently documents `--strict` as scoped to `uniqueness_timeout_risk` only.
</interfaces>
</context>

<tasks>

<task id="54-04-T1" type="auto">
  <name>Task 1: Lock worker timeout and deadline support truth in docs</name>
  <files>guides/workers-and-idempotency.md, test/oban_powertools/docs_contract_test.exs</files>
  <read_first>
    @guides/workers-and-idempotency.md
    @test/oban_powertools/docs_contract_test.exs
    @.planning/phases/54-deadline-timeout-pass-through/54-CONTEXT.md
    @.planning/phases/54-deadline-timeout-pass-through/54-RESEARCH.md
    @.planning/phases/54-deadline-timeout-pass-through/54-PATTERNS.md
    @.planning/phases/53-worker-lifecycle-hooks/53-02-SUMMARY.md
  </read_first>
  <action>Update `guides/workers-and-idempotency.md` so the "What the wrapper adds" list includes `timeout:` and `deadline:`. Add a worker example or paragraph that states these exact support-truth values: `timeout:` is positive integer milliseconds; `timeout:` delegates to Oban's per-attempt `timeout/1` kill timer; Oban timeout kills may bypass Powertools worker hooks; `deadline:` is a soft wall-clock pre-run expiry stored as `meta["__deadline_at__"]`; `deadline:` returns `{:cancel, :deadline_expired}` before `on_start/1` and `process/1` when expired; `deadline:` does not interrupt already-running work; no Powertools deadline telemetry is emitted in this phase. Update `test/oban_powertools/docs_contract_test.exs` with a focused test that reads `@worker_guide_path` and asserts those strings, plus refutes `hard deadline`, `interrupt running work`, and `deadline telemetry`. This implements D-21, D-22, D-23, D-24 support evidence, and D-27.</action>
  <verify>
    <automated>mix test test/oban_powertools/docs_contract_test.exs --trace</automated>
  </verify>
  <acceptance_criteria>
    - `guides/workers-and-idempotency.md` contains `timeout:`.
    - `guides/workers-and-idempotency.md` contains `deadline:`.
    - `guides/workers-and-idempotency.md` contains `meta["__deadline_at__"]`.
    - `guides/workers-and-idempotency.md` contains `{:cancel, :deadline_expired}`.
    - `guides/workers-and-idempotency.md` contains `does not interrupt already-running work`.
    - `test/oban_powertools/docs_contract_test.exs` contains `deadline telemetry`.
    - `mix test test/oban_powertools/docs_contract_test.exs --trace` exits 0.
  </acceptance_criteria>
  <done>Worker guide support truth for SAFE-01 through SAFE-03 is documented and locked by docs-contract tests.</done>
</task>

<task id="54-04-T2" type="auto">
  <name>Task 2: Lock Doctor expired-deadline severity and strict scope</name>
  <files>lib/mix/tasks/oban_powertools.doctor.ex, test/mix/tasks/oban_powertools.doctor_test.exs</files>
  <read_first>
    @lib/mix/tasks/oban_powertools.doctor.ex
    @test/mix/tasks/oban_powertools.doctor_test.exs
    @lib/oban_powertools/doctor.ex
    @lib/oban_powertools/doctor/checks.ex
    @.planning/phases/54-deadline-timeout-pass-through/54-CONTEXT.md
    @.planning/phases/54-deadline-timeout-pass-through/54-RESEARCH.md
    @.planning/phases/54-deadline-timeout-pass-through/54-PATTERNS.md
  </read_first>
  <action>Update the `@moduledoc` in `lib/mix/tasks/oban_powertools.doctor.ex`: change exit-code 1 text to mention `expired deadline jobs`; keep the `--strict` flag text scoped exactly to `uniqueness_timeout_risk check only`; add a severity table row `Expired deadline jobs` with `warning (1)` under Default and `warning (1)` under Under --strict. Update `test/mix/tasks/oban_powertools.doctor_test.exs` with source assertions that the moduledoc contains `Expired deadline jobs`, `warning (1)| warning (1)` or equivalent spacing, `expired_deadline_jobs`, and `Scope: uniqueness_timeout_risk check only`. Add a refute assertion that the source does not contain `expired deadline jobs) to errors` and does not contain `expired_deadline_jobs check only`. This implements D-17, D-19, D-20 support truth, D-26 CLI docs coverage, and D-27.</action>
  <verify>
    <automated>mix test test/mix/tasks/oban_powertools.doctor_test.exs --trace</automated>
  </verify>
  <acceptance_criteria>
    - `lib/mix/tasks/oban_powertools.doctor.ex` contains `Expired deadline jobs`.
    - `lib/mix/tasks/oban_powertools.doctor.ex` contains `Scope: uniqueness_timeout_risk check only`.
    - `lib/mix/tasks/oban_powertools.doctor.ex` contains `warning (1)` for expired deadline jobs under both default and strict columns.
    - `test/mix/tasks/oban_powertools.doctor_test.exs` contains `expired_deadline_jobs`.
    - `test/mix/tasks/oban_powertools.doctor_test.exs` contains `Scope: uniqueness_timeout_risk check only`.
    - `mix test test/mix/tasks/oban_powertools.doctor_test.exs --trace` exits 0.
  </acceptance_criteria>
  <done>Doctor CLI docs make expired deadline warnings visible without broadening strict-mode semantics.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Public docs -> host implementation choices | Documentation shapes how adopters configure worker safety and interpret guarantees. |
| CLI docs -> CI severity expectations | The Doctor moduledoc defines how users expect `--strict` and exit codes to behave. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-54-01 | Information Disclosure | worker docs | mitigate | Document `timeout:` as positive integer milliseconds and Oban-owned `timeout/1`, preventing accidental unsupported job-option leakage claims. |
| T-54-02 | Tampering | enqueue meta docs | transfer | Plan 54-02 tests reserved-key precedence; docs state Powertools writes `meta["__deadline_at__"]`. |
| T-54-03 | Denial of Service | malformed deadline docs | transfer | Plans 54-01 and 54-03 implement malformed-meta tolerance; docs avoid promising hard interruption. |
| T-54-04 | Tampering | Doctor prefix SQL | transfer | Plan 54-03 implements prefix validation; CLI docs remain severity-focused. |
| T-54-05 | Repudiation | Doctor `--strict` moduledoc | mitigate | Source-contract tests lock `--strict` scope to `uniqueness_timeout_risk check only` and expired deadline warnings under both modes. |
| T-54-SC | Tampering | package installs | accept | No package installation is part of this plan; RESEARCH.md says no new packages. |
</threat_model>

<verification>
Run docs-contract and CLI source-contract tests:
`mix test test/oban_powertools/docs_contract_test.exs test/mix/tasks/oban_powertools.doctor_test.exs --trace`
</verification>

<success_criteria>
Support truth is complete when worker docs explain timeout versus deadline semantics, lifecycle hook boundaries, no hard interruption, and no deadline telemetry; Doctor CLI docs list expired deadline jobs as warnings without broadening `--strict`.
</success_criteria>

<output>
Create `.planning/phases/54-deadline-timeout-pass-through/54-04-SUMMARY.md` when done.
</output>
