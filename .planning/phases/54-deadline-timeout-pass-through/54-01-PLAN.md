---
phase: 54-deadline-timeout-pass-through
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/oban_powertools/worker.ex
  - lib/oban_powertools/worker/deadlines.ex
  - test/oban_powertools/worker_test.exs
autonomous: true
requirements:
  - SAFE-01
  - SAFE-03
must_haves:
  truths:
    - "A worker declaring timeout: 5_000 exposes timeout/1 returning 5000, and Oban's executor can consume it as a worker callback."
    - "Invalid timeout: or deadline: declarations fail at compile time with ArgumentError."
    - "Expired meta[\"__deadline_at__\"] returns {:cancel, :deadline_expired} before on_start/1 and process/1."
    - "Missing or malformed meta[\"__deadline_at__\"] allows normal worker execution without crashing."
  artifacts:
    - path: lib/oban_powertools/worker/deadlines.ex
      provides: "Deadline duration normalization, deadline meta key, ISO8601 timestamp building, and defensive expiry parsing"
      exports:
        - "meta_key/0"
        - "normalize_duration!/2"
        - "build_meta/2"
        - "expired?/2"
    - path: lib/oban_powertools/worker.ex
      provides: "timeout:/deadline: option normalization, generated timeout/1, __powertools_deadline_ms__/0, and pre-hook deadline cancellation"
      contains: "__powertools_deadline_ms__"
    - path: test/oban_powertools/worker_test.exs
      provides: "SAFE-01 and SAFE-03 regression tests"
      contains: "deadline_expired"
  key_links:
    - from: lib/oban_powertools/worker.ex
      to: lib/oban_powertools/worker/deadlines.ex
      via: "ObanPowertools.Worker.Deadlines.expired?(job.meta)"
      pattern: "Deadlines\\.expired\\?"
    - from: lib/oban_powertools/worker.ex
      to: deps/oban/lib/oban/queue/executor.ex
      via: "generated timeout/1 callback"
      pattern: "def timeout\\(_job\\)"
---

<objective>
Create the compile-time and perform-wrapper safety contract for worker `timeout:` and `deadline:`.

Purpose: SAFE-01 and SAFE-03 depend on the worker macro owning option validation, Oban callback generation, and deadline pre-run cancellation before host work starts.
Output: Worker runtime support plus focused tests for timeout pass-through, deadline cancellation ordering, and malformed deadline metadata.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/54-deadline-timeout-pass-through/54-CONTEXT.md
@.planning/phases/54-deadline-timeout-pass-through/54-RESEARCH.md
@.planning/phases/54-deadline-timeout-pass-through/54-VALIDATION.md
@.planning/phases/54-deadline-timeout-pass-through/54-PATTERNS.md
@.planning/phases/53-worker-lifecycle-hooks/53-CONTEXT.md
@.planning/phases/53-worker-lifecycle-hooks/53-01-SUMMARY.md
@lib/oban_powertools/worker.ex
@lib/oban_powertools/worker/hooks.ex
@deps/oban/lib/oban/worker.ex
@deps/oban/lib/oban/queue/executor.ex
@test/oban_powertools/worker_test.exs

<interfaces>
From lib/oban_powertools/worker.ex:
- defmacro __using__(opts)
- def perform(%Oban.Job{args: args} = job) when is_map(args)
- def perform(%Oban.Job{args: %Args{}} = job)
- defp __powertools_perform__(%Oban.Job{} = job)
- def enqueue(args, opts \\ [])
- def limit_snapshot(worker_mod, args)
- defp validate_positive_integer!(value, label)

From lib/oban_powertools/worker/hooks.ex:
- def on_start(worker_mod, %Oban.Job{} = job)
- def after_result(worker_mod, %Oban.Job{} = job, result)
- {:cancel, _reason} and {:snooze, _seconds} intentionally return :ok without post-hook dispatch.

From deps/oban/lib/oban/worker.ex and deps/oban/lib/oban/queue/executor.ex:
- @callback timeout(job :: Job.t()) :: :infinity | pos_integer()
- Oban executor calls exec.worker.timeout(exec.job) before worker.perform(job) and starts :timer.exit_after(timeout, TimeoutError.exception(...)) for integer timeouts.
</interfaces>
</context>

<tasks>

<task id="54-01-T1" type="auto" tdd="true">
  <name>Task 1: Add RED worker tests for timeout and deadline wrapper behavior</name>
  <files>test/oban_powertools/worker_test.exs</files>
  <read_first>
    @test/oban_powertools/worker_test.exs
    @lib/oban_powertools/worker.ex
    @lib/oban_powertools/worker/hooks.ex
    @.planning/phases/54-deadline-timeout-pass-through/54-CONTEXT.md
    @.planning/phases/54-deadline-timeout-pass-through/54-VALIDATION.md
    @.planning/phases/54-deadline-timeout-pass-through/54-PATTERNS.md
    @deps/oban/lib/oban/worker.ex
    @deps/oban/lib/oban/queue/executor.ex
  </read_first>
  <behavior>
    - SAFE-01: `StaticTimeoutWorker.timeout(%Oban.Job{})` returns `5_000` for `use ObanPowertools.Worker, timeout: 5_000`.
    - SAFE-01/D-03: A worker that declares `timeout: 5_000` and then defines `timeout/1` returns the host override value, not `5_000`.
    - SAFE-01/D-04: `timeout: 0`, `timeout: -1`, and `timeout: :infinity` raise `ArgumentError` mentioning `:timeout`.
    - SAFE-03/D-11: Past `meta["__deadline_at__"]` returns `{:cancel, :deadline_expired}`.
    - SAFE-03/D-10/D-12: Past `meta["__deadline_at__"]` produces no `on_start`, `process`, `on_success`, `on_failure`, or `on_discard` messages.
    - SAFE-03/D-13: Missing and malformed `meta["__deadline_at__"]` run `process/1` normally.
  </behavior>
  <action>Add worker tests following the existing nested-module style. Add `StaticTimeoutWorker`, `TimeoutOverrideWorker`, and `DeadlineGeneratedWorker` inside `ObanPowertools.WorkerTest`. Extend the local `worker_job/2` helper so it sets `meta: Keyword.get(opts, :meta, %{})` on `%Oban.Job{}`. Test `DeadlineGeneratedWorker.__powertools_deadline_ms__() == 60_000` for `deadline: :timer.seconds(60)`. Use `DateTime.utc_now() |> DateTime.add(-60, :second) |> DateTime.to_iso8601()` for expired meta and `DateTime.utc_now() |> DateTime.add(60, :second) |> DateTime.to_iso8601()` for future meta. Use `refute_receive {:generated_deadline_hook, _, _}` and `refute_receive {:generated_deadline_hook, _, _, _}` to prove expired jobs do not start host work. These tests implement D-01, D-03, D-04, D-05, D-07, D-10, D-11, D-12, D-13, and D-24.</action>
  <verify>
    <automated>mix test test/oban_powertools/worker_test.exs --trace</automated>
  </verify>
  <acceptance_criteria>
    - `test/oban_powertools/worker_test.exs` contains `defmodule StaticTimeoutWorker`.
    - `test/oban_powertools/worker_test.exs` contains `defmodule TimeoutOverrideWorker`.
    - `test/oban_powertools/worker_test.exs` contains `defmodule DeadlineGeneratedWorker`.
    - `test/oban_powertools/worker_test.exs` contains `{:cancel, :deadline_expired}`.
    - `test/oban_powertools/worker_test.exs` contains `__powertools_deadline_ms__`.
    - `test/oban_powertools/worker_test.exs` contains `meta: Keyword.get(opts, :meta, %{})`.
    - Before Task 2, the automated command fails because production support is absent.
  </acceptance_criteria>
  <done>RED tests describe SAFE-01 and SAFE-03 precisely and fail for missing implementation, without modifying production files.</done>
</task>

<task id="54-01-T2" type="auto" tdd="true">
  <name>Task 2: Implement worker timeout and deadline runtime support</name>
  <files>lib/oban_powertools/worker.ex, lib/oban_powertools/worker/deadlines.ex, test/oban_powertools/worker_test.exs</files>
  <read_first>
    @lib/oban_powertools/worker.ex
    @lib/oban_powertools/worker/hooks.ex
    @test/oban_powertools/worker_test.exs
    @.planning/phases/53-worker-lifecycle-hooks/53-CONTEXT.md
    @.planning/phases/53-worker-lifecycle-hooks/53-01-SUMMARY.md
    @.planning/phases/54-deadline-timeout-pass-through/54-CONTEXT.md
    @.planning/phases/54-deadline-timeout-pass-through/54-RESEARCH.md
    @.planning/phases/54-deadline-timeout-pass-through/54-PATTERNS.md
    @deps/oban/lib/oban/worker.ex
    @deps/oban/lib/oban/queue/executor.ex
  </read_first>
  <behavior>
    - SAFE-01: `timeout:` is stripped from Oban worker opts, validated as positive integer milliseconds, generated as `timeout/1`, and remains overridable.
    - SAFE-03: Deadline expiry is checked after args validation/casting and before `ObanPowertools.Worker.Hooks.on_start/2`.
    - D-13: Missing or malformed deadline metadata returns false from deadline expiry parsing and does not crash.
  </behavior>
  <action>Create `ObanPowertools.Worker.Deadlines` with `@moduledoc false` and four functions: `meta_key/0` returns `"__deadline_at__"`; `normalize_duration!(value, label)` returns `value` only when it is an integer greater than 0 and otherwise raises `ArgumentError, "expected #{label} to be a positive integer, got: #{inspect(value)}"`; `build_meta(nil, _now)` returns `%{}` and `build_meta(duration_ms, now)` returns `%{"__deadline_at__" => DateTime.to_iso8601(DateTime.add(now, duration_ms, :millisecond))}` for positive integer milliseconds; `expired?(meta, now \\ DateTime.utc_now())` returns `true` only when `meta` is a map with a binary `"__deadline_at__"` value parseable by `DateTime.from_iso8601/1` and `DateTime.compare(deadline_at, now) == :lt`. In `lib/oban_powertools/worker.ex`, read `timeout_config = Keyword.get(opts, :timeout)` and `deadline_config = Keyword.get(opts, :deadline)` before `oban_opts`; delete `:timeout` and `:deadline` from `oban_opts` per D-02. Validate configured timeout via the existing positive-integer error style per D-04. Normalize configured deadline through `ObanPowertools.Worker.Deadlines.normalize_duration!(deadline_config, ":deadline")` per D-07. Inside the quoted worker, set `@powertools_deadline_ms` and define `__powertools_deadline_ms__/0`. Generate `@impl Oban.Worker def timeout(_job), do: configured_timeout` only when timeout is configured, and make `timeout: 1` overridable per D-01 and D-03. In `__powertools_perform__/1`, check `ObanPowertools.Worker.Deadlines.expired?(job.meta)` before `ObanPowertools.Worker.Hooks.on_start(__MODULE__, job)`; return `{:cancel, :deadline_expired}` on true and run the existing hook/process block on false. Do not emit a new telemetry family per D-23.</action>
  <verify>
    <automated>mix test test/oban_powertools/worker_test.exs --trace</automated>
  </verify>
  <acceptance_criteria>
    - `lib/oban_powertools/worker/deadlines.ex` contains `defmodule ObanPowertools.Worker.Deadlines`.
    - `lib/oban_powertools/worker/deadlines.ex` contains `def meta_key, do: "__deadline_at__"` or an equivalent zero-arity function returning that string.
    - `lib/oban_powertools/worker.ex` deletes both `:timeout` and `:deadline` before `use Oban.Worker`.
    - `lib/oban_powertools/worker.ex` contains `def __powertools_deadline_ms__`.
    - `lib/oban_powertools/worker.ex` contains `def timeout(_job)`.
    - `lib/oban_powertools/worker.ex` contains `defoverridable timeout: 1`.
    - `lib/oban_powertools/worker.ex` calls `ObanPowertools.Worker.Deadlines.expired?(job.meta)` before `ObanPowertools.Worker.Hooks.on_start`.
    - `mix test test/oban_powertools/worker_test.exs --trace` exits 0.
  </acceptance_criteria>
  <done>Worker timeout pass-through and pre-run deadline cancellation satisfy SAFE-01 and SAFE-03, with malformed deadline metadata handled defensively.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Worker module compile -> Oban.Worker macro | Host-supplied `use ObanPowertools.Worker` opts become generated callbacks and Oban worker opts. |
| Oban job meta -> Powertools perform wrapper | Stored JSON metadata may be host-supplied, manually edited, or inserted outside Powertools. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-54-01 | Tampering | `ObanPowertools.Worker.__using__/1` | mitigate | Strip `:timeout` and `:deadline` before `use Oban.Worker`; validate configured `timeout:` as positive integer milliseconds; prove generated `timeout/1` and host override behavior in worker tests. |
| T-54-02 | Tampering | enqueue meta precedence | transfer | Plan 54-02 owns caller-supplied `__deadline_at__` spoofing and reserved-key precedence in `Idempotency.transaction/3`. |
| T-54-03 | Denial of Service | `Worker.Deadlines.expired?/2` and generated `perform/1` | mitigate | Parse `meta["__deadline_at__"]` defensively with `DateTime.from_iso8601/1`; missing or malformed values return normal execution, not a crash. |
| T-54-04 | Tampering | Doctor SQL prefix | transfer | Plan 54-03 owns prefix-safe read-only SQL for expired deadline diagnostics. |
| T-54-05 | Repudiation | Doctor `--strict` behavior | transfer | Plans 54-03 and 54-04 keep expired deadlines at warning severity and lock strict scope. |
| T-54-SC | Tampering | package installs | accept | No npm, pip, cargo, Hex, or other package install is part of this plan; RESEARCH.md package audit says no new packages. |
</threat_model>

<verification>
Run the bounded worker suite:
`mix test test/oban_powertools/worker_test.exs --trace`

Then run the phase sampling command if local feedback is still green:
`mix test test/oban_powertools/worker_test.exs test/oban_powertools/idempotency_test.exs test/oban_powertools/doctor/checks_test.exs --trace`
</verification>

<success_criteria>
SAFE-01 and SAFE-03 are complete when configured timeout callbacks pass, invalid timeout/deadline declarations raise, expired deadline meta cancels before host hooks/process, and malformed deadline meta does not crash the wrapper.
</success_criteria>

<output>
Create `.planning/phases/54-deadline-timeout-pass-through/54-01-SUMMARY.md` when done.
</output>
