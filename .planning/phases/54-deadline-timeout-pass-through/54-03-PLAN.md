---
phase: 54-deadline-timeout-pass-through
plan: "03"
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/oban_powertools/doctor.ex
  - lib/oban_powertools/doctor/checks.ex
  - test/oban_powertools/doctor/checks_test.exs
  - test/oban_powertools/doctor/formatter_test.exs
  - test/oban_powertools/doctor_test.exs
autonomous: true
requirements:
  - SAFE-04
must_haves:
  truths:
    - "`ObanPowertools.Doctor.Checks.expired_deadline_jobs/2` returns warning findings for retryable jobs with parseable past `meta[\"__deadline_at__\"]`."
    - "Retryable jobs with future deadline meta, jobs in non-retryable states, and malformed deadline meta do not crash Doctor."
    - "Invalid prefixes are rejected before identifier interpolation."
    - "Doctor.run/2 includes expired deadline findings and keeps them as warnings even when `strict: true`."
    - "Doctor JSON output keeps `schema_version: 1` and renders `check: \"expired_deadline_jobs\"` through the existing finding shape."
  artifacts:
    - path: lib/oban_powertools/doctor/checks.ex
      provides: "prefix-aware read-only expired deadline diagnostic"
      exports:
        - "expired_deadline_jobs/2"
    - path: lib/oban_powertools/doctor.ex
      provides: "Doctor check composition including expired_deadline_jobs"
      contains: "Checks.expired_deadline_jobs"
    - path: test/oban_powertools/doctor/checks_test.exs
      provides: "SAFE-04 diagnostic query coverage"
      contains: "expired_deadline_jobs"
    - path: test/oban_powertools/doctor/formatter_test.exs
      provides: "SAFE-04 formatter schema-stability coverage"
      contains: "expired_deadline_jobs"
  key_links:
    - from: lib/oban_powertools/doctor.ex
      to: lib/oban_powertools/doctor/checks.ex
      via: "Checks.expired_deadline_jobs(repo, prefix)"
      pattern: "expired_deadline_jobs"
    - from: lib/oban_powertools/doctor/checks.ex
      to: oban_jobs.meta
      via: "read-only SQL selecting retryable rows with meta key __deadline_at__"
      pattern: "meta.*__deadline_at__"
---

<objective>
Add Doctor diagnostics for expired retryable job deadlines.

Purpose: SAFE-04 requires operators and CI to see retryable jobs whose Powertools deadline has already passed, without making malformed metadata or unsafe prefixes crash the diagnostic run.
Output: A new Doctor check composed into `Doctor.run/2`, plus bounded tests for findings, formatter stability, prefix safety, and strict-mode behavior.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/54-deadline-timeout-pass-through/54-CONTEXT.md
@.planning/phases/54-deadline-timeout-pass-through/54-RESEARCH.md
@.planning/phases/54-deadline-timeout-pass-through/54-VALIDATION.md
@.planning/phases/54-deadline-timeout-pass-through/54-PATTERNS.md
@lib/oban_powertools/doctor.ex
@lib/oban_powertools/doctor/checks.ex
@lib/oban_powertools/doctor/formatter.ex
@test/oban_powertools/doctor/checks_test.exs
@test/oban_powertools/doctor/formatter_test.exs
@test/oban_powertools/doctor_test.exs

<interfaces>
From lib/oban_powertools/doctor.ex:
- `Doctor.run(repo, opts \\ [])` composes checks and passes `strict:` only to `uniqueness_timeout_risk/3`.
- `Doctor.exit_code_for(findings)` returns 1 when only warning findings are present.

From lib/oban_powertools/doctor/checks.ex:
- Public checks return a list of `%ObanPowertools.Doctor.Finding{check:, severity:, message:, remediation:}`.
- `valid_identifier?/1` accepts only prefixes matching `~r/^[a-z_][a-z0-9_]*$/` before interpolating `#{prefix}.oban_jobs`.

From lib/oban_powertools/doctor/formatter.ex:
- Generic findings render in human output.
- JSON output maps findings through `finding_to_map/1` and keeps `schema_version: 1`.
</interfaces>
</context>

<tasks>

<task id="54-03-T1" type="auto" tdd="true">
  <name>Task 1: Add RED Doctor tests for expired deadline findings</name>
  <files>test/oban_powertools/doctor/checks_test.exs, test/oban_powertools/doctor/formatter_test.exs, test/oban_powertools/doctor_test.exs</files>
  <read_first>
    @test/oban_powertools/doctor/checks_test.exs
    @test/oban_powertools/doctor/formatter_test.exs
    @test/oban_powertools/doctor_test.exs
    @lib/oban_powertools/doctor.ex
    @lib/oban_powertools/doctor/checks.ex
    @lib/oban_powertools/doctor/formatter.ex
    @.planning/phases/54-deadline-timeout-pass-through/54-CONTEXT.md
    @.planning/phases/54-deadline-timeout-pass-through/54-VALIDATION.md
    @.planning/phases/54-deadline-timeout-pass-through/54-PATTERNS.md
  </read_first>
  <behavior>
    - SAFE-04/D-17: Expired retryable jobs produce warning findings with `check: :expired_deadline_jobs`.
    - D-18: The check is prefix-aware and validates prefixes before SQL identifier interpolation.
    - D-19: `strict: true` does not promote expired deadline findings to errors.
    - D-20: Malformed `meta["__deadline_at__"]` does not crash Doctor.
    - D-26: Human and JSON formatter tests prove output stability with `schema_version: 1`.
  </behavior>
  <action>In `test/oban_powertools/doctor/checks_test.exs`, add a helper that inserts an Oban job by calling `%{} |> Oban.Job.new(worker: "Example.Worker", queue: :default, meta: meta) |> Ecto.Changeset.change(state: state) |> TestRepo.insert!()`. Add tests for `Checks.expired_deadline_jobs(TestRepo, "public")`: one expired retryable job with `meta: %{"__deadline_at__" => past_iso}` returns one `%Finding{check: :expired_deadline_jobs, severity: :warning}`; a future retryable deadline returns `[]`; an expired available job returns `[]`; malformed meta `%{"__deadline_at__" => "not-a-date"}` returns `[]`; invalid prefix `"bad-prefix;drop"` returns one `%Finding{check: :expired_deadline_jobs, severity: :error}` and a message containing `not a valid identifier`. In `test/oban_powertools/doctor_test.exs`, insert an expired retryable job and assert `Doctor.run(TestRepo, prefix: "public", strict: true)` contains `check == :expired_deadline_jobs` with `severity == :warning`. In `test/oban_powertools/doctor/formatter_test.exs`, add an `@expired_deadline_finding` and assert human output contains `Expired deadline` and JSON output decodes with `schema_version == 1`, `finding["check"] == "expired_deadline_jobs"`, and `finding["severity"] == "warning"`. These tests implement D-17, D-18, D-19, D-20, and D-26.</action>
  <verify>
    <automated>mix test test/oban_powertools/doctor/checks_test.exs test/oban_powertools/doctor/formatter_test.exs test/oban_powertools/doctor_test.exs --trace</automated>
  </verify>
  <acceptance_criteria>
    - `test/oban_powertools/doctor/checks_test.exs` contains `describe "expired_deadline_jobs/2"`.
    - `test/oban_powertools/doctor/checks_test.exs` contains `bad-prefix;drop`.
    - `test/oban_powertools/doctor/checks_test.exs` contains `not-a-date`.
    - `test/oban_powertools/doctor/formatter_test.exs` contains `@expired_deadline_finding`.
    - `test/oban_powertools/doctor_test.exs` contains `strict: true`.
    - Before Task 2, the automated command fails because `expired_deadline_jobs/2` is undefined or not composed.
  </acceptance_criteria>
  <done>RED Doctor tests cover every SAFE-04 validation row and fail before the diagnostic check exists.</done>
</task>

<task id="54-03-T2" type="auto" tdd="true">
  <name>Task 2: Implement prefix-safe expired deadline Doctor check</name>
  <files>lib/oban_powertools/doctor.ex, lib/oban_powertools/doctor/checks.ex, test/oban_powertools/doctor/checks_test.exs, test/oban_powertools/doctor/formatter_test.exs, test/oban_powertools/doctor_test.exs</files>
  <read_first>
    @lib/oban_powertools/doctor.ex
    @lib/oban_powertools/doctor/checks.ex
    @lib/oban_powertools/doctor/formatter.ex
    @test/oban_powertools/doctor/checks_test.exs
    @test/oban_powertools/doctor/formatter_test.exs
    @test/oban_powertools/doctor_test.exs
    @.planning/phases/54-deadline-timeout-pass-through/54-CONTEXT.md
    @.planning/phases/54-deadline-timeout-pass-through/54-RESEARCH.md
    @.planning/phases/54-deadline-timeout-pass-through/54-PATTERNS.md
  </read_first>
  <behavior>
    - SAFE-04: `Doctor.run/2` reports expired retryable deadline jobs as warning findings.
    - D-18: Query is read-only and prefix-aware.
    - D-20: Query errors are findings and malformed deadline values are ignored rather than crashed.
  </behavior>
  <action>In `lib/oban_powertools/doctor/checks.ex`, add `def expired_deadline_jobs(repo, prefix)`. If `valid_identifier?(prefix)` is false, return one `%Finding{check: :expired_deadline_jobs, severity: :error, message: "Cannot check expired deadline jobs: prefix '#{prefix}' is not a valid identifier (must match /^[a-z_][a-z0-9_]*$/)", remediation: "Use a valid Postgres schema name for --prefix (lowercase letters, digits, underscores)."}`. If valid, run a read-only query against `#{prefix}.oban_jobs` selecting `id`, `worker`, and `meta->>$1` where `state = $2` and `meta ? $1`, with parameters `["__deadline_at__", "retryable"]`. Parse each returned deadline string with `DateTime.from_iso8601/1`; ignore parse errors; produce one `%Finding{check: :expired_deadline_jobs, severity: :warning, message: "Expired deadline: retryable job #{id} (#{worker}) has __deadline_at__ #{deadline_iso} in the past", remediation: "Inspect the job, then retry, cancel, discard, or re-enqueue it after confirming whether the work should still run."}` for each parsed deadline where `DateTime.compare(deadline_at, DateTime.utc_now()) == :lt`. On `repo.query/3` error, return one `%Finding{check: :expired_deadline_jobs, severity: :error, message: "Cannot query expired deadline jobs for #{prefix}.oban_jobs: #{inspect(reason)}", remediation: "Check that #{prefix}.oban_jobs exists and the DB is reachable."}`. In `lib/oban_powertools/doctor.ex`, append `Checks.expired_deadline_jobs(repo, prefix)` after `Checks.uniqueness_timeout_risk(repo, prefix, strict: strict)` and do not pass `strict:` to the new check. Do not change `Doctor.Formatter` unless the tests show the generic finding shape is broken.</action>
  <verify>
    <automated>mix test test/oban_powertools/doctor/checks_test.exs test/oban_powertools/doctor/formatter_test.exs test/oban_powertools/doctor_test.exs --trace</automated>
  </verify>
  <acceptance_criteria>
    - `lib/oban_powertools/doctor/checks.ex` contains `def expired_deadline_jobs(repo, prefix)`.
    - `lib/oban_powertools/doctor/checks.ex` contains `check: :expired_deadline_jobs`.
    - `lib/oban_powertools/doctor/checks.ex` contains `meta->>$1`.
    - `lib/oban_powertools/doctor/checks.ex` contains `meta ? $1`.
    - `lib/oban_powertools/doctor/checks.ex` contains `DateTime.from_iso8601`.
    - `lib/oban_powertools/doctor.ex` contains `Checks.expired_deadline_jobs(repo, prefix)`.
    - `lib/oban_powertools/doctor.ex` does not contain `expired_deadline_jobs(repo, prefix, strict:`.
    - `mix test test/oban_powertools/doctor/checks_test.exs test/oban_powertools/doctor/formatter_test.exs test/oban_powertools/doctor_test.exs --trace` exits 0.
  </acceptance_criteria>
  <done>SAFE-04 runtime diagnostics are implemented with warning severity, prefix-safe SQL, malformed-meta tolerance, and formatter schema stability.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI/config prefix -> SQL identifier | User-controlled or host-configured schema prefix is interpolated as a Postgres identifier. |
| Oban job meta -> Doctor parser | Persisted JSON metadata may be malformed, spoofed, or manually inserted. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-54-01 | Tampering | worker option validation | transfer | Plan 54-01 owns timeout/deadline option validation. |
| T-54-02 | Tampering | enqueue meta precedence | transfer | Plan 54-02 owns preventing host `__deadline_at__` spoofing during Powertools enqueue. |
| T-54-03 | Denial of Service | `Checks.expired_deadline_jobs/2` parser | mitigate | Parse deadline strings in Elixir; ignore malformed values; query failures become bounded findings. |
| T-54-04 | Tampering | `Checks.expired_deadline_jobs/2` SQL | mitigate | Validate prefix with `valid_identifier?/1` before interpolation; bind `__deadline_at__` and `retryable` as query parameters. |
| T-54-05 | Repudiation | `Doctor.run/2` strict semantics | mitigate | Compose expired deadline check without `strict:` and test `strict: true` leaves severity as `:warning`. |
| T-54-SC | Tampering | package installs | accept | No package installation is part of this plan; RESEARCH.md says no new packages. |
</threat_model>

<verification>
Run the bounded Doctor runtime suite:
`mix test test/oban_powertools/doctor/checks_test.exs test/oban_powertools/doctor/formatter_test.exs test/oban_powertools/doctor_test.exs --trace`

Then run the validation SAFE-04 command:
`mix test test/oban_powertools/doctor/checks_test.exs test/oban_powertools/doctor/formatter_test.exs test/mix/tasks/oban_powertools.doctor_test.exs --trace`
</verification>

<success_criteria>
SAFE-04 runtime behavior is complete when retryable jobs with expired parseable `__deadline_at__` meta produce warnings, malformed values do not crash, invalid prefixes are blocked, strict mode does not promote the warning, and JSON stays at `schema_version: 1`.
</success_criteria>

<output>
Create `.planning/phases/54-deadline-timeout-pass-through/54-03-SUMMARY.md` when done.
</output>
