---
phase: 50-telemetry-metrics-slo-guide
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - mix.exs
  - test/oban_powertools/telemetry_test.exs
autonomous: true
requirements: [TEL-02]
must_haves:
  truths:
    - "telemetry_metrics and telemetry_poller are declared optional: true in mix.exs, mirroring oban_web"
    - "The library still compiles in a prod tree where the optional deps are absent"
    - "Telemetry.Metrics is loadable under mix test so metrics/0 tests can run"
    - "guides/telemetry-and-slos.md is registered under the Operations groups_for_extras group"
    - "telemetry_test.exs contains failing metrics/0 structural and tag-containment tests (RED)"
  artifacts:
    - path: "mix.exs"
      provides: "Two optional-dep tuples + Operations group entry for the new guide"
      contains: "telemetry_metrics"
    - path: "test/oban_powertools/telemetry_test.exs"
      provides: "metrics/0 structural test + tag-containment (cardinality-safety) test"
      contains: "metrics/0"
  key_links:
    - from: "mix.exs deps/0"
      to: "Telemetry.Metrics module availability under :test/:dev"
      via: "only: [:test, :dev], optional: true on telemetry_metrics"
      pattern: "telemetry_metrics.*optional: true"
    - from: "mix.exs groups_for_extras Operations"
      to: "guides/telemetry-and-slos.md"
      via: "explicit list entry"
      pattern: "guides/telemetry-and-slos.md"
---

<objective>
Lay the Wave 0 foundation for Phase 50: declare the two optional telemetry deps in `mix.exs`
exactly like the existing `oban_web` optional dependency (TEL-02), register the new guide's
ExDoc group entry, make `Telemetry.Metrics` loadable under `mix test`, and scaffold the failing
`metrics/0` tests so the suite has a RED baseline before implementation lands.

Purpose: TEL-02's "optional, gated like oban_web, no runtime cost when absent" must be true before
`metrics/0` is written; the test file must exist (Nyquist) so Wave 1 implementation has a green target.
Output: edited `mix.exs` (deps + groups_for_extras), `mix deps.get` run, two new failing tests in
`test/oban_powertools/telemetry_test.exs`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/50-telemetry-metrics-slo-guide/50-CONTEXT.md
@.planning/phases/50-telemetry-metrics-slo-guide/50-RESEARCH.md
@.planning/phases/50-telemetry-metrics-slo-guide/50-PATTERNS.md
@.planning/phases/50-telemetry-metrics-slo-guide/50-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add optional telemetry deps + Operations guide group to mix.exs</name>
  <files>mix.exs</files>
  <read_first>
    - mix.exs (the file being modified — note line 54 `{:oban_web, "~> 2.10", optional: true}` and the `groups_for_extras` Operations list at lines 79-86)
    - .planning/phases/50-telemetry-metrics-slo-guide/50-PATTERNS.md (§"mix.exs — two optional deps + one groups_for_extras entry" — exact insertion points)
    - .planning/phases/50-telemetry-metrics-slo-guide/50-RESEARCH.md (§"mix.exs Changes" + Open Question 3 on the `only: [:test, :dev]` rationale)
  </read_first>
  <action>
    In `deps/0`, immediately after the `{:oban_web, "~> 2.10", optional: true}` line, add two
    new dependency tuples per D-03. Declare `{:telemetry_metrics, "~> 1.0", only: [:test, :dev], optional: true}`
    — the `only: [:test, :dev]` makes the `Telemetry.Metrics` module loadable under `mix test` so the
    metrics/0 tests can call it, while `optional: true` preserves the zero-runtime-dep promise for hosts.
    Declare `{:telemetry_poller, "~> 1.0", optional: true}` with no env restriction (guide-documentation
    only; no library or test code references it). Keep both as `optional: true` to mirror the `oban_web`
    posture exactly per TEL-02. Do NOT touch the existing `{:telemetry, "~> 1.4"}` hard dep. In `docs/0`,
    add the string `"guides/telemetry-and-slos.md"` as the second entry of the `Operations:` list in
    `groups_for_extras`, directly after `"guides/optional-oban-web-bridge.md"` per D-06 (its closest
    thematic neighbor). Do not add a stub guide file — the guide is Plan 03's deliverable; this entry
    only reserves its group placement.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/oban_powertools && mix deps.get && grep -v '^#' mix.exs | grep -c 'telemetry_metrics.*optional: true'</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `mix.exs` `deps/0` contains `{:telemetry_metrics, "~> 1.0", only: [:test, :dev], optional: true}` and `{:telemetry_poller, "~> 1.0", optional: true}`.
    - Source assertion: `mix.exs` `groups_for_extras` Operations list contains the string `"guides/telemetry-and-slos.md"`.
    - CLI: `mix deps.get` resolves successfully and `mix.lock` gains `telemetry_metrics` and `telemetry_poller` entries.
    - Behavior: `Code.ensure_loaded?(Telemetry.Metrics)` returns `true` when evaluated under `MIX_ENV=test`.
  </acceptance_criteria>
  <done>Both optional deps declared mirroring oban_web; guide group entry present; deps fetched; Telemetry.Metrics loadable in test env.</done>
</task>

<task type="auto">
  <name>Task 2: Scaffold failing metrics/0 structural + tag-containment tests (RED)</name>
  <files>test/oban_powertools/telemetry_test.exs</files>
  <read_first>
    - test/oban_powertools/telemetry_test.exs (the file being modified — header `defmodule ObanPowertools.TelemetryTest`, `@expected_contract` map at lines 4-18, and the `test "publishes the telemetry public contract"` block at lines 20-22)
    - lib/oban_powertools/telemetry.ex (the frozen `@contract` and `contract/0` — the single source of truth the tag-containment test asserts against; metrics/0 does not yet exist)
    - .planning/phases/50-telemetry-metrics-slo-guide/50-PATTERNS.md (§"test/oban_powertools/telemetry_test.exs — extend with metrics/0 tests" — the two test bodies to copy and their placement)
  </read_first>
  <action>
    Add two new tests to `test/oban_powertools/telemetry_test.exs`, placed immediately after the existing
    `test "publishes the telemetry public contract"` block and before the event-emission tests. These are
    pure data tests with no telemetry attach/detach. Test 1, named `"metrics/0 returns a non-empty list of
    Telemetry.Metrics structs"`: call `ObanPowertools.Telemetry.metrics()`, assert it is a list, assert
    non-empty, and assert every element's `__struct__` is in `[Telemetry.Metrics.Counter, Telemetry.Metrics.Sum]`.
    Test 2, named `"metrics/0 tags stay within frozen contract"` (TEL-01 + SC-4 cardinality safety): fetch
    `ObanPowertools.Telemetry.contract()`, iterate every metric, destructure `metric.event_name` as
    `[_oban_powertools, family, suffix | _]`, resolve `allowed_tags` from `get_in(contract, [:families, family])`
    handling both the flat-list families and the nested per-suffix `workflow` map (`Map.get(per_suffix_map, suffix, [])`),
    and assert every `tag` in `metric.tags` is a member of `allowed_tags` with a failure message naming the
    metric and the allowed set. Do NOT implement `metrics/0` in this plan — these tests MUST fail RED now
    (function undefined) and go green in Plan 02. Reuse the existing module/`use ExUnit.Case, async: false`
    header unchanged.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/oban_powertools && ! mix test test/oban_powertools/telemetry_test.exs 2>&1 | grep -q '0 failures'</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `telemetry_test.exs` contains a test named `"metrics/0 returns a non-empty list of Telemetry.Metrics structs"` and one named `"metrics/0 tags stay within frozen contract"`.
    - Behavior (RED): `mix test test/oban_powertools/telemetry_test.exs` shows the two new tests FAILING (or erroring on undefined `metrics/0`) — this is the expected RED baseline.
    - Source assertion: the tag-containment test handles both flat-list families and the nested `workflow` per-suffix map (a `case`/`get_in` on `[:families, family]`).
    - Source assertion: existing `test "publishes the telemetry public contract"` and all event-emission tests remain unchanged and still pass.
  </acceptance_criteria>
  <done>Two new tests added that fail because metrics/0 is not yet implemented; existing tests untouched and passing.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host deps tree → library compile | Optional deps must not become hard requirements; absence must not break the prod build |
| build/CI → package-manager fetch | `mix deps.get` pulls telemetry_metrics/telemetry_poller from hex.pm |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-50-01 | Denial of Service | mix.exs optional deps | mitigate | Both deps declared `optional: true` (mirroring oban_web); absence verified by `MIX_ENV=prod mix compile` in Plan 02 — no runtime cost or failure when host omits them |
| T-50-SC | Tampering | telemetry_metrics / telemetry_poller hex installs | accept | beam-telemetry org packages, 44M+/45M+ downloads, same org that maintains the already-present `:telemetry` hard dep; RESEARCH.md Package Legitimacy Audit dispositions both Approved (Hex, not npm — slopcheck N/A). No `[ASSUMED]`/`[SUS]`/`[SLOP]` packages — no install checkpoint required |
</threat_model>

<verification>
- `mix deps.get` succeeds and `mix.lock` records both new packages.
- `grep -v '^#' mix.exs | grep -c 'telemetry_metrics.*optional: true'` returns `1`.
- `grep -c 'guides/telemetry-and-slos.md' mix.exs` returns `1`.
- `mix test test/oban_powertools/telemetry_test.exs` shows the two new metrics/0 tests RED (metrics/0 undefined) and all pre-existing tests still passing.
</verification>

<success_criteria>
- TEL-02 dep declarations present and gated exactly like `oban_web`.
- `Telemetry.Metrics` loadable under `mix test`.
- Guide group entry reserved under Operations.
- Failing `metrics/0` tests committed as the RED baseline for Plan 02.
</success_criteria>

<output>
Create `.planning/phases/50-telemetry-metrics-slo-guide/50-01-SUMMARY.md` when done.
</output>
