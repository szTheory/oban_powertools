---
phase: 44-single-job-actions
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified: ["lib/oban_powertools/lifeline.ex", "test/oban_powertools/lifeline_test.exs"]
autonomous: true
requirements: ["QRY-03"]

must_haves:
  truths:
    - "Lifeline supports the job_discard action natively"
    - "Discarded jobs properly transition to the discarded state"
  artifacts:
    - path: "lib/oban_powertools/lifeline.ex"
      provides: "job_discard execution logic"
      contains: "defp next_job_state(\"job_discard\")"
    - path: "test/oban_powertools/lifeline_test.exs"
      provides: "job_discard unit tests"
  key_links:
    - from: "lib/oban_powertools/lifeline.ex"
      to: "Oban.Job"
      via: "mutate_target state update"
      pattern: "state: \"discarded\", discarded_at:"
---

<objective>
Implement native support for the "job_discard" action within the Lifeline repair pipeline.

Purpose: To allow destructive discard operations on jobs with full audit trailing and plan drift protection, alongside existing retry and cancel actions.
Output: A fully tested Lifeline pipeline that handles "job_discard".
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Lifeline job_discard Action Support</name>
  <files>lib/oban_powertools/lifeline.ex, test/oban_powertools/lifeline_test.exs</files>
  <behavior>
    - Ensure "job_discard" is included in `@supported_actions`.
    - Ensure `build_job_preview/5` matches on "job_discard" alongside "job_cancel".
    - `next_job_state("job_discard")` must return `"discarded"`.
    - `mutate_target` for "job_discard" must update job state to "discarded" and set `discarded_at` to the current time.
    - `repair_summary` for "job_discard" must generate a valid summary string matching the "job_cancel" pattern.
    Cases:
    - Target: "job", Action: "job_discard" -> Returns preview token, updates target to "discarded".
  </behavior>
  <action>
    Update `lib/oban_powertools/lifeline.ex`:
    1. Line 23: Add `"job_discard"` to `@supported_actions`.
    2. Line 592 (`build_job_preview` pattern): Add `"job_discard"` alongside "job_cancel".
    3. Line 805 (`next_job_state` pattern): Add `defp next_job_state("job_discard"), do: "discarded"`.
    4. Line 1155 (`mutate_target` pattern): Add a specific branch for `{"job", "job_discard"}` that changes state to `"discarded"` and `discarded_at: now`.
    5. Line 1235 (`repair_summary` pattern): Add `defp repair_summary("job_discard", "job", target_id)`.

    Add matching tests in `test/oban_powertools/lifeline_test.exs` mimicking the "job_cancel" tests.
  </action>
  <verify>
    <automated>mix test test/oban_powertools/lifeline_test.exs</automated>
  </verify>
  <done>job_discard is fully supported by Lifeline and tests pass.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Lifeline API → Oban DB | Arbitrary execution parameters entering Lifeline mutate the job database state |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-44-01 | Tampering | Lifeline | mitigate | Ensure action strictly matches `@supported_actions` before generating a preview. Verify drift plan hashes in execute_repair. |
| T-44-02 | Information Disclosure | Lifeline | accept | Error messages return specific failure reasons (e.g. heartbeat late) which is safe for operator context. |
</threat_model>

<verification>
Ensure all `job_discard` tests pass and are integrated into the pipeline seamlessly.
</verification>

<success_criteria>
`mix test test/oban_powertools/lifeline_test.exs` passes completely, ensuring the new action is properly guarded and executed.
</success_criteria>

<output>
After completion, create `.planning/phases/44-single-job-actions/44-01-SUMMARY.md`
</output>