---
phase: 44-single-job-actions
plan: 02
type: execute
wave: 2
depends_on: ["44-01"]
files_modified: ["lib/oban_powertools/web/jobs_live.ex", "test/oban_powertools/web/live/jobs_live_test.exs"]
autonomous: true
requirements: ["QRY-03"]

must_haves:
  truths:
    - "Operator sees action buttons on the job detail page based on job state"
    - "Clicking an action opens a preview modal without relying on Phoenix CoreComponents"
    - "Executing an action requires a reason and relies on Lifeline"
    - "A concurrent modification drift shows an explicit error instead of silent success"
  artifacts:
    - path: "lib/oban_powertools/web/jobs_live.ex"
      provides: "JobsLive preview and execute event handlers"
      contains: "def handle_event(\"preview\","
    - path: "test/oban_powertools/web/live/jobs_live_test.exs"
      provides: "JobsLive integration tests for actions"
  key_links:
    - from: "lib/oban_powertools/web/jobs_live.ex"
      to: "Lifeline.preview_repair"
      via: "preview event handler"
    - from: "lib/oban_powertools/web/jobs_live.ex"
      to: "Lifeline.execute_repair"
      via: "execute event handler"
---

<objective>
Introduce UI action buttons, a custom inline modal, and LiveView handlers for "Retry", "Cancel", and "Discard" actions on the single job detail page.

Purpose: To allow operators to trigger mutations directly from the UI, backed by Lifeline's audit and state drift prevention.
Output: Fully functional single-job action capabilities in `JobsLive`.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/44-UI-SPEC.md
@.planning/phases/44-PATTERNS.md
</context>

<interfaces>
<!-- Relevant aliases required for JobsLive actions -->
```elixir
alias ObanPowertools.Lifeline
alias ObanPowertools.Web.LiveAuth
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement Action Preview Handlers and Modal UI</name>
  <files>lib/oban_powertools/web/jobs_live.ex, test/oban_powertools/web/live/jobs_live_test.exs</files>
  <behavior>
    - `JobsLive` assigns `:preview`, `:reason`, and `:error_message` by default.
    - When `not @read_only?`, `JobsLive` displays an action bar next to the H1 (Retry, Cancel, Discard based on job state).
    - `handle_event("preview", ...)` fetches a `Lifeline.preview_repair/4` (with `incident_id: nil`) and sets `@preview`. It also checks `LiveAuth.authorize_action` for `:preview_repair`.
    - Clicking the actions renders an inline Tailwind modal (no `CoreComponents`) displaying the preview details and a Reason input.
  </behavior>
  <action>
    Modify `lib/oban_powertools/web/jobs_live.ex`:
    1. Import aliases: `alias ObanPowertools.Lifeline`, `alias ObanPowertools.Web.LiveAuth`.
    2. Add initial assigns in `mount/3` or `load_job_detail/2` (`preview: nil`, `reason: ""`, `error_message: nil`, `success_message: nil`).
    3. Modify `def render(%{live_action: :show} = assigns)` to inject the action buttons into the header (next to the H1). Use the classes and copy defined in `44-UI-SPEC.md`.
      - Retry Job (state in retryable, discarded, cancelled, completed)
      - Cancel Job (state in available, scheduled, executing, retryable)
      - Discard Job (state in available, scheduled, executing, retryable)
    4. Append the pure HTML/Tailwind Action Preview Modal to the bottom of the `:show` render block. The form should have `phx-change="reason"` and `phx-submit="execute"`.
    5. Add `def handle_event("preview", %{"action" => action}, socket)`:
       - Validate `:preview_repair` via `LiveAuth`.
       - Call `Lifeline.preview_repair` targeting `"job"`.
       - Assign the preview and reset reason/errors.
    6. Add `def handle_event("reason", %{"reason" => r}, socket)` to update `@reason`.

    Write tests in `test/oban_powertools/web/live/jobs_live_test.exs` verifying that the buttons render correctly depending on the job state and that the preview modal opens.
  </action>
  <verify>
    <automated>mix test test/oban_powertools/web/live/jobs_live_test.exs</automated>
  </verify>
  <done>Action bar is rendered properly, and clicking an action fetches the preview and displays the custom inline modal.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement Execution and Drift Guard Logic</name>
  <files>lib/oban_powertools/web/jobs_live.ex, test/oban_powertools/web/live/jobs_live_test.exs</files>
  <behavior>
    - Submit button in the modal is disabled if `@reason` is blank.
    - `handle_event("execute", ...)` verifies `LiveAuth` for `:execute_repair`, calls `Lifeline.execute_repair/4`.
    - If `execute_repair` returns `{:error, :preview_drifted}`, display the specific error message defined in `44-UI-SPEC.md` inside the modal.
    - If successful, close the modal, render a success flash, and reload the job details.
  </behavior>
  <action>
    Modify `lib/oban_powertools/web/jobs_live.ex`:
    1. Implement `def handle_event("execute", _, socket)`:
       - Validate `@reason != ""`.
       - Validate `:execute_repair` via `LiveAuth`.
       - Call `Lifeline.execute_repair(repo(), socket.assigns.current_actor, socket.assigns.preview.preview_token, socket.assigns.reason)`.
       - On success: clear preview, assign success message, call `load_job_detail/2` to refresh the job.
       - On `{:error, :preview_drifted}`: Assign an explicit `error_message` ("Could not execute action. The job's state was changed by another process or operator. Please refresh to see the latest state.").
    2. Write tests in `test/oban_powertools/web/live/jobs_live_test.exs`:
       - Verify execution resets the modal and flashes success.
       - Verify concurrent modification (changing the job state in the DB before execution) results in the drift error message.
  </action>
  <verify>
    <automated>mix test test/oban_powertools/web/live/jobs_live_test.exs</automated>
  </verify>
  <done>Executions update the backend appropriately, require a reason, and handle drift safely.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → LiveView | Operator inputs reasoning and submits actions |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-44-03 | Spoofing | JobsLive | mitigate | Use `LiveAuth.authorize_action` for `:preview_repair` and `:execute_repair` using `current_actor`. |
| T-44-04 | Elevation of Privilege | JobsLive | mitigate | Hide mutation buttons when `@read_only?` is true. `Lifeline.execute_repair` enforces validation as a backend guard. |
</threat_model>

<verification>
Ensure `mix test test/oban_powertools/web/live/jobs_live_test.exs` passes completely, capturing full UX interactions including concurrent drift errors.
</verification>

<success_criteria>
All single-job actions correctly interact with Lifeline and update the LiveView state.
</success_criteria>

<output>
After completion, create `.planning/phases/44-single-job-actions/44-02-SUMMARY.md`
</output>