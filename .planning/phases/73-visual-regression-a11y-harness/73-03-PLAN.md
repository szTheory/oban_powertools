---
phase: 73-visual-regression-a11y-harness
plan: 03
type: execute
wave: 3
depends_on:
  - 73-02
files_modified:
  - test/browser/support/axe.ts
  - test/browser/specs/showcase.a11y.spec.ts
  - lib/oban_powertools/web/dev/showcase_live.ex
  - assets/oban_powertools/tokens.css
  - priv/static/oban_powertools/oban_powertools.css
autonomous: true
requirements:
  - A11Y-01
must_haves:
  truths:
    - "D-13: Axe scans the same catalog-backed story targets and only implemented open-state DOM."
    - "D-14: Axe is configured for WCAG A/AA tags plus supported WCAG 2.2 AA coverage, with 0 critical/serious violations merge-blocking."
    - "D-15: Full axe JSON includes moderate, minor, incomplete, passes, and inapplicable results as review artifacts."
    - "D-16: The phase claims only an automated serious/critical axe gate, not full manual accessibility proof."
  artifacts:
    - path: "test/browser/support/axe.ts"
      provides: "AxeBuilder wrapper, result writer, and critical/serious threshold assertion"
      exports: ["runAxeForTarget", "writeAxeResult", "assertNoCriticalOrSerious"]
    - path: "test/browser/specs/showcase.a11y.spec.ts"
      provides: "Showcase story axe scan matrix"
    - path: "test-results/axe/**/*.json"
      provides: "Generated full axe result JSON during browser runs"
  key_links:
    - from: "test/browser/specs/showcase.a11y.spec.ts"
      to: "test/browser/.generated/showcase-manifest.json"
      via: "scans each manifest `a11y` selector"
      pattern: "scenario\\.a11y"
    - from: "test/browser/support/axe.ts"
      to: "@axe-core/playwright"
      via: "AxeBuilder analyzes the current Playwright page target"
      pattern: "AxeBuilder"
  prohibitions:
    - "D-13: Do not scan reserved open-state metadata unless real open-state DOM exists."
    - "D-15: Do not hide moderate, minor, or incomplete results from artifacts."
    - "D-16: Do not claim axe proves keyboard traversal, dialog focus management, screen-reader quality, reduced motion, or reflow."
---

<objective>
Add the axe-core accessibility gate against the deterministic showcase matrix.

Purpose: Make serious and critical automated accessibility regressions merge-blocking while preserving full findings for later hardening.
Output: Axe helper, axe Playwright spec, JSON result artifacts, and narrow showcase fixes only if the initial serious/critical run exposes them.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/73-visual-regression-a11y-harness/73-CONTEXT.md
@.planning/phases/73-visual-regression-a11y-harness/73-RESEARCH.md
@.planning/phases/73-visual-regression-a11y-harness/73-PATTERNS.md
@.planning/phases/73-visual-regression-a11y-harness/73-UI-SPEC.md
@test/browser/support/manifest.ts
@test/browser/support/showcase.ts
@lib/oban_powertools/web/dev/showcase_live.ex
@assets/oban_powertools/tokens.css
</context>

## Artifacts This Phase Produces

- npm scripts: `showcase:manifest`, `visual:a11y`, `visual:a11y:host`, `visual:a11y:docker`, `vrt:update`.
- Shell scripts: `scripts/showcase_manifest.exs`, `scripts/playwright-docker.sh`, `scripts/with-showcase-server.sh`.
- Config: `playwright.config.ts` with Chromium projects `chromium-320`, `chromium-tablet`, `chromium-wide`.
- Browser support modules: `test/browser/support/manifest.ts`, `test/browser/support/manifest-smoke.mjs`, `test/browser/support/deterministic.ts`, `test/browser/support/showcase.ts`, `test/browser/support/axe.ts`.
- Browser specs: `test/browser/specs/showcase.structure.spec.ts`, `test/browser/specs/showcase.a11y.spec.ts`, `test/browser/specs/showcase.vrt.spec.ts`.
- Generated and committed outputs: `test/browser/.generated/showcase-manifest.json` during runs, `test/browser/__screenshots__/chromium-*/showcase/*/*.png` with 108 committed PNG baselines.
- CI and docs: `.github/workflows/ci.yml` job `visual_a11y`, `ci-gate` `VISUAL_A11Y` fan-in, `guides/visual-regression-and-a11y.md`, README guide link, docs contract assertions.

<tasks>

<task type="auto">
  <name>Task 1: Add axe helper with narrow threshold and full result output</name>
  <files>test/browser/support/axe.ts</files>
  <read_first>
@test/browser/support/axe.ts
@test/browser/support/showcase.ts
@test/browser/support/manifest.ts
@.planning/phases/73-visual-regression-a11y-harness/73-RESEARCH.md
@.planning/phases/73-visual-regression-a11y-harness/73-UI-SPEC.md
  </read_first>
  <action>Create `test/browser/support/axe.ts` around `@axe-core/playwright` per D-13 through D-16. Export `runAxeForTarget`, `writeAxeResult`, and `assertNoCriticalOrSerious`. Configure axe through one `options({ runOnly, rules, resultTypes })` call so WCAG A/AA tags and supported `wcag22aa` coverage are active and `target-size` is enabled for the installed `axe-core@4.11.4`; do not combine `withTags()` with later `options()` because the adapter override behavior would drop tags. `assertNoCriticalOrSerious` must fail only `impact` values `critical` and `serious`; `writeAxeResult` must persist the full result object including `violations`, `passes`, `incomplete`, and `inapplicable` under `test-results/axe/` and attach it to Playwright test info.</action>
  <verify>
    <automated>node -e "const fs=require('fs'); const s=fs.readFileSync('test/browser/support/axe.ts','utf8'); for (const token of ['AxeBuilder','runAxeForTarget','writeAxeResult','assertNoCriticalOrSerious','wcag22aa','target-size','critical','serious','incomplete']) { if (!s.includes(token)) throw new Error('missing ' + token); }"</automated>
  </verify>
  <acceptance_criteria>
    - `test/browser/support/axe.ts` imports `AxeBuilder` from `@axe-core/playwright`.
    - The helper enables WCAG A/AA tags and supported WCAG 2.2 AA coverage, including the `target-size` rule.
    - The threshold helper fails critical and serious violations only.
    - The writer produces JSON files containing full axe result arrays, not only failures.
  </acceptance_criteria>
  <done>Axe helper behavior is explicit and reusable by the a11y spec and CI artifacts.</done>
</task>

<task type="auto">
  <name>Task 2: Add showcase axe matrix and close current serious or critical findings</name>
  <files>test/browser/specs/showcase.a11y.spec.ts, lib/oban_powertools/web/dev/showcase_live.ex, assets/oban_powertools/tokens.css, priv/static/oban_powertools/oban_powertools.css</files>
  <read_first>
@test/browser/specs/showcase.a11y.spec.ts
@test/browser/support/axe.ts
@test/browser/support/showcase.ts
@test/browser/support/manifest.ts
@lib/oban_powertools/web/dev/showcase_live.ex
@assets/oban_powertools/tokens.css
@priv/static/oban_powertools/oban_powertools.css
@test/oban_powertools/web/live/showcase_live_test.exs
  </read_first>
  <action>Create `test/browser/specs/showcase.a11y.spec.ts` so it runs over the manifest scenarios across all four themes and three Playwright viewport projects, using each scenario `a11y` selector per D-13. Include implemented open-state DOM only when a real story target exists; reserved metadata values alone must not create scan targets. If the first run exposes critical or serious violations in the Phase 72 showcase shell, make narrow fixes in `lib/oban_powertools/web/dev/showcase_live.ex` or `assets/oban_powertools/tokens.css`, rebuild `priv/static/oban_powertools/oban_powertools.css`, and preserve all existing selector contracts. Do not lower the threshold, delete axe rules, hide the affected DOM, or broaden the phase into manual keyboard or screen-reader proof per D-16.</action>
  <verify>
    <automated>npm run visual:a11y -- test/browser/specs/showcase.a11y.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - The a11y spec reads generated manifest scenarios and does not hardcode authoritative story IDs.
    - The spec scans all 9 current catalog targets across 4 themes and 3 browser viewport projects.
    - Critical and serious axe violations fail the command.
    - Full axe JSON files are written under `test-results/axe/` during the run.
    - If showcase markup or CSS changed, existing ExUnit showcase tests still pass and compiled CSS is regenerated.
  </acceptance_criteria>
  <done>The automated axe gate is green for current showcase targets and produces full review artifacts.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser DOM to axe engine | Runtime DOM is analyzed by axe-core inside Playwright. |
| Axe output to CI artifacts | Accessibility result JSON becomes downloadable review material. |
| Harness threshold to merge gate | Severity filtering determines whether CI fails. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-73-05 | Tampering | Axe threshold helper | mitigate | Hard-code the critical/serious impact gate and keep full result arrays attached so threshold changes are reviewable. |
| T-73-06 | Repudiation | Axe artifacts | mitigate | Persist full JSON for each target so lower-severity and incomplete findings remain visible even when non-blocking. |
| T-73-SC | Tampering | npm package installs | mitigate | Use the lockfile from Plan 73-01 and do not add additional accessibility packages. |

No high-severity ASVS Level 1 threat remains unmitigated in this plan.
</threat_model>

<verification>
Run `npm run visual:a11y -- test/browser/specs/showcase.a11y.spec.ts`.
</verification>

<success_criteria>
The showcase axe matrix passes with zero critical/serious violations and emits full JSON artifacts without overclaiming manual accessibility coverage.
</success_criteria>

<output>
Create `.planning/phases/73-visual-regression-a11y-harness/73-03-SUMMARY.md` when done.
</output>
