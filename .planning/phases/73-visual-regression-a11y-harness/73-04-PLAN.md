---
phase: 73-visual-regression-a11y-harness
plan: 04
type: execute
wave: 4
depends_on:
  - 73-03
files_modified:
  - test/browser/specs/showcase.vrt.spec.ts
  - test/browser/__screenshots__/**/*.png
autonomous: true
requirements:
  - VRT-01
  - VRT-02
  - VRT-03
must_haves:
  truths:
    - "D-05: Phase 73 screenshots cover only catalog-backed story cells from ObanPowertools.ShowcaseCatalog."
    - "D-08: Screenshot matrix is 9 stories by 4 themes by 3 real browser viewports in Chromium."
    - "D-09: Visual comparison uses Playwright `toHaveScreenshot` with committed, human-readable PNG baselines."
    - "D-10 D-11: Captures wait for fonts, deterministic theme and viewport state, and avoid broad masks."
    - "D-17 D-20: Baseline update uses explicit Docker command with changed-only update mode."
  artifacts:
    - path: "test/browser/specs/showcase.vrt.spec.ts"
      provides: "Story-level visual-regression matrix"
    - path: "test/browser/__screenshots__/chromium-*/showcase/*/*.png"
      provides: "108 committed expected PNG baselines"
  key_links:
    - from: "test/browser/specs/showcase.vrt.spec.ts"
      to: "test/browser/support/showcase.ts"
      via: "prepares theme, viewport, readiness, and story locator before `toHaveScreenshot`"
      pattern: "prepareShowcase"
    - from: "test/browser/specs/showcase.vrt.spec.ts"
      to: "test/browser/.generated/showcase-manifest.json"
      via: "snapshot names and selectors come from manifest scenarios"
      pattern: "scenario\\.snapshot"
  prohibitions:
    - "D-05: Do not snapshot reserved future-phase sections before real stories exist."
    - "D-09: Do not use custom image diffing instead of Playwright snapshot assertions."
    - "D-11: Do not mask story content, status colors, focus or hover affordances, long identifiers, RTL/non-ASCII text, redaction displays, or severity signals."
    - "D-17: Do not update snapshots in CI."
---

<objective>
Add story-level Playwright visual regression and commit the initial deterministic baselines.

Purpose: Lock the current Phase 72 catalog-backed visual contract before component and page work begins.
Output: VRT spec plus 108 committed PNG baselines generated in the pinned Docker environment.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/73-visual-regression-a11y-harness/73-CONTEXT.md
@.planning/phases/73-visual-regression-a11y-harness/73-RESEARCH.md
@.planning/phases/73-visual-regression-a11y-harness/73-PATTERNS.md
@.planning/phases/73-visual-regression-a11y-harness/73-UI-SPEC.md
@.planning/phases/73-visual-regression-a11y-harness/73-03-SUMMARY.md
@test/browser/support/manifest.ts
@test/browser/support/showcase.ts
@test/browser/styles/screenshot.css
@test/support/showcase_catalog.ex
</context>

## Artifacts This Phase Produces

- npm scripts: `showcase:manifest`, `visual:a11y`, `visual:a11y:host`, `visual:a11y:docker`, `vrt:update`.
- Shell scripts: `scripts/showcase_manifest.exs`, `scripts/playwright-docker.sh`, `scripts/with-showcase-server.sh`.
- Config: `playwright.config.ts` with Chromium projects `chromium-320`, `chromium-tablet`, `chromium-wide`.
- Browser support modules: `test/browser/support/manifest.ts`, `test/browser/support/manifest-smoke.mjs`, `test/browser/support/deterministic.ts`, `test/browser/support/showcase.ts`, `test/browser/support/axe.ts`.
- Browser specs: `test/browser/specs/showcase.structure.spec.ts`, `test/browser/specs/showcase.a11y.spec.ts`, `test/browser/specs/showcase.vrt.spec.ts`.
- Generated and committed outputs: `test/browser/.generated/showcase-manifest.json` during runs, `test/browser/__screenshots__/chromium-*/showcase/*/*.png` with 108 committed PNG baselines.
- CI and docs: `.github/workflows/ci.yml` job `visual_a11y`, `ci-gate` `VISUAL_A11Y` fan-in, `guides/visual-regression-and-a11y.md`, README guide link, docs contract assertions.

<tasks>

<task type="auto">
  <name>Task 1: Add story-level visual regression spec</name>
  <files>test/browser/specs/showcase.vrt.spec.ts</files>
  <read_first>
@test/browser/specs/showcase.vrt.spec.ts
@test/browser/support/manifest.ts
@test/browser/support/showcase.ts
@test/browser/support/deterministic.ts
@test/browser/styles/screenshot.css
@.planning/phases/73-visual-regression-a11y-harness/73-CONTEXT.md
@.planning/phases/73-visual-regression-a11y-harness/73-UI-SPEC.md
  </read_first>
  <action>Create `test/browser/specs/showcase.vrt.spec.ts` so it iterates generated manifest scenarios and themes under each configured viewport project. Use `prepareShowcase` to set the real browser viewport, the matching showcase viewport control, the theme, fonts-ready state, and deterministic `system` effective theme. Use locator-level `toHaveScreenshot` on each story cell, with snapshot path segments `showcase`, scenario ID, and theme so expected paths resolve to `test/browser/__screenshots__/{projectName}/showcase/{scenario-id}/{theme}.png`. Per D-05, do not capture reserved sections. Per D-11, avoid masks unless a specific volatile pixel is proven and documented in the spec with a narrow selector.</action>
  <verify>
    <automated>npm run visual:a11y -- test/browser/specs/showcase.vrt.spec.ts --list</automated>
  </verify>
  <acceptance_criteria>
    - The spec defines tests from generated manifest scenarios and themes, not an authoritative copied ID list.
    - The spec calls `toHaveScreenshot` on story locators, not full pages.
    - Snapshot names include story ID and theme, with viewport supplied by the Playwright project name.
    - The spec does not capture reserved showcase sections.
  </acceptance_criteria>
  <done>VRT spec enumerates the intended matrix and is ready to generate expected baselines.</done>
</task>

<task type="auto">
  <name>Task 2: Generate and verify committed baselines in Docker</name>
  <files>test/browser/__screenshots__/**/*.png</files>
  <read_first>
@test/browser/specs/showcase.vrt.spec.ts
@test/browser/support/showcase.ts
@test/browser/__screenshots__/**/*.png
@scripts/playwright-docker.sh
@scripts/with-showcase-server.sh
@package.json
  </read_first>
  <action>Run the explicit Docker update workflow from D-17 and D-20 to create the initial expected PNG baselines for the current catalog matrix. Commit exactly the expected Playwright baselines under `test/browser/__screenshots__/chromium-*/showcase/*/*.png`; do not commit `playwright-report/`, `test-results/`, generated manifest JSON, or actual/diff failure images. After generation, run the VRT spec in normal compare mode without update flags and resolve any nondeterminism by fixing harness controls rather than reducing matrix coverage.</action>
  <verify>
    <automated>npm run vrt:update && test "$(find test/browser/__screenshots__ -name '*.png' | wc -l | tr -d ' ')" = "108" && npm run visual:a11y -- test/browser/specs/showcase.vrt.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - Exactly 108 committed PNG files exist under `test/browser/__screenshots__/`.
    - The 108 files correspond to 9 catalog stories, 4 themes, and 3 Chromium viewport projects.
    - A normal VRT run passes without any snapshot update flag.
    - No failure-output PNGs from `test-results/` are committed as baselines.
  </acceptance_criteria>
  <done>Baseline screenshots are committed and the visual-regression spec passes in compare mode.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser rendering to committed PNGs | Screenshot output becomes the expected visual contract. |
| Local update command to git history | Developer-generated baselines enter reviewable source control. |
| Story fixtures to snapshot content | Synthetic catalog data appears in committed images. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-73-07 | Tampering | `test/browser/__screenshots__` | mitigate | Generate baselines only through the pinned Docker update command, require exactly 108 PNGs, and verify compare mode passes without update flags. |
| T-73-08 | Information Disclosure | Committed screenshots | accept | Screenshots contain synthetic dev/test catalog fixtures only; Phase 72 catalog has no secrets or real tenant data. |
| T-73-SC | Tampering | npm package installs | mitigate | Use existing lockfile and pinned Docker image; do not add packages in this plan. |

No high-severity ASVS Level 1 threat remains unmitigated in this plan.
</threat_model>

<verification>
Run `npm run visual:a11y -- test/browser/specs/showcase.vrt.spec.ts` after `npm run vrt:update` has generated the 108 expected baselines.
</verification>

<success_criteria>
The current showcase catalog has committed, deterministic, Docker-generated VRT baselines and normal compare mode is green.
</success_criteria>

<output>
Create `.planning/phases/73-visual-regression-a11y-harness/73-04-SUMMARY.md` when done.
</output>
