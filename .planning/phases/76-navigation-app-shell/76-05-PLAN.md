---
phase: 76-navigation-app-shell
plan: 05
type: execute
wave: 5
depends_on:
  - 76-04
files_modified:
  - test/browser/specs/shell.behavior.spec.ts
  - test/browser/__screenshots__/chromium-320/showcase/shell-*/*.png
  - test/browser/__screenshots__/chromium-tablet/showcase/shell-*/*.png
  - test/browser/__screenshots__/chromium-wide/showcase/shell-*/*.png
  - .planning/phases/76-navigation-app-shell/76-VALIDATION.md
autonomous: true
requirements:
  - NAV-01
  - NAV-02
  - NAV-03
  - NAV-04
  - A11Y-02
  - "COPY-* (nav labels)"
must_haves:
  truths:
    - "NAV-01: Browser evidence sees shell header/nav/theme/actor content on shell stories."
    - "NAV-02: Browser evidence proves mobile disclosure and no 320px horizontal overflow."
    - "NAV-03: Browser evidence proves active route and breadcrumb current state."
    - "NAV-04/A11Y-02: Browser evidence proves skip link, logical focus path, visible focus, keyboard toggle, and Escape close."
    - "COPY-* (nav labels): Shell VRT/a11y targets preserve canonical navigation labels."
  prohibitions:
    - "Do not update non-shell screenshot baselines in this plan."
    - "Do not mask shell nav labels, current route, selected theme, actor/context, or focus evidence in screenshots."
  artifacts:
    - path: "test/browser/specs/shell.behavior.spec.ts"
      provides: "Targeted shell browser behavior proof"
    - path: "test/browser/__screenshots__/chromium-*/showcase/shell-*/*.png"
      provides: "72 shell VRT baselines"
    - path: ".planning/phases/76-navigation-app-shell/76-VALIDATION.md"
      provides: "Nyquist execution evidence and final phase proof commands"
  key_links:
    - from: "test/browser/specs/shell.behavior.spec.ts"
      to: "test/browser/support/manifest.ts"
      via: "shellStories generated metadata"
      pattern: "shellStories"
    - from: "test/browser/specs/showcase.vrt.spec.ts"
      to: "test/support/shell_story_catalog.ex"
      via: "schema 4 targets include shell story snapshots"
      pattern: "shell-"
---

<objective>
Close Phase 76 with browser behavior proof, shell VRT baselines, a11y evidence, and validation metadata.

Purpose: prove the shell works across themes/viewports with generated targets and targeted behavior checks beyond axe.
Output: passing shell behavior spec, committed shell screenshots, updated validation evidence, and final proof commands.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/76-navigation-app-shell/76-RESEARCH.md
@.planning/phases/76-navigation-app-shell/76-PATTERNS.md
@.planning/phases/76-navigation-app-shell/76-UI-SPEC.md
@.planning/phases/76-navigation-app-shell/76-VALIDATION.md
@.planning/phases/73-visual-regression-a11y-harness/73-04-SUMMARY.md
@.planning/phases/74-primitives-library/74-05-SUMMARY.md
@.planning/phases/75-form-components/75-06-SUMMARY.md
@test/browser/specs/shell.behavior.spec.ts
@test/browser/specs/showcase.structure.spec.ts
@test/browser/specs/showcase.a11y.spec.ts
@test/browser/specs/showcase.vrt.spec.ts
@test/browser/support/manifest.ts
@test/browser/support/showcase.ts
@test/support/shell_story_catalog.ex
</context>

## Artifacts This Phase Produces

- New module `ObanPowertools.Web.Components.AppShell`.
- New public shell helpers `app_shell/1`, `nav_items/0`, `breadcrumb_items/1`, `theme_choices/0`, and `actor_label/1`.
- New module `ObanPowertools.ShellStoryCatalog` with `stories/0`, `story!/1`, `snapshot_name/1`, and `a11y_target/1`.
- New tests `test/oban_powertools/web/components/app_shell_test.exs`, `test/oban_powertools/web/live/app_shell_layout_test.exs`, `test/oban_powertools/shell_story_catalog_test.exs`, and `test/browser/specs/shell.behavior.spec.ts`.
- New manifest schema version `4`, top-level `shell_stories`, TypeScript type `ShowcaseShellStory`, and export `shellStories`.
- New showcase section `data-obpt-section="app-shell"` and shell story selector `data-obpt-shell-story`.
- New DOM ids `obpt-primary-nav` and `obpt-main`.
- New selectors/classes `.obpt-app-shell`, `.obpt-app-shell__skip`, `.obpt-app-shell__header`, `.obpt-app-shell__brand`, `.obpt-primary-nav`, `.obpt-primary-nav__link`, `.obpt-breadcrumb`, `.obpt-theme-choices`, `.obpt-actor-context`, and `.obpt-shell-main`.
- New data attributes `data-obpt-app-shell`, `data-obpt-nav-toggle`, `data-obpt-primary-nav`, `data-obpt-nav-state`, `data-obpt-nav-item`, `data-obpt-breadcrumb`, `data-obpt-shell-story`, and `data-obpt-a11y-target`.
- New shell VRT baselines under `test/browser/__screenshots__/chromium-*/showcase/shell-*/*.png`.

<tasks>

<task type="auto">
  <name>Task 1: Finalize targeted shell browser behavior proof</name>
  <files>test/browser/specs/shell.behavior.spec.ts</files>
  <read_first>
    @test/browser/specs/shell.behavior.spec.ts
    @test/browser/specs/primitives.behavior.spec.ts
    @test/browser/specs/forms.behavior.spec.ts
    @test/browser/support/manifest.ts
    @test/browser/support/showcase.ts
    @test/support/shell_story_catalog.ex
    @assets/oban_powertools/theme.js
    @.planning/phases/76-navigation-app-shell/76-UI-SPEC.md
  </read_first>
  <action>Finalize `test/browser/specs/shell.behavior.spec.ts` against the generated `shellStories` export. Add helpers `shellStory(id)`, `prepareShellStory(page, projectName, story, theme)`, `tabUntilFocused`, and `expectVisibleFocus` patterned after primitive/form behavior specs. Prove these browser-observable claims: primary nav role/name `Powertools surfaces` has exactly nine links in canonical order; optional bridge path is absent; `Jobs` and detail stories set `aria-current="page"` and breadcrumb current segment correctly; skip link is first reachable focus target and Enter moves focus to `main#obpt-main`; disclosure button named `Navigation` toggles `aria-expanded` on Enter, Space, and click at `chromium-320`; collapsed mobile nav links are not reachable by Tab while closed; Escape closes an open disclosure when focus is inside nav; theme buttons set `.obpt-root[data-obpt-theme]` and `aria-pressed` without mutating host document state; focus is visible for skip link, nav links, disclosure, and theme choices in all four themes; document/body/story horizontal overflow at 320px is <= 1px. Keep assertions role/name-first and use data attributes only for shell-specific state checks.</action>
  <verify>
    <automated>npm run showcase:manifest && scripts/with-showcase-server.sh npm run visual:a11y:host -- test/browser/specs/shell.behavior.spec.ts --project chromium-320</automated>
    <automated>npm run showcase:manifest && scripts/with-showcase-server.sh npm run visual:a11y:host -- test/browser/specs/shell.behavior.spec.ts --project chromium-wide</automated>
  </verify>
  <acceptance_criteria>
    - Shell behavior spec passes on `chromium-320` and `chromium-wide`.
    - Spec proves skip-link focus transfer, disclosure keyboard/click/Escape behavior, collapsed tab order, active route, breadcrumb current state, theme state, focus visibility, and 320px overflow.
    - Spec imports story metadata from `shellStories`; it does not maintain a separate TypeScript inventory of all shell target ids.
  </acceptance_criteria>
  <done>Targeted shell browser behavior proof is green.</done>
</task>

<task type="auto">
  <name>Task 2: Generate shell VRT baselines and record final validation evidence</name>
  <files>test/browser/__screenshots__/chromium-320/showcase/shell-*/*.png, test/browser/__screenshots__/chromium-tablet/showcase/shell-*/*.png, test/browser/__screenshots__/chromium-wide/showcase/shell-*/*.png, .planning/phases/76-navigation-app-shell/76-VALIDATION.md</files>
  <read_first>
    @test/browser/specs/showcase.vrt.spec.ts
    @test/browser/specs/showcase.a11y.spec.ts
    @test/browser/specs/showcase.structure.spec.ts
    @test/browser/support/manifest.ts
    @guides/visual-regression-and-a11y.md
    @.planning/phases/76-navigation-app-shell/76-VALIDATION.md
    @.planning/phases/75-form-components/75-06-SUMMARY.md
  </read_first>
  <action>Run the existing Docker-backed baseline update only for shell targets with `npm run vrt:update -- --grep shell`. Commit exactly the generated shell baseline PNGs for six shell stories across three Chromium projects and four themes; expected cardinality is 72 PNGs. Do not update scenario, primitive, or form baselines in this plan. Run focused shell structure, axe, and VRT compare commands using generated `kind shell` test names. Update `.planning/phases/76-navigation-app-shell/76-VALIDATION.md`: set `nyquist_compliant: true`, `wave_0_complete: true`, replace `TBD` task ids with 76-01..76-05 task mappings, and record executed command outcomes including any known aggregate visual gate residual from prior scenario-only drift if it still exists. Keep the validation wording scoped: shell behavior, shell axe, shell VRT, component tests, layout tests, assets tests, compile, and no unrelated baseline ownership.</action>
  <verify>
    <automated>npm run vrt:update -- --grep shell</automated>
    <automated>test "$(find test/browser/__screenshots__ -path '*/showcase/shell-*/*.png' | wc -l | tr -d ' ')" = "72"</automated>
    <automated>npm run showcase:manifest && scripts/with-showcase-server.sh npm run visual:a11y:host -- test/browser/specs/showcase.structure.spec.ts --project chromium-320 --grep shell</automated>
    <automated>npm run showcase:manifest && scripts/with-showcase-server.sh npm run visual:a11y:host -- test/browser/specs/showcase.a11y.spec.ts --project chromium-320 --grep shell</automated>
    <automated>npm run showcase:manifest && scripts/with-showcase-server.sh npm run visual:a11y:host -- test/browser/specs/showcase.vrt.spec.ts --project chromium-320 --grep shell</automated>
    <automated>mix test test/oban_powertools/web/components/app_shell_test.exs test/oban_powertools/web/live/app_shell_layout_test.exs test/oban_powertools/shell_story_catalog_test.exs test/oban_powertools/web/live/showcase_live_test.exs test/oban_powertools/web/theme_tokens_test.exs test/oban_powertools/web/assets_test.exs --seed 0</automated>
    <automated>mix compile --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - Exactly 72 shell PNG baselines exist under shell story directories and no non-shell PNGs are modified by this task.
    - Focused shell structure, axe, VRT, and behavior commands pass.
    - ExUnit component/layout/catalog/showcase/theme/assets tests pass.
    - Validation file records Nyquist-compliant status and concrete Phase 76 command evidence.
  </acceptance_criteria>
  <done>Shell visual/a11y/browser evidence is complete and validation metadata is updated.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| generated manifest -> browser tests | Browser tests consume shell selectors, snapshots, and a11y targets from JSON. |
| browser interactions -> shell DOM state | User-like keyboard/click interactions mutate disclosure and theme state. |
| screenshot baselines -> review evidence | Generated PNGs become committed regression contracts. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-76-02 | Spoofing | active route/browser state | mitigate | Behavior tests assert server-rendered `aria-current` and breadcrumb current state on generated shell stories. |
| T-76-04 | Denial of Service | keyboard/disclosure/overflow | mitigate | Behavior tests assert skip-link transfer, collapsed tab order, Escape close, visible focus, and <=1px horizontal overflow at 320px. |
| T-76-05 | Information Disclosure | theme state | mitigate | Behavior tests assert theme changes stay on `.obpt-root` and existing `data-obpt-theme-choice` state. |
| T-76-09 | Repudiation | VRT/a11y evidence | mitigate | Shell screenshots, axe results, and validation command evidence are recorded with generated target provenance. |
| T-76-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs are planned. Stop for legitimacy review if an install appears. |
</threat_model>

<verification>
Run targeted shell behavior, shell VRT update, shell structure/axe/VRT compare, focused ExUnit suites, baseline cardinality, and compile. Record any global visual gate residual explicitly without updating unrelated baselines.
</verification>

<success_criteria>
Phase 76 is complete when shell behavior tests pass, 72 shell VRT baselines are committed, generated shell axe/VRT/structure targets are green, ExUnit shell/layout/catalog/asset tests are green, and validation evidence maps every Phase 76 requirement.
</success_criteria>

<output>
Create `.planning/phases/76-navigation-app-shell/76-05-SUMMARY.md` when done.
</output>
