---
phase: 76-navigation-app-shell
plan: 04
type: execute
wave: 4
depends_on:
  - 76-03
files_modified:
  - test/support/shell_story_catalog.ex
  - test/oban_powertools/shell_story_catalog_test.exs
  - lib/oban_powertools/web/dev/showcase_live.ex
  - test/oban_powertools/web/live/showcase_live_test.exs
  - scripts/showcase_manifest.exs
  - test/browser/support/manifest.ts
  - test/browser/support/manifest-smoke.mjs
  - test/browser/support/showcase.ts
autonomous: true
requirements:
  - NAV-01
  - NAV-02
  - NAV-03
  - A11Y-02
  - "COPY-* (nav labels)"
must_haves:
  truths:
    - "NAV-01: The dev/test showcase renders real AppShell stories with header, nav, theme, actor, breadcrumb, and representative inner content."
    - "NAV-02: Shell stories include mobile collapsed/expanded and long-context wrapping states."
    - "NAV-03/COPY-*: Shell stories and manifest preserve canonical labels, active route, and breadcrumb states."
    - "A11Y-02: Shell stories become generated axe/VRT targets through the manifest instead of hardcoded TypeScript ids."
  prohibitions:
    - "Do not mix shell stories into domain stress fixtures, primitive stories, or form stories."
    - "Do not add the optional bridge to primary nav stories."
  artifacts:
    - path: "test/support/shell_story_catalog.ex"
      provides: "Dev/test-only shell story catalog"
    - path: "lib/oban_powertools/web/dev/showcase_live.ex"
      provides: "Rendered shell showcase section"
    - path: "scripts/showcase_manifest.exs"
      provides: "schema 4 manifest with shell_stories"
    - path: "test/browser/support/manifest.ts"
      provides: "ShowcaseShellStory type and shellStories export"
  key_links:
    - from: "test/support/shell_story_catalog.ex"
      to: "scripts/showcase_manifest.exs"
      via: "ShellStoryCatalog.stories/0 generates shell_stories"
      pattern: "shell_stories"
    - from: "test/browser/support/manifest.ts"
      to: "test/browser/specs/showcase.vrt.spec.ts"
      via: "targets union includes kind shell"
      pattern: "kind: 'shell'"
---

<objective>
Register AppShell showcase stories and generated browser manifest support.

Purpose: make the shell a first-class dev/test evidence target across themes and viewports without hardcoded browser target ids.
Output: `ShellStoryCatalog`, rendered AppShell showcase section, schema 4 manifest support, and structure validation for shell targets.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/76-navigation-app-shell/76-RESEARCH.md
@.planning/phases/76-navigation-app-shell/76-PATTERNS.md
@.planning/phases/76-navigation-app-shell/76-UI-SPEC.md
@.planning/phases/72-stress-fixtures-showcase-skeleton/72-04-SUMMARY.md
@.planning/phases/74-primitives-library/74-03-SUMMARY.md
@.planning/phases/75-form-components/75-04-SUMMARY.md
@test/support/form_story_catalog.ex
@lib/oban_powertools/web/dev/showcase_live.ex
@test/oban_powertools/web/live/showcase_live_test.exs
@scripts/showcase_manifest.exs
@test/browser/support/manifest.ts
@test/browser/support/manifest-smoke.mjs
@test/browser/support/showcase.ts
@lib/oban_powertools/web/components/app_shell.ex
</context>

## Artifacts This Phase Produces

- New module `ObanPowertools.Web.Components.AppShell`.
- New public shell helpers `app_shell/1`, `nav_items/0`, `breadcrumb_items/1`, `theme_choices/0`, and `actor_label/1`.
- New module `ObanPowertools.ShellStoryCatalog` with `stories/0`, `story!/1`, `snapshot_name/1`, and `a11y_target/1`.
- New tests `test/oban_powertools/web/components/app_shell_test.exs`, `test/oban_powertools/web/live/app_shell_layout_test.exs`, `test/oban_powertools/shell_story_catalog_test.exs`, and `test/browser/specs/shell.behavior.spec.ts`.
- New manifest schema version `4`, top-level `shell_stories`, TypeScript type `ShowcaseShellStory`, and export `shellStories`.
- New showcase section `data-obpt-section="app-shell"` and shell story selector `data-obpt-shell-story`.
- New DOM ids `obpt-primary-nav` and `obpt-main`.
- New selectors/classes `.obpt-app-shell`, `.obpt-app-shell__skip`, `.obpt-app-shell__header`, `.obpt-app-shell__brand`, `.obpt-primary-nav`, `.obpt-primary-nav__link`, `.obpt-breadcrumb`, `.obpt-theme-choices`, `.obpt-actor-context`, and `.obpt-shell-main`.
- New data attributes `data-obpt-app-shell`, `data-obpt-nav-toggle`, `data-obpt-primary-nav`, `data-obpt-nav-state`, `data-obpt-nav-item`, `data-obpt-breadcrumb`, `data-obpt-shell-story`, and `data-obpt-a11y-target`.
- New shell VRT baselines under `test/browser/__screenshots__/chromium-*/showcase/shell-*/*.png`.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement ShellStoryCatalog and render shell stories in showcase</name>
  <files>test/support/shell_story_catalog.ex, test/oban_powertools/shell_story_catalog_test.exs, lib/oban_powertools/web/dev/showcase_live.ex, test/oban_powertools/web/live/showcase_live_test.exs</files>
  <read_first>
    @test/support/form_story_catalog.ex
    @test/oban_powertools/form_story_catalog_test.exs
    @lib/oban_powertools/web/dev/showcase_live.ex
    @test/oban_powertools/web/live/showcase_live_test.exs
    @lib/oban_powertools/web/components/app_shell.ex
    @.planning/phases/76-navigation-app-shell/76-UI-SPEC.md
    @.planning/phases/76-navigation-app-shell/76-PATTERNS.md
  </read_first>
  <behavior>
    - NAV-01: showcase renders AppShell with representative content for shell evidence.
    - NAV-02: catalog includes collapsed, expanded, and long-context wrapping stories.
    - NAV-03/COPY-*: catalog stories pin active route, breadcrumb, and canonical labels.
    - A11Y-02: rendered story cells expose stable a11y selectors and metadata.
  </behavior>
  <action>Create `test/support/shell_story_catalog.ex` as `ObanPowertools.ShellStoryCatalog`, separate from `ShowcaseCatalog`, `PrimitiveStoryCatalog`, and `FormStoryCatalog`. Implement six deterministic stories with ids `shell-nine-surface-nav`, `shell-mobile-collapsed`, `shell-mobile-expanded`, `shell-active-breadcrumb`, `shell-theme-actor-context`, and `shell-long-context-wrapping`; each story must include `kind: :shell`, `component: :app_shell`, `components: [:app_shell]`, non-empty `variant` and `state`, display `name`, `description`, story-specific `current_path`, `actor`, `context_label`, and `nav_state` where relevant. Derive `test_targets.story` as `obpt-shell-story-{id}`, `snapshot_name/1` as `showcase/{id}`, and `a11y_target/1` as `[data-obpt-shell-story="{id}"]`. Update `test/oban_powertools/shell_story_catalog_test.exs` from 76-01 so it passes. Modify `ShowcaseLive` to load `ShellStoryCatalog` with the same support-module fallback pattern as primitive/form catalogs, add a first section `app-shell` with title `App shell`, and render each shell story as an article with `data-obpt-shell-story`, `data-obpt-component`, `data-obpt-variant`, `data-obpt-state`, and `data-obpt-a11y-target`. Use real `<AppShell.app_shell>` inside each story and representative inner content only; do not migrate production page body markup. Update `showcase_live_test.exs` to assert shell section presence, six story cells, canonical shell copy, bridge absence from primary nav, and escaped long context.</action>
  <verify>
    <automated>mix format --check-formatted test/support/shell_story_catalog.ex test/oban_powertools/shell_story_catalog_test.exs lib/oban_powertools/web/dev/showcase_live.ex test/oban_powertools/web/live/showcase_live_test.exs</automated>
    <automated>mix test test/oban_powertools/shell_story_catalog_test.exs test/oban_powertools/web/live/showcase_live_test.exs --seed 0</automated>
  </verify>
  <acceptance_criteria>
    - Six shell story ids render deterministically and have stable story/snapshot/a11y target helpers.
    - Showcase renders `data-obpt-section="app-shell"` and six `[data-obpt-shell-story]` articles.
    - Shell showcase cells render real AppShell markup, canonical copy, actor fallback/label states, and no primary bridge entry.
    - Existing scenario, primitive, and form showcase tests remain green.
  </acceptance_criteria>
  <done>Shell catalog and rendered showcase section are green.</done>
</task>

<task type="auto">
  <name>Task 2: Extend generated manifest and browser support for shell targets</name>
  <files>scripts/showcase_manifest.exs, test/browser/support/manifest.ts, test/browser/support/manifest-smoke.mjs, test/browser/support/showcase.ts</files>
  <read_first>
    @scripts/showcase_manifest.exs
    @test/browser/support/manifest.ts
    @test/browser/support/manifest-smoke.mjs
    @test/browser/support/showcase.ts
    @test/support/shell_story_catalog.ex
    @test/browser/specs/showcase.structure.spec.ts
    @test/browser/specs/showcase.a11y.spec.ts
    @test/browser/specs/showcase.vrt.spec.ts
    @.planning/phases/76-navigation-app-shell/76-PATTERNS.md
  </read_first>
  <action>Extend `scripts/showcase_manifest.exs` to alias `ObanPowertools.ShellStoryCatalog`, serialize `shell_stories` after `form_stories`, append shell stories to `targets` after forms, and bump `schema_version` from `3` to `4`. Each shell manifest item must include `id`, `kind`, `component`, `components`, `name`, `description`, `variant`, `state`, `story`, `snapshot`, and `a11y` using the catalog helpers. Update `test/browser/support/manifest.ts` to require schema version 4, add `ShowcaseShellStory`, include shell in `ShowcaseTarget`, validate top-level `shell_stories`, accept `collection: 'shell_stories'` and `kind: 'shell'` in component-story validation, expect `shell-*` ids, and export `shellStories`. Update `manifest-smoke.mjs` with the same schema/count/order checks and final log. Update `test/browser/support/showcase.ts` to include `app-shell` in `sectionIds` and assert shell target articles expose `data-obpt-shell-story`, `data-obpt-component`, `data-obpt-variant`, `data-obpt-state`, and `data-obpt-a11y-target` exactly like generated metadata. Do not hardcode the six shell ids in TypeScript support; only validate generated manifest data.</action>
  <verify>
    <automated>npm run showcase:manifest && node test/browser/support/manifest-smoke.mjs</automated>
    <automated>npm run showcase:manifest && scripts/with-showcase-server.sh npm run visual:a11y:host -- test/browser/specs/showcase.structure.spec.ts --project chromium-320</automated>
  </verify>
  <acceptance_criteria>
    - Generated manifest validates as schema version 4 with 9 scenarios, 7 primitive stories, 9 form stories, 6 shell stories, and 31 total targets.
    - TypeScript exports `shellStories` and the unified `targets` union includes shell targets.
    - Browser structure support finds the `app-shell` section and every generated shell target exactly once.
    - `rg 'shell-nine-surface-nav|shell-mobile-collapsed' test/browser --glob '*.ts' --glob '*.mjs'` returns no copied story-id inventory outside the behavior spec lookups.
  </acceptance_criteria>
  <done>Manifest and browser support treat shell stories as generated first-class targets.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| shell catalog -> showcase HEEx | Dev/test metadata and copy render into story cells. |
| shell catalog -> JSON manifest | Elixir story data becomes browser test input. |
| generated manifest -> TypeScript tests | Browser tests trust manifest selector/path metadata after validation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-76-01 | Tampering | shell story labels/context | mitigate | Render through HEEx and test escaped hostile long-context copy. |
| T-76-02 | Spoofing | shell story active route | mitigate | Story `current_path` values use the same closed AppShell nav/breadcrumb helpers as production layout. |
| T-76-03 | Information Disclosure | actor/context story data | mitigate | Stories use explicit test actor data and the production actor-label helper; no host tenant/environment claims. |
| T-76-08 | Tampering | generated manifest | mitigate | Schema 4 validators check kind, id prefix, target derivation, order, and exact rendered metadata. |
| T-76-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs are planned. Stop for legitimacy review if an install appears. |
</threat_model>

<verification>
Run ExUnit catalog/showcase tests, regenerate manifest, run smoke validation, and run the chromium-320 showcase structure spec.
</verification>

<success_criteria>
The shell is visible in the showcase as generated manifest targets, with no TypeScript-owned shell target inventory and no pollution of existing story catalogs.
</success_criteria>

<output>
Create `.planning/phases/76-navigation-app-shell/76-04-SUMMARY.md` when done.
</output>
