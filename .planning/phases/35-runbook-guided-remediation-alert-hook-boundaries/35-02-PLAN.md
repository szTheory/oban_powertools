---
phase: 35-runbook-guided-remediation-alert-hook-boundaries
plan: 02
type: execute
wave: 2
depends_on:
  - 35-01
files_modified:
  - lib/oban_powertools/runtime_config.ex
  - lib/oban_powertools/host_escalation_handler.ex
  - lib/oban_powertools/host_escalation.ex
  - lib/oban_powertools/lifeline.ex
  - lib/oban_powertools/web/control_plane_presenter.ex
  - lib/oban_powertools/web/lifeline_live.ex
  - lib/oban_powertools/web/forensics_live.ex
  - test/oban_powertools/host_escalation_test.exs
  - test/oban_powertools/lifeline_test.exs
  - test/oban_powertools/web/live/lifeline_live_test.exs
  - test/oban_powertools/web/live/forensics_live_test.exs
autonomous: true
requirements:
  - RNB-03
  - HST-05
requirements_addressed:
  - RNB-03
  - HST-05
must_haves:
  truths:
    - "Escalation delivery remains host-owned; Powertools emits bounded facts and records truthful fallback status."
    - "Missing escalation wiring is explicit (`host-owned follow-up unavailable`) and never implied as delivered alerting."
    - "Hook payloads stay low-cardinality and avoid provider-specific destination semantics."
    - "Hook execution failures do not roll back successful native remediation."
  artifacts:
    - path: "lib/oban_powertools/host_escalation_handler.ex"
      provides: "Optional host callback behavior contract for escalation seams"
      exports: ["handle_escalation/1"]
    - path: "lib/oban_powertools/host_escalation.ex"
      provides: "Dispatcher for configured/unconfigured/failure host follow-up statuses"
      contains: "dispatch/2"
    - path: "lib/oban_powertools/lifeline.ex"
      provides: "Post-remediation host follow-up emission and audit facts"
      contains: "HostEscalation.dispatch"
  key_links:
    - from: "lib/oban_powertools/lifeline.ex"
      to: "lib/oban_powertools/host_escalation.ex"
      via: "post-repair host-follow-up dispatch"
      pattern: "HostEscalation\\.dispatch"
    - from: "lib/oban_powertools/host_escalation.ex"
      to: "lib/oban_powertools/runtime_config.ex"
      via: "optional callback handler lookup"
      pattern: "RuntimeConfig\\.host_escalation_handler"
---

<objective>
Introduce a narrow host-owned escalation seam with explicit unconfigured/configured/failure states and no provider lock-in.

Purpose: satisfy HST-05 while preserving RNB-03 continuity by recording truthful host follow-up outcomes around native remediation attempts without claiming first-party alert delivery.
Output: optional host escalation behavior + dispatcher, post-remediation hook emission with bounded payloads, and operator-visible fallback/status proof.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/35-runbook-guided-remediation-alert-hook-boundaries/35-CONTEXT.md
@.planning/phases/35-runbook-guided-remediation-alert-hook-boundaries/35-RESEARCH.md
@.planning/phases/35-runbook-guided-remediation-alert-hook-boundaries/35-VALIDATION.md
@.planning/phases/35-runbook-guided-remediation-alert-hook-boundaries/35-UI-SPEC.md
@lib/oban_powertools/runtime_config.ex
@lib/oban_powertools/workflow/callback_handler.ex
@lib/oban_powertools/lifeline.ex
@lib/oban_powertools/web/control_plane_presenter.ex
@lib/oban_powertools/web/lifeline_live.ex
@lib/oban_powertools/web/forensics_live.ex
@test/oban_powertools/lifeline_test.exs
@test/oban_powertools/web/live/lifeline_live_test.exs
@test/oban_powertools/web/live/forensics_live_test.exs

<interfaces>
New host seam contracts:

```elixir
# lib/oban_powertools/host_escalation_handler.ex
@callback handle_escalation(map()) :: :ok | {:ok, map()} | {:error, term()}
```

```elixir
# lib/oban_powertools/host_escalation.ex
def dispatch(event_facts, opts \\ [])
# returns:
# %{status: "host_owned_follow_up_unconfigured" | "host_owned_follow_up_callback_invoked" | "host_owned_follow_up_callback_failed", details: map()}
```

Runtime config seam:

```elixir
# lib/oban_powertools/runtime_config.ex
def host_escalation_handler(opts \\ [])
```

Event facts payload (bounded, provider-agnostic):

```elixir
%{
  "event_name" => "lifeline.remediation_attempt",
  "attempt_state" => "succeeded",
  "action" => "...",
  "target_type" => "...",
  "target_id" => "...",
  "incident_fingerprint" => "...",
  "ownership" => "host-owned follow-up",
  "runbook_context" => %{...stable facts...}
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add optional host escalation behavior and dispatcher</name>
  <files>lib/oban_powertools/host_escalation_handler.ex, lib/oban_powertools/host_escalation.ex, lib/oban_powertools/runtime_config.ex, test/oban_powertools/host_escalation_test.exs</files>
  <read_first>
    .planning/phases/35-runbook-guided-remediation-alert-hook-boundaries/35-CONTEXT.md
    lib/oban_powertools/runtime_config.ex
    lib/oban_powertools/workflow/callback_handler.ex
    test/oban_powertools/lifeline_test.exs
  </read_first>
  <behavior>
    - Test 1: when no handler is configured, dispatcher returns `host_owned_follow_up_unconfigured` with explicit fallback detail.
    - Test 2: when a fake handler returns `:ok` or `{:ok, map()}`, dispatcher returns `host_owned_follow_up_callback_invoked`.
    - Test 3: when handler raises or returns `{:error, reason}`, dispatcher returns `host_owned_follow_up_callback_failed` without crashing callers.
  </behavior>
  <action>Create `HostEscalationHandler` behavior module with one callback `handle_escalation/1`. Create `HostEscalation` module with `dispatch/2`, `dispatch_status/1`, and `normalize_handler_result/1` helpers. Add optional `RuntimeConfig.host_escalation_handler/1` that reads `:host_escalation_handler` but does not raise when absent. In dispatcher, build deterministic status maps with exact statuses: `host_owned_follow_up_unconfigured`, `host_owned_follow_up_callback_invoked`, and `host_owned_follow_up_callback_failed`. Keep payload keys provider-agnostic (`event_name`, `attempt_state`, `action`, `target_type`, `target_id`, `incident_fingerprint`, `ownership`, `runbook_context`) and avoid destination/provider fields.</action>
  <verify>
    <automated>mix test test/oban_powertools/host_escalation_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "defmodule ObanPowertools.HostEscalationHandler|@callback handle_escalation" lib/oban_powertools/host_escalation_handler.ex` finds behavior contract.
    - `rg -n "host_owned_follow_up_unconfigured|host_owned_follow_up_callback_invoked|host_owned_follow_up_callback_failed" lib/oban_powertools/host_escalation.ex test/oban_powertools/host_escalation_test.exs` finds all required statuses.
    - `rg -n "PagerDuty|Slack|webhook_url|ticket" lib/oban_powertools/host_escalation.ex` returns no provider-specific adapter logic.
  </acceptance_criteria>
  <done>Host-owned escalation seam exists as an optional, bounded, provider-agnostic callback contract.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Emit host follow-up facts after successful native remediation without rollback coupling</name>
  <files>lib/oban_powertools/lifeline.ex, test/oban_powertools/lifeline_test.exs, test/oban_powertools/host_escalation_test.exs</files>
  <read_first>
    .planning/phases/35-runbook-guided-remediation-alert-hook-boundaries/35-CONTEXT.md
    lib/oban_powertools/lifeline.ex
    lib/oban_powertools/audit.ex
    lib/oban_powertools/host_escalation.ex
    test/oban_powertools/lifeline_test.exs
  </read_first>
  <behavior>
    - Test 1: successful `execute_repair/5` emits host follow-up via dispatcher and writes an additional audit event with host follow-up status.
    - Test 2: unconfigured handler path writes audit metadata with status `host_owned_follow_up_unconfigured` and clear fallback message.
    - Test 3: failed callback path writes audit metadata with status `host_owned_follow_up_callback_failed` while repair target mutation remains successful.
  </behavior>
  <action>In `Lifeline.apply_repair/5`, keep existing `Ecto.Multi` mutation/audit transaction unchanged for remediation success. After the transaction succeeds (`{:ok, %{target: ..., preview: ...}}`), call `HostEscalation.dispatch/2` with bounded event facts derived from preview and runbook context. Record a second audit event with action `lifeline.host_follow_up` and metadata keys: `status`, `details`, `incident_fingerprint`, `preview_token`, `plan_hash`, and `runbook_context`. Ensure any callback failure is captured as status metadata and never returns `{:error, ...}` from `execute_repair/5` once native mutation succeeded.</action>
  <verify>
    <automated>mix test test/oban_powertools/lifeline_test.exs test/oban_powertools/host_escalation_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "lifeline\\.host_follow_up|HostEscalation\\.dispatch" lib/oban_powertools/lifeline.ex` finds emission and audit wiring.
    - `rg -n "host_owned_follow_up_unconfigured|host_owned_follow_up_callback_failed|lifeline\\.host_follow_up" test/oban_powertools/lifeline_test.exs` finds status assertions.
    - `rg -n "repo\\.rollback|{:error, :host_owned_follow_up" lib/oban_powertools/lifeline.ex` confirms no callback-driven rollback path was introduced.
  </acceptance_criteria>
  <done>Native remediation remains authoritative while host follow-up status is emitted and auditable as a separate ownership seam.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Surface truthful host follow-up status in remediation and forensic UI</name>
  <files>lib/oban_powertools/web/control_plane_presenter.ex, lib/oban_powertools/web/lifeline_live.ex, lib/oban_powertools/web/forensics_live.ex, test/oban_powertools/web/live/lifeline_live_test.exs, test/oban_powertools/web/live/forensics_live_test.exs</files>
  <read_first>
    .planning/phases/35-runbook-guided-remediation-alert-hook-boundaries/35-UI-SPEC.md
    lib/oban_powertools/web/control_plane_presenter.ex
    lib/oban_powertools/web/lifeline_live.ex
    lib/oban_powertools/web/forensics_live.ex
    test/oban_powertools/web/live/lifeline_live_test.exs
    test/oban_powertools/web/live/forensics_live_test.exs
  </read_first>
  <behavior>
    - Test 1: unconfigured path renders explicit fallback copy (`Host-owned follow-up unavailable` / `No host escalation hook configured`).
    - Test 2: configured callback success renders host-owned follow-up invoked status without implying delivery guarantees.
    - Test 3: callback failure renders warning state and preserves ownership triad distinction from native/bridge-only paths.
  </behavior>
  <action>Add presenter helper `host_follow_up_status_label/1` mapping the three status values to exact UI-safe text. In `LifelineLive`, add a host-follow-up status row under the continuity panel using presenter helper output and a neutral link label (`host-owned follow-up status`). In `ForensicsLive`, render last host follow-up status inside runbook continuity details when available. Use UI-SPEC-approved fallback phrases: `Host-owned follow-up unavailable` and `No host escalation hook configured`. Do not render provider brand names, destination URLs, or claims of guaranteed delivery.</action>
  <verify>
    <automated>mix test test/oban_powertools/web/live/lifeline_live_test.exs test/oban_powertools/web/live/forensics_live_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "host_follow_up_status_label|Host-owned follow-up unavailable|No host escalation hook configured" lib/oban_powertools/web/control_plane_presenter.ex lib/oban_powertools/web/lifeline_live.ex lib/oban_powertools/web/forensics_live.ex` finds exact copy/helpers.
    - `rg -n "host_owned_follow_up_unconfigured|host_owned_follow_up_callback_invoked|host_owned_follow_up_callback_failed" test/oban_powertools/web/live/lifeline_live_test.exs test/oban_powertools/web/live/forensics_live_test.exs` finds state assertions.
    - `rg -n "PagerDuty|Slack|ticket created|alert delivered" lib/oban_powertools/web/lifeline_live.ex lib/oban_powertools/web/forensics_live.ex` returns no first-party delivery claims.
  </acceptance_criteria>
  <done>UI surfaces show honest host-owned follow-up status without blurring ownership or over-claiming delivery outcomes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| native mutation -> host callback | Optional callback receives bounded event facts after native remediation succeeds. |
| callback result -> audit/UI truth | Callback outcomes become durable operator-facing status and must remain explicit/honest. |
| runtime config -> callback execution | Host wiring controls handler selection and should not trigger hard failures when absent. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-35-02-01 | Spoofing | Host follow-up statuses | mitigate | Restrict status values to three explicit enum-like strings; map to fixed presenter labels. |
| T-35-02-02 | Tampering | Callback payload contract | mitigate | Emit server-generated facts only; no user-supplied provider destination fields. |
| T-35-02-03 | Repudiation | Host callback outcomes | mitigate | Write dedicated `lifeline.host_follow_up` audit records including status and selector context. |
| T-35-02-04 | Information Disclosure | Callback/UI metadata | mitigate | Avoid credentials, provider endpoints, and high-cardinality destination metadata in payload and UI. |
| T-35-02-05 | Denial of Service | Callback failure path | mitigate | Callback errors produce `host_owned_follow_up_callback_failed` status without rolling back native remediation transaction. |
</threat_model>

<verification>
Run:

```bash
mix test test/oban_powertools/host_escalation_test.exs test/oban_powertools/lifeline_test.exs test/oban_powertools/web/live/lifeline_live_test.exs test/oban_powertools/web/live/forensics_live_test.exs
```

Then verify no provider lock-in strings:

```bash
rg -n "PagerDuty|Slack|Opsgenie|ticket system|webhook destination" lib/oban_powertools/host_escalation.ex lib/oban_powertools/lifeline.ex lib/oban_powertools/web/lifeline_live.ex lib/oban_powertools/web/forensics_live.ex
```
</verification>

<success_criteria>
- Optional host escalation seam exists and supports unconfigured, success, and failure statuses.
- Native remediation success path cannot be rolled back by host callback outcomes.
- Host follow-up outcomes are auditable and visible with truthful ownership language.
- Provider-specific integrations remain out of scope for core implementation.
- HST-05 boundary and RNB-03 continuity remain aligned.
</success_criteria>

<source_audit>
SOURCE | ID | Feature/Requirement | Plan | Status | Notes
GOAL | phase-35 | Add explicit host-owned escalation seams with truthful fallback | 35-02 | COVERED | Optional callback seam + fallback status.
REQ | HST-05 | Host integration seam without lost ownership boundaries | 35-02 | COVERED | Behavior contract, dispatcher, audit, and UI.
REQ | RNB-03 | Continuity preserved through remediation and follow-up | 35-02 | COVERED | Post-remediation status tied to runbook context.
CONTEXT | D-09-D-12 | Narrow host seam, explicit fallback, no provider adapters, bounded payloads | 35-02 | COVERED | Task 1 and 2 constraints.
CONTEXT | D-10/D-16 | Honest unconfigured messaging and host-owned rendering | 35-02 | COVERED | Task 3 copy contracts.
RESEARCH | callback-pattern | Reuse runtime-config callback style; keep seam minimal | 35-02 | COVERED | Mirrors workflow callback style without outbox expansion.
</source_audit>

<output>
After completion, create `.planning/phases/35-runbook-guided-remediation-alert-hook-boundaries/35-02-SUMMARY.md`.
</output>
