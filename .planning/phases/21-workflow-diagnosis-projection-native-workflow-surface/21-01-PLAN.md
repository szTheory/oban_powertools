---
phase: 21-workflow-diagnosis-projection-native-workflow-surface
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/oban_powertools/workflow/runtime.ex
  - lib/oban_powertools/explain.ex
  - test/oban_powertools/workflow_runtime_test.exs
  - test/oban_powertools/explain_test.exs
autonomous: true
requirements:
  - DIA-01
  - VER-01
must_haves:
  truths:
    - "Workflow and step diagnosis vocabulary comes from one shared durable-truth projector instead of ad hoc UI narration."
    - "Final terminal truth outranks lingering request evidence once durable reconciliation has settled."
    - "Allowed next action guidance is informative and grounded in durable refusal plus legal-next-step evidence, not optimistic UI invention."
  artifacts:
    - path: "lib/oban_powertools/workflow/runtime.ex"
      provides: "Canonical diagnosis ordering, terminal-truth precedence, and explicit unsupported or unknown fallback behavior"
    - path: "lib/oban_powertools/explain.ex"
      provides: "Stable workflow and step diagnosis read model with headline, evidence, raw facts, and allowed-next-action guidance"
    - path: "test/oban_powertools/workflow_runtime_test.exs"
      provides: "Proof that terminal truth outranks stale cancel-request evidence and unknown cases stay explicit"
    - path: "test/oban_powertools/explain_test.exs"
      provides: "Proof that the shared projector emits diagnosis-first, evidence-backed workflow and step stories"
  key_links:
    - from: "lib/oban_powertools/workflow/runtime.ex"
      to: "lib/oban_powertools/explain.ex"
      via: "runtime diagnosis ordering must flow directly into the shared workflow projector"
      pattern: "workflow_diagnosis|step_diagnosis|workflow_story|step_story"
    - from: "lib/oban_powertools/explain.ex"
      to: "test/oban_powertools/explain_test.exs"
      via: "new projector fields and guidance must be proven at the shared seam before LiveView uses them"
      pattern: "headline|evidence|allowed_next_action|raw_facts"
---

<objective>
Expand the shared workflow diagnosis projector and correct runtime diagnosis precedence before any page-level UX reshaping happens.

Purpose: make durable workflow truth explainable from one shared seam so both workflow UI and Lifeline inherit the same meaning.
Output: corrected runtime diagnosis ordering plus an expanded `ObanPowertools.Explain` read model that emits diagnosis, evidence, raw facts, and guidance-only legal next actions.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/21-workflow-diagnosis-projection-native-workflow-surface/21-CONTEXT.md
@.planning/phases/21-workflow-diagnosis-projection-native-workflow-surface/21-RESEARCH.md
@.planning/phases/21-workflow-diagnosis-projection-native-workflow-surface/21-PATTERNS.md
@.planning/phases/21-workflow-diagnosis-projection-native-workflow-surface/21-VALIDATION.md
@lib/oban_powertools/workflow/runtime.ex
@lib/oban_powertools/explain.ex
@test/oban_powertools/workflow_runtime_test.exs
@test/oban_powertools/explain_test.exs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expand `Explain` into the shared workflow diagnosis projector</name>
  <files>lib/oban_powertools/explain.ex, test/oban_powertools/explain_test.exs</files>
  <read_first>.planning/phases/21-workflow-diagnosis-projection-native-workflow-surface/21-CONTEXT.md, .planning/phases/21-workflow-diagnosis-projection-native-workflow-surface/21-RESEARCH.md, lib/oban_powertools/explain.ex, test/oban_powertools/explain_test.exs</read_first>
  <action>Per D-08-D-15 and D-27-D-30, expand `workflow_story/3` and `step_story/2` from thin metadata bundles into the stable diagnosis read model for this milestone. Keep the projector behind `ObanPowertools.Explain`, and add bounded fields that the context explicitly asks for: a concise headline, diagnosis, evidence items, raw durable facts, guidance-only allowed next action, and refusal or callback or recovery summaries where present. Reuse existing durable seams such as `latest_rejection`, callback posture, recovery sessions, blocker details, and dependency snapshots. Do not invent UI-only copy or branch-specific semantics in HEEx; the output should be stable enough for both workflow UI and Lifeline to consume.</action>
  <acceptance_criteria>
    - `workflow_story/3` returns a diagnosis-first workflow read model rather than only loose metadata fields.
    - `step_story/2` returns enough bounded evidence for primary-step detail and Lifeline parity.
    - Allowed-next-action guidance is derived from durable refusal or legal-next-step evidence where possible.
    - Raw facts remain available without becoming the default operator summary.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/oban_powertools/explain_test.exs</automated>
  </verify>
  <done>The repo has one shared workflow explanation projector that downstream surfaces can consume without inventing their own meaning.</done>
</task>

<task type="auto">
  <name>Task 2: Correct runtime diagnosis precedence and explicit unknown-state posture</name>
  <files>lib/oban_powertools/workflow/runtime.ex, lib/oban_powertools/explain.ex, test/oban_powertools/workflow_runtime_test.exs, test/oban_powertools/explain_test.exs</files>
  <read_first>.planning/phases/21-workflow-diagnosis-projection-native-workflow-surface/21-CONTEXT.md, lib/oban_powertools/workflow/runtime.ex, lib/oban_powertools/explain.ex, test/oban_powertools/workflow_runtime_test.exs</read_first>
  <action>Per D-09, D-15, and D-30 plus the research note about current `cancel_requested` ordering, refactor workflow and step diagnosis precedence so final terminal truth outranks lingering request evidence once durable facts settle. Preserve in-flight `cancel_requested` guidance while ensuring settled outcomes such as `completed_after_cancel_request`, `expired_wait`, or other bounded terminal causes become the diagnosis once they are known. Add an explicit unsupported or unknown fallback path when durable facts do not justify a confident diagnosis; do not smooth ambiguous cases over with generic optimistic copy. The projector should consume this corrected runtime ordering directly.</action>
  <acceptance_criteria>
    - `workflow_diagnosis/2` and `step_diagnosis/1` prefer final durable truth over stale request evidence once settled.
    - Unsupported or ambiguous cases are surfaced explicitly instead of guessed.
    - `Explain` inherits the corrected precedence without bespoke overrides.
    - Automated proof covers at least one post-cancel terminal case and one unsupported or unknown path.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/oban_powertools/workflow_runtime_test.exs test/oban_powertools/explain_test.exs</automated>
    <automated>rg -n "cancel_requested|completed_after_cancel_request|expired_wait|unsupported|unknown|workflow_diagnosis|step_diagnosis" lib/oban_powertools/workflow/runtime.ex lib/oban_powertools/explain.ex test/oban_powertools/workflow_runtime_test.exs test/oban_powertools/explain_test.exs</automated>
  </verify>
  <done>The diagnosis vocabulary becomes durable-truth-first and explicit about unsupported cases before any UI consumes it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Durable runtime facts -> projected diagnosis | The projector must not invent meaning that runtime facts do not support. |
| Request evidence -> final terminal truth | Lingering cancel-request evidence must not outrank settled workflow truth. |
| Refusal metadata -> allowed-next-action guidance | Guidance must come from durable legal-next-step evidence, not optimistic UI assumptions. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-01 | Tampering | diagnosis ordering | mitigate | Move terminal-truth precedence into runtime-owned helpers and prove it with focused tests. |
| T-21-02 | Information Disclosure / support-truth drift | shared projector | mitigate | Keep explanation fields bounded, evidence-backed, and explicit about unsupported cases. |
| T-21-03 | Repudiation | allowed-next-action guidance | mitigate | Reuse durable refusal and `legal_next_steps` evidence instead of freehand UI wording. |
</threat_model>

<verification>
- `mix test test/oban_powertools/explain_test.exs`
- `mix test test/oban_powertools/workflow_runtime_test.exs test/oban_powertools/explain_test.exs`
- `rg -n "cancel_requested|completed_after_cancel_request|expired_wait|unsupported|unknown|workflow_diagnosis|step_diagnosis" lib/oban_powertools/workflow/runtime.ex lib/oban_powertools/explain.ex test/oban_powertools/workflow_runtime_test.exs test/oban_powertools/explain_test.exs`
</verification>

<success_criteria>
- Phase 21 gains one shared workflow diagnosis projector grounded in durable runtime truth.
- Terminal outcomes are explained ahead of stale request evidence once reconciliation settles.
- Unknown or unsupported cases stay explicit and support-truthful.
</success_criteria>

<output>
After completion, create `.planning/phases/21-workflow-diagnosis-projection-native-workflow-surface/21-01-SUMMARY.md`
</output>
