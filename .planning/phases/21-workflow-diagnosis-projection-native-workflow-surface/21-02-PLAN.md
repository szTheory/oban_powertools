---
phase: 21-workflow-diagnosis-projection-native-workflow-surface
plan: 02
type: execute
wave: 2
depends_on:
  - 21-01
files_modified:
  - lib/oban_powertools/explain.ex
  - lib/oban_powertools/web/workflows_live.ex
  - test/oban_powertools/explain_test.exs
  - test/oban_powertools/web/live/workflows_live_test.exs
autonomous: true
requirements:
  - DIA-01
  - DIA-02
  - VER-01
must_haves:
  truths:
    - "The primary diagnosis step is selected from durable evidence priority rather than display order or first blocked row."
    - "Workflow detail routing stays patch-addressable and shareable while the server still names the canonical primary diagnosis anchor."
    - "Unsupported or unknown workflow states remain explicit in the native workflow detail path."
  artifacts:
    - path: "lib/oban_powertools/explain.ex"
      provides: "Primary-step prioritization helpers and bounded reason strings for why a step is the diagnosis anchor"
    - path: "lib/oban_powertools/web/workflows_live.ex"
      provides: "Server-chosen primary-step defaulting, `?step=` override behavior, and explicit unsupported-state rendering"
    - path: "test/oban_powertools/explain_test.exs"
      provides: "Proof for durable-evidence-based primary-step selection"
    - path: "test/oban_powertools/web/live/workflows_live_test.exs"
      provides: "Proof for patch-preserving step navigation and explicit unsupported-state copy"
  key_links:
    - from: "lib/oban_powertools/explain.ex"
      to: "lib/oban_powertools/web/workflows_live.ex"
      via: "primary-step selection logic should be shared and deterministic, not reimplemented in LiveView"
      pattern: "primary_step|selected_step|anchor_reason"
    - from: "lib/oban_powertools/web/workflows_live.ex"
      to: "test/oban_powertools/web/live/workflows_live_test.exs"
      via: "patch-driven detail overrides must coexist with the server-picked diagnosis anchor"
      pattern: "?step=|handle_params|patch"
---

<objective>
Make the workflow detail surface choose and explain one primary diagnosis step while preserving patch-driven inspection of the rest of the DAG.

Purpose: stop the workflow page from defaulting to whichever blocked step appears first and replace that heuristic with a durable-evidence-based diagnosis anchor.
Output: shared primary-step selection helpers, explicit anchor reasoning, and LiveView behavior that keeps `?step=` overrides while defaulting to the canonical diagnosis step.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/21-workflow-diagnosis-projection-native-workflow-surface/21-CONTEXT.md
@.planning/phases/21-workflow-diagnosis-projection-native-workflow-surface/21-RESEARCH.md
@.planning/phases/21-workflow-diagnosis-projection-native-workflow-surface/21-PATTERNS.md
@.planning/phases/21-workflow-diagnosis-projection-native-workflow-surface/21-VALIDATION.md
@.planning/phases/21-workflow-diagnosis-projection-native-workflow-surface/21-01-PLAN.md
@lib/oban_powertools/explain.ex
@lib/oban_powertools/web/workflows_live.ex
@test/oban_powertools/explain_test.exs
@test/oban_powertools/web/live/workflows_live_test.exs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace first-blocked-step fallback with durable primary-step selection</name>
  <files>lib/oban_powertools/explain.ex, lib/oban_powertools/web/workflows_live.ex, test/oban_powertools/explain_test.exs, test/oban_powertools/web/live/workflows_live_test.exs</files>
  <read_first>.planning/phases/21-workflow-diagnosis-projection-native-workflow-surface/21-CONTEXT.md, .planning/phases/21-workflow-diagnosis-projection-native-workflow-surface/21-RESEARCH.md, lib/oban_powertools/explain.ex, lib/oban_powertools/web/workflows_live.ex</read_first>
  <action>Per D-16-D-21, add a deterministic primary-step selection helper behind the shared projector that follows the locked durable evidence priority: final or terminal actionable cause first, then expired wait or waiting on signal, then waiting on retryable dependency, then missing dependency result, then cancel-requested branch state. Return both the primary step and a bounded anchor-reason explanation so the page can show why this step is the default diagnosis focus. Update `WorkflowsLive` to use that server-owned default instead of `List.first(Enum.filter(... blocker_codes ...))`, while preserving explicit `?step=` overrides for manual inspection.</action>
  <acceptance_criteria>
    - Default selected step comes from shared durable-evidence priority rather than display order.
    - The workflow page still supports URL-driven `?step=` overrides.
    - The selected-step UI indicates which step is the diagnosis anchor and why.
    - Tests cover at least one case where the chosen primary step is not the first blocked step in position order.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/oban_powertools/explain_test.exs test/oban_powertools/web/live/workflows_live_test.exs</automated>
  </verify>
  <done>The workflow page defaults to the durable diagnosis anchor instead of the first visually convenient step.</done>
</task>

<task type="auto">
  <name>Task 2: Make unsupported and unknown workflow detail states explicit in the native surface</name>
  <files>lib/oban_powertools/explain.ex, lib/oban_powertools/web/workflows_live.ex, test/oban_powertools/explain_test.exs, test/oban_powertools/web/live/workflows_live_test.exs</files>
  <read_first>.planning/phases/21-workflow-diagnosis-projection-native-workflow-surface/21-CONTEXT.md, lib/oban_powertools/explain.ex, lib/oban_powertools/web/workflows_live.ex, test/oban_powertools/web/live/workflows_live_test.exs</read_first>
  <action>Per D-09, D-15, D-34, and the UI-SPEC copy contract, teach the workflow detail path to render explicit unsupported or unknown states from the shared projector instead of silently falling back to raw row labels. Keep the posture read-only and support-truthful: tell operators the screen is showing durable evidence only, that no additional action is available in this phase, and that bounded guidance may still come from refusal or legal-next-step evidence where present. Do not add clickable mutation controls or phase-22 preview affordances here.</action>
  <acceptance_criteria>
    - Unsupported or unknown states render explicit copy instead of generic fallback state labels alone.
    - Read-only guidance stays phase-21-scoped and does not imply executable mutation controls.
    - `WorkflowsLive` continues to use projector-owned semantics rather than ad hoc branch wording.
    - LiveView tests cover at least one unsupported or unknown render path.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/oban_powertools/explain_test.exs test/oban_powertools/web/live/workflows_live_test.exs</automated>
    <automated>rg -n "unsupported|unknown|legal next action|primary step|anchor" lib/oban_powertools/explain.ex lib/oban_powertools/web/workflows_live.ex test/oban_powertools/explain_test.exs test/oban_powertools/web/live/workflows_live_test.exs</automated>
  </verify>
  <done>The native workflow detail path stays explicit and support-truthful even when durable facts do not fit a confident happy-path diagnosis.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Durable step evidence -> selected-step default | The page must not privilege display order over actual diagnosis priority. |
| Shareable URL state -> server-owned diagnosis anchor | Operators need manual inspection without losing the canonical primary-step story. |
| Unsupported durable states -> operator guidance | The page must stay explicit rather than inventing confident actions for ambiguous facts. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-04 | Tampering | selected-step default | mitigate | Centralize primary-step selection in the shared projector and prove it with focused tests. |
| T-21-05 | Information Disclosure / UX ambiguity | unsupported-state rendering | mitigate | Render explicit unknown or unsupported copy and keep guidance informational-only. |
</threat_model>

<verification>
- `mix test test/oban_powertools/explain_test.exs test/oban_powertools/web/live/workflows_live_test.exs`
- `rg -n "unsupported|unknown|legal next action|primary step|anchor" lib/oban_powertools/explain.ex lib/oban_powertools/web/workflows_live.ex test/oban_powertools/explain_test.exs test/oban_powertools/web/live/workflows_live_test.exs`
</verification>

<success_criteria>
- The workflow detail default is chosen by durable diagnosis priority, not by row order.
- Operators can still inspect any step via patch navigation without losing the canonical diagnosis anchor.
- Unsupported and unknown cases stay explicit and read-only.
</success_criteria>

<output>
After completion, create `.planning/phases/21-workflow-diagnosis-projection-native-workflow-surface/21-02-SUMMARY.md`
</output>
