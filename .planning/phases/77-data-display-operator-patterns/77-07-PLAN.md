---
phase: 77-data-display-operator-patterns
plan: 07
type: execute
wave: 7
depends_on:
  - 77-06
files_modified:
  - test/browser/specs/data-display.behavior.spec.ts
  - test/browser/support/verify-data-baselines.mjs
  - "test/browser/__screenshots__/chromium-*/showcase/data-*/*.png"
  - .planning/phases/77-data-display-operator-patterns/77-VALIDATION.md
autonomous: true
requirements:
  - DATA-01
  - DATA-02
  - DATA-03
  - DATA-04
  - A11Y-02
must_haves:
  truths:
    - "D-24..D-25 have live focused behavior, axe, structure, and canonical Docker VRT compare evidence; discovery or file existence alone is insufficient."
    - "Browser checks prove parent-owned sorting, one-DOM 320px cards, focus/hit targets, overflow, long/code behavior, redaction non-exposure, toast urgency, progress semantics, and bounded large-row rendering."
    - "Exactly 120 data PNG baselines exist; the 108 unrelated scenario-only residual remains untouched and explicitly prevents any false aggregate-green claim."
  artifacts:
    - path: "test/browser/specs/data-display.behavior.spec.ts"
      provides: "Focused data-display interaction, responsive, accessibility, security, and bounded-data proof"
    - path: "test/browser/__screenshots__/chromium-*/showcase/data-*/*.png"
      provides: "120 canonical Docker-rendered data baselines"
    - path: "test/browser/support/verify-data-baselines.mjs"
      provides: "Manifest-derived exact baseline-matrix and screenshot change-scope gate"
    - path: ".planning/phases/77-data-display-operator-patterns/77-VALIDATION.md"
      provides: "Reconciled task/wave map and executed Nyquist evidence"
  key_links:
    - from: "test/browser/specs/data-display.behavior.spec.ts"
      to: "test/browser/support/manifest.ts"
      via: "generated dataStories"
      pattern: "dataStories"
    - from: "test/browser/specs/showcase.vrt.spec.ts"
      to: "test/browser/__screenshots__/chromium-*/showcase/data-*/*.png"
      via: "generated data targets and canonical Docker screenshot compare"
      pattern: "data-"
---

<objective>
Close Phase 77 with live browser behavior, focused axe/structure/VRT evidence, exactly 120 reviewed baselines, and a reconciled Nyquist record.

Purpose: verify semantics, confidentiality, responsiveness, and stress behavior that render tests or axe alone cannot prove.
Output: green focused behavior on 320/wide, green focused data axe and canonical Docker VRT compare, committed data-only PNGs, and signed-off `77-VALIDATION.md` that preserves unrelated residual truth.
</objective>

<execution_context>
@/Users/jon/.agents/gsd-core/workflows/execute-plan.md
@/Users/jon/.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/77-data-display-operator-patterns/77-CONTEXT.md
@.planning/phases/77-data-display-operator-patterns/77-UI-SPEC.md
@.planning/phases/77-data-display-operator-patterns/77-RESEARCH.md
@.planning/phases/77-data-display-operator-patterns/77-PATTERNS.md
@.planning/phases/77-data-display-operator-patterns/77-VALIDATION.md
@.planning/phases/77-data-display-operator-patterns/77-06-SUMMARY.md
@.planning/phases/76-navigation-app-shell/76-VALIDATION.md
@guides/visual-regression-and-a11y.md
</context>

## Artifacts this phase produces

- Exact private/support symbols: taxonomy `normalize_state/1`, `humanize/1`, `domain!/1`, `resolve_alias/2`; DataDisplay `visual_safe_rest/2`, `action_attr?/1`, `data_state/1`, `row_dom_id/2`, `aria_sort/3`, `truncate_middle/2`, `preserve_suffix/2`, `clamp_progress/2`, and `render_normalized_display/1` (or private clauses under that exact dispatcher name).
- Created files: `lib/oban_powertools/web/status_taxonomy.ex`, `lib/oban_powertools/web/components/data_display.ex`, `test/oban_powertools/web/status_taxonomy_test.exs`, `test/oban_powertools/web/components/data_display_test.exs`, `test/support/data_display_story_catalog.ex`, `test/oban_powertools/data_display_story_catalog_test.exs`, `test/browser/specs/data-display.behavior.spec.ts`, and `test/browser/support/verify-data-baselines.mjs`; generated files are exactly 120 `test/browser/__screenshots__/chromium-{320,tablet,wide}/showcase/data-*/*.png` baselines.
- Baseline verification CLI: `test/browser/support/verify-data-baselines.mjs` defaults to validating the exact manifest-derived data screenshot set with cardinality 120; its created `--changed-scope` mode rejects tracked, untracked, and renamed screenshot changes outside the expected `data-*` matrix.
- Exact CSS symbols: `.obpt-data-table`, `.obpt-data-state`, `.obpt-description-list`, `.obpt-key-value`, `.obpt-machine-value`, `.obpt-timeline`, `.obpt-progress`, `.obpt-metric-card`, `.obpt-code-block`, `.obpt-args-viewer`, `.obpt-redacted-value`, `.obpt-empty-state`, `.obpt-toast`, and `.obpt-flash-group`, all rooted below `.obpt-root` and copied into the packaged CSS by the deterministic asset build.
- Complete attr/slot manifest: `data_table/1` attrs `id/caption/rows/row_id/state/resource/row_count/pagination_summary/sort_key/sort_direction/sort_event/rest` and slots `toolbar/selection/col(label,sort_key,value_kind)/action/state_detail`; `status_pill/1` attrs `id/domain/state/rest`; `description_list/1` attrs `id/state/resource/rest` and repeated `item(label,value_kind)`; `key_value/1` attrs `id/label/value/value_kind/rest`; `machine_value/1` attrs `id/value/kind/truncate/expand/rest`; `timeline/1` attrs `id/state/resource/rest` and repeated `event(timestamp,title,source,domain,state,detail)`; `progress_bar/1` attrs `id/label/value/max/state/rest`; `metric_card/1` attrs `id/label/value/trend/status/tone/rest` plus `action`; `code_block/1` attrs `id/label/content/language/rest`; `args_viewer/1` attrs `id/label/display/kind`; `redacted_value/1` attrs `id/reason/message`; `empty_state/1` attrs `id/heading/body/rest` plus `action`; `toast/1` attrs `id/tone/urgency/dismiss_event/rest` plus `inner_block`; `flash_group/1` attrs `id/flash/dismiss_event/rest`.
- Production module/API set: `StatusTaxonomy.spec/2`, `all_specs/0`; DataDisplay `data_table/1`, `status_pill/1`, `description_list/1`, `key_value/1`, `machine_value/1`, `timeline/1`, `progress_bar/1`, `metric_card/1`, `code_block/1`, `args_viewer/1`, `redacted_value/1`, `empty_state/1`, `toast/1`, `flash_group/1` and the exact attrs/slots/private state/sort/truncate/clamp/safe-rest/normalized-display helpers established by Plans 02-05.
- CSS/package set: all `.obpt-root` table/state/description/timeline/progress/metric/machine/code/args/redaction/empty/toast selectors and deterministic packaged CSS.
- Catalog/showcase set: `ObanPowertools.DataDisplayStoryCatalog` with `stories/0`, `story!/1`, `snapshot_name/1`, `a11y_target/1`, `huge_args_fixture/0`, `long_value_fixture/0`, `timeline_fixture/0`, `large_row_total/0`, `large_row_window/0`, ten locked IDs; ShowcaseLive `@data_catalog_module`, `@data_catalog_path`, `@data_stories`, `data_story_body/1`, parent sort assigns, and `handle_event("sort-data-table", ...)`.
- Manifest/browser set: `schema_version: 5`, `data_stories`, appended `targets`, `kind: "data"`, `ShowcaseDataStory`, `dataStories`, behavior helper `dataStory(id)`, `assertShowcaseStructure` data-kind branch, and `data-display.behavior.spec.ts` tests.
- Evidence set: exactly 120 data story PNGs across 10 stories x 4 themes x 3 viewports; focused structure/behavior/axe/Docker-VRT command results; reconciled `77-VALIDATION.md` task IDs/waves/statuses/Nyquist sign-off; no scenario baseline changes.

<tasks>

<task type="auto" tdd="true">
  <name>Task 77-07-01: Make focused data behavior green on 320 and wide</name>
  <files>test/browser/specs/data-display.behavior.spec.ts</files>
  <read_first>
    - @test/browser/specs/data-display.behavior.spec.ts
    - @test/browser/specs/forms.behavior.spec.ts
    - @test/browser/specs/shell.behavior.spec.ts
    - @test/browser/support/manifest.ts
    - @test/browser/support/showcase.ts
    - @lib/oban_powertools/web/dev/showcase_live.ex
    - @lib/oban_powertools/web/components/data_display.ex
    - @assets/oban_powertools/tokens.css
    - @.planning/phases/77-data-display-operator-patterns/77-UI-SPEC.md
    - @.planning/phases/77-data-display-operator-patterns/77-PATTERNS.md
  </read_first>
  <action>Complete the Wave 0 Playwright spec against generated `dataStories` and the live example-host showcase. Reuse existing viewport/theme preparation and computed-focus helpers. Prove: click plus Enter/Space toggles parent sort and only one header owns correct `aria-sort`; focus-visible on sort/selection/expansion/code/dismiss controls in system/light/dark/high-contrast; 320 computed layout uses the same semantic table, visible mobile labels, one labelled checkbox/action per row, token-derived >=44px hit area, and document/body/story overflow delta <=1px; wide retains table layout; long IDs/modules preserve useful ends or native expansion; CodeBlock is focusable, internally scrollable, bounded, and does not move page overflow; exact redaction copy is visible while the secret sentinel is absent from text, title, every data attr, details content, and copy-related attrs; toast roles/urgency/dismiss do not steal focus; progress clamps determinate values and unavailable has no fake value; large-row story reports >=1,000 total with <=25 stable DOM rows. Keep behavior-specific story lookups only, not the complete catalog list. This task may modify only `test/browser/specs/data-display.behavior.spec.ts`; if a live assertion exposes a component, story, manifest, showcase, or CSS defect, leave the assertion failing and route the defect back to its owning Plan 77-02 through 77-06 for correction before rerunning this task. Never weaken the checks or add JS table behavior.</action>
  <verify>
    <automated>npm run showcase:manifest && npx playwright test --list test/browser/specs/data-display.behavior.spec.ts</automated>
    <automated>npm run showcase:manifest && scripts/with-showcase-server.sh scripts/playwright-docker.sh npx playwright test test/browser/specs/data-display.behavior.spec.ts --project chromium-320</automated>
    <automated>npm run showcase:manifest && scripts/with-showcase-server.sh scripts/playwright-docker.sh npx playwright test test/browser/specs/data-display.behavior.spec.ts --project chromium-wide</automated>
  </verify>
  <acceptance_criteria>
    - Discovery lists the intended focused checks and both live Docker-backed 320/wide commands pass.
    - Sorting, focus, responsive layout, selection label/target, long values, code overflow, redaction channels, toast urgency, progress, and bounded rows each have a direct behavioral assertion.
    - The 320 check proves one semantic table/one control set rather than accepting a second hidden responsive DOM.
    - No complete data story list, broad mask, client sorting, table hook/package, raw HTML, or clipboard controller is added.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 77-07-02: Generate data-only baselines and record focused Nyquist evidence</name>
  <files>test/browser/support/verify-data-baselines.mjs, test/browser/__screenshots__/chromium-*/showcase/data-*/*.png, .planning/phases/77-data-display-operator-patterns/77-VALIDATION.md</files>
  <read_first>
    - @test/browser/support/verify-data-baselines.mjs
    - @test/browser/__screenshots__/chromium-*/showcase/data-*/*.png
    - @.planning/phases/77-data-display-operator-patterns/77-VALIDATION.md
    - @guides/visual-regression-and-a11y.md
    - @package.json
    - @test/browser/specs/showcase.structure.spec.ts
    - @test/browser/specs/showcase.vrt.spec.ts
    - @test/browser/specs/showcase.a11y.spec.ts
    - @test/browser/support/manifest.ts
    - @test/browser/support/manifest-smoke.mjs
    - @.planning/phases/76-navigation-app-shell/76-VALIDATION.md
  </read_first>
  <action>Validate the focused VRT/axe grep with `--list`, then use the canonical Docker renderer to update only data targets with `--update-snapshots=changed`, followed by a no-update compare. Execute the complete `showcase.structure.spec.ts` without grep or `--list` through the Docker-backed server so its four theme tests run in all three Chromium projects; run focused data axe across the same three-project/four-theme matrix and rerun both behavior projects. Create `test/browser/support/verify-data-baselines.mjs` as an independent Node gate that reads schema-5 `test/browser/.generated/showcase-manifest.json`, derives the expected set from `data_stories[].snapshot` x manifest themes `{system,light,dark,high-contrast}` x manifest viewports mapped to projects `{chromium-320,chromium-tablet,chromium-wide}`, recursively collects actual data PNGs, and fails with separate missing/extra diagnostics unless both sets are identical and cardinality is exactly 120. Give the script a separate `--changed-scope` mode that parses `git status --porcelain=v1 -z --untracked-files=all -- test/browser/__screenshots__` and fails for every changed screenshot path outside that expected data matrix, including tracked, deleted, renamed, and untracked PNGs. Run both modes as independent commands after baseline generation. Run the full Phase 77 ExUnit/format/compile/manifest-smoke gate. Do not refresh scenario/primitive/form/shell baselines, do not commit generated JSON/reports/actuals/diffs, and do not claim `npm run visual:a11y` aggregate is green: preserve the Phase 76 record that 108 unrelated scenario-only VRT failures remain. Update `77-VALIDATION.md` to the final plan/task IDs `77-01-01`..`77-07-02`, waves 1..7, actual commands/results, file-exists/status cells, completed date, baseline cardinality, focused Docker evidence, and explicit aggregate residual. Keep `nyquist_compliant: false` until live behavior, live structure, focused data axe, the no-update Docker VRT compare, exact-120 baseline equality, `--changed-scope`, and the focused full gate have all executed green and their evidence is recorded; only then may this task flip `nyquist_compliant: true`. Set `wave_0_complete: true` only after its contract files reach meaningful RED, and change statuses from pending only for executed evidence.</action>
  <verify>
    <automated>npm run showcase:manifest && npx playwright test --list test/browser/specs/showcase.structure.spec.ts test/browser/specs/showcase.vrt.spec.ts test/browser/specs/showcase.a11y.spec.ts --grep "data data-"</automated>
    <automated>npm run showcase:manifest && scripts/with-showcase-server.sh scripts/playwright-docker.sh npx playwright test test/browser/specs/showcase.structure.spec.ts</automated>
    <automated>npm run showcase:manifest && scripts/with-showcase-server.sh scripts/playwright-docker.sh npx playwright test test/browser/specs/showcase.vrt.spec.ts --grep "data data-" --update-snapshots=changed</automated>
    <automated>npm run showcase:manifest && scripts/with-showcase-server.sh scripts/playwright-docker.sh npx playwright test test/browser/specs/showcase.vrt.spec.ts --grep "data data-"</automated>
    <automated>npm run showcase:manifest && scripts/with-showcase-server.sh scripts/playwright-docker.sh npx playwright test test/browser/specs/showcase.a11y.spec.ts --grep "data data-"</automated>
    <automated>npm run showcase:manifest && node test/browser/support/verify-data-baselines.mjs</automated>
    <automated>npm run showcase:manifest && node test/browser/support/verify-data-baselines.mjs --changed-scope</automated>
    <automated>mix format --check-formatted && mix compile --warnings-as-errors && mix test test/oban_powertools/web/status_taxonomy_test.exs test/oban_powertools/web/components/data_display_test.exs test/oban_powertools/data_display_story_catalog_test.exs test/oban_powertools/web/live/showcase_live_test.exs test/oban_powertools/web/theme_tokens_test.exs test/oban_powertools/web/assets_test.exs --seed 0 && npm run showcase:manifest && node test/browser/support/manifest-smoke.mjs</automated>
  </verify>
  <acceptance_criteria>
    - Focused list, live data structure, data axe, Docker VRT update, and subsequent Docker VRT compare all execute against generated data targets and pass; discovery alone is not recorded as proof.
    - The manifest-derived matrix command proves exactly 120 data PNGs with no missing or extra paths, and the independent `--changed-scope` command proves no changed screenshot lies outside that matrix.
    - Focused ExUnit/format/compile/manifest smoke passes and no generated report/json artifacts are selected for commit.
    - `77-VALIDATION.md` maps all final task IDs/waves/commands; Task `77-07-02` alone flips Nyquist true only after recorded green live behavior, live structure, focused axe, no-update Docker VRT compare, exact-120, changed-scope, and focused full evidence, and explicitly records the untouched 108 scenario-only residual without an aggregate-green claim.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## ASVS L1 Threat Model

| ID | Threat | Severity | Mitigation and verification |
|----|--------|----------|-----------------------------|
| T-77-07-LEAK | Raw arguments appear in text, title, tooltip, data attrs, details, screenshots, or copy buffers | high | Live browser sentinel scans every channel and VRT reviews only normalized/redacted fixtures; any sentinel hit blocks completion and baseline creation. |
| T-77-07-ATTR | Unsafe attrs/semantic overrides evade source tests in the real DOM | high | Role/aria-sort/live-region/selection relationships and hostile attrs are asserted against the live browser DOM; structure/axe add independent checks. |
| T-77-07-XSS | Hostile rendered content executes or mutates the showcase | high | Live fixtures contain HTML-looking strings; DOM assertions require escaped text and stable structure; CSP is not substituted for escaping proof. |
| T-77-07-ATOM | Unknown status/domain behavior diverges in generated stories | high | Taxonomy audit story is generated from `all_specs/0`; focused unit evidence covers atom safety/unknown domains and story tests prove no drift. |
| T-77-07-DOS | Full matrix or stress fixtures become unbounded and make gates unusable | high | Exactly ten targets, bounded <=25 DOM rows, finite payloads, focused commands, and 120-cardinality assertion constrain runtime/storage; no literal thousands-row render. |
</threat_model>

<verification>
- Execute every task-level live command, not merely `--list`.
- Review the 120 data-only baseline diff and confirm no unrelated screenshot/report/generated files changed.
- Run source-level grep/audit for raw HTML, dynamic atom conversion, inline style/width, grid role, table JS hooks, and raw/redacted dual assigns before marking Nyquist complete.
- Preserve the unrelated Phase 76 aggregate residual verbatim in validation and summary evidence.
</verification>

<success_criteria>
- Focused live behavior, structure, axe, and canonical Docker VRT compare are green for Phase 77 targets.
- Exactly 120 reviewed data baselines exist and no unrelated baseline is refreshed.
- Validation is reconciled and Nyquist-compliant with honest evidence boundaries.
- Phase 77 meets DATA-01..04/A11Y-02 at component/showcase level without page migration or false aggregate claims.
</success_criteria>

<output>
Create `.planning/phases/77-data-display-operator-patterns/77-07-SUMMARY.md` when done.
</output>
