---
phase: 77-data-display-operator-patterns
plan: 06
type: execute
wave: 6
depends_on:
  - 77-05
files_modified:
  - test/support/data_display_story_catalog.ex
  - test/oban_powertools/data_display_story_catalog_test.exs
  - lib/oban_powertools/web/dev/showcase_live.ex
  - test/oban_powertools/web/live/showcase_live_test.exs
  - scripts/showcase_manifest.exs
  - test/browser/support/manifest.ts
  - test/browser/support/manifest-smoke.mjs
  - test/browser/support/showcase.ts
  - test/browser/specs/showcase.structure.spec.ts
autonomous: true
requirements:
  - DATA-01
  - DATA-02
  - DATA-03
  - DATA-04
  - A11Y-02
must_haves:
  truths:
    - "D-21..D-23 add exactly ten deterministic dev/test-only data stories, real components, complete taxonomy/redaction/state stress, and a bounded thousands-row window with truthful totals."
    - "ShowcaseLive owns the sort assign/event used for behavior proof; production DataTable stays stateless and no production page is migrated."
    - "Manifest schema 5 appends generated data_stories after shell_stories, exports dataStories, and never hardcodes the data target list in TypeScript."
  artifacts:
    - path: "test/support/data_display_story_catalog.ex"
      provides: "ObanPowertools.DataDisplayStoryCatalog and deterministic Phase 77 fixtures"
      exports: ["stories/0", "story!/1", "snapshot_name/1", "a11y_target/1"]
    - path: "lib/oban_powertools/web/dev/showcase_live.ex"
      provides: "Real data story rendering and parent-owned showcase sort behavior"
    - path: "scripts/showcase_manifest.exs"
      provides: "Schema-5 data_stories and appended data targets"
    - path: "test/browser/support/manifest.ts"
      provides: "ShowcaseDataStory type, validation, and dataStories export"
  key_links:
    - from: "test/support/data_display_story_catalog.ex"
      to: "lib/oban_powertools/web/dev/showcase_live.ex"
      via: "runtime dev/test catalog loading"
      pattern: "DataDisplayStoryCatalog"
    - from: "scripts/showcase_manifest.exs"
      to: "test/browser/support/manifest.ts"
      via: "generated schema 5 JSON"
      pattern: "data_stories"
---

<objective>
Register the complete Phase 77 data-display surface in the dev/test showcase and generated browser manifest.

Purpose: make real component states, adversarial fixtures, and parent-owned sorting available to structure, behavior, axe, and VRT evidence without polluting domain fixtures or production pages.
Output: deterministic catalog, live showcase stories, schema-5 pipeline, and green catalog/showcase/manifest contracts.
</objective>

<execution_context>
@/Users/jon/.agents/gsd-core/workflows/execute-plan.md
@/Users/jon/.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/77-data-display-operator-patterns/77-CONTEXT.md
@.planning/phases/77-data-display-operator-patterns/77-UI-SPEC.md
@.planning/phases/77-data-display-operator-patterns/77-RESEARCH.md
@.planning/phases/77-data-display-operator-patterns/77-PATTERNS.md
@.planning/phases/77-data-display-operator-patterns/77-05-SUMMARY.md
@.planning/phases/76-navigation-app-shell/76-VALIDATION.md
</context>

## Artifacts this phase produces

- Exact private/support symbols: taxonomy `normalize_state/1`, `humanize/1`, `domain!/1`, `resolve_alias/2`; DataDisplay `visual_safe_rest/2`, `action_attr?/1`, `data_state/1`, `row_dom_id/2`, `aria_sort/3`, `truncate_middle/2`, `preserve_suffix/2`, `clamp_progress/2`, and `render_normalized_display/1` (or private clauses under that exact dispatcher name).
- Created files: `lib/oban_powertools/web/status_taxonomy.ex`, `lib/oban_powertools/web/components/data_display.ex`, `test/oban_powertools/web/status_taxonomy_test.exs`, `test/oban_powertools/web/components/data_display_test.exs`, `test/support/data_display_story_catalog.ex`, `test/oban_powertools/data_display_story_catalog_test.exs`, `test/browser/specs/data-display.behavior.spec.ts`, and `test/browser/support/verify-data-baselines.mjs`; generated files are exactly 120 `test/browser/__screenshots__/chromium-{320,tablet,wide}/showcase/data-*/*.png` baselines.
- Baseline verification CLI: `test/browser/support/verify-data-baselines.mjs` defaults to validating the exact manifest-derived data screenshot set with cardinality 120; its created `--changed-scope` mode rejects tracked, untracked, and renamed screenshot changes outside the expected `data-*` matrix.
- Exact CSS symbols: `.obpt-data-table`, `.obpt-data-state`, `.obpt-description-list`, `.obpt-key-value`, `.obpt-machine-value`, `.obpt-timeline`, `.obpt-progress`, `.obpt-metric-card`, `.obpt-code-block`, `.obpt-args-viewer`, `.obpt-redacted-value`, `.obpt-empty-state`, `.obpt-toast`, and `.obpt-flash-group`, all rooted below `.obpt-root` and copied into the packaged CSS by the deterministic asset build.
- Complete attr/slot manifest: `data_table/1` attrs `id/caption/rows/row_id/state/resource/row_count/pagination_summary/sort_key/sort_direction/sort_event/rest` and slots `toolbar/selection/col(label,sort_key,value_kind)/action/state_detail`; `status_pill/1` attrs `id/domain/state/rest`; `description_list/1` attrs `id/state/resource/rest` and repeated `item(label,value_kind)`; `key_value/1` attrs `id/label/value/value_kind/rest`; `machine_value/1` attrs `id/value/kind/truncate/expand/rest`; `timeline/1` attrs `id/state/resource/rest` and repeated `event(timestamp,title,source,domain,state,detail)`; `progress_bar/1` attrs `id/label/value/max/state/rest`; `metric_card/1` attrs `id/label/value/trend/status/tone/rest` plus `action`; `code_block/1` attrs `id/label/content/language/rest`; `args_viewer/1` attrs `id/label/display/kind`; `redacted_value/1` attrs `id/reason/message`; `empty_state/1` attrs `id/heading/body/rest` plus `action`; `toast/1` attrs `id/tone/urgency/dismiss_event/rest` plus `inner_block`; `flash_group/1` attrs `id/flash/dismiss_event/rest`.
- Production modules/components: `StatusTaxonomy.spec/2`, `all_specs/0`; DataDisplay `data_table/1`, `status_pill/1`, `description_list/1`, `key_value/1`, `machine_value/1`, `timeline/1`, `progress_bar/1`, `metric_card/1`, `code_block/1`, `args_viewer/1`, `redacted_value/1`, `empty_state/1`, `toast/1`, `flash_group/1` with the attrs/slots/private helpers defined in Plans 02-05.
- Styling/package: `.obpt-root` data table/state/description/timeline/progress/metric/machine/code/args/redaction/empty/toast class families and regenerated packaged CSS.
- Catalog file/module: `test/support/data_display_story_catalog.ex`, `ObanPowertools.DataDisplayStoryCatalog`, `stories/0`, `story!/1`, `snapshot_name/1`, `a11y_target/1`, `huge_args_fixture/0`, `long_value_fixture/0`, `timeline_fixture/0`, `large_row_total/0`, `large_row_window/0`, and pinned first/last row keys.
- Story IDs: `data-table-sort-states`, `data-table-320-stacked`, `data-table-explicit-states`, `data-status-taxonomy-all`, `data-description-list-long-values`, `data-timeline-event-log`, `data-progress-metric-cards`, `data-code-args-redaction`, `data-empty-toast-flash`, `data-table-thousands-row-stress`.
- Showcase symbols: `@data_catalog_module`, `@data_catalog_path`, `@data_stories`, data catalog loading/error assign, `data_story_body/1`, parent `data_sort_key`/`data_sort_direction`, and `handle_event("sort-data-table", ...)` (or one equivalently exact event name used consistently).
- Manifest/browser symbols: schema version 5; `data_stories`; appended `targets`; target `kind: "data"`; `ShowcaseDataStory`; union/collection validators; `dataStories`; behavior helper `dataStory(id)`; `assertShowcaseStructure` data-kind branch/metadata assertions; data-inclusive structure titles.
- Evidence: catalog/showcase/component/taxonomy tests, focused behavior spec, 120 data PNG baselines, and `77-VALIDATION.md` final command/task map.

<tasks>

<task type="auto" tdd="true">
  <name>Task 77-06-01: Build the deterministic catalog and render real live stories</name>
  <files>test/support/data_display_story_catalog.ex, test/oban_powertools/data_display_story_catalog_test.exs, lib/oban_powertools/web/dev/showcase_live.ex, test/oban_powertools/web/live/showcase_live_test.exs</files>
  <read_first>
    - @test/support/data_display_story_catalog.ex
    - @test/oban_powertools/data_display_story_catalog_test.exs
    - @lib/oban_powertools/web/dev/showcase_live.ex
    - @test/oban_powertools/web/live/showcase_live_test.exs
    - @test/support/primitive_story_catalog.ex
    - @test/support/form_story_catalog.ex
    - @test/support/shell_story_catalog.ex
    - @test/support/showcase_catalog.ex
    - @lib/oban_powertools/web/components/data_display.ex
    - @lib/oban_powertools/web/status_taxonomy.ex
    - @.planning/phases/77-data-display-operator-patterns/77-UI-SPEC.md
    - @.planning/phases/77-data-display-operator-patterns/77-PATTERNS.md
  </read_first>
  <action>Create `ObanPowertools.DataDisplayStoryCatalog` by copying the registry shape of `ShellStoryCatalog`, not the domain `ShowcaseCatalog`. Define exactly the ten locked IDs/order and fields `id`, `kind: :data`, `component`, `components`, `name`, `description`, `variant`, `state`, and generated story/snapshot/a11y targets using the data prefix. Export `huge_args_fixture/0`, `long_value_fixture/0`, `timeline_fixture/0`, `large_row_total/0`, and `large_row_window/0` alongside the registry functions. Keep fixtures deterministic, database-free, random-free, and test-support-only: all taxonomy specs; all explicit states; long IDs/modules/URLs/stacktraces; Unicode/emoji/RTL/HTML-looking values; huge nested normalized args/redaction; boundary pagination; and a thousands-row scenario whose truthful total is >=1,000 while `large_row_window/0` returns <=25 stable rows with pinned endpoints. Never include the original secret sentinel. Add runtime catalog loading to the existing compile-gated `ShowcaseLive`, replace only the data-display placeholder, render each story once in an article with complete `data-obpt-data-story` metadata, and build bodies from real DataDisplay components. Add parent-owned sort key/direction assigns and `handle_event("sort-data-table", ...)` that toggles/changes assigns then re-renders the sort story; do not add state to DataTable or `theme.js`. Extend LiveView tests for exact story metadata, real semantics/copy, hostile escaping, sentinel absence, bounded rows, and `render_click` aria-sort changes. Keep other placeholders and the prod/dev compile gate unchanged. Decision trace: D-21, D-23, D-25.</action>
  <verify>
    <automated>mix test test/oban_powertools/data_display_story_catalog_test.exs test/oban_powertools/web/live/showcase_live_test.exs test/oban_powertools/web/components/data_display_test.exs test/oban_powertools/web/status_taxonomy_test.exs --seed 0</automated>
    <automated>mix format --check-formatted test/support/data_display_story_catalog.ex test/oban_powertools/data_display_story_catalog_test.exs lib/oban_powertools/web/dev/showcase_live.ex test/oban_powertools/web/live/showcase_live_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - Catalog returns exactly the ten locked IDs in order, deterministic unique targets, complete component/state/taxonomy fixtures, and no secret sentinel.
    - The large-row story reports a truthful >=1,000 total, renders <=25 rows, and exposes deterministic first/last row keys.
    - Showcase renders one metadata-complete article per data story with real semantic components and no placeholder.
    - `render_click` on the data sort control changes the parent assign and moves truthful `aria-sort`; DataTable remains stateless.
    - Production compile guarding, existing scenario/primitive/form/shell stories, and non-data placeholders remain intact.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 77-06-02: Extend generated manifest and browser support to schema 5</name>
  <files>scripts/showcase_manifest.exs, test/browser/support/manifest.ts, test/browser/support/manifest-smoke.mjs, test/browser/support/showcase.ts, test/browser/specs/showcase.structure.spec.ts</files>
  <read_first>
    - @scripts/showcase_manifest.exs
    - @test/browser/support/manifest.ts
    - @test/browser/support/manifest-smoke.mjs
    - @test/browser/support/showcase.ts
    - @test/browser/specs/showcase.structure.spec.ts
    - @test/support/data_display_story_catalog.ex
    - @test/browser/specs/showcase.vrt.spec.ts
    - @test/browser/specs/showcase.a11y.spec.ts
    - @.planning/phases/76-navigation-app-shell/76-VALIDATION.md
    - @.planning/phases/77-data-display-operator-patterns/77-PATTERNS.md
  </read_first>
  <action>Extend `showcase_manifest.exs` to load the data catalog, serialize common story fields, set `schema_version: 5`, add `data_stories` after `shell_stories`, and append data targets after shell targets. In `manifest.ts`, add `ShowcaseDataStory` with `kind: "data"`, schema literal 5, collection and count validation, target union/append-order validation, returned `data_stories`, and `dataStories` export. In the independent smoke script, assert schema 5, exact data count 10, data prefixes/selectors/fields, and append order without importing the TS validator. Add the `kind === "data"` metadata branch to `showcase.ts`. Update structure-test title/expectations so `--grep "data data-"` discovers data structure coverage. Do not add the ten-id list to TypeScript, modify generic VRT/axe loops, or change their masks; both already consume generated targets. Decision trace: D-22, D-24, D-25.</action>
  <verify>
    <automated>npm run showcase:manifest && node test/browser/support/manifest-smoke.mjs</automated>
    <automated>npx playwright test --list test/browser/specs/showcase.structure.spec.ts test/browser/specs/showcase.vrt.spec.ts test/browser/specs/showcase.a11y.spec.ts --grep "data data-"</automated>
    <automated>npx playwright test --list test/browser/specs/data-display.behavior.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - Generated JSON has schema 5, exactly ten `data_stories`, and target order `scenarios ++ primitive ++ form ++ shell ++ data`.
    - TypeScript exports validated `dataStories`; independent smoke checks agree on count/order/selectors and do not import `manifest.ts`.
    - No browser file contains a standalone complete data story ID list; generic VRT/axe specs remain target-driven and otherwise unchanged.
    - Focused `--list --grep "data data-"` discovers the expected data structure/VRT/axe cases and the behavior spec lists without its Wave 0 missing-export guard.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## ASVS L1 Threat Model

| ID | Threat | Severity | Mitigation and verification |
|----|--------|----------|-----------------------------|
| T-77-06-LEAK | Story metadata/fixtures/manifest expose raw args via text, title, data attrs, details, or copy | high | Catalog contains normalized display fixtures only; sentinel absent from catalog/showcase/manifest; RedactedValue owns hidden output; tests inspect complete rendered/serialized artifacts. |
| T-77-06-ATTR | Manifest/story metadata overrides component semantics or injects selectors/events | high | Fixed catalog-owned metadata and validators; components filter caller attrs; sort event/key are finite dev/test values; structure tests compare exact metadata. |
| T-77-06-XSS | Hostile fixtures become executable HEEx or unsafe CSS selectors/JSON | high | HEEx and JSON encoding only; fixed generated selectors; HTML-looking fixtures remain escaped; no raw APIs/eval. |
| T-77-06-ATOM | Story state/domain strings create atoms or bypass taxonomy domains | high | Stories consume `all_specs/0` and closed atoms authored in Elixir; generated browser data stays strings; no runtime string-to-atom conversion. |
| T-77-06-DOS | Huge payloads or literal thousands of story rows slow every axe/VRT target | high | Deterministic finite huge-args fixture, <=25-row window with truthful total, exactly ten targets, and catalog/browser assertions block unbounded DOM growth. |
</threat_model>

<verification>
- Run focused ExUnit, formatter, manifest smoke, and Playwright discovery commands.
- Confirm generated `.generated` JSON and browser reports remain ignored/uncommitted.
- Confirm no production page, dependency, generic VRT/axe logic, or `theme.js` changes appear.
</verification>

<success_criteria>
- Ten real deterministic data stories cover the full component/taxonomy/state/redaction/stress contract.
- Parent-owned sort behavior is live in the dev/test showcase while production components stay stateless.
- Schema 5 and generated data targets are the single browser source of truth, ready for focused live evidence.
</success_criteria>

<output>
Create `.planning/phases/77-data-display-operator-patterns/77-06-SUMMARY.md` when done.
</output>
