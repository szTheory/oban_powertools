---
phase: 77-data-display-operator-patterns
plan: 09
type: execute
wave: 9
depends_on:
  - 77-08
files_modified:
  - lib/oban_powertools/web/dev/showcase_live.ex
  - test/oban_powertools/web/live/showcase_live_test.exs
  - .planning/phases/77-data-display-operator-patterns/77-VALIDATION.md
autonomous: true
gap_closure: true
requirements:
  - DATA-01
  - DATA-03
  - A11Y-02
must_haves:
  truths:
    - "D-01, D-02, D-10, and D-21: a packaged dev showcase whose optional DataDisplayStoryCatalog is absent mounts with an empty flash assign and reaches the existing explicit Data Display unavailable placeholder."
    - "The available local catalog path continues to seed real Phoenix flash entries and render all ten generated data stories."
    - "A temporary DataDisplayStoryCatalog whose stories/0 result is not a list mounts with data_catalog_available?: false, data_stories: [], an empty flash assign, and the existing unavailable placeholder."
    - "List-valued catalogs with no notification story, a missing fixtures.flash field, or a non-map flash fixture remain available but seed an empty flash assign; an empty story list reaches the existing unavailable placeholder."
    - "The package-boundary regression executes in an isolated code server whose load path excludes test-support catalog beams, matching the Hex package file boundary."
    - "Focused Phase 77, package-contract, manifest, and exact-baseline checks remain green after the mount hardening."
  prohibitions:
    - "Do not package test/support catalogs, add a production fallback catalog, or weaken the existing dev/test-only boundary."
    - "Do not change DataDisplay components, CSS, packaged assets, browser target schema, screenshots, dependencies, or production page LiveViews."
    - "Do not edit 77-VERIFICATION.md; the verifier owns post-execution re-verification."
  artifacts:
    - path: "lib/oban_powertools/web/dev/showcase_live.ex"
      provides: "Fail-closed optional data-catalog loading and flash seeding"
    - path: "test/oban_powertools/web/live/showcase_live_test.exs"
      provides: "Isolated package-boundary mount/render regression plus available-catalog preservation"
    - path: ".planning/phases/77-data-display-operator-patterns/77-VALIDATION.md"
      provides: "Executed VR-01 closure evidence and unchanged visual-evidence boundary"
  key_links:
    - from: "lib/oban_powertools/web/dev/showcase_live.ex"
      to: "test/support/data_display_story_catalog.ex"
      via: "optional runtime load with validated stories/0 output"
      pattern: "load_data_catalog|ensure_data_catalog_module"
    - from: "lib/oban_powertools/web/dev/showcase_live.ex"
      to: "Phoenix.LiveView.put_flash/3"
      via: "seed_data_flash/2 reduces only a verified flash map"
      pattern: "seed_data_flash|put_flash"
    - from: "test/oban_powertools/web/live/showcase_live_test.exs"
      to: "mix.exs package files"
      via: "isolated child process with test-support catalog beams removed from the code path"
      pattern: "data-obpt-data-index.*empty"
---

<objective>
Close VR-01 by making the shipped dev showcase tolerate an absent or invalid optional data story catalog.

Purpose: preserve the deliberate dev/test-only catalog package boundary while ensuring the explicit Data Display unavailable state is reachable instead of crashing during mount.
Output: fail-closed catalog/flash handling, a package-faithful isolated regression, and reconciled focused validation evidence.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/77-data-display-operator-patterns/77-CONTEXT.md
@.planning/phases/77-data-display-operator-patterns/77-UI-SPEC.md
@.planning/phases/77-data-display-operator-patterns/77-VERIFICATION.md
@.planning/phases/77-data-display-operator-patterns/77-VALIDATION.md
@.planning/phases/77-data-display-operator-patterns/77-08-SUMMARY.md
@lib/oban_powertools/web/dev/showcase_live.ex
@test/oban_powertools/web/live/showcase_live_test.exs
@test/oban_powertools/hex_release_test.exs
@mix.exs

<interfaces>
- `ShowcaseLive.mount/3` loads each optional support catalog, then calls `seed_data_flash(socket, data_catalog.stories)` before assigning catalog availability and stories.
- `load_data_catalog/0` currently marks any exported `stories/0` result available without validating that it is a list; its unavailable branch returns `%{available?: false, stories: []}`.
- `ensure_support_module/2` may require a local support source in non-test source checkouts, but an installed Hex package has no `test/support` path or catalog beam.
- `seed_data_flash/2` currently finds `data-empty-toast-flash`, projects `fixtures.flash`, and unconditionally calls `Enum.reduce/3`; absent or non-map input therefore raises before render.
- The existing unavailable render branch is selected by `@data_catalog_available?` / `@data_stories` and exposes `data-obpt-data-index="empty"`.
- `mix.exs` intentionally packages `lib` and excludes `test`; `HexReleaseTest` already guards that boundary.
</interfaces>
</context>

## Artifacts this phase produces

- New file paths: none.
- New production or public API symbols: none.
- Modified private production symbols: `load_data_catalog/0` and `seed_data_flash/2` in `ObanPowertools.Web.Dev.ShowcaseLive`.
- New test coverage in the existing `ObanPowertools.Web.Dev.ShowcaseLiveTest`: an isolated package-boundary child-process regression that removes optional story-catalog beams from its code path, mounts the shipped LiveView, renders it, and asserts the existing Data Display unavailable marker.
- Modified evidence: `77-VALIDATION.md` gains Plan 77-09 / VR-01 command results while preserving the exact-120 data baseline and unrelated 108-scenario residual boundaries.
- Schema artifacts: none. Phase scope contains ordinary operator payload fixtures but no Payload CMS, Prisma, Drizzle, Supabase, or TypeORM schema files, so no schema-push task applies.

<tasks>

<task type="auto" tdd="true">
  <name>Task 77-09-01: Make the optional data catalog fail closed at the package boundary</name>
  <files>lib/oban_powertools/web/dev/showcase_live.ex, test/oban_powertools/web/live/showcase_live_test.exs</files>
  <read_first>
    - @lib/oban_powertools/web/dev/showcase_live.ex
    - @test/oban_powertools/web/live/showcase_live_test.exs
    - @test/oban_powertools/hex_release_test.exs
    - @mix.exs
    - @.planning/phases/77-data-display-operator-patterns/77-VERIFICATION.md
    - @.planning/phases/77-data-display-operator-patterns/77-08-SUMMARY.md
    - @.planning/phases/77-data-display-operator-patterns/77-UI-SPEC.md
  </read_first>
  <behavior>
    - "With no DataDisplayStoryCatalog stub and all optional catalog beams absent, ShowcaseLive.mount/3 returns data_catalog_available?: false, data_stories: [], and flash: %{}; rendered HTML contains data-obpt-data-index=\"empty\"."
    - "A temporary DataDisplayStoryCatalog returning a non-list value is classified unavailable and returns data_catalog_available?: false, data_stories: [], and flash: %{}; rendered HTML contains the existing placeholder."
    - "A temporary DataDisplayStoryCatalog returning [] remains list-valid with data_catalog_available?: true and data_stories: [], seeds flash: %{}, and renders the existing placeholder because the notification story is absent."
    - "Temporary list-valued catalogs whose notification story omits fixtures.flash or supplies a non-map fixtures.flash retain data_catalog_available?: true and their story list but seed flash: %{}."
    - "The normal source-checkout catalog still renders ten data stories and real info/error flash entries with connected per-item dismissal."
  </behavior>
  <action>First add a table-driven regression harness to the existing ShowcaseLive test module that reproduces the package boundary in fresh isolated OS child processes, not by purging modules in the async ExUnit VM. Invoke `mix run --no-start --no-compile` under `MIX_ENV=test`; in every child, copy the active application ebin to a temporary directory while excluding the five optional story-catalog beam files, replace the original app ebin on the child code path, purge those catalog modules, and optionally compile a temporary `ObanPowertools.DataDisplayStoryCatalog` stub for that case. Create a LiveView socket with an empty flash assign, call `ShowcaseLive.mount/3`, and emit/assert the exact catalog availability, story, and flash assigns; render only cases whose story shape is sufficient and assert the existing `data-obpt-data-index="empty"` marker. Clean the temporary directory in an `after` block and make each ExUnit assertion report child stdout/stderr on failure.

Generate an independently runnable ExUnit case for each row so every fallback seam proves a meaningful RED before production changes: (1) no stub/beam must eventually return `false`, `[]`, `%{}`, and the placeholder, while current code raises `Protocol.UndefinedError`; (2) a stub returning a non-list sentinel must eventually return the same unavailable state, while current code fails while enumerating stories; (3) a stub returning `[]` must remain catalog-available, seed `%{}`, and render the placeholder, while current code reduces `nil`; (4) a stub returning `[%{id: "data-empty-toast-flash", fixtures: %{}}]` must remain available with that story retained and seed `%{}`, while current code reduces `nil`; and (5) a stub returning the same story with `fixtures.flash` set to a non-map list of tuples must remain available and seed `%{}`, while current code incorrectly seeds the tuple entry. Run and record every generated case against the unmodified source; a harness/setup failure is not an acceptable RED.

Then harden the existing private boundary per D-01, D-02, D-06, D-10, D-15, and D-21. In `load_data_catalog/0`, accept the support module only when `stories/0` returns a list; classify every other return value as `%{available?: false, stories: []}`. In `seed_data_flash/2`, derive a flash map only from a matching story map whose `fixtures.flash` value is a map; use `%{}` for an absent story, absent fixture path, or non-map value, then reduce that verified map through `put_flash/3`. Keep the valid binary-keyed flash behavior from Plan 77-08 unchanged. Do not create a packaged catalog, catch broad exceptions, change the placeholder copy/markup, or touch components, assets, browser files, dependencies, or page LiveViews.</action>
  <verify>
    <automated>mix test test/oban_powertools/web/live/showcase_live_test.exs test/oban_powertools/hex_release_test.exs --seed 0</automated>
    <automated>mix format --check-formatted lib/oban_powertools/web/dev/showcase_live.ex test/oban_powertools/web/live/showcase_live_test.exs</automated>
    <automated>mix compile --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - All five isolated child cases independently reach the specified current-source RED and then pass after implementation; no RED is caused by child-process or temporary-module setup failure.
    - With no catalog beam, direct mount returns `data_catalog_available?: false`, `data_stories: []`, and `flash: %{}`, and render exposes the existing Data Display unavailable marker.
    - `load_data_catalog/0` marks only list-valued `stories/0` output available; the non-list stub returns the same unavailable assigns and placeholder as an absent module.
    - The empty-list, missing-`fixtures.flash`, and non-map fixture stubs remain catalog-available while seeding `flash: %{}`; the empty list also renders the existing placeholder.
    - `seed_data_flash/2` calls `Enum.reduce/3` only with a map and never seeds tuple entries supplied through a non-map fixture.
    - Existing catalog-backed story rendering, binary-key tones, exact `phx-value-key` metadata, and connected sibling-preserving dismissal remain green.
    - Hex package tests continue to prove `lib` ships and `test/support` does not.
  </acceptance_criteria>
  <done>The shipped dev showcase renders its existing unavailable Data Display state when the optional catalog is absent or invalid, while the local catalog path preserves real flash/story behavior.</done>
</task>

<task type="auto">
  <name>Task 77-09-02: Record focused package-boundary and phase regression evidence</name>
  <files>.planning/phases/77-data-display-operator-patterns/77-VALIDATION.md</files>
  <read_first>
    - @.planning/phases/77-data-display-operator-patterns/77-VALIDATION.md
    - @.planning/phases/77-data-display-operator-patterns/77-VERIFICATION.md
    - @lib/oban_powertools/web/dev/showcase_live.ex
    - @test/oban_powertools/web/live/showcase_live_test.exs
    - @test/oban_powertools/hex_release_test.exs
    - @test/browser/support/manifest-smoke.mjs
    - @test/browser/support/verify-data-baselines.mjs
  </read_first>
  <action>Run the repository formatter, warnings-as-errors compile, complete focused Phase 77 ExUnit set, and Hex package contract tests. Regenerate the ignored schema-5 manifest and run its independent smoke validator; run the exact-120 data baseline verifier and confirm the screenshot tree has no diff. Update `77-VALIDATION.md` only after every command is green: add Task 77-09-01 and 77-09-02 rows, record meaningful RED and GREEN evidence for each of the five isolated package-child cases, update the focused test result without hardcoding a count before execution, and state that manifest/baseline checks remain unchanged. Preserve the documented 108 unrelated scenario-only VRT residual and the narrow component/showcase completion boundary. Do not run snapshot updates, claim the broad aggregate gate is green, or edit `77-VERIFICATION.md`; post-execution re-verification remains the verifier's next step.</action>
  <verify>
    <automated>mix format --check-formatted &amp;&amp; mix compile --warnings-as-errors &amp;&amp; mix test test/oban_powertools/web/status_taxonomy_test.exs test/oban_powertools/web/components/data_display_test.exs test/oban_powertools/data_display_story_catalog_test.exs test/oban_powertools/web/live/showcase_live_test.exs test/oban_powertools/web/theme_tokens_test.exs test/oban_powertools/web/assets_test.exs test/oban_powertools/hex_release_test.exs --seed 0</automated>
    <automated>npm run showcase:manifest &amp;&amp; node test/browser/support/manifest-smoke.mjs &amp;&amp; node test/browser/support/verify-data-baselines.mjs</automated>
    <automated>git diff --exit-code -- test/browser/__screenshots__</automated>
    <automated>git diff --exit-code -- .planning/phases/77-data-display-operator-patterns/77-VERIFICATION.md &amp;&amp; git status --short -- lib/oban_powertools/web/dev/showcase_live.ex test/oban_powertools/web/live/showcase_live_test.exs .planning/phases/77-data-display-operator-patterns/77-VALIDATION.md .planning/phases/77-data-display-operator-patterns/77-09-PLAN.md .planning/ROADMAP.md</automated>
  </verify>
  <acceptance_criteria>
    - Formatting, warnings-as-errors compilation, the full focused Phase 77 suite, and Hex package contract tests pass with the isolated missing-catalog regression included.
    - Manifest smoke still reports schema 5 with ten data stories and 41 targets, and the independent verifier still reports exactly 120 data PNGs.
    - No screenshot, CSS, packaged asset, dependency, manifest schema, component, or production page LiveView changes enter the plan diff.
    - `77-VALIDATION.md` records the package-boundary regression and Plan 77-09 evidence while retaining the 108-scenario residual and avoiding any aggregate-green claim.
    - `77-VERIFICATION.md` remains unchanged and ready for verifier-owned re-verification.
  </acceptance_criteria>
  <done>VR-01 closure has reproducible package-boundary and focused phase evidence, with the visual and aggregate residual boundaries unchanged.</done>
</task>

</tasks>

<threat_model>
## ASVS L1 Threat Model — blocking threshold: high

### Trust Boundaries

| Boundary | Untrusted or integrity-sensitive input |
|----------|----------------------------------------|
| Hex/package file set -> optional showcase catalogs | Test-support modules and source paths are intentionally absent after packaging; availability must not be assumed. |
| Optional catalog `stories/0` -> ShowcaseLive mount/render | A locally supplied support module may be absent or return an invalid shape. |
| Story `fixtures.flash` -> Phoenix socket flash | Fixture structure crosses into `put_flash/3` and must be a verified finite map before enumeration. |

### STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-77-09-AVAIL | Denial of service | `load_data_catalog/0`, `ShowcaseLive.mount/3` | medium | mitigate | Validate list-valued catalog output, fail closed to the existing unavailable shape, and prove the package-without-support path in an isolated process. |
| T-77-09-FIXTURE | Tampering / Denial of service | `seed_data_flash/2` | medium | mitigate | Pattern-match the story/fixture shape, enumerate only a verified map, and retain the existing escaped `put_flash/3` rendering path. |
| T-77-09-XSS | Elevation of privilege / Information disclosure | Valid flash message rendering | low | accept | No new input or rendering API is introduced; existing HEEx escaping, hostile-message coverage, and Plan 77-08 keyed-flash tests remain mandatory regressions. |
| T-77-09-SUPPLY | Tampering | Dependency and package metadata | low | accept | No package install, dependency edit, package-file expansion, schema push, or runtime integration occurs; Hex package exclusions are rerun as evidence. |
</threat_model>

<verification>
- Reproduce VR-01 through the isolated missing-catalog child process before implementation, then make the exact regression green.
- Run focused showcase plus Hex package tests after the private-function hardening.
- Run the complete focused Phase 77 format/compile/ExUnit gate, schema-5 manifest smoke, exact-120 baseline equality, and screenshot no-diff check.
- Inspect `git status --short --` only for the allowlisted Phase 77 paths: `showcase_live.ex`, its existing LiveView test, `77-VALIDATION.md`, this plan, and `ROADMAP.md`; do not require a repository-wide clean diff because unrelated tracked deletions predate this plan.
- Leave `77-VERIFICATION.md` unchanged for verifier-owned re-verification.
</verification>

<success_criteria>
- The optional data catalog can be absent from the Hex package without crashing ShowcaseLive mount or render.
- Invalid catalog output and missing/non-map flash fixtures fail closed without erasing valid Plan 77-08 flash behavior.
- The existing Data Display unavailable placeholder is proven reachable under a package-faithful code path.
- Focused Phase 77, package, manifest, and exact-baseline gates pass with no visual, dependency, schema, or production-page changes.
- VR-01 evidence is recorded and Phase 77 is ready for re-verification.
</success_criteria>

<output>
Create `.planning/phases/77-data-display-operator-patterns/77-09-SUMMARY.md` when done.
</output>
