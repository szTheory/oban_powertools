---
phase: 53-worker-lifecycle-hooks
plan: 02
type: execute
wave: 2
depends_on:
  - "53-01"
files_modified:
  - "guides/workers-and-idempotency.md"
  - "guides/telemetry-and-slos.md"
  - "test/oban_powertools/docs_contract_test.exs"
autonomous: true
requirements:
  - HOOK-01
  - HOOK-02
  - HOOK-03
  - HOOK-04
  - HOOK-05
requirements_addressed:
  - HOOK-01
  - HOOK-02
  - HOOK-03
  - HOOK-04
  - HOOK-05
user_setup: []
must_haves:
  truths:
    - "Builder docs explain on_start/1, on_success/2, on_failure/2, and on_discard/2 as observe-only worker-local callbacks."
    - "Builder docs state all four D-30 support truths: hooks run in the job process, outside any Powertools transaction; hook failure does not fail the job; hook failure does not crash the queue; hook execution is not retried independently."
    - "Docs do not claim hooks short-circuit execution, provide durable audit, fire for operator/Lifeline discards, or reliably fire after Oban timeout kills."
    - "Telemetry docs publish worker hook metric `oban_powertools.worker_hook.invoked.count` with tags `hook` and `outcome` only."
    - "Docs-contract tests lock the support-truth and telemetry strings."
    - "D-08: Docs state that Oban timeout kills may bypass wrapper-level failure hooks and point timeout observability to Oban [:oban, :job, :exception] telemetry, not on_failure/2."
  artifacts:
    - path: "guides/workers-and-idempotency.md"
      provides: "Worker lifecycle hook semantics and support-truth documentation"
      contains:
        - "on_start/1"
        - "on_success/2"
        - "on_failure/2"
        - "on_discard/2"
    - path: "guides/telemetry-and-slos.md"
      provides: "worker_hook metric documentation in the reporter-agnostic telemetry guide"
      contains:
        - "oban_powertools.worker_hook.invoked.count"
        - "hook"
        - "outcome"
    - path: "test/oban_powertools/docs_contract_test.exs"
      provides: "Executable docs support-truth and telemetry contract assertions"
  key_links:
    - from: "guides/workers-and-idempotency.md"
      to: "test/oban_powertools/docs_contract_test.exs"
      via: "literal support-truth strings asserted in docs contract"
      pattern: "hook failure does not fail the job"
    - from: "guides/telemetry-and-slos.md"
      to: "lib/oban_powertools/telemetry.ex"
      via: "metric name and tag set documented from the Phase 53 runtime contract"
      pattern: "oban_powertools\\.worker_hook\\.invoked\\.count"
---

<objective>
Document Phase 53's worker hook support truth and telemetry contract.

Purpose: Make the new runtime feature safe for adopters by locking the observe-only, best-effort, low-cardinality boundaries in docs and docs-contract tests.
Output: Updated worker guide, telemetry guide, and docs-contract assertions for HOOK-01 through HOOK-05.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/53-worker-lifecycle-hooks/53-CONTEXT.md
@.planning/phases/53-worker-lifecycle-hooks/53-RESEARCH.md
@.planning/phases/53-worker-lifecycle-hooks/53-VALIDATION.md
@.planning/phases/53-worker-lifecycle-hooks/53-PATTERNS.md
@.planning/phases/53-worker-lifecycle-hooks/53-01-SUMMARY.md
@guides/workers-and-idempotency.md
@guides/telemetry-and-slos.md
@test/oban_powertools/docs_contract_test.exs
@lib/oban_powertools/telemetry.ex

<interfaces>
Docs strings to lock from D-30:
- "hooks run in the job process"
- "outside any Powertools transaction"
- "hook failure does not fail the job"
- "hook failure does not crash the queue"
- "hook execution is not retried independently"

Telemetry strings to lock from D-25 through D-29:
- Event: `[:oban_powertools, :worker_hook, :invoked]`
- Metric: `oban_powertools.worker_hook.invoked.count`
- Tags: `hook`, `outcome`
- Outcome values: `"ok"` and `"crash_caught"`
- Excluded telemetry labels: job ids, args, worker module names, queue names, reasons, and stacktraces.
</interfaces>
</context>

<decision_coverage>
Documentation decision anchors for the automated translation gate:
- D-08: Oban timeout kills may bypass wrapper-level failure hooks; docs must point timeout observability to Oban `[:oban, :job, :exception]` telemetry rather than promising `on_failure/2`.
</decision_coverage>

<tasks>

<task type="auto">
  <name>Task 1: Document worker hook semantics and support truth</name>
  <files>guides/workers-and-idempotency.md, test/oban_powertools/docs_contract_test.exs</files>
  <read_first>
    - guides/workers-and-idempotency.md
    - test/oban_powertools/docs_contract_test.exs
    - lib/oban_powertools/worker.ex
    - lib/oban_powertools/worker/hooks.ex
    - .planning/phases/53-worker-lifecycle-hooks/53-CONTEXT.md
    - .planning/phases/53-worker-lifecycle-hooks/53-PATTERNS.md
  </read_first>
  <action>
    Update `guides/workers-and-idempotency.md` per D-30 and the Phase 53 roadmap success criteria. Add lifecycle hooks to the "What the wrapper adds" bullet list. Add a `## Lifecycle hooks` section that names `on_start/1`, `on_success/2`, `on_failure/2`, and `on_discard/2`; states `on_start/1` runs after validation/casting and before `process/1`; states `on_success/2` handles `:ok` and `{:ok, value}`; states `on_failure/2` handles retry-eligible `{:error, reason}` and process raises/throws/exits when the wrapper can catch them; states `on_discard/2` handles explicit discard and final retry exhaustion. Include the exact support-truth strings from the plan context: "hooks run in the job process", "outside any Powertools transaction", "hook failure does not fail the job", "hook failure does not crash the queue", and "hook execution is not retried independently". Also state that `{:cancel, reason}` and `{:snooze, _}` do not dispatch Phase 53 post hooks, operator-initiated Lifeline discards do not fire worker execution hooks, and Oban timeout kills may bypass worker hooks. Add a compact example that overrides one post hook without implying hooks can short-circuit execution or provide durable audit. Extend `test/oban_powertools/docs_contract_test.exs` with source assertions for the exact support-truth strings and the four callback names.
  </action>
  <verify>
    <automated>mix test test/oban_powertools/docs_contract_test.exs --trace</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `guides/workers-and-idempotency.md` contains `on_start/1`, `on_success/2`, `on_failure/2`, and `on_discard/2`.
    - Source assertion: `guides/workers-and-idempotency.md` contains the exact strings `hooks run in the job process`, `outside any Powertools transaction`, `hook failure does not fail the job`, `hook failure does not crash the queue`, and `hook execution is not retried independently`.
    - Source assertion: `guides/workers-and-idempotency.md` contains `operator-initiated Lifeline discards do not fire worker execution hooks`.
    - Source assertion: `guides/workers-and-idempotency.md` contains `Oban timeout kills may bypass worker hooks`.
    - Source assertion: `guides/workers-and-idempotency.md` does not contain the exact forbidden positive phrases `hooks can short-circuit`, `hooks halt execution`, `hooks provide durable audit`, or `hooks are retried independently`; preserve the required negative sentence `hook execution is not retried independently`.
    - Test command `mix test test/oban_powertools/docs_contract_test.exs --trace` exits 0.
  </acceptance_criteria>
  <done>Builder docs explain hook routing and lock the Phase 53 support-truth boundaries with automated docs-contract coverage.</done>
</task>

<task type="auto">
  <name>Task 2: Document worker_hook telemetry and low-cardinality limits</name>
  <files>guides/telemetry-and-slos.md, test/oban_powertools/docs_contract_test.exs</files>
  <read_first>
    - guides/telemetry-and-slos.md
    - test/oban_powertools/docs_contract_test.exs
    - lib/oban_powertools/telemetry.ex
    - test/oban_powertools/telemetry_test.exs
    - .planning/phases/53-worker-lifecycle-hooks/53-CONTEXT.md
    - .planning/phases/53-worker-lifecycle-hooks/53-PATTERNS.md
    - .planning/milestones/v1.6-phases/50-telemetry-metrics-slo-guide/50-03-SUMMARY.md
  </read_first>
  <action>
    Update `guides/telemetry-and-slos.md` per D-22 through D-29 and HOOK-05. Change references to "five frozen Powertools control-plane event families" so the guide accurately includes the additive `:worker_hook` family. In "Powertools control-plane SLIs", add worker lifecycle hooks to the list of control-plane signals. Add a `### Worker lifecycle hooks` metric table with metric `oban_powertools.worker_hook.invoked.count`, tags `hook`, `outcome`, and a description that it tracks worker hook dispatch attempts after the hook returned or was crash-caught. State the event name exactly as `[:oban_powertools, :worker_hook, :invoked]`, measurement `%{count: 1}`, allowed `hook` values `"on_start"`, `"on_success"`, `"on_failure"`, `"on_discard"`, and allowed `outcome` values `"ok"` and `"crash_caught"`. Explicitly state that this telemetry family excludes job ids, args, worker module names, queue names, reasons, stacktraces, hook durations, and span-style metadata. Extend `test/oban_powertools/docs_contract_test.exs` with assertions for the metric name, event name, tag names, outcome value `"crash_caught"`, and the high-cardinality exclusion sentence.
  </action>
  <verify>
    <automated>mix test test/oban_powertools/docs_contract_test.exs test/oban_powertools/telemetry_test.exs --trace</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `guides/telemetry-and-slos.md` contains `oban_powertools.worker_hook.invoked.count`.
    - Source assertion: `guides/telemetry-and-slos.md` contains `[:oban_powertools, :worker_hook, :invoked]`.
    - Source assertion: `guides/telemetry-and-slos.md` contains `hook`, `outcome`, `on_start`, `on_success`, `on_failure`, `on_discard`, `ok`, and `crash_caught`.
    - Source assertion: `guides/telemetry-and-slos.md` contains a sentence excluding `job ids`, `args`, `worker module names`, `queue names`, `reasons`, and `stacktraces` from worker hook telemetry labels.
    - Source assertion: `guides/telemetry-and-slos.md` no longer describes the Powertools control-plane contract as exactly five families.
    - Test command `mix test test/oban_powertools/docs_contract_test.exs test/oban_powertools/telemetry_test.exs --trace` exits 0.
  </acceptance_criteria>
  <done>Telemetry docs publish the worker_hook metric and lock low-cardinality boundaries with automated docs-contract coverage.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Library docs -> host developer behavior | Documentation shapes whether adopters treat hooks as durable, retryable, or outcome-changing operations. |
| Telemetry docs -> host metrics labels | Documentation determines whether hosts add high-cardinality or sensitive labels around worker hook events. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-53-06 | Repudiation | `guides/workers-and-idempotency.md` | mitigate | Lock D-30 support truth in docs and docs-contract tests: in-process, outside Powertools transaction, hook failure does not fail the job or crash the queue, and hook execution is not retried independently. |
| T-53-07 | Information Disclosure | `guides/telemetry-and-slos.md` | mitigate | Document only `hook` and `outcome` tags and assert the guide excludes job ids, args, worker module names, queue names, reasons, and stacktraces from labels. |
| T-53-08 | Tampering | Worker hook guide examples | mitigate | Examples must not claim hooks can short-circuit, halt, cancel, retry, or replace durable audit. Docs-contract source assertions reject positive claims around short-circuiting and durable audit. |
| T-53-09 | Denial of Service | Hook support truth docs | mitigate | State hooks are synchronous in the job process and not independently retried so adopters avoid slow or blocking hook side effects. |
| T-53-SC | Tampering | npm/pip/cargo installs | mitigate | No package-manager install tasks exist in this plan; Phase 53 research package audit says no new packages. |
</threat_model>

<verification>
Automated plan gate:
- `mix test test/oban_powertools/docs_contract_test.exs test/oban_powertools/telemetry_test.exs --trace`
- `mix test`

Source gates:
- `rg "hook failure does not fail the job" guides/workers-and-idempotency.md test/oban_powertools/docs_contract_test.exs`
- `rg "oban_powertools\\.worker_hook\\.invoked\\.count" guides/telemetry-and-slos.md test/oban_powertools/docs_contract_test.exs`
- `rg "five frozen Powertools control-plane event families" guides/telemetry-and-slos.md` should return no matches.
</verification>

<success_criteria>
- HOOK-01 through HOOK-04 semantics are documented with exact support-truth boundaries.
- HOOK-05 telemetry event, metric, tags, and low-cardinality exclusions are documented.
- Docs-contract tests fail if the support-truth or worker_hook metric strings drift.
- Documentation contains no deferred ideas: no global hook registry, no hook latency spans, no on_cancel/2, no operator/Lifeline discard hooks, no output recording, no deadline behavior, and no redaction behavior.
</success_criteria>

<source_audit>
SOURCE | ID | Feature/Requirement | Plan | Status | Notes
GOAL | - | Every ObanPowertools.Worker can observe its own execution lifecycle with crash-safe callbacks that never affect job outcome | 53-01, 53-02 | COVERED | Runtime in 53-01; support truth in 53-02.
REQ | HOOK-01 | on_start/1 before process/1, observe-only, crash-caught, no-op default | 53-01, 53-02 | COVERED | D-02, D-10, D-16, D-27.
REQ | HOOK-02 | on_success/2 for :ok and {:ok, _} | 53-01, 53-02 | COVERED | D-03, D-12.
REQ | HOOK-03 | on_failure/2 for retry-eligible {:error, _} or raised/caught process failure | 53-01, 53-02 | COVERED | D-04, D-13.
REQ | HOOK-04 | on_discard/2 after retry exhaustion or explicit discard | 53-01, 53-02 | COVERED | D-05, D-06, D-14, D-32.
REQ | HOOK-05 | worker_hook telemetry family with hook and outcome keys | 53-01, 53-02 | COVERED | D-22 through D-29.
RESEARCH | - | No new dependencies | 53-01, 53-02 | COVERED | Both plans have no package installs and T-53-SC.
RESEARCH | - | Wrapper-owned dispatch, not Oban telemetry handlers | 53-01 | COVERED | Implemented in generated perform/1 per D-17.
RESEARCH | - | Internal dispatcher module `ObanPowertools.Worker.Hooks` | 53-01 | COVERED | Task 2 creates it per D-18.
RESEARCH | - | Compile-time override tracking for omitted defaults | 53-01 | COVERED | Task 3 generates `__powertools_hook_overridden?/1` per D-27.
RESEARCH | - | Preserve raised process exception semantics | 53-01 | COVERED | Task 3 dispatches then reraises/rethrows/exits.
RESEARCH | - | Docs support-truth and docs-contract proof | 53-02 | COVERED | D-30 locked in docs tests.
CONTEXT | D-01 through D-21 | Lifecycle semantics, payload envelopes, dispatch architecture, no deferred hook surfaces | 53-01, 53-02 | COVERED | Task actions cite D-IDs and exclude deferred ideas.
CONTEXT | D-22 through D-29 | worker_hook telemetry event, measurement, metadata, metric, and exclusions | 53-01, 53-02 | COVERED | Telemetry runtime plus docs.
CONTEXT | D-30 through D-32 | Support truth and routing/non-double-fire tests | 53-01, 53-02 | COVERED | Worker tests plus docs contract.
</source_audit>

<output>
Create `.planning/phases/53-worker-lifecycle-hooks/53-02-SUMMARY.md` when done.
</output>
