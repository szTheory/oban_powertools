---
phase: 15-upgrade-lane-support-truth-public-docs-integrity
plan: 02
type: execute
wave: 2
depends_on:
  - "15-01"
files_modified:
  - test/support/example_host_contract.ex
  - test/oban_powertools/example_host_contract_test.exs
  - .github/workflows/host-contract-proof.yml
  - guides/upgrade-and-compatibility.md
autonomous: true
requirements:
  - PKG-02
must_haves:
  truths:
    - "The upgrade-proof lane starts from the archived historical fixture rather than mutating the current fixture in place."
    - "The only upgrade actions performed in proof are the same host actions described in the public upgrade guide."
    - "After upgrade, the host proves one meaningful native operator behavior rather than only config restoration and migration success."
  artifacts:
    - path: "test/support/example_host_contract.ex"
      provides: "Real upgrade-lane harness that copies the archived historical source fixture and applies documented upgrade actions"
    - path: "test/oban_powertools/example_host_contract_test.exs"
      provides: "Executable `upgrade-proof` assertions for native post-upgrade behavior"
    - path: "guides/upgrade-and-compatibility.md"
      provides: "Singular supported upgrade lane described in host-shape terms rather than phase-number terms"
    - path: ".github/workflows/host-contract-proof.yml"
      provides: "Dedicated CI lane that runs the rebuilt upgrade proof"
  key_links:
    - from: "test/support/example_host_contract.ex"
      to: "examples/phoenix_host_upgrade_source/config/config.exs"
      via: "fixture copy and documented upgrade actions"
      pattern: "phoenix_host_upgrade_source|display_policy"
    - from: "guides/upgrade-and-compatibility.md"
      to: "test/oban_powertools/example_host_contract_test.exs"
      via: "same upgrade steps and same canonical post-upgrade proof target"
      pattern: "pause_cron_entry|nightly_sync|upgrade-proof"
---

<objective>
Replace the synthetic upgrade proof with a real archived-host upgrade lane and align the public upgrade guide to it.

Purpose: Make `PKG-02` evidence-backed with one supported source host, one documented upgrade path, and one deterministic post-upgrade proof action.
Output: A rebuilt `upgrade-proof` harness, stronger upgrade assertions, and a host-shape-based upgrade guide.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/15-upgrade-lane-support-truth-public-docs-integrity/15-CONTEXT.md
@.planning/phases/15-upgrade-lane-support-truth-public-docs-integrity/15-RESEARCH.md
@.planning/phases/15-upgrade-lane-support-truth-public-docs-integrity/15-PATTERNS.md
@.planning/phases/15-upgrade-lane-support-truth-public-docs-integrity/15-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rebuild the upgrade harness around the archived historical fixture</name>
  <files>test/support/example_host_contract.ex</files>
  <read_first>.planning/phases/15-upgrade-lane-support-truth-public-docs-integrity/15-CONTEXT.md, .planning/phases/15-upgrade-lane-support-truth-public-docs-integrity/15-RESEARCH.md, test/support/example_host_contract.ex, examples/phoenix_host_upgrade_source/README.md, examples/phoenix_host_upgrade_source/config/config.exs, examples/phoenix_host/config/config.exs, examples/phoenix_host/lib/phoenix_host_web/oban_powertools_display_policy.ex</read_first>
  <action>Replace `simulate_upgrade_source!/1` with a real upgrade preparation flow per D-09 through D-12. Add a second fixture root for `examples/phoenix_host_upgrade_source`, copy that tree for the `upgrade` lane, rewrite the local Powertools path, and apply only the guide-documented upgrade actions: add `display_policy: PhoenixHostWeb.ObanPowertoolsDisplayPolicy` to `config/config.exs`, restore `lib/phoenix_host_web/oban_powertools_display_policy.ex` from the canonical current fixture if the archived source lacks it, then run `mix deps.get`, `mix compile`, `MIX_ENV=test mix ecto.reset`, and the native post-upgrade proof command. Keep the existing `native-only`, `bridge-enabled`, and `first_session` behavior intact.</action>
  <acceptance_criteria>
    - `test/support/example_host_contract.ex` references `examples/phoenix_host_upgrade_source`.
    - `test/support/example_host_contract.ex` no longer defines `simulate_upgrade_source!/1`.
    - The upgrade preparation logic adds `display_policy: PhoenixHostWeb.ObanPowertoolsDisplayPolicy` and materializes `lib/phoenix_host_web/oban_powertools_display_policy.ex` before proof execution.
    - The `native-only` branch still removes `{:oban_web, "~> 2.10", optional: true}` and the bridge-enabled branch still runs `oban_web_bridge_smoke_test.exs`.
  </acceptance_criteria>
  <verify>
    <automated>rg -n "phoenix_host_upgrade_source|display_policy: PhoenixHostWeb\\.ObanPowertoolsDisplayPolicy|oban_powertools_display_policy\\.ex|remove_optional_oban_web_dependency!|oban_web_bridge_smoke_test\\.exs" test/support/example_host_contract.ex && ! rg -n "simulate_upgrade_source!" test/support/example_host_contract.ex</automated>
  </verify>
  <done>The upgrade lane uses a real archived source host and only documented upgrade actions before proof runs.</done>
</task>

<task type="auto">
  <name>Task 2: Strengthen the executable upgrade-proof lane and CI command</name>
  <files>test/oban_powertools/example_host_contract_test.exs, .github/workflows/host-contract-proof.yml</files>
  <read_first>.planning/phases/15-upgrade-lane-support-truth-public-docs-integrity/15-CONTEXT.md, test/oban_powertools/example_host_contract_test.exs, .github/workflows/host-contract-proof.yml, test/support/example_host_contract.ex, guides/first-operator-session.md</read_first>
  <action>Rewrite the `@tag :"upgrade-proof"` test so it proves one meaningful native post-upgrade action per D-11 and D-26. Reuse the canonical threshold from the first-session lane: assert the upgraded host reaches the native cron proof and emits `ops-demo`, `nightly_sync`, and `pause_cron_entry` in the result output after the upgrade actions complete. Preserve `result.reset_output =~ "Migrated"` as a secondary assertion, not the main acceptance condition. Keep the workflow job name `upgrade-proof` and its command `mix test test/oban_powertools/example_host_contract_test.exs --only upgrade-proof` so the CI lane remains singular and explicit.</action>
  <acceptance_criteria>
    - `test/oban_powertools/example_host_contract_test.exs` contains `@tag :"upgrade-proof"`.
    - The upgrade-proof test asserts `ops-demo`, `nightly_sync`, and `pause_cron_entry`.
    - `.github/workflows/host-contract-proof.yml` contains an `upgrade-proof:` job that runs `mix test test/oban_powertools/example_host_contract_test.exs --only upgrade-proof`.
    - The upgrade-proof test does not stop at checking only `display_policy` restoration.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/oban_powertools/example_host_contract_test.exs --only upgrade-proof && rg -n "@tag :\"upgrade-proof\"|ops-demo|nightly_sync|pause_cron_entry|mix test test/oban_powertools/example_host_contract_test\\.exs --only upgrade-proof|upgrade-proof:" test/oban_powertools/example_host_contract_test.exs .github/workflows/host-contract-proof.yml</automated>
  </verify>
  <done>The executable upgrade lane proves a real native post-upgrade behavior and CI runs that exact lane explicitly.</done>
</task>

<task type="auto">
  <name>Task 3: Rewrite the public upgrade guide around the singular supported host shape</name>
  <files>guides/upgrade-and-compatibility.md</files>
  <read_first>.planning/phases/15-upgrade-lane-support-truth-public-docs-integrity/15-CONTEXT.md, guides/upgrade-and-compatibility.md, guides/first-operator-session.md, examples/phoenix_host_upgrade_source/README.md</read_first>
  <action>Rewrite `guides/upgrade-and-compatibility.md` so it describes the supported source lane in host-shape terms per D-05 through D-08 instead of “Phase 8/9/10” language. The guide must carry the full five-bucket support-truth contract from D-15 through D-20: `supported`, `tested`, `best-effort`, `host-owned`, and `intentionally unsupported`. Name one supported source lane with these concrete prerequisites: Phoenix host from `mix phx.new`, Postgres/Ecto, Powertools already installed, `repo` already configured, `auth_module` already configured, `/ops/jobs` already mounted, Powertools migrations already present, and `display_policy` still missing. Document the exact upgrade actions the harness performs: add `MyAppWeb.ObanPowertoolsDisplayPolicy`, wire `display_policy` into `config :oban_powertools`, re-check the host-owned `/ops/jobs` scope and browser pipeline, choose whether to enable optional `oban_web`, run current migrations, and prove the native `ops-demo` -> `pause_cron_entry` on `nightly_sync` threshold. Mark bridge-enabled, diverged, partially adopted, or missing-prerequisite hosts as `best-effort`, list auth/session/router/pipeline choices as `host-owned`, and state that bridge write parity and broader compatibility claims are `intentionally unsupported`.</action>
  <acceptance_criteria>
    - `guides/upgrade-and-compatibility.md` does not contain `Phase 8`, `Phase 9`, or `Phase 10`.
    - The guide contains the exact bucket labels `supported`, `tested`, `best-effort`, `host-owned`, and `intentionally unsupported`.
    - The guide names `display_policy`, `/ops/jobs`, `ops-demo`, `nightly_sync`, and `pause_cron_entry`.
    - The guide states that bridge-enabled or diverged source hosts are `best-effort`, that auth/session/router choices are `host-owned`, and that bridge write parity is `intentionally unsupported`.
  </acceptance_criteria>
  <verify>
    <automated>rg -n "supported|tested|best-effort|host-owned|intentionally unsupported|display_policy|/ops/jobs|ops-demo|nightly_sync|pause_cron_entry|bridge-enabled|diverged" guides/upgrade-and-compatibility.md && ! rg -n "Phase 8|Phase 9|Phase 10" guides/upgrade-and-compatibility.md</automated>
  </verify>
  <done>The public upgrade guide and the executable upgrade lane describe the same singular source host and the same proof threshold.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Archived fixture -> upgrade harness | Hidden extra rewrites would make the guide and executable lane diverge. |
| Public upgrade guide -> CI lane | If docs overstate or misdescribe the source lane, maintainers cannot trust the upgrade promise. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-04 | T | `test/support/example_host_contract.ex` | mitigate | Restrict the upgrade lane to archived-fixture copy plus documented `display_policy` actions; remove synthetic mutation logic. |
| T-15-05 | R | `guides/upgrade-and-compatibility.md` | mitigate | Remove internal phase-number wording and lock the singular host-shape prerequisites plus best-effort exclusions in docs and tests. |
| T-15-06 | E | native-vs-bridge support boundary | mitigate | Keep the native `/ops/jobs` post-upgrade proof as the required success action and avoid any bridge-write implication in the upgrade lane. |
</threat_model>

<verification>
- `rg -n "phoenix_host_upgrade_source|display_policy: PhoenixHostWeb\\.ObanPowertoolsDisplayPolicy|oban_powertools_display_policy\\.ex" test/support/example_host_contract.ex`
- `mix test test/oban_powertools/example_host_contract_test.exs --only upgrade-proof`
- `rg -n "supported|tested|best-effort|ops-demo|nightly_sync|pause_cron_entry" guides/upgrade-and-compatibility.md`
</verification>

<success_criteria>
- The `upgrade-proof` lane starts from a real archived host and applies only public guide steps.
- The public upgrade guide no longer relies on internal phase chronology.
- A maintainer can prove the documented upgrade lane with one deterministic native post-upgrade action.
</success_criteria>

<output>
After completion, create `.planning/phases/15-upgrade-lane-support-truth-public-docs-integrity/15-02-SUMMARY.md`
</output>
