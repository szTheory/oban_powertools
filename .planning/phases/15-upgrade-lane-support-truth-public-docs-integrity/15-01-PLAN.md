---
phase: 15-upgrade-lane-support-truth-public-docs-integrity
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - examples/phoenix_host_upgrade_source/README.md
  - examples/phoenix_host_upgrade_source/regenerate.sh
  - examples/phoenix_host_upgrade_source/mix.exs
  - examples/phoenix_host_upgrade_source/config/config.exs
  - examples/phoenix_host_upgrade_source/lib/phoenix_host_web/router.ex
  - examples/phoenix_host_upgrade_source/lib/phoenix_host_web/oban_powertools_auth.ex
  - examples/phoenix_host_upgrade_source/priv/repo/migrations/*.exs
  - examples/phoenix_host_upgrade_source/priv/repo/seeds.exs
autonomous: true
requirements:
  - PKG-02
must_haves:
  truths:
    - "The repo contains one frozen historical host fixture that represents the singular supported upgrade source lane before the explicit `display_policy` contract."
    - "Maintainers can tell exactly which historical commit the archived source fixture came from and how to regenerate it off the hot CI path."
    - "The archived source fixture keeps the native `/ops/jobs` shell, `repo`, `auth_module`, and Powertools migrations while intentionally omitting the current `display_policy` contract."
  artifacts:
    - path: "examples/phoenix_host_upgrade_source/README.md"
      provides: "Provenance, source commit, and support-truth notes for the historical upgrade-source fixture"
    - path: "examples/phoenix_host_upgrade_source/regenerate.sh"
      provides: "Maintainer-only regeneration script tied to the documented historical commit"
    - path: "examples/phoenix_host_upgrade_source/config/config.exs"
      provides: "Pre-display-policy host config that still preserves `repo` and `auth_module` wiring"
    - path: "examples/phoenix_host_upgrade_source/lib/phoenix_host_web/router.ex"
      provides: "Host-owned `/ops/jobs` scope and nested `/oban` bridge mount shape in the archived lane"
  key_links:
    - from: "examples/phoenix_host_upgrade_source/README.md"
      to: "examples/phoenix_host_upgrade_source/regenerate.sh"
      via: "documented source commit and regeneration path"
      pattern: "commit|regenerate"
    - from: "examples/phoenix_host_upgrade_source/config/config.exs"
      to: "examples/phoenix_host_upgrade_source/lib/phoenix_host_web/router.ex"
      via: "same supported source host shape with config and route ownership both present"
      pattern: "auth_module|/ops/jobs"
---

<objective>
Create the one frozen historical host fixture that defines the singular supported upgrade source lane.

Purpose: Replace the synthetic upgrade starting point with a real archived host shape per D-04 through D-13.
Output: A checked-in `examples/phoenix_host_upgrade_source` fixture with explicit provenance and a maintainer-only regeneration path.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/15-upgrade-lane-support-truth-public-docs-integrity/15-CONTEXT.md
@.planning/phases/15-upgrade-lane-support-truth-public-docs-integrity/15-RESEARCH.md
@.planning/phases/15-upgrade-lane-support-truth-public-docs-integrity/15-PATTERNS.md
@examples/phoenix_host/README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Freeze the real supported upgrade-source host shape</name>
  <files>examples/phoenix_host_upgrade_source/mix.exs, examples/phoenix_host_upgrade_source/config/config.exs, examples/phoenix_host_upgrade_source/lib/phoenix_host_web/router.ex, examples/phoenix_host_upgrade_source/lib/phoenix_host_web/oban_powertools_auth.ex, examples/phoenix_host_upgrade_source/priv/repo/migrations/*.exs, examples/phoenix_host_upgrade_source/priv/repo/seeds.exs</files>
  <read_first>.planning/phases/15-upgrade-lane-support-truth-public-docs-integrity/15-CONTEXT.md, .planning/phases/15-upgrade-lane-support-truth-public-docs-integrity/15-RESEARCH.md, .planning/phases/15-upgrade-lane-support-truth-public-docs-integrity/15-PATTERNS.md, examples/phoenix_host/mix.exs, examples/phoenix_host/config/config.exs, examples/phoenix_host/lib/phoenix_host_web/router.ex, examples/phoenix_host/lib/phoenix_host_web/oban_powertools_auth.ex, examples/phoenix_host/priv/repo/migrations/*.exs, examples/phoenix_host/priv/repo/seeds.exs</read_first>
  <action>Create `examples/phoenix_host_upgrade_source/` as the one archived source host per D-04, D-05, and D-09. Anchor provenance to commit `a1fed86`, because that installer shape already emitted `repo`, `auth_module`, and `/ops/jobs` but predates the explicit `display_policy` contract. Base the archived fixture on that host shape, not on a synthetic mutation of `examples/phoenix_host`. Preserve the supported-lane prerequisites verbatim: `repo` wiring, `auth_module: PhoenixHostWeb.ObanPowertoolsAuth`, the host-owned `/ops/jobs` scope with `ObanPowertools.Web.Router.oban_powertools_routes("/oban")`, the checked-in Powertools migration set under `priv/repo/migrations/`, and the narrow `ops-demo` / `nightly_sync` seed story. Intentionally omit `display_policy: PhoenixHostWeb.ObanPowertoolsDisplayPolicy` from the archived fixture config and omit any archived source file that would make the fixture look like the current post-upgrade host posture.</action>
  <acceptance_criteria>
    - `examples/phoenix_host_upgrade_source/config/config.exs` contains `repo: PhoenixHost.Repo` and `auth_module: PhoenixHostWeb.ObanPowertoolsAuth`.
    - `examples/phoenix_host_upgrade_source/config/config.exs` does not contain `display_policy: PhoenixHostWeb.ObanPowertoolsDisplayPolicy`.
    - `examples/phoenix_host_upgrade_source/lib/phoenix_host_web/router.ex` contains `scope "/ops/jobs"` and `oban_powertools_routes("/oban")`.
    - `examples/phoenix_host_upgrade_source/priv/repo/migrations/` contains the Powertools migration files for `oban_powertools_audit_events`, `oban_powertools_workflows`, `oban_powertools_heartbeats`, and `oban_powertools_repair_archives`.
    - `examples/phoenix_host_upgrade_source/priv/repo/seeds.exs` contains `ops-demo` and `nightly_sync`.
  </acceptance_criteria>
  <verify>
    <automated>rg -n "repo: PhoenixHost\\.Repo|auth_module: PhoenixHostWeb\\.ObanPowertoolsAuth|scope \"/ops/jobs\"|oban_powertools_routes\\(\"/oban\"\\)|ops-demo|nightly_sync" examples/phoenix_host_upgrade_source && ! rg -n "display_policy: PhoenixHostWeb\\.ObanPowertoolsDisplayPolicy" examples/phoenix_host_upgrade_source/config/config.exs && rg -n "oban_powertools_audit_events|oban_powertools_workflows|oban_powertools_heartbeats|oban_powertools_repair_archives" examples/phoenix_host_upgrade_source/priv/repo/migrations</automated>
  </verify>
  <done>The repo contains one auditable historical source fixture whose host shape matches the supported upgrade lane and is clearly pre-`display_policy`.</done>
</task>

<task type="auto">
  <name>Task 2: Document provenance and the maintainer-only regeneration path</name>
  <files>examples/phoenix_host_upgrade_source/README.md, examples/phoenix_host_upgrade_source/regenerate.sh</files>
  <read_first>.planning/phases/15-upgrade-lane-support-truth-public-docs-integrity/15-CONTEXT.md, .planning/phases/15-upgrade-lane-support-truth-public-docs-integrity/15-RESEARCH.md, examples/phoenix_host/README.md, examples/phoenix_host/regenerate.sh</read_first>
  <action>Write `examples/phoenix_host_upgrade_source/README.md` so it names the exact historical source commit SHA per D-09 and D-13, explains that this fixture is the singular supported upgrade-source lane per D-04 and D-05, and states that bridge-enabled hosts, diverged hosts, partially adopted hosts, or hosts missing `repo`, `auth_module`, or `/ops/jobs` are best-effort per D-08. Add `examples/phoenix_host_upgrade_source/regenerate.sh` as a maintainer-only helper that rebuilds the archived host from that exact commit and leaves it intentionally outside the normal PR proof path per D-12 and D-13.</action>
  <acceptance_criteria>
    - `examples/phoenix_host_upgrade_source/README.md` contains a literal historical commit SHA.
    - `examples/phoenix_host_upgrade_source/README.md` contains the phrases `supported upgrade source lane` and `best-effort`.
    - `examples/phoenix_host_upgrade_source/regenerate.sh` contains the documented commit SHA and a comment or echo indicating the script is maintainer-only.
    - `examples/phoenix_host_upgrade_source/regenerate.sh` does not run as part of CI workflow files.
  </acceptance_criteria>
  <verify>
    <automated>rg -n "supported upgrade source lane|best-effort|[0-9a-f]{7,40}" examples/phoenix_host_upgrade_source/README.md examples/phoenix_host_upgrade_source/regenerate.sh && ! rg -n "phoenix_host_upgrade_source/regenerate\\.sh" .github/workflows/host-contract-proof.yml</automated>
  </verify>
  <done>Maintainers can identify, explain, and if necessary regenerate the archived source fixture without weakening the hot-path proof model.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Archived upgrade source fixture -> upgrade proof harness | If the fixture drifts forward, `PKG-02` becomes a synthetic claim again. |
| Maintainer regeneration path -> public support promise | An undocumented or ambiguous source commit would make the supported lane impossible to audit. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-01 | T | `examples/phoenix_host_upgrade_source/*` | mitigate | Freeze a second fixture tree with no `display_policy` config and document that it is the singular supported source lane per D-04 through D-10. |
| T-15-02 | R | `examples/phoenix_host_upgrade_source/README.md` | mitigate | Record the exact source commit SHA and best-effort exclusions so maintainers can prove provenance later. |
| T-15-03 | I | support-truth in archived fixture docs | mitigate | State explicitly that regeneration is maintainer-only and not part of normal CI so the repo does not imply broader historical replay support. |
</threat_model>

<verification>
- `rg -n "repo: PhoenixHost\\.Repo|auth_module: PhoenixHostWeb\\.ObanPowertoolsAuth|scope \"/ops/jobs\"|oban_powertools_routes\\(\"/oban\"\\)|ops-demo|nightly_sync" examples/phoenix_host_upgrade_source`
- `! rg -n "display_policy: PhoenixHostWeb\\.ObanPowertoolsDisplayPolicy" examples/phoenix_host_upgrade_source/config/config.exs`
- `rg -n "supported upgrade source lane|best-effort|[0-9a-f]{7,40}" examples/phoenix_host_upgrade_source/README.md examples/phoenix_host_upgrade_source/regenerate.sh`
</verification>

<success_criteria>
- The repo has one and only one archived upgrade-source host lane.
- The archived host keeps the supported native-first prerequisites while remaining honestly pre-`display_policy`.
- Maintainers can verify provenance from the fixture itself without using CI replay or internal phase archaeology.
</success_criteria>

<output>
After completion, create `.planning/phases/15-upgrade-lane-support-truth-public-docs-integrity/15-01-SUMMARY.md`
</output>
