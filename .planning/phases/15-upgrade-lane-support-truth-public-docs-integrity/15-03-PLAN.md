---
phase: 15-upgrade-lane-support-truth-public-docs-integrity
plan: 03
type: execute
wave: 3
depends_on:
  - "15-02"
files_modified:
  - README.md
  - guides/support-truth-and-ownership-boundaries.md
  - guides/production-hardening.md
  - guides/troubleshooting.md
  - guides/example-app-walkthrough.md
  - examples/phoenix_host/README.md
  - test/oban_powertools/docs_contract_test.exs
autonomous: true
requirements:
  - PKG-02
  - HST-03
  - DOC-02
must_haves:
  truths:
    - "Public docs use one repeated five-bucket support-truth vocabulary: supported, tested, best-effort, host-owned, and intentionally unsupported."
    - "README, guide surfaces, and fixture docs all say the native `/ops/jobs` shell is primary and the optional `/ops/jobs/oban` bridge is a narrower read-only annex."
    - "Production-hardening and troubleshooting guidance points to real host-owned seams and real fail-fast runtime errors without turning narrative guidance into a brittle prose snapshot."
  artifacts:
    - path: "README.md"
      provides: "Front-door support-truth summary aligned to the real tested lanes and upgrade posture"
    - path: "guides/support-truth-and-ownership-boundaries.md"
      provides: "Primary long-form explanation of the five support-truth buckets and host/library ownership split"
    - path: "guides/production-hardening.md"
      provides: "Host-owned operational checklist tied to auth, telemetry, optional bridge, and redaction seams"
    - path: "guides/troubleshooting.md"
      provides: "Fail-fast contract errors and operational troubleshooting boundaries"
    - path: "test/oban_powertools/docs_contract_test.exs"
      provides: "Narrow claim-based assertions that lock stable public support-truth claims"
  key_links:
    - from: "README.md"
      to: "guides/support-truth-and-ownership-boundaries.md"
      via: "shared five-bucket vocabulary"
      pattern: "supported|tested|best-effort|host-owned|intentionally unsupported"
    - from: "guides/troubleshooting.md"
      to: "lib/oban_powertools/runtime_config.ex"
      via: "exact fail-fast setup error strings"
      pattern: "requires :repo|requires :auth_module|requires :display_policy"
    - from: "test/oban_powertools/docs_contract_test.exs"
      to: "README.md"
      via: "claim-based markers for support-truth and tested lanes"
      pattern: "supported|tested|best-effort|read-only"
---

<objective>
Align public support-truth, hardening, and troubleshooting docs with the actual tested host contract.

Purpose: Close `HST-03` and `DOC-02` by making docs say exactly what the fixture, upgrade lane, and runtime checks prove, without widening the support promise.
Output: Updated README and guide surfaces plus a narrowed docs-contract test that enforces stable claims only.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/15-upgrade-lane-support-truth-public-docs-integrity/15-CONTEXT.md
@.planning/phases/15-upgrade-lane-support-truth-public-docs-integrity/15-RESEARCH.md
@.planning/phases/15-upgrade-lane-support-truth-public-docs-integrity/15-PATTERNS.md
@.planning/phases/15-upgrade-lane-support-truth-public-docs-integrity/15-02-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite the public support-truth surfaces around the five explicit buckets</name>
  <files>README.md, guides/support-truth-and-ownership-boundaries.md, guides/example-app-walkthrough.md, examples/phoenix_host/README.md</files>
  <read_first>.planning/phases/15-upgrade-lane-support-truth-public-docs-integrity/15-CONTEXT.md, README.md, guides/support-truth-and-ownership-boundaries.md, guides/example-app-walkthrough.md, examples/phoenix_host/README.md, guides/optional-oban-web-bridge.md, guides/upgrade-and-compatibility.md</read_first>
  <action>Rewrite the public support-truth sections per D-14 through D-21 so all front-door docs use the same five explicit buckets: `supported`, `tested`, `best-effort`, `host-owned`, and `intentionally unsupported`. In `README.md`, keep the short-form summary concise but explicit: native `/ops/jobs` shell supported, optional `/ops/jobs/oban` bridge read-only and narrower, supported upgrade lane singular, tested lanes enumerated, best-effort outside tested lanes, host-owned seams listed, intentionally unsupported cases listed. In `guides/support-truth-and-ownership-boundaries.md`, make those five buckets the primary structure. In `guides/example-app-walkthrough.md` and `examples/phoenix_host/README.md`, explain that `examples/phoenix_host` remains the current-state canonical fixture while `examples/phoenix_host_upgrade_source` exists only for the supported upgrade lane.</action>
  <acceptance_criteria>
    - `README.md` and `guides/support-truth-and-ownership-boundaries.md` contain the exact bucket labels `supported`, `tested`, `best-effort`, `host-owned`, and `intentionally unsupported`.
    - `README.md` contains `/ops/jobs`, `/ops/jobs/oban`, `read-only`, and `best-effort outside tested lanes`.
    - `guides/example-app-walkthrough.md` or `examples/phoenix_host/README.md` mentions both `examples/phoenix_host` and `examples/phoenix_host_upgrade_source`.
    - No edited support-truth surface implies bridge write parity.
  </acceptance_criteria>
  <verify>
    <automated>rg -n "supported|tested|best-effort|host-owned|intentionally unsupported|/ops/jobs|/ops/jobs/oban|read-only" README.md && rg -n "supported|tested|best-effort|host-owned|intentionally unsupported" guides/support-truth-and-ownership-boundaries.md && rg -n "examples/phoenix_host_upgrade_source" guides/example-app-walkthrough.md examples/phoenix_host/README.md && ! rg -n "write parity|co-equal product surface" README.md guides/support-truth-and-ownership-boundaries.md</automated>
  </verify>
  <done>Public support-truth language is layered, repeated, and consistent across the repo’s main entrypoints.</done>
</task>

<task type="auto">
  <name>Task 2: Align hardening and troubleshooting guidance to real host-owned seams</name>
  <files>guides/production-hardening.md, guides/troubleshooting.md</files>
  <read_first>.planning/phases/15-upgrade-lane-support-truth-public-docs-integrity/15-CONTEXT.md, guides/production-hardening.md, guides/troubleshooting.md, lib/oban_powertools/runtime_config.ex, README.md</read_first>
  <action>Update `guides/production-hardening.md` and `guides/troubleshooting.md` to close DOC-02 without over-specifying prose per D-24 and D-25. Keep `production-hardening.md` as narrative checklist guidance, but ensure it explicitly covers host-owned auth policy, actor/session lookup, display/redaction policy, browser pipeline around `/ops/jobs`, reverse-proxy/WebSocket behavior, optional bridge exposure in production, and telemetry consumers staying inside the public low-cardinality contract. Keep `troubleshooting.md` anchored to real runtime contract checks by reusing the exact `RuntimeConfig` error strings for missing `repo`, `auth_module`, and `display_policy`, then connect them to operational caveats like wrong browser pipeline, supervision gaps, and broken LiveView transport.</action>
  <acceptance_criteria>
    - `guides/production-hardening.md` contains `auth_module`, `display_policy`, `router` or `/ops/jobs`, `reverse-proxy`, `WebSocket`, and `telemetry`.
    - `guides/troubleshooting.md` contains the exact runtime error prefixes `Oban Powertools requires :repo`, `Oban Powertools requires :auth_module`, and `Oban Powertools requires :display_policy`.
    - `guides/troubleshooting.md` contains `/ops/jobs`, `session/auth`, and `WebSocket`.
    - Neither file turns the entire checklist or troubleshooting prose into exact line-by-line runtime guarantees beyond the fail-fast errors.
  </acceptance_criteria>
  <verify>
    <automated>rg -n "auth_module|display_policy|/ops/jobs|reverse-proxy|WebSocket|telemetry|Oban Powertools requires :repo|Oban Powertools requires :auth_module|Oban Powertools requires :display_policy|session/auth" guides/production-hardening.md guides/troubleshooting.md</automated>
  </verify>
  <done>The hardening and troubleshooting guides now teach the real host-owned seams and fail-fast errors a maintainer will actually encounter.</done>
</task>

<task type="auto">
  <name>Task 3: Narrow docs contract enforcement to stable claim markers</name>
  <files>test/oban_powertools/docs_contract_test.exs</files>
  <read_first>.planning/phases/15-upgrade-lane-support-truth-public-docs-integrity/15-CONTEXT.md, test/oban_powertools/docs_contract_test.exs, README.md, guides/upgrade-and-compatibility.md, guides/support-truth-and-ownership-boundaries.md, guides/production-hardening.md, guides/troubleshooting.md, .github/workflows/host-contract-proof.yml</read_first>
  <action>Update `test/oban_powertools/docs_contract_test.exs` so it remains claim-based per D-22 through D-25. Expand `@docs_files` to include `guides/production-hardening.md` and `guides/troubleshooting.md`. Assert the stable public markers only: canonical commands and paths, lane names, the five support-truth bucket labels, `best-effort outside tested lanes`, the native-first support wording, the optional bridge read-only wording, and the exact runtime fail-fast error strings. Do not snapshot whole paragraphs or checklist ordering. Keep the workflow assertion checking explicit lane names, including `upgrade-proof`.</action>
  <acceptance_criteria>
    - `test/oban_powertools/docs_contract_test.exs` includes `guides/production-hardening.md` and `guides/troubleshooting.md` in `@docs_files`.
    - The test asserts `supported`, `tested`, `best-effort`, `host-owned`, and `intentionally unsupported`.
    - The test asserts `best-effort outside tested lanes`.
    - The test asserts `upgrade-proof:` from `.github/workflows/host-contract-proof.yml`.
    - The file does not assert whole narrative paragraphs from `guides/production-hardening.md` or `guides/troubleshooting.md`.
  </acceptance_criteria>
  <verify>
    <automated>rg -n "guides/production-hardening\\.md|guides/troubleshooting\\.md|supported|tested|best-effort|host-owned|intentionally unsupported|best-effort outside tested lanes|upgrade-proof:" test/oban_powertools/docs_contract_test.exs && mix test test/oban_powertools/docs_contract_test.exs</automated>
  </verify>
  <done>The docs contract protects the stable public claims for support truth and hardening without freezing narrative prose.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Public docs -> host integrator assumptions | Overstated or blurry support language can push unsafe host decisions onto the wrong owner. |
| Narrative guides -> docs contract test | If tests snapshot prose, maintainers will avoid improving guidance or will encode false guarantees. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-07 | S | README and support-truth docs | mitigate | Make the host-owned seams and the five bucket labels explicit so library-vs-host responsibilities cannot masquerade as each other. |
| T-15-08 | E | optional bridge posture | mitigate | Repeat that `/ops/jobs/oban` is read-only and narrower than the native surface; exclude bridge write parity from supported claims. |
| T-15-09 | R | compatibility and upgrade promise | mitigate | Assert `best-effort outside tested lanes` in docs contract so unsupported combinations are never implied as tested support. |
| T-15-10 | T | docs contract scope | mitigate | Keep assertions on commands, lane names, bucket labels, and runtime error strings only, not whole prose snapshots. |
</threat_model>

<verification>
- `rg -n "supported|tested|best-effort|host-owned|intentionally unsupported|best-effort outside tested lanes" README.md guides/support-truth-and-ownership-boundaries.md test/oban_powertools/docs_contract_test.exs`
- `rg -n "Oban Powertools requires :repo|Oban Powertools requires :auth_module|Oban Powertools requires :display_policy" guides/troubleshooting.md test/oban_powertools/docs_contract_test.exs`
- `mix test test/oban_powertools/docs_contract_test.exs`
</verification>

<success_criteria>
- Support-truth docs present one coherent public vocabulary with no contradiction between README, guides, and fixture docs.
- Hardening and troubleshooting docs point to real seams and runtime failures a host maintainer can act on.
- Docs contract enforcement stays narrow, stable, and aligned with the real tested host-contract lanes.
</success_criteria>

<output>
After completion, create `.planning/phases/15-upgrade-lane-support-truth-public-docs-integrity/15-03-SUMMARY.md`
</output>
