---
phase: 81-page-migration-wave-3-batches-workflows-lifeline
plan: 13
type: execute
wave: 12
depends_on:
  - 81-09
  - 81-11
  - 81-12
files_modified:
  - test/browser/voiceover/page.voiceover.spec.ts
  - .github/workflows/ci.yml
  - test/browser/support/verify-page-script-order.mjs
autonomous: true
requirements:
  - PAGE-03
  - PAGE-04
  - PAGE-07
  - PAGE-10
  - A11Y-01
  - A11Y-02
  - A11Y-03
  - A11Y-04
must_haves:
  truths:
    - "Required CI runs the full 163-target nine-family axe/ARIA/VRT graph with zero critical/serious axe findings."
    - "VoiceOver discovery resolves exactly the Batches confirmation, blocked Workflow step, and Lifeline preview/result targets."
  artifacts:
    - path: "test/browser/voiceover/page.voiceover.spec.ts"
      provides: "Three exact manifest-derived Wave 3 VoiceOver discovery targets"
    - path: ".github/workflows/ci.yml"
      provides: "Merge-blocking full page-quality command"
    - path: "test/browser/support/verify-page-script-order.mjs"
      provides: "Executable CI full-gate and ci-gate dependency validator"
  key_links:
    - from: ".github/workflows/ci.yml"
      to: "test/browser/support/verify-page-script-order.mjs"
      via: "structured assertion of the required unfiltered command and ci-gate dependency"
      pattern: "page_quality|ci-gate|verify:pages"
---

<objective>
Register exact screen-reader discovery and make the complete nine-family quality graph merge-blocking.

Purpose: separate CI/VoiceOver policy from artifact mutation and final validation.
Output: exact VoiceOver list inventory and required full page-quality CI.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/81-page-migration-wave-3-batches-workflows-lifeline/81-09-SUMMARY.md
@.planning/phases/81-page-migration-wave-3-batches-workflows-lifeline/81-11-SUMMARY.md
@.planning/phases/81-page-migration-wave-3-batches-workflows-lifeline/81-12-SUMMARY.md
@.github/workflows/ci.yml
</context>

<tasks>

<task type="auto">
  <name>Task 81-13-01: Register exact VoiceOver discovery targets</name>
  <files>test/browser/voiceover/page.voiceover.spec.ts</files>
  <read_first>test/browser/voiceover/page.voiceover.spec.ts, voiceover.config.ts, test/browser/support/manifest.ts</read_first>
  <action>Add exactly three manifest-derived discovery targets: Batches confirmation, blocked Workflow step, and Lifeline preview/result. Require the exact catalog IDs and fail if absent or duplicated. Keep transcript capture truthful and conditional on supported macOS Guidepup setup; never invent transcripts.</action>
  <acceptance_criteria>VoiceOver list output names exactly the three Wave 3 targets in addition to preserved prior targets and resolves them from the generated manifest.</acceptance_criteria>
  <verify>
    <automated>npm run showcase:manifest &amp;&amp; npx playwright test --config=voiceover.config.ts --list</automated>
  </verify>
  <done>Wave 3 screen-reader discovery is exact and executable.</done>
</task>

<task type="auto">
  <name>Task 81-13-02: Require the full page-quality graph in CI</name>
  <files>.github/workflows/ci.yml, test/browser/support/verify-page-script-order.mjs</files>
  <read_first>.github/workflows/ci.yml, .github/workflows/page-quality-nightly.yml, package.json, test/browser/support/verify-page-script-order.mjs</read_first>
  <action>Require `npm run verify:pages` in the merge-blocking `page_quality` job after browser/container prerequisites. Do not substitute a family allowlist, grep/filter, page-only shortcut, update/watch mode, ignored failure, `continue-on-error`, or nightly-only run. Preserve the nightly lane as additive evidence. Extend `test/browser/support/verify-page-script-order.mjs` with a concrete executable CI validator that reads `.github/workflows/ci.yml`, parses its YAML structure, and asserts: the exact unfiltered `npm run verify:pages` run step occurs exactly once in the workflow; it belongs to `jobs.page_quality`; `jobs.page_quality` is a direct `ci-gate.needs` dependency exactly once; `ci-gate.needs` contains exactly the seven required jobs `format`, `compile`, `test`, `page_quality`, `visual_a11y`, `docs_package`, and `actionlint` with no duplicate or omitted dependency; and the required run step/job cannot ignore failure. The validator must exit nonzero for a filtered, duplicated, moved, optional, or detached gate.</action>
  <acceptance_criteria>CI syntax is valid, the executable validator passes, and mutation fixtures or equivalent assertions prove it fails for duplicate/filtered `verify:pages`, wrong job ownership, missing/duplicate `page_quality` dependency, or incorrect ci-gate dependency cardinality.</acceptance_criteria>
  <verify>
    <automated>node test/browser/support/verify-page-script-order.mjs</automated>
  </verify>
  <done>The full page-quality contract is merge-blocking.</done>
</task>

</tasks>

<threat_model>
## ASVS L1 Trust Boundaries

| Boundary | Threat | Severity | Disposition and blocking mitigation |
|---|---|---|---|
| CI configuration → merge decision | Partial or nightly-only evidence permits regression | high | Mitigate; exact required full-command assertion blocks completion. |
| Screen-reader artifact → user | Invented transcript misrepresents assistive output | high | Mitigate; discovery is automated; transcript capture remains conditional and truthful. |
</threat_model>

<verification>
List exact VoiceOver targets, parse CI YAML, and assert the full command graph is required.
</verification>

<success_criteria>
Screen-reader discovery is exact and all nine-family page-quality evidence is merge-blocking.
</success_criteria>

## Artifacts this phase produces

- Three exact VoiceOver discovery targets.
- Merge-blocking full nine-family quality CI.

<output>
Create `.planning/phases/81-page-migration-wave-3-batches-workflows-lifeline/81-13-SUMMARY.md` when done.
</output>
