---
phase: 58
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/oban_powertools/cron.ex
  - test/oban_powertools/cron_test.exs
autonomous: true
requirements:
  - INT-02
must_haves:
  truths:
    - A cron-enqueued job from a `deadline:`-configured Powertools worker has `__deadline_at__` present in the database record meta
    - A cron-enqueued job from a Powertools worker without `deadline:` has no `__deadline_at__` key in meta
    - A cron-enqueued job from a worker with both `redact:` and `deadline:` has both `__redacted_fields__` and `__deadline_at__` present in meta
    - Plain (non-Powertools) Oban workers scheduled via cron are unaffected
  artifacts:
    - path: lib/oban_powertools/cron.ex
      provides: Cron slot claiming and job insertion logic
    - path: test/oban_powertools/cron_test.exs
      provides: Test coverage for cron deadline injection
  key_links:
    - from: lib/oban_powertools/cron.ex
      to: ObanPowertools.Worker.Deadlines
      via: build_meta(deadline_ms, now)
---

<objective>
Inject `__deadline_at__` meta into the database record for cron-scheduled Powertools workers configured with a `deadline:`, closing the INT-02 integration gap.

Purpose: Brings the cron enqueue path into alignment with the primary idempotency enqueue path, where deadlines are correctly injected.
Output: Modified `cron.ex` that threads `now` and passes `meta:` to `worker_module.new/2`, and accompanying tests in `cron_test.exs`.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@lib/oban_powertools/cron.ex
@test/oban_powertools/cron_test.exs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add Cron Deadline Tests</name>
  <files>test/oban_powertools/cron_test.exs</files>
  <behavior>
    - Define `CronDeadlineWorker` (deadline: {1, :hour}) and `CronRedactDeadlineWorker` (redact: [:ssn], deadline: {1, :hour}) at the top of the file
    - Assert cron enqueueing assigns `__deadline_at__` meta for the deadline-configured worker
    - Assert cron enqueueing correctly composes `__deadline_at__` and `__redacted_fields__` when both are present
    - Assert plain powertools workers and bare Oban workers have no `__deadline_at__`
  </behavior>
  <action>
    Replicate the existing `CronRedactWorker` pattern to create `CronDeadlineWorker` and `CronRedactDeadlineWorker` definitions.
    Add test cases inside the "claim_slot/4" test block that verify `job.meta["__deadline_at__"]` is present/absent correctly for these workers upon enqueueing via `Cron.claim_slot/3`.
  </action>
  <verify>
    <automated>mix test test/oban_powertools/cron_test.exs</automated>
  </verify>
  <done>Failing tests are correctly defined to cover all INT-02 scenarios.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Cron Meta Injection</name>
  <files>lib/oban_powertools/cron.ex</files>
  <action>
    Per D-01: Thread `now` (bound around line 52 in `claim_slot/4`) as the fifth parameter through all four `maybe_insert_job` clause heads. Pass it via the `Multi.run` lambda.
    
    Per D-02 and D-04: Inside the `function_exported?(:__powertools_limits__, 0)` true branch in `maybe_insert_job`, resolve `deadline_ms` via `worker_module.__powertools_deadline_ms__()` (if exported), and then construct `deadline_meta = ObanPowertools.Worker.Deadlines.build_meta(deadline_ms, now)`. Do not touch the `else` or `rescue` paths.
    
    Per D-03: Pass this meta in the initialization options: `worker_module.new(args, queue: queue, meta: deadline_meta)`. This delegates to the worker's `new/2` which will safely deep-merge its own redaction metadata on top.
  </action>
  <verify>
    <automated>mix test test/oban_powertools/cron_test.exs</automated>
  </verify>
  <done>Cron execution logic correctly injects deadline meta, and all tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DB -> Code | Reading worker strings from the job entry requires safety checks. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-58-01 | Elevation of Privilege | `cron.ex` / `String.to_existing_atom` | mitigate | Ensure we only call powertools specific functions inside the `function_exported?` check, gracefully falling back to `Oban.Job.new/2` on missing modules. |
</threat_model>

<verification>
mix test test/oban_powertools/cron_test.exs
</verification>

<success_criteria>
- Cron-enqueued jobs via ObanPowertools.Worker with `deadline:` have the `__deadline_at__` meta key set.
- Jobs with both `redact:` and `deadline:` correctly compose their meta attributes.
</success_criteria>

<output>
Create `.planning/phases/58-cron-deadline-injection/58-01-SUMMARY.md` when done
</output>