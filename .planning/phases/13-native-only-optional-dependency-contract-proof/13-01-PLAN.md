---
phase: 13-native-only-optional-dependency-contract-proof
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - test/support/example_host_contract.ex
  - test/oban_powertools/example_host_contract_test.exs
  - examples/phoenix_host/test/phoenix_host_web/oban_web_bridge_smoke_test.exs
autonomous: true
requirements:
  - PKG-03
  - DOC-03
must_haves:
  truths:
    - "A copied canonical host can compile and reset after `oban_web` is removed before dependency resolution."
    - "The optional bridge lane proves one real `/ops/jobs/oban` render through the shared host session."
    - "The bounded bridge remains a read-only annex rather than a write-capable operator surface."
  artifacts:
    - path: "test/support/example_host_contract.ex"
      provides: "Temp-fixture lane rewrites and lane-specific proof orchestration"
    - path: "test/oban_powertools/example_host_contract_test.exs"
      provides: "Root proof assertions for native-only and bridge-enabled lanes"
    - path: "examples/phoenix_host/test/phoenix_host_web/oban_web_bridge_smoke_test.exs"
      provides: "Fixture-backed bridge render smoke at /ops/jobs/oban"
  key_links:
    - from: "test/support/example_host_contract.ex"
      to: "examples/phoenix_host/mix.exs"
      via: "native-only temp rewrite removing `:oban_web` before `mix deps.get`"
      pattern: "remove_optional_oban_web_dependency!"
    - from: "test/support/example_host_contract.ex"
      to: "examples/phoenix_host/test/phoenix_host_web/oban_web_bridge_smoke_test.exs"
      via: "bridge-enabled lane running a focused fixture test"
      pattern: "oban_web_bridge_smoke_test\\.exs"
    - from: "examples/phoenix_host/test/phoenix_host_web/oban_web_bridge_smoke_test.exs"
      to: "/ops/jobs/oban"
      via: "Phoenix.LiveViewTest.live/2 under a real session"
      pattern: "live\\(conn, \"/ops/jobs/oban\"\\)"
---

<objective>
Make the canonical example-host proof honest for both optional-dependency paths.

Purpose: Close the false native-only proof gap per D-03 through D-04 while adding the one real bridge render smoke required by D-11 through D-12.
Output: A temp-fixture harness that removes `oban_web` only for the copied native-only lane, plus fixture-backed bridge render proof through the shared auth/session path.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/13-native-only-optional-dependency-contract-proof/13-CONTEXT.md
@.planning/phases/13-native-only-optional-dependency-contract-proof/13-RESEARCH.md
@.planning/phases/13-native-only-optional-dependency-contract-proof/13-PATTERNS.md
@.planning/phases/13-native-only-optional-dependency-contract-proof/13-VALIDATION.md
@.planning/phases/11-docs-example-app-compatibility-contract-proof/11-04-SUMMARY.md
@.planning/phases/12-fresh-host-install-path-example-fixture-repair/12-03-SUMMARY.md
@examples/phoenix_host/AGENTS.md
@test/support/example_host_contract.ex
@test/oban_powertools/example_host_contract_test.exs
@lib/oban_powertools/web/router.ex
@lib/oban_powertools/web/oban_web_bridge.ex

<interfaces>
From `test/support/example_host_contract.ex`:
```elixir
def prepare_host!(lane)
def run!(dir, env, command, args)
def proof!(lane)
def first_session!()
```

From `lib/oban_powertools/web/router.ex`:
```elixir
oban_dashboard(path,
  resolver: ObanPowertools.Web.ObanWebBridge,
  on_mount: [ObanPowertools.Web.LiveAuth]
)
```

From `lib/oban_powertools/web/oban_web_bridge.ex`:
```elixir
def resolve_user(conn), do: Auth.current_actor(conn)
def resolve_access(actor)
def format_job_args(job)
def format_job_meta(job)
def format_recorded(recorded, job)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Make the native-only lane remove `oban_web` before dependency resolution</name>
  <files>test/support/example_host_contract.ex, test/oban_powertools/example_host_contract_test.exs</files>
  <read_first>.planning/phases/13-native-only-optional-dependency-contract-proof/13-CONTEXT.md, .planning/phases/13-native-only-optional-dependency-contract-proof/13-RESEARCH.md, examples/phoenix_host/AGENTS.md, test/support/example_host_contract.ex, test/oban_powertools/example_host_contract_test.exs, examples/phoenix_host/mix.exs</read_first>
  <behavior>
    - Test 1: `ExampleHostContract.proof!("native-only")` rewrites the copied fixture so `mix.exs` no longer contains `{:oban_web, "~> 2.10", optional: true}` before `mix deps.get`.
    - Test 2: the native-only contract test proves the copied host still compiles and resets, and a supplemental `mix compile --no-optional-deps --warnings-as-errors` run succeeds in the copied host.
    - Test 3: `mix deps` executed inside the copied native-only host does not list `oban_web`.
  </behavior>
  <action>Update `prepare_host!/1` so the `"native-only"` branch calls a new private helper `remove_optional_oban_web_dependency!/1` before any `mix deps.get` call, per D-03, D-04, and D-09. Implement that helper in `test/support/example_host_contract.ex` by reading the copied `mix.exs`, removing the exact dependency entry `{:oban_web, "~> 2.10", optional: true}`, failing fast if the source is unchanged, writing the updated file back, and then running `mix deps.unlock --unused` in the copied host so stale lock entries do not mask the lane truth. Keep the rewrite scoped to the temp copy only; do not touch the checked-in fixture and do not add any other lane rewrites. In `test/oban_powertools/example_host_contract_test.exs`, extend the `"native-only lane compiles and resets cleanly"` test so it reads `Path.join(result.dir, "mix.exs")`, refutes `":oban_web"`, runs `ExampleHostContract.run!(result.dir, [], "mix", ["deps"])` and refutes `"oban_web"` in that output, then runs `ExampleHostContract.run!(result.dir, [], "mix", ["compile", "--no-optional-deps", "--warnings-as-errors"])` as the supplemental guard from D-05 while still asserting the existing compile/reset outputs.</action>
  <acceptance_criteria>
    - `test/support/example_host_contract.ex` contains a `"native-only" -> remove_optional_oban_web_dependency!(target)` branch in `prepare_host!/1`.
    - `test/support/example_host_contract.ex` defines `defp remove_optional_oban_web_dependency!(dir)` and includes the exact command `["deps.unlock", "--unused"]`.
    - `test/oban_powertools/example_host_contract_test.exs` contains `refute mix_source =~ ":oban_web"`.
    - `test/oban_powertools/example_host_contract_test.exs` contains the exact compile guard command `["compile", "--no-optional-deps", "--warnings-as-errors"]`.
    - `test/oban_powertools/example_host_contract_test.exs` contains a `refute deps_output =~ "oban_web"` assertion.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/oban_powertools/example_host_contract_test.exs --only native-only</automated>
  </verify>
  <done>The native-only lane proves a copied host with `oban_web` physically absent from its dependency list can still compile and reset cleanly, and the proof remains limited to the temp fixture copy per D-07 through D-10.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add one bounded bridge render smoke to the canonical fixture lane</name>
  <files>test/support/example_host_contract.ex, test/oban_powertools/example_host_contract_test.exs, examples/phoenix_host/test/phoenix_host_web/oban_web_bridge_smoke_test.exs</files>
  <read_first>.planning/phases/13-native-only-optional-dependency-contract-proof/13-CONTEXT.md, .planning/phases/13-native-only-optional-dependency-contract-proof/13-RESEARCH.md, examples/phoenix_host/AGENTS.md, test/support/example_host_contract.ex, test/oban_powertools/example_host_contract_test.exs, examples/phoenix_host/test/phoenix_host_web/oban_powertools_first_session_test.exs, lib/oban_powertools/web/router.ex, lib/oban_powertools/web/oban_web_bridge.ex</read_first>
  <behavior>
    - Test 1: the bridge-enabled root contract lane runs a focused fixture test that mounts `/ops/jobs/oban` under a real session and returns `1 test, 0 failures`.
    - Test 2: the fixture smoke initializes the session with `PhoenixHostWeb.ObanPowertoolsAuth.demo_actor()` and reaches `live(conn, "/ops/jobs/oban")` successfully.
    - Test 3: the bridge proof continues to assert the bounded read-only contract through the existing resolver/access seams rather than adding broad Oban Web UI parity assertions.
  </behavior>
  <action>Keep `proof!("bridge-enabled")` as the canonical root lane entry point, but extend it so after `mix deps.get`, `mix compile`, `MIX_ENV=test mix ecto.reset`, and `MIX_ENV=test mix run priv/repo/seeds.exs`, it also runs `MIX_ENV=test mix test --trace test/phoenix_host_web/oban_web_bridge_smoke_test.exs` and returns that command output in a new `render_output` field. Create `examples/phoenix_host/test/phoenix_host_web/oban_web_bridge_smoke_test.exs` using the existing `PhoenixHostWeb.ConnCase` plus `Phoenix.LiveViewTest` pattern from the first-session proof: initialize the session with `%{"ops_actor" => PhoenixHostWeb.ObanPowertoolsAuth.demo_actor()}`, call `live(conn, "/ops/jobs/oban")`, and assert the page renders stable bridge markers without broad dashboard-parity checks. Use concrete assertions for `"Oban Web"` and for the nested path `"/ops/jobs/oban"`, and keep the test focused on mount success plus shared auth continuity per D-11 through D-13. In `test/oban_powertools/example_host_contract_test.exs`, replace the current bridge-enabled compile/reset-only assertions with checks on `result.render_output` for `"PhoenixHostWeb.ObanWebBridgeSmokeTest"`, `"1 test, 0 failures"`, `"/ops/jobs/oban"`, and `"Oban Web"`. Keep the task-level verify loop limited to the focused bridge smoke sampling command; because this task changes code under `examples/phoenix_host`, reserve `mix precommit` for wave-end/full-suite verification so the plan still ends with the fixture-level contract required by `examples/phoenix_host/AGENTS.md`.</action>
  <acceptance_criteria>
    - `test/support/example_host_contract.ex` returns a `render_output` field for the `"bridge-enabled"` proof lane.
    - `test/support/example_host_contract.ex` contains `test/phoenix_host_web/oban_web_bridge_smoke_test.exs`.
    - `examples/phoenix_host/test/phoenix_host_web/oban_web_bridge_smoke_test.exs` contains `live(conn, "/ops/jobs/oban")`.
    - `examples/phoenix_host/test/phoenix_host_web/oban_web_bridge_smoke_test.exs` contains `PhoenixHostWeb.ObanPowertoolsAuth.demo_actor()`.
    - `test/oban_powertools/example_host_contract_test.exs` asserts `result.render_output =~ "PhoenixHostWeb.ObanWebBridgeSmokeTest"` and `result.render_output =~ "1 test, 0 failures"`.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/oban_powertools/example_host_contract_test.exs --only bridge-enabled</automated>
  </verify>
  <done>The bridge-enabled lane now proves one real `/ops/jobs/oban` render through the shared host session while staying within the bounded read-only bridge contract.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| temp fixture rewrite -> Mix dependency resolution | Untrusted proof drift can masquerade as native-only truth if the copied host still resolves `oban_web` |
| host session -> `/ops/jobs/oban` LiveView mount | The bridge render must reuse the same host-owned auth/session seam as native pages |
| bridge resolver -> operator permissions | The optional bridge must never escalate from read-only inspection into write access |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-01 | T | `test/support/example_host_contract.ex` native-only lane | mitigate | Remove `oban_web` from the copied `mix.exs` before `mix deps.get`, run `mix deps.unlock --unused`, and assert the copied host no longer lists `oban_web` in tests. |
| T-13-02 | E | `/ops/jobs/oban` bridge proof | mitigate | Mount the bridge only through `live(conn, "/ops/jobs/oban")` with a real session actor and preserve `ObanPowertools.Web.ObanWebBridge` read-only assertions rather than adding mutation flows. |
| T-13-03 | R | Bridge support claims | mitigate | Keep the bridge smoke limited to one mount proof and stable output markers so CI evidence matches the bounded support promise. |
</threat_model>

<verification>
- `mix test test/oban_powertools/example_host_contract_test.exs --only native-only`
- `mix test test/oban_powertools/example_host_contract_test.exs --only bridge-enabled`
- `bash -lc 'cd examples/phoenix_host && mix precommit && cd ../.. && mix test test/oban_powertools/example_host_contract_test.exs test/oban_powertools/docs_contract_test.exs test/oban_powertools/web/router_test.exs test/oban_powertools/fresh_host_contract_test.exs'`
</verification>

<success_criteria>
- The native-only lane physically removes `oban_web` from the copied host and still passes compile/reset proof.
- The bridge-enabled lane proves one real `/ops/jobs/oban` render through the shared actor/session path.
- No checked-in second fixture host is created, and the fresh-host installer lane remains untouched.
</success_criteria>

<output>
After completion, create `.planning/phases/13-native-only-optional-dependency-contract-proof/13-01-SUMMARY.md`
</output>
