---
phase: 13-native-only-optional-dependency-contract-proof
plan: 03
type: execute
wave: 2
depends_on:
  - 13-01
  - 13-02
files_modified:
  - test/oban_powertools/docs_contract_test.exs
  - .github/workflows/host-contract-proof.yml
  - .planning/phases/13-native-only-optional-dependency-contract-proof/13-VALIDATION.md
autonomous: true
requirements:
  - DOC-03
  - PKG-03
must_haves:
  truths:
    - "Docs-contract tests fail if native-first wording or optional-bridge wording drifts."
    - "The CI workflow exposes explicit native-first and optional-bridge proof jobs that match the public docs."
    - "The separate `fresh-host` installer lane remains intact and distinct from the canonical fixture lanes."
  artifacts:
    - path: "test/oban_powertools/docs_contract_test.exs"
      provides: "Automated support-truth marker assertions for docs and workflow names"
    - path: ".github/workflows/host-contract-proof.yml"
      provides: "Native-first proof job names and preserved fresh-host lane"
    - path: ".planning/phases/13-native-only-optional-dependency-contract-proof/13-VALIDATION.md"
      provides: "Nyquist-complete task ownership and validation sampling for all Phase 13 plans"
  key_links:
    - from: "test/oban_powertools/docs_contract_test.exs"
      to: ".github/workflows/host-contract-proof.yml"
      via: "job-name assertions for proof lanes"
      pattern: "native-first|optional-bridge|fresh-host"
    - from: "test/oban_powertools/docs_contract_test.exs"
      to: "README.md"
      via: "exact support-truth wording assertions"
      pattern: "native, host-owned operator shell at `/ops/jobs`"
    - from: ".planning/phases/13-native-only-optional-dependency-contract-proof/13-VALIDATION.md"
      to: "13-01-PLAN.md and 13-02-PLAN.md"
      via: "per-task verification map and wave ownership"
      pattern: "13-01-01|13-01-02|13-02-01|13-02-02"
---

<objective>
Lock the Phase 13 support story into automated docs-contract tests and workflow lane names.

Purpose: Finish DOC-03 by making CI and docs drift checks fail whenever native-first wording or optional-bridge naming diverges from the actual proof stack.
Output: Updated docs-contract assertions, a host-contract workflow with native-first lane names that still preserve the separate fresh-host proof, and a refreshed validation contract that maps every Phase 13 task.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/13-native-only-optional-dependency-contract-proof/13-CONTEXT.md
@.planning/phases/13-native-only-optional-dependency-contract-proof/13-RESEARCH.md
@.planning/phases/13-native-only-optional-dependency-contract-proof/13-VALIDATION.md
@.planning/phases/12-fresh-host-install-path-example-fixture-repair/12-04-SUMMARY.md
@test/oban_powertools/docs_contract_test.exs
@.github/workflows/host-contract-proof.yml

<interfaces>
From `test/oban_powertools/docs_contract_test.exs`:
```elixir
@docs_files [...]
@workflow_file ".github/workflows/host-contract-proof.yml"
test "support truth stays locked in docs" do
  source = joined_docs()
end
```

Current workflow jobs:
```yaml
structural:
fresh-host:
docs-contract:
native-only:
first-session:
bridge-enabled:
upgrade-proof:
```

Current validation coverage gaps:
```text
13-VALIDATION.md currently maps only one task per plan and leaves nyquist_compliant false.
Plan 13-03 must refresh that file so all six planned tasks, both waves, and the final lane names are captured.
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tighten docs-contract markers around the native-first support story</name>
  <files>test/oban_powertools/docs_contract_test.exs</files>
  <read_first>.planning/phases/13-native-only-optional-dependency-contract-proof/13-CONTEXT.md, .planning/phases/13-native-only-optional-dependency-contract-proof/13-RESEARCH.md, test/oban_powertools/docs_contract_test.exs, README.md, guides/installation.md, guides/first-operator-session.md, guides/optional-oban-web-bridge.md, guides/upgrade-and-compatibility.md</read_first>
  <action>Update `test/oban_powertools/docs_contract_test.exs` so the docs assertions enforce the exact native-first language added in Plan 13-02. Add exact-string assertions for `Oban Powertools ships a native, host-owned operator shell at \`/ops/jobs\`.`, ``oban_web` is optional; when installed, Powertools mounts a nested read-only Oban Web bridge at `/ops/jobs/oban` for additional inspection.`, `Native Powertools pages are the supported mutation surface.`, and `The host owns router scope, browser pipeline, auth, display policy, and runtime config.`. Keep the existing first-session markers for `ops-demo`, `nightly_sync`, and `pause_cron_entry`, and keep the workflow assertion in this file as the authority for lane names. Do not weaken the test into partial-substring approximations; assert the full public sentences so support-truth drift fails loudly.</action>
  <acceptance_criteria>
    - `test/oban_powertools/docs_contract_test.exs` contains `Oban Powertools ships a native, host-owned operator shell at \`/ops/jobs\`.`.
    - `test/oban_powertools/docs_contract_test.exs` contains ``oban_web` is optional; when installed, Powertools mounts a nested read-only Oban Web bridge at `/ops/jobs/oban` for additional inspection.`.
    - `test/oban_powertools/docs_contract_test.exs` contains `Native Powertools pages are the supported mutation surface.`.
    - `test/oban_powertools/docs_contract_test.exs` contains `The host owns router scope, browser pipeline, auth, display policy, and runtime config.`.
    - `test/oban_powertools/docs_contract_test.exs` still contains `ops-demo`, `nightly_sync`, and `pause_cron_entry`.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/oban_powertools/docs_contract_test.exs</automated>
  </verify>
  <done>The docs-contract test now fails on any drift away from the Phase 13 native-first public wording.</done>
</task>

<task type="auto">
  <name>Task 2: Rename workflow jobs and refresh the Phase 13 validation contract</name>
  <files>.github/workflows/host-contract-proof.yml, test/oban_powertools/docs_contract_test.exs, .planning/phases/13-native-only-optional-dependency-contract-proof/13-VALIDATION.md</files>
  <read_first>.planning/phases/13-native-only-optional-dependency-contract-proof/13-CONTEXT.md, .planning/phases/13-native-only-optional-dependency-contract-proof/13-RESEARCH.md, .planning/phases/13-native-only-optional-dependency-contract-proof/13-VALIDATION.md, .github/workflows/host-contract-proof.yml, test/oban_powertools/docs_contract_test.exs</read_first>
  <action>Rename the GitHub Actions jobs in `.github/workflows/host-contract-proof.yml` from `native-only` to `native-first` and from `bridge-enabled` to `optional-bridge`, while preserving the existing `fresh-host`, `docs-contract`, `first-session`, `upgrade-proof`, and `structural` jobs exactly as separate proof lanes per D-08. Keep the underlying ExUnit commands unchanged: `native-first` should still run `mix test test/oban_powertools/example_host_contract_test.exs --only native-only`, and `optional-bridge` should still run `mix test test/oban_powertools/example_host_contract_test.exs --only bridge-enabled`. Update the workflow assertions in `test/oban_powertools/docs_contract_test.exs` to look for `native-first:` and `optional-bridge:` instead of the old job names, while still asserting `fresh-host:` remains present and `--only first_session` remains the dedicated first-session command. In the same task, refresh `.planning/phases/13-native-only-optional-dependency-contract-proof/13-VALIDATION.md` so it references all six Phase 13 tasks with correct plan ownership, wave numbers, requirements, and threat refs; flip `wave_0_complete` to `true` once the bridge smoke file, native-only rewrite, docs markers, and renamed workflow jobs all exist; and keep `nyquist_compliant: true` in the frontmatter so the validation contract matches the executed phase state.</action>
  <acceptance_criteria>
    - `.github/workflows/host-contract-proof.yml` contains `native-first:`.
    - `.github/workflows/host-contract-proof.yml` contains `optional-bridge:`.
    - `.github/workflows/host-contract-proof.yml` still contains `fresh-host:`.
    - `.github/workflows/host-contract-proof.yml` still contains `--only native-only` and `--only bridge-enabled`.
    - `test/oban_powertools/docs_contract_test.exs` asserts `native-first:` and `optional-bridge:`.
    - `.planning/phases/13-native-only-optional-dependency-contract-proof/13-VALIDATION.md` contains rows for `13-01-01`, `13-01-02`, `13-02-01`, `13-02-02`, `13-03-01`, and `13-03-02`.
    - `.planning/phases/13-native-only-optional-dependency-contract-proof/13-VALIDATION.md` contains `wave_0_complete: true` after execution of this task.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/oban_powertools/docs_contract_test.exs test/oban_powertools/web/router_test.exs && rg -n "native-first:|optional-bridge:|fresh-host:|13-01-01|13-01-02|13-02-01|13-02-02|13-03-01|13-03-02|wave_0_complete: true|nyquist_compliant: true" .github/workflows/host-contract-proof.yml .planning/phases/13-native-only-optional-dependency-contract-proof/13-VALIDATION.md</automated>
  </verify>
  <done>The workflow job names, docs-contract assertions, and Phase 13 validation contract now encode the native-first support story without collapsing the separate fresh-host lane or leaving stale Nyquist metadata behind.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docs-contract assertions -> public docs | Weak drift checks let inaccurate support claims ship silently |
| workflow names -> maintainer expectations | CI labels are part of the public proof contract and can mislead if they stay lane-symmetric |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-06 | R | `test/oban_powertools/docs_contract_test.exs` | mitigate | Assert the full native-first public sentences and the exact workflow lane names so docs and CI cannot drift independently. |
| T-13-07 | T | `.github/workflows/host-contract-proof.yml` | mitigate | Rename only the public job labels, preserve the underlying proof commands, and keep `fresh-host` as a distinct installer lane. |
</threat_model>

<verification>
- `mix test test/oban_powertools/docs_contract_test.exs`
- `mix test test/oban_powertools/docs_contract_test.exs test/oban_powertools/web/router_test.exs`
</verification>

<success_criteria>
- The docs-contract test encodes the exact native-first wording introduced in Phase 13 docs.
- Workflow job names present the proof stack as `native-first` plus `optional-bridge` without removing `fresh-host`.
- CI labels, docs assertions, and the actual ExUnit lane commands stay aligned.
</success_criteria>

<output>
After completion, create `.planning/phases/13-native-only-optional-dependency-contract-proof/13-03-SUMMARY.md`
</output>
