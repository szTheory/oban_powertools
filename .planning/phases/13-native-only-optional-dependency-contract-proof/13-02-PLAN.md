---
phase: 13-native-only-optional-dependency-contract-proof
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - guides/installation.md
  - guides/first-operator-session.md
  - guides/optional-oban-web-bridge.md
  - guides/upgrade-and-compatibility.md
autonomous: true
requirements:
  - PKG-03
  - DOC-03
must_haves:
  truths:
    - "Public docs lead with the native `/ops/jobs` shell as the default paved road."
    - "Docs describe `oban_web` as optional and `/ops/jobs/oban` as a nested read-only inspection annex."
    - "Compatibility guidance distinguishes the native-first lane from the optional bridge lane without implying equal surface area."
  artifacts:
    - path: "README.md"
      provides: "Top-level native-first install and support-truth summary"
    - path: "guides/installation.md"
      provides: "Explicit day-0 install path with optional bridge wording"
    - path: "guides/first-operator-session.md"
      provides: "Native mutation-first operator walkthrough"
    - path: "guides/optional-oban-web-bridge.md"
      provides: "Bounded bridge posture and caveats"
    - path: "guides/upgrade-and-compatibility.md"
      provides: "Named tested lanes and compatibility promise"
  key_links:
    - from: "README.md"
      to: "guides/installation.md"
      via: "shared D-17 native-first wording"
      pattern: "native, host-owned operator shell at `/ops/jobs`"
    - from: "guides/first-operator-session.md"
      to: "guides/optional-oban-web-bridge.md"
      via: "native mutation surface vs optional inspection annex"
      pattern: "read-only"
    - from: "guides/upgrade-and-compatibility.md"
      to: "examples/phoenix_host"
      via: "canonical proof-host reference"
      pattern: "examples/phoenix_host"
---

<objective>
Rewrite the public support story so it is explicitly native-first while still documenting the optional bridge lane honestly.

Purpose: Close the DOC-03 support-truth gap by making README and guides say the same thing the proof lanes actually verify, per D-14 through D-17.
Output: Native-first wording across README, install guidance, first-session guidance, bridge posture, and compatibility language.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/13-native-only-optional-dependency-contract-proof/13-CONTEXT.md
@.planning/phases/13-native-only-optional-dependency-contract-proof/13-RESEARCH.md
@.planning/phases/13-native-only-optional-dependency-contract-proof/13-PATTERNS.md
@.planning/phases/12-fresh-host-install-path-example-fixture-repair/12-04-SUMMARY.md
@README.md
@guides/installation.md
@guides/first-operator-session.md
@guides/optional-oban-web-bridge.md
@guides/upgrade-and-compatibility.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reframe the public install and first-session docs around the native shell</name>
  <files>README.md, guides/installation.md, guides/first-operator-session.md</files>
  <read_first>.planning/phases/13-native-only-optional-dependency-contract-proof/13-CONTEXT.md, .planning/phases/13-native-only-optional-dependency-contract-proof/13-RESEARCH.md, README.md, guides/installation.md, guides/first-operator-session.md</read_first>
  <action>Apply the D-17 wording directly in `README.md`, `guides/installation.md`, and `guides/first-operator-session.md`. Use the exact sentence `Oban Powertools ships a native, host-owned operator shell at \`/ops/jobs\`.` near the top of the README and installation narrative. Keep `oban_web` documented as optional, but rewrite the intro and support-truth sections so `/ops/jobs` is the default mental model and `Native Powertools pages are the supported mutation surface.` appears verbatim. Add `The host owns router scope, browser pipeline, auth, display policy, and runtime config.` verbatim in the support-truth text. In `guides/first-operator-session.md`, make the bridge step subordinate to the native audited mutation flow by keeping the canonical proof actor/resource/action (`ops-demo`, `nightly_sync`, `pause_cron_entry`) as the success threshold and explicitly describing `/ops/jobs/oban` as an additional read-only inspection stop only when the dependency is installed, per D-14 through D-17.</action>
  <acceptance_criteria>
    - `README.md` contains `Oban Powertools ships a native, host-owned operator shell at \`/ops/jobs\`.`.
    - `README.md` contains `Native Powertools pages are the supported mutation surface.`.
    - `README.md` contains `The host owns router scope, browser pipeline, auth, display policy, and runtime config.`.
    - `guides/installation.md` contains `\`oban_web\` is optional`.
    - `guides/first-operator-session.md` still contains `ops-demo`, `nightly_sync`, and `pause_cron_entry`.
  </acceptance_criteria>
  <verify>
    <automated>rg -n "native, host-owned operator shell at `/ops/jobs`|Native Powertools pages are the supported mutation surface\\.|The host owns router scope, browser pipeline, auth, display policy, and runtime config\\.|`oban_web` is optional|ops-demo|nightly_sync|pause_cron_entry" README.md guides/installation.md guides/first-operator-session.md</automated>
  </verify>
  <done>The public install and first-session docs lead with the native shell and keep the optional bridge strictly secondary.</done>
</task>

<task type="auto">
  <name>Task 2: Narrow the bridge and compatibility guides to a native-first support promise</name>
  <files>guides/optional-oban-web-bridge.md, guides/upgrade-and-compatibility.md</files>
  <read_first>.planning/phases/13-native-only-optional-dependency-contract-proof/13-CONTEXT.md, .planning/phases/13-native-only-optional-dependency-contract-proof/13-RESEARCH.md, guides/optional-oban-web-bridge.md, guides/upgrade-and-compatibility.md</read_first>
  <action>Rewrite `guides/optional-oban-web-bridge.md` so it explicitly describes `/ops/jobs/oban` as an additive read-only inspection annex rather than a co-equal surface, per D-15 and D-16. Keep the existing caveats, but add wording that the bridge reuses the host-owned actor and display seams while native Powertools pages remain the mutation surface. In `guides/upgrade-and-compatibility.md`, rename the compatibility table rows to `tested native-first lane` and `tested optional bridge lane`, keep `best-effort` for everything else, and describe the bridge lane as native pages plus the optional read-only `/ops/jobs/oban` bridge. Preserve the `examples/phoenix_host` canonical-proof reference and keep the guide scoped to supported-host reality rather than promising broad matrix coverage.</action>
  <acceptance_criteria>
    - `guides/optional-oban-web-bridge.md` contains `additive read-only inspection annex`.
    - `guides/optional-oban-web-bridge.md` contains `Native Powertools pages own audited mutations.`.
    - `guides/upgrade-and-compatibility.md` contains `tested native-first lane`.
    - `guides/upgrade-and-compatibility.md` contains `tested optional bridge lane`.
    - `guides/upgrade-and-compatibility.md` contains `examples/phoenix_host`.
  </acceptance_criteria>
  <verify>
    <automated>rg -n "additive read-only inspection annex|Native Powertools pages own audited mutations\\.|tested native-first lane|tested optional bridge lane|examples/phoenix_host" guides/optional-oban-web-bridge.md guides/upgrade-and-compatibility.md</automated>
  </verify>
  <done>The bridge and compatibility guides now acknowledge two tested lanes without presenting the optional bridge as a co-equal operator surface.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public docs -> maintainer expectations | Wording drift can overstate supported behavior beyond what CI and tests actually prove |
| compatibility guide -> upgrade decisions | Symmetric lane language can mislead maintainers into treating the bridge as a co-equal mutation surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-04 | R | README and guides | mitigate | Use the exact D-17 native-first wording and keep the bridge described as optional, nested, and read-only. |
| T-13-05 | I | `guides/upgrade-and-compatibility.md` | mitigate | Narrow the compatibility table to `tested native-first lane`, `tested optional bridge lane`, and `best-effort` so support truth is explicit. |
</threat_model>

<verification>
- `rg -n "native, host-owned operator shell at `/ops/jobs`|Native Powertools pages are the supported mutation surface\\.|The host owns router scope, browser pipeline, auth, display policy, and runtime config\\.|additive read-only inspection annex|tested native-first lane|tested optional bridge lane" README.md guides/installation.md guides/first-operator-session.md guides/optional-oban-web-bridge.md guides/upgrade-and-compatibility.md`
</verification>

<success_criteria>
- README and the day-0/day-2 guides all describe `/ops/jobs` as the default paved road.
- The optional bridge is consistently described as read-only, additive, and narrower than the native surface.
- The compatibility guide names the tested lanes without implying parity between them.
</success_criteria>

<output>
After completion, create `.planning/phases/13-native-only-optional-dependency-contract-proof/13-02-SUMMARY.md`
</output>
