---
phase: 11
plan: 04
type: execute
wave: 3
depends_on: ["11-01", "11-02", "11-03"]
files_modified: ["test/oban_powertools/docs_contract_test.exs", "test/oban_powertools/example_host_contract_test.exs", "test/support/example_host_contract.ex", ".github/workflows/host-contract-proof.yml"]
autonomous: true
requirements: ["DOC-03", "PKG-02", "DOC-01", "HST-03"]
must_haves:
  truths:
    - "Docs drift is caught automatically for the canonical install snippets and support-truth statements."
    - "The public host contract is proven in native-only, bridge-enabled, and documented upgrade lanes."
    - "The Phase 11 upgrade lane specifically proves the explicit display_policy step rather than assuming it."
  artifacts:
    - path: "test/oban_powertools/docs_contract_test.exs"
      provides: "Docs contract assertions for canonical snippets and support-truth markers"
      contains: "display_policy"
    - path: "test/support/example_host_contract.ex"
      provides: "Reusable helper for fixture-host proof commands"
      contains: "examples/phoenix_host"
    - path: "test/oban_powertools/example_host_contract_test.exs"
      provides: "Native-only, bridge-enabled, and upgrade proof tests"
      contains: "upgrade"
    - path: ".github/workflows/host-contract-proof.yml"
      provides: "CI lanes for the layered proof stack"
      contains: "bridge-enabled"
  key_links:
    - from: "test/oban_powertools/docs_contract_test.exs"
      to: "README.md and guides/"
      via: "exact snippet and support-truth assertions"
      pattern: "display_policy"
    - from: "test/oban_powertools/example_host_contract_test.exs"
      to: "examples/phoenix_host"
      via: "native-only and bridge-enabled smoke proof"
      pattern: "examples/phoenix_host"
    - from: "upgrade proof lane"
      to: "guides/upgrade-and-compatibility.md"
      via: "documented source lane requiring explicit display_policy wiring"
      pattern: "Phase 8"
---

<objective>
Finish Phase 11 by turning the docs and example-host contract into continuously verified proof: docs contract tests, fixture-host smoke tests, and CI lanes for native-only, bridge-enabled, and upgrade flows.

Purpose: Make the published host contract trustworthy by proving the same day-0/day-2 path that the docs describe.
Output: Docs contract tests, reusable example-host proof helpers, fixture-host smoke tests, and a dedicated contract-proof workflow.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-docs-example-app-compatibility-contract-proof/11-CONTEXT.md
@.planning/phases/11-docs-example-app-compatibility-contract-proof/11-RESEARCH.md
@.planning/phases/11-docs-example-app-compatibility-contract-proof/11-PATTERNS.md
@.planning/phases/11-docs-example-app-compatibility-contract-proof/11-VALIDATION.md
@README.md
@guides/installation.md
@guides/first-operator-session.md
@guides/example-app-walkthrough.md
@guides/upgrade-and-compatibility.md
@guides/optional-oban-web-bridge.md
@guides/support-truth-and-ownership-boundaries.md
@examples/phoenix_host/README.md
@examples/phoenix_host/mix.exs
@test/mix/tasks/oban_powertools.install_test.exs
@test/oban_powertools/web/router_test.exs
</context>

<tasks>

<task type="execute" tdd="true">
  <name>Task 1: Add docs contract tests for canonical snippets and support-truth markers</name>
  <files>test/oban_powertools/docs_contract_test.exs</files>
  <read_first>
    - README.md
    - guides/installation.md
    - guides/first-operator-session.md
    - guides/example-app-walkthrough.md
    - guides/upgrade-and-compatibility.md
    - guides/optional-oban-web-bridge.md
    - guides/support-truth-and-ownership-boundaries.md
    - test/mix/tasks/oban_powertools.install_test.exs
    - test/oban_powertools/web/router_test.exs
    - .planning/phases/11-docs-example-app-compatibility-contract-proof/11-PATTERNS.md
  </read_first>
  <action>
    Create `test/oban_powertools/docs_contract_test.exs` using the same cheap `File.read!/1` contract-test style as the installer and router proof per D-23, D-24, and D-27.
    Assert the canonical install/router/config snippets and support-truth markers across `README.md` and `guides/`: `display_policy`, `auth_module`, `/ops/jobs`, `/ops/jobs/oban`, `read-only`, `native Powertools pages own audited mutations`, `host owns router scope`, and the example-host path.
    Keep the test narrow and grep-like. It should fail when docs drift, not when copy changes cosmetically outside the locked contract markers.
  </action>
  <acceptance_criteria>
    - `test/oban_powertools/docs_contract_test.exs` contains `File.read!`
    - `test/oban_powertools/docs_contract_test.exs` contains `display_policy`
    - `test/oban_powertools/docs_contract_test.exs` contains `/ops/jobs/oban`
    - `test/oban_powertools/docs_contract_test.exs` contains `native Powertools pages own audited mutations`
    - `mix test test/oban_powertools/docs_contract_test.exs` exits 0
  </acceptance_criteria>
  <verify>
    <automated>mix test test/oban_powertools/docs_contract_test.exs</automated>
  </verify>
  <done>Canonical docs snippets and support-truth markers are enforced by automated contract tests.</done>
</task>

<task type="execute" tdd="true">
  <name>Task 2: Add fixture-host proof helpers, native/bridge lanes, and the upgrade-proof workflow</name>
  <files>test/support/example_host_contract.ex, test/oban_powertools/example_host_contract_test.exs, .github/workflows/host-contract-proof.yml</files>
  <read_first>
    - examples/phoenix_host/mix.exs
    - examples/phoenix_host/config/config.exs
    - examples/phoenix_host/lib/phoenix_host_web/router.ex
    - examples/phoenix_host/lib/phoenix_host_web/oban_powertools_auth.ex
    - examples/phoenix_host/lib/phoenix_host_web/oban_powertools_display_policy.ex
    - examples/phoenix_host/priv/repo/seeds.exs
    - guides/upgrade-and-compatibility.md
    - test/mix/tasks/oban_powertools.install_test.exs
    - test/oban_powertools/web/router_test.exs
    - .planning/phases/11-docs-example-app-compatibility-contract-proof/11-CONTEXT.md
    - .planning/phases/11-docs-example-app-compatibility-contract-proof/11-RESEARCH.md
  </read_first>
  <action>
    Implement the layered proof stack per D-24 through D-30.
    Add `test/support/example_host_contract.ex` as the helper that shells into `examples/phoenix_host` for native-only and bridge-enabled smoke proof and also creates a temporary upgrade-source host representing the shipped Phase 8 through Phase 10 installer contract: `repo` plus `auth_module` already present, `display_policy` still missing. The helper must apply the documented Phase 11 upgrade steps, then compile, migrate, and seed successfully.
    Add `test/oban_powertools/example_host_contract_test.exs` with three focused proofs: native-only lane, bridge-enabled lane, and the documented upgrade lane that proves the explicit `display_policy` addition rather than assuming it. Standardize every new proof reference, workflow command, and helper naming on `example_host_contract_test.exs`; do not introduce or preserve any `example_app_contract_test.exs` alias.
    Add `.github/workflows/host-contract-proof.yml` so CI runs the existing structural tests plus the new docs contract test and the three fixture-host lanes. Keep the lane names explicit: `structural`, `docs-contract`, `native-only`, `bridge-enabled`, and `upgrade-proof`, and point the workflow's automated command at `test/oban_powertools/example_host_contract_test.exs`.
  </action>
  <acceptance_criteria>
    - `test/support/example_host_contract.ex` contains `examples/phoenix_host`
    - `test/oban_powertools/example_host_contract_test.exs` contains `native-only`
    - `test/oban_powertools/example_host_contract_test.exs` contains `bridge-enabled`
    - `test/oban_powertools/example_host_contract_test.exs` contains `upgrade`
    - `test/oban_powertools/example_host_contract_test.exs` does not contain `example_app_contract_test`
    - `.github/workflows/host-contract-proof.yml` contains `structural`
    - `.github/workflows/host-contract-proof.yml` contains `docs-contract`
    - `.github/workflows/host-contract-proof.yml` contains `upgrade-proof`
  </acceptance_criteria>
  <verify>
    <automated>mix test test/oban_powertools/example_host_contract_test.exs && rg -n "structural|docs-contract|native-only|bridge-enabled|upgrade-proof|example_host_contract_test\\.exs" .github/workflows/host-contract-proof.yml && ! rg -n "example_app_contract_test\\.exs" test/oban_powertools/example_host_contract_test.exs .github/workflows/host-contract-proof.yml</automated>
  </verify>
  <done>The repo continuously proves docs drift, native-only support, bridge-enabled support, and the one documented upgrade lane that adds `display_policy` explicitly, all under the standardized `example_host_contract_test.exs` proof stack.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Docs contract tests -> public docs | Verification must catch contract drift without pretending unverified prose is guaranteed. |
| Fixture-host proof -> support promise | CI lanes must prove only the advertised native-only, bridge-enabled, and single upgrade source lanes. |
| Upgrade-source host -> Phase 11 docs | The scripted upgrade path must model the real pre-Phase-11 gap around missing `display_policy`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-10 | Tampering | docs contract proof | mitigate | Add `File.read!/1` contract tests for locked snippets and support-truth markers. |
| T-11-11 | Elevation of Privilege | bridge-enabled proof lane | mitigate | Prove bridge-enabled support as a distinct read-only lane instead of inheriting native mutation guarantees. |
| T-11-12 | Repudiation | upgrade-proof integrity | mitigate | Model the actual legacy source lane missing `display_policy` and require the documented fix-up steps in automated proof. |
</threat_model>

<verification>
mix test test/mix/tasks/oban_powertools.install_test.exs test/oban_powertools/web/router_test.exs test/oban_powertools/docs_contract_test.exs test/oban_powertools/example_host_contract_test.exs
rg -n "docs-contract|native-only|bridge-enabled|upgrade-proof" .github/workflows/host-contract-proof.yml
</verification>

<success_criteria>
The repo proves the published host contract in layers: fast structural tests, docs contract tests, one canonical example host, native-only support, optional bridge support, and the single documented upgrade lane that fixes the `display_policy` gap explicitly.
</success_criteria>

<output>
After completion, create `.planning/phases/11-docs-example-app-compatibility-contract-proof/11-04-SUMMARY.md`
</output>
