---
phase: 11
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: ["README.md", "mix.exs", "guides/installation.md", "guides/first-operator-session.md", "guides/example-app-walkthrough.md"]
autonomous: true
requirements: ["DOC-01", "HST-03"]
must_haves:
  truths:
    - "A new host can follow one short entry path from README into versioned guides without guessing hidden setup."
    - "The day-0 contract states plainly that the host must provide auth, display policy, router scope, migrations, and seedable operator data."
    - "The public docs point readers to the canonical example host instead of a hand-waved or divergent demo path."
  artifacts:
    - path: "README.md"
      provides: "Concise public entrypoint with support-truth summary and guide links"
      contains: "display_policy"
    - path: "mix.exs"
      provides: "ExDoc extras/groups configuration for the Phase 11 guide set"
      contains: "groups_for_extras"
    - path: "guides/installation.md"
      provides: "Exact install path including the explicit display_policy step"
      contains: "ObanPowertoolsDisplayPolicy"
    - path: "guides/first-operator-session.md"
      provides: "First successful operator session walkthrough"
      contains: "/ops/jobs"
    - path: "guides/example-app-walkthrough.md"
      provides: "Public pointer to the canonical generated host fixture"
      contains: "examples/phoenix_host"
  key_links:
    - from: "README.md"
      to: "guides/installation.md"
      via: "60-second install and guide link"
      pattern: "mix oban_powertools.install"
    - from: "guides/installation.md"
      to: "guides/first-operator-session.md"
      via: "day-0 flow including display_policy and router mount"
      pattern: "display_policy"
    - from: "guides/example-app-walkthrough.md"
      to: "examples/phoenix_host"
      via: "canonical fixture path"
      pattern: "examples/phoenix_host"
---

<objective>
Publish the Phase 11 day-0 documentation entrypoint: a short README, ExDoc-backed guide structure, and exact first-success steps that make the host-owned contract honest on day 0.

Purpose: Close the current adoption ambiguity around first install, especially the missing `display_policy` step, without widening support promises.
Output: A concise README, ExDoc guide wiring, and three day-0 guides that point directly at the canonical example host.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/MILESTONE-ARC.md
@.planning/phases/11-docs-example-app-compatibility-contract-proof/11-CONTEXT.md
@.planning/phases/11-docs-example-app-compatibility-contract-proof/11-RESEARCH.md
@.planning/phases/11-docs-example-app-compatibility-contract-proof/11-PATTERNS.md
@.planning/phases/11-docs-example-app-compatibility-contract-proof/11-VALIDATION.md
@README.md
@mix.exs
@lib/mix/tasks/oban_powertools.install.ex
@lib/oban_powertools/runtime_config.ex
</context>

<tasks>

<task type="execute">
  <name>Task 1: Add the canonical docs surface and shrink README to the honest entry contract</name>
  <files>README.md, mix.exs</files>
  <read_first>
    - README.md
    - mix.exs
    - .planning/phases/11-docs-example-app-compatibility-contract-proof/11-CONTEXT.md
    - .planning/phases/11-docs-example-app-compatibility-contract-proof/11-RESEARCH.md
    - .planning/phases/11-docs-example-app-compatibility-contract-proof/11-PATTERNS.md
    - lib/mix/tasks/oban_powertools.install.ex
    - lib/oban_powertools/runtime_config.ex
  </read_first>
  <action>
    Update `mix.exs` to add ExDoc and a `docs:` configuration that treats `README.md` as the entry page and groups the full Phase 11 guide set into a day-0/day-2 structure per D-10 through D-13. Make the guide wiring Wave 1-safe: use a stable wildcard or equivalent pattern such as `Path.wildcard("guides/*.md")` so `mix docs` succeeds before the Wave 2 guide files exist, while still guaranteeing that later Phase 11 guides are published automatically once created.
    Rewrite `README.md` into the Phase 11 short form per D-11, D-16, D-31 through D-35, and D-36 through D-39: positioning, one dependency snippet, `mix oban_powertools.install`, one router example, one optional `oban_web` note, explicit support-truth, and links to the guides plus `examples/phoenix_host`.
    Make the day-0 contract honest: do not imply the installer scaffolds `display_policy`; say plainly that the host must add `display_policy: MyAppWeb.ObanPowertoolsDisplayPolicy` before policy-sensitive native pages are mounted.
  </action>
  <acceptance_criteria>
    - `mix.exs` contains `{:ex_doc,`
    - `mix.exs` contains `groups_for_extras`
    - `mix.exs` contains `Path.wildcard("guides/*.md")` or an equivalent wildcard-based extras strategy
    - `README.md` contains `display_policy: MyAppWeb.ObanPowertoolsDisplayPolicy`
    - `README.md` contains `examples/phoenix_host`
    - `README.md` contains `read-only`
    - `README.md` contains `native Powertools pages own audited mutations`
  </acceptance_criteria>
  <verify>
    <automated>mix docs && rg -n "groups_for_extras|Path\\.wildcard\\(\"guides/\\*\\.md\"\\)|display_policy: MyAppWeb.ObanPowertoolsDisplayPolicy|examples/phoenix_host|read-only|native Powertools pages own audited mutations" mix.exs README.md</automated>
  </verify>
  <done>The public entry surface is short, guide-backed, and explicit about the real host-owned day-0 contract.</done>
</task>

<task type="execute">
  <name>Task 2: Write the exact day-0 guides and close the display_policy gap in docs</name>
  <files>guides/installation.md, guides/first-operator-session.md, guides/example-app-walkthrough.md</files>
  <read_first>
    - README.md
    - lib/mix/tasks/oban_powertools.install.ex
    - lib/oban_powertools/runtime_config.ex
    - .planning/phases/11-docs-example-app-compatibility-contract-proof/11-CONTEXT.md
    - .planning/phases/11-docs-example-app-compatibility-contract-proof/11-RESEARCH.md
    - .planning/phases/11-docs-example-app-compatibility-contract-proof/11-PATTERNS.md
  </read_first>
  <action>
    Create `guides/installation.md`, `guides/first-operator-session.md`, and `guides/example-app-walkthrough.md` per D-07 through D-09, D-13, D-15, D-16, D-33, D-35, and D-38.
    `guides/installation.md` must show the exact post-installer host step for `display_policy`, the host-owned `auth_module`, the `/ops/jobs` router scope, migrations, and the fact that `oban_web` is optional.
    `guides/first-operator-session.md` must walk a reader from dependency install through `/ops/jobs`, seeded actor access, at least one native audited mutation, and the read-only `/ops/jobs/oban` bridge note.
    `guides/example-app-walkthrough.md` must point to `examples/phoenix_host`, explain what the fixture proves, and state that it is generated from `mix phx.new` plus `mix oban_powertools.install` rather than hand-built.
  </action>
  <acceptance_criteria>
    - `guides/installation.md` contains `display_policy: MyAppWeb.ObanPowertoolsDisplayPolicy`
    - `guides/installation.md` contains `ObanPowertools.Web.Router.oban_powertools_routes("/oban")`
    - `guides/first-operator-session.md` contains `/ops/jobs/oban`
    - `guides/first-operator-session.md` contains `audited mutation`
    - `guides/example-app-walkthrough.md` contains `mix phx.new`
    - `guides/example-app-walkthrough.md` contains `mix oban_powertools.install`
  </acceptance_criteria>
  <verify>
    <automated>mix docs && rg -n "display_policy: MyAppWeb.ObanPowertoolsDisplayPolicy|oban_powertools_routes\\(\"/oban\"\\)|/ops/jobs/oban|audited mutation|mix phx.new|mix oban_powertools.install" guides/installation.md guides/first-operator-session.md guides/example-app-walkthrough.md</automated>
  </verify>
  <done>The day-0 guides teach the real host path, including the explicit `display_policy` step and the canonical example-host walkthrough.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| README / guides -> adopters | Public docs must not overstate what the installer or library configures automatically. |
| Day-0 install path -> policy-sensitive pages | Missing `display_policy` or auth wiring must be called out before readers reach native operator pages. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01 | Tampering | README and day-0 guides | mitigate | Lock the day-0 contract to exact strings for `display_policy`, router mount, and native-vs-bridge support truth. |
| T-11-02 | Information Disclosure | first-session docs | mitigate | State explicitly that display/redaction policy is host-owned and required before policy-sensitive pages are mounted. |
| T-11-03 | Repudiation | support-truth messaging | mitigate | Repeat the native audited mutation ownership statement across README and guides so support boundaries cannot drift silently. |
</threat_model>

<verification>
mix docs
rg -n "display_policy|examples/phoenix_host|read-only|audited mutations|mix phx.new|mix oban_powertools.install" README.md guides/
</verification>

<success_criteria>
The published day-0 docs are concise, guide-backed, and honest about the real host-owned steps needed to reach a first operator session.
</success_criteria>

<output>
After completion, create `.planning/phases/11-docs-example-app-compatibility-contract-proof/11-01-SUMMARY.md`
</output>
