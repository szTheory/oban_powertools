---
phase: 11
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: ["examples/phoenix_host/mix.exs", "examples/phoenix_host/config/config.exs", "examples/phoenix_host/config/runtime.exs", "examples/phoenix_host/lib/phoenix_host/application.ex", "examples/phoenix_host/lib/phoenix_host_web/router.ex", "examples/phoenix_host/lib/phoenix_host_web/oban_powertools_auth.ex", "examples/phoenix_host/lib/phoenix_host_web/oban_powertools_display_policy.ex", "examples/phoenix_host/priv/repo/seeds.exs", "examples/phoenix_host/README.md", "examples/phoenix_host/regenerate.sh"]
autonomous: true
requirements: ["DOC-01", "HST-03"]
must_haves:
  truths:
    - "The canonical example host proves the real paved road rather than a hand-built demo."
    - "The example host includes real auth, display policy, router scope, and seeded operator data."
    - "The same example path can be exercised in native-only and bridge-enabled modes without implying broader bridge parity."
    - "Maintainers can reproduce the fixture from `mix phx.new` plus `mix oban_powertools.install` instead of trusting undocumented drift."
  artifacts:
    - path: "examples/phoenix_host/mix.exs"
      provides: "Canonical generated host dependency contract"
      contains: "oban_powertools"
    - path: "examples/phoenix_host/lib/phoenix_host_web/router.ex"
      provides: "Host-owned `/ops/jobs` scope around Powertools routes"
      contains: "oban_powertools_routes(\"/oban\")"
    - path: "examples/phoenix_host/lib/phoenix_host_web/oban_powertools_auth.ex"
      provides: "Real host auth seam implementation"
      contains: "@behaviour ObanPowertools.Auth"
    - path: "examples/phoenix_host/lib/phoenix_host_web/oban_powertools_display_policy.ex"
      provides: "Real host display-policy seam implementation"
      contains: "def display("
    - path: "examples/phoenix_host/priv/repo/seeds.exs"
      provides: "Seeded operator data for first-session docs and proof"
      contains: "ops"
    - path: "examples/phoenix_host/regenerate.sh"
      provides: "Repeatable provenance script for rebuilding the canonical host from generator plus installer"
      contains: "mix phx.new"
  key_links:
    - from: "examples/phoenix_host/config/config.exs"
      to: "examples/phoenix_host/lib/phoenix_host_web/oban_powertools_display_policy.ex"
      via: "display_policy config"
      pattern: "display_policy:"
    - from: "examples/phoenix_host/lib/phoenix_host_web/router.ex"
      to: "README / guides"
      via: "host-owned outer /ops/jobs shell"
      pattern: "scope \"/ops/jobs\""
    - from: "examples/phoenix_host/priv/repo/seeds.exs"
      to: "guides/first-operator-session.md"
      via: "repeatable first operator session"
      pattern: "seed"
    - from: "examples/phoenix_host/regenerate.sh"
      to: "examples/phoenix_host/README.md"
      via: "documented regeneration/provenance path"
      pattern: "mix oban_powertools.install"
---

<objective>
Create the one canonical example host that Phase 11 documents and tests: a thin generated Phoenix app that exercises the real host-owned seams and first-session data path.

Purpose: Give adopters and maintainers one concrete host shape that matches the paved-road install contract and closes the day-0 honesty gap around auth and display policy.
Output: `examples/phoenix_host` with real host wiring, policy modules, seeded data, a terse local README, and a regeneration script proving generator-backed provenance.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-docs-example-app-compatibility-contract-proof/11-CONTEXT.md
@.planning/phases/11-docs-example-app-compatibility-contract-proof/11-RESEARCH.md
@.planning/phases/11-docs-example-app-compatibility-contract-proof/11-PATTERNS.md
@.planning/phases/11-docs-example-app-compatibility-contract-proof/11-VALIDATION.md
@README.md
@lib/mix/tasks/oban_powertools.install.ex
@lib/oban_powertools/auth.ex
@lib/oban_powertools/runtime_config.ex
@lib/oban_powertools/web/router.ex
@test/support/test_auth.ex
@test/support/test_router.ex
</context>

<tasks>

<task type="execute">
  <name>Task 1: Commit the thin generated host shell with the real Powertools mount contract</name>
  <files>examples/phoenix_host/mix.exs, examples/phoenix_host/config/config.exs, examples/phoenix_host/config/runtime.exs, examples/phoenix_host/lib/phoenix_host/application.ex, examples/phoenix_host/lib/phoenix_host_web/router.ex</files>
  <read_first>
    - lib/mix/tasks/oban_powertools.install.ex
    - lib/oban_powertools/runtime_config.ex
    - lib/oban_powertools/web/router.ex
    - test/support/test_router.ex
    - .planning/phases/11-docs-example-app-compatibility-contract-proof/11-CONTEXT.md
    - .planning/phases/11-docs-example-app-compatibility-contract-proof/11-PATTERNS.md
  </read_first>
  <action>
    Create `examples/phoenix_host` from the real paved road per D-04 through D-07, D-09, D-25, and D-29: generated from `mix phx.new`, then wired exactly as a host app would wire Powertools.
    In `examples/phoenix_host/mix.exs`, keep `oban_powertools` explicit and make `oban_web` a documented optional lane toggle rather than a second example app; the native-only default must still compile and the bridge-enabled path must be activatable from the same host tree.
    In config and router files, keep the host-owned outer `/ops/jobs` scope, browser pipeline, runtime config, and optional bridge posture explicit. Do not hide router ownership or imply that the library owns the outer shell.
  </action>
  <acceptance_criteria>
    - `examples/phoenix_host/mix.exs` contains `{:oban_powertools,`
    - `examples/phoenix_host/mix.exs` contains `oban_web`
    - `examples/phoenix_host/config/config.exs` contains `auth_module: PhoenixHostWeb.ObanPowertoolsAuth`
    - `examples/phoenix_host/config/config.exs` contains `display_policy: PhoenixHostWeb.ObanPowertoolsDisplayPolicy`
    - `examples/phoenix_host/lib/phoenix_host_web/router.ex` contains `scope "/ops/jobs"`
    - `examples/phoenix_host/lib/phoenix_host_web/router.ex` contains `ObanPowertools.Web.Router.oban_powertools_routes("/oban")`
  </acceptance_criteria>
  <verify>
    <automated>cd examples/phoenix_host && mix deps.get && mix compile && rg -n "auth_module: PhoenixHostWeb.ObanPowertoolsAuth|display_policy: PhoenixHostWeb.ObanPowertoolsDisplayPolicy|scope \"/ops/jobs\"|oban_powertools_routes\\(\"/oban\"\\)|oban_web" mix.exs config/config.exs lib/phoenix_host_web/router.ex</automated>
  </verify>
  <done>The committed example host matches the real host mount/config shape and supports one canonical native-first path with an optional bridge lane.</done>
</task>

<task type="execute">
  <name>Task 2: Add real host auth, display policy, seeded operator data, and provenance docs for the fixture</name>
  <files>examples/phoenix_host/lib/phoenix_host_web/oban_powertools_auth.ex, examples/phoenix_host/lib/phoenix_host_web/oban_powertools_display_policy.ex, examples/phoenix_host/priv/repo/seeds.exs, examples/phoenix_host/README.md, examples/phoenix_host/regenerate.sh</files>
  <read_first>
    - examples/phoenix_host/mix.exs
    - lib/oban_powertools/auth.ex
    - lib/oban_powertools/runtime_config.ex
    - test/support/test_auth.ex
    - .planning/phases/11-docs-example-app-compatibility-contract-proof/11-CONTEXT.md
    - .planning/phases/11-docs-example-app-compatibility-contract-proof/11-PATTERNS.md
    - .planning/phases/11-docs-example-app-compatibility-contract-proof/11-VALIDATION.md
  </read_first>
  <action>
    Implement the real host seams per D-07, D-08, D-33, D-35, and D-38: a tiny but real `ObanPowertoolsAuth` module, a tiny but real `ObanPowertoolsDisplayPolicy` module that implements `display/3`, seeded operator-visible data, and a fixture README that says exactly what this host proves.
    Seed enough data to exercise one native audited mutation path plus the bridge inspection path described in the guides. Keep the seed set intentionally narrow; this fixture is proof infrastructure, not a productized demo.
    In `examples/phoenix_host/README.md`, document the local run steps, the seeded credentials/actor assumptions, the exact host-owned responsibilities that remain outside the library, the provenance note that this fixture is regenerated from `mix phx.new` plus `mix oban_powertools.install`, and the D-38 caveats that materially affect mounted `/ops/jobs` behavior: reverse-proxy forwarding expectations, LiveView/WebSocket transport requirements, and the fact that host auth/session wiring must already be correct before Powertools pages or the read-only bridge behave as documented.
    Add `examples/phoenix_host/regenerate.sh` as the concrete repro helper: it must create a fresh Phoenix host, apply the documented Powertools dependency/install steps, and leave obvious markers for the thin manual follow-up seams. Keep the script deterministic and repo-local so maintainers can diff the checked-in host against the paved road per D-04 and D-07.
    Make verification rerunnable: use an idempotent test-database reset path rather than `mix ecto.create`, and verify both the seam files and the provenance markers in the README/script.
  </action>
  <acceptance_criteria>
    - `examples/phoenix_host/lib/phoenix_host_web/oban_powertools_auth.ex` contains `@behaviour ObanPowertools.Auth`
    - `examples/phoenix_host/lib/phoenix_host_web/oban_powertools_display_policy.ex` contains `def display(`
    - `examples/phoenix_host/priv/repo/seeds.exs` contains `ops`
    - `examples/phoenix_host/README.md` contains `display_policy`
    - `examples/phoenix_host/README.md` contains `read-only`
    - `examples/phoenix_host/README.md` contains `mix phx.new`
    - `examples/phoenix_host/README.md` contains `reverse-proxy`
    - `examples/phoenix_host/README.md` contains `WebSocket`
    - `examples/phoenix_host/README.md` contains `auth/session`
    - `examples/phoenix_host/regenerate.sh` contains `mix oban_powertools.install`
  </acceptance_criteria>
  <verify>
    <automated>cd examples/phoenix_host && MIX_ENV=test mix ecto.reset && MIX_ENV=test mix run priv/repo/seeds.exs && rg -n "@behaviour ObanPowertools.Auth|def display\\(|ops|display_policy|read-only|mix phx.new|reverse-proxy|WebSocket|auth/session|mix oban_powertools.install" lib/phoenix_host_web/oban_powertools_auth.ex lib/phoenix_host_web/oban_powertools_display_policy.ex priv/repo/seeds.exs README.md regenerate.sh</automated>
  </verify>
  <done>The example host includes real host-owned seams, rerunnable seeded-data verification, and a concrete regeneration artifact proving the fixture derives from `mix phx.new` plus `mix oban_powertools.install`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Canonical example host -> public support claims | The fixture must not become a bespoke demo that implies unsupported surface area. |
| Host auth/display modules -> Powertools pages and bridge | Policy hooks must be real host-owned seams, not test-only stubs masquerading as the public contract. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-04 | Spoofing | example-host auth seam | mitigate | Implement a real host `ObanPowertoolsAuth` behaviour module and make docs/tests use it instead of fake bypass wiring. |
| T-11-05 | Tampering | example-host support truth | mitigate | Keep the fixture intentionally thin, native-first, explicit that the bridge is optional and read-only, and regenerable from the paved road so bespoke drift is visible. |
| T-11-06 | Elevation of Privilege | bridge-enabled example mode | mitigate | Require the same host auth seam in both native-only and bridge-enabled modes and document that bridge access stays inspection-only. |
</threat_model>

<verification>
cd examples/phoenix_host && mix deps.get && mix compile
cd examples/phoenix_host && MIX_ENV=test mix ecto.reset && MIX_ENV=test mix run priv/repo/seeds.exs
</verification>

<success_criteria>
There is one committed generated host app that proves the real host-owned seams and supports the documented first operator session without hiding the optional-bridge boundary.
</success_criteria>

<output>
After completion, create `.planning/phases/11-docs-example-app-compatibility-contract-proof/11-02-SUMMARY.md`
</output>
