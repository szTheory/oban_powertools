---
phase: 45-bulk-operations
plan: 02
type: tdd
wave: 2
depends_on: ["45-01-PLAN.md"]
files_modified: ["lib/oban_powertools/web/jobs_live.ex", "test/oban_powertools/web/live/jobs_live_test.exs"]
autonomous: true
requirements: ["QRY-04"]

must_haves:
  truths:
    - "Bulk actions run Lifeline.execute_repair sequentially for each job"
    - "Bulk operations provide a preview showing the number of jobs affected"
  artifacts:
    - path: "lib/oban_powertools/web/jobs_live.ex"
      provides: "bulk action bar, modal, and execution logic"
    - path: "test/oban_powertools/web/live/jobs_live_test.exs"
      provides: "bulk execution tests"
---

<objective>
Implement the bulk action UI (action bar + preview modal) and the execution pipeline that runs independent repairs per job, providing honest outcome reporting.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/45-bulk-operations/45-UI-SPEC.md
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Bulk Action Bar and Modal</name>
  <files>lib/oban_powertools/web/jobs_live.ex</files>
  <behavior>
    - Conditionally render a bulk action bar when `MapSet.size(@selected_jobs) > 0`.
    - Display retry/cancel/discard buttons based on `@filter.state`.
    - Implement `phx-click="preview_bulk"` taking the action type.
    - Render a bulk preview modal prompting for a reason, mirroring the single-action design but showing the count.
  </behavior>
  <action>
    Add HTML to the `render/1` function in `jobs_live.ex`. Add `handle_event` for `preview_bulk`.
  </action>
  <verify>
    <automated>mix test test/oban_powertools/web/live/jobs_live_test.exs</automated>
  </verify>
  <done>Bulk action bar and modal render correctly based on selection.</done>
</task>

<task type="auto" tdd="true">
  <name>Bulk Execution Pipeline</name>
  <files>lib/oban_powertools/web/jobs_live.ex, test/oban_powertools/web/live/jobs_live_test.exs</files>
  <behavior>
    - Implement `phx-submit="execute_bulk"`.
    - Iterate over `@selected_jobs`. For each:
      - Call `Lifeline.preview_repair`.
      - Call `Lifeline.execute_repair`.
    - Collect success and error counts.
    - Put a flash message detailing the outcome (e.g., "Bulk action complete: 2 successes, 1 failure").
    - Clear `@selected_jobs` and reload the job list via `load_jobs/2`.
  </behavior>
  <action>
    Update `jobs_live.ex` event handlers. Write tests verifying iteration and partial failure reporting.
  </action>
  <verify>
    <automated>mix test test/oban_powertools/web/live/jobs_live_test.exs</automated>
  </verify>
  <done>Bulk actions execute correctly and report outcomes.</done>
</task>
</tasks>
