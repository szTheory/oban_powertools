---
phase: 43-read-only-job-browse
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/oban_powertools/jobs.ex
  - lib/oban_powertools/runtime_config.ex
  - lib/oban_powertools/web/selectors.ex
  - test/oban_powertools/jobs_test.exs
autonomous: true
requirements:
  - QRY-01
  - QRY-02
tags:
  - oban
  - ecto
  - context-module
  - display-policy

must_haves:
  truths:
    - "ObanPowertools.Jobs.list/3 returns jobs filtered by state and (optionally) queue, worker, tags with state leading the WHERE clause"
    - "ObanPowertools.Jobs.count_by_state/2 returns a map keyed by all 7 Oban state strings, each value an integer count under the same non-state filters"
    - "ObanPowertools.Jobs.get/2 returns an %Oban.Job{} by id (or nil when not found)"
    - "DisplayPolicy.render_job_field(:job_args | :job_meta, value, context) returns a tagged tuple ({:raw_json, _} | {:string, _} | {:fallback, _})"
    - "Selectors.jobs_path/1 encodes /ops/jobs/jobs with nil/empty params dropped (mirrors lifeline_path/1 shape)"
  artifacts:
    - path: "lib/oban_powertools/jobs.ex"
      provides: "ObanPowertools.Jobs context module, %JobFilter{} defstruct, list/3, get/2, count_by_state/2"
      contains: "defstruct state:"
    - path: "lib/oban_powertools/runtime_config.ex"
      provides: "DisplayPolicy.render_job_field/3 public helper + :job_args/:job_meta support"
      contains: "def render_job_field"
    - path: "lib/oban_powertools/web/selectors.ex"
      provides: "jobs_path/1 helper and :jobs canonical path entry"
      contains: "def jobs_path"
    - path: "test/oban_powertools/jobs_test.exs"
      provides: "Unit tests for Jobs.list/3, get/2, count_by_state/2"
      contains: "defmodule ObanPowertools.JobsTest"
  key_links:
    - from: "lib/oban_powertools/jobs.ex"
      to: "Oban.Job (deps/oban/lib/oban/job.ex)"
      via: "Ecto.Query against Oban.Job schema"
      pattern: "where\\(\\[j\\], j\\.state == "
    - from: "lib/oban_powertools/runtime_config.ex"
      to: "host-configured :display_policy module"
      via: "apply_policy(:job_args | :job_meta, value, context)"
      pattern: "apply_policy\\(:job_(args|meta)"
    - from: "lib/oban_powertools/web/selectors.ex"
      to: "@canonical_paths"
      via: "encode(:jobs, params)"
      pattern: "jobs: \"/ops/jobs/jobs\""
---

<objective>
Lay the entire data + helper contract layer for Phase 43 (read-only job browse) so that the LiveView plans in Wave 2 and Wave 3 have zero exploration cost. This plan creates the `ObanPowertools.Jobs` context module (with `%JobFilter{}` struct, `list/3`, `get/2`, `count_by_state/2`), extends `ObanPowertools.DisplayPolicy` with the new `render_job_field/3` helper and `:job_args`/`:job_meta` kinds, adds `Selectors.jobs_path/1` + `:jobs` canonical path entry, and ships full unit tests against `TestRepo`.

Purpose: Per D-01 the `%JobFilter{}` struct MUST exist before any event handler is written. Per D-16 the DisplayPolicy extension is the redaction seam for the detail view. Per D-10 the `ObanPowertools.Jobs` module is the single owner of all `oban_jobs` queries (LiveView never queries directly). Per Pattern 5 (RESEARCH.md) `Selectors.jobs_path/1` must exist before any `push_patch` callsite in Wave 2.

Output: Five files — one new context module, two modified module-attribute extensions, one new test file, and a unit-test green bar that proves the query layer enforces D-05 (state leads WHERE) and D-11 (order scheduled_at DESC, id DESC).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/43-read-only-job-browse/43-CONTEXT.md
@.planning/phases/43-read-only-job-browse/43-RESEARCH.md
@.planning/phases/43-read-only-job-browse/43-PATTERNS.md

# Implementation surfaces this plan touches or mirrors
@lib/oban_powertools/runtime_config.ex
@lib/oban_powertools/web/selectors.ex
@lib/oban_powertools/cron.ex
@test/oban_powertools/lifeline_test.exs

<interfaces>
<!-- Executor receives these contracts; no codebase exploration required for them. -->

From lib/oban_powertools/runtime_config.ex (existing DisplayPolicy primitives — DO NOT change shape):
- `defp apply_policy(kind, value, context)` — calls `policy_module!().display(kind, value, context)`; returns whatever the host policy returns or `nil`.
- `defp render_text(kind, value, context, fallback)` — fallback-returning text variant; the new `render_job_field/3` follows the same case-on-`apply_policy/3` shape but with different return arms.
- `defp invalid_return_message(kind, other)` — reuse for the ArgumentError path.
- `DisplayPolicy.assert_configured!/0` — already exists; LiveView calls it; this plan does NOT change it.

From lib/oban_powertools/web/selectors.ex:
- `@canonical_paths` — map of atom → path string. Currently has `:lifeline`, `:forensics`, `:audit`, `:limiters`, `:cron`. Add `:jobs` entry mapping to `"/ops/jobs/jobs"`.
- `def encode/2` — generic encoder. Drops nil/`""` values, preserves keyword-list iteration order via `URI.encode_query/1`. Reuse via `jobs_path/1`.
- Pattern reference: `def lifeline_path(params), do: encode(:lifeline, params)` — mirror exactly for `jobs_path/1`.

From deps/oban/lib/oban/job.ex (oban 2.22.1) — fields used in this plan:
- `state :: binary` (stored as string "available" | "scheduled" | "executing" | "retryable" | "cancelled" | "discarded" | "completed")
- `queue :: binary`
- `worker :: binary`
- `tags :: {:array, :string}` — filtered with `fragment("? @> ?", j.tags, ^tags)`
- `args :: :map`, `meta :: :map`, `errors :: {:array, :map}`
- `scheduled_at, inserted_at, attempted_at, completed_at, cancelled_at, discarded_at :: utc_datetime_usec` (timing fields used by Jobs.get/2's caller in Wave 3)
- `attempt, max_attempts :: integer`

From CONTEXT.md (locked decisions implemented by this plan):
- D-01: `%JobFilter{}` struct MUST be defined before any event handler — this plan defines it.
- D-03: offset pagination; document keyset upgrade path as a single-function change in `Jobs.list/3` moduledoc.
- D-04: GIN index caveat documented in `ObanPowertools.Jobs` `@moduledoc`.
- D-05: state MUST lead every WHERE clause — enforced by `Jobs.list/3` and `Jobs.count_by_state/2`.
- D-10: `Jobs.list/3` signature is `list(repo, %JobFilter{}, opts \\ [])`; `Jobs.get/2` is `get(repo, job_id)`.
- D-11: default order is `ORDER BY scheduled_at DESC, id DESC`.
- D-13: count query is per-state (one COUNT per state) under the same non-state filters; returns a map keyed by state string.
- D-16: `:job_args` and `:job_meta` kinds added; return contract is `nil → raw JSON`, `String → string`, `Map → formatted JSON`; new public helper `render_job_field/3`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create ObanPowertools.Jobs context module with %JobFilter{}, list/3, get/2, count_by_state/2</name>
  <files>lib/oban_powertools/jobs.ex</files>
  <read_first>
    - lib/oban_powertools/cron.ex (mirror module-opening shape: `defmodule`, `@moduledoc`, `import Ecto.Query`, public functions with `repo` as first arg, `defp maybe_*` filter helpers with nil-pass-first clause)
    - lib/oban_powertools/runtime_config.ex (for `Application.fetch_env!(:oban_powertools, :repo)` usage style; this module does NOT use `RuntimeConfig.repo!/1` — callers pass `repo` explicitly)
    - .planning/phases/43-read-only-job-browse/43-CONTEXT.md (D-01, D-03, D-04, D-05, D-10, D-11, D-13)
    - .planning/phases/43-read-only-job-browse/43-RESEARCH.md (Pattern 4 ObanPowertools.Jobs context module — full reference implementation)
    - .planning/phases/43-read-only-job-browse/43-PATTERNS.md (Pattern Assignments → lib/oban_powertools/jobs.ex)
    - deps/oban/lib/oban/job.ex (Oban.Job schema — `state` field is a string, NOT an atom; tags is `{:array, :string}`)
  </read_first>
  <behavior>
    - Calling `Jobs.list(TestRepo, %JobFilter{state: :available})` returns only jobs whose `state == "available"` (state atom is converted to string via `to_string/1` at the WHERE boundary, per D-05).
    - Calling `Jobs.list(TestRepo, %JobFilter{state: :executing, queue: "default"})` returns only `state="executing"` AND `queue="default"` jobs; the `queue` filter is composed via `maybe_filter_queue/2` (nil-pass clause first).
    - Calling `Jobs.list(TestRepo, %JobFilter{state: :available, worker: "MyApp.IngestWorker"})` filters by exact worker string match.
    - Calling `Jobs.list(TestRepo, %JobFilter{state: :available, tags: ["alpha"]})` filters using `fragment("? @> ?", j.tags, ^tags)`.
    - Calling `Jobs.list(TestRepo, %JobFilter{state: :available, page: 1, page_size: 2})` returns at most 2 rows; `page: 2, page_size: 2` returns the next 2.
    - Default ordering for `Jobs.list/3` is `ORDER BY scheduled_at DESC, id DESC` (D-11) — when 3 jobs have identical `scheduled_at`, the higher `id` comes first.
    - `Jobs.get(TestRepo, job_id)` returns the matching `%Oban.Job{}` or `nil` for unknown ids.
    - `Jobs.count_by_state(TestRepo, %JobFilter{state: :available})` returns a map with exactly these 7 keys: `"available"`, `"scheduled"`, `"executing"`, `"retryable"`, `"cancelled"`, `"discarded"`, `"completed"` — each value is a non-negative integer; only the non-state filters (queue/worker/tags) on the passed filter narrow each per-state count (the `state` field of the passed filter is ignored because we iterate all 7 states).
  </behavior>
  <action>
Create a new module file `lib/oban_powertools/jobs.ex` named `ObanPowertools.Jobs`. The module must:

- Open with `import Ecto.Query` (no other aliases needed for this file).
- Carry a `@moduledoc` that documents (a) the read-only job browse purpose, (b) the GIN index caveat for tags filtering per D-04 — include the literal SQL `CREATE INDEX CONCURRENTLY oban_jobs_tags_gin ON oban_jobs USING gin(tags);` and the sentence "Oban does not create this index. The host application owns it.", and (c) the keyset pagination upgrade path per D-03 — note that swapping the `offset:` clause for a `where: j.scheduled_at < ^cursor_scheduled_at or (j.scheduled_at == ^cursor_scheduled_at and j.id < ^cursor_id)` clause is a single-function change.
- Define `defstruct` with fields `state: :available, queue: nil, worker: nil, tags: nil, page: 1, page_size: 20` per D-10.
- Declare `@type t :: %__MODULE__{state: atom(), queue: String.t() | nil, worker: String.t() | nil, tags: [String.t()] | nil, page: pos_integer(), page_size: pos_integer()}`.
- Define `def list(repo, %__MODULE__{} = filter, _opts \\ [])` — pipeline: `Oban.Job |> where([j], j.state == ^to_string(filter.state)) |> maybe_filter_queue(filter.queue) |> maybe_filter_worker(filter.worker) |> maybe_filter_tags(filter.tags) |> order_by([j], [desc: j.scheduled_at, desc: j.id]) |> limit(^filter.page_size) |> offset(^offset) |> repo.all()`. Compute `offset = (filter.page - 1) * filter.page_size`. The `state` MUST lead the WHERE clause (D-05).
- Define `def get(repo, job_id)`, which delegates to `repo.get(Oban.Job, job_id)`.
- Define `def count_by_state(repo, %__MODULE__{} = base_filter)` — iterate over the 7 state strings `~w(available scheduled executing retryable cancelled discarded completed)`, run one COUNT query per state with the non-state filters from `base_filter` applied (queue/worker/tags), return a map `%{"available" => n, ...}` per D-13. Build the query as `Oban.Job |> where([j], j.state == ^state) |> maybe_filter_queue(base_filter.queue) |> maybe_filter_worker(base_filter.worker) |> maybe_filter_tags(base_filter.tags) |> select([j], count(j.id)) |> repo.one()`. Document inline that this issues 7 round-trips per filter change and is acceptable for Phase 43 because each query uses the state-leading composite index.
- Define `defp maybe_filter_queue(query, nil), do: query` then `defp maybe_filter_queue(query, queue), do: where(query, [j], j.queue == ^queue)`. Same nil-pass-first shape for `maybe_filter_worker/2` (worker exact-match) and `maybe_filter_tags/2` (three clauses: nil-pass, empty-list-pass, list-with-elements uses `fragment("? @> ?", j.tags, ^tags)`).

Anti-patterns to avoid (per RESEARCH.md):
- DO NOT introduce a `defp repo/0` helper inside this module — callers pass `repo` explicitly per the existing convention in `ObanPowertools.Cron`.
- DO NOT use `Oban.cancel_job/1` or any `Oban` runtime function from this module — read-only context only.
- DO NOT collapse `count_by_state/2` into a single `GROUP BY state` query — per D-13 each state must appear in the result map with `0` if absent.
- DO NOT use `String.to_atom(state)` or `String.to_existing_atom(state)` inside `count_by_state/2`'s return map keys — keep keys as strings to match the `oban_jobs.state` column shape.
  </action>
  <verify>
    <automated>mix compile --warnings-as-errors 2>&amp;1 | grep -E "warning|error" | grep -v "Compiling" || echo "clean"</automated>
  </verify>
  <acceptance_criteria>
    - File `lib/oban_powertools/jobs.ex` exists.
    - `grep -c 'defmodule ObanPowertools.Jobs do' lib/oban_powertools/jobs.ex` returns `1`.
    - `grep -c 'defstruct state: :available' lib/oban_powertools/jobs.ex` returns `1`.
    - `grep -c 'def list(repo, %__MODULE__{} = filter' lib/oban_powertools/jobs.ex` returns `1`.
    - `grep -c 'def get(repo, job_id)' lib/oban_powertools/jobs.ex` returns `1`.
    - `grep -c 'def count_by_state(repo' lib/oban_powertools/jobs.ex` returns `1`.
    - `grep -c 'j.state == \^to_string(filter.state)' lib/oban_powertools/jobs.ex` returns at least `1` (state leads WHERE).
    - `grep -c 'order_by(\[j\], \[desc: j.scheduled_at, desc: j.id\])' lib/oban_powertools/jobs.ex` returns `1` (D-11 ordering).
    - `grep -c 'oban_jobs_tags_gin' lib/oban_powertools/jobs.ex` returns at least `1` (D-04 GIN index documented).
    - `grep -c 'keyset' lib/oban_powertools/jobs.ex` returns at least `1` (D-03 upgrade path documented).
    - `grep -cE 'fragment\("\? @> \?", j.tags' lib/oban_powertools/jobs.ex` returns `1`.
    - `mix compile --warnings-as-errors` exits 0.
  </acceptance_criteria>
  <done>The `ObanPowertools.Jobs` module exists, compiles cleanly with `--warnings-as-errors`, defines `%JobFilter{}` (as `%__MODULE__{}`), exposes `list/3`, `get/2`, `count_by_state/2` with state-leading WHERE and D-11 ordering, documents both the GIN index caveat (D-04) and the keyset upgrade path (D-03), and contains no `Oban` runtime function calls.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend DisplayPolicy with render_job_field/3 and add Selectors.jobs_path/1</name>
  <files>lib/oban_powertools/runtime_config.ex, lib/oban_powertools/web/selectors.ex</files>
  <read_first>
    - lib/oban_powertools/runtime_config.ex (the entire `ObanPowertools.DisplayPolicy` module — sections `apply_policy/3`, `render_text/4`, `invalid_return_message/2`; the new helper mirrors `render_text/4`'s shape)
    - lib/oban_powertools/web/selectors.ex (`@canonical_paths` map, `encode/2`, `lifeline_path/1` — the new entry and helper mirror these exactly)
    - .planning/phases/43-read-only-job-browse/43-CONTEXT.md (D-16 return contract for `render_job_field/3`)
    - .planning/phases/43-read-only-job-browse/43-RESEARCH.md (Pattern 3 DisplayPolicy render helper; Pattern 5 Selectors extension)
    - .planning/phases/43-read-only-job-browse/43-PATTERNS.md (Pattern Assignments → runtime_config.ex and selectors.ex sections)
  </read_first>
  <behavior>
    - For host display policy `display(:job_args, value, context) :: nil`, `DisplayPolicy.render_job_field(:job_args, %{"a" => 1}, %{})` returns `{:raw_json, "{\n  \"a\": 1\n}"}` (pretty JSON encoding of the original value).
    - For host display policy `display(:job_args, _, _) :: "redacted text"`, the helper returns `{:string, "redacted text"}`.
    - For host display policy `display(:job_meta, _, _) :: %{"trace" => "abc"}`, the helper returns `{:raw_json, "{\n  \"trace\": \"abc\"\n}"}` (pretty JSON of the host-returned map).
    - For host display policy that raises, the helper rescues and returns `{:fallback, "[redacted]"}` — the LiveView must never crash on a misbehaving host policy.
    - For host display policy returning an invalid type (e.g. an atom, integer, list), the helper raises `ArgumentError` carrying the existing `invalid_return_message/2` text — same shape as `render_text/4`.
    - `Selectors.jobs_path([{"state", "available"}, {"queue", nil}])` returns the string `"/ops/jobs/jobs?state=available"` (nil dropped).
    - `Selectors.jobs_path([])` returns `"/ops/jobs/jobs"` (no trailing `?`).
    - `Selectors.encode(:jobs, [])` returns `"/ops/jobs/jobs"`.
  </behavior>
  <action>
**File 1: `lib/oban_powertools/runtime_config.ex`** — extend the `ObanPowertools.DisplayPolicy` module (the second `defmodule` in this file, starting around line 78). Add a new public function `def render_job_field(kind, value, context)`. The function must:

- Accept any atom `kind` (the LiveView calls it with `:job_args` and `:job_meta`; the helper is generic so future kinds can reuse it without further edits).
- Call the existing private `apply_policy(kind, value, context)`.
- Branch on the return:
  - `nil` → return `{:raw_json, Jason.encode!(value || %{}, pretty: true)}` (per D-16: nil → raw JSON; normalize `nil` input to `%{}` to avoid `Jason` crash on bare nil values).
  - `text when is_binary(text)` → return `{:string, text}`.
  - `%{} = redacted_map` → return `{:raw_json, Jason.encode!(redacted_map, pretty: true)}`.
  - any other value → `raise ArgumentError, invalid_return_message(kind, other)`.
- Wrap the entire body in `rescue _ -> {:fallback, "[redacted]"}` so a misbehaving host policy (or a Jason error from cyclic data) never crashes the LiveView. Note: the explicit `ArgumentError` for invalid return shapes is intentionally caught by the rescue and becomes `{:fallback, "[redacted]"}`.
- Place `render_job_field/3` immediately after the existing public function `workflow_result/2` block and before the `defp render_text/4` private section, so all public DisplayPolicy functions cluster together.
- Do NOT modify `apply_policy/3`, `render_text/4`, `invalid_return_message/2`, or any other existing function.
- Do NOT add new aliases or imports — `Jason` is already a transitive dependency available at runtime (used by `Oban.Job` itself).

**File 2: `lib/oban_powertools/web/selectors.ex`** — additive only:

- In `@canonical_paths`, add a new entry `jobs: "/ops/jobs/jobs"` (after the existing `cron:` entry, preserving alphabetical-ish convention is not required — append at the end of the map literal).
- Add a new public helper `def jobs_path(params \\ []), do: encode(:jobs, params)` with an `@doc "Returns the `/ops/jobs/jobs` path with the given params encoded."` matching the shape of the existing `lifeline_path/1` helper (selectors.ex line 67).
- Do NOT change `encode/2` or any existing helper.
  </action>
  <verify>
    <automated>mix compile --warnings-as-errors 2>&amp;1 | grep -E "warning|error" | grep -v "Compiling" || echo "clean"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'def render_job_field(kind, value, context)' lib/oban_powertools/runtime_config.ex` returns `1`.
    - `grep -c '{:raw_json, Jason.encode!' lib/oban_powertools/runtime_config.ex` returns at least `2` (nil arm and map arm).
    - `grep -c '{:string, text}' lib/oban_powertools/runtime_config.ex` returns at least `1`.
    - `grep -c '{:fallback, "\[redacted\]"}' lib/oban_powertools/runtime_config.ex` returns at least `1`.
    - `grep -c 'rescue' lib/oban_powertools/runtime_config.ex` returns at least `1` (new rescue clause).
    - `grep -cE '^[[:space:]]*jobs: "/ops/jobs/jobs"' lib/oban_powertools/web/selectors.ex` returns `1`.
    - `grep -c 'def jobs_path(params \\\\ \[\]), do: encode(:jobs, params)' lib/oban_powertools/web/selectors.ex` returns `1`.
    - `mix compile --warnings-as-errors` exits 0.
    - `mix test test/oban_powertools/web/selectors_test.exs` (existing test file, if present) still exits 0 — additive change must not break canonical path tests.
  </acceptance_criteria>
  <done>The `DisplayPolicy.render_job_field/3` helper exists with the three-arm return contract per D-16 plus a `rescue → {:fallback, "[redacted]"}` safety net. The `Selectors` module gains a `:jobs` canonical path entry and a `jobs_path/1` helper mirroring `lifeline_path/1`. No existing function is changed. `mix compile --warnings-as-errors` exits 0.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Write unit tests for ObanPowertools.Jobs (list/3, get/2, count_by_state/2)</name>
  <files>test/oban_powertools/jobs_test.exs</files>
  <read_first>
    - test/oban_powertools/lifeline_test.exs (analog: test module structure, `use ObanPowertools.DataCase, async: false`, `alias ObanPowertools.TestRepo` usage)
    - test/support/data_case.ex (sandbox checkout pattern)
    - lib/oban_powertools/jobs.ex (the module being tested — read after Task 1 commits)
    - .planning/phases/43-read-only-job-browse/43-RESEARCH.md (Validation Architecture → Phase Requirements → Test Map; sampling rate)
    - deps/oban/lib/oban/job.ex (Oban.Job.new/2 changeset shape — args is a required map; queue/worker/state can be set via `new/2` opts)
  </read_first>
  <behavior>
    - Test "list/3 filters by state with state leading the WHERE clause": insert one job with `state: "available"` and one with `state: "executing"`, call `Jobs.list(TestRepo, %ObanPowertools.Jobs{state: :available})`, assert exactly the available job is returned.
    - Test "list/3 narrows by queue, worker, and tags": insert two `state: "available"` jobs with different queues, assert filtering by queue returns exactly one. Repeat for worker and tags.
    - Test "list/3 orders by scheduled_at DESC, id DESC": insert two jobs with identical `scheduled_at` (set the field explicitly via `Ecto.Changeset.change/2` after insert), assert higher `id` appears first.
    - Test "list/3 paginates by page/page_size": insert 3 jobs with `state: "available"`, query with `page: 1, page_size: 2` returns 2 jobs, `page: 2, page_size: 2` returns 1 job.
    - Test "get/2 returns the job by id, nil when not found": insert a job, assert `Jobs.get(TestRepo, job.id)` returns an `%Oban.Job{}` with matching id; assert `Jobs.get(TestRepo, 999_999_999)` returns `nil`.
    - Test "count_by_state/2 returns map keyed by all 7 state strings": insert 2 jobs in `"available"` and 1 in `"executing"`, call `Jobs.count_by_state(TestRepo, %ObanPowertools.Jobs{})`, assert the returned map has all 7 keys including states with zero counts (`"scheduled" => 0`, etc.).
    - Test "count_by_state/2 honors non-state filters": insert one `available + queue=default` and one `available + queue=other`, call `count_by_state` with `%ObanPowertools.Jobs{queue: "default"}`, assert the `"available"` count is 1.
  </behavior>
  <action>
Create `test/oban_powertools/jobs_test.exs` defining `ObanPowertools.JobsTest` with `use ObanPowertools.DataCase, async: false` and `alias ObanPowertools.{Jobs, TestRepo}`.

Add a local fixture helper `defp insert_job!(attrs)` that builds an Oban job via `Oban.Job.new(args, opts)` (args defaults to `%{}`, opts include `:queue`, `:worker`, `:tags`) and inserts it via `TestRepo.insert!/1`. For tests that need a specific `scheduled_at`, follow up with `Ecto.Changeset.change(job, scheduled_at: explicit_dt) |> TestRepo.update!/1`. For tests that need a specific `state` other than `"available"` (the default for new jobs), update via changeset since `Oban.Job.new/2` does not accept a `state` opt.

Cover at minimum these test cases (each as a separate `test "..."` block):

1. `list/3 filters by state` — state leads WHERE
2. `list/3 narrows by queue`
3. `list/3 narrows by worker`
4. `list/3 narrows by tags via @> array contains`
5. `list/3 orders by scheduled_at DESC, id DESC` — D-11 (set identical `scheduled_at` on 2 jobs, assert higher id first)
6. `list/3 paginates by page/page_size` — D-03 offset behavior
7. `get/2 returns job by id, nil when not found`
8. `count_by_state/2 returns map with all 7 state keys` — including zero counts (D-13)
9. `count_by_state/2 honors non-state filters` — queue/worker/tags from base_filter narrow each per-state count

Do NOT use Oban.insert/1 — go directly through TestRepo.insert!/1 against the changeset from Oban.Job.new/2; this avoids needing a running Oban supervisor in the test.

Do NOT depend on the Oban supervisor or pubsub — these tests touch only the `oban_jobs` table via the repo.

Do NOT use `async: true` — the sandbox checkout in `DataCase` mirrors `LifelineTest`'s `async: false` setup.
  </action>
  <verify>
    <automated>mix test test/oban_powertools/jobs_test.exs --max-failures 1</automated>
  </verify>
  <acceptance_criteria>
    - File `test/oban_powertools/jobs_test.exs` exists.
    - `grep -c 'defmodule ObanPowertools.JobsTest' test/oban_powertools/jobs_test.exs` returns `1`.
    - `grep -c 'use ObanPowertools.DataCase' test/oban_powertools/jobs_test.exs` returns `1`.
    - `grep -cE '^[[:space:]]*test "' test/oban_powertools/jobs_test.exs` returns at least `9` (one per behavior bullet above).
    - `grep -c 'count_by_state' test/oban_powertools/jobs_test.exs` returns at least `2` (2+ tests cover it).
    - `grep -c 'order' test/oban_powertools/jobs_test.exs` returns at least `1` (D-11 ordering test present).
    - `grep -cE 'page_size|page:' test/oban_powertools/jobs_test.exs` returns at least `1` (pagination test present).
    - `mix test test/oban_powertools/jobs_test.exs` exits 0.
  </acceptance_criteria>
  <done>All unit tests pass against the implementation from Task 1. The test file proves D-05 (state leads WHERE), D-11 (scheduled_at DESC, id DESC order), D-03 (offset pagination behavior), and D-13 (count_by_state returns all 7 state keys including zeros) by behavioral assertions, not by inspecting query AST.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host repo → ObanPowertools.Jobs | Repo handle is supplied by the LiveView; this module is a pure query owner with no actor or principal handling. |
| host display_policy → ObanPowertools.DisplayPolicy | Host implementation of `display/3` is untrusted user code; the new `render_job_field/3` must not crash the LiveView if the host raises or returns junk. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-43-01-01 | Tampering | `Jobs.list/3` filter inputs reaching `oban_jobs` | mitigate | All `%JobFilter{}` fields are bound through Ecto parameterized placeholders (`^to_string(filter.state)`, `^queue`, `^worker`, `^tags`) — no string interpolation into queries; SQL injection impossible. |
| T-43-01-02 | Denial of Service | full-table scan via missing leading state filter | mitigate | `Jobs.list/3` unconditionally emits `where([j], j.state == ^to_string(filter.state))` as the first WHERE clause (D-05) — composite index `oban_jobs_state_queue_priority_scheduled_at_id_index` always applies; verified by the "state leads WHERE" unit test. |
| T-43-01-03 | Denial of Service | tags filter without GIN index → sequential scan on state-filtered rows | accept | Host owns the GIN index per D-04. Module `@moduledoc` documents the required `CREATE INDEX CONCURRENTLY oban_jobs_tags_gin ON oban_jobs USING gin(tags);`. Sequential scan on state-filtered rows is bounded — never an unfiltered full-table scan. |
| T-43-01-04 | Information Disclosure | raw `args`/`meta` JSON exposed when host policy returns junk or raises | mitigate | `DisplayPolicy.render_job_field/3` rescues any exception → `{:fallback, "[redacted]"}`; invalid return shapes raise `ArgumentError` and are caught by the same rescue. LiveView renders `[redacted]` rather than the raw value. |
| T-43-01-05 | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan; no Hex package additions. RESEARCH.md confirms zero new dependencies. |
</threat_model>

<verification>
- `mix compile --warnings-as-errors` exits 0 after each task.
- `mix test test/oban_powertools/jobs_test.exs` exits 0 (9+ tests pass).
- `mix test` exits 0 (full suite green; existing Selectors and DisplayPolicy tests still pass with additive changes).
- All 5 D-IDs implemented by this plan (D-01, D-03, D-04, D-05, D-10, D-11, D-13, D-16) are observable in the resulting source via the grep gates listed in each task's `acceptance_criteria`.
</verification>

<success_criteria>
1. `ObanPowertools.Jobs` module exists, defines `%JobFilter{}`, exposes `list/3`, `get/2`, `count_by_state/2`.
2. `Jobs.list/3` enforces D-05 (state leads WHERE) and D-11 (scheduled_at DESC, id DESC order); offset pagination per D-03; keyset upgrade path documented in moduledoc.
3. `Jobs.count_by_state/2` returns a map with all 7 state strings as keys per D-13.
4. GIN index caveat documented in `Jobs` moduledoc per D-04.
5. `DisplayPolicy.render_job_field/3` exists with the D-16 return contract and `rescue → {:fallback, "[redacted]"}` safety net.
6. `Selectors.jobs_path/1` and `:jobs` canonical path entry exist.
7. All 9+ unit tests pass against the implementation.
8. `mix compile --warnings-as-errors` exits 0; `mix test` exits 0.
</success_criteria>

<output>
Create `.planning/phases/43-read-only-job-browse/43-01-SUMMARY.md` when done. Include:
- File-by-file diff summary
- Test counts (passed/skipped) for `mix test test/oban_powertools/jobs_test.exs`
- Compile warnings (should be zero)
- Confirmation that D-01, D-03, D-04, D-05, D-10, D-11, D-13, D-16 are implemented
</output>
