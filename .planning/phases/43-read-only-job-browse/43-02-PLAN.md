---
phase: 43-read-only-job-browse
plan: 02
type: execute
wave: 2
depends_on:
  - 43-01
files_modified:
  - lib/oban_powertools/web/live_auth.ex
  - lib/oban_powertools/web/router.ex
  - lib/oban_powertools/web/jobs_live.ex
  - test/oban_powertools/web/live/jobs_live_test.exs
autonomous: true
requirements:
  - QRY-01
tags:
  - phoenix-liveview
  - routing
  - auth
  - oban
  - filter-state

must_haves:
  truths:
    - "Operator can open /ops/jobs/jobs and see a list of jobs scoped to a single state with 7 state-tab buttons across the top, each showing a count"
    - "Operator can narrow the list by queue, worker, and tags via inline form controls — filter changes update URL query params and the query re-runs"
    - "URL filter state is preserved across browser back/forward — handle_params/3 rebuilds the filter from URL and loads data"
    - "Arriving at /ops/jobs/jobs with no state param triggers push_patch to ?state=available before any query runs"
    - "Unauthorized actors are redirected to / via LiveAuth.authorize_page/3"
  artifacts:
    - path: "lib/oban_powertools/web/live_auth.ex"
      provides: ":view_jobs, :view_job_detail, :retry_job, :cancel_job, :discard_job atoms in @permission_messages; :jobs and :job_detail entries in @page_read_only_banners"
      contains: "view_jobs:"
    - path: "lib/oban_powertools/web/router.ex"
      provides: "/jobs and /jobs/:id routes inside live_session :oban_powertools_native"
      contains: "ObanPowertools.Web.JobsLive, :index"
    - path: "lib/oban_powertools/web/jobs_live.ex"
      provides: "ObanPowertools.Web.JobsLive with :index and :show actions; mount/3 branches per-action for auth; handle_params/3 dispatches list vs detail"
      contains: "defmodule ObanPowertools.Web.JobsLive"
    - path: "test/oban_powertools/web/live/jobs_live_test.exs"
      provides: "LiveView integration tests for the :index action — auth, redirect, state tabs, filter, URL preservation, read-only banner"
      contains: "defmodule ObanPowertools.Web.JobsLiveTest"
  key_links:
    - from: "lib/oban_powertools/web/jobs_live.ex"
      to: "ObanPowertools.Jobs"
      via: "Jobs.list/3 and Jobs.count_by_state/2 calls"
      pattern: "Jobs\\.(list|count_by_state)"
    - from: "lib/oban_powertools/web/jobs_live.ex"
      to: "LiveAuth.authorize_page/3"
      via: "mount/3 auth gate"
      pattern: "LiveAuth\\.authorize_page\\(socket, :view_jobs"
    - from: "lib/oban_powertools/web/jobs_live.ex"
      to: "Selectors.jobs_path/1"
      via: "push_patch URL construction"
      pattern: "Selectors\\.jobs_path"
    - from: "lib/oban_powertools/web/router.ex"
      to: "ObanPowertools.Web.JobsLive"
      via: "live macro registration"
      pattern: "live\\(\"/jobs\", ObanPowertools.Web.JobsLive"
---

<objective>
Ship the read-only job list page: routes, LiveAuth atoms, JobsLive `:index` action, and integration tests. Wave 2 produces a working `/ops/jobs/jobs?state=available` URL that lists jobs scoped to a single state, exposes a 7-tab state navigation (with per-state counts), supports queue/worker/tags filtering through URL query params (D-07), and redirects unauthorized actors per the existing LiveAuth pattern.

Purpose: Implement QRY-01 end-to-end at the LiveView layer. Wave 1 produced the data + helper contracts; this wave wires them into the operator-facing surface. Per D-15 this page has zero mutation controls — Phase 44 will attach actions to the same module.

Output: Three modified source files (live_auth.ex, router.ex, jobs_live.ex created here), one new test file, and a green LiveView test bar for the list page. The `:show` action skeleton is added with auth + minimal stub handle_params so the route compiles, but the detail render is owned by Wave 3.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/43-read-only-job-browse/43-CONTEXT.md
@.planning/phases/43-read-only-job-browse/43-RESEARCH.md
@.planning/phases/43-read-only-job-browse/43-PATTERNS.md
@.planning/phases/43-read-only-job-browse/43-UI-SPEC.md

# Implementation surfaces this plan touches or mirrors
@lib/oban_powertools/web/live_auth.ex
@lib/oban_powertools/web/router.ex
@lib/oban_powertools/web/workflows_live.ex
@lib/oban_powertools/web/lifeline_live.ex
@lib/oban_powertools/web/selectors.ex
@lib/oban_powertools/jobs.ex
@test/oban_powertools/web/live/workflows_live_test.exs

# Wave 1 summary — read after 43-01 commits
@.planning/phases/43-read-only-job-browse/43-01-SUMMARY.md

<interfaces>
<!-- Contracts produced by Wave 1, consumed by this plan -->

From lib/oban_powertools/jobs.ex (created in 43-01):
- `defmodule ObanPowertools.Jobs` with `defstruct state: :available, queue: nil, worker: nil, tags: nil, page: 1, page_size: 20`
- `def list(repo, %ObanPowertools.Jobs{} = filter, _opts \\ [])` returns `[%Oban.Job{}]` — state leads WHERE; offset pagination
- `def get(repo, job_id)` returns `%Oban.Job{} | nil`
- `def count_by_state(repo, %ObanPowertools.Jobs{} = base_filter)` returns `%{"available" => n, "scheduled" => n, "executing" => n, "retryable" => n, "cancelled" => n, "discarded" => n, "completed" => n}`

From lib/oban_powertools/runtime_config.ex (extended in 43-01):
- `DisplayPolicy.render_job_field(kind, value, context) :: {:raw_json, json_string} | {:string, text} | {:fallback, "[redacted]"}` — used by Wave 3, NOT by this plan.

From lib/oban_powertools/web/selectors.ex (extended in 43-01):
- `Selectors.jobs_path(params)` returns `"/ops/jobs/jobs"` or `"/ops/jobs/jobs?<query>"`; nil/empty values dropped; preserves keyword-list order.

From lib/oban_powertools/web/live_auth.ex (existing):
- `LiveAuth.authorize_page(socket, action, resource) :: {:ok, socket} | {:error, socket_with_redirect}`
- `LiveAuth.authorized?(actor, action, resource) :: boolean`
- `LiveAuth.page_read_only_banner(:jobs)` will raise `KeyError` until live_auth.ex is extended (Task 1).

Existing canonical LiveView shape (workflows_live.ex line 14, lifeline_live.ex line 17):
- `mount(_params, %{"oban_dashboard_path" => dashboard_path}, socket)` — session map destructure is mandatory.
- `with {:ok, socket} <- LiveAuth.authorize_page(...) do ... else {:error, socket} -> {:ok, socket} end` — the unauthorized branch returns `{:ok, socket}` because `authorize_page` already wraps the redirect.
- `:ok = DisplayPolicy.assert_configured!()` — called immediately after auth succeeds; existing pattern in all native LiveViews.
- `defp repo, do: Application.fetch_env!(:oban_powertools, :repo)` — local helper used by every LiveView; copy verbatim.

UI-SPEC Component Inventory (Job List Page section) — exact Tailwind classes and copy strings are normative:
- Page header `h1.text-2xl.font-semibold` "Jobs" + subtitle `p.text-sm.text-zinc-600`
- Read-only banner `p.rounded-lg.border.border-amber-200.bg-amber-50.px-4.py-3.text-sm.text-amber-800`
- State tab button (active): `rounded border border-indigo-300 bg-indigo-50 px-3 py-2 text-sm font-semibold text-indigo-700`
- State tab button (inactive): `rounded border border-slate-200 bg-white px-3 py-2 text-sm font-semibold text-slate-600`
- Tab label format: `executing (12)`
- Table wrapper: `div.overflow-hidden.rounded-lg.border.bg-white`
- `thead.bg-slate-50.text-left.text-sm` with `th.px-4.py-3.font-semibold`
- Table columns (in order): State, Worker, Queue, Job ID, Scheduled At, Attempts
- Pagination button: `rounded border px-3 py-2 text-sm`; disabled adds `cursor-not-allowed text-zinc-400`
- Empty state: `div.rounded-lg.border.bg-white.p-6` containing `h2.text-base.font-semibold` + `p.mt-2.text-sm.text-zinc-600`
- State badge color map per UI-SPEC §Color (e.g., `executing` → `border-indigo-200 bg-indigo-50 text-indigo-700`)

UI-SPEC Copywriting Contract — verbatim strings to render:
- Page title: `Jobs`
- Page subtitle: `Browse and inspect Oban jobs by state.` followed by the native banner from `ControlPlanePresenter.native_banner/0` (existing helper, see workflows_live.ex line 86).
- State tab labels: lowercase state name plus `(<count>)` — e.g. `available (3)`.
- Empty state heading: `No <state> jobs`
- Empty state body: `No jobs are currently in the <state> state. Try a different state or filter.`
- Filter labels: `Queue`, `Worker`, `Tags` with placeholders `All queues`, `All workers`, `Any tag`.
- Pagination buttons: `Previous`, `Next`.
- Primary row CTA copy: `Inspect` (used in row navigation aria-label/link text).

UI-SPEC Interaction Contract — normative event names:
- State tab click: `phx-click="select_state" phx-value-state="executing"` → `push_patch` to update `?state=`.
- Filter form: `phx-change="filter"` form with `name="filter[queue]"`, `name="filter[worker]"`, `name="filter[tags]"` selects/inputs.
- Row click navigates to `/ops/jobs/jobs/#{id}` via inline `JS.navigate` or an enclosing `<.link navigate=...>` per UI-SPEC §Row Navigation.
- Pagination: `phx-click="paginate" phx-value-page="2"` → `push_patch`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend LiveAuth permission_messages and page_read_only_banners; add JobsLive routes</name>
  <files>lib/oban_powertools/web/live_auth.ex, lib/oban_powertools/web/router.ex</files>
  <read_first>
    - lib/oban_powertools/web/live_auth.ex (lines 24-47: `@permission_messages` and `@page_read_only_banners` map literals — extend, do not replace existing entries)
    - lib/oban_powertools/web/router.ex (lines 53-64: the `live_session :oban_powertools_native` block — insertion point for the two new routes)
    - .planning/phases/43-read-only-job-browse/43-CONTEXT.md (D-02, D-06, D-19, D-20)
    - .planning/phases/43-read-only-job-browse/43-PATTERNS.md (live_auth.ex pattern map — voice for new entries; router.ex pattern map — insertion point)
    - .planning/phases/43-read-only-job-browse/43-UI-SPEC.md (Copywriting Contract — exact banner strings for `:jobs` and `:job_detail`)
  </read_first>
  <action>
**File 1: `lib/oban_powertools/web/live_auth.ex`** — additive map extensions only. Do NOT modify any existing entry. Do NOT add new function definitions.

Extend `@permission_messages` with 5 atoms per D-20 (preserve voice from existing entries — "Permission: read-only. You can inspect..."):

- `:view_jobs` → "Permission: read-only. You can inspect the Powertools-native job list, but mutation controls stay disabled until you receive broader permission."
- `:view_job_detail` → "Permission: read-only. You can inspect this Powertools-native job detail, but mutation controls stay disabled until you receive broader permission."
- `:retry_job` → "Permission: read-only. You can inspect this Powertools-native job, but you do not have permission to retry it."
- `:cancel_job` → "Permission: read-only. You can inspect this Powertools-native job, but you do not have permission to cancel it."
- `:discard_job` → "Permission: read-only. You can inspect this Powertools-native job, but you do not have permission to discard it."

Extend `@page_read_only_banners` with 2 atoms per D-19 (voice matches existing entries — short banner, "stays visible" framing, per UI-SPEC §Copywriting Contract):

- `:jobs` → "Permission: read-only. Job list stays visible, but mutation controls stay disabled until you receive broader permission."
- `:job_detail` → "Permission: read-only. Job detail stays visible, but retry, cancel, and discard controls stay disabled until you receive broader permission."

Note: pre-declaring `:retry_job`, `:cancel_job`, `:discard_job` here (per D-02) is intentional — Phase 44 will use them. Phase 43 does not call `permission_message(:retry_job)` directly, but compile-time presence avoids cross-phase coordination cost.

**File 2: `lib/oban_powertools/web/router.ex`** — additive only. Inside the existing `live_session :oban_powertools_native` `do` block (around line 53-64), append two new `live/3` route declarations AFTER the existing `live("/workflows/:id", ObanPowertools.Web.WorkflowsLive, :show)` line:

- First new line: `live` macro call with path `"/jobs"`, module `ObanPowertools.Web.JobsLive`, action atom `:index`.
- Second new line: `live` macro call with path `"/jobs/:id"`, module `ObanPowertools.Web.JobsLive`, action atom `:show`.

(These are macro-style configuration entries that mirror the existing `live(...)` invocations in this block — two single-line additions; no other change.)

Do NOT create a new `live_session`. Do NOT change the existing `on_mount` or session map. Do NOT touch the `oban_dashboard` bridge block.
  </action>
  <verify>
    <automated>mix compile --warnings-as-errors 2>&amp;1 | grep -E "warning|error" | grep -v "Compiling" || echo "clean"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'view_jobs:' lib/oban_powertools/web/live_auth.ex` returns `1`.
    - `grep -c 'view_job_detail:' lib/oban_powertools/web/live_auth.ex` returns `1`.
    - `grep -c 'retry_job:' lib/oban_powertools/web/live_auth.ex` returns `1`.
    - `grep -c 'cancel_job:' lib/oban_powertools/web/live_auth.ex` returns `1`.
    - `grep -c 'discard_job:' lib/oban_powertools/web/live_auth.ex` returns `1`.
    - `grep -cE '^[[:space:]]*jobs:' lib/oban_powertools/web/live_auth.ex` returns at least `1` (new `:jobs` banner entry).
    - `grep -cE '^[[:space:]]*job_detail:' lib/oban_powertools/web/live_auth.ex` returns `1`.
    - `grep -c 'Job list stays visible' lib/oban_powertools/web/live_auth.ex` returns `1` (per UI-SPEC).
    - `grep -c 'Job detail stays visible' lib/oban_powertools/web/live_auth.ex` returns `1` (per UI-SPEC).
    - `grep -c 'preview_repair:\|execute_repair:\|pause_cron_entry:' lib/oban_powertools/web/live_auth.ex` returns `3` (existing entries preserved).
    - `grep -c 'cron:\|lifeline:\|audit:\|workflows:\|forensics:' lib/oban_powertools/web/live_auth.ex` returns `5` (existing banner entries preserved).
    - `grep -cE 'live\("/jobs", ObanPowertools\.Web\.JobsLive, :index\)' lib/oban_powertools/web/router.ex` returns `1`.
    - `grep -cE 'live\("/jobs/:id", ObanPowertools\.Web\.JobsLive, :show\)' lib/oban_powertools/web/router.ex` returns `1`.
    - `mix compile --warnings-as-errors` exits 0.
    - Quick smoke: `iex -S mix` then `ObanPowertools.Web.LiveAuth.page_read_only_banner(:jobs)` returns the new banner string (no KeyError).
  </acceptance_criteria>
  <done>LiveAuth `@permission_messages` and `@page_read_only_banners` extended with the 5 + 2 new entries; existing entries untouched. Two new routes added inside the existing `live_session :oban_powertools_native` block. `mix compile --warnings-as-errors` exits 0. `LiveAuth.page_read_only_banner(:jobs)` no longer raises `KeyError`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement ObanPowertools.Web.JobsLive :index action (list page render, filter, paginate, push_patch)</name>
  <files>lib/oban_powertools/web/jobs_live.ex</files>
  <read_first>
    - lib/oban_powertools/web/workflows_live.ex (entire file — primary analog for double-route mount/3, handle_params/3 dispatch, render/1 layout shell)
    - lib/oban_powertools/web/lifeline_live.ex (lines 36-72 for handle_params + push_patch; lines 212-214 for read-only banner; lines 600-... for load_data shape; lines 983-988 for `view_toggle_class/1`; lines 1072-1096 for `timestamp_copy/1`; line 1273 for `defp repo/0`)
    - lib/oban_powertools/web/selectors.ex (the just-added `jobs_path/1` helper from 43-01)
    - lib/oban_powertools/jobs.ex (from 43-01 — `%ObanPowertools.Jobs{}` struct, `list/3`, `count_by_state/2`)
    - lib/oban_powertools/web/live_auth.ex (just-extended `@permission_messages`, `@page_read_only_banners`)
    - lib/oban_powertools/web/control_plane_presenter.ex (only the `native_banner/0` helper used in workflows_live.ex line 86 subtitle)
    - .planning/phases/43-read-only-job-browse/43-CONTEXT.md (D-06, D-07, D-08, D-09, D-12, D-13, D-15, D-17, D-18)
    - .planning/phases/43-read-only-job-browse/43-PATTERNS.md (jobs_live.ex pattern map — entire section)
    - .planning/phases/43-read-only-job-browse/43-UI-SPEC.md (Job List Page Component Inventory, Copywriting Contract for list-page strings, Interaction Contract Filter Flow + Tab Active State)
    - .planning/phases/43-read-only-job-browse/43-RESEARCH.md (Common Pitfalls 1, 2, 5, 6, 7 — branch-on-action auth, missing-state push_patch, tags param splitting, page_read_only_banner KeyError safety, selectors :jobs key safety)
  </read_first>
  <action>
Create `lib/oban_powertools/web/jobs_live.ex` containing `ObanPowertools.Web.JobsLive`. The module must:

**Module shell** — wrap the entire `defmodule` in `if Code.ensure_loaded?(Phoenix.LiveView) do ... end` (mandatory pattern per workflows_live.ex line 1 and lifeline_live.ex line 1). Declare `@moduledoc false`. `use Phoenix.LiveView`. Alias `ObanPowertools.{DisplayPolicy, Jobs}` and `ObanPowertools.Web.{ControlPlanePresenter, LiveAuth, Selectors}`. The alias `Jobs` is the new context module.

**`mount/3`** (Pitfall 1 — branch on `socket.assigns.live_action` per D-17/D-18):

- Pattern-match on `(params, %{"oban_dashboard_path" => dashboard_path}, socket)`.
- Set `action = socket.assigns.live_action`, then derive `permission`, `resource_type`, `resource_id`:
  - `:show` action → `:view_job_detail`, `:job`, `params["id"]`
  - else → `:view_jobs`, `:page`, `"jobs"`
- Open a `with {:ok, socket} <- LiveAuth.authorize_page(socket, permission, %{type: resource_type, id: resource_id}) do ... else {:error, socket} -> {:ok, socket} end` block.
- Inside the `do` block, call `:ok = DisplayPolicy.assert_configured!()` immediately.
- Return `{:ok, socket |> assign(:oban_dashboard_path, dashboard_path) |> assign_defaults()}` where `assign_defaults/1` initializes assigns: `:jobs` (empty list), `:filter` (`%Jobs{}` default — though tests will always re-derive), `:counts` (map with 7 string keys all `0`), `:job` (nil — Wave 3 will fill it), `:read_only?` (boolean derived from `LiveAuth.authorized?(actor, :retry_job, %{type: :page, id: "jobs"})` — `not authorized?` flips `:read_only?` true; this is the Phase 43 read-only banner gate per UI-SPEC §Unauthorized State).

**`handle_params/3`** — dispatch on action. Two clauses:

- First clause: pattern-match on `(%{"id" => id}, _uri, socket)` (the `:show` action). For Wave 2 this is a stub: `{:noreply, assign(socket, :job, nil) |> assign(:job_id, id)}`. Wave 3 will implement the real detail loader; the route must compile and not crash if hit.
- Second clause: `handle_params(params, _uri, socket)` — list-page logic. Per Pitfall 2 (RESEARCH.md), check `Map.get(params, "state")`:
  - If `nil`, `push_patch(socket, to: Selectors.jobs_path([{"state", "available"}]))` and return `{:noreply, socket_after_push}`. Do NOT run any query in this branch.
  - Otherwise, build a `%Jobs{}` filter via the private `filter_from_params/1` helper and call `load_jobs(socket, filter)` which runs `Jobs.list/3` and `Jobs.count_by_state/2`.

**`filter_from_params/1`** (private) — convert a params map to `%Jobs{}` per UI-SPEC tag parsing (Pitfall 5):

- `state` → `String.to_existing_atom(Map.get(params, "state", "available"))`.
- `queue` → `Map.get(params, "queue")` (nil-passthrough).
- `worker` → `Map.get(params, "worker")` (nil-passthrough).
- `tags` → split comma-separated string; nil and `""` map to `nil`; otherwise `str |> String.split(",") |> Enum.map(&String.trim/1) |> Enum.reject(&(&1 == ""))`.
- `page` → `String.to_integer(Map.get(params, "page", "1"))` (default `1`).
- `page_size` → fixed at `20`.

**`load_jobs/2`** (private) — run the data load and update assigns:

- Call `Jobs.list(repo(), filter)` → assign `:jobs`.
- Call `Jobs.count_by_state(repo(), filter)` → assign `:counts`.
- Assign `:filter` to the supplied filter.
- Recompute `:read_only?` (same gate as `mount/3` `assign_defaults`).

**`handle_event/3`** — three events per UI-SPEC §Interaction Contract:

- `"select_state"` with `%{"state" => state}` → `push_patch` to `Selectors.jobs_path/1` carrying the new state plus the current filter's queue/worker/tags/page=1 (state change resets page to 1).
- `"filter"` with `%{"filter" => %{"queue" => q, "worker" => w, "tags" => tags_str}}` → `push_patch` to `Selectors.jobs_path/1` carrying the current state and the new queue/worker/tags values; reset page to 1. Empty-string values must be passed as-is so `Selectors.encode/2` drops them.
- `"paginate"` with `%{"page" => page_str}` → `push_patch` to `Selectors.jobs_path/1` carrying the current filter unchanged plus the new `page` value.

`push_patch` callsites must build params via a private helper `filter_path/1` that emits the keyword list `[{"state", filter.state}, {"queue", filter.queue}, {"worker", filter.worker}, {"tags", filter.tags && Enum.join(filter.tags, ",")}, {"page", filter.page > 1 && to_string(filter.page)}]` and pass it to `Selectors.jobs_path/1` (nil values dropped by `Selectors.encode/2`).

**`render/1`** — return HEEX matching UI-SPEC component inventory:

- Outer wrapper: `div.space-y-6.p-6`.
- For the `:show` action (Wave 2 stub): render a single placeholder `div.text-sm.text-zinc-600` reading "Job detail loading — Wave 3 owns this view." This stub MUST be replaced by Wave 3; do not delete this comment in Wave 2.
- For the `:index` action:
  1. Header block: `h1.text-2xl.font-semibold` reading `Jobs`; `p.text-sm.text-zinc-600` reading `Browse and inspect Oban jobs by state. <%= ControlPlanePresenter.native_banner() %>`.
  2. Read-only banner: a `<p :if={@read_only?} class="rounded-lg border border-amber-200 bg-amber-50 px-4 py-3 text-sm text-amber-800">` containing `<%= LiveAuth.page_read_only_banner(:jobs) %>`.
  3. State tab bar: a `nav.flex.gap-2.flex-wrap` containing 7 buttons (one per state: `available`, `scheduled`, `executing`, `retryable`, `cancelled`, `discarded`, `completed` — order per D-09). Each button: `<button phx-click="select_state" phx-value-state={state} class={state_tab_class(@filter.state == String.to_existing_atom(state))}><%= state %> (<%= Map.get(@counts, state, 0) %>)</button>`. The active/inactive class is derived by the private `state_tab_class/1` helper using the exact classes from UI-SPEC §Component Inventory item 3 (font-semibold, not font-medium — UI-SPEC collapses to 2 weights). NOTE: the existing `LifelineLive.view_toggle_class/1` uses `font-medium` — do NOT copy that helper verbatim; the new helper must use `font-semibold` per UI-SPEC §Typography 2-weight constraint.
  4. Filter bar: `form phx-change="filter"` containing three inline selects/inputs — queue (select with options derived from distinct queues in `@jobs`, or a single text input — implementer's call within UI-SPEC bounds), worker (same), tags (text input). Labels per UI-SPEC Copywriting Contract: `Queue`, `Worker`, `Tags`. Placeholders: `All queues`, `All workers`, `Any tag`. The current filter values must populate the controls.
  5. Job table OR empty state:
     - If `@jobs == []`, render the empty state block (`div.rounded-lg.border.bg-white.p-6` with `h2.text-base.font-semibold` reading `No <%= @filter.state %> jobs` and `p.mt-2.text-sm.text-zinc-600` reading `No jobs are currently in the <%= @filter.state %> state. Try a different state or filter.`).
     - Otherwise render `div.overflow-hidden.rounded-lg.border.bg-white` wrapping `table.min-w-full.divide-y`. `thead.bg-slate-50.text-left.text-sm` with `th.px-4.py-3.font-semibold` columns in this order: `State`, `Worker`, `Queue`, `Job ID`, `Scheduled At`, `Attempts`. `tbody.divide-y.text-sm` with one `tr` per job. Each row is wrapped in or contains a navigate link to `/ops/jobs/jobs/#{job.id}` (use `<.link navigate={"/ops/jobs/jobs/#{job.id}"}>` per WorkflowsLive precedent — workflows_live.ex line 118 — or alternatively `phx-click="select_job" phx-value-id={job.id}` with `JS.navigate/1` on the click handler). Per D-12, render `<td>` cells in this order: state badge (with classes from UI-SPEC §Color state badge map), short worker name (last segment after splitting `worker` on `"."` via a private `short_worker_name/1` helper), queue, `job.id`, `timestamp_copy(job.scheduled_at)`, `<%= job.attempt %> / <%= job.max_attempts %>` (attempt count).
  6. Pagination block: `Previous` and `Next` buttons using `rounded border px-3 py-2 text-sm` per UI-SPEC. `Previous` disabled when `@filter.page <= 1` (adds `cursor-not-allowed text-zinc-400` and renders as a `span` instead of an interactive button). `Next` disabled when `length(@jobs) < @filter.page_size`. Active buttons emit `phx-click="paginate" phx-value-page={page}`.

**Private helpers** (defined at the bottom of the module):

- `defp repo, do: Application.fetch_env!(:oban_powertools, :repo)` — verbatim from workflows_live.ex line 472 / lifeline_live.ex line 1273.
- `defp short_worker_name(worker) when is_binary(worker), do: worker |> String.split(".") |> List.last()` and `defp short_worker_name(nil), do: "—"` — per D-12 + RESEARCH.md Code Examples.
- `defp timestamp_copy(...)` — copy verbatim from `lifeline_live.ex` lines 1072-1096 (handles nil, NaiveDateTime, DateTime, binary, anything-else → "Unknown"). Use `Calendar.strftime/2` exactly as in the analog.
- `defp state_tab_class(true), do: "rounded border border-indigo-300 bg-indigo-50 px-3 py-2 text-sm font-semibold text-indigo-700"` and `defp state_tab_class(false), do: "rounded border border-slate-200 bg-white px-3 py-2 text-sm font-semibold text-slate-600"`. Note `font-semibold`, not `font-medium`.
- `defp state_badge_class(state)` returning the exact Tailwind class string per UI-SPEC §Color state badge map (one clause per state string).
- `defp filter_path(filter)` (described above).
- `defp filter_from_params(params)` (described above).
- `defp assign_defaults(socket)` (described above).
- `defp load_jobs(socket, filter)` (described above).

Anti-patterns to avoid (per RESEARCH.md §Anti-Patterns):

- DO NOT query `Oban.Job` directly from the LiveView — every DB access goes through `Jobs.list/3`, `Jobs.get/2`, `Jobs.count_by_state/2`.
- DO NOT call `Oban` runtime functions (retry, cancel, discard) — Phase 43 is read-only per D-15.
- DO NOT create a new `live_session` for the routes — Task 1 added them to the existing block.
- DO NOT split `live_action` branching out via `Map.get(params, "id")` for permission selection — use `socket.assigns.live_action` per Pitfall 1.
- DO NOT pass the tags string through to `%Jobs{tags: "foo,bar"}` — split first per Pitfall 5.
- DO NOT hardcode `/ops/jobs` prefixes in templates — use `Selectors.jobs_path/1` and `"/ops/jobs/jobs/#{id}"` inline detail-path construction per Pattern 5.
- DO NOT inline `font-medium` in the state tab class — UI-SPEC mandates `font-semibold` for the 2-weight typography contract.
  </action>
  <verify>
    <automated>mix compile --warnings-as-errors 2>&amp;1 | grep -E "warning|error" | grep -v "Compiling" || echo "clean"</automated>
  </verify>
  <acceptance_criteria>
    - File `lib/oban_powertools/web/jobs_live.ex` exists.
    - `grep -c 'if Code.ensure_loaded?(Phoenix.LiveView) do' lib/oban_powertools/web/jobs_live.ex` returns `1`.
    - `grep -c 'defmodule ObanPowertools.Web.JobsLive do' lib/oban_powertools/web/jobs_live.ex` returns `1`.
    - `grep -c 'LiveAuth.authorize_page(socket, permission' lib/oban_powertools/web/jobs_live.ex` returns `1` (branching auth).
    - `grep -c ':view_jobs' lib/oban_powertools/web/jobs_live.ex` returns at least `1`.
    - `grep -c ':view_job_detail' lib/oban_powertools/web/jobs_live.ex` returns at least `1`.
    - `grep -c 'DisplayPolicy.assert_configured!()' lib/oban_powertools/web/jobs_live.ex` returns `1`.
    - `grep -c 'Jobs.list(' lib/oban_powertools/web/jobs_live.ex` returns at least `1`.
    - `grep -c 'Jobs.count_by_state(' lib/oban_powertools/web/jobs_live.ex` returns at least `1`.
    - `grep -c 'Selectors.jobs_path' lib/oban_powertools/web/jobs_live.ex` returns at least `2` (default-state push_patch + filter_path).
    - `grep -c 'push_patch' lib/oban_powertools/web/jobs_live.ex` returns at least `2`.
    - `grep -cE 'handle_event\("(select_state|filter|paginate)"' lib/oban_powertools/web/jobs_live.ex` returns `3`.
    - `grep -c 'String.to_existing_atom' lib/oban_powertools/web/jobs_live.ex` returns at least `1`.
    - `grep -c 'String.split(",")' lib/oban_powertools/web/jobs_live.ex` returns at least `1` (tags parsing per Pitfall 5).
    - `grep -c 'page_read_only_banner(:jobs)' lib/oban_powertools/web/jobs_live.ex` returns at least `1`.
    - `grep -c 'state_tab_class' lib/oban_powertools/web/jobs_live.ex` returns at least `3` (2 defs + at least 1 usage in render).
    - `grep -c 'font-semibold' lib/oban_powertools/web/jobs_live.ex` returns at least `2` (state tabs + table headers); `grep -c 'font-medium' lib/oban_powertools/web/jobs_live.ex` returns `0` (UI-SPEC 2-weight constraint).
    - `grep -c 'defp repo' lib/oban_powertools/web/jobs_live.ex` returns `1`.
    - `grep -c 'defp short_worker_name' lib/oban_powertools/web/jobs_live.ex` returns at least `1`.
    - `grep -c 'defp timestamp_copy' lib/oban_powertools/web/jobs_live.ex` returns at least `3` (nil, NaiveDateTime, DateTime clauses minimum).
    - `grep -cE 'available|scheduled|executing|retryable|cancelled|discarded|completed' lib/oban_powertools/web/jobs_live.ex` returns at least `7` (all state strings referenced in tabs).
    - `mix compile --warnings-as-errors` exits 0.
  </acceptance_criteria>
  <done>`JobsLive` compiles cleanly. `mount/3` branches on `live_action` for the two permission atoms per D-17/D-18. `handle_params/3` redirects via `push_patch` to `?state=available` when the state param is missing (Pitfall 2). Filter, state-tab, and paginate events all push URL-patches through `Selectors.jobs_path/1`. The render emits the UI-SPEC list-page structure including the 7-tab state bar, filter form, job table, empty state, and pagination — all under `font-semibold` typography. The `:show` action compiles with a stub render — Wave 3 will own it.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Write LiveView integration tests for JobsLive :index action</name>
  <files>test/oban_powertools/web/live/jobs_live_test.exs</files>
  <read_first>
    - test/oban_powertools/web/live/workflows_live_test.exs (analog: test-display-policy module pattern at the top of the file, `setup` block that swaps `Application.put_env(:oban_powertools, :display_policy, ...)` with `on_exit` restoration, `Plug.Test.init_test_session` with `current_actor`, `live(conn, "/ops/jobs/...")` calls, `assert {:error, {:redirect, %{to: "/"}}}` for unauthorized, `has_element?/2` usage)
    - test/support/live_case.ex (the `use ObanPowertools.LiveCase, async: false` macro and sandbox checkout)
    - lib/oban_powertools/web/jobs_live.ex (the LiveView under test — read after Task 2 commits)
    - lib/oban_powertools/jobs.ex (the data layer — needed to insert fixtures)
    - .planning/phases/43-read-only-job-browse/43-RESEARCH.md (Validation Architecture → Phase Requirements → Test Map; specifically the QRY-01 rows)
    - .planning/phases/43-read-only-job-browse/43-UI-SPEC.md (Copywriting Contract for assertion strings)
  </read_first>
  <action>
Create `test/oban_powertools/web/live/jobs_live_test.exs`. Open with a small inline display-policy module per the WorkflowsLive precedent:

`defmodule ObanPowertools.Web.JobsLiveTestDisplayPolicy do` with `def display(:job_args, _value, _context), do: nil`, `def display(:job_meta, _value, _context), do: nil`, and a catch-all `def display(_kind, _value, _context), do: nil`. (Wave 3 will exercise the redacting code paths in detail tests; Wave 2 only needs the inline policy to mount the list page.)

Then `defmodule ObanPowertools.Web.JobsLiveTest do use ObanPowertools.LiveCase, async: false` with a `setup` block that:

- Saves `original_display_policy = Application.get_env(:oban_powertools, :display_policy)`.
- Calls `Application.put_env(:oban_powertools, :display_policy, ObanPowertools.Web.JobsLiveTestDisplayPolicy)`.
- Restores in `on_exit`.

Add a local fixture helper `defp insert_job!(attrs)` that mirrors the one from `jobs_test.exs` (Wave 1) but lives in the test module. Insert via `TestRepo.insert!(Oban.Job.new(...))`. For tests that need a non-default state, follow up with `Ecto.Changeset.change/2` + `TestRepo.update!/1`.

Write at minimum these test cases (each as a separate `test "..." block`):

1. **"redirects unauthorized viewers"** — `current_actor: %{id: "ops-2", permissions: []}`, assert `{:error, {:redirect, %{to: "/"}}}` from `live(conn, "/ops/jobs/jobs")`.

2. **"redirects when no state param then loads default state"** — `current_actor: %{id: "ops-1", permissions: [:view_jobs]}`. `live(conn, "/ops/jobs/jobs")` returns `{:error, {:live_redirect, %{to: "/ops/jobs/jobs?state=available"}}}` OR a redirect-to-self pattern (consult workflows_live_test.exs `assert_patch/2` if the response shape is a soft patch; Phoenix LiveView 1.1.30 emits `{:live_redirect, ...}` for `push_patch` during mount). The assertion that matters: after the auto-patch, the URL contains `state=available`.

3. **"renders list page with state tabs and headings"** — actor with `:view_jobs`. Insert 2 available jobs and 1 executing job. Call `live(conn, "/ops/jobs/jobs?state=available")`. Assert the rendered HTML contains:
   - `"Jobs"` (h1)
   - `"Browse and inspect Oban jobs by state."`
   - the 7 state names verbatim: `available`, `scheduled`, `executing`, `retryable`, `cancelled`, `discarded`, `completed`
   - the per-state counts in the tab bar — at minimum `available (2)` and `executing (1)` substrings
   - exactly 2 table rows for the available state.

4. **"filters by queue via push_patch"** — actor with `:view_jobs`. Insert 1 available job with `queue: "default"` and 1 with `queue: "alpha"`. Render `live(conn, "/ops/jobs/jobs?state=available")`. Use `view |> form("form[phx-change=filter]", filter: %{queue: "alpha", worker: "", tags: ""}) |> render_change()` (or the appropriate `render_change` shape for the form). Assert the resulting HTML contains the alpha queue row but not the default queue row, and the URL after the patch contains `queue=alpha`. (Use `assert_patch/2` from LiveViewTest if the form fires a push_patch.)

5. **"filters by worker"** — similar shape, two jobs with different workers, assert filtering narrows.

6. **"filters by tags"** — two jobs with different tags, assert `?tags=foo` narrows the list to the single foo-tagged job. This implicitly tests the comma-split logic via the URL bound.

7. **"navigates state via tab click"** — actor with `:view_jobs`. Insert 1 executing job. Render `live(conn, "/ops/jobs/jobs?state=available")`. Click the executing tab via `view |> element("button[phx-value-state=executing]") |> render_click()`. Assert the URL after the click contains `state=executing` and the executing job's id is in the HTML.

8. **"read-only banner renders when actor lacks mutation permissions"** — actor with only `:view_jobs` (no `:retry_job` etc.). Render the list page. Assert HTML contains `"Permission: read-only. Job list stays visible"`.

9. **"hides read-only banner when actor has retry_job permission"** — actor with `[:view_jobs, :retry_job]`. Render the list page. Assert HTML does NOT contain `"Job list stays visible"` (verify gate flips correctly).

10. **"renders empty state when no jobs match the filter"** — actor with `:view_jobs`. Insert zero jobs. Render `live(conn, "/ops/jobs/jobs?state=available")`. Assert HTML contains `"No available jobs"` and `"No jobs are currently in the available state."`.

Each test must include `Plug.Test.init_test_session(conn, current_actor: %{id: ..., permissions: [...]})` before the `live(conn, url)` call, matching the WorkflowsLive precedent. Do NOT mock `LiveAuth` — use the real `current_actor` permissions list, since the underlying auth callback resolves through `Auth.authorization_outcome/3` against actor permissions.
  </action>
  <verify>
    <automated>mix test test/oban_powertools/web/live/jobs_live_test.exs --max-failures 1</automated>
  </verify>
  <acceptance_criteria>
    - File `test/oban_powertools/web/live/jobs_live_test.exs` exists.
    - `grep -c 'defmodule ObanPowertools.Web.JobsLiveTestDisplayPolicy' test/oban_powertools/web/live/jobs_live_test.exs` returns `1`.
    - `grep -c 'defmodule ObanPowertools.Web.JobsLiveTest' test/oban_powertools/web/live/jobs_live_test.exs` returns `1`.
    - `grep -c 'use ObanPowertools.LiveCase' test/oban_powertools/web/live/jobs_live_test.exs` returns `1`.
    - `grep -c 'Application.put_env(:oban_powertools, :display_policy' test/oban_powertools/web/live/jobs_live_test.exs` returns `1`.
    - `grep -cE '^[[:space:]]*test "' test/oban_powertools/web/live/jobs_live_test.exs` returns at least `10`.
    - `grep -c 'redirects unauthorized' test/oban_powertools/web/live/jobs_live_test.exs` returns at least `1`.
    - `grep -c 'state=available' test/oban_powertools/web/live/jobs_live_test.exs` returns at least `2` (default-state redirect + assertion bodies).
    - `grep -c ':view_jobs' test/oban_powertools/web/live/jobs_live_test.exs` returns at least `8` (most authorized tests carry this permission).
    - `grep -c 'Job list stays visible' test/oban_powertools/web/live/jobs_live_test.exs` returns at least `2` (read-only banner ON + OFF assertions).
    - `grep -c 'No available jobs' test/oban_powertools/web/live/jobs_live_test.exs` returns at least `1` (empty state).
    - `mix test test/oban_powertools/web/live/jobs_live_test.exs` exits 0 (all 10+ tests pass).
  </acceptance_criteria>
  <done>All 10+ LiveView integration tests pass. The tests cover unauthorized redirect, default-state push_patch, list render with state tabs and counts, queue/worker/tags filtering, tab navigation, read-only banner gate (both directions), and empty state — collectively proving QRY-01 acceptance criteria 1, 2, 4, 5 (browse, narrow, URL serialization, unauthorized banner) end-to-end. The :show action is not yet covered by tests — Wave 3 owns that.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → LiveView mount | Untrusted URL params reach `mount/3` (`params["id"]`, `params["state"]`, `params["queue"]`, `params["worker"]`, `params["tags"]`, `params["page"]`). |
| LiveView → ObanPowertools.Jobs | Parameterized Ecto query — no string interpolation; SQL injection impossible. |
| session → LiveAuth | `current_actor` comes from the host-supplied session map; permission atoms are checked via `Auth.authorization_outcome/3`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-43-02-01 | Spoofing / Elevation of Privilege | `mount/3` permission gate | mitigate | `LiveAuth.authorize_page(socket, :view_jobs, ...)` (for `:index`) and `LiveAuth.authorize_page(socket, :view_job_detail, ...)` (for `:show`) are called BEFORE any data load, per existing pattern. Test #1 asserts unauthorized actors redirect to `/`. |
| T-43-02-02 | Information Disclosure | unauthorized actor sees job detail by hand-crafting `/ops/jobs/jobs/:id` | mitigate | `:show` action's `mount/3` branch uses `:view_job_detail` permission (D-18). Phase 43 ships the auth check; Wave 3's render exposes redacted args/meta. Even with the Wave 2 stub render, the auth gate fires before any controller action. |
| T-43-02-03 | Tampering | URL param injection (`state=DROP TABLE`, `tags=' OR 1=1`) | mitigate | `state` is converted via `String.to_existing_atom/1` — unknown atoms raise `ArgumentError` before reaching Ecto. `queue`, `worker`, `tags` are bound as Ecto parameters by `Jobs.list/3` (Wave 1 mitigation already in place). |
| T-43-02-04 | Denial of Service | atom-table exhaustion via `String.to_atom(state)` | mitigate | `filter_from_params/1` uses `String.to_existing_atom/1` — attacker-supplied state strings outside the 7 known states raise instead of growing the atom table. |
| T-43-02-05 | Denial of Service | unbounded list size | mitigate | `Jobs.list/3` enforces `limit(^filter.page_size)` (default 20, hardcoded in `filter_from_params/1` — not user-controllable). Pagination via `?page=N` is integer-only via `String.to_integer/1`, which raises on non-numeric input rather than running unbounded queries. |
| T-43-02-06 | Repudiation | none | accept | Phase 43 is read-only; no mutations occur. Audit writes start in Phase 44. |
| T-43-02-07 | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan. |
</threat_model>

<verification>
- `mix compile --warnings-as-errors` exits 0 after each task.
- `mix test test/oban_powertools/web/live/jobs_live_test.exs` exits 0.
- `mix test` exits 0 (no regressions in `workflows_live_test.exs`, `lifeline_live_test.exs`, etc.).
- `iex -S mix` smoke: `ObanPowertools.Web.LiveAuth.permission_message(:view_jobs)` returns the new permission message string; `ObanPowertools.Web.LiveAuth.page_read_only_banner(:jobs)` returns the new banner string.
- D-02, D-06, D-07, D-09, D-12, D-13, D-15, D-17, D-19, D-20 are observable in source via the grep gates above.
</verification>

<success_criteria>
1. `LiveAuth.@permission_messages` contains `:view_jobs`, `:view_job_detail`, `:retry_job`, `:cancel_job`, `:discard_job` per D-20.
2. `LiveAuth.@page_read_only_banners` contains `:jobs` and `:job_detail` with UI-SPEC copy per D-19.
3. Router contains `live("/jobs", ObanPowertools.Web.JobsLive, :index)` and `live("/jobs/:id", ObanPowertools.Web.JobsLive, :show)` inside the existing `live_session :oban_powertools_native` block per D-06.
4. `JobsLive.mount/3` branches on `live_action` to choose `:view_jobs` vs `:view_job_detail` per D-17/D-18.
5. `JobsLive.handle_params/3` issues `push_patch` to `?state=available` when no state param is present (Pitfall 2).
6. `JobsLive` exposes a 7-tab state bar per D-09, queue/worker/tags filter form per D-07, and a job table per D-12.
7. All 10+ LiveView integration tests pass.
8. UI-SPEC typography contract: `font-semibold` only — `grep -c 'font-medium' lib/oban_powertools/web/jobs_live.ex` returns `0`.
9. `mix compile --warnings-as-errors` exits 0; `mix test` exits 0.
</success_criteria>

<output>
Create `.planning/phases/43-read-only-job-browse/43-02-SUMMARY.md` when done. Include:
- File-by-file diff summary
- Routes added (literal `live(...)` calls)
- New LiveAuth atoms and banner entries (literal strings)
- LiveView test counts (passed/skipped/excluded)
- Confirmation that D-02, D-06, D-07, D-09, D-12, D-13, D-15, D-17, D-19, D-20 are implemented
- Note that the `:show` action render is a stub awaiting Wave 3
</output>
