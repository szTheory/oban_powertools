---
phase: 20-cancellation-late-completion-expiry-semantics
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/oban_powertools/workflow/runtime.ex
  - lib/oban_powertools/workflow/workflow.ex
  - lib/oban_powertools/workflow/step.ex
  - lib/oban_powertools/workflow/signal_record.ex
  - lib/oban_powertools/workflow/command_attempt.ex
  - lib/mix/tasks/oban_powertools.install.ex
  - test/support/migrations/2_phase_3_tables.exs
  - examples/phoenix_host/priv/repo/migrations/20260522000024_oban_powertools_workflow_semantics.exs
  - examples/phoenix_host_upgrade_source/priv/repo/migrations/20260522000024_oban_powertools_workflow_semantics.exs
  - test/oban_powertools/workflow_runtime_test.exs
autonomous: true
requirements:
  - REC-03
  - SIG-03
  - VER-02
must_haves:
  truths:
    - "Cancel request evidence is durable but not itself the final workflow or step outcome."
    - "One canonical reducer derives final workflow and step truth from durable facts after reconciliation."
    - "Late or post-cancel evidence uses a bounded vocabulary that stays semver-safe and support-truthful."
  artifacts:
    - path: "lib/oban_powertools/workflow/runtime.ex"
      provides: "Canonical request/evidence/outcome reducer plus bounded precedence rules"
    - path: "lib/oban_powertools/workflow/workflow.ex"
      provides: "Workflow-row fields and validations aligned to request-versus-outcome semantics"
    - path: "lib/oban_powertools/workflow/step.ex"
      provides: "Step-row fields and validations aligned to cooperative cancel plus terminal-truth semantics"
    - path: "lib/oban_powertools/workflow/signal_record.ex"
      provides: "Late-signal status vocabulary that stays coherent with expiry and post-terminal evidence policy"
    - path: "lib/oban_powertools/workflow/command_attempt.ex"
      provides: "Append-only evidence posture for cancel-request, duplicate, and post-terminal arrival stories"
    - path: "lib/mix/tasks/oban_powertools.install.ex"
      provides: "Supported-host schema contract for any new bounded cause or evidence fields"
    - path: "test/oban_powertools/workflow_runtime_test.exs"
      provides: "Proof that request evidence and final truth no longer drift"
  key_links:
    - from: "lib/oban_powertools/workflow/runtime.ex"
      to: "lib/oban_powertools/workflow/workflow.ex"
      via: "runtime reduction rules and durable workflow fields must agree on request-versus-outcome semantics"
      pattern: "cancel_requested_at|terminal_cause|refresh_workflow|workflow_terminal_cause"
    - from: "lib/oban_powertools/workflow/runtime.ex"
      to: "lib/oban_powertools/workflow/step.ex"
      via: "step terminal causes and cancel metadata must reduce through one canonical runtime path"
      pattern: "terminal_cause_for_status|step_diagnosis|cancel_requested_at"
---

<objective>
Introduce one canonical request/evidence/outcome reducer and align the durable workflow, step, signal, and command-evidence contract around Phase 20’s bounded race semantics.

Purpose: stop request evidence from masquerading as final truth and create the bounded vocabulary needed for truthful cancel, expiry, and late-arrival handling.
Output: centralized precedence logic, additive durable fields or statuses where required, and schema parity across repo and supported hosts.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/20-cancellation-late-completion-expiry-semantics/20-CONTEXT.md
@.planning/phases/20-cancellation-late-completion-expiry-semantics/20-RESEARCH.md
@.planning/phases/20-cancellation-late-completion-expiry-semantics/20-PATTERNS.md
@.planning/phases/19-await-registration-signal-facts-expiry-authority/19-03-SUMMARY.md
@lib/oban_powertools/workflow/runtime.ex
@lib/oban_powertools/workflow/workflow.ex
@lib/oban_powertools/workflow/step.ex
@lib/oban_powertools/workflow/signal_record.ex
@lib/oban_powertools/workflow/command_attempt.ex
@lib/mix/tasks/oban_powertools.install.ex
@test/support/migrations/2_phase_3_tables.exs
@test/oban_powertools/workflow_runtime_test.exs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Centralize final workflow and step reduction around request-versus-outcome truth</name>
  <files>lib/oban_powertools/workflow/runtime.ex, lib/oban_powertools/workflow/workflow.ex, lib/oban_powertools/workflow/step.ex, test/oban_powertools/workflow_runtime_test.exs</files>
  <read_first>.planning/phases/20-cancellation-late-completion-expiry-semantics/20-CONTEXT.md, .planning/phases/20-cancellation-late-completion-expiry-semantics/20-RESEARCH.md, lib/oban_powertools/workflow/runtime.ex, lib/oban_powertools/workflow/workflow.ex, lib/oban_powertools/workflow/step.ex</read_first>
  <action>Per D-05-D-10, D-19-D-20, and D-24-D-25, extract or refactor one canonical reducer inside `ObanPowertools.Workflow.Runtime` that turns durable request evidence plus step facts into final workflow and step outcomes. Remove any logic path where `cancel_requested` or a transient pre-refresh terminal cause can outrank later durable truth. Keep the reducer bounded and repo-local: final states still flow from `Workflow` and `Step` rows, not from PubSub, host callback timing, or new in-memory state. Preserve existing `completed_after_cancel_request` semantics and add only the minimum new bounded causes needed for post-cancel failure or other race outcomes the context explicitly requires.</action>
  <acceptance_criteria>
    - `cancel_requested_at` remains durable request evidence rather than a final outcome by itself.
    - Workflow and step final causes reduce from durable facts in one canonical runtime path.
    - Existing `completed_after_cancel_request` semantics remain intact.
    - Diagnosis and callback code can consume the same reducer without bespoke precedence forks.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/oban_powertools/workflow_runtime_test.exs</automated>
  </verify>
  <done>The runtime owns one coherent request/evidence/outcome contract instead of several partially overlapping precedence branches.</done>
</task>

<task type="auto">
  <name>Task 2: Land bounded durable evidence vocabulary and keep schema parity across supported hosts</name>
  <files>lib/oban_powertools/workflow/signal_record.ex, lib/oban_powertools/workflow/command_attempt.ex, lib/mix/tasks/oban_powertools.install.ex, test/support/migrations/2_phase_3_tables.exs, examples/phoenix_host/priv/repo/migrations/20260522000024_oban_powertools_workflow_semantics.exs, examples/phoenix_host_upgrade_source/priv/repo/migrations/20260522000024_oban_powertools_workflow_semantics.exs, test/oban_powertools/workflow_runtime_test.exs</files>
  <read_first>.planning/phases/20-cancellation-late-completion-expiry-semantics/20-CONTEXT.md, lib/oban_powertools/workflow/signal_record.ex, lib/oban_powertools/workflow/command_attempt.ex, lib/mix/tasks/oban_powertools.install.ex, test/support/migrations/2_phase_3_tables.exs</read_first>
  <action>Per D-18-D-23 and VER-02, define the bounded durable vocabulary needed for Phase 20’s late and post-cancel evidence stories. Reuse `SignalRecord` and `CommandAttempt` where they already fit, and add new durable fields or narrow append-only evidence only if the existing rows cannot explain late completion, late failure, or post-terminal duplicate arrival supportably. If schemas change, update runtime schemas, installer migration, repo test migration, and both supported-host example migrations together in one pass. Keep the contract narrow: no generic event history bus, no unbounded free-form evidence taxonomy, and no second source of truth beside the canonical workflow/step rows.</action>
  <acceptance_criteria>
    - Phase 20 has a bounded durable vocabulary for the late or post-cancel cases it owns.
    - Canonical workflow and step rows remain authoritative for final outcome.
    - Late or duplicate evidence is preserved append-only rather than rewriting final truth.
    - Runtime schema and supported-host migrations stay in lockstep.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/oban_powertools/workflow_runtime_test.exs</automated>
    <automated>rg -n "cancel_requested_at|completed_after_cancel_request|expired_wait|late|operator_cancelled|terminal_cause" lib/mix/tasks/oban_powertools.install.ex test/support/migrations/2_phase_3_tables.exs examples/phoenix_host/priv/repo/migrations examples/phoenix_host_upgrade_source/priv/repo/migrations</automated>
  </verify>
  <done>The repo and supported hosts share one bounded durable contract for request evidence, final truth, and late-arrival evidence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Cancel request evidence -> final workflow truth | Request timestamps must not be mistaken for terminal meaning once later facts arrive. |
| Canonical workflow or step truth -> append-only evidence | Late or duplicate arrivals must enrich support history without mutating final truth. |
| Runtime schema -> installer and supported hosts | If bounded cause or evidence fields drift across migrations, support truth breaks immediately. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-01 | T | outcome reduction | mitigate | Centralize request/evidence/outcome reduction in one runtime-owned path and eliminate precedence forks. |
| T-20-02 | I | durable cause vocabulary | mitigate | Keep a bounded, explicit vocabulary for late and post-cancel cases instead of opaque metadata or transient wording. |
| T-20-03 | D | schema parity | mitigate | Land runtime and supported-host schema changes together and verify them with grep parity checks. |
</threat_model>

<verification>
- `mix test test/oban_powertools/workflow_runtime_test.exs`
- `rg -n "cancel_requested_at|completed_after_cancel_request|expired_wait|late|operator_cancelled|terminal_cause" lib/mix/tasks/oban_powertools.install.ex test/support/migrations/2_phase_3_tables.exs examples/phoenix_host/priv/repo/migrations examples/phoenix_host_upgrade_source/priv/repo/migrations`
</verification>

<success_criteria>
- Request evidence and final truth are reduced by one canonical runtime contract.
- The durable vocabulary for late or post-cancel evidence stays bounded and support-truthful.
- Repo and supported-host schemas remain aligned if new fields or statuses land.
</success_criteria>

<output>
After completion, create `.planning/phases/20-cancellation-late-completion-expiry-semantics/20-01-SUMMARY.md`
</output>
