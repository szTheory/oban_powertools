---
phase: 20-cancellation-late-completion-expiry-semantics
plan: 02
type: execute
wave: 2
depends_on:
  - 20-01
files_modified:
  - lib/oban_powertools/workflow/runtime.ex
  - lib/oban_powertools/workflow.ex
  - lib/oban_powertools/explain.ex
  - test/oban_powertools/workflow_runtime_test.exs
  - test/oban_powertools/workflow_coordinator_test.exs
  - test/oban_powertools/explain_test.exs
autonomous: true
requirements:
  - REC-03
  - SIG-03
  - DIA-01
  - VER-01
must_haves:
  truths:
    - "Cancellation is eager for idle work and cooperative for in-flight work."
    - "Diagnosis and callback surfaces present final truth before lingering request evidence."
    - "Late signal and post-cancel outcome stories remain durable and explainable without reopening terminal truth."
  artifacts:
    - path: "lib/oban_powertools/workflow/runtime.ex"
      provides: "Cooperative cancel propagation, truthful terminal callback emission, and shared diagnosis ordering"
    - path: "lib/oban_powertools/workflow.ex"
      provides: "Public paved-road workflow API that preserves narrow semantics while runtime behavior hardens"
    - path: "lib/oban_powertools/explain.ex"
      provides: "Support-facing workflow and step stories that inherit terminal-truth-first diagnosis"
    - path: "test/oban_powertools/workflow_runtime_test.exs"
      provides: "Proof for cancel-versus-complete, cancel-versus-failure, cancel-versus-expiry, and terminal callback truth"
    - path: "test/oban_powertools/workflow_coordinator_test.exs"
      provides: "Proof that advisory wakeup or duplicate signals cannot override the canonical race outcome"
    - path: "test/oban_powertools/explain_test.exs"
      provides: "Proof that explain helpers report final truth before request evidence"
  key_links:
    - from: "lib/oban_powertools/workflow/runtime.ex"
      to: "lib/oban_powertools/explain.ex"
      via: "runtime diagnosis ordering must flow directly into support-facing explain helpers"
      pattern: "workflow_diagnosis|step_diagnosis|workflow_story|step_story"
    - from: "lib/oban_powertools/workflow/runtime.ex"
      to: "test/oban_powertools/workflow_runtime_test.exs"
      via: "cancel propagation, race outcomes, and terminal callback payloads are all proven at the runtime seam"
      pattern: "request_cancel|completed_after_cancel_request|expired_wait|workflow.terminal"
---

<objective>
Harden cooperative cancellation, downstream suppression, terminal callback truth, and terminal-truth-first diagnosis on top of the bounded reducer from Plan 20-01.

Purpose: make real cancel, completion, expiry, and late-signal races support-truthful without widening the public API or pulling UI work ahead of Phase 21.
Output: corrected cancel propagation behavior, truthful callback payloads, and diagnosis helpers that describe final truth before request evidence.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/20-cancellation-late-completion-expiry-semantics/20-CONTEXT.md
@.planning/phases/20-cancellation-late-completion-expiry-semantics/20-RESEARCH.md
@.planning/phases/20-cancellation-late-completion-expiry-semantics/20-PATTERNS.md
@.planning/phases/20-cancellation-late-completion-expiry-semantics/20-01-PLAN.md
@lib/oban_powertools/workflow/runtime.ex
@lib/oban_powertools/workflow.ex
@lib/oban_powertools/explain.ex
@test/oban_powertools/workflow_runtime_test.exs
@test/oban_powertools/workflow_coordinator_test.exs
@test/oban_powertools/explain_test.exs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Make cancel propagation cooperative for in-flight work and suppress new downstream scheduling truthfully</name>
  <files>lib/oban_powertools/workflow/runtime.ex, lib/oban_powertools/workflow.ex, test/oban_powertools/workflow_runtime_test.exs, test/oban_powertools/workflow_coordinator_test.exs</files>
  <read_first>.planning/phases/20-cancellation-late-completion-expiry-semantics/20-CONTEXT.md, .planning/phases/20-cancellation-late-completion-expiry-semantics/20-RESEARCH.md, lib/oban_powertools/workflow/runtime.ex, test/oban_powertools/workflow_runtime_test.exs</read_first>
  <action>Per D-11-D-18 and D-28-D-30, tighten `request_cancel` semantics so idle work (`pending`, `available`, and `awaiting_signal`, plus truly queued retry work if distinguishable) cancels eagerly while executing or otherwise in-flight work settles cooperatively to its real final truth. Keep cancel request evidence durable on workflow and step rows, suppress new downstream scheduling after cancel request except where a downstream step is already durably entitled to proceed under existing dependency semantics, and ensure dependency failure does not get erased by an over-broad cancel-always-wins rule. Reuse the canonical reducer from Plan 20-01 rather than layering new one-off conditionals into reconcile.</action>
  <acceptance_criteria>
    - Idle work cancels immediately on request.
    - In-flight work can still settle to completion or failure after cancel request without being rewritten to plain cancellation.
    - New downstream scheduling is suppressed after cancel request unless already durably entitled.
    - Dependency-policy-local failure behavior remains intact.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/oban_powertools/workflow_runtime_test.exs test/oban_powertools/workflow_coordinator_test.exs</automated>
  </verify>
  <done>Cancel propagation is explicit, cooperative, and consistent with the locked request/evidence/outcome model.</done>
</task>

<task type="auto">
  <name>Task 2: Make diagnosis helpers and terminal callbacks present final truth before request evidence</name>
  <files>lib/oban_powertools/workflow/runtime.ex, lib/oban_powertools/explain.ex, test/oban_powertools/workflow_runtime_test.exs, test/oban_powertools/explain_test.exs</files>
  <read_first>.planning/phases/20-cancellation-late-completion-expiry-semantics/20-CONTEXT.md, lib/oban_powertools/workflow/runtime.ex, lib/oban_powertools/explain.ex, test/oban_powertools/explain_test.exs</read_first>
  <action>Per D-24-D-27 and VER-01, refactor runtime diagnosis ordering and terminal callback emission so both surfaces present the true final workflow or step outcome once known. `cancel_requested` should remain visible as durable evidence, but it must not outrank `completed_after_cancel_request`, expiry, or any bounded post-cancel failure cause after reconciliation settles. Keep callback semantics narrow: `workflow.terminal` still emits only one truthful final state transition with supportable metadata, not an event stream. If additional payload keys are needed to explain request-versus-outcome semantics, add only bounded low-cardinality fields that follow the same support-truth posture as the rest of v1.2.</action>
  <acceptance_criteria>
    - `workflow_diagnosis/2` and `step_diagnosis/1` prefer terminal truth over request evidence once settled.
    - `ObanPowertools.Explain` surfaces inherit that corrected ordering without custom overrides.
    - `workflow.terminal` payloads reflect the true final state after cancel races.
    - Late signals or duplicate post-terminal arrivals remain evidence only and do not reopen terminal truth.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/oban_powertools/workflow_runtime_test.exs test/oban_powertools/explain_test.exs</automated>
    <automated>rg -n "completed_after_cancel_request|cancel_requested|expired_wait|workflow\\.terminal|terminal_cause" test/oban_powertools/workflow_runtime_test.exs test/oban_powertools/explain_test.exs lib/oban_powertools/workflow/runtime.ex lib/oban_powertools/explain.ex</automated>
  </verify>
  <done>Support-facing diagnosis and callback stories become truthful consumers of the same runtime semantics instead of special-case wording layers.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Cancel request -> in-flight execution truth | A request must not overrule the actual outcome of already-running work. |
| Runtime reduction -> explain and callback consumers | Support-facing readers must inherit canonical truth instead of inventing their own precedence. |
| Advisory signals or wakeups -> terminal workflow meaning | Late or duplicate arrivals must not reopen or rewrite a settled terminal outcome. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-04 | T | cancel propagation | mitigate | Distinguish eager idle cancellation from cooperative in-flight settlement and suppress downstream scheduling through the shared reducer. |
| T-20-05 | I | diagnosis and callback truth | mitigate | Make terminal-truth-first ordering runtime-owned so explain and callback surfaces cannot drift. |
| T-20-06 | D | late or duplicate arrivals | mitigate | Preserve them as evidence only and prove they cannot reopen terminal workflow truth. |
</threat_model>

<verification>
- `mix test test/oban_powertools/workflow_runtime_test.exs test/oban_powertools/workflow_coordinator_test.exs`
- `mix test test/oban_powertools/workflow_runtime_test.exs test/oban_powertools/explain_test.exs`
- `rg -n "completed_after_cancel_request|cancel_requested|expired_wait|workflow\\.terminal|terminal_cause" test/oban_powertools/workflow_runtime_test.exs test/oban_powertools/explain_test.exs lib/oban_powertools/workflow/runtime.ex lib/oban_powertools/explain.ex`
</verification>

<success_criteria>
- Cooperative cancel semantics match the locked Phase 20 defaults.
- Terminal-truth-first diagnosis flows from runtime helpers into explain surfaces automatically.
- Terminal callback payloads stay narrow and truthful after cancel, expiry, and late-arrival races.
</success_criteria>

<output>
After completion, create `.planning/phases/20-cancellation-late-completion-expiry-semantics/20-02-SUMMARY.md`
</output>
