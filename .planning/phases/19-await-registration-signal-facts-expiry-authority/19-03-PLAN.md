---
phase: 19-await-registration-signal-facts-expiry-authority
plan: 03
type: execute
wave: 3
depends_on:
  - 19-01
  - 19-02
files_modified:
  - lib/oban_powertools/workflow/runtime.ex
  - test/support/example_host_contract.ex
  - test/oban_powertools/example_host_contract_test.exs
  - test/oban_powertools/workflow_runtime_test.exs
  - test/oban_powertools/workflow_coordinator_test.exs
  - .planning/PROJECT.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements:
  - SIG-03
  - VER-01
  - VER-02
must_haves:
  truths:
    - "Wait expiry is finalized through one authoritative DB-first reconciliation path."
    - "Late signals are preserved as evidence after expiry without reopening or reclassifying the expired wait."
    - "The repo proves duplicate, late, dropped, and lost-wakeup paths and aligns planning truth to the semantics actually shipped."
  artifacts:
    - path: "lib/oban_powertools/workflow/runtime.ex"
      provides: "Single authoritative expiry-finalization path and late-signal evidence handling"
    - path: "test/oban_powertools/workflow_runtime_test.exs"
      provides: "Focused proof for expiry, late-arrival, and authoritative reconcile outcomes"
    - path: "test/oban_powertools/workflow_coordinator_test.exs"
      provides: "Proof that advisory coordinator behavior cannot override DB-first expiry truth"
    - path: "test/oban_powertools/example_host_contract_test.exs"
      provides: "Upgrade proof that migrated waiting workflows remain explainable and reconcilable after the Phase 19 contract lands"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Updated Phase 19 traceability for SIG-01, SIG-02, SIG-03, VER-01, and VER-02 if execution materially closes them"
    - path: ".planning/PROJECT.md"
      provides: "Milestone posture aligned to the Phase 19 wait, signal, and expiry contract that the repo proves"
  key_links:
    - from: "lib/oban_powertools/workflow/runtime.ex"
      to: "test/oban_powertools/workflow_runtime_test.exs"
      via: "tests prove that only reconcile finalizes expiry and that late signals remain evidence rather than reopening waits"
      pattern: "reconcile_workflow|expire_waits|late|expired_wait"
    - from: ".planning/REQUIREMENTS.md"
      to: ".planning/PROJECT.md"
      via: "traceability and milestone posture converge on the same narrow Phase 19 support truth"
      pattern: "SIG-01|SIG-02|SIG-03|VER-01|VER-02|late|expiry|workflow-scoped"
---

<objective>
Collapse wait expiry into one authoritative DB-first reconciliation path, prove the late-arrival and no-wakeup semantics, and align planning truth to the contract that actually lands.

Purpose: finish the Phase 19 supportable semantics boundary before Phase 20 builds broader cancel and late-completion precedence on top of it.
Output: one legal expiry path, focused expiry and late-signal proof, and updated project traceability where execution materially closes requirement ownership.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/19-await-registration-signal-facts-expiry-authority/19-CONTEXT.md
@.planning/phases/19-await-registration-signal-facts-expiry-authority/19-RESEARCH.md
@.planning/phases/19-await-registration-signal-facts-expiry-authority/19-PATTERNS.md
@.planning/phases/19-await-registration-signal-facts-expiry-authority/19-01-PLAN.md
@.planning/phases/19-await-registration-signal-facts-expiry-authority/19-02-PLAN.md
@.planning/phases/17-db-first-transition-engine-command-pipeline/17-CONTEXT.md
@lib/oban_powertools/workflow/runtime.ex
@test/support/example_host_contract.ex
@test/oban_powertools/example_host_contract_test.exs
@test/oban_powertools/workflow_runtime_test.exs
@test/oban_powertools/workflow_coordinator_test.exs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Route every legal wait-expiry outcome through one shared reconcile path</name>
  <files>lib/oban_powertools/workflow/runtime.ex, test/oban_powertools/workflow_runtime_test.exs, test/oban_powertools/workflow_coordinator_test.exs</files>
  <read_first>.planning/phases/19-await-registration-signal-facts-expiry-authority/19-CONTEXT.md, lib/oban_powertools/workflow/runtime.ex, test/oban_powertools/workflow_runtime_test.exs, test/oban_powertools/workflow_coordinator_test.exs</read_first>
  <action>Per D-17-D-20 and Phase 17's DB-first command-core posture, collapse expiry legality to `reconcile_workflow/3` and its shared helpers. Remove or narrow any ingress-side shortcut that currently finalizes expiry semantics during `deliver_signal/2`; ingress may append late evidence, but it must not decide the legal expired outcome independently. Preserve idempotent replay behavior, keep late signals as durable evidence after expiry, and ensure expired waits are not reopened by a later arrival. Do not settle broader cancel-versus-signal or completion-versus-expiry precedence beyond the narrow Phase 19 wait-expiry authority this phase owns.</action>
  <acceptance_criteria>
    - One DB-first reconcile path finalizes wait expiry.
    - Signal ingress can record lateness but cannot own expiry semantics.
    - Expired waits stay expired when late signals arrive.
    - Focused tests prove idempotent repeated reconcile and no-advisory-wakeup correctness.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/oban_powertools/workflow_runtime_test.exs test/oban_powertools/workflow_coordinator_test.exs</automated>
  </verify>
  <done>Expiry authority is singular, replayable, and explainable from durable rows alone.</done>
</task>

<task type="auto">
  <name>Task 2: Close the proof lane with archived upgrade coverage and update planning truth only for semantics the repo now proves</name>
  <files>test/support/example_host_contract.ex, test/oban_powertools/example_host_contract_test.exs, test/oban_powertools/workflow_runtime_test.exs, test/oban_powertools/workflow_coordinator_test.exs, .planning/REQUIREMENTS.md, .planning/PROJECT.md</files>
  <read_first>.planning/REQUIREMENTS.md, .planning/PROJECT.md, test/support/example_host_contract.ex, test/oban_powertools/example_host_contract_test.exs, test/oban_powertools/workflow_runtime_test.exs, test/oban_powertools/workflow_coordinator_test.exs</read_first>
  <action>Per D-21-D-23 and VER-01/VER-02, extend the focused proof bundle so the repo explicitly covers pre-await signals, duplicates, replay attempts, ambiguous correlation, late-after-expiry evidence, and lost-wakeup reconciliation from rows alone. Add an archived upgrade-proof case that starts from `examples/phoenix_host_upgrade_source`, creates or restores an in-flight waiting workflow before the Phase 19 contract changes are exercised, migrates into the current host shape, and proves the resulting rows remain explainable plus reconcilable under the new await/signal semantics. After the code and tests prove the Phase 19 contract, update `.planning/REQUIREMENTS.md` and `.planning/PROJECT.md` only where execution materially changes requirement mapping or milestone posture. Keep the support truth narrow: no generic event-bus claims, no exactly-once delivery claims, and no Phase 20 precedence promises landing early.</action>
  <acceptance_criteria>
    - Focused tests cover duplicate, replay, ambiguous, late, and lost-wakeup paths.
    - The archived upgrade lane proves an in-flight waiting workflow survives migration with explainable await/signal rows and a legal reconcile path.
    - Planning artifacts only claim semantics the repo now proves.
    - Phase 19 traceability maps the signal requirements to the phase that actually implements them.
    - Support language stays explicitly Postgres-first and workflow-scoped.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/oban_powertools/workflow_runtime_test.exs test/oban_powertools/workflow_coordinator_test.exs</automated>
    <automated>mix test test/oban_powertools/example_host_contract_test.exs --only upgrade-proof</automated>
    <automated>rg -n "SIG-01|SIG-02|SIG-03|VER-01|VER-02|workflow-scoped|late|expired_wait|Postgres" .planning/REQUIREMENTS.md .planning/PROJECT.md test/oban_powertools/workflow_runtime_test.exs test/oban_powertools/workflow_coordinator_test.exs</automated>
  </verify>
  <done>The code, targeted upgrade proof, and planning artifacts all tell the same narrow Phase 19 story about durable waits, signal facts, and authoritative expiry.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Signal ingress -> expiry legality | Ingress may observe lateness, but only the shared reconcile path may finalize expiry. |
| Advisory coordinator -> durable workflow truth | Missing or duplicated advisory wakeups must not change expiry or signal truth. |
| Repo proof -> planning truth | Planning artifacts must not claim broader semantics than tests actually prove. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-07 | T | expiry legality | mitigate | Route legal expiry through one shared DB-first reconcile path and remove ingress-side finalization shortcuts. |
| T-19-08 | D | advisory wakeup loss or duplication | mitigate | Prove that repeated reconcile and no-wakeup paths preserve the same durable expiry outcome. |
| T-19-09 | I | support-truth drift | mitigate | Update `.planning/PROJECT.md` and `.planning/REQUIREMENTS.md` only after focused tests prove the exact Phase 19 semantics. |
</threat_model>

<verification>
- `mix test test/oban_powertools/workflow_runtime_test.exs test/oban_powertools/workflow_coordinator_test.exs`
- `mix test test/oban_powertools/example_host_contract_test.exs --only upgrade-proof`
- `rg -n "SIG-01|SIG-02|SIG-03|VER-01|VER-02|workflow-scoped|late|expired_wait|Postgres" .planning/REQUIREMENTS.md .planning/PROJECT.md test/oban_powertools/workflow_runtime_test.exs test/oban_powertools/workflow_coordinator_test.exs`
</verification>

<success_criteria>
- Expiry is finalized through one authoritative DB-first reconciliation path.
- Late signals remain durable evidence and cannot reopen expired waits.
- Focused proof, archived upgrade proof, and planning truth align on the exact Phase 19 semantics the repo ships.
</success_criteria>

<output>
After completion, create `.planning/phases/19-await-registration-signal-facts-expiry-authority/19-03-SUMMARY.md`
</output>
