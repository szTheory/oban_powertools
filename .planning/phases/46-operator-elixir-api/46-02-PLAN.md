---
phase: 46-operator-elixir-api
plan: 02
type: execute
wave: 2
depends_on: [46-01]
files_modified:
  - lib/oban_powertools/operator.ex
  - test/oban_powertools/operator_test.exs
autonomous: true
requirements: [API-01, API-02]
must_haves:
  truths:
    - API can process multiple jobs in a single call
    - Bulk operations report granular success and failure lists
    - One job failing does not prevent others from succeeding
  artifacts:
    - path: lib/oban_powertools/operator.ex
      provides: Bulk action API
  key_links:
    - from: Operator.bulk_retry_jobs
      to: Operator.do_repair
      via: Enum.reduce to accumulate results
---

<objective>
Implement bulk job mutation functions in the Operator API.

Purpose: Allow programmatic bulk operations (e.g., custom host UI, cron jobs) with honest per-job success/failure reporting.
Output: Bulk functions in `Operator.ex` and corresponding test coverage.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/46-operator-elixir-api/46-CONTEXT.md
@.planning/phases/46-operator-elixir-api/46-RESEARCH.md
@.planning/phases/46-operator-elixir-api/46-PATTERNS.md
</context>

<interfaces>
From lib/oban_powertools/operator.ex:
```elixir
defmodule ObanPowertools.Operator do
  # private do_repair handles the Lifeline interaction
  # defp do_repair(repo, actor, action, job_id, reason, opts)
end
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement bulk operations</name>
  <files>lib/oban_powertools/operator.ex, test/oban_powertools/operator_test.exs</files>
  <behavior>
    - `Operator.bulk_retry_jobs/5`, `bulk_cancel_jobs/5`, `bulk_discard_jobs/5` accept a list of job IDs.
    - Each function iterates over the list using the private `do_repair/6` function.
    - They return a map `%{successes: [job_id], failures: [{job_id, error}]}`.
  </behavior>
  <action>
    Add `bulk_retry_jobs(repo, actor, job_ids, reason, opts \\ [])`, `bulk_cancel_jobs(...)`, and `bulk_discard_jobs(...)` to `Operator`.
    Implement a private `do_bulk_repair(repo, actor, action, job_ids, reason, opts)` that uses `Enum.reduce` to fold over `job_ids`. For each ID, call `do_repair`. If `{:ok, _}` is returned, append the ID to `:successes`. If `{:error, error}` is returned, append `{job_id, error}` to `:failures`. Return the accumulated map (ensure lists are reversed if built via prepend, or just built correctly).
    Add comprehensive tests in `operator_test.exs` verifying partial success (some jobs succeed, some fail due to invalid state or missing).
  </action>
  <verify>
    <automated>mix test test/oban_powertools/operator_test.exs</automated>
  </verify>
  <done>Bulk operations return correct success/failure maps and execute independently.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Caller→Operator | Host app code calling the programmatic API |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-46-03 | Denial of Service | Operator | accept | Unbounded bulk lists could tie up the database, but this is an internal Elixir API. Host apps are responsible for chunking if they pass thousands of IDs. |
</threat_model>

<verification>
mix test test/oban_powertools/operator_test.exs
</verification>

<success_criteria>
`ObanPowertools.Operator` successfully implements `bulk_*` functions with map-based granular reporting.
</success_criteria>

<output>
After completion, create `.planning/phases/46-operator-elixir-api/46-02-SUMMARY.md`
</output>
