---
phase: 49-limiter-explain-simulate-cli
plan: 02
type: execute
wave: 2
depends_on: ["49-01"]
files_modified:
  - lib/mix/tasks/oban_powertools.limiter.explain.ex
  - test/mix/tasks/oban_powertools.limiter.explain_test.exs
autonomous: true
requirements: [OPS-06, OPS-08]

must_haves:
  truths:
    - "mix oban_powertools.limiter.explain --resource NAME explains a limiter's current blocking state via Explain.explain_snapshot/2"
    - "An unknown/no-state resource reports runnable with 'no limiter state recorded yet' (honest empty state), not an error"
    - "mix oban_powertools.limiter.explain --worker MOD --args JSON maps onto Explain.explain/3"
    - "An unknown --worker module is a cannot-run error (exit 2)"
    - "--format json emits a payload containing schema_version:1"
    - "The task @moduledoc surfaces the rate-limit glossary"
  artifacts:
    - path: "lib/mix/tasks/oban_powertools.limiter.explain.ex"
      provides: "Mix.Tasks.ObanPowertools.Limiter.Explain CLI entry (OPS-06)"
      contains: "def run("
    - path: "test/mix/tasks/oban_powertools.limiter.explain_test.exs"
      provides: "Source-inspection + DB-integration tests for explain"
      contains: "explain"
  key_links:
    - from: "lib/mix/tasks/oban_powertools.limiter.explain.ex"
      to: "ObanPowertools.Explain.explain_snapshot/2"
      via: "resource-primary path over latest snapshot by scope_id"
      pattern: "Explain\\.explain_snapshot"
    - from: "lib/mix/tasks/oban_powertools.limiter.explain.ex"
      to: "ObanPowertools.Explain.explain/3"
      via: "worker+args secondary path"
      pattern: "Explain\\.explain\\("
    - from: "lib/mix/tasks/oban_powertools.limiter.explain.ex"
      to: "ObanPowertools.Limits.Glossary.text/0"
      via: "@moduledoc glossary reference"
      pattern: "Glossary"
---

<objective>
Ship `mix oban_powertools.limiter.explain` (OPS-06): a read-only operator CLI that diagnoses a limiter's current blocking state, mirroring the Doctor task wholesale and reusing `ObanPowertools.Explain` without duplicating limiter logic. Resource-primary (`--resource`/`--partition`) with a worker+args fallback (D-03/D-04). Surface the shared glossary in `@moduledoc` (OPS-08).

Purpose: Give operators the resource-name vocabulary they already see in the Limiters UI and forensics, from the command line.
Output: New explain Mix task + its test file.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/49-limiter-explain-simulate-cli/49-CONTEXT.md
@.planning/phases/49-limiter-explain-simulate-cli/49-RESEARCH.md
@.planning/phases/49-limiter-explain-simulate-cli/49-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the limiter.explain Mix task</name>
  <files>lib/mix/tasks/oban_powertools.limiter.explain.ex</files>
  <read_first>
    - lib/mix/tasks/oban_powertools.doctor.ex (the CLI template — copy boot strategy, resolve_repo, resolve_prefix, format mapping, System.halt-outside-callback verbatim)
    - lib/oban_powertools/explain.ex (explain/3 returns the map directly or {:ok, nil} passthrough; explain_snapshot/2 takes an %Explain{} struct and returns %{status, blockers, live_now, snapshot_at_block_start}; status from a snapshot is a DB STRING not an atom — normalize at the boundary)
    - lib/oban_powertools/web/limiters_live.ex (lines ~266-289 — the resource-primary path the CLI mirrors: query latest Explain by scope_id == resource_name, then explain_snapshot/2; nil snapshot => honest empty state)
    - lib/oban_powertools/limits.ex (partition_defaults/0 for the __global__ default)
    - lib/oban_powertools/limits/glossary.ex (the shared glossary string created in Plan 49-01 — reference its terms in @moduledoc)
  </read_first>
  <action>
    Create `lib/mix/tasks/oban_powertools.limiter.explain.ex` defining `Mix.Tasks.ObanPowertools.Limiter.Explain` with `use Mix.Task`, a `@shortdoc`, and a `@moduledoc` that includes the rate-limit glossary terms (token_bucket, bucket_capacity, bucket_span_ms, weight/weight_by, partition/partition_by/scope, cooldown, limit_reached) sourced to match `ObanPowertools.Limits.Glossary.text/0` — the docs_contract_test in Plan 49-03 will assert term parity. Declare `@switches [repo: :string, prefix: :string, oban_name: :string, format: :string, resource: :string, partition: :string, worker: :string, args: :string]`. Implement `run/1`: first line `Mix.Task.run("app.config")` (NEVER `@requirements`, NEVER `app.start`); parse with `OptionParser.parse(argv, strict: @switches)`; resolve repo via `resolve_repo/1` (Keyword.get :repo -> `Module.safe_concat([repo_string])`, fallback `ObanPowertools.RuntimeConfig.repo!()`); wrap work in `Ecto.Migrator.with_repo(repo_module, fn repo -> ... end, pool_size: 2)` returning an integer exit code; call `System.halt` ONLY in the outer `case result do {:ok, exit_code, _apps} -> System.halt(exit_code); {:error, reason} -> Mix.shell().error(...); System.halt(2) end`. Map `--format` via closed `case "json" -> :json; _ -> :human`. Dispatch: if `--resource` given, run the resource-primary path — query `repo.one(from event in ObanPowertools.Explain, where: event.scope_id == ^resource_name, order_by: [desc: event.captured_at], limit: 1)` with partition defaulting to `"__global__"`; on `nil` snapshot print the honest empty state ("no limiter state recorded yet", status runnable) and return 0; otherwise call `ObanPowertools.Explain.explain_snapshot(snapshot, repo: repo)`, normalize the string `status` to an atom at the boundary via a closed helper (`"runnable" -> :runnable; "blocked" -> :blocked; other -> :unknown` — NOT String.to_atom on raw values), format and return 0. If `--worker` given (secondary path), resolve via `Module.safe_concat([worker_string])`, parse `--args` (default "{}") with `Jason.decode/1` into a string-keyed map (NEVER atomize untrusted keys), call `ObanPowertools.Explain.explain(worker_mod, parsed_args, repo: repo)`; on `{:ok, nil}` or nil (worker has no limits) print "worker has no limits configured" and return 2; on a map, format and return 0. On bad JSON args return 2 with a clear message. If neither `--resource` nor `--worker` is given, return 2 with usage guidance. Implement a private formatter handling both `:human` (sectioned, ANSI via `IO.ANSI.enabled?()`-gated colorize) and `:json` (`Jason.encode!` of a payload with top-level `schema_version: 1` — independent from doctor's schema, version 1). Reuse the verbatim `resolve_prefix/1` from doctor.ex (cond on `--prefix`, else best-effort `String.to_existing_atom(oban_name)` rescue then app env, else "public"); keep the "use --prefix for reliable production results" caveat in `@moduledoc`.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/oban_powertools && mix compile --warnings-as-errors 2>&1 | tail -5 && grep -c "Explain.explain_snapshot" lib/mix/tasks/oban_powertools.limiter.explain.ex</automated>
  </verify>
  <acceptance_criteria>
    - `mix help oban_powertools.limiter.explain` succeeds and its output contains the glossary term `token_bucket`
    - Source contains `use Mix.Task` and does NOT contain `use Igniter.Mix.Task`, `@requirements`, or `Oban.start_link` (grep -v '^#' filtered)
    - Source contains `Ecto.Migrator.with_repo`, `Module.safe_concat`, and `Explain.explain_snapshot`; calls `Explain.explain(` for the worker path
    - `grep -c "String.to_atom(" lib/mix/tasks/oban_powertools.limiter.explain.ex` returns 0 (T-48-05: never atomize raw CLI input)
    - Source matches `->\s+System\.halt` (halt outside the callback)
    - `@switches` declares resource, partition, worker, args, repo, prefix, oban_name, format
    - JSON output path emits a payload containing `schema_version` set to 1
  </acceptance_criteria>
  <done>The explain task compiles warning-free, mirrors doctor conventions, drives Explain.explain_snapshot/2 (resource path) and Explain.explain/3 (worker path), handles honest empty state, and surfaces the glossary in @moduledoc.</done>
</task>

<task type="auto">
  <name>Task 2: Test the explain task (source-inspection + DB integration)</name>
  <files>test/mix/tasks/oban_powertools.limiter.explain_test.exs</files>
  <read_first>
    - test/mix/tasks/oban_powertools.doctor_test.exs (source-inspection test conventions: @task_path read, refute String.to_atom, assert Module.safe_concat, assert with_repo, switch assertions, @shortdoc)
    - test/oban_powertools/limits_test.exs (DataCase setup pattern for creating Resource/State/Explain rows; `use ObanPowertools.DataCase, async: false`)
    - lib/mix/tasks/oban_powertools.limiter.explain.ex (the task created in Task 1)
    - lib/oban_powertools/explain.ex (schema field scope_id holds the resource name; status is a string field)
  </read_first>
  <action>
    Create `test/mix/tasks/oban_powertools.limiter.explain_test.exs`. Include a source-inspection module (`use ExUnit.Case`, `@task_path "lib/mix/tasks/oban_powertools.limiter.explain.ex"`) asserting: the module defines `run/1` (`function_exported?/3` after `Code.ensure_loaded?`); `source =~ "use Mix.Task"` and `refute source =~ "use Igniter.Mix.Task"`; `refute source =~ "@requirements"` and `refute source =~ "Oban.start_link"`; `assert source =~ "Ecto.Migrator.with_repo"`; `refute source =~ ~r/String\.to_atom\(/`; `assert source =~ "Module.safe_concat"`; `assert source =~ ~r/->\s+System\.halt/`; switches present (`resource:`, `partition:`, `worker:`, `args:`, `repo:`, `format:`); `assert source =~ "@shortdoc"`; and the glossary term `token_bucket` appears in source (OPS-08 in-task surface). Include a DB-integration module (`use ObanPowertools.DataCase, async: false`) that exercises the resource-primary logic at the function level rather than through `System.halt`: insert a `Resource` + `State` + `Explain` snapshot row (scope_id = resource name) and assert `Explain.explain_snapshot(snapshot, repo: repo())` returns a map whose normalized status renders correctly; and assert that querying a non-existent resource name yields the honest empty-state path (snapshot nil => runnable / "no limiter state recorded yet"). If the explain task exposes its core path only via `run/1`+`System.halt`, extract the formatting/decision into a testable private-or-public helper in Task 1 and call that here (do not invoke System.halt in tests). Keep test names mapped to OPS-06.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/oban_powertools && mix test test/mix/tasks/oban_powertools.limiter.explain_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `mix test test/mix/tasks/oban_powertools.limiter.explain_test.exs` passes with zero failures
    - The test file asserts both `refute source =~ ~r/String\.to_atom\(/` and `assert source =~ "Module.safe_concat"`
    - A DB-integration test proves the resource-primary path resolves via `Explain.explain_snapshot/2` against an inserted snapshot row
    - A test proves the honest empty state: a resource with no snapshot yields runnable / "no limiter state recorded yet"
    - A test asserts the task source contains the glossary term `token_bucket`
  </acceptance_criteria>
  <done>Explain task has passing source-inspection tests (safety conventions) and DB-integration tests covering the resource-primary path and honest empty state, all green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator CLI → explain task | Untrusted flag input: `--repo`, `--worker` (module names), `--args` (JSON), `--format`, `--resource`, `--partition` |
| explain task → DB (via with_repo) | Read-only queries over Explain/Resource/State; no writes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-49-03 | Tampering / DoS | `--repo` / `--worker` module-name flags | mitigate | Resolve via `Module.safe_concat([string])`; NEVER `String.to_atom`/`String.to_existing_atom` on raw input (T-48-05). Test asserts `refute source =~ ~r/String\.to_atom\(/` |
| T-49-04 | Tampering | `--args JSON` untrusted keys | mitigate | Parse with `Jason.decode/1` into a string-keyed map; never atomize keys; bad JSON returns exit 2 |
| T-49-05 | Tampering | `--format` flag | mitigate | Map via closed `case "json" -> :json; _ -> :human`; no atom creation from input |
| T-49-06 | Spoofing/Info | explain reads but never mutates limiter state | accept | Read-only by construction (no repo.update/insert in explain path); diagnostic only |
| T-49-SC | Tampering | npm/pip/cargo installs | accept | No package installs (zero-new-runtime-deps, REL-01); RESEARCH audit confirms none |
</threat_model>

<verification>
- `mix test test/mix/tasks/oban_powertools.limiter.explain_test.exs` passes
- `mix compile --warnings-as-errors` clean
- `mix help oban_powertools.limiter.explain` renders with glossary terms present
- New task file is auto-included by the `:files` whitelist (REL-01): `lib` glob already covers `lib/mix/tasks/` — confirm via `mix hex.build --unpack` listing or `lib` glob inspection; no mix.exs change required
</verification>

<success_criteria>
- `mix oban_powertools.limiter.explain` explains a limiter's current blocking state via the existing Explain API (Success Criterion #1)
- Reuses Explain without duplicating limiter logic (Success Criterion #4)
- Glossary surfaced in @moduledoc/help (Success Criterion #3, in-task surface)
- Untrusted CLI input safely resolved (T-48-05)
</success_criteria>

<output>
Create `.planning/phases/49-limiter-explain-simulate-cli/49-02-SUMMARY.md` when done
</output>
