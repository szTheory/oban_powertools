---
phase: 56-redact-at-rest
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/oban_powertools/worker.ex
  - lib/oban_powertools/worker/redaction.ex
  - test/oban_powertools/worker_redact_test.exs
  - test/oban_powertools/idempotency_test.exs
autonomous: true
requirements: [REDACT-01, REDACT-02]
must_haves:
  truths:
    - "A worker declaring redact: [:ssn, :token] has those keys absent from oban_jobs.args after enqueue"
    - "The idempotency fingerprint is computed from the original unredacted args, not the redacted args"
    - "Enqueued jobs store __redacted_fields__ in meta as a sorted string list"
    - "A typed+redacted worker validates and processes a job whose stored args lack the redacted field, across retries"
    - "redact: with an undeclared field raises ArgumentError at compile time"
    - "redact: overlapping a partition_by {:args, key} raises ArgumentError at compile time"
  artifacts:
    - path: "lib/oban_powertools/worker/redaction.ex"
      provides: "Key normalization, Map.drop redaction, and __redacted_fields__ meta injection at the new/2 boundary"
      contains: "defmodule ObanPowertools.Worker.Redaction"
    - path: "lib/oban_powertools/worker.ex"
      provides: "redact: opt parsing, compile-time guards, __powertools_redact__/0, required-field exemption, new/1,2 override"
      contains: "def __powertools_redact__"
    - path: "test/oban_powertools/worker_redact_test.exs"
      provides: "Redaction behavior + invariant + compile-time guard coverage"
      contains: "redact"
  key_links:
    - from: "lib/oban_powertools/worker.ex new/2 override"
      to: "lib/oban_powertools/worker/redaction.ex apply/4"
      via: "delegated call; helper invokes worker's __powertools_new_delegate__/2 (explicit Oban.Job.new + merge_opts, OQ1-resolved)"
      pattern: "ObanPowertools.Worker.Redaction.apply"
    - from: "lib/oban_powertools/idempotency.ex transaction/3"
      to: "worker_mod.new/2 override"
      via: "new called after generate_fingerprint (line 46 before line 81)"
      pattern: "worker_mod.new"
---

<objective>
Implement the redaction engine: the `redact:` worker opt, its compile-time guards, the `new/1,2` override that drops fields and injects `__redacted_fields__` meta, the required-field exemption, and a new `ObanPowertools.Worker.Redaction` helper module. This is the security core of Phase 56 — it closes the direct-insert and transaction/3 PII surfaces (REDACT-01, REDACT-02).

Purpose: Drop declared sensitive arg fields from `oban_jobs.args` at persist time, strictly after the idempotency fingerprint is computed, while keeping the row self-describing via meta.
Output: Modified `worker.ex`, new `worker/redaction.ex`, new `worker_redact_test.exs` test group, redaction tests added to `idempotency_test.exs`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/56-redact-at-rest/56-CONTEXT.md
@.planning/phases/56-redact-at-rest/56-RESEARCH.md
@.planning/phases/56-redact-at-rest/56-PATTERNS.md

<interfaces>
<!-- Contracts the executor needs — extracted from codebase. Do not re-explore. -->

worker.ex existing seams (verified line refs):
- `__using__/1` opts pipeline: args/limits/timeout/deadline/record_output/output_limit/output_retention parsed and stripped (PATTERNS.md lines 32–53)
- compile-time validator chain: validate_args_config!, normalize_limits_config!, normalize_timeout_config!, normalize_deadline_config! (PATTERNS.md lines 56–63)
- module attr + accessor block at worker.ex:82–84 (`def __powertools_limits__, do: @powertools_limits`)
- Args.changeset/2 at worker.ex:96–101: `cast(params, fields) |> validate_required(fields)`
- defoverridable pattern at worker.ex:46 (timeout) and :124 (hooks)
- perform/1 wrap via private helper at worker.ex:126–140
- `function_exported?(worker_mod, :__powertools_limits__, 0)` sentinel at worker.ex:240

new/2 override delegation (OQ1-RESOLVED — explicit, no super):
- The override calls `ObanPowertools.Worker.Redaction.apply(__MODULE__, args, opts, @powertools_redact)`
- The helper delegates back to a generated public `__powertools_new_delegate__/2` whose body is `Oban.Job.new(args, Oban.Worker.merge_opts(__opts__(), opts))` — mirrors Oban's own generated new/2 body, sidesteps super-in-quote scoping (RESEARCH Open Questions OQ1 RESOLVED; Code Examples 1 & 2)

idempotency.ex (DO NOT MODIFY ordering):
- transaction/3 computes `fingerprint = generate_fingerprint(worker_mod, casted_args)` at line 46, then calls `worker_mod.new(args_map, merge_powertools_meta(...))` at line 81
- args_map reaches new/2 as `Map.from_struct(casted_struct)` → atom keys (D-16)
- deep_merge/2 two-clause pattern at idempotency.ex:178–182

Analog module to mirror for redaction.ex:
- `lib/oban_powertools/worker/deadlines.ex` — `@moduledoc false`, pure functions, no struct (PATTERNS.md lines 180–198)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 0: Wave 0 — failing redaction test scaffold</name>
  <files>test/oban_powertools/worker_redact_test.exs</files>
  <read_first>
    - test/oban_powertools/worker_test.exs (compile-time raise test pattern lines 260–271; worker def pattern lines 24–37; setup/Sandbox checkout lines 7–22)
    - test/oban_powertools/idempotency_test.exs (deadline meta preservation test lines 52–79 — the enqueue + meta-assert shape)
    - .planning/phases/56-redact-at-rest/56-RESEARCH.md (Validation Architecture → Phase Requirements → Test Map, and Redaction Invariant Validation table)
    - .planning/phases/56-redact-at-rest/56-PATTERNS.md (test analogs lines 472–584)
  </read_first>
  <action>
    Create test/oban_powertools/worker_redact_test.exs. Use `use ObanPowertools.DataCase, async: false` (mirror idempotency_test.exs line 2) so DB integration tests can assert real oban_jobs rows. Define at top-of-file worker modules for the integration cases (a worker with `args: [user_id: :integer, ssn: :string], redact: [:ssn]` and `process/1` that sends `{:processed, user_id}` to self — mirror BasicWorker at worker_test.exs:24–37).

    Add these failing tests (they MUST fail now since redaction is unimplemented):
    1. REDACT-01 key-absent (D-02): enqueue %{ssn: "123", user_id: 1}; assert `refute Map.has_key?(job.args, "ssn")` AND `refute Map.has_key?(job.args, :ssn)`; assert `job.args["user_id"] == 1`.
    2. REDACT-01 string-key normalization (D-16): enqueue via direct `RedactWorker.new(%{"ssn" => "x", "user_id" => 1}) |> insert`; assert ssn key absent from stored args.
    3. REDACT-01 required-field exemption (D-06): a typed+redacted worker — call its `validate(%{"user_id" => 1})` (the stored-args shape, ssn absent) and assert `{:ok, _}` (NOT a changeset error). Then run perform/1 on a job with that args and assert it returns `:ok` (proves it survives the re-cast across retries).
    4. REDACT-02 meta (D-17): enqueue worker with `redact: [:token, :ssn]`; assert `job.meta["__redacted_fields__"] == ["ssn", "token"]` (sorted string list).
    5. D-07 typo guard: `assert_raise ArgumentError, ~r/redact: key :typo_field is not declared/, fn -> defmodule TypoRedactWorker do use ObanPowertools.Worker, args: [user_id: :integer], redact: [:typo_field] end end` (mirror worker_test.exs:260–271).
    6. D-09 partition guard: `assert_raise ArgumentError, ~r/partition/, fn -> ... use ObanPowertools.Worker, args: [user_id: :integer], limits: [name: "r", scope: :partitioned, partition_by: {:args, :user_id}, bucket_capacity: 10, bucket_span_ms: 60_000], redact: [:user_id] end end`.

    Do NOT use watch-mode flags. Do NOT inline implementation in test setup.
  </action>
  <verify>
    <automated>mix test test/oban_powertools/worker_redact_test.exs 2>&1 | tail -20 # expect failures/compile errors — proves RED</automated>
  </verify>
  <acceptance_criteria>
    - test/oban_powertools/worker_redact_test.exs exists and contains test cases referencing each of REDACT-01, REDACT-02, D-06, D-07, D-09, D-16, D-17
    - Running the file currently fails or raises (RED state) — the override and guards do not exist yet
    - No `--watch`/watch flags present in any command
  </acceptance_criteria>
  <done>Failing test file committed; covers key-absent, string-key normalization, required-field exemption, sorted-string meta, typo guard, partition guard.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 1: Redaction helper module + worker.ex parse, guards, exemption, new/2 override</name>
  <files>lib/oban_powertools/worker/redaction.ex, lib/oban_powertools/worker.ex</files>
  <read_first>
    - lib/oban_powertools/worker.ex (full — the __using__ opts pipeline, validator chain, Args.changeset, defoverridable + perform wrap, __powertools_* accessors)
    - lib/oban_powertools/worker/deadlines.ex (structural analog for the new Redaction module: @moduledoc false, pure functions)
    - lib/oban_powertools/idempotency.ex (deep_merge/2 at lines 178–182; transaction/3 ordering lines 42–81; merge_powertools_meta lines 147–176 — confirm new/2 receives opts with meta already merged, D-04)
    - deps/oban/lib/oban/worker.ex (the generated new/2 body — confirm it is `Oban.Job.new(args, merge_opts(__opts__(), opts))`; this is what __powertools_new_delegate__/2 reproduces, OQ1)
    - .planning/phases/56-redact-at-rest/56-RESEARCH.md (Code Examples 1 & 2; Patterns 1,3,4,6,7; Open Questions OQ1 RESOLVED; Anti-Patterns list)
    - .planning/phases/56-redact-at-rest/56-PATTERNS.md (worker.ex section lines 28–172; redaction.ex section lines 176–229)
  </read_first>
  <behavior>
    - apply(worker_mod, args, opts, []) returns worker_mod.__powertools_new_delegate__(args, opts) unchanged (no redact config = pass-through delegation)
    - apply(worker_mod, args, opts, [:ssn]): normalizes string-keyed "ssn" to atom :ssn, Map.drop removes :ssn (key-absent), injects meta["__redacted_fields__"] = ["ssn"] via deep_merge without clobbering existing meta keys, then calls worker_mod.__powertools_new_delegate__ with clean args + merged opts
    - __redacted_fields__ is always a sorted list of strings (D-17)
    - __powertools_new_delegate__/2 builds the changeset via Oban.Job.new(args, Oban.Worker.merge_opts(__opts__(), opts)) — explicit delegation mirroring Oban's generated new/2, NO super (OQ1-resolved)
    - validate_redact_config! raises ArgumentError when a redact key is not a declared args field (D-07)
    - validate_redact_config! raises ArgumentError when a redact key equals the partition_by {:args, key} field (D-09)
    - Args.changeset/2 casts ALL declared fields but validate_required only on (declared -- redacted) fields (D-06)
  </behavior>
  <action>
    Create lib/oban_powertools/worker/redaction.ex as `ObanPowertools.Worker.Redaction` with `@moduledoc false`. Public `apply/4` taking `(worker_mod, args, opts, redact_keys)` per RESEARCH Example 2: a no-op clause for `[]` redact keys that calls `worker_mod.__powertools_new_delegate__(args, opts)`, and a work clause that computes `redact_strings = redact_keys |> Enum.map(&Atom.to_string/1) |> Enum.sort()` (D-17), normalizes keys via private `normalize_to_atom_keys/2` (RESEARCH Example 2 — only re-key string keys that map to a declared redact atom, leave unknown keys untouched), `Map.drop`s the atom redact keys (D-02 key-absent — never nil), injects meta via private `inject_meta/2` using a private two-clause `deep_merge/2` copied verbatim from idempotency.ex:178–182, then calls `worker_mod.__powertools_new_delegate__(clean_args, opts_with_meta)`.

    In worker.ex __using__: parse `redact_config = Keyword.get(opts, :redact, [])`; add `|> Keyword.delete(:redact)` to the oban_opts strip chain (PATTERNS.md line 52). After the existing validator chain (after normalize_deadline_config!), add a new private `validate_redact_config!(redact_config, args_config, normalized_limits)` call. Implement validate_redact_config! mirroring validate_args_config! raise style (PATTERNS.md lines 65–80, 83–88): D-07 — raise `ArgumentError, "redact: key #{inspect(key)} is not declared in :args schema..."` for any key not in `Keyword.keys(args_config)`; D-09 — extract partition field from `Keyword.get(normalized_limits, :partition_by)` matching `{:args, field}`; raise `ArgumentError, "redact: field ... is also used as partition_by ..."` if that field is in redact_config.

    In the quote block: add `@powertools_redact unquote(redact_config)` and `def __powertools_redact__, do: @powertools_redact` beside the existing accessors (worker.ex:82–84). Compute `required_fields = Keyword.keys(args_config) -- redact_config` at macro time and change Args.changeset/2 (worker.ex:96–101) to `cast(params, all_fields) |> validate_required(required_fields)` — keep cast on ALL fields, shrink only validate_required (D-06).

    Add the new/2 override and its delegate after `use Oban.Worker` and after the perform/hook defoverridable blocks. The override: `@impl Oban.Worker` + `def new(args, opts \\ []) when is_map(args) and is_list(opts) do ObanPowertools.Worker.Redaction.apply(__MODULE__, args, opts, @powertools_redact) end`. The delegate (NOT a super lambda): `def __powertools_new_delegate__(args, opts) do Oban.Job.new(args, Oban.Worker.merge_opts(__opts__(), opts)) end` — this reproduces the body of Oban's generated new/2 and is the OQ1-resolved path that sidesteps any super-in-quote scoping subtlety. Then `defoverridable new: 1, new: 2`. Do NOT use a `super` call or a captured `&super/2` lambda anywhere in the override. Do NOT inject __redacted_fields__ anywhere in idempotency.ex (D-04 single injection — the override owns it; transaction/3 calls new/2 and inherits it).

    Keep quoted macro code minimal — all drop/normalize/merge logic lives in Redaction; the quoted code is only the override, the delegate, the accessor, the guards-call, and the changeset shrink (per CONTEXT specifics).
  </action>
  <verify>
    <automated>mix test test/oban_powertools/worker_redact_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - mix test test/oban_powertools/worker_redact_test.exs exits 0 (all RED tests from Task 0 now GREEN)
    - `grep -n "def __powertools_redact__" lib/oban_powertools/worker.ex` returns a match
    - `grep -n "ObanPowertools.Worker.Redaction.apply" lib/oban_powertools/worker.ex` returns a match
    - `grep -n "__powertools_new_delegate__" lib/oban_powertools/worker.ex` returns a match (explicit delegate present)
    - `grep -n "Oban.Worker.merge_opts" lib/oban_powertools/worker.ex` returns a match (OQ1 explicit-delegation body, not super)
    - `grep -c "super" lib/oban_powertools/worker.ex` shows no super introduced in the new/2 override path
    - `grep -n "defoverridable new: 1, new: 2" lib/oban_powertools/worker.ex` returns a match
    - lib/oban_powertools/worker/redaction.ex starts with `defmodule ObanPowertools.Worker.Redaction do` and `@moduledoc false`
    - No `__redacted_fields__` injection added to lib/oban_powertools/idempotency.ex (`grep -c "__redacted_fields__" lib/oban_powertools/idempotency.ex` == 0)
    - `mix compile --warnings-as-errors` exits 0
  </acceptance_criteria>
  <done>redact: parses, guards raise at compile time, override drops fields key-absent + injects sorted-string meta via the helper, delegation is the explicit Oban.Job.new + merge_opts form (no super), required fields exempt redacted ones, worker_redact_test.exs green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Fingerprint-ordering + single-meta-injection invariant tests in idempotency_test.exs</name>
  <files>test/oban_powertools/idempotency_test.exs</files>
  <read_first>
    - test/oban_powertools/idempotency_test.exs (deadline meta preservation tests lines 52–79 — enqueue + meta assert shape; setup at top of file)
    - lib/oban_powertools/idempotency.ex (transaction/3 lines 42–81; merge_powertools_meta lines 147–176 — to assert __redacted_fields__ coexists with fingerprint/limits/deadline meta)
    - .planning/phases/56-redact-at-rest/56-PATTERNS.md (idempotency_test.exs section lines 552–584)
    - .planning/phases/56-redact-at-rest/56-RESEARCH.md (Redaction Invariant Validation table: fingerprint-before-drop, single meta injection, key-absent-not-nil)
  </read_first>
  <behavior>
    - Two jobs with same user_id but different ssn (ssn redacted) produce DIFFERENT idempotency fingerprints (fingerprint computed from full unredacted args — D-03)
    - job.meta["__redacted_fields__"] coexists with job.meta["oban_powertools"]["idempotency_fingerprint"] — neither clobbers the other (D-04)
    - job.meta["__redacted_fields__"] is a flat List(String), not a list-of-lists (single injection — D-04)
    - Caller-supplied meta keys survive alongside __redacted_fields__ (deep_merge)
  </behavior>
  <action>
    Add a "redaction meta + fingerprint ordering" describe block to idempotency_test.exs. Define (or reuse from worker_redact_test if shared module strategy used; otherwise define locally) a `redact: [:ssn]` worker with `args: [user_id: :integer, ssn: :string]`.

    Test A (D-03 fingerprint-before-drop): enqueue %{user_id: 1, ssn: "aaa"} and %{user_id: 1, ssn: "bbb"}; assert the two stored `job.meta["oban_powertools"]["idempotency_fingerprint"]` values differ (proves fingerprint used full args, not redacted). Assert both jobs have ssn absent from args.

    Test B (D-04 non-clobber + single injection): enqueue with caller `meta: %{"source" => "host"}`; assert `job.meta["source"] == "host"`, `is_binary(get_in(job.meta, ["oban_powertools", "idempotency_fingerprint"]))`, `job.meta["__redacted_fields__"] == ["ssn"]`, and `Enum.all?(job.meta["__redacted_fields__"], &is_binary/1)` (flat string list, not nested).

    Mirror the existing deadline-meta enqueue+assert shape (lines 52–79). Use the configured TestRepo setup already present in the file.
  </action>
  <verify>
    <automated>mix test test/oban_powertools/idempotency_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - mix test test/oban_powertools/idempotency_test.exs exits 0
    - Test asserts two same-user_id/different-ssn jobs have differing idempotency_fingerprint values
    - Test asserts job.meta["__redacted_fields__"] == ["ssn"] AND fingerprint meta present AND caller meta "source" preserved
    - Test asserts __redacted_fields__ is a flat list of strings (no nested list)
  </acceptance_criteria>
  <done>idempotency_test.exs proves fingerprint-before-drop and single non-clobbering meta injection; suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host enqueue → oban_jobs.args (JSONB) | Untrusted/sensitive arg values cross into durable Postgres storage at new/2. This is the redaction boundary. |
| macro config (compile time) → runtime override behavior | A misconfigured redact: declaration silently failing is itself a leak vector. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-56-01 | Information Disclosure | new/2 direct-insert path | mitigate | Override on new/1,2 (D-01) drops fields for BOTH MyWorker.new(args) and transaction/3; Task 1 + Task 0 integration test assert ssn absent from real oban_jobs row |
| T-56-02 | Information Disclosure | string-keyed args bypassing atom Map.drop | mitigate | normalize_to_atom_keys/2 re-keys declared redact fields before drop (D-16); Task 0 string-key test asserts absence |
| T-56-03 | Tampering / silent no-op | typo'd redact key → Map.drop nothing → leak | mitigate | compile-time ArgumentError guard (D-07); Task 0 typo-guard test |
| T-56-04 | Information Disclosure | partition key value leaking into meta when redacted field is a partition_by key | mitigate | compile-time ArgumentError guard (D-09); Task 0 partition-guard test |
| T-56-05 | Information Disclosure | fingerprint computed from redacted args (false dedup / weak hash) | mitigate | fingerprint at idempotency.ex:46 runs before new/2 at :81 (D-03); Task 2 asserts distinct fingerprints for same-user/different-ssn |
| T-56-06 | Tampering | __redacted_fields__ double-injected or clobbered, hiding what was dropped | mitigate | single injection in new/2 only (D-04); Task 2 asserts flat string list + non-clobber |
| T-56-07 | Denial of Service | redacted typed field fails validate_required on every retry (job dead-on-arrival) | mitigate | required-field exemption (D-06); Task 0 exemption test runs perform/1 to :ok |
| T-56-SC | Tampering | npm/pip/cargo installs | accept | No new packages this phase (RESEARCH: zero new runtime deps; Package Legitimacy Audit N/A) |
</threat_model>

<verification>
- `mix test test/oban_powertools/worker_redact_test.exs test/oban_powertools/idempotency_test.exs` exits 0
- `mix compile --warnings-as-errors` exits 0
- Real oban_jobs row (Repo.get(Oban.Job, id)) for a redact worker has no "ssn"/"token" key in .args
- job.meta["__redacted_fields__"] is a sorted list of strings on every redact-worker job
- No `__redacted_fields__` reference exists in idempotency.ex (single-injection guarantee held in new/2)
- new/2 override delegates via explicit `Oban.Job.new(args, Oban.Worker.merge_opts(__opts__(), opts))` (no super) per OQ1
</verification>

<success_criteria>
- ROADMAP success criterion 1 satisfied: redacted keys absent from oban_jobs.args; fingerprint from unredacted args
- ROADMAP success criterion 2 satisfied: __redacted_fields__ present in meta as sorted string list
- D-06, D-07, D-09 guards proven by tests
- Macro quoted code stays minimal (logic in Redaction helper)
</success_criteria>

<output>
Create `.planning/phases/56-redact-at-rest/56-01-SUMMARY.md` when done
</output>
