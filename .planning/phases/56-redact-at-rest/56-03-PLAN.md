---
phase: 56-redact-at-rest
plan: 03
type: execute
wave: 2
depends_on: ["56-01"]
files_modified:
  - lib/oban_powertools/runtime_config.ex
  - lib/oban_powertools/web/jobs_live.ex
  - test/oban_powertools/web/live/jobs_live_test.exs
autonomous: true
requirements: [REDACT-03, REDACT-04]
must_haves:
  truths:
    - "The /ops/jobs job detail view renders 'Fields redacted at enqueue: [:ssn, :token]' when meta['__redacted_fields__'] is present — the ROADMAP UI-annotation obligation met at the args panel where redaction happened, separate from the recorded-output card (D-12)"
    - "DisplayPolicy.render_job_field/3 shows 'Redacted at enqueue' for any arg field listed in __redacted_fields__"
    - "A job with no __redacted_fields__ in meta shows no redaction disclosure (honest empty state)"
    - "The [redacted] bounded fallback is preserved when the display policy raises"
    - "A host display policy returning a custom map for :job_args is not overridden with the Powertools default overlay"
  artifacts:
    - path: "lib/oban_powertools/runtime_config.ex"
      provides: ":job_args render clause that overlays 'Redacted at enqueue' from meta"
      contains: "Redacted at enqueue"
    - path: "lib/oban_powertools/web/jobs_live.ex"
      provides: "Redaction disclosure block near the Meta card + :redacted_fields assigns"
      contains: "Fields redacted at enqueue"
    - path: "test/oban_powertools/web/live/jobs_live_test.exs"
      provides: "REDACT-03/04 disclosure + empty-state + fallback coverage"
      contains: "redact"
  key_links:
    - from: "lib/oban_powertools/web/jobs_live.ex render template"
      to: "@redacted_fields assign from job.meta['__redacted_fields__']"
      via: "load_job_detail/2 + assign_defaults/1"
      pattern: "redacted_fields"
    - from: "lib/oban_powertools/runtime_config.ex render_job_field(:job_args,...)"
      to: "context[:job].meta['__redacted_fields__']"
      via: "get_redacted_fields/1 overlay"
      pattern: "__redacted_fields__"
---

<objective>
Surface redaction to operators (REDACT-03, REDACT-04) per the LOCKED 56-UI-SPEC.md. Extend `DisplayPolicy.render_job_field/3` so each redacted arg field renders inline as "Redacted at enqueue", and add a disclosure block to the `/ops/jobs` job detail view near the Meta card reading "Fields redacted at enqueue: [:ssn, :token]". Read-only, neutral colors, no new top-level card, `[redacted]` fallback preserved.

Purpose: Make the absence of redacted fields read as intentional data minimization, not missing data.
Output: Modified `runtime_config.ex`, `jobs_live.ex`, LiveView redaction tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/56-redact-at-rest/56-CONTEXT.md
@.planning/phases/56-redact-at-rest/56-RESEARCH.md
@.planning/phases/56-redact-at-rest/56-PATTERNS.md
@.planning/phases/56-redact-at-rest/56-UI-SPEC.md

<interfaces>
<!-- Contracts the executor needs — extracted from codebase. -->

runtime_config.ex render_job_field/3 (verified lines 206–215):
- Tagged returns: {:raw_json, json}, {:string, text}, {:fallback, "[redacted]"} (rescue path)
- Existing :job_recorded clause precedes the generic clause at lines 202–204 — model for adding a :job_args clause BEFORE the generic one
- read_key/2 atom-or-string helper at lines 337–345

jobs_live.ex (verified):
- load_job_detail/2 assign chain at lines 723–738 (args_display, meta_display, recorded_output)
- nil/not-found branch at lines 717–721
- assign_defaults/1 at lines 780–791 (must add matching default)
- Meta card </div> at line 388; recorded-output panel begins line 391 — insert disclosure between them
- context passed as %{job: job} at line 724
- existing dt label color: class="w-36 text-zinc-500" (line 354)

UI-SPEC LOCKED copy:
- Disclosure: "Fields redacted at enqueue: [:ssn, :token]" (atoms with leading colon, comma-joined)
- Per-field: "Redacted at enqueue"
- Fallback: "[redacted]" (do NOT change)
- Empty-state heading: "No fields redacted at enqueue"
- Colors: neutral only (#F3F4F6 bg, #71717A / text-zinc-500 label). NO red. NO "Hidden"/"Masked"/"Encrypted".
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 0: Wave 0 — failing render_job_field + LiveView disclosure tests</name>
  <files>test/oban_powertools/web/live/jobs_live_test.exs</files>
  <read_first>
    - test/oban_powertools/web/live/jobs_live_test.exs (display policy module defs lines 1–47; Detail page describe lines 283–691; insert_job! with meta lines 323–341; LiveView assertion pattern lines 335–341)
    - lib/oban_powertools/runtime_config.ex (render_job_field/3 lines 206–215; :job_recorded clause 202–204)
    - .planning/phases/56-redact-at-rest/56-PATTERNS.md (jobs_live_test.exs section lines 588–619; runtime_config.ex section lines 319–373)
    - .planning/phases/56-redact-at-rest/56-UI-SPEC.md (Copywriting Contract — exact strings)
  </read_first>
  <action>
    Add a redaction describe block to jobs_live_test.exs. Use insert_job! (lines 323–341 pattern) to insert a job with `args: %{"user_id" => 42}` and `meta: %{"__redacted_fields__" => ["ssn", "token"]}`.

    REDACT-03 LiveView tests (RED now):
    1. Disclosure present: `{:ok, _v, html} = live(conn, "/ops/jobs/jobs/#{job.id}")`; assert the JOINED disclosure string renders — `html =~ "Fields redacted at enqueue"` AND `html =~ ":ssn, :token"` (the comma-joined atom-presentation form per D-13/D-17/UI-SPEC, leading colons, comma-space separator). Do NOT assert `:ssn` and `:token` as separate disjoint substrings — assert the single joined `":ssn, :token"` fragment so the test pins the locked presentation form, not incidental occurrences.
    2. Empty state: insert a job with meta lacking __redacted_fields__; assert `refute html =~ "Fields redacted at enqueue"`.

    REDACT-04 unit tests against runtime_config (RED now):
    3. `render_job_field(:job_args, %{"user_id" => 42}, %{job: job_with_redacted_meta})` returns a tagged tuple whose payload contains "Redacted at enqueue" for ssn and token. Assert on the encoded JSON / tuple content.
    4. Host-policy passthrough: define a policy module returning a custom map for :job_args (mirror lines 1–47); assert the Powertools overlay is NOT applied when the host policy returns a non-nil custom map (RESEARCH Open Questions OQ3 RESOLVED — overlay only on nil/default path).
    5. Fallback preserved: a policy that raises → render_job_field returns `{:fallback, "[redacted]"}`.

    Configure host display policy via the same Application.put_env / policy-module convention already used in this test file. No watch flags.
  </action>
  <verify>
    <automated>mix test test/oban_powertools/web/live/jobs_live_test.exs 2>&1 | tail -20 # expect redaction failures — RED</automated>
  </verify>
  <acceptance_criteria>
    - jobs_live_test.exs contains redaction tests referencing REDACT-03 (disclosure + empty state) and REDACT-04 (per-field overlay + host passthrough + fallback)
    - The disclosure test asserts the JOINED string: `html =~ "Fields redacted at enqueue"` AND `html =~ ":ssn, :token"` (comma-joined atom-presentation form per D-13/UI-SPEC) — NOT separate `:ssn` / `:token` substring assertions
    - The disclosure + per-field tests currently fail (RED) — rendering not implemented yet
    - The [redacted] fallback test references the exact string "[redacted]"
  </acceptance_criteria>
  <done>Failing LiveView + render_job_field redaction tests committed covering REDACT-03/04, empty state, host passthrough, and fallback; disclosure assertion pins the comma-joined ":ssn, :token" form.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 1: render_job_field(:job_args) overlay in runtime_config.ex</name>
  <files>lib/oban_powertools/runtime_config.ex</files>
  <read_first>
    - lib/oban_powertools/runtime_config.ex (render_job_field/3 lines 206–215; :job_recorded clause 202–204 — add :job_args clause BEFORE generic; read_key/2 lines 337–345; preserve the rescue fallback)
    - .planning/phases/56-redact-at-rest/56-RESEARCH.md (Code Example 3; Pitfall 6; Open Questions OQ3 RESOLVED)
    - .planning/phases/56-redact-at-rest/56-PATTERNS.md (runtime_config.ex section lines 319–373 — tagged return shape + rescue fallback is load-bearing)
    - .planning/phases/56-redact-at-rest/56-UI-SPEC.md (Copy rules — "Redacted at enqueue"; never Hidden/Masked/Encrypted; [redacted] fallback unchanged)
  </read_first>
  <behavior>
    - render_job_field(:job_args, args, %{job: job}) with meta __redacted_fields__ == ["ssn"] and host policy returning nil → returns {:raw_json, json} where json contains "ssn": "Redacted at enqueue"
    - render_job_field(:job_args, args, ctx) with empty/absent __redacted_fields__ → behaves exactly as today (no overlay)
    - render_job_field(:job_args, args, ctx) where host policy returns a custom map → returned as-is, NO overlay (host responsibility)
    - render_job_field that raises → {:fallback, "[redacted]"} (unchanged)
  </behavior>
  <action>
    Add a new `def render_job_field(:job_args, value, context)` clause ABOVE the generic `render_job_field(kind, value, context)` clause (mirroring the :job_recorded clause placement at 202–204). Extract redacted fields via a private `get_redacted_fields/1` matching `%{job: %Oban.Job{meta: meta}}` → `Map.get(meta || %{}, "__redacted_fields__", [])`, falling back to `[]` for any other context shape (RESEARCH Example 3).

    When redacted_fields == [], delegate to the existing default behavior (call the generic path / same case-on-apply_policy logic). When non-empty: run `apply_policy(:job_args, value, context)`; if it returns nil (default path), build an annotated map via private `build_redacted_args_map/2` that merges `%{field => "Redacted at enqueue"}` (string keys, D-17/D-14) over the args map, then `{:raw_json, Jason.encode!(annotated, pretty: true)}`. If the host policy returns a custom binary or custom map, pass it through unchanged — do NOT overlay (OQ3 RESOLVED). Preserve the `rescue _ -> {:fallback, "[redacted]"}` on the new clause (PATTERNS.md: load-bearing; do not change the "[redacted]" string). Never use Hidden/Masked/Encrypted copy.
  </action>
  <verify>
    <automated>mix test test/oban_powertools/web/live/jobs_live_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - The render_job_field REDACT-04 unit tests from Task 0 pass
    - `grep -n "Redacted at enqueue" lib/oban_powertools/runtime_config.ex` returns a match
    - `grep -v '^#' lib/oban_powertools/runtime_config.ex | grep -c "Hidden\|Masked\|Encrypted"` == 0 in the new clause context (no forbidden copy)
    - The {:fallback, "[redacted]"} rescue is present on the :job_args clause
    - Host-policy-returns-custom-map test passes (no overlay applied)
    - `mix compile --warnings-as-errors` exits 0
  </acceptance_criteria>
  <done>render_job_field(:job_args) overlays "Redacted at enqueue" only on the default path, respects host policy custom returns, preserves [redacted] fallback.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Redaction disclosure block + assigns in jobs_live.ex</name>
  <files>lib/oban_powertools/web/jobs_live.ex</files>
  <read_first>
    - lib/oban_powertools/web/jobs_live.ex (load_job_detail/2 assign chain lines 723–738; nil branch 717–721; assign_defaults/1 780–791; Meta card </div> line 388 + recorded-output panel start line 391; dt label color line 354)
    - .planning/phases/56-redact-at-rest/56-PATTERNS.md (jobs_live.ex section lines 377–438 — exact insertion point + heex block + color guidance)
    - .planning/phases/56-redact-at-rest/56-UI-SPEC.md (Visual hierarchy: disclosure near Meta card, no new top-level card; neutral colors; read-only; Copywriting Contract)
  </read_first>
  <behavior>
    - Job detail with meta __redacted_fields__ == ["ssn","token"] renders a card "Fields redacted at enqueue: :ssn, :token" near the Meta card (comma-joined atom-presentation form)
    - Job detail with no __redacted_fields__ renders no disclosure block
    - No new destructive/retry/reveal/edit action introduced
  </behavior>
  <action>
    In load_job_detail/2 (lines 723–738), after the recorded_output assign, compute `redacted_fields = get_in(job.meta || %{}, ["__redacted_fields__"]) || []` and add `|> assign(:redacted_fields, redacted_fields)`. Add `|> assign(:redacted_fields, [])` to the nil/not-found branch (717–721) and to assign_defaults/1 (780–791) — PATTERNS.md: every new assign in load_job_detail must also appear in assign_defaults.

    In the render template, insert a conditional disclosure block between the Meta card `</div>` (line 388) and the recorded-output panel (line 391), per UI-SPEC D-13 "near Meta card, no new top-level card": `<%= if @redacted_fields != [] do %>` a single bordered card (`rounded-lg border bg-white p-4`) containing a label-styled paragraph (`text-xs font-semibold text-zinc-500`) reading `Fields redacted at enqueue:` followed by `<%= Enum.map(@redacted_fields, &":#{&1}") |> Enum.join(", ") %>` (produces the locked `:ssn, :token` comma-joined form). Wrap long lists naturally (no horizontal page scroll). Neutral colors only — text-zinc-500, no red (UI-SPEC Color section). Read-only: add no actions.
  </action>
  <verify>
    <automated>mix test test/oban_powertools/web/live/jobs_live_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - The REDACT-03 LiveView disclosure + empty-state tests from Task 0 pass
    - `grep -n "Fields redacted at enqueue" lib/oban_powertools/web/jobs_live.ex` returns a match
    - `grep -c "redacted_fields" lib/oban_powertools/web/jobs_live.ex` shows assigns in load_job_detail, nil branch, AND assign_defaults (>= 3 occurrences)
    - `grep -v '^#' lib/oban_powertools/web/jobs_live.ex | grep -c "text-red\|bg-red"` shows no red applied to the disclosure block
    - mix test test/oban_powertools/web/live/jobs_live_test.exs exits 0
    - `mix compile --warnings-as-errors` exits 0
  </acceptance_criteria>
  <done>Disclosure card renders near the Meta card from __redacted_fields__ in the comma-joined :ssn, :token form, hidden when absent, neutral/read-only per UI-SPEC; LiveView tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| stored job meta/args (JSONB) → operator browser (LiveView HTML) | Display of redacted-field state must not re-leak any redacted value; it shows only field NAMES, never values. |
| host DisplayPolicy code → render_job_field/3 | A raising/invalid host policy must not expose raw args as a recovery path. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-56-11 | Information Disclosure | redaction disclosure re-leaking values | mitigate | Disclosure renders only field NAMES from __redacted_fields__ (D-13/D-17, string list); values are key-absent in args by Plan 56-01; Task 0 tests assert only names appear |
| T-56-12 | Information Disclosure | host policy raising → raw args exposed | mitigate | Preserve `rescue _ -> {:fallback, "[redacted]"}` on the :job_args clause (D-14, UI-SPEC); Task 0 fallback test asserts "[redacted]" |
| T-56-13 | Tampering / UX | overlay clobbering a host's custom :job_args policy | accept | Host owns custom returns; overlay only on default (nil) path (OQ3 RESOLVED); documented; Task 0 passthrough test guards |
| T-56-SC | Tampering | npm/pip/cargo installs | accept | No new packages this phase; no shadcn/registry code (UI-SPEC Registry Safety: none) |
</threat_model>

<verification>
- `mix test test/oban_powertools/web/live/jobs_live_test.exs` exits 0
- `mix compile --warnings-as-errors` exits 0
- /ops/jobs detail renders "Fields redacted at enqueue: :ssn, :token" for a job with __redacted_fields__
- render_job_field(:job_args) shows "Redacted at enqueue" per listed field on the default path
- No __redacted_fields__ → no disclosure (honest empty state)
- [redacted] fallback intact; host custom policy not overridden
- No red / destructive styling on redaction surfaces (UI-SPEC)
</verification>

<success_criteria>
- ROADMAP success criteria 3 & 4 satisfied
- 56-UI-SPEC.md copy, color, placement, and read-only contract honored exactly
- Disclosure shows field names only — no redacted values re-leak
</success_criteria>

<output>
Create `.planning/phases/56-redact-at-rest/56-03-SUMMARY.md` when done
</output>
