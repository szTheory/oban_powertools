---
phase: 56-redact-at-rest
plan: 04
type: execute
wave: 2
depends_on: ["56-01"]
files_modified:
  - guides/workers-and-idempotency.md
  - test/oban_powertools/docs_contract_test.exs
autonomous: true
requirements: [REDACT-01]  # D-11 documentation complement only — NOT a behavioral coverage slice. REDACT-01 behavior is fully delivered by 56-01 (engine) and 56-02 (cron path). This plan locks the support-truth copy so it cannot silently regress.
must_haves:
  truths:
    - "The workers guide documents the redact: option with a declaration example"
    - "The guide states redact: removes fields from args at enqueue and does NOT scrub recorded outputs"
    - "The guide states workers must not return redacted/sensitive data from process/1"
    - "A docs-contract test locks the redact: support-truth copy so it cannot silently regress"
  artifacts:
    - path: "guides/workers-and-idempotency.md"
      provides: "At-rest argument redaction support-truth section"
      contains: "At-rest argument redaction"
    - path: "test/oban_powertools/docs_contract_test.exs"
      provides: "redact: support-truth lock test"
      contains: "does NOT scrub recorded outputs"
  key_links:
    - from: "guides/workers-and-idempotency.md"
      to: "test/oban_powertools/docs_contract_test.exs"
      via: "assert source =~ exact D-11 boundary sentences"
      pattern: "does NOT scrub recorded outputs"
---

<objective>
Document the `redact:` support truth (D-10, D-11) and lock it with a docs-contract test. The guide must state the at-rest redaction boundary honestly: `redact:` removes fields from args at enqueue; it does NOT scrub recorded outputs; workers must not return redacted/sensitive data from `process/1`. This is the documentation half of the "recording integration" ROADMAP obligation.

> Traceability note: this plan is documentation-only (D-11 support-truth). Its `requirements: [REDACT-01]` association is the **D-11 documentation complement** to REDACT-01 — NOT a behavioral coverage slice. The redaction behavior of REDACT-01 is fully delivered by 56-01 (the engine: override, drop, meta, guards) and 56-02 (cron-path coverage). 56-04 only documents and locks the support-truth boundary copy.

Purpose: Prevent the false assurance that JobRecord output or meta/errors are scrubbed (D-10 keeps JobRecord.redacted honestly false).
Output: New "At-rest argument redaction" guide section + a docs-contract lock test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/56-redact-at-rest/56-CONTEXT.md
@.planning/phases/56-redact-at-rest/56-PATTERNS.md

<interfaces>
<!-- Contracts the executor needs — extracted from codebase. -->

guides/workers-and-idempotency.md structure (verified):
- "## Output recording" section at line 148 (model section: 1-line description → use ObanPowertools.Worker code block → bulleted boundary notes)
- "## Timeout and deadline safety" at line 118; "## Validation without enqueue" at line 194 — place new section after Output recording, before Validation without enqueue

docs_contract_test.exs (verified):
- @worker_guide_path = "guides/workers-and-idempotency.md" (line 234)
- Existing lock-test pattern (lines 277–289): `source = File.read!(@worker_guide_path)` then `assert source =~ "<exact phrase>"` for each support-truth phrase

D-11 REQUIRED exact sentence (LOCKED):
"`redact:` removes fields from args at enqueue; it does NOT scrub recorded outputs. Workers must not return redacted/sensitive data from `process/1`."
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 0: Wave 0 — failing docs-contract lock test for redact: support truth</name>
  <files>test/oban_powertools/docs_contract_test.exs</files>
  <read_first>
    - test/oban_powertools/docs_contract_test.exs (worker guide lock-test pattern lines 277–289; @worker_guide_path line 234)
    - .planning/phases/56-redact-at-rest/56-CONTEXT.md (D-10, D-11 — exact support-truth wording)
  </read_first>
  <action>
    Add a new test "redact: support truth stays locked in builder docs" mirroring the existing worker-lifecycle lock test (lines 277–289). Read `@worker_guide_path` and assert the guide source contains each LOCKED phrase:
    - "At-rest argument redaction" (section heading)
    - "redact:" (option name)
    - "does NOT scrub recorded outputs"
    - "Workers must not return redacted/sensitive data from"
    - "removes fields from args at enqueue"

    This MUST fail now (the guide section does not exist yet) — RED state. No watch flags.
  </action>
  <verify>
    <automated>mix test test/oban_powertools/docs_contract_test.exs 2>&1 | tail -15 # expect new redact test to fail — RED</automated>
  </verify>
  <acceptance_criteria>
    - docs_contract_test.exs contains a test asserting the five redact: support-truth phrases against @worker_guide_path
    - The new test currently fails (guide section absent) — RED
  </acceptance_criteria>
  <done>Failing docs-contract lock test for redact: support truth committed.</done>
</task>

<task type="auto">
  <name>Task 1: Add "At-rest argument redaction" section to the workers guide</name>
  <files>guides/workers-and-idempotency.md</files>
  <read_first>
    - guides/workers-and-idempotency.md ("## Output recording" section lines 148–193 — the section-shape analog: 1-line description, use ObanPowertools.Worker example, bulleted boundary notes)
    - .planning/phases/56-redact-at-rest/56-CONTEXT.md (D-02 key-absent, D-03 fingerprint-first, D-08 top-level keys, D-10 JobRecord untouched, D-11 exact sentence)
    - .planning/phases/56-redact-at-rest/56-PATTERNS.md (guide section lines 441–468)
  </read_first>
  <action>
    Add a new `## At-rest argument redaction` section after "## Output recording" (ends ~line 193) and before "## Validation without enqueue" (line 194), following the Output recording section shape. Include:
    - One-line description: declaring `redact: [:ssn, :token]` drops those keys from `oban_jobs.args` at enqueue.
    - A `use ObanPowertools.Worker` code block showing `args: [user_id: :integer, ssn: :string], redact: [:ssn]`.
    - Bulleted boundary notes capturing the LOCKED support truths: fields are removed (key-absent, not nilled — D-02); the idempotency fingerprint is computed from the full unredacted args BEFORE redaction (D-03); top-level declared arg keys only in v1.7 (D-08); redacted typed fields are auto-exempt from validate_required so jobs run/retry cleanly (D-06); a typo'd redact key or a redact key that is also a partition_by key fails at compile time (D-07/D-09); the field is also dropped on the cron-scheduled path (D-05).
    - The EXACT D-11 boundary sentence verbatim: "`redact:` removes fields from args at enqueue; it does NOT scrub recorded outputs. Workers must not return redacted/sensitive data from `process/1`."
    - State that meta/errors/stacktraces are NOT scrubbed (operational evidence stays intact) and JobRecord.redacted remains its honest false default (D-10) — do not imply payloads are cleaned.

    Match the guide's existing voice and markdown conventions. Ensure all five phrases the Task 0 test asserts appear verbatim.
  </action>
  <verify>
    <automated>mix test test/oban_powertools/docs_contract_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - mix test test/oban_powertools/docs_contract_test.exs exits 0 (Task 0 RED test now GREEN)
    - `grep -c "At-rest argument redaction" guides/workers-and-idempotency.md` >= 1
    - `grep -c "does NOT scrub recorded outputs" guides/workers-and-idempotency.md` >= 1
    - The guide does NOT claim meta/errors or recorded output are scrubbed (no "scrubs the output"/"masks errors" assertions)
    - `grep -c "Hidden\|Masked\|Encrypted" guides/workers-and-idempotency.md` does not introduce redaction-as-encryption framing in the new section
  </acceptance_criteria>
  <done>Guide documents redact: with the locked support-truth boundary; docs-contract test green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| documentation → adopter mental model | Misleading docs that imply recorded outputs / meta / errors are scrubbed create a false-assurance leak vector (adopter stops scrubbing their own payloads). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-56-14 | Information Disclosure (false assurance) | guide overstating redaction scope | mitigate | D-11 exact boundary sentence + D-10 "JobRecord.redacted stays false" note; docs-contract lock test (Task 0) prevents silent regression |
| T-56-SC | Tampering | npm/pip/cargo installs | accept | No new packages; docs-only change |
</threat_model>

<verification>
- `mix test test/oban_powertools/docs_contract_test.exs` exits 0
- Guide contains the "At-rest argument redaction" section with the D-11 verbatim boundary sentence
- Guide does not claim outputs/meta/errors are scrubbed
</verification>

<success_criteria>
- D-11 documented verbatim and locked by a contract test
- D-10 honest-boundary framing present (no false payload-cleanliness assurance)
- Support-truth core value upheld
</success_criteria>

<output>
Create `.planning/phases/56-redact-at-rest/56-04-SUMMARY.md` when done
</output>
