---
phase: 56-redact-at-rest
plan: 02
type: execute
wave: 2
depends_on: ["56-01"]
files_modified:
  - lib/oban_powertools/cron.ex
  - test/oban_powertools/cron_test.exs
autonomous: true
requirements: [REDACT-01, REDACT-02]
must_haves:
  truths:
    - "A cron-scheduled job for a redact: worker has the redacted field absent from oban_jobs.args"
    - "A cron-scheduled job for a redact: worker has __redacted_fields__ in meta"
    - "A cron-scheduled job for a plain Oban.Worker still enqueues via bare Oban.Job.new (unchanged behavior)"
  artifacts:
    - path: "lib/oban_powertools/cron.ex"
      provides: "Powertools-aware cron enqueue routing through entry.worker.new/2"
      contains: "function_exported?"
    - path: "test/oban_powertools/cron_test.exs"
      provides: "Cron-path redaction coverage"
      contains: "redact"
  key_links:
    - from: "lib/oban_powertools/cron.ex maybe_insert_job/4"
      to: "entry.worker.new/2 override"
      via: "function_exported?(worker_module, :__powertools_limits__, 0) sentinel routing"
      pattern: "function_exported\\?.*__powertools_limits__"
---

<objective>
Close the cron-path PII bypass (D-05). `cron.ex:422` currently builds jobs with bare `Oban.Job.new(args, worker: ..., queue: ...)`, skipping `worker_mod.new/2` entirely — so without this fix a cron-scheduled redact-worker would persist PII un-redacted. Route scheduled enqueue through `entry.worker.new/2` for ObanPowertools workers, falling back to bare `Oban.Job.new/2` for plain Oban.Worker cron entries (REDACT-01, REDACT-02 on the scheduled path).

Purpose: Genuine universal redaction coverage — not a blessed-path illusion.
Output: Modified `cron.ex`, cron-path redaction tests in `cron_test.exs`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/56-redact-at-rest/56-CONTEXT.md
@.planning/phases/56-redact-at-rest/56-RESEARCH.md
@.planning/phases/56-redact-at-rest/56-PATTERNS.md

<interfaces>
<!-- Contracts the executor needs — extracted from codebase. -->

cron.ex current bypass (verified):
- maybe_insert_job/4 at lines 417–422; line 422: `repo.insert(Oban.Job.new(args, worker: entry.worker, queue: String.to_atom(entry.queue)))`
- entry.worker is a STRING module name (e.g. "MyApp.MyWorker")
- entry.queue is a STRING

Sentinel (from Plan 56-01 / existing):
- `function_exported?(worker_module, :__powertools_limits__, 0)` is true for every ObanPowertools.Worker (always generated, even with limits: []) — worker.ex:82. Use this, NOT __powertools_redact__/0 (which only exists when redact: is declared). Verified worker.ex:240 uses this exact sentinel.

new/2 override (from Plan 56-01):
- `worker_module.new(args, queue: ...)` returns a changeset with redaction applied + __redacted_fields__ meta injected.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 0: Wave 0 — failing cron-path redaction test</name>
  <files>test/oban_powertools/cron_test.exs</files>
  <read_first>
    - test/oban_powertools/cron_test.exs (existing structure; file already uses `use ObanPowertools.DataCase, async: false` per PATTERNS.md line 687 — no setup change needed; find how cron entries are configured/triggered and how inserted jobs are queried)
    - lib/oban_powertools/cron.ex (maybe_insert_job/4 lines 417–422; the run/insert entry path around line 85)
    - .planning/phases/56-redact-at-rest/56-PATTERNS.md (cron.ex section lines 290–315)
    - .planning/phases/56-redact-at-rest/56-RESEARCH.md (Pattern 5; Pitfall 5)
  </read_first>
  <action>
    Add a cron redaction describe block to cron_test.exs. Define a cron-targeted `redact: [:ssn]` worker (`args: [user_id: :integer, ssn: :string]`) and a plain `Oban.Worker` control worker.

    Following the file's existing pattern for configuring a cron entry and invoking the cron run path that reaches maybe_insert_job, add tests:
    1. REDACT-01/02 cron coverage (RED now): schedule/enqueue the redact worker via the cron path with args containing ssn; query the inserted oban_jobs row; assert `refute Map.has_key?(inserted.args, "ssn")` and `inserted.meta["__redacted_fields__"] == ["ssn"]`.
    2. Plain-worker regression guard: schedule the plain Oban.Worker via cron; assert it still inserts successfully with its args intact (no crash, no redaction applied).

    Match the file's existing convention for triggering cron inserts (reuse the same helper/setup other cron_test cases use). Do not use watch flags.
  </action>
  <verify>
    <automated>mix test test/oban_powertools/cron_test.exs 2>&1 | tail -20 # expect redaction test failures — RED</automated>
  </verify>
  <acceptance_criteria>
    - cron_test.exs contains a redaction describe/test referencing REDACT-01/02 on the cron path
    - The redact-worker cron test currently fails (ssn present in args / __redacted_fields__ absent) — RED state proving the bypass exists pre-fix
    - A plain Oban.Worker cron test exists asserting unchanged insert behavior
  </acceptance_criteria>
  <done>Failing cron redaction test + plain-worker regression test committed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 1: Route cron enqueue through entry.worker.new/2 for Powertools workers</name>
  <files>lib/oban_powertools/cron.ex</files>
  <read_first>
    - lib/oban_powertools/cron.ex (maybe_insert_job/4 lines 417–422 — the exact clause to modify; surrounding clauses 417–419 for skipped/follow-up/duplicate decisions must stay untouched)
    - lib/oban_powertools/worker.ex (function_exported? sentinel usage at line 240 — copy this detection form)
    - .planning/phases/56-redact-at-rest/56-RESEARCH.md (Pattern 5 code example; Open Question 2 — String.to_existing_atom safety + rescue fallback; Assumptions A1, A2)
    - .planning/phases/56-redact-at-rest/56-PATTERNS.md (cron.ex section lines 290–315 — rescue/fallback degradation guidance)
  </read_first>
  <behavior>
    - Cron entry whose worker is an ObanPowertools.Worker → job built via worker_module.new(args, queue: ...) → redacted fields absent, __redacted_fields__ in meta
    - Cron entry whose worker is a plain Oban.Worker → job built via bare Oban.Job.new(args, worker: ..., queue: ...) → unchanged behavior
    - Unloadable/unknown worker module string → degrades to bare Oban.Job.new (rescue), never crashes the cron run
  </behavior>
  <action>
    Modify only the final `maybe_insert_job(repo, entry, args, _decision)` clause (cron.ex:421–422). Resolve the worker module atom from `entry.worker` (a string) via `String.to_existing_atom/1` (entry.worker already includes the "Elixir." prefix or build it per the existing codebase convention — confirm by reading how entry.worker is used at cron.ex:345). Detect Powertools workers with `function_exported?(worker_module, :__powertools_limits__, 0)` (RESEARCH Assumption A1 — the always-generated sentinel; do NOT use __powertools_redact__/0). When true, build the changeset via `worker_module.new(args, queue: String.to_atom(entry.queue))` so redaction + meta injection fire; otherwise keep the existing `Oban.Job.new(args, worker: entry.worker, queue: String.to_atom(entry.queue))`. Then `repo.insert(changeset)`.

    Wrap the atom resolution + routing in a `rescue ArgumentError -> ` fallback that builds the bare `Oban.Job.new(...)` changeset, so an unloaded/removed worker module degrades gracefully rather than crashing the cron run (RESEARCH Open Question 2 / Assumption A2). Do NOT inject __redacted_fields__ here — the override owns it (D-04). Leave the skipped/follow-up/duplicate clauses (lines 417–419) untouched.
  </action>
  <verify>
    <automated>mix test test/oban_powertools/cron_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - mix test test/oban_powertools/cron_test.exs exits 0 (Task 0 RED tests now GREEN)
    - `grep -n "function_exported?" lib/oban_powertools/cron.ex` returns a match in maybe_insert_job
    - `grep -n "rescue" lib/oban_powertools/cron.ex` shows the fallback in the insert path
    - A cron-enqueued redact worker's stored oban_jobs row has no "ssn" key in args and __redacted_fields__ in meta
    - Plain Oban.Worker cron entry still inserts via bare Oban.Job.new (regression test green)
    - `mix compile --warnings-as-errors` exits 0
  </acceptance_criteria>
  <done>Cron path routes Powertools workers through new/2 (redaction inherited), plain workers unchanged, unloadable modules degrade safely; cron_test green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cron scheduler → oban_jobs.args (JSONB) | Scheduled enqueue is a second path where sensitive args reach durable storage; previously bypassed the redaction override. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-56-08 | Information Disclosure / Security Feature Bypass | cron.ex:422 bare Oban.Job.new | mitigate | Route Powertools workers through entry.worker.new/2 (D-05); Task 0 cron integration test asserts ssn absent + meta present on scheduled jobs |
| T-56-09 | Denial of Service | unloaded/removed cron worker module crashes the cron run | mitigate | rescue ArgumentError → bare Oban.Job.new fallback (RESEARCH OQ2/A2); resilience, not a leak — plain fallback applies no redaction config because plain workers have none |
| T-56-10 | Tampering | misrouting a plain Oban.Worker through a non-existent new/2 | mitigate | function_exported?(mod, :__powertools_limits__, 0) sentinel gates routing (A1); plain-worker regression test guards |
| T-56-SC | Tampering | npm/pip/cargo installs | accept | No new packages this phase |
</threat_model>

<verification>
- `mix test test/oban_powertools/cron_test.exs` exits 0
- `mix compile --warnings-as-errors` exits 0
- Cron-scheduled redact worker: stored args lack the redacted key; meta has __redacted_fields__
- Cron-scheduled plain worker: unchanged insert, args intact
</verification>

<success_criteria>
- ROADMAP success criteria 1 & 2 hold on the cron-scheduled path, not just direct/transaction enqueue
- No double meta injection (cron does not inject __redacted_fields__; new/2 does)
- Plain Oban.Worker cron behavior is byte-for-byte unchanged
</success_criteria>

<output>
Create `.planning/phases/56-redact-at-rest/56-02-SUMMARY.md` when done
</output>
