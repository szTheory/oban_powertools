---
phase: 67-stability-and-static-analysis-sweep
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: 
  - "lib/mix/tasks/oban_powertools.install.ex"
  - "test/support/migrations/"
  - "examples/"
autonomous: true
requirements: ["R-01"]

must_haves:
  truths:
    - "High-volume tables use concurrent index creation in their setup migrations"
    - "Migrations adding concurrent indexes specify @disable_ddl_transaction true"
    - "Existing test and example migrations reflect the concurrent index changes"
  artifacts:
    - path: "lib/mix/tasks/oban_powertools.install.ex"
      provides: "Igniter task generation for Ecto migrations"
  key_links: []
---

<objective>
Update the Igniter Ecto migration generator and all existing checked-in migrations to ensure safe, concurrent index creation for potentially large tables.

Purpose: To guarantee index safety on high-volume tables (e.g. `_job_records`, `_workflows`, `_batches`, `_callbacks`) for both new installs and upgrades in production Postgres environments.
Output: Safely configured Ecto migrations with `@disable_ddl_transaction true` and `concurrently: true` on index creations.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@lib/mix/tasks/oban_powertools.install.ex
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update Igniter migration generator for large tables</name>
  <files>lib/mix/tasks/oban_powertools.install.ex</files>
  <action>
    Locate the `Igniter.Libs.Ecto.gen_migration` blocks for potentially large tables: `oban_powertools_job_records`, `oban_powertools_workflows`, `oban_powertools_workflow_steps`, `oban_powertools_batches_and_callbacks` (which alters `_callbacks` and creates `_batches`), `oban_powertools_limiter_history_facts`, and `oban_powertools_archive_runs`.
    For each of these targeted migrations:
    1. Add `@disable_ddl_transaction true` inside the migration module body (before `def change do`).
    2. Add `concurrently: true` to every `create index(...)` and `create unique_index(...)` call within those specific migrations.
    
    Ensure the syntax matches standard Ecto `create index` options: `create index(:table, [:column], concurrently: true)`.
  </action>
  <verify>
    <automated>mix compile --warnings-as-errors</automated>
  </verify>
  <done>The `install.ex` task generates migrations with `@disable_ddl_transaction true` and `concurrently: true` for the targeted large tables.</done>
</task>

<task type="auto">
  <name>Task 2: Backport concurrent indexes to checked-in test/example migrations</name>
  <files>test/support/migrations/, examples/phoenix_host/priv/repo/migrations/, examples/phoenix_host_upgrade_source/priv/repo/migrations/</files>
  <action>
    Find the existing `.exs` migration files for the same large tables targeted in Task 1 across `test/support/migrations/`, `examples/phoenix_host/priv/repo/migrations/`, and `examples/phoenix_host_upgrade_source/priv/repo/migrations/`.
    For each matched file:
    1. Add `@disable_ddl_transaction true` below the `use Ecto.Migration` line.
    2. Update its `create index` and `create unique_index` lines to include `concurrently: true`.
    
    This ensures our tests and fixture apps reflect the exact behavior the generator will produce for new users.
  </action>
  <verify>
    <automated>MIX_ENV=test mix ecto.create || true && MIX_ENV=test mix ecto.migrate</automated>
  </verify>
  <done>All checked-in migrations for high-volume tables now disable DDL transactions and build indexes concurrently without syntax errors.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DB Migration -> Production DB | DDL execution must not lock active operational tables. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-67-01   | Denial of Service | Ecto Migrations | mitigate | Use concurrent index creation to prevent table locks from blocking other DB consumers on large datasets. |
</threat_model>

<verification>
- Verify `mix test` passes to ensure the migrations still execute cleanly.
- Verify `lib/mix/tasks/oban_powertools.install.ex` has `concurrently: true` for `_job_records` and `_workflows`.
</verification>

<success_criteria>
High-volume Ecto migrations are now completely lock-safe for production index building.
</success_criteria>

<output>
Create `.planning/phases/67-stability-and-static-analysis-sweep/67-01-SUMMARY.md` when done
</output>
