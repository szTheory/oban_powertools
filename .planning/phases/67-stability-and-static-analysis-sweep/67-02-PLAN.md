---
phase: 67-stability-and-static-analysis-sweep
plan: 02
type: execute
wave: 2
depends_on: ["67-01"]
files_modified: 
  - "mix.exs"
  - ".credo.exs"
  - "lib/"
  - "test/"
autonomous: true
requirements: ["R-01"]

must_haves:
  truths:
    - "Credo runs strictly with zero warnings"
    - "Dialyzer runs with zero type errors"
  artifacts:
    - path: "mix.exs"
      provides: "credo and dialyxir dependencies"
    - path: ".credo.exs"
      provides: "Credo strict configuration"
  key_links: []
---

<objective>
Configure and execute a strict static analysis sweep across the codebase using Credo and Dialyzer to guarantee 0 warnings and 0 errors.

Purpose: To solidify the public contract and ensure codebase hygiene prior to the 1.0 release.
Output: Lint-free, type-safe Elixir code with updated CI/CD or local test tools.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@mix.exs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Setup Static Analysis Dependencies</name>
  <files>mix.exs, .credo.exs</files>
  <action>
    1. Add `{:credo, "~> 1.7", only: [:dev, :test], runtime: false}` to `mix.exs` dependencies.
    2. Add `{:dialyxir, "~> 1.4", only: [:dev, :test], runtime: false}` to `mix.exs` dependencies.
    3. Configure `dialyzer` under `project/0` in `mix.exs` with standard PLT paths (e.g., `plt_add_apps: [:mix, :ex_unit]`, `plt_file: {:no_warn, "priv/plts/dialyzer.plt"}`).
    4. Run `mix deps.get`.
    5. Run `mix credo.gen.config` to generate `.credo.exs`.
  </action>
  <verify>
    <automated>mix deps.get && mix compile</automated>
  </verify>
  <done>Dependencies installed and Credo config exists.</done>
</task>

<task type="auto">
  <name>Task 2: Credo Zero-Warning Pass</name>
  <files>lib/, test/</files>
  <action>
    Run `mix credo --strict`. 
    Resolve all warnings. If a specific warning is purely stylistic and resolving it would degrade readability or conflict with established codebase patterns, you may disable that specific rule in `.credo.exs` or ignore it via `@dialyzer` / inline credo comments, but fixing the code is preferred.
    Pay special attention to module documentation and complexity checks.
  </action>
  <verify>
    <automated>mix credo --strict</automated>
  </verify>
  <done>Credo returns 0 warnings.</done>
</task>

<task type="auto">
  <name>Task 3: Dialyzer Zero-Error Pass</name>
  <files>lib/</files>
  <action>
    Run `mix dialyzer`. 
    Analyze any type mismatches or missing specs that Dialyzer highlights.
    Add or fix `@spec`s across the `lib/` directory. Ensure all public functions of the library API surface are correctly typed for 1.0.
  </action>
  <verify>
    <automated>mix dialyzer</automated>
  </verify>
  <done>Dialyzer returns 0 errors.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Static Analysis -> Source Code | Linting tooling runs in the development environment only. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-67-02   | Tampering | mix.exs / npm | mitigate | slopcheck legitimacy check on new dependencies. Credo and Dialyxir are standard community packages. |
</threat_model>

<verification>
- `mix credo --strict` exits 0.
- `mix dialyzer` exits 0.
</verification>

<success_criteria>
The library has robust typespecs and 0 linter warnings, proving maturity for 1.0.
</success_criteria>

<output>
Create `.planning/phases/67-stability-and-static-analysis-sweep/67-02-SUMMARY.md` when done
</output>
