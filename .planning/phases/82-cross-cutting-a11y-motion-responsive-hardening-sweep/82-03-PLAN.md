---
phase: 82-cross-cutting-a11y-motion-responsive-hardening-sweep
plan: "03"
type: execute
wave: 2
depends_on:
  - "82-01"
files_modified:
  - test/browser/support/verify-phase82-quality.mjs
  - test/browser/support/verify-phase82-quality.test.mjs
autonomous: true
requirements:
  - A11Y-01
  - A11Y-04
  - MOTION-01
  - MOTION-02
  - NAV-02
  - DATA-03
  - COPY-01
  - COPY-02
must_haves:
  truths:
    - "One early Node module is the sole Phase 82 owner for static quality, motion-source inventory, exception, route/inventory, artifact, and bypass policy."
    - "Every declared production motion root and exact exclusion is mutation-tested before token, asset, component, or page repair begins."
    - "The validator derives schema-8 and 163/99 inventory facts from generated inputs and never introduces a handwritten target list."
    - "Diagnostics are bounded to path, line, rule, and counts and never contain fixture, preview, reason, provider, or payload data."
  artifacts:
    - path: test/browser/support/verify-phase82-quality.mjs
      provides: "Sole exported Node policy implementation and CLI for Phase 82"
    - path: test/browser/support/verify-phase82-quality.test.mjs
      provides: "Green mutation-backed proof for every static and bypass rule"
  key_links:
    - from: test/browser/support/verify-phase82-quality.test.mjs
      to: test/browser/support/verify-phase82-quality.mjs
      via: "all RED mutations from Plan 82-01 become green against one owner"
      pattern: "verify-phase82-quality"
    - from: test/browser/support/verify-phase82-quality.mjs
      to: test/browser/.generated/showcase-manifest.json
      via: "generated manifest is the only target/page inventory"
      pattern: "showcase-manifest"
---

<objective>
Implement the sole Node owner for Phase 82 quality and motion policy immediately
after RED, before any token, asset, shared-component, or page repair.

Purpose: prevent a later CSS/ExUnit source walker or CI-only validator from
becoming a competing policy implementation.
Output: one exported mutation-tested Node validator and bounded CLI report.
</objective>

<execution_context>
@/Users/jon/.agents/gsd-core/workflows/execute-plan.md
@/Users/jon/.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/82-cross-cutting-a11y-motion-responsive-hardening-sweep/82-CONTEXT.md
@.planning/phases/82-cross-cutting-a11y-motion-responsive-hardening-sweep/82-RESEARCH.md
@.planning/phases/82-cross-cutting-a11y-motion-responsive-hardening-sweep/82-UI-SPEC.md
@.planning/phases/82-cross-cutting-a11y-motion-responsive-hardening-sweep/82-PATTERNS.md
@.planning/phases/82-cross-cutting-a11y-motion-responsive-hardening-sweep/82-01-SUMMARY.md
@test/browser/support/manifest-smoke.mjs
@test/browser/support/verify-page-script-order.mjs
</context>

<tasks>

The worktree is dirty. Re-read both assigned files and their current diffs
before editing, preserve unrelated hunks, and stage only these exact paths.

<task type="auto" tdd="true">
  <name>Task 1: Implement the single exported static-quality policy</name>
  <files>test/browser/support/verify-phase82-quality.mjs, test/browser/support/verify-phase82-quality.test.mjs</files>
  <behavior>
    - Reject raw duration/easing, unknown keyframes, unscoped motion, absent OS or root reduction, and source/package drift.
    - Visit production `.ex`/`.heex` components/pages, client `.js`/`.ts`, source CSS, and packaged CSS; reject missing, broad, stale, or unused exclusions.
    - Reject missing/extra/duplicate/hard-coded target inventories, route-family drift, stale exceptions, unsafe reports, and Phase 82 graph bypass mutations.
    - Every mutation proves it changed the intended structured node or production root before requiring an exact path:line failure.
  </behavior>
  <action>
Implement the public pure validator exports and CLI required by Plan 82-01.
Use parsed/structured inputs and exact declared roots rather than broad grep.
Derive schema 8 and 163/99 from the generated manifest and validate, but do not
duplicate, copy policy metadata. Emit a compact deterministic redacted report.
This module is the only Node owner: later token/assets, package, and CI plans
must call these exports/CLI and must not implement a parallel source walker,
exception registry, motion parser, or target inventory.
  </action>
  <verify>
    <automated>npm run showcase:manifest &amp;&amp; node --test test/browser/support/verify-phase82-quality.test.mjs &amp;&amp; node test/browser/support/verify-phase82-quality.mjs</automated>
  </verify>
  <done>All Plan 82-01 Node mutations are green against one exported owner and the CLI emits only bounded deterministic diagnostics.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Repository sources → static quality decision | Every production root must be visited with exact exclusions. |
| Generated manifest → inventory policy | Generated schema-8 data is authoritative; handwritten target sets are forbidden. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-82-03-BYPASS | Tampering | validator inputs | critical | mitigate | Mutation-test missing roots, exclusions, inventory entries, and graph edges. |
| T-82-03-LEAK | Information disclosure | diagnostics/report | high | mitigate | Allow only bounded path, line, rule, and count fields; reject sensitive sentinels. |
| T-82-03-DUAL | Repudiation | policy ownership | high | mitigate | Export one module; downstream plans consume it and explicitly forbid parallel walkers. |
</threat_model>

<verification>
Node self-tests and CLI pass after manifest generation; `git diff --check`
passes; no token, asset, component, page, package, or CI file is modified.
</verification>

<success_criteria>
Phase 82 has exactly one early mutation-backed Node policy owner before repair
work begins.
</success_criteria>

<output>
Create `82-03-SUMMARY.md` when complete.
</output>
