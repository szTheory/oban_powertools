---
phase: 82-cross-cutting-a11y-motion-responsive-hardening-sweep
plan: "04"
type: execute
wave: 2
depends_on:
  - "82-01"
files_modified:
  - test/browser/support/system-quality.ts
  - test/browser/support/axe.ts
  - test/browser/specs/system-quality-contract.spec.ts
autonomous: true
requirements:
  - A11Y-01
  - A11Y-02
  - A11Y-03
  - A11Y-04
  - MOTION-02
  - NAV-02
  - DATA-03
must_haves:
  truths:
    - "One shared bounded browser module owns contrast, targets, focus, reflow, one-tree, zoom, motion, and rendered-copy mechanics."
    - "Axe blocks critical, serious, and moderate by default; only exact, expiring, used exceptions with compensating assertions pass."
    - "All media, root attributes, CDP metrics, theme, and overlay state are restored in finally blocks."
    - "The shared theme auditor proves computed-role equivalence for system under OS light, OS dark, and prefers-contrast-more."
  artifacts:
    - path: test/browser/support/system-quality.ts
      provides: "Reusable standards-accurate browser auditors with structured diagnostics"
    - path: test/browser/support/axe.ts
      provides: "WCAG-tagged fail-closed impact and exception policy"
    - path: test/browser/specs/system-quality-contract.spec.ts
      provides: "Green unit/browser proof for every algorithm"
  key_links:
    - from: test/browser/support/system-quality.ts
      to: test/browser/specs/showcase.a11y.spec.ts
      via: "auditSystemQuality import consumed later by Plan 82-08"
      pattern: "auditSystemQuality"
    - from: test/browser/support/axe.ts
      to: test/browser/specs/system-quality.spec.ts
      via: "same policy applies to connected routes"
      pattern: "assertAxePolicy"
---

<objective>
Implement and prove the shared Phase 82 browser auditors and strict axe policy.

Purpose: Turn D-05..D-13 and D-19 into one reusable implementation instead of
page-local focus, motion, target, or reflow systems.
Output: A bounded TypeScript support module, strict axe policy, and green Wave
0 browser contracts.
</objective>

<execution_context>
@/Users/jon/.agents/gsd-core/workflows/execute-plan.md
@/Users/jon/.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/82-cross-cutting-a11y-motion-responsive-hardening-sweep/82-CONTEXT.md
@.planning/phases/82-cross-cutting-a11y-motion-responsive-hardening-sweep/82-RESEARCH.md
@.planning/phases/82-cross-cutting-a11y-motion-responsive-hardening-sweep/82-UI-SPEC.md
@.planning/phases/82-cross-cutting-a11y-motion-responsive-hardening-sweep/82-PATTERNS.md
@.planning/phases/82-cross-cutting-a11y-motion-responsive-hardening-sweep/82-01-SUMMARY.md
@test/browser/support/axe.ts
@test/browser/specs/page-migration-wave-1.spec.ts
@test/browser/specs/page-migration-wave-2.spec.ts
@test/browser/specs/page-migration-wave-3.spec.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement exact geometry, color, reflow, and motion auditors</name>
  <files>test/browser/support/system-quality.ts, test/browser/specs/system-quality-contract.spec.ts</files>
  <behavior>
    - Exact unrounded sRGB/alpha contrast and adjacency thresholds pass/fail at the researched boundaries.
    - Target spacing circles, explicit exceptions, overlap, and comfort classes produce deterministic reports.
    - Focus active-element, indicator thickness/contrast, viewport intersection, hit testing, and occluders are all enforced.
    - Overflow is permitted only for registered labelled focusable bounded machine regions; one-tree and zoom restoration are exact.
    - OS and root reduced-motion paths are independent and preserve visible/operable/announced state.
    - System/OS light computed roles equal explicit light; system/OS dark equals explicit dark; system with prefers-contrast-more equals explicit high-contrast under both OS schemes.
  </behavior>
  <action>
Implement small exported pure helpers plus Playwright wrappers in
`system-quality.ts`. Follow the exact algorithms in 82-RESEARCH: do not round
contrast, do not infer target exceptions from tags, fail unknown color
adjacency, sample deterministic inset hit-test points, inspect root/body/
document/page overflow, and restore CDP/media/root state in `finally`.
Separate the normative 24px result from the 44px comfort result. Bound DOM
walks and diagnostics. Keep auditors observational per D-20: they may not
activate arbitrary mutations or retain fixture/browser-channel data. Add one
exported system-theme runtime helper that snapshots the current color scheme,
contrast preference, root theme/motion attributes, and finite computed
role/property vector; runs light/no-preference, dark/no-preference, and
contrast-more under both color schemes; compares each vector to the matching
explicit light/dark/high-contrast vector; and restores all state in `finally`.
  </action>
  <verify>
    <automated>npx playwright test test/browser/specs/system-quality-contract.spec.ts --project=chromium-wide</automated>
  </verify>
  <done>Every Wave 0 browser case is green and failures carry exact target, selector, mechanism, measurement, and threshold context.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Make axe findings and exceptions fail closed</name>
  <files>test/browser/support/axe.ts, test/browser/specs/system-quality-contract.spec.ts</files>
  <behavior>
    - Existing WCAG tag set remains exact.
    - Critical and serious always block; moderate blocks by default.
    - A moderate exception needs exact rule and target/route/story scope, standards rationale, owner, expiry, and compensating assertion.
    - Unknown, broad, expired, duplicate, unmatched, or unused exceptions fail.
    - Diagnostics report every affected selector in a bounded structured artifact.
  </behavior>
  <action>
Replace the two-impact helper with a finite policy evaluator while preserving
`runAxeForTarget` and redacted artifact writing. Export a route-capable runner
or selector option used by Plan 82-08. Do not truncate evidence to the first
five selectors; bound total diagnostic size while retaining each affected
node's selector. Keep minor results recorded but non-blocking. Add contract
cases that mutation-prove stale and unused exception rejection per D-05.
  </action>
  <verify>
    <automated>npx playwright test test/browser/specs/system-quality-contract.spec.ts --project=chromium-wide</automated>
  </verify>
  <done>The full contract suite passes with the unchanged WCAG 2.2 tag set and fail-closed moderate policy.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Rendered DOM/computed style → quality report | Hidden, covered, ambiguous, or hostile content must not spoof a pass or exhaust diagnostics. |
| Axe results → exception registry | Findings must not be laundered through stale or broad waivers. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-82-03-GEOMETRY | Spoofing | system-quality.ts | high | mitigate | Require visibility, activeElement, rect, hit testing, overlap, and exact computed values. |
| T-82-03-WAIVER | Tampering | axe.ts | high | mitigate | Closed exact expiring registry with used/unused accounting and compensating assertion IDs. |
| T-82-03-REPORT-DOS | Denial of service | diagnostics | high | mitigate | Bound DOM nodes, sample points, strings, and structured report arrays. |
| T-82-03-LEAK | Information disclosure | artifacts | high | mitigate | Emit selectors/measurements only; never DOM dumps, credentials, reasons, or payloads. |
</threat_model>

<verification>
Wave 0 is green with no retries/sleeps. TypeScript parses through Playwright
and `git diff --check` passes.
</verification>

<success_criteria>
Every later browser suite can import one strict implementation for system
quality and axe policy without local substitutes.
</success_criteria>

<output>
Create `82-04-SUMMARY.md` when complete.
</output>
