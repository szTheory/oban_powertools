---
phase: 82-cross-cutting-a11y-motion-responsive-hardening-sweep
plan: "10"
type: execute
wave: 10
depends_on:
  - "82-03"
  - "82-08"
  - "82-09"
files_modified:
  - test/browser/support/verify-page-script-order.mjs
  - voiceover.config.ts
  - package.json
  - .github/workflows/ci.yml
autonomous: true
requirements:
  - A11Y-01
  - A11Y-04
  - MOTION-01
  - MOTION-02
  - NAV-02
  - DATA-03
  - COPY-01
  - COPY-02
must_haves:
  truths:
    - "Plan 82-03 remains the sole Node quality/motion policy owner; this plan only invokes it in package and CI graphs."
    - "Phase 82 validators, exact connected spec, page acceptance, exhaustive axe/mechanical quality, and compare-only VRT are direct ordered required members."
    - "Omission, reorder, filtering, duplication, retries, update/watch/UI, ignored failure, detached jobs, and missing gate edges fail."
    - "Both page_quality and full unfiltered visual_a11y remain direct ci-gate dependencies."
    - "All VoiceOver and browser evidence paths enforce numeric retries=0."
  artifacts:
    - path: test/browser/support/verify-phase82-quality.mjs
      provides: "Consumed unchanged as the sole early Node policy owner"
    - path: test/browser/support/verify-page-script-order.mjs
      provides: "Mutation-tested required graph order/ownership validator"
    - path: package.json
      provides: "Manifest-first required scripts invoking the Phase 82 owner"
    - path: .github/workflows/ci.yml
      provides: "Merge-blocking page_quality and visual_a11y ownership"
    - path: voiceover.config.ts
      provides: "Canonical zero-retry real-VoiceOver configuration"
  key_links:
    - from: package.json
      to: test/browser/support/verify-phase82-quality.mjs
      via: "manifest-first validator prefix consumes the sole owner"
      pattern: "verify-phase82-quality"
    - from: .github/workflows/ci.yml
      to: package.json
      via: "direct unfiltered verify:pages and visual:a11y steps"
      pattern: "verify:pages"
---

<objective>
Make the complete Phase 82 graph direct, ordered, mutation-tested, and
merge-blocking while consuming the Plan 82-03 policy unchanged.
</objective>

<execution_context>
@/Users/jon/.agents/gsd-core/workflows/execute-plan.md
@/Users/jon/.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/82-cross-cutting-a11y-motion-responsive-hardening-sweep/82-CONTEXT.md
@.planning/phases/82-cross-cutting-a11y-motion-responsive-hardening-sweep/82-03-SUMMARY.md
@.planning/phases/82-cross-cutting-a11y-motion-responsive-hardening-sweep/82-08-SUMMARY.md
@.planning/phases/82-cross-cutting-a11y-motion-responsive-hardening-sweep/82-09-SUMMARY.md
@package.json
@test/browser/support/verify-page-script-order.mjs
@.github/workflows/ci.yml
</context>

<tasks>

Re-read dirty package/CI/config files before each edit, preserve unrelated
hunks, and stage exact task paths only. Never modify
`verify-phase82-quality.mjs` or its tests in this plan.

<task type="auto" tdd="true">
  <name>Task 1: Wire strict manifest-first package order</name>
  <files>package.json, voiceover.config.ts, test/browser/support/verify-page-script-order.mjs</files>
  <behavior>
    - Host/Docker scripts invoke the existing Phase 82 validator before Wave 1, Wave 2, Phase 81 fixture, Wave 3, system sweep, page acceptance, exhaustive axe/mechanical quality, and compare-only VRT.
    - system-quality.spec.ts occurs exactly once and PAGE_QUALITY_ONLY remains exact.
    - Filter/update/watch/UI/retry/ignored/duplicate/reorder mutations fail.
    - VoiceOver config and command resolve retries to numeric zero.
  </behavior>
  <action>
Wire the Plan 82-03 CLI into the validator prefix and exact browser order.
Update only the structural graph validator and its mutations. Do not duplicate
the Node source walker, policy, exceptions, report, target inventory, or tests.
Preserve full unfiltered `visual:a11y` and fixture order.
  </action>
  <verify>
    <automated>npm run showcase:manifest &amp;&amp; node --test test/browser/support/verify-phase82-quality.test.mjs &amp;&amp; node test/browser/support/verify-phase82-quality.mjs &amp;&amp; node test/browser/support/verify-page-script-order.mjs &amp;&amp; npx playwright test test/browser/specs/system-quality.spec.ts --list &amp;&amp; npx playwright test --config=voiceover.config.ts --list</automated>
  </verify>
  <done>Package/config order is exact and the sole Node owner remains green and unchanged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Keep both required CI lanes directly merge-blocking</name>
  <files>.github/workflows/ci.yml, test/browser/support/verify-page-script-order.mjs</files>
  <behavior>
    - page_quality runs exactly one unfiltered verify:pages command and uploads bounded Phase 82/manifest/Playwright artifacts on failure.
    - visual_a11y remains full and unfiltered; ci-gate directly requires both jobs.
    - continue-on-error, detached ownership, missing edges, retry inheritance, and nonzero retry configs fail.
  </behavior>
  <action>
Expose the existing bounded Phase 82 report while preserving serial fixture
execution. Extend graph mutations for direct ownership, edges, and zero retry.
Never weaken Phase 73/81 lanes or expose fixture/operator-sensitive data.
  </action>
  <verify>
    <automated>node test/browser/support/verify-page-script-order.mjs &amp;&amp; node test/browser/support/verify-phase82-quality.mjs &amp;&amp; actionlint .github/workflows/ci.yml</automated>
  </verify>
  <done>Both quality jobs remain directly required and every graph bypass mutation fails.</done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-82-10-BYPASS | Tampering | package/CI graph | critical | mitigate | Exact parser and mutations for every known bypass and gate edge. |
| T-82-10-DUAL | Repudiation | Node policy | high | mitigate | Consume Plan 82-03 unchanged; forbid a second walker or registry. |
| T-82-10-LEAK | Information disclosure | CI artifacts | high | mitigate | Upload bounded structured evidence only. |
</threat_model>

<verification>
Sole Node validator/self-tests, script-order validator, actionlint, discovery,
and `git diff --check` pass.
</verification>

<success_criteria>
Every Phase 82 check is directly merge-blocking with one Node policy owner.
</success_criteria>

<output>
Create `82-10-SUMMARY.md` when complete.
</output>
