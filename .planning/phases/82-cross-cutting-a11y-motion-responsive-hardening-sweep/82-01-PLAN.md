---
phase: 82-cross-cutting-a11y-motion-responsive-hardening-sweep
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - test/browser/specs/system-quality-contract.spec.ts
  - test/browser/support/verify-phase82-quality.test.mjs
autonomous: true
requirements:
  - A11Y-01
  - A11Y-02
  - A11Y-03
  - A11Y-04
  - MOTION-01
  - MOTION-02
  - NAV-02
  - DATA-03
must_haves:
  truths:
    - "Wave 0 contains deterministic RED contracts for every mechanical WCAG, motion, reflow, inventory, and CI-bypass algorithm before implementation."
    - "The tests distinguish the normative 24×24-or-spacing rule from the separate 44px operator comfort rule."
    - "Unknown colors, stale exceptions, missing reduction paths, duplicate inventory, and detached CI work fail closed."
    - "System theme is contract-tested under OS light, OS dark, and prefers-contrast-more, with computed-role equivalence and exact restoration."
    - "Motion inventory fails closed across production HEEx/Elixir components/pages, client JavaScript, source CSS, and packaged CSS."
  artifacts:
    - path: test/browser/specs/system-quality-contract.spec.ts
      provides: "Small browser fixtures for contrast, target, focus, reflow, zoom, motion, and axe policy"
    - path: test/browser/support/verify-phase82-quality.test.mjs
      provides: "Pure mutation fixtures for inventory, exception, static CSS, and CI bypass rejection"
  key_links:
    - from: test/browser/specs/system-quality-contract.spec.ts
      to: test/browser/support/system-quality.ts
      via: "future imports define the implementation contract"
      pattern: "system-quality"
    - from: test/browser/support/verify-phase82-quality.test.mjs
      to: test/browser/support/verify-phase82-quality.mjs
      via: "future validator exports exercised by mutations"
      pattern: "verify-phase82-quality"
---

<objective>
Create the Wave 0 RED contract for Phase 82's browser algorithms and fail-closed
quality-policy validator.

Purpose: Make standards accuracy and CI/inventory integrity executable before
any repair, per D-03, D-05..D-13, D-18, D-21, and D-22.
Output: Focused Playwright fixtures and Node mutation tests that fail only
because the Phase 82 support seams do not yet exist.
</objective>

<execution_context>
@/Users/jon/.agents/gsd-core/workflows/execute-plan.md
@/Users/jon/.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/82-cross-cutting-a11y-motion-responsive-hardening-sweep/82-CONTEXT.md
@.planning/phases/82-cross-cutting-a11y-motion-responsive-hardening-sweep/82-RESEARCH.md
@.planning/phases/82-cross-cutting-a11y-motion-responsive-hardening-sweep/82-UI-SPEC.md
@.planning/phases/82-cross-cutting-a11y-motion-responsive-hardening-sweep/82-PATTERNS.md
@test/browser/support/axe.ts
@test/browser/support/manifest.ts
@test/browser/support/verify-page-script-order.mjs
</context>

<tasks>

Wave 0 files may already have milestone-closure hunks. Re-read each assigned
file and its current diff immediately before editing; preserve unrelated
changes. Run each RED discovery separately and stage only that task's exact
paths/hunks. Never use broad staging or overwrite from a stale read.

<task type="auto" tdd="true">
  <name>Task 1: Write RED browser algorithm fixtures</name>
  <files>test/browser/specs/system-quality-contract.spec.ts</files>
  <behavior>
    - Contrast uses unrounded sRGB luminance, alpha composition, 4.5:1 normal, 3:1 large/non-text, 4.5:1 muted, 7:1 high-contrast, and fails unknown gradients.
    - Target geometry implements 24×24-or-spacing with exact reviewed exceptions, detects overlaps, and separately enforces 44px in one axis for named comfort controls.
    - Focus requires active connected focus, a 2px indicator with 3:1 contrast, viewport/hit-test visibility, sticky/top-layer occlusion detection, and restored fallback.
    - Reflow checks root, body, document, page, bounded labelled machine scrollers, one tree, and CDP metric restoration.
    - Motion proves both OS and root reduction independently, while content, action, focus, progress, and announcements remain available.
    - System theme runs an exact media matrix: OS light/no-preference equals explicit light computed roles; OS dark/no-preference equals explicit dark; prefers-contrast-more equals explicit high-contrast for both OS color schemes. Each case snapshots and restores color scheme, contrast, root theme/motion attributes, and computed roles in `finally`.
    - Axe blocks critical, serious, and moderate by default; only exact, expiring, used, compensating exceptions can pass.
  </behavior>
  <action>
Build small `page.setContent` fixtures and pure input tables that exercise the
exact algorithms in 82-RESEARCH. Import the future public helpers from
`system-quality.ts` and the future axe policy exports. Keep every test bounded,
deterministic, and independent of the connected host. Include explicit
diagnostic assertions naming selector, threshold, measured value, mechanism,
exception decision, OS media mode, and expected/actual computed role. The
system-theme fixture must compare a finite role/property vector, not theme
labels or attributes. Do not implement the helpers in this plan and do not
weaken failures to discovery-only checks. This is D-05..D-13's RED seam.
  </action>
  <verify>
    <automated>npx playwright test test/browser/specs/system-quality-contract.spec.ts --project=chromium-wide</automated>
  </verify>
  <done>The suite is syntactically discoverable and fails for the missing Phase 82 exports, with all researched edge cases represented.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write RED validator and bypass mutation fixtures</name>
  <files>test/browser/support/verify-phase82-quality.test.mjs</files>
  <behavior>
    - Reject missing, extra, duplicate, renamed, or hard-coded target inventories and route-family drift while preserving schema 8 and 163/99.
    - Reject raw timings/easing, unknown keyframes, unscoped motion, absent media/root reduction, stale/unused exceptions, and source/package drift.
    - Reject a declaration added to production `.heex`/Elixir components or pages, client `.js`/`.ts`, source CSS, or packaged CSS; exact generated/vendor/test-fixture exclusions must be used and broad/missing/stale exclusions fail with path:line diagnostics.
    - Reject omitted, reordered, filtered, duplicated, update/watch/UI, ignored-failure, detached-job, and missing-gate-edge CI mutations.
    - Every mutation first proves it changed the source and then proves validation fails.
  </behavior>
  <action>
Create pure Node mutation fixtures against the future exported validator
functions. Use exact structured inputs rather than broad grep-only assertions.
Keep the generated manifest as the only showcase inventory per D-01 and make
the separate route map prove exact equality with manifest-derived page
families. Cover D-03, D-18, D-21, and D-22 bypasses. Do not write the validator
implementation or edit package/CI files. Mutation-test every declared motion
scan root and exclusion: first prove the mutation lands in the intended
production root, then require a diagnostic containing its exact path and line.
  </action>
  <verify>
    <automated>node --test test/browser/support/verify-phase82-quality.test.mjs</automated>
  </verify>
  <done>Node discovers all mutation cases and they fail because the validator exports are absent, not because fixtures are malformed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Test fixture → quality algorithm | Adversarial geometry, colors, policy, and exception inputs must not produce false passes. |
| Repository config → required CI graph | Modified scripts/YAML must not detach or filter required evidence. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-82-01-FALSE-GREEN | Tampering | RED fixtures | high | mitigate | Mutation-paired positive/negative cases and exact diagnostics block permissive implementations. |
| T-82-01-DOS | Denial of service | DOM fixtures | medium | mitigate | Bound nodes, points, and diagnostics; use tiny synthetic documents. |
| T-82-01-CI-BYPASS | Repudiation | CI mutation fixtures | high | mitigate | Exercise every omission/filter/update/detachment path before CI wiring. |
</threat_model>

<verification>
Both files are discoverable; failures identify only missing Phase 82
implementation seams. `git diff --check` passes.
</verification>

<success_criteria>
Wave 0 mechanically specifies the standards and bypass policy without adding
product behavior, dependencies, stories, or passing implementation.
</success_criteria>

<output>
Create `82-01-SUMMARY.md` when complete.
</output>
