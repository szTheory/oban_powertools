---
phase: 28-diagnosis-first-overview-context-preserving-drilldowns
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/oban_powertools/web/overview_read_model.ex
  - lib/oban_powertools/web/engine_overview_live.ex
  - lib/oban_powertools/web/control_plane_presenter.ex
  - test/oban_powertools/web/live/engine_overview_live_test.exs
autonomous: true
requirements:
  - OVR-01
  - OVR-02
must_haves:
  truths:
    - "The `/ops/jobs` landing page stops being a metric wall and becomes a diagnosis-first triage surface built from one coherent read-model contract."
    - "Every overview card answers status, diagnosis, next step, venue, and evidence without inventing native parity for bridge-only follow-up."
    - "Resolved continuity remains secondary and honest about source/window semantics."
  artifacts:
    - path: "lib/oban_powertools/web/overview_read_model.ex"
      provides: "Bounded bucket queries and a stable card contract containing count, diagnosis, exemplars, ownership, venue, and next-step metadata"
    - path: "lib/oban_powertools/web/engine_overview_live.ex"
      provides: "Diagnosis-first overview rendering wired to the shared read model instead of local metric-card math"
    - path: "test/oban_powertools/web/live/engine_overview_live_test.exs"
      provides: "Hermetic proof for bucket labels, diagnosis copy, venue wording, exemplars, and CTA posture"
  key_links:
    - from: "lib/oban_powertools/web/overview_read_model.ex"
      to: ".planning/phases/28-diagnosis-first-overview-context-preserving-drilldowns/28-CONTEXT.md"
      via: "the read model must emit the exact bucket set and layered `status -> diagnosis -> next action -> venue -> evidence -> audit` posture"
      pattern: "Needs Review|Blocked|Waiting|Runnable|Resolved Recently|Bridge-only Follow-up"
    - from: "lib/oban_powertools/web/engine_overview_live.ex"
      to: ".planning/phases/28-diagnosis-first-overview-context-preserving-drilldowns/28-UI-SPEC.md"
      via: "the LiveView must render triage-first cards with bridge/native labels and a quieter resolved block"
      pattern: "Review Needs Review|Oban Web bridge|Inspection only|Resolved Recently"
---

<objective>
Create the shared overview read model and replace the count-heavy `/ops/jobs` surface with diagnosis-first triage cards.

Purpose: make the overview itself answer what needs attention, why it matters, and where the operator should go next before Phase 28 starts tightening drilldown continuity.
Output: one bounded overview read-model seam, a rewritten overview LiveView, and a new overview proof file.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/28-diagnosis-first-overview-context-preserving-drilldowns/28-CONTEXT.md
@.planning/phases/28-diagnosis-first-overview-context-preserving-drilldowns/28-RESEARCH.md
@.planning/phases/28-diagnosis-first-overview-context-preserving-drilldowns/28-PATTERNS.md
@.planning/phases/28-diagnosis-first-overview-context-preserving-drilldowns/28-VALIDATION.md
@.planning/phases/28-diagnosis-first-overview-context-preserving-drilldowns/28-UI-SPEC.md
@lib/oban_powertools/web/engine_overview_live.ex
@lib/oban_powertools/web/control_plane_presenter.ex
@lib/oban_powertools/control_plane.ex
@lib/oban_powertools/web/workflows_live.ex
@lib/oban_powertools/web/lifeline_live.ex
@lib/oban_powertools/web/limiters_live.ex
@lib/oban_powertools/web/cron_live.ex
@lib/oban_powertools/web/audit_live.ex
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the bounded overview read-model seam and freeze bucket semantics</name>
  <files>lib/oban_powertools/web/overview_read_model.ex, lib/oban_powertools/web/control_plane_presenter.ex</files>
  <read_first>.planning/phases/28-diagnosis-first-overview-context-preserving-drilldowns/28-CONTEXT.md, .planning/phases/28-diagnosis-first-overview-context-preserving-drilldowns/28-RESEARCH.md, .planning/phases/28-diagnosis-first-overview-context-preserving-drilldowns/28-UI-SPEC.md, lib/oban_powertools/web/control_plane_presenter.ex, lib/oban_powertools/control_plane.ex, lib/oban_powertools/web/workflows_live.ex, lib/oban_powertools/web/lifeline_live.ex, lib/oban_powertools/web/limiters_live.ex, lib/oban_powertools/web/cron_live.ex, lib/oban_powertools/web/audit_live.ex</read_first>
  <action>Create `lib/oban_powertools/web/overview_read_model.ex` as the single Phase 28 source for overview buckets. Define a stable bucket contract with these exact visible statuses: `Needs Review`, `Blocked`, `Waiting`, `Runnable`, `Resolved Recently`, and `Bridge-only Follow-up`. For each bucket return concrete fields for `status`, `count`, `diagnosis`, `ownership`, `venue`, `next_step_label`, `next_step_path`, and one to three deterministic exemplar rows with durable fact strings. Use existing durable seams instead of HEEx-local logic: Lifeline incident and archive truth for `Needs Review` and `Resolved Recently`; limiter blocker/cooldown truth for `Blocked` and `Waiting`; workflow and bridge-oriented generic-job follow-up for `Bridge-only Follow-up`; and bounded healthy/runnable signals for `Runnable`. Extend `ControlPlanePresenter` only as needed to render shared venue, ownership, and posture labels such as `Powertools-native`, `Oban Web bridge`, `Audited action`, and `Inspection only`. Do not emit preview tokens, mutable form state, or fake bridge-only native deep links from the read model.</action>
  <acceptance_criteria>
    - `lib/oban_powertools/web/overview_read_model.ex` exists and contains the exact bucket labels `Needs Review`, `Blocked`, `Waiting`, `Runnable`, `Resolved Recently`, and `Bridge-only Follow-up`.
    - The read model exposes deterministic card metadata including diagnosis, next-step label, and next-step path rather than raw counts alone.
    - Bridge-only metadata includes `Oban Web bridge` and `Inspection only`.
    - No query-param contract in the new module includes preview tokens, draft reasons, or other action-execution internals.
  </acceptance_criteria>
  <verify>
    <automated>bash -lc 'test -f lib/oban_powertools/web/overview_read_model.ex && rg -n "Needs Review|Blocked|Waiting|Runnable|Resolved Recently|Bridge-only Follow-up|Oban Web bridge|Inspection only" lib/oban_powertools/web/overview_read_model.ex lib/oban_powertools/web/control_plane_presenter.ex && ! rg -n "preview_token|reason=" lib/oban_powertools/web/overview_read_model.ex'</automated>
  </verify>
  <done>The repo has one bounded overview contract that later plans can reuse without rebuilding bucket semantics in templates.</done>
</task>

<task type="auto">
  <name>Task 2: Rebuild `EngineOverviewLive` around diagnosis-first cards and add overview proof</name>
  <files>lib/oban_powertools/web/engine_overview_live.ex, test/oban_powertools/web/live/engine_overview_live_test.exs</files>
  <read_first>.planning/phases/28-diagnosis-first-overview-context-preserving-drilldowns/28-CONTEXT.md, .planning/phases/28-diagnosis-first-overview-context-preserving-drilldowns/28-RESEARCH.md, .planning/phases/28-diagnosis-first-overview-context-preserving-drilldowns/28-UI-SPEC.md, lib/oban_powertools/web/engine_overview_live.ex, lib/oban_powertools/web/overview_read_model.ex, test/oban_powertools/web/live/workflows_live_test.exs, test/oban_powertools/web/live/lifeline_live_test.exs, test/oban_powertools/web/live/limiters_live_test.exs</read_first>
  <action>Replace the current overview metric wall in `lib/oban_powertools/web/engine_overview_live.ex` with a triage-first composition backed by `OverviewReadModel`. Render one orientation header, one primary triage region for `Needs Review`, `Blocked`, `Waiting`, `Runnable`, and `Bridge-only Follow-up`, and one quieter secondary `Resolved Recently` block. Each active card must visibly include the status label, subordinate count, one factual diagnosis sentence, one to three exemplar rows, and a venue-aware CTA such as `Review Needs Review`. Bridge-only cards must show `Oban Web bridge` and `Inspection only` before click. Create `test/oban_powertools/web/live/engine_overview_live_test.exs` and assert the page renders diagnosis-first cards, bounded exemplars, bridge/native posture labels, and honest resolved continuity wording instead of only feature counts. Do not introduce overview mutations, generic job tables, or a new component library.</action>
  <acceptance_criteria>
    - `test/oban_powertools/web/live/engine_overview_live_test.exs` exists.
    - The rendered overview includes diagnosis text and next-step CTA text, not only numeric counters.
    - Bridge-only cards show `Oban Web bridge` and `Inspection only` before navigation.
    - The resolved continuity region is rendered separately from the primary active triage cards.
    - `mix test test/oban_powertools/web/live/engine_overview_live_test.exs` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>bash -lc 'test -f test/oban_powertools/web/live/engine_overview_live_test.exs && mix test test/oban_powertools/web/live/engine_overview_live_test.exs'</automated>
    <automated>bash -lc 'rg -n "Review Needs Review|Oban Web bridge|Inspection only|Resolved Recently|Bridge-only Follow-up" lib/oban_powertools/web/engine_overview_live.ex test/oban_powertools/web/live/engine_overview_live_test.exs'</automated>
  </verify>
  <done>The operator landing page becomes a real triage entrypoint with executable next-step guidance and repo-local proof.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| durable engine and audit truth -> overview diagnosis cards | The overview must summarize without inventing unsupported semantics or hiding venue ownership. |
| native control plane -> bridge-only follow-up | The overview may route to bridge inspection, but it must keep that venue visibly distinct from native audited-action surfaces. |
| resolved continuity evidence -> active triage urgency | Resolved history should reassure operators without competing visually or semantically with active attention buckets. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-01 | T | overview bucket contract | mitigate | Build one bounded read-model seam that owns count, diagnosis, venue, and exemplar selection. |
| T-28-02 | I | bridge/native ownership wording | mitigate | Require `Oban Web bridge` and `Inspection only` copy on bridge-only cards before navigation. |
| T-28-03 | R | overview support truth | mitigate | Prove the diagnosis-first overview in a dedicated LiveView test file rather than relying on manual visual review alone. |
</threat_model>

<verification>
- `bash -lc 'test -f lib/oban_powertools/web/overview_read_model.ex && rg -n "Needs Review|Blocked|Waiting|Runnable|Resolved Recently|Bridge-only Follow-up|Oban Web bridge|Inspection only" lib/oban_powertools/web/overview_read_model.ex lib/oban_powertools/web/control_plane_presenter.ex && ! rg -n "preview_token|reason=" lib/oban_powertools/web/overview_read_model.ex'`
- `bash -lc 'test -f test/oban_powertools/web/live/engine_overview_live_test.exs && mix test test/oban_powertools/web/live/engine_overview_live_test.exs'`
- `bash -lc 'rg -n "Review Needs Review|Oban Web bridge|Inspection only|Resolved Recently|Bridge-only Follow-up" lib/oban_powertools/web/engine_overview_live.ex test/oban_powertools/web/live/engine_overview_live_test.exs'`
</verification>

<success_criteria>
- `/ops/jobs` surfaces durable attention buckets and venue-aware next steps instead of a metric-first wall.
- One overview read-model contract owns diagnosis, exemplars, and CTA metadata.
- Bridge-only follow-up remains explicit and support-truthful on the overview itself.
</success_criteria>

<output>
After completion, create `.planning/phases/28-diagnosis-first-overview-context-preserving-drilldowns/28-01-SUMMARY.md`
</output>
